// Copyright 2026 RegaliumOS™.
// SPDX-License-Identifier: Apache-2.0
//
// Generated by sync from Google's Digital Buildings ontology,
// revision 2026-09-09 (4a794ac).
// Portions Copyright 2020 Google LLC, licensed under Apache-2.0.
// DO NOT EDIT.
//
// Regenerate with: just sync
// Revision history: docs/spec.md

syntax = "proto3";

package protobuf.digitalbuildings.lighting.luminaire_group.v1;

import "buf/validate/validate.proto";
import "google/api/field_behavior.proto";
import "google/api/field_info.proto";
import "google/api/resource.proto";
import "protobuf/digitalbuildings/annotations/v1/annotations.proto";
import "protobuf/digitalbuildings/lighting/luminaire_group/v1/absent_present_value.proto";
import "protobuf/digitalbuildings/lighting/luminaire_group/v1/active_inactive_value.proto";
import "protobuf/digitalbuildings/lighting/luminaire_group/v1/auto_constant_light_control_dimming_scene_control_value.proto";
import "protobuf/digitalbuildings/lighting/luminaire_group/v1/bypass_occupied_standby_unknown_unoccupied_value.proto";
import "protobuf/digitalbuildings/lighting/luminaire_group/v1/connection.proto";
import "protobuf/digitalbuildings/lighting/luminaire_group/v1/field_translation.proto";
import "protobuf/digitalbuildings/lighting/luminaire_group/v1/inactive_occupied_unknown_unoccupied_value.proto";
import "protobuf/digitalbuildings/lighting/luminaire_group/v1/link.proto";
import "protobuf/digitalbuildings/lighting/luminaire_group/v1/luminaire_group_type.proto";
import "protobuf/digitalbuildings/lighting/luminaire_group/v1/off_on_standby_unknown_value.proto";
import "protobuf/digitalbuildings/lighting/luminaire_group/v1/off_on_value.proto";

option java_multiple_files = true;
option java_outer_classname = "LuminaireGroupProto";
option java_package = "com.regalium.protobuf.digitalbuildings.lighting.luminaire_group.v1";

// A group containing one or more luminaires.
//
// Ontology entity type `LIGHTING/LGRP`, a general type carrying 26 canonical
// variant(s). Rule 7: the general type is the resource and each variant is a
// value of LuminaireGroupType, so this message holds the union of every
// field any variant uses -- all of them optional.
//
// Reference: Digital Buildings abstract model.
// https://github.com/google/digitalbuildings/blob/master/ontology/docs/model.md
message LuminaireGroup {
  option (google.api.resource) = {
    type: "digitalbuildings.googleapis.com/LuminaireGroup"
    pattern: "buildings/{building}/luminaireGroups/{luminaire_group}"
    singular: "luminaireGroup"
    plural: "luminaireGroups"
  };
  option (protobuf.digitalbuildings.annotations.v1.entity_type) = {
    guid: "7b2723ef-361a-40ff-8399-4a907a52c357"
    namespace: "LIGHTING"
    general_type: "LGRP"
  };

  // Resource name, "buildings/{building}/luminaireGroups/{luminaire_group}".
  // Assigned by the server.
  string name = 1 [
    (google.api.field_behavior) = IDENTIFIER,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The ontology's GUID for this entity.
  //
  // Rule 10: this is the ontology's own identifier, not a second one minted
  // beside it. Digital Buildings assigns a stable UUID to every entity and
  // it survives renames, which is exactly what AIP-148 asks a `uid` to be.
  string uid = 2 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (google.api.field_info).format = UUID4,
    (buf.validate.field).string.uuid = true,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The building config's human-readable entity code, e.g. `FCU-123`.
  //
  // Locally unique rather than globally, chosen at design time, and it must
  // survive a round trip unchanged or every re-import duplicates the entity.
  // The old building-config format keys entities by this; the new one keys
  // them by `uid`. Both are representable because both exist.
  string code = 3 [
    (google.api.field_behavior) = OPTIONAL,
    (google.api.field_behavior) = IMMUTABLE,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The canonical type this entity conforms to.
  //
  // The variant's exact field sets ride on the enum value in
  // (annotations.canonical_type), so a validator can reject an entity
  // reporting a field its declared type does not have.
  LuminaireGroupType entity_type = 4 [(google.api.field_behavior) = OPTIONAL];

  // Directed relationships to other entities.
  //
  // Rule 4: a connection is not a field. AIP-215 forbids referencing a
  // message in another proto package, and the other end of a connection may
  // be any resource in any namespace -- so it is carried as a resource name
  // with a wildcard reference.
  repeated Connection connections = 5 [(google.api.field_behavior) = OPTIONAL];

  // Standard fields mapped from another entity's telemetry.
  //
  // The building config's `links` block: a virtual entity reports data
  // linked from reporting devices rather than emitting it itself.
  repeated Link links = 6 [(google.api.field_behavior) = OPTIONAL];

  // How this entity's native payload maps onto the standard fields, when it
  // reports telemetry directly.
  repeated FieldTranslation translations = 7 [(google.api.field_behavior) = OPTIONAL];

  // Held for the fields on every read path (rule 9).
  reserved 8 to 15;

  // The aisle brightness percentage command.
  //
  // Composed of:
  //   aisle (descriptor): Passage between two rows of server racks.
  //   brightness (measurement_descriptor): Giving out or reflecting light;
  //     overall level of light in a space.
  //   percentage (measurement): Measurement per hundred.
  //   command (point_type): The signal given to make an action happen.
  //     Defaults to multistate unless used alongside a measurement_type.
  //
  // Ontology field: `aisle_brightness_percentage_command`, point type
  // `command`. Measured as percentage; values are in percent, the ontology's
  // standard unit for that quantity.
  //
  // Valid range 0 to 100, enforced by protovalidate.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double aisle_brightness_percentage_command = 16 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "aisle_brightness_percentage_command"
      subfields: "aisle"
      subfields: "brightness"
      subfields: "percentage"
      subfields: "command"
      point_type: "command"
      measurement: "percentage"
      standard_unit: "percent"
    },
    (buf.validate.field).double.gte = 0.0,
    (buf.validate.field).double.lte = 100.0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The blue scene dimmer percentage command.
  //
  // Composed of:
  //   blue (descriptor): Blue light fracture of ambient light.
  //   scene (descriptor): The holistic ambience achieved from coordinated
  //     control of an area's lighting levels and colors. One of many stored
  //     setpoints that may be recalled to apply predetermined lighting
  //     levels across a number of fixtures.
  //   dimmer (component): A device which can reduce lighting output of a
  //     lamp (thus 'dimming' the light).
  //   percentage (measurement): Measurement per hundred.
  //   command (point_type): The signal given to make an action happen.
  //     Defaults to multistate unless used alongside a measurement_type.
  //
  // Ontology field: `blue_scene_dimmer_percentage_command`, point type
  // `command`. Measured as percentage; values are in percent, the ontology's
  // standard unit for that quantity.
  //
  // Valid range 0 to 100, enforced by protovalidate.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double blue_scene_dimmer_percentage_command = 17 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "blue_scene_dimmer_percentage_command"
      subfields: "blue"
      subfields: "scene"
      subfields: "dimmer"
      subfields: "percentage"
      subfields: "command"
      point_type: "command"
      measurement: "percentage"
      standard_unit: "percent"
    },
    (buf.validate.field).double.gte = 0.0,
    (buf.validate.field).double.lte = 100.0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The brightness percentage command.
  //
  // Composed of:
  //   brightness (measurement_descriptor): Giving out or reflecting light;
  //     overall level of light in a space.
  //   percentage (measurement): Measurement per hundred.
  //   command (point_type): The signal given to make an action happen.
  //     Defaults to multistate unless used alongside a measurement_type.
  //
  // Ontology field: `brightness_percentage_command`, point type `command`.
  // Measured as percentage; values are in percent, the ontology's standard
  // unit for that quantity.
  //
  // Valid range 0 to 100, enforced by protovalidate.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double brightness_percentage_command = 18 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "brightness_percentage_command"
      subfields: "brightness"
      subfields: "percentage"
      subfields: "command"
      point_type: "command"
      measurement: "percentage"
      standard_unit: "percent"
    },
    (buf.validate.field).double.gte = 0.0,
    (buf.validate.field).double.lte = 100.0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The brightness percentage sensor.
  //
  // Composed of:
  //   brightness (measurement_descriptor): Giving out or reflecting light;
  //     overall level of light in a space.
  //   percentage (measurement): Measurement per hundred.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `brightness_percentage_sensor`, point type `sensor`.
  // Measured as percentage; values are in percent, the ontology's standard
  // unit for that quantity.
  //
  // Valid range 0 to 100, enforced by protovalidate.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double brightness_percentage_sensor = 19 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "brightness_percentage_sensor"
      subfields: "brightness"
      subfields: "percentage"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "percentage"
      standard_unit: "percent"
    },
    (buf.validate.field).double.gte = 0.0,
    (buf.validate.field).double.lte = 100.0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The energy accumulator.
  //
  // Composed of:
  //   energy (measurement): The quantitative property that must be
  //     transferred to an object to perform work (default electrical unless
  //     otherwise modified).
  //   accumulator (point_type): The total accumulated quantity (e.g.,
  //     total energy accumulated).
  //
  // Ontology field: `energy_accumulator`, point type `accumulator`. Measured
  // as energy; values are in joules, the ontology's standard unit for that
  // quantity.
  //
  // Valid range from 0, enforced by protovalidate. Expected range up to
  // 3.6e+19. This is a data-quality expectation, not a constraint: a reading
  // outside it is a fault to report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double energy_accumulator = 20 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "energy_accumulator"
      subfields: "energy"
      subfields: "accumulator"
      point_type: "accumulator"
      measurement: "energy"
      standard_unit: "joules"
      expected_max: 36000000000000000000.0
    },
    (buf.validate.field).double.gte = 0.0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The green scene dimmer percentage command.
  //
  // Composed of:
  //   green (descriptor): Green light fracture of ambient light
  //   scene (descriptor): The holistic ambience achieved from coordinated
  //     control of an area's lighting levels and colors. One of many stored
  //     setpoints that may be recalled to apply predetermined lighting
  //     levels across a number of fixtures.
  //   dimmer (component): A device which can reduce lighting output of a
  //     lamp (thus 'dimming' the light).
  //   percentage (measurement): Measurement per hundred.
  //   command (point_type): The signal given to make an action happen.
  //     Defaults to multistate unless used alongside a measurement_type.
  //
  // Ontology field: `green_scene_dimmer_percentage_command`, point type
  // `command`. Measured as percentage; values are in percent, the ontology's
  // standard unit for that quantity.
  //
  // Valid range 0 to 100, enforced by protovalidate.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double green_scene_dimmer_percentage_command = 21 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "green_scene_dimmer_percentage_command"
      subfields: "green"
      subfields: "scene"
      subfields: "dimmer"
      subfields: "percentage"
      subfields: "command"
      point_type: "command"
      measurement: "percentage"
      standard_unit: "percent"
    },
    (buf.validate.field).double.gte = 0.0,
    (buf.validate.field).double.lte = 100.0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The group mode.
  //
  // Composed of:
  //   group (descriptor): A set of entities or devices
  //   mode (point_type): Distinct mode of operation within system. For
  //     example economizer_mode has modes of 'enabled' or 'disabled.'
  //
  // Ontology field: `group_mode`, point type `mode`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional AutoConstantLightControlDimmingSceneControlValue group_mode = 22 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "group_mode"
      subfields: "group"
      subfields: "mode"
      point_type: "mode"
    }
  ];

  // The high scene dimmer percentage setpoint.
  //
  // Composed of:
  //   high (descriptor): Level of control or measurement; above medium and
  //     low; opposite of low.
  //   scene (descriptor): The holistic ambience achieved from coordinated
  //     control of an area's lighting levels and colors. One of many stored
  //     setpoints that may be recalled to apply predetermined lighting
  //     levels across a number of fixtures.
  //   dimmer (component): A device which can reduce lighting output of a
  //     lamp (thus 'dimming' the light).
  //   percentage (measurement): Measurement per hundred.
  //   setpoint (point_type): Control target of process or system.
  //
  // Ontology field: `high_scene_dimmer_percentage_setpoint`, point type
  // `setpoint`. Measured as percentage; values are in percent, the
  // ontology's standard unit for that quantity.
  //
  // Valid range 0 to 100, enforced by protovalidate.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double high_scene_dimmer_percentage_setpoint = 23 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "high_scene_dimmer_percentage_setpoint"
      subfields: "high"
      subfields: "scene"
      subfields: "dimmer"
      subfields: "percentage"
      subfields: "setpoint"
      point_type: "setpoint"
      measurement: "percentage"
      standard_unit: "percent"
    },
    (buf.validate.field).double.gte = 0.0,
    (buf.validate.field).double.lte = 100.0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The illuminance sensor.
  //
  // Composed of:
  //   illuminance (measurement): Measurement of light.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `illuminance_sensor`, point type `sensor`. Measured as
  // illuminance; values are in lux, the ontology's standard unit for that
  // quantity.
  //
  // Expected range 0 to 5000. This is a data-quality expectation, not a
  // constraint: a reading outside it is a fault to report, not a message to
  // reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double illuminance_sensor = 24 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "illuminance_sensor"
      subfields: "illuminance"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "illuminance"
      standard_unit: "lux"
      expected_min: 0.0
      expected_max: 5000.0
    }
  ];

  // The illuminance setpoint.
  //
  // Composed of:
  //   illuminance (measurement): Measurement of light.
  //   setpoint (point_type): Control target of process or system.
  //
  // Ontology field: `illuminance_setpoint`, point type `setpoint`. Measured
  // as illuminance; values are in lux, the ontology's standard unit for that
  // quantity.
  //
  // Expected range 0 to 5000. This is a data-quality expectation, not a
  // constraint: a reading outside it is a fault to report, not a message to
  // reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double illuminance_setpoint = 25 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "illuminance_setpoint"
      subfields: "illuminance"
      subfields: "setpoint"
      point_type: "setpoint"
      measurement: "illuminance"
      standard_unit: "lux"
      expected_min: 0.0
      expected_max: 5000.0
    }
  ];

  // The lighting switch status.
  //
  // Composed of:
  //   lighting (descriptor): Illumination of a room.
  //   switch (component): Component to disrupt an electrical cirucit.
  //   status (point_type): The multistate value indicating an observed
  //     state in a piece of equipment, often indicating if a command was
  //     effected. It is a neutral observation (i.e., no quality judgment of
  //     'good' or 'bad'). It also has no units of measurement; therefore, if
  //     combined with a measurement subfield, it will indicate that the
  //     field is the directional status based on some measurement of that
  //     type (e.g., power_status equates to an on/off value based on some
  //     inference of power).
  //
  // Ontology field: `lighting_switch_status`, point type `status`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional OffOnValue lighting_switch_status = 26 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "lighting_switch_status"
      subfields: "lighting"
      subfields: "switch"
      subfields: "status"
      point_type: "status"
    }
  ];

  // The low scene dimmer percentage setpoint.
  //
  // Composed of:
  //   low (descriptor): Level of control or measurement; below medium and
  //     high; opposite of high.
  //   scene (descriptor): The holistic ambience achieved from coordinated
  //     control of an area's lighting levels and colors. One of many stored
  //     setpoints that may be recalled to apply predetermined lighting
  //     levels across a number of fixtures.
  //   dimmer (component): A device which can reduce lighting output of a
  //     lamp (thus 'dimming' the light).
  //   percentage (measurement): Measurement per hundred.
  //   setpoint (point_type): Control target of process or system.
  //
  // Ontology field: `low_scene_dimmer_percentage_setpoint`, point type
  // `setpoint`. Measured as percentage; values are in percent, the
  // ontology's standard unit for that quantity.
  //
  // Valid range 0 to 100, enforced by protovalidate.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double low_scene_dimmer_percentage_setpoint = 27 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "low_scene_dimmer_percentage_setpoint"
      subfields: "low"
      subfields: "scene"
      subfields: "dimmer"
      subfields: "percentage"
      subfields: "setpoint"
      point_type: "setpoint"
      measurement: "percentage"
      standard_unit: "percent"
    },
    (buf.validate.field).double.gte = 0.0,
    (buf.validate.field).double.lte = 100.0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The motion status.
  //
  // Composed of:
  //   motion (measurement_descriptor): Detectable movement of a thing.
  //   status (point_type): The multistate value indicating an observed
  //     state in a piece of equipment, often indicating if a command was
  //     effected. It is a neutral observation (i.e., no quality judgment of
  //     'good' or 'bad'). It also has no units of measurement; therefore, if
  //     combined with a measurement subfield, it will indicate that the
  //     field is the directional status based on some measurement of that
  //     type (e.g., power_status equates to an on/off value based on some
  //     inference of power).
  //
  // Ontology field: `motion_status`, point type `status`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional AbsentPresentValue motion_status = 28 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "motion_status"
      subfields: "motion"
      subfields: "status"
      point_type: "status"
    }
  ];

  // The power sensor.
  //
  // Composed of:
  //   power (measurement): Rate of energy consumption (assumed context is
  //     electrical and real, unless otherwise modified).
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `power_sensor`, point type `sensor`. Measured as power;
  // values are in watts, the ontology's standard unit for that quantity.
  //
  // Expected range 0 to 1e+07. This is a data-quality expectation, not a
  // constraint: a reading outside it is a fault to report, not a message to
  // reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double power_sensor = 29 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "power_sensor"
      subfields: "power"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "power"
      standard_unit: "watts"
      expected_min: 0.0
      expected_max: 10000000.0
    }
  ];

  // The red scene dimmer percentage command.
  //
  // Composed of:
  //   red (descriptor): Red light fracture of ambient light.
  //   scene (descriptor): The holistic ambience achieved from coordinated
  //     control of an area's lighting levels and colors. One of many stored
  //     setpoints that may be recalled to apply predetermined lighting
  //     levels across a number of fixtures.
  //   dimmer (component): A device which can reduce lighting output of a
  //     lamp (thus 'dimming' the light).
  //   percentage (measurement): Measurement per hundred.
  //   command (point_type): The signal given to make an action happen.
  //     Defaults to multistate unless used alongside a measurement_type.
  //
  // Ontology field: `red_scene_dimmer_percentage_command`, point type
  // `command`. Measured as percentage; values are in percent, the ontology's
  // standard unit for that quantity.
  //
  // Valid range 0 to 100, enforced by protovalidate.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double red_scene_dimmer_percentage_command = 30 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "red_scene_dimmer_percentage_command"
      subfields: "red"
      subfields: "scene"
      subfields: "dimmer"
      subfields: "percentage"
      subfields: "command"
      point_type: "command"
      measurement: "percentage"
      standard_unit: "percent"
    },
    (buf.validate.field).double.gte = 0.0,
    (buf.validate.field).double.lte = 100.0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The room occupancy status.
  //
  // Composed of:
  //   room (descriptor): A space that can be occupied, or a part or
  //     division of a building or floor enclosed by walls.
  //   occupancy (descriptor): State of being occupied.
  //   status (point_type): The multistate value indicating an observed
  //     state in a piece of equipment, often indicating if a command was
  //     effected. It is a neutral observation (i.e., no quality judgment of
  //     'good' or 'bad'). It also has no units of measurement; therefore, if
  //     combined with a measurement subfield, it will indicate that the
  //     field is the directional status based on some measurement of that
  //     type (e.g., power_status equates to an on/off value based on some
  //     inference of power).
  //
  // Ontology field: `room_occupancy_status`, point type `status`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional BypassOccupiedStandbyUnknownUnoccupiedValue room_occupancy_status = 31 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "room_occupancy_status"
      subfields: "room"
      subfields: "occupancy"
      subfields: "status"
      point_type: "status"
    }
  ];

  // The run command.
  //
  // Composed of:
  //   run (measurement_descriptor): State of being active or operating. By
  //     default applies to control program for system (e.g., VAV program
  //     run_command).
  //   command (point_type): The signal given to make an action happen.
  //     Defaults to multistate unless used alongside a measurement_type.
  //
  // Ontology field: `run_command`, point type `command`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional OffOnValue run_command = 32 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "run_command"
      subfields: "run"
      subfields: "command"
      point_type: "command"
    }
  ];

  // The run status.
  //
  // Composed of:
  //   run (measurement_descriptor): State of being active or operating. By
  //     default applies to control program for system (e.g., VAV program
  //     run_command).
  //   status (point_type): The multistate value indicating an observed
  //     state in a piece of equipment, often indicating if a command was
  //     effected. It is a neutral observation (i.e., no quality judgment of
  //     'good' or 'bad'). It also has no units of measurement; therefore, if
  //     combined with a measurement subfield, it will indicate that the
  //     field is the directional status based on some measurement of that
  //     type (e.g., power_status equates to an on/off value based on some
  //     inference of power).
  //
  // Ontology field: `run_status`, point type `status`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional OffOnStandbyUnknownValue run_status = 33 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "run_status"
      subfields: "run"
      subfields: "status"
      point_type: "status"
    }
  ];

  // The scene command.
  //
  // Composed of:
  //   scene (descriptor): The holistic ambience achieved from coordinated
  //     control of an area's lighting levels and colors. One of many stored
  //     setpoints that may be recalled to apply predetermined lighting
  //     levels across a number of fixtures.
  //   command (point_type): The signal given to make an action happen.
  //     Defaults to multistate unless used alongside a measurement_type.
  //
  // Ontology field: `scene_command`, point type `command`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue scene_command = 34 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "scene_command"
      subfields: "scene"
      subfields: "command"
      point_type: "command"
    }
  ];

  // The scene dimmer percentage command.
  //
  // Composed of:
  //   scene (descriptor): The holistic ambience achieved from coordinated
  //     control of an area's lighting levels and colors. One of many stored
  //     setpoints that may be recalled to apply predetermined lighting
  //     levels across a number of fixtures.
  //   dimmer (component): A device which can reduce lighting output of a
  //     lamp (thus 'dimming' the light).
  //   percentage (measurement): Measurement per hundred.
  //   command (point_type): The signal given to make an action happen.
  //     Defaults to multistate unless used alongside a measurement_type.
  //
  // Ontology field: `scene_dimmer_percentage_command`, point type `command`.
  // Measured as percentage; values are in percent, the ontology's standard
  // unit for that quantity.
  //
  // Valid range 0 to 100, enforced by protovalidate.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double scene_dimmer_percentage_command = 35 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "scene_dimmer_percentage_command"
      subfields: "scene"
      subfields: "dimmer"
      subfields: "percentage"
      subfields: "command"
      point_type: "command"
      measurement: "percentage"
      standard_unit: "percent"
    },
    (buf.validate.field).double.gte = 0.0,
    (buf.validate.field).double.lte = 100.0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The scene dimmer percentage sensor.
  //
  // Composed of:
  //   scene (descriptor): The holistic ambience achieved from coordinated
  //     control of an area's lighting levels and colors. One of many stored
  //     setpoints that may be recalled to apply predetermined lighting
  //     levels across a number of fixtures.
  //   dimmer (component): A device which can reduce lighting output of a
  //     lamp (thus 'dimming' the light).
  //   percentage (measurement): Measurement per hundred.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `scene_dimmer_percentage_sensor`, point type `sensor`.
  // Measured as percentage; values are in percent, the ontology's standard
  // unit for that quantity.
  //
  // Valid range 0 to 100, enforced by protovalidate.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double scene_dimmer_percentage_sensor = 36 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "scene_dimmer_percentage_sensor"
      subfields: "scene"
      subfields: "dimmer"
      subfields: "percentage"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "percentage"
      standard_unit: "percent"
    },
    (buf.validate.field).double.gte = 0.0,
    (buf.validate.field).double.lte = 100.0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The scene index command.
  //
  // Composed of:
  //   scene (descriptor): The holistic ambience achieved from coordinated
  //     control of an area's lighting levels and colors. One of many stored
  //     setpoints that may be recalled to apply predetermined lighting
  //     levels across a number of fixtures.
  //   index (measurement_descriptor): An integer which indicates the
  //     location of a particular data point in a list or numbered set (e.g.,
  //     index 1 of ['a','b','c'] would return 'b', assuming the indexing
  //     starts at 0).
  //   command (point_type): The signal given to make an action happen.
  //     Defaults to multistate unless used alongside a measurement_type.
  //
  // Ontology field: `scene_index_command`, point type `command`.
  // Dimensionless: the ontology defines this point type as an integer count
  // rather than a measured quantity.
  //
  // Valid range from 0, enforced by protovalidate. Expected range up to 100.
  // This is a data-quality expectation, not a constraint: a reading outside
  // it is a fault to report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional int64 scene_index_command = 37 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "scene_index_command"
      subfields: "scene"
      subfields: "index"
      subfields: "command"
      point_type: "command"
      expected_max: 100.0
    },
    (buf.validate.field).int64.gte = 0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The switch command.
  //
  // Composed of:
  //   switch (component): Component to disrupt an electrical cirucit.
  //   command (point_type): The signal given to make an action happen.
  //     Defaults to multistate unless used alongside a measurement_type.
  //
  // Ontology field: `switch_command`, point type `command`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional OffOnValue switch_command = 38 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "switch_command"
      subfields: "switch"
      subfields: "command"
      point_type: "command"
    }
  ];

  // The total failed communication counter.
  //
  // Composed of:
  //   total (aggregation): Sum total of some set of values (e.g.
  //     total_request_heating_count)
  //   failed (descriptor): Indicates that a point is not operating as
  //     intended or is incapable of operating.
  //   communication (descriptor): Actual transmit and recieve of data or
  //     connectivity between devices.
  //   counter (point_type): Special case of accumulator that assumes
  //     integer values and non-dimensional units. For example, the field
  //     message_counter increments in integers whenever a message is sent.
  //
  // Ontology field: `total_failed_communication_counter`, point type
  // `counter`. Dimensionless: the ontology defines this point type as an
  // integer count rather than a measured quantity.
  //
  // Valid range from 0, enforced by protovalidate. Expected range up to
  // 1e+07. This is a data-quality expectation, not a constraint: a reading
  // outside it is a fault to report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional int64 total_failed_communication_counter = 39 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "total_failed_communication_counter"
      subfields: "total"
      subfields: "failed"
      subfields: "communication"
      subfields: "counter"
      point_type: "counter"
      expected_max: 10000000.0
    },
    (buf.validate.field).int64.gte = 0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The total failed lamp counter.
  //
  // Composed of:
  //   total (aggregation): Sum total of some set of values (e.g.
  //     total_request_heating_count)
  //   failed (descriptor): Indicates that a point is not operating as
  //     intended or is incapable of operating.
  //   lamp (component): The component of a lighting device that emits the
  //     light.
  //   counter (point_type): Special case of accumulator that assumes
  //     integer values and non-dimensional units. For example, the field
  //     message_counter increments in integers whenever a message is sent.
  //
  // Ontology field: `total_failed_lamp_counter`, point type `counter`.
  // Dimensionless: the ontology defines this point type as an integer count
  // rather than a measured quantity.
  //
  // Valid range from 0, enforced by protovalidate. Expected range up to
  // 1e+07. This is a data-quality expectation, not a constraint: a reading
  // outside it is a fault to report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional int64 total_failed_lamp_counter = 40 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "total_failed_lamp_counter"
      subfields: "total"
      subfields: "failed"
      subfields: "lamp"
      subfields: "counter"
      point_type: "counter"
      expected_max: 10000000.0
    },
    (buf.validate.field).int64.gte = 0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The total failed wireless device counter.
  //
  // Composed of:
  //   total (aggregation): Sum total of some set of values (e.g.
  //     total_request_heating_count)
  //   failed (descriptor): Indicates that a point is not operating as
  //     intended or is incapable of operating.
  //   wireless (descriptor): Devices capable of communication wirelessly
  //     through electromagnetic fields.(e.g., wireless_sensors).
  //   device (component): The core equipment being represented by the
  //     Field groupings.
  //   counter (point_type): Special case of accumulator that assumes
  //     integer values and non-dimensional units. For example, the field
  //     message_counter increments in integers whenever a message is sent.
  //
  // Ontology field: `total_failed_wireless_device_counter`, point type
  // `counter`. Dimensionless: the ontology defines this point type as an
  // integer count rather than a measured quantity.
  //
  // Valid range from 0, enforced by protovalidate. Expected range up to
  // 1e+07. This is a data-quality expectation, not a constraint: a reading
  // outside it is a fault to report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional int64 total_failed_wireless_device_counter = 41 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "total_failed_wireless_device_counter"
      subfields: "total"
      subfields: "failed"
      subfields: "wireless"
      subfields: "device"
      subfields: "counter"
      point_type: "counter"
      expected_max: 10000000.0
    },
    (buf.validate.field).int64.gte = 0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The window brightness percentage command.
  //
  // Composed of:
  //   window (descriptor): Transparent part of the facade
  //   brightness (measurement_descriptor): Giving out or reflecting light;
  //     overall level of light in a space.
  //   percentage (measurement): Measurement per hundred.
  //   command (point_type): The signal given to make an action happen.
  //     Defaults to multistate unless used alongside a measurement_type.
  //
  // Ontology field: `window_brightness_percentage_command`, point type
  // `command`. Measured as percentage; values are in percent, the ontology's
  // standard unit for that quantity.
  //
  // Valid range 0 to 100, enforced by protovalidate.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double window_brightness_percentage_command = 42 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "window_brightness_percentage_command"
      subfields: "window"
      subfields: "brightness"
      subfields: "percentage"
      subfields: "command"
      point_type: "command"
      measurement: "percentage"
      standard_unit: "percent"
    },
    (buf.validate.field).double.gte = 0.0,
    (buf.validate.field).double.lte = 100.0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The zone occupancy status.
  //
  // Composed of:
  //   zone (descriptor): Region of building which is conditioned.
  //   occupancy (descriptor): State of being occupied.
  //   status (point_type): The multistate value indicating an observed
  //     state in a piece of equipment, often indicating if a command was
  //     effected. It is a neutral observation (i.e., no quality judgment of
  //     'good' or 'bad'). It also has no units of measurement; therefore, if
  //     combined with a measurement subfield, it will indicate that the
  //     field is the directional status based on some measurement of that
  //     type (e.g., power_status equates to an on/off value based on some
  //     inference of power).
  //
  // Ontology field: `zone_occupancy_status`, point type `status`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional InactiveOccupiedUnknownUnoccupiedValue zone_occupancy_status = 43 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "zone_occupancy_status"
      subfields: "zone"
      subfields: "occupancy"
      subfields: "status"
      point_type: "status"
    }
  ];

  // The zone scene command.
  //
  // Composed of:
  //   zone (descriptor): Region of building which is conditioned.
  //   scene (descriptor): The holistic ambience achieved from coordinated
  //     control of an area's lighting levels and colors. One of many stored
  //     setpoints that may be recalled to apply predetermined lighting
  //     levels across a number of fixtures.
  //   command (point_type): The signal given to make an action happen.
  //     Defaults to multistate unless used alongside a measurement_type.
  //
  // Ontology field: `zone_scene_command`, point type `command`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue zone_scene_command = 44 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "zone_scene_command"
      subfields: "zone"
      subfields: "scene"
      subfields: "command"
      point_type: "command"
    }
  ];
}
