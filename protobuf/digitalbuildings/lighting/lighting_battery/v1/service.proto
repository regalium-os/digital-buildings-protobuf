// Copyright 2026 RegaliumOS™.
// SPDX-License-Identifier: Apache-2.0
//
// Generated by sync from Google's Digital Buildings ontology,
// revision 2026-09-09 (4a794ac).
// Portions Copyright 2020 Google LLC, licensed under Apache-2.0.
// DO NOT EDIT.
//
// Regenerate with: just sync
// Revision history: docs/spec.md

syntax = "proto3";

package protobuf.digitalbuildings.lighting.lighting_battery.v1;

import "google/api/annotations.proto";
import "google/api/client.proto";
import "google/protobuf/empty.proto";
import "protobuf/digitalbuildings/lighting/lighting_battery/v1/lighting_battery.proto";
import "protobuf/digitalbuildings/lighting/lighting_battery/v1/messages.proto";

option java_multiple_files = true;
option java_outer_classname = "ServiceProto";
option java_package = "com.regalium.protobuf.digitalbuildings.lighting.lighting_battery.v1";

// Manages lighting batteries.
//
// AIP-131 names the service for the collection, not
// `LightingBatteryService`: that is buf STANDARD's convention and the two
// contradict, which is why buf.yaml selects BASIC and lets api-linter own
// API design (rule 1).
service LightingBatteries {
  option (google.api.default_host) = "digitalbuildings.googleapis.com";

  // Returns one lighting battery.
  rpc GetLightingBattery(GetLightingBatteryRequest) returns (LightingBattery) {
    option (google.api.http) = {get: "/v1/{name=buildings/*/lightingBatteries/*}"};
    option (google.api.method_signature) = "name";
  }

  // Lists the lighting batteries in a building.
  rpc ListLightingBatteries(ListLightingBatteriesRequest) returns (ListLightingBatteriesResponse) {
    option (google.api.http) = {get: "/v1/{parent=buildings/*}/lightingBatteries"};
    option (google.api.method_signature) = "parent";
  }

  // Creates a lighting battery.
  rpc CreateLightingBattery(CreateLightingBatteryRequest) returns (LightingBattery) {
    option (google.api.http) = {
      post: "/v1/{parent=buildings/*}/lightingBatteries"
      body: "lighting_battery"
    };
    option (google.api.method_signature) = "parent,lighting_battery,lighting_battery_id";
  }

  // Updates a lighting battery.
  //
  // Rule 8: an update_mask naming an OUTPUT_ONLY field is rejected rather
  // than silently ignored. Most fields here are OUTPUT_ONLY -- the equipment
  // reports them and no API call sets them.
  rpc UpdateLightingBattery(UpdateLightingBatteryRequest) returns (LightingBattery) {
    option (google.api.http) = {
      patch: "/v1/{lighting_battery.name=buildings/*/lightingBatteries/*}"
      body: "lighting_battery"
    };
    option (google.api.method_signature) = "lighting_battery,update_mask";
  }

  // Deletes a lighting battery.
  rpc DeleteLightingBattery(DeleteLightingBatteryRequest) returns (google.protobuf.Empty) {
    option (google.api.http) = {delete: "/v1/{name=buildings/*/lightingBatteries/*}"};
    option (google.api.method_signature) = "name";
  }

  // Restores a deleted lighting battery (AIP-164 <https://aip.dev/164>).
  rpc UndeleteLightingBattery(UndeleteLightingBatteryRequest) returns (LightingBattery) {
    option (google.api.http) = {
      post: "/v1/{name=buildings/*/lightingBatteries/*}:undelete"
      body: "*"
    };
    option (google.api.method_signature) = "name";
  }
}
