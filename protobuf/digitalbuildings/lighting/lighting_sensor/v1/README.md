<!--
Generated by `just docs` from Google's Digital Buildings ontology,
revision 2026-09-09 (4a794ac).
DO NOT EDIT -- edit sync/internal/docs and regenerate.
-->

# LightingSensor

Lighting system specific sensor.

|   |   |
| --- | --- |
| Ontology type | `LIGHTING/LS` |
| GUID | `768b8a60-ae3f-445e-8169-3b001513e752` |
| Kind | equipment |
| Proto package | `protobuf.digitalbuildings.lighting.lighting_sensor.v1` |
| Resource name | `buildings/{building}/lightingSensors/{lighting_sensor}` |
| Fields | 8 fields, 2 writable |
| Canonical types | 9 |

## Files

Rule 2: one message per file, plus its enums. `messages.proto` is the single exception, because a request and its response are meaningless apart.

| File | Contains |
| --- | --- |
| `lighting_sensor.proto` | the `LightingSensor` message |
| `lighting_sensor_type.proto` | `LightingSensorType`, one value per canonical type (rule 7) |
| `service.proto` | the `LightingSensors` service |
| `messages.proto` | its request and response messages |
| `connection.proto` | `Connection` -- how this entity relates to another (rule 4) |
| `link.proto` | `Link` -- this entity's fields mapped onto another's |
| `field_translation.proto` | `FieldTranslation`, `UnitMapping`, `StateMapping`, `ValueRange` |
| `absent_present_value.proto` | a multistate enum (rule 13) |
| `inactive_occupied_unknown_unoccupied_value.proto` | a multistate enum (rule 13) |

## Service

`LightingSensors` in `service.proto`. Every resource is a collection (rule 7): there are no singletons here, so it gets the full standard method set plus AIP-164 `Undelete`.

AIP-131 names the service for the collection rather than `LightingSensorService`; rule 1 has why buf STANDARD is not selected.

| Method | HTTP |
| --- | --- |
| `GetLightingSensor` | `GET /v1/{name=buildings/*/lightingSensors/*}` |
| `ListLightingSensors` | `GET /v1/{parent=buildings/*}/lightingSensors` |
| `CreateLightingSensor` | `POST /v1/{parent=buildings/*}/lightingSensors` |
| `UpdateLightingSensor` | `PATCH /v1/{lighting_sensor.name=buildings/*/lightingSensors/*}` |
| `DeleteLightingSensor` | `DELETE /v1/{name=buildings/*/lightingSensors/*}` |
| `UndeleteLightingSensor` | `POST /v1/{name=buildings/*/lightingSensors/*}:undelete` |

## Fields

Every field is `optional` (rule 7): the resource carries the union of every field any of its canonical types uses, and no single device reports all of them.

**Writable** is rule 8. A `command` or a `setpoint` is `OPTIONAL` and can be set; everything else is `OUTPUT_ONLY`, because the equipment reports it and no API call sets it. An `update_mask` naming an `OUTPUT_ONLY` field is rejected rather than silently ignored.

| Field | Type | Writable | Unit / states | Ontology literal | Field no. |
| --- | --- | --- | --- | --- | --- |
| `illuminance_sensor` | `double` | no | `lux` | `illuminance_sensor` | 16 |
| `lighting_percentage_sensor` | `double` | no | `percent` | `lighting_percentage_sensor` | 17 |
| `manufacturer_label` | `string` | at create | -- | `manufacturer_label` | 18 |
| `model_label` | `string` | at create | -- | `model_label` | 19 |
| `motion_status` | `AbsentPresentValue` | no | `AbsentPresentValue` | `motion_status` | 20 |
| `zone_air_relative_humidity_sensor` | `double` | no | `percent_relative_humidity` | `zone_air_relative_humidity_sensor` | 21 |
| `zone_air_temperature_sensor` | `double` | no | `kelvin` | `zone_air_temperature_sensor` | 22 |
| `zone_occupancy_status` | `InactiveOccupiedUnknownUnoccupiedValue` | no | `InactiveOccupiedUnknownUnoccupiedValue` | `zone_occupancy_status` | 23 |

### Multistate enums

2 enums, one per distinct state set rather than one per field. Across the ontology 602 multistate fields draw on only 40 sets, so an enum per field would mean 305 different spellings of {ACTIVE, INACTIVE} that no consumer could pass to a common function (rule 13).

Each is copied into this package rather than imported: AIP-215 forbids a field referencing a type in another proto package (rule 4).

| File | Enum | States |
| --- | --- | --- |
| `absent_present_value.proto` | `AbsentPresentValue` | `ABSENT`, `PRESENT` |
| `inactive_occupied_unknown_unoccupied_value.proto` | `InactiveOccupiedUnknownUnoccupiedValue` | `INACTIVE`, `OCCUPIED`, `UNKNOWN`, `UNOCCUPIED` |

## Canonical types

9 canonical types, each a value of `LightingSensorType`. The ontology's name is kept verbatim, because that name *is* the identifier.

Each value carries its exact `implements` / `uses` / `opt_uses` sets in `(annotations.canonical_type)`, so a validator can still reject a variant reporting a field its declared type does not have. That is what makes the cut in rule 7 lossless.

| Enum value | Ontology type | Implements | Description |
| --- | --- | --- | --- |
| `LIGHTING_SENSOR_TYPE_LS_IS_OS` | `LIGHTING/LS_IS_OS` | `IS`, `LS`, `OS` | Illuminance sensing and zone occupancy monitoring |
| `LIGHTING_SENSOR_TYPE_LS_LMS` | `LIGHTING/LS_LMS` | `LS`, `MS` | Lighting system motion sensor. |
| `LIGHTING_SENSOR_TYPE_LS_LOS` | `LIGHTING/LS_LOS` | `LS`, `OS` | Lighting system motion sensor. |
| `LIGHTING_SENSOR_TYPE_LS_LPS` | `LIGHTING/LS_LPS` | `LS` | Lighting system photocell sensor. |
| `LIGHTING_SENSOR_TYPE_LS_LPS_OS` | `LIGHTING/LS_LPS_OS` | `LPS`, `LS`, `OS` | Lighting percentage sensing and zone occupancy monitoring |
| `LIGHTING_SENSOR_TYPE_LS_MS_IS` | `LIGHTING/LS_MS_IS` | `IS`, `LS`, `MS` | lighting multi-sensor for motion and illuminance. |
| `LIGHTING_SENSOR_TYPE_LS_MTS` | `LIGHTING/LS_MTS` | `LS` | Lighting multi-sensor, for temperature and brightness. |
| `LIGHTING_SENSOR_TYPE_LS_OS` | `LIGHTING/LS_OS` | `LS`, `OS` | Zone occupancy monitoring |
| `LIGHTING_SENSOR_TYPE_LS_OS_IS_MTS` | `LIGHTING/LS_OS_IS_MTS` | `IS`, `LS`, `OS` | Ceiling mounted multi sensor for zone occupancy monitoring, brightness- and temperature control |

## Provenance

Generated from `LIGHTING/LS` in Google's Digital Buildings ontology.

- [Ontology concepts](https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md)
- [Abstract model](https://github.com/google/digitalbuildings/blob/master/ontology/docs/model.md)
- [Building configuration](https://github.com/google/digitalbuildings/blob/master/ontology/docs/building_config.md)
