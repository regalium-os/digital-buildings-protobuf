// Copyright 2026 RegaliumOS™.
// SPDX-License-Identifier: Apache-2.0
//
// Generated by sync from Google's Digital Buildings ontology,
// revision 2026-09-09 (4a794ac).
// Portions Copyright 2020 Google LLC, licensed under Apache-2.0.
// DO NOT EDIT.
//
// Regenerate with: just sync
// Revision history: docs/spec.md

syntax = "proto3";

package protobuf.digitalbuildings.lighting.lighting_sensor.v1;

import "google/api/annotations.proto";
import "google/api/client.proto";
import "google/protobuf/empty.proto";
import "protobuf/digitalbuildings/lighting/lighting_sensor/v1/lighting_sensor.proto";
import "protobuf/digitalbuildings/lighting/lighting_sensor/v1/messages.proto";

option java_multiple_files = true;
option java_outer_classname = "ServiceProto";
option java_package = "com.regalium.protobuf.digitalbuildings.lighting.lighting_sensor.v1";

// Manages lighting sensors.
//
// AIP-131 names the service for the collection, not `LightingSensorService`:
// that is buf STANDARD's convention and the two contradict, which is why
// buf.yaml selects BASIC and lets api-linter own API design (rule 1).
service LightingSensors {
  option (google.api.default_host) = "digitalbuildings.googleapis.com";

  // Returns one lighting sensor.
  rpc GetLightingSensor(GetLightingSensorRequest) returns (LightingSensor) {
    option (google.api.http) = {get: "/v1/{name=buildings/*/lightingSensors/*}"};
    option (google.api.method_signature) = "name";
  }

  // Lists the lighting sensors in a building.
  rpc ListLightingSensors(ListLightingSensorsRequest) returns (ListLightingSensorsResponse) {
    option (google.api.http) = {get: "/v1/{parent=buildings/*}/lightingSensors"};
    option (google.api.method_signature) = "parent";
  }

  // Creates a lighting sensor.
  rpc CreateLightingSensor(CreateLightingSensorRequest) returns (LightingSensor) {
    option (google.api.http) = {
      post: "/v1/{parent=buildings/*}/lightingSensors"
      body: "lighting_sensor"
    };
    option (google.api.method_signature) = "parent,lighting_sensor,lighting_sensor_id";
  }

  // Updates a lighting sensor.
  //
  // Rule 8: an update_mask naming an OUTPUT_ONLY field is rejected rather
  // than silently ignored. Most fields here are OUTPUT_ONLY -- the equipment
  // reports them and no API call sets them.
  rpc UpdateLightingSensor(UpdateLightingSensorRequest) returns (LightingSensor) {
    option (google.api.http) = {
      patch: "/v1/{lighting_sensor.name=buildings/*/lightingSensors/*}"
      body: "lighting_sensor"
    };
    option (google.api.method_signature) = "lighting_sensor,update_mask";
  }

  // Deletes a lighting sensor.
  rpc DeleteLightingSensor(DeleteLightingSensorRequest) returns (google.protobuf.Empty) {
    option (google.api.http) = {delete: "/v1/{name=buildings/*/lightingSensors/*}"};
    option (google.api.method_signature) = "name";
  }

  // Restores a deleted lighting sensor (AIP-164 <https://aip.dev/164>).
  rpc UndeleteLightingSensor(UndeleteLightingSensorRequest) returns (LightingSensor) {
    option (google.api.http) = {
      post: "/v1/{name=buildings/*/lightingSensors/*}:undelete"
      body: "*"
    };
    option (google.api.method_signature) = "name";
  }
}
