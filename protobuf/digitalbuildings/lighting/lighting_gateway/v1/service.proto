// Copyright 2026 RegaliumOS™.
// SPDX-License-Identifier: Apache-2.0
//
// Generated by sync from Google's Digital Buildings ontology,
// revision 2026-09-09 (4a794ac).
// Portions Copyright 2020 Google LLC, licensed under Apache-2.0.
// DO NOT EDIT.
//
// Regenerate with: just sync
// Revision history: docs/spec.md

syntax = "proto3";

package protobuf.digitalbuildings.lighting.lighting_gateway.v1;

import "google/api/annotations.proto";
import "google/api/client.proto";
import "google/protobuf/empty.proto";
import "protobuf/digitalbuildings/lighting/lighting_gateway/v1/lighting_gateway.proto";
import "protobuf/digitalbuildings/lighting/lighting_gateway/v1/messages.proto";

option java_multiple_files = true;
option java_outer_classname = "ServiceProto";
option java_package = "com.regalium.protobuf.digitalbuildings.lighting.lighting_gateway.v1";

// Manages lighting gateways.
//
// AIP-131 names the service for the collection, not
// `LightingGatewayService`: that is buf STANDARD's convention and the two
// contradict, which is why buf.yaml selects BASIC and lets api-linter own
// API design (rule 1).
service LightingGateways {
  option (google.api.default_host) = "digitalbuildings.googleapis.com";

  // Returns one lighting gateway.
  rpc GetLightingGateway(GetLightingGatewayRequest) returns (LightingGateway) {
    option (google.api.http) = {get: "/v1/{name=buildings/*/lightingGateways/*}"};
    option (google.api.method_signature) = "name";
  }

  // Lists the lighting gateways in a building.
  rpc ListLightingGateways(ListLightingGatewaysRequest) returns (ListLightingGatewaysResponse) {
    option (google.api.http) = {get: "/v1/{parent=buildings/*}/lightingGateways"};
    option (google.api.method_signature) = "parent";
  }

  // Creates a lighting gateway.
  rpc CreateLightingGateway(CreateLightingGatewayRequest) returns (LightingGateway) {
    option (google.api.http) = {
      post: "/v1/{parent=buildings/*}/lightingGateways"
      body: "lighting_gateway"
    };
    option (google.api.method_signature) = "parent,lighting_gateway,lighting_gateway_id";
  }

  // Updates a lighting gateway.
  //
  // Rule 8: an update_mask naming an OUTPUT_ONLY field is rejected rather
  // than silently ignored. Most fields here are OUTPUT_ONLY -- the equipment
  // reports them and no API call sets them.
  rpc UpdateLightingGateway(UpdateLightingGatewayRequest) returns (LightingGateway) {
    option (google.api.http) = {
      patch: "/v1/{lighting_gateway.name=buildings/*/lightingGateways/*}"
      body: "lighting_gateway"
    };
    option (google.api.method_signature) = "lighting_gateway,update_mask";
  }

  // Deletes a lighting gateway.
  rpc DeleteLightingGateway(DeleteLightingGatewayRequest) returns (google.protobuf.Empty) {
    option (google.api.http) = {delete: "/v1/{name=buildings/*/lightingGateways/*}"};
    option (google.api.method_signature) = "name";
  }

  // Restores a deleted lighting gateway (AIP-164 <https://aip.dev/164>).
  rpc UndeleteLightingGateway(UndeleteLightingGatewayRequest) returns (LightingGateway) {
    option (google.api.http) = {
      post: "/v1/{name=buildings/*/lightingGateways/*}:undelete"
      body: "*"
    };
    option (google.api.method_signature) = "name";
  }
}
