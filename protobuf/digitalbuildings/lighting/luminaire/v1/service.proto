// Copyright 2026 RegaliumOS™.
// SPDX-License-Identifier: Apache-2.0
//
// Generated by sync from Google's Digital Buildings ontology,
// revision 2026-09-09 (4a794ac).
// Portions Copyright 2020 Google LLC, licensed under Apache-2.0.
// DO NOT EDIT.
//
// Regenerate with: just sync
// Revision history: docs/spec.md

syntax = "proto3";

package protobuf.digitalbuildings.lighting.luminaire.v1;

import "google/api/annotations.proto";
import "google/api/client.proto";
import "google/protobuf/empty.proto";
import "protobuf/digitalbuildings/lighting/luminaire/v1/luminaire.proto";
import "protobuf/digitalbuildings/lighting/luminaire/v1/messages.proto";

option java_multiple_files = true;
option java_outer_classname = "ServiceProto";
option java_package = "com.regalium.protobuf.digitalbuildings.lighting.luminaire.v1";

// Manages luminaires.
//
// AIP-131 names the service for the collection, not `LuminaireService`: that
// is buf STANDARD's convention and the two contradict, which is why buf.yaml
// selects BASIC and lets api-linter own API design (rule 1).
service Luminaires {
  option (google.api.default_host) = "digitalbuildings.googleapis.com";

  // Returns one luminaire.
  rpc GetLuminaire(GetLuminaireRequest) returns (Luminaire) {
    option (google.api.http) = {get: "/v1/{name=buildings/*/luminaires/*}"};
    option (google.api.method_signature) = "name";
  }

  // Lists the luminaires in a building.
  rpc ListLuminaires(ListLuminairesRequest) returns (ListLuminairesResponse) {
    option (google.api.http) = {get: "/v1/{parent=buildings/*}/luminaires"};
    option (google.api.method_signature) = "parent";
  }

  // Creates a luminaire.
  rpc CreateLuminaire(CreateLuminaireRequest) returns (Luminaire) {
    option (google.api.http) = {
      post: "/v1/{parent=buildings/*}/luminaires"
      body: "luminaire"
    };
    option (google.api.method_signature) = "parent,luminaire,luminaire_id";
  }

  // Updates a luminaire.
  //
  // Rule 8: an update_mask naming an OUTPUT_ONLY field is rejected rather
  // than silently ignored. Most fields here are OUTPUT_ONLY -- the equipment
  // reports them and no API call sets them.
  rpc UpdateLuminaire(UpdateLuminaireRequest) returns (Luminaire) {
    option (google.api.http) = {
      patch: "/v1/{luminaire.name=buildings/*/luminaires/*}"
      body: "luminaire"
    };
    option (google.api.method_signature) = "luminaire,update_mask";
  }

  // Deletes a luminaire.
  rpc DeleteLuminaire(DeleteLuminaireRequest) returns (google.protobuf.Empty) {
    option (google.api.http) = {delete: "/v1/{name=buildings/*/luminaires/*}"};
    option (google.api.method_signature) = "name";
  }

  // Restores a deleted luminaire (AIP-164 <https://aip.dev/164>).
  rpc UndeleteLuminaire(UndeleteLuminaireRequest) returns (Luminaire) {
    option (google.api.http) = {
      post: "/v1/{name=buildings/*/luminaires/*}:undelete"
      body: "*"
    };
    option (google.api.method_signature) = "name";
  }
}
