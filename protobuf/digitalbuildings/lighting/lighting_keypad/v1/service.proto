// Copyright 2026 RegaliumOS™.
// SPDX-License-Identifier: Apache-2.0
//
// Generated by sync from Google's Digital Buildings ontology,
// revision 2026-09-09 (4a794ac).
// Portions Copyright 2020 Google LLC, licensed under Apache-2.0.
// DO NOT EDIT.
//
// Regenerate with: just sync
// Revision history: docs/spec.md

syntax = "proto3";

package protobuf.digitalbuildings.lighting.lighting_keypad.v1;

import "google/api/annotations.proto";
import "google/api/client.proto";
import "google/protobuf/empty.proto";
import "protobuf/digitalbuildings/lighting/lighting_keypad/v1/lighting_keypad.proto";
import "protobuf/digitalbuildings/lighting/lighting_keypad/v1/messages.proto";

option java_multiple_files = true;
option java_outer_classname = "ServiceProto";
option java_package = "com.regalium.protobuf.digitalbuildings.lighting.lighting_keypad.v1";

// Manages lighting keypads.
//
// AIP-131 names the service for the collection, not `LightingKeypadService`:
// that is buf STANDARD's convention and the two contradict, which is why
// buf.yaml selects BASIC and lets api-linter own API design (rule 1).
service LightingKeypads {
  option (google.api.default_host) = "digitalbuildings.googleapis.com";

  // Returns one lighting keypad.
  rpc GetLightingKeypad(GetLightingKeypadRequest) returns (LightingKeypad) {
    option (google.api.http) = {get: "/v1/{name=buildings/*/lightingKeypads/*}"};
    option (google.api.method_signature) = "name";
  }

  // Lists the lighting keypads in a building.
  rpc ListLightingKeypads(ListLightingKeypadsRequest) returns (ListLightingKeypadsResponse) {
    option (google.api.http) = {get: "/v1/{parent=buildings/*}/lightingKeypads"};
    option (google.api.method_signature) = "parent";
  }

  // Creates a lighting keypad.
  rpc CreateLightingKeypad(CreateLightingKeypadRequest) returns (LightingKeypad) {
    option (google.api.http) = {
      post: "/v1/{parent=buildings/*}/lightingKeypads"
      body: "lighting_keypad"
    };
    option (google.api.method_signature) = "parent,lighting_keypad,lighting_keypad_id";
  }

  // Updates a lighting keypad.
  //
  // Rule 8: an update_mask naming an OUTPUT_ONLY field is rejected rather
  // than silently ignored. Most fields here are OUTPUT_ONLY -- the equipment
  // reports them and no API call sets them.
  rpc UpdateLightingKeypad(UpdateLightingKeypadRequest) returns (LightingKeypad) {
    option (google.api.http) = {
      patch: "/v1/{lighting_keypad.name=buildings/*/lightingKeypads/*}"
      body: "lighting_keypad"
    };
    option (google.api.method_signature) = "lighting_keypad,update_mask";
  }

  // Deletes a lighting keypad.
  rpc DeleteLightingKeypad(DeleteLightingKeypadRequest) returns (google.protobuf.Empty) {
    option (google.api.http) = {delete: "/v1/{name=buildings/*/lightingKeypads/*}"};
    option (google.api.method_signature) = "name";
  }

  // Restores a deleted lighting keypad (AIP-164 <https://aip.dev/164>).
  rpc UndeleteLightingKeypad(UndeleteLightingKeypadRequest) returns (LightingKeypad) {
    option (google.api.http) = {
      post: "/v1/{name=buildings/*/lightingKeypads/*}:undelete"
      body: "*"
    };
    option (google.api.method_signature) = "name";
  }
}
