// Copyright 2026 RegaliumOS™.
// SPDX-License-Identifier: Apache-2.0
//
// Generated by sync from Google's Digital Buildings ontology,
// revision 2026-09-09 (4a794ac).
// Portions Copyright 2020 Google LLC, licensed under Apache-2.0.
// DO NOT EDIT.
//
// Regenerate with: just sync
// Revision history: docs/spec.md

syntax = "proto3";

package protobuf.digitalbuildings.gateways.passthrough_gateway.v1;

import "google/api/field_behavior.proto";
import "google/api/resource.proto";

option java_multiple_files = true;
option java_outer_classname = "ConnectionProto";
option java_package = "com.regalium.protobuf.digitalbuildings.gateways.passthrough_gateway.v1";

// A directed relationship to another entity.
//
// Rule 4: a connection is not a field. A floor does not hold its fan coil
// units -- AIP-215 forbids a field referencing a message in another proto
// package, and the other end may be any resource in any namespace. So the
// target is a resource name with a wildcard reference.
message Connection {
  // The kind of relationship.
  ConnectionType type = 1 [(google.api.field_behavior) = REQUIRED];

  // The resource name of the entity at the other end.
  //
  // `type: "*"` because a connection may point at any resource in any
  // namespace: a floor CONTAINS equipment of every kind.
  string entity = 2 [
    (google.api.field_behavior) = REQUIRED,
    (google.api.resource_reference).type = "*"
  ];
}

// The ontology's connection types.
enum ConnectionType {
  // Default. The relationship is unspecified.
  CONNECTION_TYPE_UNSPECIFIED = 0;

  // Source physically encapsulates at least part of Target.
  CONNECTION_TYPE_CONTAINS = 1;

  // Source determines or affects the internal state or behavior of Target.
  CONNECTION_TYPE_CONTROLS = 2;

  // Source provides some media (ex: water or air) to Target.
  CONNECTION_TYPE_FEEDS = 3;

  // Source combines attributes of the Targets and the Targets' attributes
  // fully account for the Source's attributes.
  CONNECTION_TYPE_FULLY_AGGREGATES = 4;

  // Source has some component or part defined by Target.
  CONNECTION_TYPE_HAS_PART = 5;

  // Source has a coverage or detection range defined by Target.
  CONNECTION_TYPE_HAS_RANGE = 6;

  // Source quantifies attributes of the Target.
  CONNECTION_TYPE_MEASURES = 7;

  // Source quantifies attributes that are categorized by the Target.
  CONNECTION_TYPE_MEASURES_TYPE = 8;

  // Source combines attributes of the Targets but the Targets' attributes do
  // not fully account for the Source's attributes.
  //
  // The ontology spells this without the second `t`. Kept verbatim: it is
  // the wire value, and correcting it here would desynchronise this schema
  // from every config written against the ontology.
  CONNECTION_TYPE_PARIALLY_AGGREGATES = 9;
}
