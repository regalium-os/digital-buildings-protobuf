<!--
Generated by `just docs` from Google's Digital Buildings ontology,
revision 2026-09-09 (4a794ac).
DO NOT EDIT -- edit sync/internal/docs and regenerate.
-->

# PassthroughGateway

A device that provides translations for virtual entities.

|   |   |
| --- | --- |
| Ontology type | `GATEWAYS/PASSTHROUGH` |
| GUID | `065f9ad3-7c50-44cd-8cad-5a8ae4d16d2c` |
| Kind | not equipment (a system, a riser, a zone grouping) |
| Proto package | `protobuf.digitalbuildings.gateways.passthrough_gateway.v1` |
| Resource name | `buildings/{building}/passthroughGateways/{passthrough_gateway}` |
| Fields | 0 fields, 0 writable |
| Canonical types | 0 |

## Files

Rule 2: one message per file, plus its enums. `messages.proto` is the single exception, because a request and its response are meaningless apart.

| File | Contains |
| --- | --- |
| `passthrough_gateway.proto` | the `PassthroughGateway` message |
| `service.proto` | the `PassthroughGateways` service |
| `messages.proto` | its request and response messages |
| `connection.proto` | `Connection` -- how this entity relates to another (rule 4) |
| `link.proto` | `Link` -- this entity's fields mapped onto another's |
| `field_translation.proto` | `FieldTranslation`, `UnitMapping`, `StateMapping`, `ValueRange` |

## Service

`PassthroughGateways` in `service.proto`. Every resource is a collection (rule 7): there are no singletons here, so it gets the full standard method set plus AIP-164 `Undelete`.

AIP-131 names the service for the collection rather than `PassthroughGatewayService`; rule 1 has why buf STANDARD is not selected.

| Method | HTTP |
| --- | --- |
| `GetPassthroughGateway` | `GET /v1/{name=buildings/*/passthroughGateways/*}` |
| `ListPassthroughGateways` | `GET /v1/{parent=buildings/*}/passthroughGateways` |
| `CreatePassthroughGateway` | `POST /v1/{parent=buildings/*}/passthroughGateways` |
| `UpdatePassthroughGateway` | `PATCH /v1/{passthrough_gateway.name=buildings/*/passthroughGateways/*}` |
| `DeletePassthroughGateway` | `DELETE /v1/{name=buildings/*/passthroughGateways/*}` |
| `UndeletePassthroughGateway` | `POST /v1/{name=buildings/*/passthroughGateways/*}:undelete` |

## Provenance

Generated from `GATEWAYS/PASSTHROUGH` in Google's Digital Buildings ontology.

- [Ontology concepts](https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md)
- [Abstract model](https://github.com/google/digitalbuildings/blob/master/ontology/docs/model.md)
- [Building configuration](https://github.com/google/digitalbuildings/blob/master/ontology/docs/building_config.md)
