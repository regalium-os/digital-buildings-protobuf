<!--
Generated by `just docs` from Google's Digital Buildings ontology,
revision 2026-09-09 (4a794ac).
DO NOT EDIT -- edit sync/internal/docs and regenerate.
-->

# ReverseOsmosisUnit

Reverse osmosis is a water purification process that uses a semi-permeable membrane (synthetic lining) to filter out unwanted molecules and large particles such as contaminants and sediments like chlorine, salt, and dirt from drinking water.

|   |   |
| --- | --- |
| Ontology type | `PLUMBING/RO` |
| GUID | `9f9b8144-df6c-4b20-bbbb-110f718c1cb1` |
| Kind | equipment |
| Proto package | `protobuf.digitalbuildings.plumbing.reverse_osmosis_unit.v1` |
| Resource name | `buildings/{building}/reverseOsmosisUnits/{reverse_osmosis_unit}` |
| Fields | 5 fields, 2 writable |
| Canonical types | 1 |

## Files

Rule 2: one message per file, plus its enums. `messages.proto` is the single exception, because a request and its response are meaningless apart.

| File | Contains |
| --- | --- |
| `reverse_osmosis_unit.proto` | the `ReverseOsmosisUnit` message |
| `reverse_osmosis_unit_type.proto` | `ReverseOsmosisUnitType`, one value per canonical type (rule 7) |
| `service.proto` | the `ReverseOsmosisUnits` service |
| `messages.proto` | its request and response messages |
| `connection.proto` | `Connection` -- how this entity relates to another (rule 4) |
| `link.proto` | `Link` -- this entity's fields mapped onto another's |
| `field_translation.proto` | `FieldTranslation`, `UnitMapping`, `StateMapping`, `ValueRange` |
| `auto_cooling_dehumidify_fan_only_heating_manual_off_value.proto` | a multistate enum (rule 13) |

## Service

`ReverseOsmosisUnits` in `service.proto`. Every resource is a collection (rule 7): there are no singletons here, so it gets the full standard method set plus AIP-164 `Undelete`.

AIP-131 names the service for the collection rather than `ReverseOsmosisUnitService`; rule 1 has why buf STANDARD is not selected.

| Method | HTTP |
| --- | --- |
| `GetReverseOsmosisUnit` | `GET /v1/{name=buildings/*/reverseOsmosisUnits/*}` |
| `ListReverseOsmosisUnits` | `GET /v1/{parent=buildings/*}/reverseOsmosisUnits` |
| `CreateReverseOsmosisUnit` | `POST /v1/{parent=buildings/*}/reverseOsmosisUnits` |
| `UpdateReverseOsmosisUnit` | `PATCH /v1/{reverse_osmosis_unit.name=buildings/*/reverseOsmosisUnits/*}` |
| `DeleteReverseOsmosisUnit` | `DELETE /v1/{name=buildings/*/reverseOsmosisUnits/*}` |
| `UndeleteReverseOsmosisUnit` | `POST /v1/{name=buildings/*/reverseOsmosisUnits/*}:undelete` |

## Fields

Every field is `optional` (rule 7): the resource carries the union of every field any of its canonical types uses, and no single device reports all of them.

**Writable** is rule 8. A `command` or a `setpoint` is `OPTIONAL` and can be set; everything else is `OUTPUT_ONLY`, because the equipment reports it and no API call sets it. An `update_mask` naming an `OUTPUT_ONLY` field is rejected rather than silently ignored.

| Field | Type | Writable | Unit / states | Ontology literal | Field no. |
| --- | --- | --- | --- | --- | --- |
| `control_mode` | `AutoCoolingDehumidifyFanOnlyHeatingManualOffValue` | no | `AutoCoolingDehumidifyFanOnlyHeatingManualOffValue` | `control_mode` | 16 |
| `line_pressure_sensor` | `double` | no | `pascals` | `line_pressure_sensor` | 17 |
| `manufacturer_label` | `string` | at create | -- | `manufacturer_label` | 18 |
| `model_label` | `string` | at create | -- | `model_label` | 19 |
| `water_totaldissolvedsolids_concentration_sensor` | `double` | no | `parts_per_unit` | `water_totaldissolvedsolids_concentration_sensor` | 20 |

### Multistate enums

1 enum, one per distinct state set rather than one per field. Across the ontology 602 multistate fields draw on only 40 sets, so an enum per field would mean 305 different spellings of {ACTIVE, INACTIVE} that no consumer could pass to a common function (rule 13).

Each is copied into this package rather than imported: AIP-215 forbids a field referencing a type in another proto package (rule 4).

| File | Enum | States |
| --- | --- | --- |
| `auto_cooling_dehumidify_fan_only_heating_manual_off_value.proto` | `AutoCoolingDehumidifyFanOnlyHeatingManualOffValue` | `AUTO`, `COOLING`, `DEHUMIDIFY`, `FAN_ONLY`, `HEATING`, `MANUAL`, `OFF` |

## Canonical types

1 canonical type, each a value of `ReverseOsmosisUnitType`. The ontology's name is kept verbatim, because that name *is* the identifier.

Each value carries its exact `implements` / `uses` / `opt_uses` sets in `(annotations.canonical_type)`, so a validator can still reject a variant reporting a field its declared type does not have. That is what makes the cut in rule 7 lossless.

| Enum value | Ontology type | Implements | Description |
| --- | --- | --- | --- |
| `REVERSE_OSMOSIS_UNIT_TYPE_RO_STD` | `PLUMBING/RO_STD` | `RO` | Standard reverse osmosis (RO) water system. |

## Provenance

Generated from `PLUMBING/RO` in Google's Digital Buildings ontology.

- [Ontology concepts](https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md)
- [Abstract model](https://github.com/google/digitalbuildings/blob/master/ontology/docs/model.md)
- [Building configuration](https://github.com/google/digitalbuildings/blob/master/ontology/docs/building_config.md)
