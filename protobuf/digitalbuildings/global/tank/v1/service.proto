// Copyright 2026 RegaliumOS™.
// SPDX-License-Identifier: Apache-2.0
//
// Generated by sync from Google's Digital Buildings ontology,
// revision 2026-09-09 (4a794ac).
// Portions Copyright 2020 Google LLC, licensed under Apache-2.0.
// DO NOT EDIT.
//
// Regenerate with: just sync
// Revision history: docs/spec.md

syntax = "proto3";

package protobuf.digitalbuildings.global.tank.v1;

import "google/api/annotations.proto";
import "google/api/client.proto";
import "google/protobuf/empty.proto";
import "protobuf/digitalbuildings/global/tank/v1/messages.proto";
import "protobuf/digitalbuildings/global/tank/v1/tank.proto";

option java_multiple_files = true;
option java_outer_classname = "ServiceProto";
option java_package = "com.regalium.protobuf.digitalbuildings.global.tank.v1";

// Manages tanks.
//
// AIP-131 names the service for the collection, not `TankService`: that is
// buf STANDARD's convention and the two contradict, which is why buf.yaml
// selects BASIC and lets api-linter own API design (rule 1).
service Tanks {
  option (google.api.default_host) = "digitalbuildings.googleapis.com";

  // Returns one tank.
  rpc GetTank(GetTankRequest) returns (Tank) {
    option (google.api.http) = {get: "/v1/{name=buildings/*/tanks/*}"};
    option (google.api.method_signature) = "name";
  }

  // Lists the tanks in a building.
  rpc ListTanks(ListTanksRequest) returns (ListTanksResponse) {
    option (google.api.http) = {get: "/v1/{parent=buildings/*}/tanks"};
    option (google.api.method_signature) = "parent";
  }

  // Creates a tank.
  rpc CreateTank(CreateTankRequest) returns (Tank) {
    option (google.api.http) = {
      post: "/v1/{parent=buildings/*}/tanks"
      body: "tank"
    };
    option (google.api.method_signature) = "parent,tank,tank_id";
  }

  // Updates a tank.
  //
  // Rule 8: an update_mask naming an OUTPUT_ONLY field is rejected rather
  // than silently ignored. Most fields here are OUTPUT_ONLY -- the equipment
  // reports them and no API call sets them.
  rpc UpdateTank(UpdateTankRequest) returns (Tank) {
    option (google.api.http) = {
      patch: "/v1/{tank.name=buildings/*/tanks/*}"
      body: "tank"
    };
    option (google.api.method_signature) = "tank,update_mask";
  }

  // Deletes a tank.
  rpc DeleteTank(DeleteTankRequest) returns (google.protobuf.Empty) {
    option (google.api.http) = {delete: "/v1/{name=buildings/*/tanks/*}"};
    option (google.api.method_signature) = "name";
  }

  // Restores a deleted tank (AIP-164 <https://aip.dev/164>).
  rpc UndeleteTank(UndeleteTankRequest) returns (Tank) {
    option (google.api.http) = {
      post: "/v1/{name=buildings/*/tanks/*}:undelete"
      body: "*"
    };
    option (google.api.method_signature) = "name";
  }
}
