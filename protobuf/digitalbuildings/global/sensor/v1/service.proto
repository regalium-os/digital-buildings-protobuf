// Copyright 2026 RegaliumOS™.
// SPDX-License-Identifier: Apache-2.0
//
// Generated by sync from Google's Digital Buildings ontology,
// revision 2026-09-09 (4a794ac).
// Portions Copyright 2020 Google LLC, licensed under Apache-2.0.
// DO NOT EDIT.
//
// Regenerate with: just sync
// Revision history: docs/spec.md

syntax = "proto3";

package protobuf.digitalbuildings.global.sensor.v1;

import "google/api/annotations.proto";
import "google/api/client.proto";
import "google/protobuf/empty.proto";
import "protobuf/digitalbuildings/global/sensor/v1/messages.proto";
import "protobuf/digitalbuildings/global/sensor/v1/sensor.proto";

option java_multiple_files = true;
option java_outer_classname = "ServiceProto";
option java_package = "com.regalium.protobuf.digitalbuildings.global.sensor.v1";

// Manages sensors.
//
// AIP-131 names the service for the collection, not `SensorService`: that is
// buf STANDARD's convention and the two contradict, which is why buf.yaml
// selects BASIC and lets api-linter own API design (rule 1).
service Sensors {
  option (google.api.default_host) = "digitalbuildings.googleapis.com";

  // Returns one sensor.
  rpc GetSensor(GetSensorRequest) returns (Sensor) {
    option (google.api.http) = {get: "/v1/{name=buildings/*/sensors/*}"};
    option (google.api.method_signature) = "name";
  }

  // Lists the sensors in a building.
  rpc ListSensors(ListSensorsRequest) returns (ListSensorsResponse) {
    option (google.api.http) = {get: "/v1/{parent=buildings/*}/sensors"};
    option (google.api.method_signature) = "parent";
  }

  // Creates a sensor.
  rpc CreateSensor(CreateSensorRequest) returns (Sensor) {
    option (google.api.http) = {
      post: "/v1/{parent=buildings/*}/sensors"
      body: "sensor"
    };
    option (google.api.method_signature) = "parent,sensor,sensor_id";
  }

  // Updates a sensor.
  //
  // Rule 8: an update_mask naming an OUTPUT_ONLY field is rejected rather
  // than silently ignored. Most fields here are OUTPUT_ONLY -- the equipment
  // reports them and no API call sets them.
  rpc UpdateSensor(UpdateSensorRequest) returns (Sensor) {
    option (google.api.http) = {
      patch: "/v1/{sensor.name=buildings/*/sensors/*}"
      body: "sensor"
    };
    option (google.api.method_signature) = "sensor,update_mask";
  }

  // Deletes a sensor.
  rpc DeleteSensor(DeleteSensorRequest) returns (google.protobuf.Empty) {
    option (google.api.http) = {delete: "/v1/{name=buildings/*/sensors/*}"};
    option (google.api.method_signature) = "name";
  }

  // Restores a deleted sensor (AIP-164 <https://aip.dev/164>).
  rpc UndeleteSensor(UndeleteSensorRequest) returns (Sensor) {
    option (google.api.http) = {
      post: "/v1/{name=buildings/*/sensors/*}:undelete"
      body: "*"
    };
    option (google.api.method_signature) = "name";
  }
}
