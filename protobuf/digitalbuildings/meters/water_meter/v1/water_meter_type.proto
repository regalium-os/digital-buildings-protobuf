// Copyright 2026 RegaliumOS™.
// SPDX-License-Identifier: Apache-2.0
//
// Generated by sync from Google's Digital Buildings ontology,
// revision 2026-09-09 (4a794ac).
// Portions Copyright 2020 Google LLC, licensed under Apache-2.0.
// DO NOT EDIT.
//
// Regenerate with: just sync
// Revision history: docs/spec.md

syntax = "proto3";

package protobuf.digitalbuildings.meters.water_meter.v1;

import "protobuf/digitalbuildings/annotations/v1/annotations.proto";

option java_multiple_files = true;
option java_outer_classname = "WaterMeterTypeProto";
option java_package = "com.regalium.protobuf.digitalbuildings.meters.water_meter.v1";

// The canonical types of WaterMeter.
//
// Rule 7: a message per canonical type would be 1,587 messages and 1,587
// CRUD services, which is not a service surface. Each variant is a value
// here instead, and its exact field sets ride on the value in
// (annotations.canonical_type) -- so nothing the ontology states is lost,
// only compile-time enforcement of it.
//
// Names are the ontology's own, verbatim: `WM_LM` *is* the identifier, and
// expanding it would invent a second one.
enum WaterMeterType {
  // Default. The entity's canonical type is unspecified.
  WATER_METER_TYPE_UNSPECIFIED = 0;

  // Tank water level monitoring.
  //
  // Ontology type `METERS/WM_LM`.
  WATER_METER_TYPE_WM_LM = 1 [(protobuf.digitalbuildings.annotations.v1.canonical_type) = {
    guid: "e3dbd5de-d89b-4d3b-91e1-73ae42997195"
    qualified_type: "METERS/WM_LM"
    implemented_types: "METERS/WM"
    uses: "water_level_sensor"
  }];

  // Water meter that operates by counting pulses, interpreted into flow and
  // accumulated volume.
  //
  // Ontology type `METERS/WM_PULSE`.
  WATER_METER_TYPE_WM_PULSE = 2 [(protobuf.digitalbuildings.annotations.v1.canonical_type) = {
    guid: "0d030780-2212-41e0-8b7e-664db9f4caf2"
    qualified_type: "METERS/WM_PULSE"
    implemented_types: "METERS/WM"
    uses: "water_flowrate_counter"
    uses: "water_volume_accumulator"
    opt_uses: "flowrate_sensor"
  }];

  // Standard building water meter.
  //
  // Ontology type `METERS/WM_STANDARD`.
  WATER_METER_TYPE_WM_STANDARD = 3 [(protobuf.digitalbuildings.annotations.v1.canonical_type) = {
    guid: "7cbdf4a9-82f0-4dd5-991f-14984cbce4cb"
    qualified_type: "METERS/WM_STANDARD"
    implemented_types: "METERS/WM"
    uses: "water_volume_accumulator"
    opt_uses: "failed_alarm"
    opt_uses: "flowrate_sensor"
    opt_uses: "water_temperature_sensor"
  }];

  // Rainwater treatment system meter.
  //
  // Ontology type `METERS/WM_STORM`.
  WATER_METER_TYPE_WM_STORM = 4 [(protobuf.digitalbuildings.annotations.v1.canonical_type) = {
    guid: "ed623d59-c0a5-4839-92cf-fb593dbd697d"
    qualified_type: "METERS/WM_STORM"
    implemented_types: "METERS/WM"
    uses: "mixing_tank_level_sensor"
    uses: "potable_water_volume_accumulator"
    uses: "rain_water_volume_accumulator"
    uses: "water_volume_accumulator"
  }];

  // Water supply flow rate meter with counter.
  //
  // Ontology type `METERS/WM_WF`.
  WATER_METER_TYPE_WM_WF = 5 [(protobuf.digitalbuildings.annotations.v1.canonical_type) = {
    guid: "df942136-4b98-4da7-9e68-acc210d6cf03"
    qualified_type: "METERS/WM_WF"
    implemented_types: "METERS/WM"
    uses: "supply_water_flowrate_sensor"
    opt_uses: "supply_water_volume_accumulator"
    opt_uses: "water_linearvelocity_sensor"
    opt_uses: "water_volume_accumulator"
  }];
}
