// Copyright 2026 RegaliumOS™.
// SPDX-License-Identifier: Apache-2.0
//
// Generated by sync from Google's Digital Buildings ontology,
// revision 2026-09-09 (4a794ac).
// Portions Copyright 2020 Google LLC, licensed under Apache-2.0.
// DO NOT EDIT.
//
// Regenerate with: just sync
// Revision history: docs/spec.md

syntax = "proto3";

package protobuf.digitalbuildings.meters.gas_meter.v1;

import "google/api/annotations.proto";
import "google/api/client.proto";
import "google/protobuf/empty.proto";
import "protobuf/digitalbuildings/meters/gas_meter/v1/gas_meter.proto";
import "protobuf/digitalbuildings/meters/gas_meter/v1/messages.proto";

option java_multiple_files = true;
option java_outer_classname = "ServiceProto";
option java_package = "com.regalium.protobuf.digitalbuildings.meters.gas_meter.v1";

// Manages gas meters.
//
// AIP-131 names the service for the collection, not `GasMeterService`: that
// is buf STANDARD's convention and the two contradict, which is why buf.yaml
// selects BASIC and lets api-linter own API design (rule 1).
service GasMeters {
  option (google.api.default_host) = "digitalbuildings.googleapis.com";

  // Returns one gas meter.
  rpc GetGasMeter(GetGasMeterRequest) returns (GasMeter) {
    option (google.api.http) = {get: "/v1/{name=buildings/*/gasMeters/*}"};
    option (google.api.method_signature) = "name";
  }

  // Lists the gas meters in a building.
  rpc ListGasMeters(ListGasMetersRequest) returns (ListGasMetersResponse) {
    option (google.api.http) = {get: "/v1/{parent=buildings/*}/gasMeters"};
    option (google.api.method_signature) = "parent";
  }

  // Creates a gas meter.
  rpc CreateGasMeter(CreateGasMeterRequest) returns (GasMeter) {
    option (google.api.http) = {
      post: "/v1/{parent=buildings/*}/gasMeters"
      body: "gas_meter"
    };
    option (google.api.method_signature) = "parent,gas_meter,gas_meter_id";
  }

  // Updates a gas meter.
  //
  // Rule 8: an update_mask naming an OUTPUT_ONLY field is rejected rather
  // than silently ignored. Most fields here are OUTPUT_ONLY -- the equipment
  // reports them and no API call sets them.
  rpc UpdateGasMeter(UpdateGasMeterRequest) returns (GasMeter) {
    option (google.api.http) = {
      patch: "/v1/{gas_meter.name=buildings/*/gasMeters/*}"
      body: "gas_meter"
    };
    option (google.api.method_signature) = "gas_meter,update_mask";
  }

  // Deletes a gas meter.
  rpc DeleteGasMeter(DeleteGasMeterRequest) returns (google.protobuf.Empty) {
    option (google.api.http) = {delete: "/v1/{name=buildings/*/gasMeters/*}"};
    option (google.api.method_signature) = "name";
  }

  // Restores a deleted gas meter (AIP-164 <https://aip.dev/164>).
  rpc UndeleteGasMeter(UndeleteGasMeterRequest) returns (GasMeter) {
    option (google.api.http) = {
      post: "/v1/{name=buildings/*/gasMeters/*}:undelete"
      body: "*"
    };
    option (google.api.method_signature) = "name";
  }
}
