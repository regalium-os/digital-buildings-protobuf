// Copyright 2026 RegaliumOS™.
// SPDX-License-Identifier: Apache-2.0
//
// Generated by sync from Google's Digital Buildings ontology,
// revision 2026-09-09 (4a794ac).
// Portions Copyright 2020 Google LLC, licensed under Apache-2.0.
// DO NOT EDIT.
//
// Regenerate with: just sync
// Revision history: docs/spec.md

syntax = "proto3";

package protobuf.digitalbuildings.meters.electrical_meter.v1;

import "google/api/annotations.proto";
import "google/api/client.proto";
import "google/protobuf/empty.proto";
import "protobuf/digitalbuildings/meters/electrical_meter/v1/electrical_meter.proto";
import "protobuf/digitalbuildings/meters/electrical_meter/v1/messages.proto";

option java_multiple_files = true;
option java_outer_classname = "ServiceProto";
option java_package = "com.regalium.protobuf.digitalbuildings.meters.electrical_meter.v1";

// Manages electrical meters.
//
// AIP-131 names the service for the collection, not
// `ElectricalMeterService`: that is buf STANDARD's convention and the two
// contradict, which is why buf.yaml selects BASIC and lets api-linter own
// API design (rule 1).
service ElectricalMeters {
  option (google.api.default_host) = "digitalbuildings.googleapis.com";

  // Returns one electrical meter.
  rpc GetElectricalMeter(GetElectricalMeterRequest) returns (ElectricalMeter) {
    option (google.api.http) = {get: "/v1/{name=buildings/*/electricalMeters/*}"};
    option (google.api.method_signature) = "name";
  }

  // Lists the electrical meters in a building.
  rpc ListElectricalMeters(ListElectricalMetersRequest) returns (ListElectricalMetersResponse) {
    option (google.api.http) = {get: "/v1/{parent=buildings/*}/electricalMeters"};
    option (google.api.method_signature) = "parent";
  }

  // Creates a electrical meter.
  rpc CreateElectricalMeter(CreateElectricalMeterRequest) returns (ElectricalMeter) {
    option (google.api.http) = {
      post: "/v1/{parent=buildings/*}/electricalMeters"
      body: "electrical_meter"
    };
    option (google.api.method_signature) = "parent,electrical_meter,electrical_meter_id";
  }

  // Updates a electrical meter.
  //
  // Rule 8: an update_mask naming an OUTPUT_ONLY field is rejected rather
  // than silently ignored. Most fields here are OUTPUT_ONLY -- the equipment
  // reports them and no API call sets them.
  rpc UpdateElectricalMeter(UpdateElectricalMeterRequest) returns (ElectricalMeter) {
    option (google.api.http) = {
      patch: "/v1/{electrical_meter.name=buildings/*/electricalMeters/*}"
      body: "electrical_meter"
    };
    option (google.api.method_signature) = "electrical_meter,update_mask";
  }

  // Deletes a electrical meter.
  rpc DeleteElectricalMeter(DeleteElectricalMeterRequest) returns (google.protobuf.Empty) {
    option (google.api.http) = {delete: "/v1/{name=buildings/*/electricalMeters/*}"};
    option (google.api.method_signature) = "name";
  }

  // Restores a deleted electrical meter (AIP-164 <https://aip.dev/164>).
  rpc UndeleteElectricalMeter(UndeleteElectricalMeterRequest) returns (ElectricalMeter) {
    option (google.api.http) = {
      post: "/v1/{name=buildings/*/electricalMeters/*}:undelete"
      body: "*"
    };
    option (google.api.method_signature) = "name";
  }
}
