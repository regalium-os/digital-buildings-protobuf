// Copyright 2026 RegaliumOS™.
// SPDX-License-Identifier: Apache-2.0
//
// Generated by sync from Google's Digital Buildings ontology,
// revision 2026-09-09 (4a794ac).
// Portions Copyright 2020 Google LLC, licensed under Apache-2.0.
// DO NOT EDIT.
//
// Regenerate with: just sync
// Revision history: docs/spec.md

syntax = "proto3";

package protobuf.digitalbuildings.meters.meter.v1;

import "google/api/annotations.proto";
import "google/api/client.proto";
import "google/protobuf/empty.proto";
import "protobuf/digitalbuildings/meters/meter/v1/messages.proto";
import "protobuf/digitalbuildings/meters/meter/v1/meter.proto";

option java_multiple_files = true;
option java_outer_classname = "ServiceProto";
option java_package = "com.regalium.protobuf.digitalbuildings.meters.meter.v1";

// Manages meters.
//
// AIP-131 names the service for the collection, not `MeterService`: that is
// buf STANDARD's convention and the two contradict, which is why buf.yaml
// selects BASIC and lets api-linter own API design (rule 1).
service Meters {
  option (google.api.default_host) = "digitalbuildings.googleapis.com";

  // Returns one meter.
  rpc GetMeter(GetMeterRequest) returns (Meter) {
    option (google.api.http) = {get: "/v1/{name=buildings/*/meters/*}"};
    option (google.api.method_signature) = "name";
  }

  // Lists the meters in a building.
  rpc ListMeters(ListMetersRequest) returns (ListMetersResponse) {
    option (google.api.http) = {get: "/v1/{parent=buildings/*}/meters"};
    option (google.api.method_signature) = "parent";
  }

  // Creates a meter.
  rpc CreateMeter(CreateMeterRequest) returns (Meter) {
    option (google.api.http) = {
      post: "/v1/{parent=buildings/*}/meters"
      body: "meter"
    };
    option (google.api.method_signature) = "parent,meter,meter_id";
  }

  // Updates a meter.
  //
  // Rule 8: an update_mask naming an OUTPUT_ONLY field is rejected rather
  // than silently ignored. Most fields here are OUTPUT_ONLY -- the equipment
  // reports them and no API call sets them.
  rpc UpdateMeter(UpdateMeterRequest) returns (Meter) {
    option (google.api.http) = {
      patch: "/v1/{meter.name=buildings/*/meters/*}"
      body: "meter"
    };
    option (google.api.method_signature) = "meter,update_mask";
  }

  // Deletes a meter.
  rpc DeleteMeter(DeleteMeterRequest) returns (google.protobuf.Empty) {
    option (google.api.http) = {delete: "/v1/{name=buildings/*/meters/*}"};
    option (google.api.method_signature) = "name";
  }

  // Restores a deleted meter (AIP-164 <https://aip.dev/164>).
  rpc UndeleteMeter(UndeleteMeterRequest) returns (Meter) {
    option (google.api.http) = {
      post: "/v1/{name=buildings/*/meters/*}:undelete"
      body: "*"
    };
    option (google.api.method_signature) = "name";
  }
}
