// Copyright 2026 RegaliumOS™.
// SPDX-License-Identifier: Apache-2.0
//
// Generated by sync from Google's Digital Buildings ontology,
// revision 2026-09-09 (4a794ac).
// Portions Copyright 2020 Google LLC, licensed under Apache-2.0.
// DO NOT EDIT.
//
// Regenerate with: just sync
// Revision history: docs/spec.md

syntax = "proto3";

package protobuf.digitalbuildings.transport.elevator.v1;

import "google/api/annotations.proto";
import "google/api/client.proto";
import "google/protobuf/empty.proto";
import "protobuf/digitalbuildings/transport/elevator/v1/elevator.proto";
import "protobuf/digitalbuildings/transport/elevator/v1/messages.proto";

option java_multiple_files = true;
option java_outer_classname = "ServiceProto";
option java_package = "com.regalium.protobuf.digitalbuildings.transport.elevator.v1";

// Manages elevators.
//
// AIP-131 names the service for the collection, not `ElevatorService`: that
// is buf STANDARD's convention and the two contradict, which is why buf.yaml
// selects BASIC and lets api-linter own API design (rule 1).
service Elevators {
  option (google.api.default_host) = "digitalbuildings.googleapis.com";

  // Returns one elevator.
  rpc GetElevator(GetElevatorRequest) returns (Elevator) {
    option (google.api.http) = {get: "/v1/{name=buildings/*/elevators/*}"};
    option (google.api.method_signature) = "name";
  }

  // Lists the elevators in a building.
  rpc ListElevators(ListElevatorsRequest) returns (ListElevatorsResponse) {
    option (google.api.http) = {get: "/v1/{parent=buildings/*}/elevators"};
    option (google.api.method_signature) = "parent";
  }

  // Creates a elevator.
  rpc CreateElevator(CreateElevatorRequest) returns (Elevator) {
    option (google.api.http) = {
      post: "/v1/{parent=buildings/*}/elevators"
      body: "elevator"
    };
    option (google.api.method_signature) = "parent,elevator,elevator_id";
  }

  // Updates a elevator.
  //
  // Rule 8: an update_mask naming an OUTPUT_ONLY field is rejected rather
  // than silently ignored. Most fields here are OUTPUT_ONLY -- the equipment
  // reports them and no API call sets them.
  rpc UpdateElevator(UpdateElevatorRequest) returns (Elevator) {
    option (google.api.http) = {
      patch: "/v1/{elevator.name=buildings/*/elevators/*}"
      body: "elevator"
    };
    option (google.api.method_signature) = "elevator,update_mask";
  }

  // Deletes a elevator.
  rpc DeleteElevator(DeleteElevatorRequest) returns (google.protobuf.Empty) {
    option (google.api.http) = {delete: "/v1/{name=buildings/*/elevators/*}"};
    option (google.api.method_signature) = "name";
  }

  // Restores a deleted elevator (AIP-164 <https://aip.dev/164>).
  rpc UndeleteElevator(UndeleteElevatorRequest) returns (Elevator) {
    option (google.api.http) = {
      post: "/v1/{name=buildings/*/elevators/*}:undelete"
      body: "*"
    };
    option (google.api.method_signature) = "name";
  }
}
