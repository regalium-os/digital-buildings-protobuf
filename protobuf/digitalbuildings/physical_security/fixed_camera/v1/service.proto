// Copyright 2026 RegaliumOS™.
// SPDX-License-Identifier: Apache-2.0
//
// Generated by sync from Google's Digital Buildings ontology,
// revision 2026-09-09 (4a794ac).
// Portions Copyright 2020 Google LLC, licensed under Apache-2.0.
// DO NOT EDIT.
//
// Regenerate with: just sync
// Revision history: docs/spec.md

syntax = "proto3";

package protobuf.digitalbuildings.physical_security.fixed_camera.v1;

import "google/api/annotations.proto";
import "google/api/client.proto";
import "google/protobuf/empty.proto";
import "protobuf/digitalbuildings/physical_security/fixed_camera/v1/fixed_camera.proto";
import "protobuf/digitalbuildings/physical_security/fixed_camera/v1/messages.proto";

option java_multiple_files = true;
option java_outer_classname = "ServiceProto";
option java_package = "com.regalium.protobuf.digitalbuildings.physical_security.fixed_camera.v1";

// Manages fixed cameras.
//
// AIP-131 names the service for the collection, not `FixedCameraService`:
// that is buf STANDARD's convention and the two contradict, which is why
// buf.yaml selects BASIC and lets api-linter own API design (rule 1).
service FixedCameras {
  option (google.api.default_host) = "digitalbuildings.googleapis.com";

  // Returns one fixed camera.
  rpc GetFixedCamera(GetFixedCameraRequest) returns (FixedCamera) {
    option (google.api.http) = {get: "/v1/{name=buildings/*/fixedCameras/*}"};
    option (google.api.method_signature) = "name";
  }

  // Lists the fixed cameras in a building.
  rpc ListFixedCameras(ListFixedCamerasRequest) returns (ListFixedCamerasResponse) {
    option (google.api.http) = {get: "/v1/{parent=buildings/*}/fixedCameras"};
    option (google.api.method_signature) = "parent";
  }

  // Creates a fixed camera.
  rpc CreateFixedCamera(CreateFixedCameraRequest) returns (FixedCamera) {
    option (google.api.http) = {
      post: "/v1/{parent=buildings/*}/fixedCameras"
      body: "fixed_camera"
    };
    option (google.api.method_signature) = "parent,fixed_camera,fixed_camera_id";
  }

  // Updates a fixed camera.
  //
  // Rule 8: an update_mask naming an OUTPUT_ONLY field is rejected rather
  // than silently ignored. Most fields here are OUTPUT_ONLY -- the equipment
  // reports them and no API call sets them.
  rpc UpdateFixedCamera(UpdateFixedCameraRequest) returns (FixedCamera) {
    option (google.api.http) = {
      patch: "/v1/{fixed_camera.name=buildings/*/fixedCameras/*}"
      body: "fixed_camera"
    };
    option (google.api.method_signature) = "fixed_camera,update_mask";
  }

  // Deletes a fixed camera.
  rpc DeleteFixedCamera(DeleteFixedCameraRequest) returns (google.protobuf.Empty) {
    option (google.api.http) = {delete: "/v1/{name=buildings/*/fixedCameras/*}"};
    option (google.api.method_signature) = "name";
  }

  // Restores a deleted fixed camera (AIP-164 <https://aip.dev/164>).
  rpc UndeleteFixedCamera(UndeleteFixedCameraRequest) returns (FixedCamera) {
    option (google.api.http) = {
      post: "/v1/{name=buildings/*/fixedCameras/*}:undelete"
      body: "*"
    };
    option (google.api.method_signature) = "name";
  }
}
