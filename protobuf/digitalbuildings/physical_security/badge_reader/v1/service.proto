// Copyright 2026 RegaliumOS™.
// SPDX-License-Identifier: Apache-2.0
//
// Generated by sync from Google's Digital Buildings ontology,
// revision 2026-09-09 (4a794ac).
// Portions Copyright 2020 Google LLC, licensed under Apache-2.0.
// DO NOT EDIT.
//
// Regenerate with: just sync
// Revision history: docs/spec.md

syntax = "proto3";

package protobuf.digitalbuildings.physical_security.badge_reader.v1;

import "google/api/annotations.proto";
import "google/api/client.proto";
import "google/protobuf/empty.proto";
import "protobuf/digitalbuildings/physical_security/badge_reader/v1/badge_reader.proto";
import "protobuf/digitalbuildings/physical_security/badge_reader/v1/messages.proto";

option java_multiple_files = true;
option java_outer_classname = "ServiceProto";
option java_package = "com.regalium.protobuf.digitalbuildings.physical_security.badge_reader.v1";

// Manages badge readers.
//
// AIP-131 names the service for the collection, not `BadgeReaderService`:
// that is buf STANDARD's convention and the two contradict, which is why
// buf.yaml selects BASIC and lets api-linter own API design (rule 1).
service BadgeReaders {
  option (google.api.default_host) = "digitalbuildings.googleapis.com";

  // Returns one badge reader.
  rpc GetBadgeReader(GetBadgeReaderRequest) returns (BadgeReader) {
    option (google.api.http) = {get: "/v1/{name=buildings/*/badgeReaders/*}"};
    option (google.api.method_signature) = "name";
  }

  // Lists the badge readers in a building.
  rpc ListBadgeReaders(ListBadgeReadersRequest) returns (ListBadgeReadersResponse) {
    option (google.api.http) = {get: "/v1/{parent=buildings/*}/badgeReaders"};
    option (google.api.method_signature) = "parent";
  }

  // Creates a badge reader.
  rpc CreateBadgeReader(CreateBadgeReaderRequest) returns (BadgeReader) {
    option (google.api.http) = {
      post: "/v1/{parent=buildings/*}/badgeReaders"
      body: "badge_reader"
    };
    option (google.api.method_signature) = "parent,badge_reader,badge_reader_id";
  }

  // Updates a badge reader.
  //
  // Rule 8: an update_mask naming an OUTPUT_ONLY field is rejected rather
  // than silently ignored. Most fields here are OUTPUT_ONLY -- the equipment
  // reports them and no API call sets them.
  rpc UpdateBadgeReader(UpdateBadgeReaderRequest) returns (BadgeReader) {
    option (google.api.http) = {
      patch: "/v1/{badge_reader.name=buildings/*/badgeReaders/*}"
      body: "badge_reader"
    };
    option (google.api.method_signature) = "badge_reader,update_mask";
  }

  // Deletes a badge reader.
  rpc DeleteBadgeReader(DeleteBadgeReaderRequest) returns (google.protobuf.Empty) {
    option (google.api.http) = {delete: "/v1/{name=buildings/*/badgeReaders/*}"};
    option (google.api.method_signature) = "name";
  }

  // Restores a deleted badge reader (AIP-164 <https://aip.dev/164>).
  rpc UndeleteBadgeReader(UndeleteBadgeReaderRequest) returns (BadgeReader) {
    option (google.api.http) = {
      post: "/v1/{name=buildings/*/badgeReaders/*}:undelete"
      body: "*"
    };
    option (google.api.method_signature) = "name";
  }
}
