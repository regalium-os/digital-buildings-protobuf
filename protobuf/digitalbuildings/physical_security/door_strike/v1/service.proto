// Copyright 2026 RegaliumOS™.
// SPDX-License-Identifier: Apache-2.0
//
// Generated by sync from Google's Digital Buildings ontology,
// revision 2026-09-09 (4a794ac).
// Portions Copyright 2020 Google LLC, licensed under Apache-2.0.
// DO NOT EDIT.
//
// Regenerate with: just sync
// Revision history: docs/spec.md

syntax = "proto3";

package protobuf.digitalbuildings.physical_security.door_strike.v1;

import "google/api/annotations.proto";
import "google/api/client.proto";
import "google/protobuf/empty.proto";
import "protobuf/digitalbuildings/physical_security/door_strike/v1/door_strike.proto";
import "protobuf/digitalbuildings/physical_security/door_strike/v1/messages.proto";

option java_multiple_files = true;
option java_outer_classname = "ServiceProto";
option java_package = "com.regalium.protobuf.digitalbuildings.physical_security.door_strike.v1";

// Manages door strikes.
//
// AIP-131 names the service for the collection, not `DoorStrikeService`:
// that is buf STANDARD's convention and the two contradict, which is why
// buf.yaml selects BASIC and lets api-linter own API design (rule 1).
service DoorStrikes {
  option (google.api.default_host) = "digitalbuildings.googleapis.com";

  // Returns one door strike.
  rpc GetDoorStrike(GetDoorStrikeRequest) returns (DoorStrike) {
    option (google.api.http) = {get: "/v1/{name=buildings/*/doorStrikes/*}"};
    option (google.api.method_signature) = "name";
  }

  // Lists the door strikes in a building.
  rpc ListDoorStrikes(ListDoorStrikesRequest) returns (ListDoorStrikesResponse) {
    option (google.api.http) = {get: "/v1/{parent=buildings/*}/doorStrikes"};
    option (google.api.method_signature) = "parent";
  }

  // Creates a door strike.
  rpc CreateDoorStrike(CreateDoorStrikeRequest) returns (DoorStrike) {
    option (google.api.http) = {
      post: "/v1/{parent=buildings/*}/doorStrikes"
      body: "door_strike"
    };
    option (google.api.method_signature) = "parent,door_strike,door_strike_id";
  }

  // Updates a door strike.
  //
  // Rule 8: an update_mask naming an OUTPUT_ONLY field is rejected rather
  // than silently ignored. Most fields here are OUTPUT_ONLY -- the equipment
  // reports them and no API call sets them.
  rpc UpdateDoorStrike(UpdateDoorStrikeRequest) returns (DoorStrike) {
    option (google.api.http) = {
      patch: "/v1/{door_strike.name=buildings/*/doorStrikes/*}"
      body: "door_strike"
    };
    option (google.api.method_signature) = "door_strike,update_mask";
  }

  // Deletes a door strike.
  rpc DeleteDoorStrike(DeleteDoorStrikeRequest) returns (google.protobuf.Empty) {
    option (google.api.http) = {delete: "/v1/{name=buildings/*/doorStrikes/*}"};
    option (google.api.method_signature) = "name";
  }

  // Restores a deleted door strike (AIP-164 <https://aip.dev/164>).
  rpc UndeleteDoorStrike(UndeleteDoorStrikeRequest) returns (DoorStrike) {
    option (google.api.http) = {
      post: "/v1/{name=buildings/*/doorStrikes/*}:undelete"
      body: "*"
    };
    option (google.api.method_signature) = "name";
  }
}
