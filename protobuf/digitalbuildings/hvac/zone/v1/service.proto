// Copyright 2026 RegaliumOS™.
// SPDX-License-Identifier: Apache-2.0
//
// Generated by sync from Google's Digital Buildings ontology,
// revision 2026-09-09 (4a794ac).
// Portions Copyright 2020 Google LLC, licensed under Apache-2.0.
// DO NOT EDIT.
//
// Regenerate with: just sync
// Revision history: docs/spec.md

syntax = "proto3";

package protobuf.digitalbuildings.hvac.zone.v1;

import "google/api/annotations.proto";
import "google/api/client.proto";
import "google/protobuf/empty.proto";
import "protobuf/digitalbuildings/hvac/zone/v1/messages.proto";
import "protobuf/digitalbuildings/hvac/zone/v1/zone.proto";

option java_multiple_files = true;
option java_outer_classname = "ServiceProto";
option java_package = "com.regalium.protobuf.digitalbuildings.hvac.zone.v1";

// Manages zones.
//
// AIP-131 names the service for the collection, not `ZoneService`: that is
// buf STANDARD's convention and the two contradict, which is why buf.yaml
// selects BASIC and lets api-linter own API design (rule 1).
service Zones {
  option (google.api.default_host) = "digitalbuildings.googleapis.com";

  // Returns one zone.
  rpc GetZone(GetZoneRequest) returns (Zone) {
    option (google.api.http) = {get: "/v1/{name=buildings/*/zones/*}"};
    option (google.api.method_signature) = "name";
  }

  // Lists the zones in a building.
  rpc ListZones(ListZonesRequest) returns (ListZonesResponse) {
    option (google.api.http) = {get: "/v1/{parent=buildings/*}/zones"};
    option (google.api.method_signature) = "parent";
  }

  // Creates a zone.
  rpc CreateZone(CreateZoneRequest) returns (Zone) {
    option (google.api.http) = {
      post: "/v1/{parent=buildings/*}/zones"
      body: "zone"
    };
    option (google.api.method_signature) = "parent,zone,zone_id";
  }

  // Updates a zone.
  //
  // Rule 8: an update_mask naming an OUTPUT_ONLY field is rejected rather
  // than silently ignored. Most fields here are OUTPUT_ONLY -- the equipment
  // reports them and no API call sets them.
  rpc UpdateZone(UpdateZoneRequest) returns (Zone) {
    option (google.api.http) = {
      patch: "/v1/{zone.name=buildings/*/zones/*}"
      body: "zone"
    };
    option (google.api.method_signature) = "zone,update_mask";
  }

  // Deletes a zone.
  rpc DeleteZone(DeleteZoneRequest) returns (google.protobuf.Empty) {
    option (google.api.http) = {delete: "/v1/{name=buildings/*/zones/*}"};
    option (google.api.method_signature) = "name";
  }

  // Restores a deleted zone (AIP-164 <https://aip.dev/164>).
  rpc UndeleteZone(UndeleteZoneRequest) returns (Zone) {
    option (google.api.http) = {
      post: "/v1/{name=buildings/*/zones/*}:undelete"
      body: "*"
    };
    option (google.api.method_signature) = "name";
  }
}
