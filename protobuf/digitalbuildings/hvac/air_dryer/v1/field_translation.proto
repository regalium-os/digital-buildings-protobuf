// Copyright 2026 RegaliumOS™.
// SPDX-License-Identifier: Apache-2.0
//
// Generated by sync from Google's Digital Buildings ontology,
// revision 2026-09-09 (4a794ac).
// Portions Copyright 2020 Google LLC, licensed under Apache-2.0.
// DO NOT EDIT.
//
// Regenerate with: just sync
// Revision history: docs/spec.md

syntax = "proto3";

package protobuf.digitalbuildings.hvac.air_dryer.v1;

import "google/api/field_behavior.proto";

option java_multiple_files = true;
option java_outer_classname = "FieldTranslationProto";
option java_package = "com.regalium.protobuf.digitalbuildings.hvac.air_dryer.v1";

// How one native payload point maps onto a standard field.
//
// The building config's `translation` block. A real device's payload differs
// from the modelled type in field names, values, units and multistate
// definitions, and this is where that is reconciled.
message FieldTranslation {
  // The standard field literal this maps onto.
  string field = 1 [(google.api.field_behavior) = REQUIRED];

  // The enumeration index, where the entity has more than one point of this
  // field.
  //
  // Rule 12: the ontology's `_1`, `_2` increment is instance data that
  // leaked into the vocabulary, so the schema emits the base field once as
  // repeated and the index lives here. A device reporting `_1` and `_7` and
  // nothing between records that fact in this field, and the schema does not
  // change when it grows a third point.
  int32 index = 2 [(google.api.field_behavior) = OPTIONAL];

  // Path to the value in the device's native payload, e.g.
  // `points.temp_1.present_value`.
  string present_value = 3 [(google.api.field_behavior) = OPTIONAL];

  // Whether the device lacks this field entirely. The building config writes
  // `MISSING`.
  bool missing = 4 [(google.api.field_behavior) = OPTIONAL];

  // The device's native unit token, mapped to the ontology's standard unit.
  repeated UnitMapping units = 5 [(google.api.field_behavior) = OPTIONAL];

  // The device's native state values, mapped to standard states.
  repeated StateMapping states = 6 [(google.api.field_behavior) = OPTIONAL];

  // The expected range of the value, in the standard unit.
  optional ValueRange value_range = 7 [(google.api.field_behavior) = OPTIONAL];
}

// A device's native unit token and what it means.
message UnitMapping {
  // The ontology's unit name, e.g. `degrees_celsius`.
  string unit = 1 [(google.api.field_behavior) = REQUIRED];

  // The token the device's payload uses, e.g. `degC`.
  string native_value = 2 [(google.api.field_behavior) = REQUIRED];
}

// A device's native state value and what it means.
message StateMapping {
  // The ontology's state name, e.g. `OPEN`.
  string state = 1 [(google.api.field_behavior) = REQUIRED];

  // The values the device reports for it. More than one native value may map
  // to the same standard state.
  repeated string native_values = 2 [(google.api.field_behavior) = REQUIRED];
}

// An expected minimum and maximum, in the standard unit.
message ValueRange {
  // Lower bound.
  double min_value = 1 [(google.api.field_behavior) = REQUIRED];

  // Upper bound.
  double max_value = 2 [(google.api.field_behavior) = REQUIRED];
}
