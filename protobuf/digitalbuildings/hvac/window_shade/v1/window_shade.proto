// Copyright 2026 RegaliumOS™.
// SPDX-License-Identifier: Apache-2.0
//
// Generated by sync from Google's Digital Buildings ontology,
// revision 2026-09-09 (4a794ac).
// Portions Copyright 2020 Google LLC, licensed under Apache-2.0.
// DO NOT EDIT.
//
// Regenerate with: just sync
// Revision history: docs/spec.md

syntax = "proto3";

package protobuf.digitalbuildings.hvac.window_shade.v1;

import "buf/validate/validate.proto";
import "google/api/field_behavior.proto";
import "google/api/field_info.proto";
import "google/api/resource.proto";
import "protobuf/digitalbuildings/annotations/v1/annotations.proto";
import "protobuf/digitalbuildings/hvac/window_shade/v1/active_inactive_value.proto";
import "protobuf/digitalbuildings/hvac/window_shade/v1/auto_day_night_manual_thermal_window_open_value.proto";
import "protobuf/digitalbuildings/hvac/window_shade/v1/connection.proto";
import "protobuf/digitalbuildings/hvac/window_shade/v1/east_north_south_west_value.proto";
import "protobuf/digitalbuildings/hvac/window_shade/v1/field_translation.proto";
import "protobuf/digitalbuildings/hvac/window_shade/v1/link.proto";
import "protobuf/digitalbuildings/hvac/window_shade/v1/off_on_value.proto";
import "protobuf/digitalbuildings/hvac/window_shade/v1/window_shade_type.proto";

option java_multiple_files = true;
option java_outer_classname = "WindowShadeProto";
option java_package = "com.regalium.protobuf.digitalbuildings.hvac.window_shade.v1";

// Tag for automated window shades. These shades are typically mechnically
// controlled and can be protracted or retracted to control the amount of
// sunlight coming through the windows into a space.
//
// Ontology entity type `HVAC/SDC`, a general type carrying 12 canonical
// variant(s). Rule 7: the general type is the resource and each variant is a
// value of WindowShadeType, so this message holds the union of every field
// any variant uses -- all of them optional.
//
// Reference: Digital Buildings abstract model.
// https://github.com/google/digitalbuildings/blob/master/ontology/docs/model.md
message WindowShade {
  option (google.api.resource) = {
    type: "digitalbuildings.googleapis.com/WindowShade"
    pattern: "buildings/{building}/windowShades/{window_shade}"
    singular: "windowShade"
    plural: "windowShades"
  };
  option (protobuf.digitalbuildings.annotations.v1.entity_type) = {
    guid: "25506deb-aa79-4eb1-9f48-173091472765"
    namespace: "HVAC"
    general_type: "SDC"
  };

  // Resource name, "buildings/{building}/windowShades/{window_shade}".
  // Assigned by the server.
  string name = 1 [
    (google.api.field_behavior) = IDENTIFIER,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The ontology's GUID for this entity.
  //
  // Rule 10: this is the ontology's own identifier, not a second one minted
  // beside it. Digital Buildings assigns a stable UUID to every entity and
  // it survives renames, which is exactly what AIP-148 asks a `uid` to be.
  string uid = 2 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (google.api.field_info).format = UUID4,
    (buf.validate.field).string.uuid = true,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The building config's human-readable entity code, e.g. `FCU-123`.
  //
  // Locally unique rather than globally, chosen at design time, and it must
  // survive a round trip unchanged or every re-import duplicates the entity.
  // The old building-config format keys entities by this; the new one keys
  // them by `uid`. Both are representable because both exist.
  string code = 3 [
    (google.api.field_behavior) = OPTIONAL,
    (google.api.field_behavior) = IMMUTABLE,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The canonical type this entity conforms to.
  //
  // The variant's exact field sets ride on the enum value in
  // (annotations.canonical_type), so a validator can reject an entity
  // reporting a field its declared type does not have.
  WindowShadeType entity_type = 4 [(google.api.field_behavior) = OPTIONAL];

  // Directed relationships to other entities.
  //
  // Rule 4: a connection is not a field. AIP-215 forbids referencing a
  // message in another proto package, and the other end of a connection may
  // be any resource in any namespace -- so it is carried as a resource name
  // with a wildcard reference.
  repeated Connection connections = 5 [(google.api.field_behavior) = OPTIONAL];

  // Standard fields mapped from another entity's telemetry.
  //
  // The building config's `links` block: a virtual entity reports data
  // linked from reporting devices rather than emitting it itself.
  repeated Link links = 6 [(google.api.field_behavior) = OPTIONAL];

  // How this entity's native payload maps onto the standard fields, when it
  // reports telemetry directly.
  repeated FieldTranslation translations = 7 [(google.api.field_behavior) = OPTIONAL];

  // Held for the fields on every read path (rule 9).
  reserved 8 to 15;

  // The east illuminance sensor.
  //
  // Composed of:
  //   east (descriptor): Cardinal direction; opposite of west.
  //   illuminance (measurement): Measurement of light.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `east_illuminance_sensor`, point type `sensor`. Measured
  // as illuminance; values are in lux, the ontology's standard unit for that
  // quantity.
  //
  // Expected range 0 to 5000. This is a data-quality expectation, not a
  // constraint: a reading outside it is a fault to report, not a message to
  // reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double east_illuminance_sensor = 16 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "east_illuminance_sensor"
      subfields: "east"
      subfields: "illuminance"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "illuminance"
      standard_unit: "lux"
      expected_min: 0.0
      expected_max: 5000.0
    }
  ];

  // The east wind linearvelocity sensor.
  //
  // Composed of:
  //   east (descriptor): Cardinal direction; opposite of west.
  //   wind (descriptor): Movement of ambient air.
  //   linearvelocity (measurement): The rate of change of displacement
  //     with respect to time. For example, wind speed represents the linear
  //     velocity of the wind.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `east_wind_linearvelocity_sensor`, point type `sensor`.
  // Measured as linearvelocity; values are in meters_per_second, the
  // ontology's standard unit for that quantity.
  //
  // Expected range 0 to 44.704. This is a data-quality expectation, not a
  // constraint: a reading outside it is a fault to report, not a message to
  // reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  repeated double east_wind_linearvelocity_sensor = 17 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "east_wind_linearvelocity_sensor"
      subfields: "east"
      subfields: "wind"
      subfields: "linearvelocity"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "linearvelocity"
      standard_unit: "meters_per_second"
      enumerated: true
      expected_min: 0.0
      expected_max: 44.704
    }
  ];

  // The failed alarm.
  //
  // Composed of:
  //   failed (descriptor): Indicates that a point is not operating as
  //     intended or is incapable of operating.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `failed_alarm`, point type `alarm`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue failed_alarm = 18 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "failed_alarm"
      subfields: "failed"
      subfields: "alarm"
      point_type: "alarm"
    }
  ];

  // The failed communication alarm.
  //
  // Composed of:
  //   failed (descriptor): Indicates that a point is not operating as
  //     intended or is incapable of operating.
  //   communication (descriptor): Actual transmit and recieve of data or
  //     connectivity between devices.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `failed_communication_alarm`, point type `alarm`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue failed_communication_alarm = 19 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "failed_communication_alarm"
      subfields: "failed"
      subfields: "communication"
      subfields: "alarm"
      point_type: "alarm"
    }
  ];

  // The high limit illuminance setpoint.
  //
  // Composed of:
  //   high (descriptor): Level of control or measurement; above medium and
  //     low; opposite of low.
  //   limit (descriptor): A boundary condition for a control (e.g., low
  //     limit).
  //   illuminance (measurement): Measurement of light.
  //   setpoint (point_type): Control target of process or system.
  //
  // Ontology field: `high_limit_illuminance_setpoint`, point type
  // `setpoint`. Measured as illuminance; values are in lux, the ontology's
  // standard unit for that quantity.
  //
  // Expected range 0 to 5000. This is a data-quality expectation, not a
  // constraint: a reading outside it is a fault to report, not a message to
  // reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double high_limit_illuminance_setpoint = 20 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "high_limit_illuminance_setpoint"
      subfields: "high"
      subfields: "limit"
      subfields: "illuminance"
      subfields: "setpoint"
      point_type: "setpoint"
      measurement: "illuminance"
      standard_unit: "lux"
      expected_min: 0.0
      expected_max: 5000.0
    }
  ];

  // The low limit illuminance setpoint.
  //
  // Composed of:
  //   low (descriptor): Level of control or measurement; below medium and
  //     high; opposite of high.
  //   limit (descriptor): A boundary condition for a control (e.g., low
  //     limit).
  //   illuminance (measurement): Measurement of light.
  //   setpoint (point_type): Control target of process or system.
  //
  // Ontology field: `low_limit_illuminance_setpoint`, point type `setpoint`.
  // Measured as illuminance; values are in lux, the ontology's standard unit
  // for that quantity.
  //
  // Expected range 0 to 5000. This is a data-quality expectation, not a
  // constraint: a reading outside it is a fault to report, not a message to
  // reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double low_limit_illuminance_setpoint = 21 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "low_limit_illuminance_setpoint"
      subfields: "low"
      subfields: "limit"
      subfields: "illuminance"
      subfields: "setpoint"
      point_type: "setpoint"
      measurement: "illuminance"
      standard_unit: "lux"
      expected_min: 0.0
      expected_max: 5000.0
    }
  ];

  // The manufacturer label.
  //
  // Composed of:
  //   manufacturer (descriptor): The company which produces the asset or
  //     device.
  //   label (point_type): Identifying alias for component or system.
  //
  // Ontology field: `manufacturer_label`, point type `label`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional string manufacturer_label = 22 [
    (google.api.field_behavior) = OPTIONAL,
    (google.api.field_behavior) = IMMUTABLE,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "manufacturer_label"
      subfields: "manufacturer"
      subfields: "label"
      point_type: "label"
    }
  ];

  // The model label.
  //
  // Composed of:
  //   model (descriptor): Particular design version of an asset (e.g.
  //     model_label).
  //   label (point_type): Identifying alias for component or system.
  //
  // Ontology field: `model_label`, point type `label`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional string model_label = 23 [
    (google.api.field_behavior) = OPTIONAL,
    (google.api.field_behavior) = IMMUTABLE,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "model_label"
      subfields: "model"
      subfields: "label"
      point_type: "label"
    }
  ];

  // The north wind linearvelocity sensor.
  //
  // Composed of:
  //   north (descriptor): Cardinal direction; opposite of south
  //   wind (descriptor): Movement of ambient air.
  //   linearvelocity (measurement): The rate of change of displacement
  //     with respect to time. For example, wind speed represents the linear
  //     velocity of the wind.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `north_wind_linearvelocity_sensor`, point type `sensor`.
  // Measured as linearvelocity; values are in meters_per_second, the
  // ontology's standard unit for that quantity.
  //
  // Expected range 0 to 44.704. This is a data-quality expectation, not a
  // constraint: a reading outside it is a fault to report, not a message to
  // reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  repeated double north_wind_linearvelocity_sensor = 24 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "north_wind_linearvelocity_sensor"
      subfields: "north"
      subfields: "wind"
      subfields: "linearvelocity"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "linearvelocity"
      standard_unit: "meters_per_second"
      enumerated: true
      expected_min: 0.0
      expected_max: 44.704
    }
  ];

  // The outside illuminance sensor.
  //
  // Composed of:
  //   outside (descriptor): Process or measurement of local atmospheric
  //     conditions.
  //   illuminance (measurement): Measurement of light.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `outside_illuminance_sensor`, point type `sensor`.
  // Measured as illuminance; values are in lux, the ontology's standard unit
  // for that quantity.
  //
  // Expected range 0 to 5000. This is a data-quality expectation, not a
  // constraint: a reading outside it is a fault to report, not a message to
  // reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double outside_illuminance_sensor = 25 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "outside_illuminance_sensor"
      subfields: "outside"
      subfields: "illuminance"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "illuminance"
      standard_unit: "lux"
      expected_min: 0.0
      expected_max: 5000.0
    }
  ];

  // The shade down command.
  //
  // Composed of:
  //   shade (component): Window covering (typically used to regulate the
  //     amount of sunlight coming through the window).
  //   down (measurement_descriptor): The direction of a spatial movement,
  //     opposite of up.
  //   command (point_type): The signal given to make an action happen.
  //     Defaults to multistate unless used alongside a measurement_type.
  //
  // Ontology field: `shade_down_command`, point type `command`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional OffOnValue shade_down_command = 26 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "shade_down_command"
      subfields: "shade"
      subfields: "down"
      subfields: "command"
      point_type: "command"
    }
  ];

  // The shade extent percentage command.
  //
  // Composed of:
  //   shade (component): Window covering (typically used to regulate the
  //     amount of sunlight coming through the window).
  //   extent (measurement_descriptor): The range over which something has
  //     spread.
  //   percentage (measurement): Measurement per hundred.
  //   command (point_type): The signal given to make an action happen.
  //     Defaults to multistate unless used alongside a measurement_type.
  //
  // Ontology field: `shade_extent_percentage_command`, point type `command`.
  // Measured as percentage; values are in percent, the ontology's standard
  // unit for that quantity.
  //
  // Valid range 0 to 100, enforced by protovalidate.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double shade_extent_percentage_command = 27 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "shade_extent_percentage_command"
      subfields: "shade"
      subfields: "extent"
      subfields: "percentage"
      subfields: "command"
      point_type: "command"
      measurement: "percentage"
      standard_unit: "percent"
    },
    (buf.validate.field).double.gte = 0.0,
    (buf.validate.field).double.lte = 100.0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The shade extent percentage sensor.
  //
  // Composed of:
  //   shade (component): Window covering (typically used to regulate the
  //     amount of sunlight coming through the window).
  //   extent (measurement_descriptor): The range over which something has
  //     spread.
  //   percentage (measurement): Measurement per hundred.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `shade_extent_percentage_sensor`, point type `sensor`.
  // Measured as percentage; values are in percent, the ontology's standard
  // unit for that quantity.
  //
  // Valid range 0 to 100, enforced by protovalidate.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double shade_extent_percentage_sensor = 28 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "shade_extent_percentage_sensor"
      subfields: "shade"
      subfields: "extent"
      subfields: "percentage"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "percentage"
      standard_unit: "percent"
    },
    (buf.validate.field).double.gte = 0.0,
    (buf.validate.field).double.lte = 100.0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The shade mode.
  //
  // Composed of:
  //   shade (component): Window covering (typically used to regulate the
  //     amount of sunlight coming through the window).
  //   mode (point_type): Distinct mode of operation within system. For
  //     example economizer_mode has modes of 'enabled' or 'disabled.'
  //
  // Ontology field: `shade_mode`, point type `mode`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional AutoDayNightManualThermalWindowOpenValue shade_mode = 29 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "shade_mode"
      subfields: "shade"
      subfields: "mode"
      point_type: "mode"
    }
  ];

  // The shade orientation command.
  //
  // Composed of:
  //   shade (component): Window covering (typically used to regulate the
  //     amount of sunlight coming through the window).
  //   orientation (measurement_descriptor): Description of cardinal
  //     direction.
  //   command (point_type): The signal given to make an action happen.
  //     Defaults to multistate unless used alongside a measurement_type.
  //
  // Ontology field: `shade_orientation_command`, point type `command`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional EastNorthSouthWestValue shade_orientation_command = 30 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "shade_orientation_command"
      subfields: "shade"
      subfields: "orientation"
      subfields: "command"
      point_type: "command"
    }
  ];

  // The shade tilt percentage command.
  //
  // Composed of:
  //   shade (component): Window covering (typically used to regulate the
  //     amount of sunlight coming through the window).
  //   tilt (measurement_descriptor): Degree of radial rotation.
  //   percentage (measurement): Measurement per hundred.
  //   command (point_type): The signal given to make an action happen.
  //     Defaults to multistate unless used alongside a measurement_type.
  //
  // Ontology field: `shade_tilt_percentage_command`, point type `command`.
  // Measured as percentage; values are in percent, the ontology's standard
  // unit for that quantity.
  //
  // Valid range 0 to 100, enforced by protovalidate.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double shade_tilt_percentage_command = 31 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "shade_tilt_percentage_command"
      subfields: "shade"
      subfields: "tilt"
      subfields: "percentage"
      subfields: "command"
      point_type: "command"
      measurement: "percentage"
      standard_unit: "percent"
    },
    (buf.validate.field).double.gte = 0.0,
    (buf.validate.field).double.lte = 100.0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The shade up command.
  //
  // Composed of:
  //   shade (component): Window covering (typically used to regulate the
  //     amount of sunlight coming through the window).
  //   up (measurement_descriptor): The direction of a spatial movement,
  //     opposite of down.
  //   command (point_type): The signal given to make an action happen.
  //     Defaults to multistate unless used alongside a measurement_type.
  //
  // Ontology field: `shade_up_command`, point type `command`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional OffOnValue shade_up_command = 32 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "shade_up_command"
      subfields: "shade"
      subfields: "up"
      subfields: "command"
      point_type: "command"
    }
  ];

  // The south illuminance sensor.
  //
  // Composed of:
  //   south (descriptor): Cardinal direction; opposite of north
  //   illuminance (measurement): Measurement of light.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `south_illuminance_sensor`, point type `sensor`.
  // Measured as illuminance; values are in lux, the ontology's standard unit
  // for that quantity.
  //
  // Expected range 0 to 5000. This is a data-quality expectation, not a
  // constraint: a reading outside it is a fault to report, not a message to
  // reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double south_illuminance_sensor = 33 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "south_illuminance_sensor"
      subfields: "south"
      subfields: "illuminance"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "illuminance"
      standard_unit: "lux"
      expected_min: 0.0
      expected_max: 5000.0
    }
  ];

  // The south wind linearvelocity sensor.
  //
  // Composed of:
  //   south (descriptor): Cardinal direction; opposite of north
  //   wind (descriptor): Movement of ambient air.
  //   linearvelocity (measurement): The rate of change of displacement
  //     with respect to time. For example, wind speed represents the linear
  //     velocity of the wind.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `south_wind_linearvelocity_sensor`, point type `sensor`.
  // Measured as linearvelocity; values are in meters_per_second, the
  // ontology's standard unit for that quantity.
  //
  // Expected range 0 to 44.704. This is a data-quality expectation, not a
  // constraint: a reading outside it is a fault to report, not a message to
  // reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  repeated double south_wind_linearvelocity_sensor = 34 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "south_wind_linearvelocity_sensor"
      subfields: "south"
      subfields: "wind"
      subfields: "linearvelocity"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "linearvelocity"
      standard_unit: "meters_per_second"
      enumerated: true
      expected_min: 0.0
      expected_max: 44.704
    }
  ];

  // The southeast illuminance sensor.
  //
  // Composed of:
  //   southeast (descriptor): Inter-cardinal direction between South and
  //     East.
  //   illuminance (measurement): Measurement of light.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `southeast_illuminance_sensor`, point type `sensor`.
  // Measured as illuminance; values are in lux, the ontology's standard unit
  // for that quantity.
  //
  // Expected range 0 to 5000. This is a data-quality expectation, not a
  // constraint: a reading outside it is a fault to report, not a message to
  // reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double southeast_illuminance_sensor = 35 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "southeast_illuminance_sensor"
      subfields: "southeast"
      subfields: "illuminance"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "illuminance"
      standard_unit: "lux"
      expected_min: 0.0
      expected_max: 5000.0
    }
  ];

  // The southwest illuminance sensor.
  //
  // Composed of:
  //   southwest (descriptor): Inter-cardinal direction between South and
  //     West.
  //   illuminance (measurement): Measurement of light.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `southwest_illuminance_sensor`, point type `sensor`.
  // Measured as illuminance; values are in lux, the ontology's standard unit
  // for that quantity.
  //
  // Expected range 0 to 5000. This is a data-quality expectation, not a
  // constraint: a reading outside it is a fault to report, not a message to
  // reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double southwest_illuminance_sensor = 36 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "southwest_illuminance_sensor"
      subfields: "southwest"
      subfields: "illuminance"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "illuminance"
      standard_unit: "lux"
      expected_min: 0.0
      expected_max: 5000.0
    }
  ];

  // The west illuminance sensor.
  //
  // Composed of:
  //   west (descriptor): Cardinal direction; opposite of east
  //   illuminance (measurement): Measurement of light.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `west_illuminance_sensor`, point type `sensor`. Measured
  // as illuminance; values are in lux, the ontology's standard unit for that
  // quantity.
  //
  // Expected range 0 to 5000. This is a data-quality expectation, not a
  // constraint: a reading outside it is a fault to report, not a message to
  // reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double west_illuminance_sensor = 37 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "west_illuminance_sensor"
      subfields: "west"
      subfields: "illuminance"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "illuminance"
      standard_unit: "lux"
      expected_min: 0.0
      expected_max: 5000.0
    }
  ];

  // The west wind linearvelocity sensor.
  //
  // Composed of:
  //   west (descriptor): Cardinal direction; opposite of east
  //   wind (descriptor): Movement of ambient air.
  //   linearvelocity (measurement): The rate of change of displacement
  //     with respect to time. For example, wind speed represents the linear
  //     velocity of the wind.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `west_wind_linearvelocity_sensor`, point type `sensor`.
  // Measured as linearvelocity; values are in meters_per_second, the
  // ontology's standard unit for that quantity.
  //
  // Expected range 0 to 44.704. This is a data-quality expectation, not a
  // constraint: a reading outside it is a fault to report, not a message to
  // reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  repeated double west_wind_linearvelocity_sensor = 38 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "west_wind_linearvelocity_sensor"
      subfields: "west"
      subfields: "wind"
      subfields: "linearvelocity"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "linearvelocity"
      standard_unit: "meters_per_second"
      enumerated: true
      expected_min: 0.0
      expected_max: 44.704
    }
  ];
}
