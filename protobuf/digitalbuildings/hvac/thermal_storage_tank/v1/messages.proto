// Copyright 2026 RegaliumOS™.
// SPDX-License-Identifier: Apache-2.0
//
// Generated by sync from Google's Digital Buildings ontology,
// revision 2026-09-09 (4a794ac).
// Portions Copyright 2020 Google LLC, licensed under Apache-2.0.
// DO NOT EDIT.
//
// Regenerate with: just sync
// Revision history: docs/spec.md

syntax = "proto3";

package protobuf.digitalbuildings.hvac.thermal_storage_tank.v1;

import "buf/validate/validate.proto";
import "google/api/field_behavior.proto";
import "google/api/resource.proto";
import "google/protobuf/field_mask.proto";
import "protobuf/digitalbuildings/hvac/thermal_storage_tank/v1/thermal_storage_tank.proto";

option java_multiple_files = true;
option java_outer_classname = "MessagesProto";
option java_package = "com.regalium.protobuf.digitalbuildings.hvac.thermal_storage_tank.v1";

// Request for `GetThermalStorageTank`.
message GetThermalStorageTankRequest {
  // The name of the thermal storage tank to return.
  string name = 1 [
    (google.api.field_behavior) = REQUIRED,
    (google.api.resource_reference).type = "digitalbuildings.googleapis.com/ThermalStorageTank",
    (buf.validate.field).string.min_len = 1
  ];
}

// Request for `ListThermalStorageTanks`.
message ListThermalStorageTanksRequest {
  // The building whose thermal storage tanks are listed.
  string parent = 1 [
    (google.api.field_behavior) = REQUIRED,
    (google.api.resource_reference).child_type = "digitalbuildings.googleapis.com/ThermalStorageTank",
    (buf.validate.field).string.min_len = 1
  ];

  // Maximum number to return. The server may return fewer.
  int32 page_size = 2 [
    (google.api.field_behavior) = OPTIONAL,
    (buf.validate.field).int32.gte = 0
  ];

  // A page token from a previous response.
  string page_token = 3 [(google.api.field_behavior) = OPTIONAL];
}

// Response for `ListThermalStorageTanks`.
message ListThermalStorageTanksResponse {
  // The page of results.
  repeated ThermalStorageTank thermal_storage_tanks = 1;

  // Token for the next page, empty if there are no more.
  string next_page_token = 2;
}

// Request for `CreateThermalStorageTank`.
message CreateThermalStorageTankRequest {
  // The building to create it in.
  string parent = 1 [
    (google.api.field_behavior) = REQUIRED,
    (google.api.resource_reference).child_type = "digitalbuildings.googleapis.com/ThermalStorageTank",
    (buf.validate.field).string.min_len = 1
  ];

  // The id to use, which becomes the last segment of the name. By convention
  // this is the building config's entity code.
  string thermal_storage_tank_id = 2 [(google.api.field_behavior) = OPTIONAL];

  // The resource to create.
  ThermalStorageTank thermal_storage_tank = 3 [(google.api.field_behavior) = REQUIRED];
}

// Request for `UpdateThermalStorageTank`.
message UpdateThermalStorageTankRequest {
  // The resource to update. Its `name` identifies it.
  ThermalStorageTank thermal_storage_tank = 1 [(google.api.field_behavior) = REQUIRED];

  // The fields to update.
  //
  // Rule 8: naming an OUTPUT_ONLY field here is rejected rather than
  // silently ignored. Only `command` and `setpoint` fields, and the metadata
  // fields at create time, can be written.
  google.protobuf.FieldMask update_mask = 2 [(google.api.field_behavior) = OPTIONAL];
}

// Request for `DeleteThermalStorageTank`.
message DeleteThermalStorageTankRequest {
  // The name of the thermal storage tank to delete.
  string name = 1 [
    (google.api.field_behavior) = REQUIRED,
    (google.api.resource_reference).type = "digitalbuildings.googleapis.com/ThermalStorageTank",
    (buf.validate.field).string.min_len = 1
  ];
}

// Request for `UndeleteThermalStorageTank`.
message UndeleteThermalStorageTankRequest {
  // The name of the thermal storage tank to restore.
  string name = 1 [
    (google.api.field_behavior) = REQUIRED,
    (google.api.resource_reference).type = "digitalbuildings.googleapis.com/ThermalStorageTank",
    (buf.validate.field).string.min_len = 1
  ];
}
