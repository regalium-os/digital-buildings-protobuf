// Copyright 2026 RegaliumOS™.
// SPDX-License-Identifier: Apache-2.0
//
// Generated by sync from Google's Digital Buildings ontology,
// revision 2026-09-09 (4a794ac).
// Portions Copyright 2020 Google LLC, licensed under Apache-2.0.
// DO NOT EDIT.
//
// Regenerate with: just sync
// Revision history: docs/spec.md

syntax = "proto3";

package protobuf.digitalbuildings.hvac.thermal_storage_tank.v1;

import "google/api/annotations.proto";
import "google/api/client.proto";
import "google/protobuf/empty.proto";
import "protobuf/digitalbuildings/hvac/thermal_storage_tank/v1/messages.proto";
import "protobuf/digitalbuildings/hvac/thermal_storage_tank/v1/thermal_storage_tank.proto";

option java_multiple_files = true;
option java_outer_classname = "ServiceProto";
option java_package = "com.regalium.protobuf.digitalbuildings.hvac.thermal_storage_tank.v1";

// Manages thermal storage tanks.
//
// AIP-131 names the service for the collection, not
// `ThermalStorageTankService`: that is buf STANDARD's convention and the two
// contradict, which is why buf.yaml selects BASIC and lets api-linter own
// API design (rule 1).
service ThermalStorageTanks {
  option (google.api.default_host) = "digitalbuildings.googleapis.com";

  // Returns one thermal storage tank.
  rpc GetThermalStorageTank(GetThermalStorageTankRequest) returns (ThermalStorageTank) {
    option (google.api.http) = {get: "/v1/{name=buildings/*/thermalStorageTanks/*}"};
    option (google.api.method_signature) = "name";
  }

  // Lists the thermal storage tanks in a building.
  rpc ListThermalStorageTanks(ListThermalStorageTanksRequest) returns (ListThermalStorageTanksResponse) {
    option (google.api.http) = {get: "/v1/{parent=buildings/*}/thermalStorageTanks"};
    option (google.api.method_signature) = "parent";
  }

  // Creates a thermal storage tank.
  rpc CreateThermalStorageTank(CreateThermalStorageTankRequest) returns (ThermalStorageTank) {
    option (google.api.http) = {
      post: "/v1/{parent=buildings/*}/thermalStorageTanks"
      body: "thermal_storage_tank"
    };
    option (google.api.method_signature) = "parent,thermal_storage_tank,thermal_storage_tank_id";
  }

  // Updates a thermal storage tank.
  //
  // Rule 8: an update_mask naming an OUTPUT_ONLY field is rejected rather
  // than silently ignored. Most fields here are OUTPUT_ONLY -- the equipment
  // reports them and no API call sets them.
  rpc UpdateThermalStorageTank(UpdateThermalStorageTankRequest) returns (ThermalStorageTank) {
    option (google.api.http) = {
      patch: "/v1/{thermal_storage_tank.name=buildings/*/thermalStorageTanks/*}"
      body: "thermal_storage_tank"
    };
    option (google.api.method_signature) = "thermal_storage_tank,update_mask";
  }

  // Deletes a thermal storage tank.
  rpc DeleteThermalStorageTank(DeleteThermalStorageTankRequest) returns (google.protobuf.Empty) {
    option (google.api.http) = {delete: "/v1/{name=buildings/*/thermalStorageTanks/*}"};
    option (google.api.method_signature) = "name";
  }

  // Restores a deleted thermal storage tank (AIP-164 <https://aip.dev/164>).
  rpc UndeleteThermalStorageTank(UndeleteThermalStorageTankRequest) returns (ThermalStorageTank) {
    option (google.api.http) = {
      post: "/v1/{name=buildings/*/thermalStorageTanks/*}:undelete"
      body: "*"
    };
    option (google.api.method_signature) = "name";
  }
}
