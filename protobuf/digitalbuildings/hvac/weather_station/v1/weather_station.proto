// Copyright 2026 RegaliumOS™.
// SPDX-License-Identifier: Apache-2.0
//
// Generated by sync from Google's Digital Buildings ontology,
// revision 2026-09-09 (4a794ac).
// Portions Copyright 2020 Google LLC, licensed under Apache-2.0.
// DO NOT EDIT.
//
// Regenerate with: just sync
// Revision history: docs/spec.md

syntax = "proto3";

package protobuf.digitalbuildings.hvac.weather_station.v1;

import "buf/validate/validate.proto";
import "google/api/field_behavior.proto";
import "google/api/field_info.proto";
import "google/api/resource.proto";
import "protobuf/digitalbuildings/annotations/v1/annotations.proto";
import "protobuf/digitalbuildings/hvac/weather_station/v1/active_inactive_value.proto";
import "protobuf/digitalbuildings/hvac/weather_station/v1/connection.proto";
import "protobuf/digitalbuildings/hvac/weather_station/v1/field_translation.proto";
import "protobuf/digitalbuildings/hvac/weather_station/v1/link.proto";
import "protobuf/digitalbuildings/hvac/weather_station/v1/weather_station_type.proto";

option java_multiple_files = true;
option java_outer_classname = "WeatherStationProto";
option java_package = "com.regalium.protobuf.digitalbuildings.hvac.weather_station.v1";

// Tag for weather stations.
//
// Ontology entity type `HVAC/WEATHER`, a general type carrying 2 canonical
// variant(s). Rule 7: the general type is the resource and each variant is a
// value of WeatherStationType, so this message holds the union of every
// field any variant uses -- all of them optional.
//
// Reference: Digital Buildings abstract model.
// https://github.com/google/digitalbuildings/blob/master/ontology/docs/model.md
message WeatherStation {
  option (google.api.resource) = {
    type: "digitalbuildings.googleapis.com/WeatherStation"
    pattern: "buildings/{building}/weatherStations/{weather_station}"
    singular: "weatherStation"
    plural: "weatherStations"
  };
  option (protobuf.digitalbuildings.annotations.v1.entity_type) = {
    guid: "328ecf20-f894-4524-a6ae-8fd83a35a95b"
    namespace: "HVAC"
    general_type: "WEATHER"
  };

  // Resource name, "buildings/{building}/weatherStations/{weather_station}".
  // Assigned by the server.
  string name = 1 [
    (google.api.field_behavior) = IDENTIFIER,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The ontology's GUID for this entity.
  //
  // Rule 10: this is the ontology's own identifier, not a second one minted
  // beside it. Digital Buildings assigns a stable UUID to every entity and
  // it survives renames, which is exactly what AIP-148 asks a `uid` to be.
  string uid = 2 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (google.api.field_info).format = UUID4,
    (buf.validate.field).string.uuid = true,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The building config's human-readable entity code, e.g. `FCU-123`.
  //
  // Locally unique rather than globally, chosen at design time, and it must
  // survive a round trip unchanged or every re-import duplicates the entity.
  // The old building-config format keys entities by this; the new one keys
  // them by `uid`. Both are representable because both exist.
  string code = 3 [
    (google.api.field_behavior) = OPTIONAL,
    (google.api.field_behavior) = IMMUTABLE,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The canonical type this entity conforms to.
  //
  // The variant's exact field sets ride on the enum value in
  // (annotations.canonical_type), so a validator can reject an entity
  // reporting a field its declared type does not have.
  WeatherStationType entity_type = 4 [(google.api.field_behavior) = OPTIONAL];

  // Directed relationships to other entities.
  //
  // Rule 4: a connection is not a field. AIP-215 forbids referencing a
  // message in another proto package, and the other end of a connection may
  // be any resource in any namespace -- so it is carried as a resource name
  // with a wildcard reference.
  repeated Connection connections = 5 [(google.api.field_behavior) = OPTIONAL];

  // Standard fields mapped from another entity's telemetry.
  //
  // The building config's `links` block: a virtual entity reports data
  // linked from reporting devices rather than emitting it itself.
  repeated Link links = 6 [(google.api.field_behavior) = OPTIONAL];

  // How this entity's native payload maps onto the standard fields, when it
  // reports telemetry directly.
  repeated FieldTranslation translations = 7 [(google.api.field_behavior) = OPTIONAL];

  // Held for the fields on every read path (rule 9).
  reserved 8 to 15;

  // The failed communication alarm.
  //
  // Composed of:
  //   failed (descriptor): Indicates that a point is not operating as
  //     intended or is incapable of operating.
  //   communication (descriptor): Actual transmit and recieve of data or
  //     connectivity between devices.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `failed_communication_alarm`, point type `alarm`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue failed_communication_alarm = 16 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "failed_communication_alarm"
      subfields: "failed"
      subfields: "communication"
      subfields: "alarm"
      point_type: "alarm"
    }
  ];

  // The failed outside air temperature alarm.
  //
  // Composed of:
  //   failed (descriptor): Indicates that a point is not operating as
  //     intended or is incapable of operating.
  //   outside (descriptor): Process or measurement of local atmospheric
  //     conditions.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `failed_outside_air_temperature_alarm`, point type
  // `alarm`. Measured as temperature; values are in kelvin, the ontology's
  // standard unit for that quantity.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue failed_outside_air_temperature_alarm = 17 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "failed_outside_air_temperature_alarm"
      subfields: "failed"
      subfields: "outside"
      subfields: "air"
      subfields: "temperature"
      subfields: "alarm"
      point_type: "alarm"
      measurement: "temperature"
      standard_unit: "kelvin"
    }
  ];

  // The outside air aqi sensor.
  //
  // Composed of:
  //   outside (descriptor): Process or measurement of local atmospheric
  //     conditions.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   aqi (measurement): Abbreviation for air quality index, which is a
  //     set of special standard gradings for the pollution in air.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `outside_air_aqi_sensor`, point type `sensor`. Measured
  // as aqi; values are in epa, the ontology's standard unit for that
  // quantity.
  //
  // Valid range 0 to 500, enforced by protovalidate.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double outside_air_aqi_sensor = 18 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "outside_air_aqi_sensor"
      subfields: "outside"
      subfields: "air"
      subfields: "aqi"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "aqi"
      standard_unit: "epa"
    },
    (buf.validate.field).double.gte = 0.0,
    (buf.validate.field).double.lte = 500.0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The outside air co2 concentration sensor.
  //
  // Composed of:
  //   outside (descriptor): Process or measurement of local atmospheric
  //     conditions.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   co2 (descriptor): Chemical formula and abbreviation for carbon
  //     dioxide.
  //   concentration (measurement): Concentration of something relative to
  //     something else (usually in parts per million or parts per billion).
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `outside_air_co2_concentration_sensor`, point type
  // `sensor`. Measured as concentration; values are in parts_per_unit, the
  // ontology's standard unit for that quantity.
  //
  // Expected range 5e-05 to 0.005. This is a data-quality expectation, not a
  // constraint: a reading outside it is a fault to report, not a message to
  // reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double outside_air_co2_concentration_sensor = 44 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "outside_air_co2_concentration_sensor"
      subfields: "outside"
      subfields: "air"
      subfields: "co2"
      subfields: "concentration"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "concentration"
      standard_unit: "parts_per_unit"
      expected_min: 0.00005
      expected_max: 0.005
    }
  ];

  // The outside air co concentration sensor.
  //
  // Composed of:
  //   outside (descriptor): Process or measurement of local atmospheric
  //     conditions.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   co (descriptor): Chemical formula and abbreviation for carbon
  //     monoxide.
  //   concentration (measurement): Concentration of something relative to
  //     something else (usually in parts per million or parts per billion).
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `outside_air_co_concentration_sensor`, point type
  // `sensor`. Measured as concentration; values are in parts_per_unit, the
  // ontology's standard unit for that quantity.
  //
  // Expected range 0.00035 to 0.0015. This is a data-quality expectation,
  // not a constraint: a reading outside it is a fault to report, not a
  // message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double outside_air_co_concentration_sensor = 20 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "outside_air_co_concentration_sensor"
      subfields: "outside"
      subfields: "air"
      subfields: "co"
      subfields: "concentration"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "concentration"
      standard_unit: "parts_per_unit"
      expected_min: 0.00035
      expected_max: 0.0015
    }
  ];

  // The outside air dewpoint temperature sensor.
  //
  // Composed of:
  //   outside (descriptor): Process or measurement of local atmospheric
  //     conditions.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   dewpoint (measurement_descriptor): The thermodynamic point at which
  //     water condenses from standing air.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `outside_air_dewpoint_temperature_sensor`, point type
  // `sensor`. Measured as temperature; values are in kelvin, the ontology's
  // standard unit for that quantity.
  //
  // Expected range 255.9259259 to 310.9259259. This is a data-quality
  // expectation, not a constraint: a reading outside it is a fault to
  // report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double outside_air_dewpoint_temperature_sensor = 21 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "outside_air_dewpoint_temperature_sensor"
      subfields: "outside"
      subfields: "air"
      subfields: "dewpoint"
      subfields: "temperature"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "temperature"
      standard_unit: "kelvin"
      expected_min: 255.925926
      expected_max: 310.925926
    }
  ];

  // The outside air no2 density sensor.
  //
  // Composed of:
  //   outside (descriptor): Process or measurement of local atmospheric
  //     conditions.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   no2 (descriptor): Chemical formula and abbreviation for nitrogen
  //     dioxide.
  //   density (measurement): Amount of mass contained in a unit volume.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `outside_air_no2_density_sensor`, point type `sensor`.
  // Measured as density; values are in kilograms_per_cubic_meter, the
  // ontology's standard unit for that quantity.
  //
  // Expected range 0 to 0.0002. This is a data-quality expectation, not a
  // constraint: a reading outside it is a fault to report, not a message to
  // reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double outside_air_no2_density_sensor = 45 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "outside_air_no2_density_sensor"
      subfields: "outside"
      subfields: "air"
      subfields: "no2"
      subfields: "density"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "density"
      standard_unit: "kilograms_per_cubic_meter"
      expected_min: 0.0
      expected_max: 0.0002
    }
  ];

  // The outside air no density sensor.
  //
  // Composed of:
  //   outside (descriptor): Process or measurement of local atmospheric
  //     conditions.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   no (descriptor): Chemical formula and abbreviation for nitrogen
  //     monoxide, which is a colorless gas and a toxic air pollutant.
  //   density (measurement): Amount of mass contained in a unit volume.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `outside_air_no_density_sensor`, point type `sensor`.
  // Measured as density; values are in kilograms_per_cubic_meter, the
  // ontology's standard unit for that quantity.
  //
  // Expected range 0 to 0.0002. This is a data-quality expectation, not a
  // constraint: a reading outside it is a fault to report, not a message to
  // reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double outside_air_no_density_sensor = 23 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "outside_air_no_density_sensor"
      subfields: "outside"
      subfields: "air"
      subfields: "no"
      subfields: "density"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "density"
      standard_unit: "kilograms_per_cubic_meter"
      expected_min: 0.0
      expected_max: 0.0002
    }
  ];

  // The outside air pm10pt0 density sensor.
  //
  // Composed of:
  //   outside (descriptor): Process or measurement of local atmospheric
  //     conditions.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   pm10pt0 (descriptor): Particulate of diameter 10.0 micrometers or
  //     less.
  //   density (measurement): Amount of mass contained in a unit volume.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `outside_air_pm10pt0_density_sensor`, point type
  // `sensor`. Measured as density; values are in kilograms_per_cubic_meter,
  // the ontology's standard unit for that quantity.
  //
  // Expected range 0 to 0.001. This is a data-quality expectation, not a
  // constraint: a reading outside it is a fault to report, not a message to
  // reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double outside_air_pm10pt0_density_sensor = 46 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "outside_air_pm10pt0_density_sensor"
      subfields: "outside"
      subfields: "air"
      subfields: "pm10pt0"
      subfields: "density"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "density"
      standard_unit: "kilograms_per_cubic_meter"
      expected_min: 0.0
      expected_max: 0.001
    }
  ];

  // The outside air pm1pt0 density sensor.
  //
  // Composed of:
  //   outside (descriptor): Process or measurement of local atmospheric
  //     conditions.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   pm1pt0 (descriptor): Particulate of diameter 1.5 micrometers or
  //     less.
  //   density (measurement): Amount of mass contained in a unit volume.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `outside_air_pm1pt0_density_sensor`, point type
  // `sensor`. Measured as density; values are in kilograms_per_cubic_meter,
  // the ontology's standard unit for that quantity.
  //
  // Expected range 0 to 0.001. This is a data-quality expectation, not a
  // constraint: a reading outside it is a fault to report, not a message to
  // reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double outside_air_pm1pt0_density_sensor = 47 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "outside_air_pm1pt0_density_sensor"
      subfields: "outside"
      subfields: "air"
      subfields: "pm1pt0"
      subfields: "density"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "density"
      standard_unit: "kilograms_per_cubic_meter"
      expected_min: 0.0
      expected_max: 0.001
    }
  ];

  // The outside air pm2pt5 density sensor.
  //
  // Composed of:
  //   outside (descriptor): Process or measurement of local atmospheric
  //     conditions.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   pm2pt5 (descriptor): Particulate of diameter 2.5 micrometers or
  //     less.
  //   density (measurement): Amount of mass contained in a unit volume.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `outside_air_pm2pt5_density_sensor`, point type
  // `sensor`. Measured as density; values are in kilograms_per_cubic_meter,
  // the ontology's standard unit for that quantity.
  //
  // Expected range 0 to 0.001. This is a data-quality expectation, not a
  // constraint: a reading outside it is a fault to report, not a message to
  // reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double outside_air_pm2pt5_density_sensor = 48 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "outside_air_pm2pt5_density_sensor"
      subfields: "outside"
      subfields: "air"
      subfields: "pm2pt5"
      subfields: "density"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "density"
      standard_unit: "kilograms_per_cubic_meter"
      expected_min: 0.0
      expected_max: 0.001
    }
  ];

  // The outside air pressure sensor.
  //
  // Composed of:
  //   outside (descriptor): Process or measurement of local atmospheric
  //     conditions.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   pressure (measurement): Measured force per unit area within a system
  //     or process.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `outside_air_pressure_sensor`, point type `sensor`.
  // Measured as pressure; values are in pascals, the ontology's standard
  // unit for that quantity.
  //
  // Expected range 500 to 200000. This is a data-quality expectation, not a
  // constraint: a reading outside it is a fault to report, not a message to
  // reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double outside_air_pressure_sensor = 27 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "outside_air_pressure_sensor"
      subfields: "outside"
      subfields: "air"
      subfields: "pressure"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "pressure"
      standard_unit: "pascals"
      expected_min: 500.0
      expected_max: 200000.0
    }
  ];

  // The outside air rain level sensor.
  //
  // Composed of:
  //   outside (descriptor): Process or measurement of local atmospheric
  //     conditions.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   rain (descriptor): Liquid water in the form of droplets that have
  //     condensed from atmospheric water vapor.
  //   level (measurement): The height of something with regard to a datum
  //     (e.g. the water level of a tank relative to the bottom).
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `outside_air_rain_level_sensor`, point type `sensor`.
  // Measured as level; values are in meters, the ontology's standard unit
  // for that quantity.
  //
  // Expected range 0 to 5e-05. This is a data-quality expectation, not a
  // constraint: a reading outside it is a fault to report, not a message to
  // reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double outside_air_rain_level_sensor = 28 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "outside_air_rain_level_sensor"
      subfields: "outside"
      subfields: "air"
      subfields: "rain"
      subfields: "level"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "level"
      standard_unit: "meters"
      expected_min: 0.0
      expected_max: 0.00005
    }
  ];

  // The outside air relative humidity sensor.
  //
  // Composed of:
  //   outside (descriptor): Process or measurement of local atmospheric
  //     conditions.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   relative (measurement_descriptor): Quality of media with respect to
  //     theoretical minimum or maximum value for a given condition (e.g.,
  //     relative humidity).
  //   humidity (measurement): Concentration of water vapor in air
  //     (conditioned or unconditioned).
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `outside_air_relative_humidity_sensor`, point type
  // `sensor`. Measured as humidity; values are in percent_relative_humidity,
  // the ontology's standard unit for that quantity.
  //
  // Valid range 0 to 100, enforced by protovalidate.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double outside_air_relative_humidity_sensor = 29 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "outside_air_relative_humidity_sensor"
      subfields: "outside"
      subfields: "air"
      subfields: "relative"
      subfields: "humidity"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "humidity"
      standard_unit: "percent_relative_humidity"
    },
    (buf.validate.field).double.gte = 0.0,
    (buf.validate.field).double.lte = 100.0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The outside air so2 density sensor.
  //
  // Composed of:
  //   outside (descriptor): Process or measurement of local atmospheric
  //     conditions.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   so2 (descriptor): Chemical formula and abbreviation for sulfur
  //     dioxide.
  //   density (measurement): Amount of mass contained in a unit volume.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `outside_air_so2_density_sensor`, point type `sensor`.
  // Measured as density; values are in kilograms_per_cubic_meter, the
  // ontology's standard unit for that quantity.
  //
  // Expected range 0 to 0.002. This is a data-quality expectation, not a
  // constraint: a reading outside it is a fault to report, not a message to
  // reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double outside_air_so2_density_sensor = 49 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "outside_air_so2_density_sensor"
      subfields: "outside"
      subfields: "air"
      subfields: "so2"
      subfields: "density"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "density"
      standard_unit: "kilograms_per_cubic_meter"
      expected_min: 0.0
      expected_max: 0.002
    }
  ];

  // The outside air specificenthalpy sensor.
  //
  // Composed of:
  //   outside (descriptor): Process or measurement of local atmospheric
  //     conditions.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   specificenthalpy (measurement): Measure of energy of air per unit
  //     mass.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `outside_air_specificenthalpy_sensor`, point type
  // `sensor`. Measured as specificenthalpy; values are in
  // joules_per_kilogram, the ontology's standard unit for that quantity.
  //
  // Expected range 11630 to 93040. This is a data-quality expectation, not a
  // constraint: a reading outside it is a fault to report, not a message to
  // reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double outside_air_specificenthalpy_sensor = 31 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "outside_air_specificenthalpy_sensor"
      subfields: "outside"
      subfields: "air"
      subfields: "specificenthalpy"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "specificenthalpy"
      standard_unit: "joules_per_kilogram"
      expected_min: 11630.0
      expected_max: 93040.0
    }
  ];

  // The outside air temperature sensor.
  //
  // Composed of:
  //   outside (descriptor): Process or measurement of local atmospheric
  //     conditions.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `outside_air_temperature_sensor`, point type `sensor`.
  // Measured as temperature; values are in kelvin, the ontology's standard
  // unit for that quantity.
  //
  // Expected range 255.9259259 to 322.037037. This is a data-quality
  // expectation, not a constraint: a reading outside it is a fault to
  // report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double outside_air_temperature_sensor = 32 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "outside_air_temperature_sensor"
      subfields: "outside"
      subfields: "air"
      subfields: "temperature"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "temperature"
      standard_unit: "kelvin"
      expected_min: 255.925926
      expected_max: 322.037037
    }
  ];

  // The outside air voc concentration sensor.
  //
  // Composed of:
  //   outside (descriptor): Process or measurement of local atmospheric
  //     conditions.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   voc (descriptor): Abbreviation for volatile organic componds, which
  //     are groups of chemicals emitted by many household objects like
  //     furniture and paint.
  //   concentration (measurement): Concentration of something relative to
  //     something else (usually in parts per million or parts per billion).
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `outside_air_voc_concentration_sensor`, point type
  // `sensor`. Measured as concentration; values are in parts_per_unit, the
  // ontology's standard unit for that quantity.
  //
  // Expected range 0 to 0.005. This is a data-quality expectation, not a
  // constraint: a reading outside it is a fault to report, not a message to
  // reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double outside_air_voc_concentration_sensor = 33 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "outside_air_voc_concentration_sensor"
      subfields: "outside"
      subfields: "air"
      subfields: "voc"
      subfields: "concentration"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "concentration"
      standard_unit: "parts_per_unit"
      expected_min: 0.0
      expected_max: 0.005
    }
  ];

  // The outside air wetbulb temperature sensor.
  //
  // Composed of:
  //   outside (descriptor): Process or measurement of local atmospheric
  //     conditions.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   wetbulb (measurement_descriptor): Describes air temperature measured
  //     at 100% relative humidity (saturation).
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `outside_air_wetbulb_temperature_sensor`, point type
  // `sensor`. Measured as temperature; values are in kelvin, the ontology's
  // standard unit for that quantity.
  //
  // Expected range 255.9259259 to 322.037037. This is a data-quality
  // expectation, not a constraint: a reading outside it is a fault to
  // report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double outside_air_wetbulb_temperature_sensor = 34 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "outside_air_wetbulb_temperature_sensor"
      subfields: "outside"
      subfields: "air"
      subfields: "wetbulb"
      subfields: "temperature"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "temperature"
      standard_unit: "kelvin"
      expected_min: 255.925926
      expected_max: 322.037037
    }
  ];

  // The ultraviolet irradiance sensor.
  //
  // Composed of:
  //   ultraviolet (descriptor): A short wavelength type of light that is
  //     beyond the visible spectrum. It is damaging to DNA and cells, and is
  //     commonly used to sterilize surfaces.
  //   irradiance (measurement): The flux of radiant energy per unit area
  //     (normal to the direction of flow of radiant energy through a
  //     medium).
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `ultraviolet_irradiance_sensor`, point type `sensor`.
  // Measured as irradiance; values are in watts_per_square_meter_irradiance,
  // the ontology's standard unit for that quantity.
  //
  // Valid range from 0, enforced by protovalidate. Expected range up to
  // 2000. This is a data-quality expectation, not a constraint: a reading
  // outside it is a fault to report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double ultraviolet_irradiance_sensor = 35 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "ultraviolet_irradiance_sensor"
      subfields: "ultraviolet"
      subfields: "irradiance"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "irradiance"
      standard_unit: "watts_per_square_meter_irradiance"
      expected_max: 2000.0
    },
    (buf.validate.field).double.gte = 0.0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The wind direction angle sensor.
  //
  // Composed of:
  //   wind (descriptor): Movement of ambient air.
  //   direction (descriptor): Direction in which particular entity is
  //     moving or is present.
  //   angle (measurement): The figure formed by two rays, sharing a common
  //     endpoint.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `wind_direction_angle_sensor`, point type `sensor`.
  // Measured as angle; values are in degrees, the ontology's standard unit
  // for that quantity.
  //
  // Valid range 0 to 360, enforced by protovalidate.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double wind_direction_angle_sensor = 36 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "wind_direction_angle_sensor"
      subfields: "wind"
      subfields: "direction"
      subfields: "angle"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "angle"
      standard_unit: "degrees"
    },
    (buf.validate.field).double.gte = 0.0,
    (buf.validate.field).double.lte = 360.0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The wind linearvelocity sensor.
  //
  // Composed of:
  //   wind (descriptor): Movement of ambient air.
  //   linearvelocity (measurement): The rate of change of displacement
  //     with respect to time. For example, wind speed represents the linear
  //     velocity of the wind.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `wind_linearvelocity_sensor`, point type `sensor`.
  // Measured as linearvelocity; values are in meters_per_second, the
  // ontology's standard unit for that quantity.
  //
  // Expected range 0 to 100. This is a data-quality expectation, not a
  // constraint: a reading outside it is a fault to report, not a message to
  // reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double wind_linearvelocity_sensor = 37 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "wind_linearvelocity_sensor"
      subfields: "wind"
      subfields: "linearvelocity"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "linearvelocity"
      standard_unit: "meters_per_second"
      expected_min: 0.0
      expected_max: 100.0
    }
  ];

  // Numbers whose field the ontology no longer declares. Never reissued: a
  // consumer may still hold a message that used one.
  reserved 19, 22, 24, 25, 26, 30, 38, 39, 40, 41, 42, 43;
}
