// Copyright 2026 RegaliumOS™.
// SPDX-License-Identifier: Apache-2.0
//
// Generated by sync from Google's Digital Buildings ontology,
// revision 2026-09-09 (4a794ac).
// Portions Copyright 2020 Google LLC, licensed under Apache-2.0.
// DO NOT EDIT.
//
// Regenerate with: just sync
// Revision history: docs/spec.md

syntax = "proto3";

package protobuf.digitalbuildings.hvac.heating_water_system.v1;

import "buf/validate/validate.proto";
import "google/api/field_behavior.proto";
import "google/api/field_info.proto";
import "google/api/resource.proto";
import "protobuf/digitalbuildings/annotations/v1/annotations.proto";
import "protobuf/digitalbuildings/hvac/heating_water_system/v1/active_inactive_value.proto";
import "protobuf/digitalbuildings/hvac/heating_water_system/v1/auto_commissioning_cooling_dehumidify_fan_only_flushing_heating_manual_neutral_off_unknown_value.proto";
import "protobuf/digitalbuildings/hvac/heating_water_system/v1/auto_cooling_dehumidify_fan_only_heating_manual_off_value.proto";
import "protobuf/digitalbuildings/hvac/heating_water_system/v1/auto_off_on_value.proto";
import "protobuf/digitalbuildings/hvac/heating_water_system/v1/closed_open_value.proto";
import "protobuf/digitalbuildings/hvac/heating_water_system/v1/connection.proto";
import "protobuf/digitalbuildings/hvac/heating_water_system/v1/disabled_enabled_occupied_unoccupied_value.proto";
import "protobuf/digitalbuildings/hvac/heating_water_system/v1/disabled_enabled_value.proto";
import "protobuf/digitalbuildings/hvac/heating_water_system/v1/field_translation.proto";
import "protobuf/digitalbuildings/hvac/heating_water_system/v1/heating_water_system_type.proto";
import "protobuf/digitalbuildings/hvac/heating_water_system/v1/link.proto";
import "protobuf/digitalbuildings/hvac/heating_water_system/v1/local_remote_value.proto";
import "protobuf/digitalbuildings/hvac/heating_water_system/v1/off_on_standby_unknown_value.proto";
import "protobuf/digitalbuildings/hvac/heating_water_system/v1/off_on_unknown_value.proto";
import "protobuf/digitalbuildings/hvac/heating_water_system/v1/off_on_value.proto";

option java_multiple_files = true;
option java_outer_classname = "HeatingWaterSystemProto";
option java_package = "com.regalium.protobuf.digitalbuildings.hvac.heating_water_system.v1";

// Tag for heating water systems. This tag represents the system itself, and
// fields are mapped to it when they don't belong to another device within
// that system (e.g. supply_water_temperature_sensor for the supply header
// belongs to the system, not to any single device).
//
// Ontology entity type `HVAC/HWS`, a general type carrying 54 canonical
// variant(s). Rule 7: the general type is the resource and each variant is a
// value of HeatingWaterSystemType, so this message holds the union of every
// field any variant uses -- all of them optional.
//
// Reference: Digital Buildings abstract model.
// https://github.com/google/digitalbuildings/blob/master/ontology/docs/model.md
message HeatingWaterSystem {
  option (google.api.resource) = {
    type: "digitalbuildings.googleapis.com/HeatingWaterSystem"
    pattern: "buildings/{building}/heatingWaterSystems/{heating_water_system}"
    singular: "heatingWaterSystem"
    plural: "heatingWaterSystems"
  };
  option (protobuf.digitalbuildings.annotations.v1.entity_type) = {
    guid: "548fd420-92cb-47e1-926e-d5e2500bd3c7"
    namespace: "HVAC"
    general_type: "HWS"
  };

  // Resource name,
  // "buildings/{building}/heatingWaterSystems/{heating_water_system}".
  // Assigned by the server.
  string name = 1 [
    (google.api.field_behavior) = IDENTIFIER,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The ontology's GUID for this entity.
  //
  // Rule 10: this is the ontology's own identifier, not a second one minted
  // beside it. Digital Buildings assigns a stable UUID to every entity and
  // it survives renames, which is exactly what AIP-148 asks a `uid` to be.
  string uid = 2 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (google.api.field_info).format = UUID4,
    (buf.validate.field).string.uuid = true,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The building config's human-readable entity code, e.g. `FCU-123`.
  //
  // Locally unique rather than globally, chosen at design time, and it must
  // survive a round trip unchanged or every re-import duplicates the entity.
  // The old building-config format keys entities by this; the new one keys
  // them by `uid`. Both are representable because both exist.
  string code = 3 [
    (google.api.field_behavior) = OPTIONAL,
    (google.api.field_behavior) = IMMUTABLE,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The canonical type this entity conforms to.
  //
  // The variant's exact field sets ride on the enum value in
  // (annotations.canonical_type), so a validator can reject an entity
  // reporting a field its declared type does not have.
  HeatingWaterSystemType entity_type = 4 [(google.api.field_behavior) = OPTIONAL];

  // Directed relationships to other entities.
  //
  // Rule 4: a connection is not a field. AIP-215 forbids referencing a
  // message in another proto package, and the other end of a connection may
  // be any resource in any namespace -- so it is carried as a resource name
  // with a wildcard reference.
  repeated Connection connections = 5 [(google.api.field_behavior) = OPTIONAL];

  // Standard fields mapped from another entity's telemetry.
  //
  // The building config's `links` block: a virtual entity reports data
  // linked from reporting devices rather than emitting it itself.
  repeated Link links = 6 [(google.api.field_behavior) = OPTIONAL];

  // How this entity's native payload maps onto the standard fields, when it
  // reports telemetry directly.
  repeated FieldTranslation translations = 7 [(google.api.field_behavior) = OPTIONAL];

  // Held for the fields on every read path (rule 9).
  reserved 8 to 15;

  // The bypass valve percentage command.
  //
  // Composed of:
  //   bypass (descriptor): Route which fluid takes to bypass a process.
  //   valve (component): Component which meters the flow of water within a
  //     system or device.
  //   percentage (measurement): Measurement per hundred.
  //   command (point_type): The signal given to make an action happen.
  //     Defaults to multistate unless used alongside a measurement_type.
  //
  // Ontology field: `bypass_valve_percentage_command`, point type `command`.
  // Measured as percentage; values are in percent, the ontology's standard
  // unit for that quantity.
  //
  // Valid range 0 to 100, enforced by protovalidate.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double bypass_valve_percentage_command = 16 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "bypass_valve_percentage_command"
      subfields: "bypass"
      subfields: "valve"
      subfields: "percentage"
      subfields: "command"
      point_type: "command"
      measurement: "percentage"
      standard_unit: "percent"
    },
    (buf.validate.field).double.gte = 0.0,
    (buf.validate.field).double.lte = 100.0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The bypass valve percentage sensor.
  //
  // Composed of:
  //   bypass (descriptor): Route which fluid takes to bypass a process.
  //   valve (component): Component which meters the flow of water within a
  //     system or device.
  //   percentage (measurement): Measurement per hundred.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `bypass_valve_percentage_sensor`, point type `sensor`.
  // Measured as percentage; values are in percent, the ontology's standard
  // unit for that quantity.
  //
  // Valid range 0 to 100, enforced by protovalidate.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double bypass_valve_percentage_sensor = 17 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "bypass_valve_percentage_sensor"
      subfields: "bypass"
      subfields: "valve"
      subfields: "percentage"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "percentage"
      standard_unit: "percent"
    },
    (buf.validate.field).double.gte = 0.0,
    (buf.validate.field).double.lte = 100.0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The cogeneration return water temperature sensor.
  //
  // Composed of:
  //   cogeneration (descriptor): The process of generating both thermal
  //     heat and electricity.
  //   return (descriptor): Measurement or process of media as it is
  //     returned from the end-use equipment within the system.
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `cogeneration_return_water_temperature_sensor`, point
  // type `sensor`. Measured as temperature; values are in kelvin, the
  // ontology's standard unit for that quantity.
  //
  // Expected range 255.9259259 to 380.3703704. This is a data-quality
  // expectation, not a constraint: a reading outside it is a fault to
  // report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double cogeneration_return_water_temperature_sensor = 18 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "cogeneration_return_water_temperature_sensor"
      subfields: "cogeneration"
      subfields: "return"
      subfields: "water"
      subfields: "temperature"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "temperature"
      standard_unit: "kelvin"
      expected_min: 255.925926
      expected_max: 380.37037
    }
  ];

  // The cogeneration return water temperature setpoint.
  //
  // Composed of:
  //   cogeneration (descriptor): The process of generating both thermal
  //     heat and electricity.
  //   return (descriptor): Measurement or process of media as it is
  //     returned from the end-use equipment within the system.
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   setpoint (point_type): Control target of process or system.
  //
  // Ontology field: `cogeneration_return_water_temperature_setpoint`, point
  // type `setpoint`. Measured as temperature; values are in kelvin, the
  // ontology's standard unit for that quantity.
  //
  // Expected range 255.9259259 to 380.3703704. This is a data-quality
  // expectation, not a constraint: a reading outside it is a fault to
  // report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double cogeneration_return_water_temperature_setpoint = 19 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "cogeneration_return_water_temperature_setpoint"
      subfields: "cogeneration"
      subfields: "return"
      subfields: "water"
      subfields: "temperature"
      subfields: "setpoint"
      point_type: "setpoint"
      measurement: "temperature"
      standard_unit: "kelvin"
      expected_min: 255.925926
      expected_max: 380.37037
    }
  ];

  // The cogeneration supply water temperature sensor.
  //
  // Composed of:
  //   cogeneration (descriptor): The process of generating both thermal
  //     heat and electricity.
  //   supply (descriptor): Measurement or process of media as it is
  //     supplied to the end-use equipment within the system.
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `cogeneration_supply_water_temperature_sensor`, point
  // type `sensor`. Measured as temperature; values are in kelvin, the
  // ontology's standard unit for that quantity.
  //
  // Expected range 255.9259259 to 380.3703704. This is a data-quality
  // expectation, not a constraint: a reading outside it is a fault to
  // report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double cogeneration_supply_water_temperature_sensor = 20 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "cogeneration_supply_water_temperature_sensor"
      subfields: "cogeneration"
      subfields: "supply"
      subfields: "water"
      subfields: "temperature"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "temperature"
      standard_unit: "kelvin"
      expected_min: 255.925926
      expected_max: 380.37037
    }
  ];

  // The communication status.
  //
  // Composed of:
  //   communication (descriptor): Actual transmit and recieve of data or
  //     connectivity between devices.
  //   status (point_type): The multistate value indicating an observed
  //     state in a piece of equipment, often indicating if a command was
  //     effected. It is a neutral observation (i.e., no quality judgment of
  //     'good' or 'bad'). It also has no units of measurement; therefore, if
  //     combined with a measurement subfield, it will indicate that the
  //     field is the directional status based on some measurement of that
  //     type (e.g., power_status equates to an on/off value based on some
  //     inference of power).
  //
  // Ontology field: `communication_status`, point type `status`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional OffOnValue communication_status = 21 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "communication_status"
      subfields: "communication"
      subfields: "status"
      point_type: "status"
    }
  ];

  // The compressor run status.
  //
  // Composed of:
  //   compressor (component): Component which drives refrigerant
  //     compression (and thus cooling processes) within a device or system.
  //   run (measurement_descriptor): State of being active or operating. By
  //     default applies to control program for system (e.g., VAV program
  //     run_command).
  //   status (point_type): The multistate value indicating an observed
  //     state in a piece of equipment, often indicating if a command was
  //     effected. It is a neutral observation (i.e., no quality judgment of
  //     'good' or 'bad'). It also has no units of measurement; therefore, if
  //     combined with a measurement subfield, it will indicate that the
  //     field is the directional status based on some measurement of that
  //     type (e.g., power_status equates to an on/off value based on some
  //     inference of power).
  //
  // Ontology field: `compressor_run_status`, point type `status`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional OffOnUnknownValue compressor_run_status = 22 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "compressor_run_status"
      subfields: "compressor"
      subfields: "run"
      subfields: "status"
      point_type: "status"
    }
  ];

  // The control mode.
  //
  // Composed of:
  //   control (descriptor): The process of governing the actions of a
  //     device.
  //   mode (point_type): Distinct mode of operation within system. For
  //     example economizer_mode has modes of 'enabled' or 'disabled.'
  //
  // Ontology field: `control_mode`, point type `mode`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional AutoCoolingDehumidifyFanOnlyHeatingManualOffValue control_mode = 23 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "control_mode"
      subfields: "control"
      subfields: "mode"
      point_type: "mode"
    }
  ];

  // The control status.
  //
  // Composed of:
  //   control (descriptor): The process of governing the actions of a
  //     device.
  //   status (point_type): The multistate value indicating an observed
  //     state in a piece of equipment, often indicating if a command was
  //     effected. It is a neutral observation (i.e., no quality judgment of
  //     'good' or 'bad'). It also has no units of measurement; therefore, if
  //     combined with a measurement subfield, it will indicate that the
  //     field is the directional status based on some measurement of that
  //     type (e.g., power_status equates to an on/off value based on some
  //     inference of power).
  //
  // Ontology field: `control_status`, point type `status`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional LocalRemoteValue control_status = 24 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "control_status"
      subfields: "control"
      subfields: "status"
      point_type: "status"
    }
  ];

  // The cooling percentage command.
  //
  // Composed of:
  //   cooling (descriptor): Process, measured effect or requirement for
  //     cooling.
  //   percentage (measurement): Measurement per hundred.
  //   command (point_type): The signal given to make an action happen.
  //     Defaults to multistate unless used alongside a measurement_type.
  //
  // Ontology field: `cooling_percentage_command`, point type `command`.
  // Measured as percentage; values are in percent, the ontology's standard
  // unit for that quantity.
  //
  // Valid range 0 to 100, enforced by protovalidate.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double cooling_percentage_command = 25 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "cooling_percentage_command"
      subfields: "cooling"
      subfields: "percentage"
      subfields: "command"
      point_type: "command"
      measurement: "percentage"
      standard_unit: "percent"
    },
    (buf.validate.field).double.gte = 0.0,
    (buf.validate.field).double.lte = 100.0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The cooling request count.
  //
  // Composed of:
  //   cooling (descriptor): Process, measured effect or requirement for
  //     cooling.
  //   request (descriptor): A signal from a system device that calls for
  //     more of a particular conditioning process (e.g., pressure requests
  //     from terminal units).
  //   count (point_type): Total count of actions or requests.
  //
  // Ontology field: `cooling_request_count`, point type `count`.
  // Dimensionless: the ontology defines this point type as an integer count
  // rather than a measured quantity.
  //
  // Valid range from 0, enforced by protovalidate. Expected range up to 500.
  // This is a data-quality expectation, not a constraint: a reading outside
  // it is a fault to report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional int64 cooling_request_count = 26 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "cooling_request_count"
      subfields: "cooling"
      subfields: "request"
      subfields: "count"
      point_type: "count"
      expected_max: 500.0
    },
    (buf.validate.field).int64.gte = 0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The current sensor.
  //
  // Composed of:
  //   current (measurement): Flow of electric charge.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `current_sensor`, point type `sensor`. Measured as
  // current; values are in amperes, the ontology's standard unit for that
  // quantity.
  //
  // Valid range from 0, enforced by protovalidate. Expected range up to 200.
  // This is a data-quality expectation, not a constraint: a reading outside
  // it is a fault to report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double current_sensor = 27 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "current_sensor"
      subfields: "current"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "current"
      standard_unit: "amperes"
      expected_max: 200.0
    },
    (buf.validate.field).double.gte = 0.0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The differential pressure sensor.
  //
  // Composed of:
  //   differential (measurement_descriptor): Measured difference between
  //     two reference points (e.g. differential_water_pressure)
  //   pressure (measurement): Measured force per unit area within a system
  //     or process.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `differential_pressure_sensor`, point type `sensor`.
  // Measured as pressure; values are in pascals, the ontology's standard
  // unit for that quantity.
  //
  // Expected range -34473.78945 to 344737.8945. This is a data-quality
  // expectation, not a constraint: a reading outside it is a fault to
  // report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  repeated double differential_pressure_sensor = 28 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "differential_pressure_sensor"
      subfields: "differential"
      subfields: "pressure"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "pressure"
      standard_unit: "pascals"
      enumerated: true
      expected_min: -34473.78945
      expected_max: 344737.8945
    }
  ];

  // The differential pressure setpoint.
  //
  // Composed of:
  //   differential (measurement_descriptor): Measured difference between
  //     two reference points (e.g. differential_water_pressure)
  //   pressure (measurement): Measured force per unit area within a system
  //     or process.
  //   setpoint (point_type): Control target of process or system.
  //
  // Ontology field: `differential_pressure_setpoint`, point type `setpoint`.
  // Measured as pressure; values are in pascals, the ontology's standard
  // unit for that quantity.
  //
  // Expected range -34473.78945 to 344737.8945. This is a data-quality
  // expectation, not a constraint: a reading outside it is a fault to
  // report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double differential_pressure_setpoint = 29 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "differential_pressure_setpoint"
      subfields: "differential"
      subfields: "pressure"
      subfields: "setpoint"
      point_type: "setpoint"
      measurement: "pressure"
      standard_unit: "pascals"
      expected_min: -34473.78945
      expected_max: 344737.8945
    }
  ];

  // The differential pressure specification.
  //
  // Composed of:
  //   differential (measurement_descriptor): Measured difference between
  //     two reference points (e.g. differential_water_pressure)
  //   pressure (measurement): Measured force per unit area within a system
  //     or process.
  //   specification (point_type): The specified design value for a
  //     particular operating condition (e.g., differential pressure
  //     specification).
  //
  // Ontology field: `differential_pressure_specification`, point type
  // `specification`. Measured as pressure; values are in pascals, the
  // ontology's standard unit for that quantity.
  //
  // Expected range -34473.78945 to 344737.8945. This is a data-quality
  // expectation, not a constraint: a reading outside it is a fault to
  // report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double differential_pressure_spec = 178 [
    (google.api.field_behavior) = OPTIONAL,
    (google.api.field_behavior) = IMMUTABLE,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "differential_pressure_specification"
      subfields: "differential"
      subfields: "pressure"
      subfields: "specification"
      point_type: "specification"
      measurement: "pressure"
      standard_unit: "pascals"
      expected_min: -34473.78945
      expected_max: 344737.8945
    }
  ];

  // The economizer bypass valve command.
  //
  // Composed of:
  //   economizer (descriptor): Process or component responsible for the
  //     utilization of free cooling.
  //   bypass (descriptor): Route which fluid takes to bypass a process.
  //   valve (component): Component which meters the flow of water within a
  //     system or device.
  //   command (point_type): The signal given to make an action happen.
  //     Defaults to multistate unless used alongside a measurement_type.
  //
  // Ontology field: `economizer_bypass_valve_command`, point type `command`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ClosedOpenValue economizer_bypass_valve_command = 31 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "economizer_bypass_valve_command"
      subfields: "economizer"
      subfields: "bypass"
      subfields: "valve"
      subfields: "command"
      point_type: "command"
    }
  ];

  // The economizer bypass valve percentage command.
  //
  // Composed of:
  //   economizer (descriptor): Process or component responsible for the
  //     utilization of free cooling.
  //   bypass (descriptor): Route which fluid takes to bypass a process.
  //   valve (component): Component which meters the flow of water within a
  //     system or device.
  //   percentage (measurement): Measurement per hundred.
  //   command (point_type): The signal given to make an action happen.
  //     Defaults to multistate unless used alongside a measurement_type.
  //
  // Ontology field: `economizer_bypass_valve_percentage_command`, point type
  // `command`. Measured as percentage; values are in percent, the ontology's
  // standard unit for that quantity.
  //
  // Valid range 0 to 100, enforced by protovalidate.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double economizer_bypass_valve_percentage_command = 32 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "economizer_bypass_valve_percentage_command"
      subfields: "economizer"
      subfields: "bypass"
      subfields: "valve"
      subfields: "percentage"
      subfields: "command"
      point_type: "command"
      measurement: "percentage"
      standard_unit: "percent"
    },
    (buf.validate.field).double.gte = 0.0,
    (buf.validate.field).double.lte = 100.0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The economizer bypass valve percentage sensor.
  //
  // Composed of:
  //   economizer (descriptor): Process or component responsible for the
  //     utilization of free cooling.
  //   bypass (descriptor): Route which fluid takes to bypass a process.
  //   valve (component): Component which meters the flow of water within a
  //     system or device.
  //   percentage (measurement): Measurement per hundred.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `economizer_bypass_valve_percentage_sensor`, point type
  // `sensor`. Measured as percentage; values are in percent, the ontology's
  // standard unit for that quantity.
  //
  // Valid range 0 to 100, enforced by protovalidate.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double economizer_bypass_valve_percentage_sensor = 33 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "economizer_bypass_valve_percentage_sensor"
      subfields: "economizer"
      subfields: "bypass"
      subfields: "valve"
      subfields: "percentage"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "percentage"
      standard_unit: "percent"
    },
    (buf.validate.field).double.gte = 0.0,
    (buf.validate.field).double.lte = 100.0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The economizer bypass valve status.
  //
  // Composed of:
  //   economizer (descriptor): Process or component responsible for the
  //     utilization of free cooling.
  //   bypass (descriptor): Route which fluid takes to bypass a process.
  //   valve (component): Component which meters the flow of water within a
  //     system or device.
  //   status (point_type): The multistate value indicating an observed
  //     state in a piece of equipment, often indicating if a command was
  //     effected. It is a neutral observation (i.e., no quality judgment of
  //     'good' or 'bad'). It also has no units of measurement; therefore, if
  //     combined with a measurement subfield, it will indicate that the
  //     field is the directional status based on some measurement of that
  //     type (e.g., power_status equates to an on/off value based on some
  //     inference of power).
  //
  // Ontology field: `economizer_bypass_valve_status`, point type `status`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ClosedOpenValue economizer_bypass_valve_status = 34 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "economizer_bypass_valve_status"
      subfields: "economizer"
      subfields: "bypass"
      subfields: "valve"
      subfields: "status"
      point_type: "status"
    }
  ];

  // The economizer disable command.
  //
  // Composed of:
  //   economizer (descriptor): Process or component responsible for the
  //     utilization of free cooling.
  //   disable (measurement_descriptor): State of being unable to be active
  //     or operating.
  //   command (point_type): The signal given to make an action happen.
  //     Defaults to multistate unless used alongside a measurement_type.
  //
  // Ontology field: `economizer_disable_command`, point type `command`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional OffOnValue economizer_disable_command = 35 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "economizer_disable_command"
      subfields: "economizer"
      subfields: "disable"
      subfields: "command"
      point_type: "command"
    }
  ];

  // The economizer mode.
  //
  // Composed of:
  //   economizer (descriptor): Process or component responsible for the
  //     utilization of free cooling.
  //   mode (point_type): Distinct mode of operation within system. For
  //     example economizer_mode has modes of 'enabled' or 'disabled.'
  //
  // Ontology field: `economizer_mode`, point type `mode`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional AutoOffOnValue economizer_mode = 36 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "economizer_mode"
      subfields: "economizer"
      subfields: "mode"
      point_type: "mode"
    }
  ];

  // The economizer thermal power sensor.
  //
  // Composed of:
  //   economizer (descriptor): Process or component responsible for the
  //     utilization of free cooling.
  //   thermal (measurement_descriptor): Relating to the transfer of heat.
  //   power (measurement): Rate of energy consumption (assumed context is
  //     electrical and real, unless otherwise modified).
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `economizer_thermal_power_sensor`, point type `sensor`.
  // Measured as power; values are in watts, the ontology's standard unit for
  // that quantity.
  //
  // Expected range 0 to 3.517e+06. This is a data-quality expectation, not a
  // constraint: a reading outside it is a fault to report, not a message to
  // reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double economizer_thermal_power_sensor = 37 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "economizer_thermal_power_sensor"
      subfields: "economizer"
      subfields: "thermal"
      subfields: "power"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "power"
      standard_unit: "watts"
      expected_min: 0.0
      expected_max: 3517000.0
    }
  ];

  // The efficiency percentage sensor.
  //
  // Composed of:
  //   efficiency (measurement_descriptor): The ratio of required input to
  //     actual output.
  //   percentage (measurement): Measurement per hundred.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `efficiency_percentage_sensor`, point type `sensor`.
  // Measured as percentage; values are in percent, the ontology's standard
  // unit for that quantity.
  //
  // Valid range 0 to 100, enforced by protovalidate.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double efficiency_percentage_sensor = 38 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "efficiency_percentage_sensor"
      subfields: "efficiency"
      subfields: "percentage"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "percentage"
      standard_unit: "percent"
    },
    (buf.validate.field).double.gte = 0.0,
    (buf.validate.field).double.lte = 100.0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The energy accumulator.
  //
  // Composed of:
  //   energy (measurement): The quantitative property that must be
  //     transferred to an object to perform work (default electrical unless
  //     otherwise modified).
  //   accumulator (point_type): The total accumulated quantity (e.g.,
  //     total energy accumulated).
  //
  // Ontology field: `energy_accumulator`, point type `accumulator`. Measured
  // as energy; values are in joules, the ontology's standard unit for that
  // quantity.
  //
  // Valid range from 0, enforced by protovalidate. Expected range up to
  // 3.6e+19. This is a data-quality expectation, not a constraint: a reading
  // outside it is a fault to report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double energy_accumulator = 39 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "energy_accumulator"
      subfields: "energy"
      subfields: "accumulator"
      point_type: "accumulator"
      measurement: "energy"
      standard_unit: "joules"
      expected_max: 36000000000000000000.0
    },
    (buf.validate.field).double.gte = 0.0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The failed alarm.
  //
  // Composed of:
  //   failed (descriptor): Indicates that a point is not operating as
  //     intended or is incapable of operating.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `failed_alarm`, point type `alarm`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue failed_alarm = 40 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "failed_alarm"
      subfields: "failed"
      subfields: "alarm"
      point_type: "alarm"
    }
  ];

  // The failed bypass valve alarm.
  //
  // Composed of:
  //   failed (descriptor): Indicates that a point is not operating as
  //     intended or is incapable of operating.
  //   bypass (descriptor): Route which fluid takes to bypass a process.
  //   valve (component): Component which meters the flow of water within a
  //     system or device.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `failed_bypass_valve_alarm`, point type `alarm`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue failed_bypass_valve_alarm = 41 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "failed_bypass_valve_alarm"
      subfields: "failed"
      subfields: "bypass"
      subfields: "valve"
      subfields: "alarm"
      point_type: "alarm"
    }
  ];

  // The failed communication alarm.
  //
  // Composed of:
  //   failed (descriptor): Indicates that a point is not operating as
  //     intended or is incapable of operating.
  //   communication (descriptor): Actual transmit and recieve of data or
  //     connectivity between devices.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `failed_communication_alarm`, point type `alarm`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue failed_communication_alarm = 42 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "failed_communication_alarm"
      subfields: "failed"
      subfields: "communication"
      subfields: "alarm"
      point_type: "alarm"
    }
  ];

  // The failed differential pressure alarm.
  //
  // Composed of:
  //   failed (descriptor): Indicates that a point is not operating as
  //     intended or is incapable of operating.
  //   differential (measurement_descriptor): Measured difference between
  //     two reference points (e.g. differential_water_pressure)
  //   pressure (measurement): Measured force per unit area within a system
  //     or process.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `failed_differential_pressure_alarm`, point type
  // `alarm`. Measured as pressure; values are in pascals, the ontology's
  // standard unit for that quantity.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue failed_differential_pressure_alarm = 43 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "failed_differential_pressure_alarm"
      subfields: "failed"
      subfields: "differential"
      subfields: "pressure"
      subfields: "alarm"
      point_type: "alarm"
      measurement: "pressure"
      standard_unit: "pascals"
    }
  ];

  // The failed economizer bypass valve alarm.
  //
  // Composed of:
  //   failed (descriptor): Indicates that a point is not operating as
  //     intended or is incapable of operating.
  //   economizer (descriptor): Process or component responsible for the
  //     utilization of free cooling.
  //   bypass (descriptor): Route which fluid takes to bypass a process.
  //   valve (component): Component which meters the flow of water within a
  //     system or device.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `failed_economizer_bypass_valve_alarm`, point type
  // `alarm`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue failed_economizer_bypass_valve_alarm = 44 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "failed_economizer_bypass_valve_alarm"
      subfields: "failed"
      subfields: "economizer"
      subfields: "bypass"
      subfields: "valve"
      subfields: "alarm"
      point_type: "alarm"
    }
  ];

  // The failed flowrate alarm.
  //
  // Composed of:
  //   failed (descriptor): Indicates that a point is not operating as
  //     intended or is incapable of operating.
  //   flowrate (measurement): Rate of fluid movement.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `failed_flowrate_alarm`, point type `alarm`. Measured as
  // flowrate; values are in cubic_meters_per_second, the ontology's standard
  // unit for that quantity.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue failed_flowrate_alarm = 45 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "failed_flowrate_alarm"
      subfields: "failed"
      subfields: "flowrate"
      subfields: "alarm"
      point_type: "alarm"
      measurement: "flowrate"
      standard_unit: "cubic_meters_per_second"
    }
  ];

  // The failed isolation valve alarm.
  //
  // Composed of:
  //   failed (descriptor): Indicates that a point is not operating as
  //     intended or is incapable of operating.
  //   isolation (descriptor): Process of isolating one component or
  //     process from another.
  //   valve (component): Component which meters the flow of water within a
  //     system or device.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `failed_isolation_valve_alarm`, point type `alarm`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue failed_isolation_valve_alarm = 46 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "failed_isolation_valve_alarm"
      subfields: "failed"
      subfields: "isolation"
      subfields: "valve"
      subfields: "alarm"
      point_type: "alarm"
    }
  ];

  // The failed makeup water flowrate alarm.
  //
  // Composed of:
  //   failed (descriptor): Indicates that a point is not operating as
  //     intended or is incapable of operating.
  //   makeup (descriptor): Process of adding ("making-up") water that has
  //     been lost due to blowdown or evaporation.
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   flowrate (measurement): Rate of fluid movement.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `failed_makeup_water_flowrate_alarm`, point type
  // `alarm`. Measured as flowrate; values are in cubic_meters_per_second,
  // the ontology's standard unit for that quantity.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue failed_makeup_water_flowrate_alarm = 47 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "failed_makeup_water_flowrate_alarm"
      subfields: "failed"
      subfields: "makeup"
      subfields: "water"
      subfields: "flowrate"
      subfields: "alarm"
      point_type: "alarm"
      measurement: "flowrate"
      standard_unit: "cubic_meters_per_second"
    }
  ];

  // The failed primary return water temperature alarm.
  //
  // Composed of:
  //   failed (descriptor): Indicates that a point is not operating as
  //     intended or is incapable of operating.
  //   primary (descriptor): Associated with the primary (production) loop
  //     of a production process.
  //   return (descriptor): Measurement or process of media as it is
  //     returned from the end-use equipment within the system.
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `failed_primary_return_water_temperature_alarm`, point
  // type `alarm`. Measured as temperature; values are in kelvin, the
  // ontology's standard unit for that quantity.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue failed_primary_return_water_temperature_alarm = 48 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "failed_primary_return_water_temperature_alarm"
      subfields: "failed"
      subfields: "primary"
      subfields: "return"
      subfields: "water"
      subfields: "temperature"
      subfields: "alarm"
      point_type: "alarm"
      measurement: "temperature"
      standard_unit: "kelvin"
    }
  ];

  // The failed primary supply water flowrate alarm.
  //
  // Composed of:
  //   failed (descriptor): Indicates that a point is not operating as
  //     intended or is incapable of operating.
  //   primary (descriptor): Associated with the primary (production) loop
  //     of a production process.
  //   supply (descriptor): Measurement or process of media as it is
  //     supplied to the end-use equipment within the system.
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   flowrate (measurement): Rate of fluid movement.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `failed_primary_supply_water_flowrate_alarm`, point type
  // `alarm`. Measured as flowrate; values are in cubic_meters_per_second,
  // the ontology's standard unit for that quantity.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue failed_primary_supply_water_flowrate_alarm = 49 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "failed_primary_supply_water_flowrate_alarm"
      subfields: "failed"
      subfields: "primary"
      subfields: "supply"
      subfields: "water"
      subfields: "flowrate"
      subfields: "alarm"
      point_type: "alarm"
      measurement: "flowrate"
      standard_unit: "cubic_meters_per_second"
    }
  ];

  // The failed primary supply water temperature alarm.
  //
  // Composed of:
  //   failed (descriptor): Indicates that a point is not operating as
  //     intended or is incapable of operating.
  //   primary (descriptor): Associated with the primary (production) loop
  //     of a production process.
  //   supply (descriptor): Measurement or process of media as it is
  //     supplied to the end-use equipment within the system.
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `failed_primary_supply_water_temperature_alarm`, point
  // type `alarm`. Measured as temperature; values are in kelvin, the
  // ontology's standard unit for that quantity.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue failed_primary_supply_water_temperature_alarm = 50 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "failed_primary_supply_water_temperature_alarm"
      subfields: "failed"
      subfields: "primary"
      subfields: "supply"
      subfields: "water"
      subfields: "temperature"
      subfields: "alarm"
      point_type: "alarm"
      measurement: "temperature"
      standard_unit: "kelvin"
    }
  ];

  // The failed return water changeover valve alarm.
  //
  // Composed of:
  //   failed (descriptor): Indicates that a point is not operating as
  //     intended or is incapable of operating.
  //   return (descriptor): Measurement or process of media as it is
  //     returned from the end-use equipment within the system.
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   changeover (descriptor): Connecting to a different equipment circuit
  //     (e.g., switching between chiller and hot water).
  //   valve (component): Component which meters the flow of water within a
  //     system or device.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `failed_return_water_changeover_valve_alarm`, point type
  // `alarm`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue failed_return_water_changeover_valve_alarm = 51 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "failed_return_water_changeover_valve_alarm"
      subfields: "failed"
      subfields: "return"
      subfields: "water"
      subfields: "changeover"
      subfields: "valve"
      subfields: "alarm"
      point_type: "alarm"
    }
  ];

  // The failed return water isolation valve alarm.
  //
  // Composed of:
  //   failed (descriptor): Indicates that a point is not operating as
  //     intended or is incapable of operating.
  //   return (descriptor): Measurement or process of media as it is
  //     returned from the end-use equipment within the system.
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   isolation (descriptor): Process of isolating one component or
  //     process from another.
  //   valve (component): Component which meters the flow of water within a
  //     system or device.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `failed_return_water_isolation_valve_alarm`, point type
  // `alarm`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue failed_return_water_isolation_valve_alarm = 52 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "failed_return_water_isolation_valve_alarm"
      subfields: "failed"
      subfields: "return"
      subfields: "water"
      subfields: "isolation"
      subfields: "valve"
      subfields: "alarm"
      point_type: "alarm"
    }
  ];

  // The failed return water temperature alarm.
  //
  // Composed of:
  //   failed (descriptor): Indicates that a point is not operating as
  //     intended or is incapable of operating.
  //   return (descriptor): Measurement or process of media as it is
  //     returned from the end-use equipment within the system.
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `failed_return_water_temperature_alarm`, point type
  // `alarm`. Measured as temperature; values are in kelvin, the ontology's
  // standard unit for that quantity.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue failed_return_water_temperature_alarm = 53 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "failed_return_water_temperature_alarm"
      subfields: "failed"
      subfields: "return"
      subfields: "water"
      subfields: "temperature"
      subfields: "alarm"
      point_type: "alarm"
      measurement: "temperature"
      standard_unit: "kelvin"
    }
  ];

  // The failed secondary return water temperature alarm.
  //
  // Composed of:
  //   failed (descriptor): Indicates that a point is not operating as
  //     intended or is incapable of operating.
  //   secondary (descriptor): Associated with the secondary (distribution)
  //     loop of a produciton process.
  //   return (descriptor): Measurement or process of media as it is
  //     returned from the end-use equipment within the system.
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `failed_secondary_return_water_temperature_alarm`, point
  // type `alarm`. Measured as temperature; values are in kelvin, the
  // ontology's standard unit for that quantity.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue failed_secondary_return_water_temperature_alarm = 54 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "failed_secondary_return_water_temperature_alarm"
      subfields: "failed"
      subfields: "secondary"
      subfields: "return"
      subfields: "water"
      subfields: "temperature"
      subfields: "alarm"
      point_type: "alarm"
      measurement: "temperature"
      standard_unit: "kelvin"
    }
  ];

  // The failed secondary supply water flowrate alarm.
  //
  // Composed of:
  //   failed (descriptor): Indicates that a point is not operating as
  //     intended or is incapable of operating.
  //   secondary (descriptor): Associated with the secondary (distribution)
  //     loop of a produciton process.
  //   supply (descriptor): Measurement or process of media as it is
  //     supplied to the end-use equipment within the system.
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   flowrate (measurement): Rate of fluid movement.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `failed_secondary_supply_water_flowrate_alarm`, point
  // type `alarm`. Measured as flowrate; values are in
  // cubic_meters_per_second, the ontology's standard unit for that quantity.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue failed_secondary_supply_water_flowrate_alarm = 55 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "failed_secondary_supply_water_flowrate_alarm"
      subfields: "failed"
      subfields: "secondary"
      subfields: "supply"
      subfields: "water"
      subfields: "flowrate"
      subfields: "alarm"
      point_type: "alarm"
      measurement: "flowrate"
      standard_unit: "cubic_meters_per_second"
    }
  ];

  // The failed secondary supply water temperature alarm.
  //
  // Composed of:
  //   failed (descriptor): Indicates that a point is not operating as
  //     intended or is incapable of operating.
  //   secondary (descriptor): Associated with the secondary (distribution)
  //     loop of a produciton process.
  //   supply (descriptor): Measurement or process of media as it is
  //     supplied to the end-use equipment within the system.
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `failed_secondary_supply_water_temperature_alarm`, point
  // type `alarm`. Measured as temperature; values are in kelvin, the
  // ontology's standard unit for that quantity.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue failed_secondary_supply_water_temperature_alarm = 56 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "failed_secondary_supply_water_temperature_alarm"
      subfields: "failed"
      subfields: "secondary"
      subfields: "supply"
      subfields: "water"
      subfields: "temperature"
      subfields: "alarm"
      point_type: "alarm"
      measurement: "temperature"
      standard_unit: "kelvin"
    }
  ];

  // The failed secondary water differential pressure alarm.
  //
  // Composed of:
  //   failed (descriptor): Indicates that a point is not operating as
  //     intended or is incapable of operating.
  //   secondary (descriptor): Associated with the secondary (distribution)
  //     loop of a produciton process.
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   differential (measurement_descriptor): Measured difference between
  //     two reference points (e.g. differential_water_pressure)
  //   pressure (measurement): Measured force per unit area within a system
  //     or process.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `failed_secondary_water_differential_pressure_alarm`,
  // point type `alarm`. Measured as pressure; values are in pascals, the
  // ontology's standard unit for that quantity.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue failed_secondary_water_differential_pressure_alarm = 57 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "failed_secondary_water_differential_pressure_alarm"
      subfields: "failed"
      subfields: "secondary"
      subfields: "water"
      subfields: "differential"
      subfields: "pressure"
      subfields: "alarm"
      point_type: "alarm"
      measurement: "pressure"
      standard_unit: "pascals"
    }
  ];

  // The failed supply water changeover valve alarm.
  //
  // Composed of:
  //   failed (descriptor): Indicates that a point is not operating as
  //     intended or is incapable of operating.
  //   supply (descriptor): Measurement or process of media as it is
  //     supplied to the end-use equipment within the system.
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   changeover (descriptor): Connecting to a different equipment circuit
  //     (e.g., switching between chiller and hot water).
  //   valve (component): Component which meters the flow of water within a
  //     system or device.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `failed_supply_water_changeover_valve_alarm`, point type
  // `alarm`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue failed_supply_water_changeover_valve_alarm = 58 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "failed_supply_water_changeover_valve_alarm"
      subfields: "failed"
      subfields: "supply"
      subfields: "water"
      subfields: "changeover"
      subfields: "valve"
      subfields: "alarm"
      point_type: "alarm"
    }
  ];

  // The failed supply water isolation valve alarm.
  //
  // Composed of:
  //   failed (descriptor): Indicates that a point is not operating as
  //     intended or is incapable of operating.
  //   supply (descriptor): Measurement or process of media as it is
  //     supplied to the end-use equipment within the system.
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   isolation (descriptor): Process of isolating one component or
  //     process from another.
  //   valve (component): Component which meters the flow of water within a
  //     system or device.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `failed_supply_water_isolation_valve_alarm`, point type
  // `alarm`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue failed_supply_water_isolation_valve_alarm = 59 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "failed_supply_water_isolation_valve_alarm"
      subfields: "failed"
      subfields: "supply"
      subfields: "water"
      subfields: "isolation"
      subfields: "valve"
      subfields: "alarm"
      point_type: "alarm"
    }
  ];

  // The failed supply water temperature alarm.
  //
  // Composed of:
  //   failed (descriptor): Indicates that a point is not operating as
  //     intended or is incapable of operating.
  //   supply (descriptor): Measurement or process of media as it is
  //     supplied to the end-use equipment within the system.
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `failed_supply_water_temperature_alarm`, point type
  // `alarm`. Measured as temperature; values are in kelvin, the ontology's
  // standard unit for that quantity.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue failed_supply_water_temperature_alarm = 60 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "failed_supply_water_temperature_alarm"
      subfields: "failed"
      subfields: "supply"
      subfields: "water"
      subfields: "temperature"
      subfields: "alarm"
      point_type: "alarm"
      measurement: "temperature"
      standard_unit: "kelvin"
    }
  ];

  // The flowrate capacity.
  //
  // Composed of:
  //   flowrate (measurement): Rate of fluid movement.
  //   capacity (point_type): A design parameter quantity (e.g., design
  //     motor power capacity). Is always a maximum limit.
  //
  // Ontology field: `flowrate_capacity`, point type `capacity`. Measured as
  // flowrate; values are in cubic_meters_per_second, the ontology's standard
  // unit for that quantity.
  //
  // Valid range from 0, enforced by protovalidate. Expected range up to
  // 141.584233. This is a data-quality expectation, not a constraint: a
  // reading outside it is a fault to report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double flowrate_capacity = 61 [
    (google.api.field_behavior) = OPTIONAL,
    (google.api.field_behavior) = IMMUTABLE,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "flowrate_capacity"
      subfields: "flowrate"
      subfields: "capacity"
      point_type: "capacity"
      measurement: "flowrate"
      standard_unit: "cubic_meters_per_second"
      expected_max: 141.584233
    },
    (buf.validate.field).double.gte = 0.0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The flowrate requirement.
  //
  // Composed of:
  //   flowrate (measurement): Rate of fluid movement.
  //   requirement (point_type): A lower limit design parameter (e.g.,
  //     minimum flowrate requirement). Is always a lower limit.
  //
  // Ontology field: `flowrate_requirement`, point type `requirement`.
  // Measured as flowrate; values are in cubic_meters_per_second, the
  // ontology's standard unit for that quantity.
  //
  // Valid range from 0, enforced by protovalidate. Expected range up to
  // 18.92706. This is a data-quality expectation, not a constraint: a
  // reading outside it is a fault to report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double flowrate_requirement = 62 [
    (google.api.field_behavior) = OPTIONAL,
    (google.api.field_behavior) = IMMUTABLE,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "flowrate_requirement"
      subfields: "flowrate"
      subfields: "requirement"
      point_type: "requirement"
      measurement: "flowrate"
      standard_unit: "cubic_meters_per_second"
      expected_max: 18.92706
    },
    (buf.validate.field).double.gte = 0.0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The flowrate sensor.
  //
  // Composed of:
  //   flowrate (measurement): Rate of fluid movement.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `flowrate_sensor`, point type `sensor`. Measured as
  // flowrate; values are in cubic_meters_per_second, the ontology's standard
  // unit for that quantity.
  //
  // Expected range -0.000315451 to 0.315451. This is a data-quality
  // expectation, not a constraint: a reading outside it is a fault to
  // report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double flowrate_sensor = 63 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "flowrate_sensor"
      subfields: "flowrate"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "flowrate"
      standard_unit: "cubic_meters_per_second"
      expected_min: -0.000315
      expected_max: 0.315451
    }
  ];

  // The flowrate setpoint.
  //
  // Composed of:
  //   flowrate (measurement): Rate of fluid movement.
  //   setpoint (point_type): Control target of process or system.
  //
  // Ontology field: `flowrate_setpoint`, point type `setpoint`. Measured as
  // flowrate; values are in cubic_meters_per_second, the ontology's standard
  // unit for that quantity.
  //
  // Expected range 0 to 0.315451. This is a data-quality expectation, not a
  // constraint: a reading outside it is a fault to report, not a message to
  // reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double flowrate_setpoint = 64 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "flowrate_setpoint"
      subfields: "flowrate"
      subfields: "setpoint"
      point_type: "setpoint"
      measurement: "flowrate"
      standard_unit: "cubic_meters_per_second"
      expected_min: 0.0
      expected_max: 0.315451
    }
  ];

  // The heating percentage command.
  //
  // Composed of:
  //   heating (descriptor): Process, measured effect or requirement for
  //     heating.
  //   percentage (measurement): Measurement per hundred.
  //   command (point_type): The signal given to make an action happen.
  //     Defaults to multistate unless used alongside a measurement_type.
  //
  // Ontology field: `heating_percentage_command`, point type `command`.
  // Measured as percentage; values are in percent, the ontology's standard
  // unit for that quantity.
  //
  // Valid range 0 to 100, enforced by protovalidate.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double heating_percentage_command = 65 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "heating_percentage_command"
      subfields: "heating"
      subfields: "percentage"
      subfields: "command"
      point_type: "command"
      measurement: "percentage"
      standard_unit: "percent"
    },
    (buf.validate.field).double.gte = 0.0,
    (buf.validate.field).double.lte = 100.0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The heating request count.
  //
  // Composed of:
  //   heating (descriptor): Process, measured effect or requirement for
  //     heating.
  //   request (descriptor): A signal from a system device that calls for
  //     more of a particular conditioning process (e.g., pressure requests
  //     from terminal units).
  //   count (point_type): Total count of actions or requests.
  //
  // Ontology field: `heating_request_count`, point type `count`.
  // Dimensionless: the ontology defines this point type as an integer count
  // rather than a measured quantity.
  //
  // Valid range from 0, enforced by protovalidate. Expected range up to 250.
  // This is a data-quality expectation, not a constraint: a reading outside
  // it is a fault to report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional int64 heating_request_count = 66 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "heating_request_count"
      subfields: "heating"
      subfields: "request"
      subfields: "count"
      point_type: "count"
      expected_max: 250.0
    },
    (buf.validate.field).int64.gte = 0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The heating thermal power capacity.
  //
  // Composed of:
  //   heating (descriptor): Process, measured effect or requirement for
  //     heating.
  //   thermal (measurement_descriptor): Relating to the transfer of heat.
  //   power (measurement): Rate of energy consumption (assumed context is
  //     electrical and real, unless otherwise modified).
  //   capacity (point_type): A design parameter quantity (e.g., design
  //     motor power capacity). Is always a maximum limit.
  //
  // Ontology field: `heating_thermal_power_capacity`, point type `capacity`.
  // Measured as power; values are in watts, the ontology's standard unit for
  // that quantity.
  //
  // Valid range from 0, enforced by protovalidate. Expected range up to
  // 2.9307107e+06. This is a data-quality expectation, not a constraint: a
  // reading outside it is a fault to report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double heating_thermal_power_capacity = 67 [
    (google.api.field_behavior) = OPTIONAL,
    (google.api.field_behavior) = IMMUTABLE,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "heating_thermal_power_capacity"
      subfields: "heating"
      subfields: "thermal"
      subfields: "power"
      subfields: "capacity"
      point_type: "capacity"
      measurement: "power"
      standard_unit: "watts"
      expected_max: 2930710.7
    },
    (buf.validate.field).double.gte = 0.0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The heating thermal power sensor.
  //
  // Composed of:
  //   heating (descriptor): Process, measured effect or requirement for
  //     heating.
  //   thermal (measurement_descriptor): Relating to the transfer of heat.
  //   power (measurement): Rate of energy consumption (assumed context is
  //     electrical and real, unless otherwise modified).
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `heating_thermal_power_sensor`, point type `sensor`.
  // Measured as power; values are in watts, the ontology's standard unit for
  // that quantity.
  //
  // Expected range 0 to 586142.14. This is a data-quality expectation, not a
  // constraint: a reading outside it is a fault to report, not a message to
  // reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double heating_thermal_power_sensor = 68 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "heating_thermal_power_sensor"
      subfields: "heating"
      subfields: "thermal"
      subfields: "power"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "power"
      standard_unit: "watts"
      expected_min: 0.0
      expected_max: 586142.14
    }
  ];

  // The heating water flowrate sensor.
  //
  // Composed of:
  //   heating (descriptor): Process, measured effect or requirement for
  //     heating.
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   flowrate (measurement): Rate of fluid movement.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `heating_water_flowrate_sensor`, point type `sensor`.
  // Measured as flowrate; values are in cubic_meters_per_second, the
  // ontology's standard unit for that quantity.
  //
  // Expected range -0.000315451 to 0.0315451. This is a data-quality
  // expectation, not a constraint: a reading outside it is a fault to
  // report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double heating_water_flowrate_sensor = 69 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "heating_water_flowrate_sensor"
      subfields: "heating"
      subfields: "water"
      subfields: "flowrate"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "flowrate"
      standard_unit: "cubic_meters_per_second"
      expected_min: -0.000315
      expected_max: 0.031545
    }
  ];

  // The heating water valve percentage command.
  //
  // Composed of:
  //   heating (descriptor): Process, measured effect or requirement for
  //     heating.
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   valve (component): Component which meters the flow of water within a
  //     system or device.
  //   percentage (measurement): Measurement per hundred.
  //   command (point_type): The signal given to make an action happen.
  //     Defaults to multistate unless used alongside a measurement_type.
  //
  // Ontology field: `heating_water_valve_percentage_command`, point type
  // `command`. Measured as percentage; values are in percent, the ontology's
  // standard unit for that quantity.
  //
  // Valid range 0 to 100, enforced by protovalidate.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double heating_water_valve_percentage_command = 70 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "heating_water_valve_percentage_command"
      subfields: "heating"
      subfields: "water"
      subfields: "valve"
      subfields: "percentage"
      subfields: "command"
      point_type: "command"
      measurement: "percentage"
      standard_unit: "percent"
    },
    (buf.validate.field).double.gte = 0.0,
    (buf.validate.field).double.lte = 100.0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The heating water valve percentage sensor.
  //
  // Composed of:
  //   heating (descriptor): Process, measured effect or requirement for
  //     heating.
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   valve (component): Component which meters the flow of water within a
  //     system or device.
  //   percentage (measurement): Measurement per hundred.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `heating_water_valve_percentage_sensor`, point type
  // `sensor`. Measured as percentage; values are in percent, the ontology's
  // standard unit for that quantity.
  //
  // Valid range 0 to 100, enforced by protovalidate.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double heating_water_valve_percentage_sensor = 71 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "heating_water_valve_percentage_sensor"
      subfields: "heating"
      subfields: "water"
      subfields: "valve"
      subfields: "percentage"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "percentage"
      standard_unit: "percent"
    },
    (buf.validate.field).double.gte = 0.0,
    (buf.validate.field).double.lte = 100.0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The high differential pressure alarm.
  //
  // Composed of:
  //   high (descriptor): Level of control or measurement; above medium and
  //     low; opposite of low.
  //   differential (measurement_descriptor): Measured difference between
  //     two reference points (e.g. differential_water_pressure)
  //   pressure (measurement): Measured force per unit area within a system
  //     or process.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `high_differential_pressure_alarm`, point type `alarm`.
  // Measured as pressure; values are in pascals, the ontology's standard
  // unit for that quantity.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue high_differential_pressure_alarm = 72 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "high_differential_pressure_alarm"
      subfields: "high"
      subfields: "differential"
      subfields: "pressure"
      subfields: "alarm"
      point_type: "alarm"
      measurement: "pressure"
      standard_unit: "pascals"
    }
  ];

  // The high flowrate alarm.
  //
  // Composed of:
  //   high (descriptor): Level of control or measurement; above medium and
  //     low; opposite of low.
  //   flowrate (measurement): Rate of fluid movement.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `high_flowrate_alarm`, point type `alarm`. Measured as
  // flowrate; values are in cubic_meters_per_second, the ontology's standard
  // unit for that quantity.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue high_flowrate_alarm = 73 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "high_flowrate_alarm"
      subfields: "high"
      subfields: "flowrate"
      subfields: "alarm"
      point_type: "alarm"
      measurement: "flowrate"
      standard_unit: "cubic_meters_per_second"
    }
  ];

  // The high mechanical heating differential pressure alarm.
  //
  // Composed of:
  //   high (descriptor): Level of control or measurement; above medium and
  //     low; opposite of low.
  //   mechanical (descriptor): A process driven by machinery; specifically
  //     applied to cooling processes (e. g. using a refrigeration cycle for
  //     cooling).
  //   heating (descriptor): Process, measured effect or requirement for
  //     heating.
  //   differential (measurement_descriptor): Measured difference between
  //     two reference points (e.g. differential_water_pressure)
  //   pressure (measurement): Measured force per unit area within a system
  //     or process.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `high_mechanical_heating_differential_pressure_alarm`,
  // point type `alarm`. Measured as pressure; values are in pascals, the
  // ontology's standard unit for that quantity.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue high_mechanical_heating_differential_pressure_alarm = 74 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "high_mechanical_heating_differential_pressure_alarm"
      subfields: "high"
      subfields: "mechanical"
      subfields: "heating"
      subfields: "differential"
      subfields: "pressure"
      subfields: "alarm"
      point_type: "alarm"
      measurement: "pressure"
      standard_unit: "pascals"
    }
  ];

  // The high primary return water temperature alarm.
  //
  // Composed of:
  //   high (descriptor): Level of control or measurement; above medium and
  //     low; opposite of low.
  //   primary (descriptor): Associated with the primary (production) loop
  //     of a production process.
  //   return (descriptor): Measurement or process of media as it is
  //     returned from the end-use equipment within the system.
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `high_primary_return_water_temperature_alarm`, point
  // type `alarm`. Measured as temperature; values are in kelvin, the
  // ontology's standard unit for that quantity.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue high_primary_return_water_temperature_alarm = 75 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "high_primary_return_water_temperature_alarm"
      subfields: "high"
      subfields: "primary"
      subfields: "return"
      subfields: "water"
      subfields: "temperature"
      subfields: "alarm"
      point_type: "alarm"
      measurement: "temperature"
      standard_unit: "kelvin"
    }
  ];

  // The high primary supply water temperature alarm.
  //
  // Composed of:
  //   high (descriptor): Level of control or measurement; above medium and
  //     low; opposite of low.
  //   primary (descriptor): Associated with the primary (production) loop
  //     of a production process.
  //   supply (descriptor): Measurement or process of media as it is
  //     supplied to the end-use equipment within the system.
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `high_primary_supply_water_temperature_alarm`, point
  // type `alarm`. Measured as temperature; values are in kelvin, the
  // ontology's standard unit for that quantity.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue high_primary_supply_water_temperature_alarm = 76 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "high_primary_supply_water_temperature_alarm"
      subfields: "high"
      subfields: "primary"
      subfields: "supply"
      subfields: "water"
      subfields: "temperature"
      subfields: "alarm"
      point_type: "alarm"
      measurement: "temperature"
      standard_unit: "kelvin"
    }
  ];

  // The high primary water differential pressure alarm.
  //
  // Composed of:
  //   high (descriptor): Level of control or measurement; above medium and
  //     low; opposite of low.
  //   primary (descriptor): Associated with the primary (production) loop
  //     of a production process.
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   differential (measurement_descriptor): Measured difference between
  //     two reference points (e.g. differential_water_pressure)
  //   pressure (measurement): Measured force per unit area within a system
  //     or process.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `high_primary_water_differential_pressure_alarm`, point
  // type `alarm`. Measured as pressure; values are in pascals, the
  // ontology's standard unit for that quantity.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue high_primary_water_differential_pressure_alarm = 77 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "high_primary_water_differential_pressure_alarm"
      subfields: "high"
      subfields: "primary"
      subfields: "water"
      subfields: "differential"
      subfields: "pressure"
      subfields: "alarm"
      point_type: "alarm"
      measurement: "pressure"
      standard_unit: "pascals"
    }
  ];

  // The high return water temperature alarm.
  //
  // Composed of:
  //   high (descriptor): Level of control or measurement; above medium and
  //     low; opposite of low.
  //   return (descriptor): Measurement or process of media as it is
  //     returned from the end-use equipment within the system.
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `high_return_water_temperature_alarm`, point type
  // `alarm`. Measured as temperature; values are in kelvin, the ontology's
  // standard unit for that quantity.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue high_return_water_temperature_alarm = 78 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "high_return_water_temperature_alarm"
      subfields: "high"
      subfields: "return"
      subfields: "water"
      subfields: "temperature"
      subfields: "alarm"
      point_type: "alarm"
      measurement: "temperature"
      standard_unit: "kelvin"
    }
  ];

  // The high secondary return water temperature alarm.
  //
  // Composed of:
  //   high (descriptor): Level of control or measurement; above medium and
  //     low; opposite of low.
  //   secondary (descriptor): Associated with the secondary (distribution)
  //     loop of a produciton process.
  //   return (descriptor): Measurement or process of media as it is
  //     returned from the end-use equipment within the system.
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `high_secondary_return_water_temperature_alarm`, point
  // type `alarm`. Measured as temperature; values are in kelvin, the
  // ontology's standard unit for that quantity.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue high_secondary_return_water_temperature_alarm = 79 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "high_secondary_return_water_temperature_alarm"
      subfields: "high"
      subfields: "secondary"
      subfields: "return"
      subfields: "water"
      subfields: "temperature"
      subfields: "alarm"
      point_type: "alarm"
      measurement: "temperature"
      standard_unit: "kelvin"
    }
  ];

  // The high secondary supply water temperature alarm.
  //
  // Composed of:
  //   high (descriptor): Level of control or measurement; above medium and
  //     low; opposite of low.
  //   secondary (descriptor): Associated with the secondary (distribution)
  //     loop of a produciton process.
  //   supply (descriptor): Measurement or process of media as it is
  //     supplied to the end-use equipment within the system.
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `high_secondary_supply_water_temperature_alarm`, point
  // type `alarm`. Measured as temperature; values are in kelvin, the
  // ontology's standard unit for that quantity.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue high_secondary_supply_water_temperature_alarm = 80 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "high_secondary_supply_water_temperature_alarm"
      subfields: "high"
      subfields: "secondary"
      subfields: "supply"
      subfields: "water"
      subfields: "temperature"
      subfields: "alarm"
      point_type: "alarm"
      measurement: "temperature"
      standard_unit: "kelvin"
    }
  ];

  // The high secondary water differential pressure alarm.
  //
  // Composed of:
  //   high (descriptor): Level of control or measurement; above medium and
  //     low; opposite of low.
  //   secondary (descriptor): Associated with the secondary (distribution)
  //     loop of a produciton process.
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   differential (measurement_descriptor): Measured difference between
  //     two reference points (e.g. differential_water_pressure)
  //   pressure (measurement): Measured force per unit area within a system
  //     or process.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `high_secondary_water_differential_pressure_alarm`,
  // point type `alarm`. Measured as pressure; values are in pascals, the
  // ontology's standard unit for that quantity.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue high_secondary_water_differential_pressure_alarm = 81 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "high_secondary_water_differential_pressure_alarm"
      subfields: "high"
      subfields: "secondary"
      subfields: "water"
      subfields: "differential"
      subfields: "pressure"
      subfields: "alarm"
      point_type: "alarm"
      measurement: "pressure"
      standard_unit: "pascals"
    }
  ];

  // The high supply water temperature alarm.
  //
  // Composed of:
  //   high (descriptor): Level of control or measurement; above medium and
  //     low; opposite of low.
  //   supply (descriptor): Measurement or process of media as it is
  //     supplied to the end-use equipment within the system.
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `high_supply_water_temperature_alarm`, point type
  // `alarm`. Measured as temperature; values are in kelvin, the ontology's
  // standard unit for that quantity.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue high_supply_water_temperature_alarm = 82 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "high_supply_water_temperature_alarm"
      subfields: "high"
      subfields: "supply"
      subfields: "water"
      subfields: "temperature"
      subfields: "alarm"
      point_type: "alarm"
      measurement: "temperature"
      standard_unit: "kelvin"
    }
  ];

  // The high thermally conditioned water differential pressure alarm.
  //
  // Composed of:
  //   high (descriptor): Level of control or measurement; above medium and
  //     low; opposite of low.
  //   thermally (descriptor): Relating to temperature regulation of a
  //     substance or environment
  //   conditioned (descriptor): A substance modified to meet specific
  //     requirements
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   differential (measurement_descriptor): Measured difference between
  //     two reference points (e.g. differential_water_pressure)
  //   pressure (measurement): Measured force per unit area within a system
  //     or process.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field:
  // `high_thermally_conditioned_water_differential_pressure_alarm`, point
  // type `alarm`. Measured as pressure; values are in pascals, the
  // ontology's standard unit for that quantity.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue high_thermally_conditioned_water_differential_pressure_alarm = 83 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "high_thermally_conditioned_water_differential_pressure_alarm"
      subfields: "high"
      subfields: "thermally"
      subfields: "conditioned"
      subfields: "water"
      subfields: "differential"
      subfields: "pressure"
      subfields: "alarm"
      point_type: "alarm"
      measurement: "pressure"
      standard_unit: "pascals"
    }
  ];

  // The high water temperature alarm.
  //
  // Composed of:
  //   high (descriptor): Level of control or measurement; above medium and
  //     low; opposite of low.
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `high_water_temperature_alarm`, point type `alarm`.
  // Measured as temperature; values are in kelvin, the ontology's standard
  // unit for that quantity.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue high_water_temperature_alarm = 84 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "high_water_temperature_alarm"
      subfields: "high"
      subfields: "water"
      subfields: "temperature"
      subfields: "alarm"
      point_type: "alarm"
      measurement: "temperature"
      standard_unit: "kelvin"
    }
  ];

  // The hot water tank temperature sensor.
  //
  // Composed of:
  //   hot (descriptor): Used to describe something at a high (or
  //     relatively high) temperature.
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   tank (component): Storage device for media; for example, a thermal
  //     storage tank in a heating water system.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `hot_water_tank_temperature_sensor`, point type
  // `sensor`. Measured as temperature; values are in kelvin, the ontology's
  // standard unit for that quantity.
  //
  // Expected range 255.9259259 to 380.3703704. This is a data-quality
  // expectation, not a constraint: a reading outside it is a fault to
  // report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double hot_water_tank_temperature_sensor = 85 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "hot_water_tank_temperature_sensor"
      subfields: "hot"
      subfields: "water"
      subfields: "tank"
      subfields: "temperature"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "temperature"
      standard_unit: "kelvin"
      expected_min: 255.925926
      expected_max: 380.37037
    }
  ];

  // The hot water tank temperature setpoint.
  //
  // Composed of:
  //   hot (descriptor): Used to describe something at a high (or
  //     relatively high) temperature.
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   tank (component): Storage device for media; for example, a thermal
  //     storage tank in a heating water system.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   setpoint (point_type): Control target of process or system.
  //
  // Ontology field: `hot_water_tank_temperature_setpoint`, point type
  // `setpoint`. Measured as temperature; values are in kelvin, the
  // ontology's standard unit for that quantity.
  //
  // Expected range 255.9259259 to 380.3703704. This is a data-quality
  // expectation, not a constraint: a reading outside it is a fault to
  // report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double hot_water_tank_temperature_setpoint = 86 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "hot_water_tank_temperature_setpoint"
      subfields: "hot"
      subfields: "water"
      subfields: "tank"
      subfields: "temperature"
      subfields: "setpoint"
      point_type: "setpoint"
      measurement: "temperature"
      standard_unit: "kelvin"
      expected_min: 255.925926
      expected_max: 380.37037
    }
  ];

  // The load percentage sensor.
  //
  // Composed of:
  //   load (descriptor): Electrical load which indicates the consumption
  //     of electrical power.
  //   percentage (measurement): Measurement per hundred.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `load_percentage_sensor`, point type `sensor`. Measured
  // as percentage; values are in percent, the ontology's standard unit for
  // that quantity.
  //
  // Valid range 0 to 100, enforced by protovalidate.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double load_percentage_sensor = 87 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "load_percentage_sensor"
      subfields: "load"
      subfields: "percentage"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "percentage"
      standard_unit: "percent"
    },
    (buf.validate.field).double.gte = 0.0,
    (buf.validate.field).double.lte = 100.0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The local override alarm.
  //
  // Composed of:
  //   local (descriptor): A definite spatial location involving only a
  //     subset of the larger surrounding area.
  //   override (descriptor): An action that interrupts, cancels, or
  //     changes the current action or status.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `local_override_alarm`, point type `alarm`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue local_override_alarm = 88 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "local_override_alarm"
      subfields: "local"
      subfields: "override"
      subfields: "alarm"
      point_type: "alarm"
    }
  ];

  // The local override isolation valve alarm.
  //
  // Composed of:
  //   local (descriptor): A definite spatial location involving only a
  //     subset of the larger surrounding area.
  //   override (descriptor): An action that interrupts, cancels, or
  //     changes the current action or status.
  //   isolation (descriptor): Process of isolating one component or
  //     process from another.
  //   valve (component): Component which meters the flow of water within a
  //     system or device.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `local_override_isolation_valve_alarm`, point type
  // `alarm`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue local_override_isolation_valve_alarm = 89 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "local_override_isolation_valve_alarm"
      subfields: "local"
      subfields: "override"
      subfields: "isolation"
      subfields: "valve"
      subfields: "alarm"
      point_type: "alarm"
    }
  ];

  // The lost power alarm.
  //
  // Composed of:
  //   lost (measurement_descriptor): Indicates that a point has lost some
  //     quality or quantity inadvertently.
  //   power (measurement): Rate of energy consumption (assumed context is
  //     electrical and real, unless otherwise modified).
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `lost_power_alarm`, point type `alarm`. Measured as
  // power; values are in watts, the ontology's standard unit for that
  // quantity.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue lost_power_alarm = 90 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "lost_power_alarm"
      subfields: "lost"
      subfields: "power"
      subfields: "alarm"
      point_type: "alarm"
      measurement: "power"
      standard_unit: "watts"
    }
  ];

  // The low differential pressure alarm.
  //
  // Composed of:
  //   low (descriptor): Level of control or measurement; below medium and
  //     high; opposite of high.
  //   differential (measurement_descriptor): Measured difference between
  //     two reference points (e.g. differential_water_pressure)
  //   pressure (measurement): Measured force per unit area within a system
  //     or process.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `low_differential_pressure_alarm`, point type `alarm`.
  // Measured as pressure; values are in pascals, the ontology's standard
  // unit for that quantity.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue low_differential_pressure_alarm = 91 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "low_differential_pressure_alarm"
      subfields: "low"
      subfields: "differential"
      subfields: "pressure"
      subfields: "alarm"
      point_type: "alarm"
      measurement: "pressure"
      standard_unit: "pascals"
    }
  ];

  // The low flowrate alarm.
  //
  // Composed of:
  //   low (descriptor): Level of control or measurement; below medium and
  //     high; opposite of high.
  //   flowrate (measurement): Rate of fluid movement.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `low_flowrate_alarm`, point type `alarm`. Measured as
  // flowrate; values are in cubic_meters_per_second, the ontology's standard
  // unit for that quantity.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue low_flowrate_alarm = 92 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "low_flowrate_alarm"
      subfields: "low"
      subfields: "flowrate"
      subfields: "alarm"
      point_type: "alarm"
      measurement: "flowrate"
      standard_unit: "cubic_meters_per_second"
    }
  ];

  // The low limit mechanical heating differential pressure setpoint.
  //
  // Composed of:
  //   low (descriptor): Level of control or measurement; below medium and
  //     high; opposite of high.
  //   limit (descriptor): A boundary condition for a control (e.g., low
  //     limit).
  //   mechanical (descriptor): A process driven by machinery; specifically
  //     applied to cooling processes (e. g. using a refrigeration cycle for
  //     cooling).
  //   heating (descriptor): Process, measured effect or requirement for
  //     heating.
  //   differential (measurement_descriptor): Measured difference between
  //     two reference points (e.g. differential_water_pressure)
  //   pressure (measurement): Measured force per unit area within a system
  //     or process.
  //   setpoint (point_type): Control target of process or system.
  //
  // Ontology field:
  // `low_limit_mechanical_heating_differential_pressure_setpoint`, point
  // type `setpoint`. Measured as pressure; values are in pascals, the
  // ontology's standard unit for that quantity.
  //
  // Expected range -34473.78945 to 344737.8945. This is a data-quality
  // expectation, not a constraint: a reading outside it is a fault to
  // report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double low_limit_mechanical_heating_differential_pressure_setpoint = 93 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "low_limit_mechanical_heating_differential_pressure_setpoint"
      subfields: "low"
      subfields: "limit"
      subfields: "mechanical"
      subfields: "heating"
      subfields: "differential"
      subfields: "pressure"
      subfields: "setpoint"
      point_type: "setpoint"
      measurement: "pressure"
      standard_unit: "pascals"
      expected_min: -34473.78945
      expected_max: 344737.8945
    }
  ];

  // The low mechanical heating differential pressure alarm.
  //
  // Composed of:
  //   low (descriptor): Level of control or measurement; below medium and
  //     high; opposite of high.
  //   mechanical (descriptor): A process driven by machinery; specifically
  //     applied to cooling processes (e. g. using a refrigeration cycle for
  //     cooling).
  //   heating (descriptor): Process, measured effect or requirement for
  //     heating.
  //   differential (measurement_descriptor): Measured difference between
  //     two reference points (e.g. differential_water_pressure)
  //   pressure (measurement): Measured force per unit area within a system
  //     or process.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `low_mechanical_heating_differential_pressure_alarm`,
  // point type `alarm`. Measured as pressure; values are in pascals, the
  // ontology's standard unit for that quantity.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue low_mechanical_heating_differential_pressure_alarm = 94 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "low_mechanical_heating_differential_pressure_alarm"
      subfields: "low"
      subfields: "mechanical"
      subfields: "heating"
      subfields: "differential"
      subfields: "pressure"
      subfields: "alarm"
      point_type: "alarm"
      measurement: "pressure"
      standard_unit: "pascals"
    }
  ];

  // The low primary return water temperature alarm.
  //
  // Composed of:
  //   low (descriptor): Level of control or measurement; below medium and
  //     high; opposite of high.
  //   primary (descriptor): Associated with the primary (production) loop
  //     of a production process.
  //   return (descriptor): Measurement or process of media as it is
  //     returned from the end-use equipment within the system.
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `low_primary_return_water_temperature_alarm`, point type
  // `alarm`. Measured as temperature; values are in kelvin, the ontology's
  // standard unit for that quantity.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue low_primary_return_water_temperature_alarm = 95 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "low_primary_return_water_temperature_alarm"
      subfields: "low"
      subfields: "primary"
      subfields: "return"
      subfields: "water"
      subfields: "temperature"
      subfields: "alarm"
      point_type: "alarm"
      measurement: "temperature"
      standard_unit: "kelvin"
    }
  ];

  // The low primary supply water temperature alarm.
  //
  // Composed of:
  //   low (descriptor): Level of control or measurement; below medium and
  //     high; opposite of high.
  //   primary (descriptor): Associated with the primary (production) loop
  //     of a production process.
  //   supply (descriptor): Measurement or process of media as it is
  //     supplied to the end-use equipment within the system.
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `low_primary_supply_water_temperature_alarm`, point type
  // `alarm`. Measured as temperature; values are in kelvin, the ontology's
  // standard unit for that quantity.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue low_primary_supply_water_temperature_alarm = 96 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "low_primary_supply_water_temperature_alarm"
      subfields: "low"
      subfields: "primary"
      subfields: "supply"
      subfields: "water"
      subfields: "temperature"
      subfields: "alarm"
      point_type: "alarm"
      measurement: "temperature"
      standard_unit: "kelvin"
    }
  ];

  // The low primary water differential pressure alarm.
  //
  // Composed of:
  //   low (descriptor): Level of control or measurement; below medium and
  //     high; opposite of high.
  //   primary (descriptor): Associated with the primary (production) loop
  //     of a production process.
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   differential (measurement_descriptor): Measured difference between
  //     two reference points (e.g. differential_water_pressure)
  //   pressure (measurement): Measured force per unit area within a system
  //     or process.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `low_primary_water_differential_pressure_alarm`, point
  // type `alarm`. Measured as pressure; values are in pascals, the
  // ontology's standard unit for that quantity.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue low_primary_water_differential_pressure_alarm = 97 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "low_primary_water_differential_pressure_alarm"
      subfields: "low"
      subfields: "primary"
      subfields: "water"
      subfields: "differential"
      subfields: "pressure"
      subfields: "alarm"
      point_type: "alarm"
      measurement: "pressure"
      standard_unit: "pascals"
    }
  ];

  // The low return water temperature alarm.
  //
  // Composed of:
  //   low (descriptor): Level of control or measurement; below medium and
  //     high; opposite of high.
  //   return (descriptor): Measurement or process of media as it is
  //     returned from the end-use equipment within the system.
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `low_return_water_temperature_alarm`, point type
  // `alarm`. Measured as temperature; values are in kelvin, the ontology's
  // standard unit for that quantity.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue low_return_water_temperature_alarm = 98 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "low_return_water_temperature_alarm"
      subfields: "low"
      subfields: "return"
      subfields: "water"
      subfields: "temperature"
      subfields: "alarm"
      point_type: "alarm"
      measurement: "temperature"
      standard_unit: "kelvin"
    }
  ];

  // The low secondary return water temperature alarm.
  //
  // Composed of:
  //   low (descriptor): Level of control or measurement; below medium and
  //     high; opposite of high.
  //   secondary (descriptor): Associated with the secondary (distribution)
  //     loop of a produciton process.
  //   return (descriptor): Measurement or process of media as it is
  //     returned from the end-use equipment within the system.
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `low_secondary_return_water_temperature_alarm`, point
  // type `alarm`. Measured as temperature; values are in kelvin, the
  // ontology's standard unit for that quantity.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue low_secondary_return_water_temperature_alarm = 99 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "low_secondary_return_water_temperature_alarm"
      subfields: "low"
      subfields: "secondary"
      subfields: "return"
      subfields: "water"
      subfields: "temperature"
      subfields: "alarm"
      point_type: "alarm"
      measurement: "temperature"
      standard_unit: "kelvin"
    }
  ];

  // The low secondary supply water temperature alarm.
  //
  // Composed of:
  //   low (descriptor): Level of control or measurement; below medium and
  //     high; opposite of high.
  //   secondary (descriptor): Associated with the secondary (distribution)
  //     loop of a produciton process.
  //   supply (descriptor): Measurement or process of media as it is
  //     supplied to the end-use equipment within the system.
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `low_secondary_supply_water_temperature_alarm`, point
  // type `alarm`. Measured as temperature; values are in kelvin, the
  // ontology's standard unit for that quantity.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue low_secondary_supply_water_temperature_alarm = 100 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "low_secondary_supply_water_temperature_alarm"
      subfields: "low"
      subfields: "secondary"
      subfields: "supply"
      subfields: "water"
      subfields: "temperature"
      subfields: "alarm"
      point_type: "alarm"
      measurement: "temperature"
      standard_unit: "kelvin"
    }
  ];

  // The low secondary water differential pressure alarm.
  //
  // Composed of:
  //   low (descriptor): Level of control or measurement; below medium and
  //     high; opposite of high.
  //   secondary (descriptor): Associated with the secondary (distribution)
  //     loop of a produciton process.
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   differential (measurement_descriptor): Measured difference between
  //     two reference points (e.g. differential_water_pressure)
  //   pressure (measurement): Measured force per unit area within a system
  //     or process.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `low_secondary_water_differential_pressure_alarm`, point
  // type `alarm`. Measured as pressure; values are in pascals, the
  // ontology's standard unit for that quantity.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue low_secondary_water_differential_pressure_alarm = 101 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "low_secondary_water_differential_pressure_alarm"
      subfields: "low"
      subfields: "secondary"
      subfields: "water"
      subfields: "differential"
      subfields: "pressure"
      subfields: "alarm"
      point_type: "alarm"
      measurement: "pressure"
      standard_unit: "pascals"
    }
  ];

  // The low supply water temperature alarm.
  //
  // Composed of:
  //   low (descriptor): Level of control or measurement; below medium and
  //     high; opposite of high.
  //   supply (descriptor): Measurement or process of media as it is
  //     supplied to the end-use equipment within the system.
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `low_supply_water_temperature_alarm`, point type
  // `alarm`. Measured as temperature; values are in kelvin, the ontology's
  // standard unit for that quantity.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue low_supply_water_temperature_alarm = 102 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "low_supply_water_temperature_alarm"
      subfields: "low"
      subfields: "supply"
      subfields: "water"
      subfields: "temperature"
      subfields: "alarm"
      point_type: "alarm"
      measurement: "temperature"
      standard_unit: "kelvin"
    }
  ];

  // The low thermally conditioned water differential pressure alarm.
  //
  // Composed of:
  //   low (descriptor): Level of control or measurement; below medium and
  //     high; opposite of high.
  //   thermally (descriptor): Relating to temperature regulation of a
  //     substance or environment
  //   conditioned (descriptor): A substance modified to meet specific
  //     requirements
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   differential (measurement_descriptor): Measured difference between
  //     two reference points (e.g. differential_water_pressure)
  //   pressure (measurement): Measured force per unit area within a system
  //     or process.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field:
  // `low_thermally_conditioned_water_differential_pressure_alarm`, point
  // type `alarm`. Measured as pressure; values are in pascals, the
  // ontology's standard unit for that quantity.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue low_thermally_conditioned_water_differential_pressure_alarm = 103 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "low_thermally_conditioned_water_differential_pressure_alarm"
      subfields: "low"
      subfields: "thermally"
      subfields: "conditioned"
      subfields: "water"
      subfields: "differential"
      subfields: "pressure"
      subfields: "alarm"
      point_type: "alarm"
      measurement: "pressure"
      standard_unit: "pascals"
    }
  ];

  // The makeup water flowrate sensor.
  //
  // Composed of:
  //   makeup (descriptor): Process of adding ("making-up") water that has
  //     been lost due to blowdown or evaporation.
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   flowrate (measurement): Rate of fluid movement.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `makeup_water_flowrate_sensor`, point type `sensor`.
  // Measured as flowrate; values are in cubic_meters_per_second, the
  // ontology's standard unit for that quantity.
  //
  // Expected range -0.000315451 to 0.315451. This is a data-quality
  // expectation, not a constraint: a reading outside it is a fault to
  // report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double makeup_water_flowrate_sensor = 104 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "makeup_water_flowrate_sensor"
      subfields: "makeup"
      subfields: "water"
      subfields: "flowrate"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "flowrate"
      standard_unit: "cubic_meters_per_second"
      expected_min: -0.000315
      expected_max: 0.315451
    }
  ];

  // The makeup water volume accumulator.
  //
  // Composed of:
  //   makeup (descriptor): Process of adding ("making-up") water that has
  //     been lost due to blowdown or evaporation.
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   volume (measurement): The quantity of three-dimensional space
  //     contained by a closed surface.
  //   accumulator (point_type): The total accumulated quantity (e.g.,
  //     total energy accumulated).
  //
  // Ontology field: `makeup_water_volume_accumulator`, point type
  // `accumulator`. Measured as volume; values are in cubic_meters, the
  // ontology's standard unit for that quantity.
  //
  // Expected range 0 to 100000. This is a data-quality expectation, not a
  // constraint: a reading outside it is a fault to report, not a message to
  // reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double makeup_water_volume_accumulator = 105 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "makeup_water_volume_accumulator"
      subfields: "makeup"
      subfields: "water"
      subfields: "volume"
      subfields: "accumulator"
      point_type: "accumulator"
      measurement: "volume"
      standard_unit: "cubic_meters"
      expected_min: 0.0
      expected_max: 100000.0
    }
  ];

  // The mechanical heating differential pressure sensor.
  //
  // Composed of:
  //   mechanical (descriptor): A process driven by machinery; specifically
  //     applied to cooling processes (e. g. using a refrigeration cycle for
  //     cooling).
  //   heating (descriptor): Process, measured effect or requirement for
  //     heating.
  //   differential (measurement_descriptor): Measured difference between
  //     two reference points (e.g. differential_water_pressure)
  //   pressure (measurement): Measured force per unit area within a system
  //     or process.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `mechanical_heating_differential_pressure_sensor`, point
  // type `sensor`. Measured as pressure; values are in pascals, the
  // ontology's standard unit for that quantity.
  //
  // Expected range -34473.78945 to 344737.8945. This is a data-quality
  // expectation, not a constraint: a reading outside it is a fault to
  // report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double mechanical_heating_differential_pressure_sensor = 106 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "mechanical_heating_differential_pressure_sensor"
      subfields: "mechanical"
      subfields: "heating"
      subfields: "differential"
      subfields: "pressure"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "pressure"
      standard_unit: "pascals"
      expected_min: -34473.78945
      expected_max: 344737.8945
    }
  ];

  // The min flowrate setpoint.
  //
  // Composed of:
  //   min (aggregation): Minimum value; the smallest value in a set of
  //     numeric values. Can be used for spatial (e.g.,
  //     min_zone_air_relative_humidity_sensor) or temporal (e.g.,
  //     fivesecondrolling_min_xaxis_linearacceleration_sensor) aggregations.
  //   flowrate (measurement): Rate of fluid movement.
  //   setpoint (point_type): Control target of process or system.
  //
  // Ontology field: `min_flowrate_setpoint`, point type `setpoint`. Measured
  // as flowrate; values are in cubic_meters_per_second, the ontology's
  // standard unit for that quantity.
  //
  // Expected range 0 to 0.315451. This is a data-quality expectation, not a
  // constraint: a reading outside it is a fault to report, not a message to
  // reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double min_flowrate_setpoint = 107 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "min_flowrate_setpoint"
      subfields: "min"
      subfields: "flowrate"
      subfields: "setpoint"
      point_type: "setpoint"
      measurement: "flowrate"
      standard_unit: "cubic_meters_per_second"
      expected_min: 0.0
      expected_max: 0.315451
    }
  ];

  // The mixing valve percentage command.
  //
  // Composed of:
  //   mixing (descriptor): Process of mixing substance.
  //   valve (component): Component which meters the flow of water within a
  //     system or device.
  //   percentage (measurement): Measurement per hundred.
  //   command (point_type): The signal given to make an action happen.
  //     Defaults to multistate unless used alongside a measurement_type.
  //
  // Ontology field: `mixing_valve_percentage_command`, point type `command`.
  // Measured as percentage; values are in percent, the ontology's standard
  // unit for that quantity.
  //
  // Valid range 0 to 100, enforced by protovalidate.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double mixing_valve_percentage_command = 108 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "mixing_valve_percentage_command"
      subfields: "mixing"
      subfields: "valve"
      subfields: "percentage"
      subfields: "command"
      point_type: "command"
      measurement: "percentage"
      standard_unit: "percent"
    },
    (buf.validate.field).double.gte = 0.0,
    (buf.validate.field).double.lte = 100.0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The mixing valve percentage sensor.
  //
  // Composed of:
  //   mixing (descriptor): Process of mixing substance.
  //   valve (component): Component which meters the flow of water within a
  //     system or device.
  //   percentage (measurement): Measurement per hundred.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `mixing_valve_percentage_sensor`, point type `sensor`.
  // Measured as percentage; values are in percent, the ontology's standard
  // unit for that quantity.
  //
  // Valid range 0 to 100, enforced by protovalidate.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double mixing_valve_percentage_sensor = 109 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "mixing_valve_percentage_sensor"
      subfields: "mixing"
      subfields: "valve"
      subfields: "percentage"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "percentage"
      standard_unit: "percent"
    },
    (buf.validate.field).double.gte = 0.0,
    (buf.validate.field).double.lte = 100.0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The optimal stop command.
  //
  // Composed of:
  //   optimal (descriptor): A method or solution being the most desirable
  //     or effective of all possibilities. Typically used in 'optimal
  //     start/stop,' referring to the most efficient point in time at which
  //     a system can turn off or on, while maintaining desired conditions
  //     and avoiding excess energy consumption or equipment wear.
  //   stop (measurement_descriptor): Indicates a point in time or space at
  //     which something has its ending.
  //   command (point_type): The signal given to make an action happen.
  //     Defaults to multistate unless used alongside a measurement_type.
  //
  // Ontology field: `optimal_stop_command`, point type `command`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional OffOnValue optimal_stop_command = 110 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "optimal_stop_command"
      subfields: "optimal"
      subfields: "stop"
      subfields: "command"
      point_type: "command"
    }
  ];

  // The power capacity.
  //
  // Composed of:
  //   power (measurement): Rate of energy consumption (assumed context is
  //     electrical and real, unless otherwise modified).
  //   capacity (point_type): A design parameter quantity (e.g., design
  //     motor power capacity). Is always a maximum limit.
  //
  // Ontology field: `power_capacity`, point type `capacity`. Measured as
  // power; values are in watts, the ontology's standard unit for that
  // quantity.
  //
  // Valid range from 0, enforced by protovalidate. Expected range up to
  // 100000. This is a data-quality expectation, not a constraint: a reading
  // outside it is a fault to report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double power_capacity = 111 [
    (google.api.field_behavior) = OPTIONAL,
    (google.api.field_behavior) = IMMUTABLE,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "power_capacity"
      subfields: "power"
      subfields: "capacity"
      point_type: "capacity"
      measurement: "power"
      standard_unit: "watts"
      expected_max: 100000.0
    },
    (buf.validate.field).double.gte = 0.0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The power sensor.
  //
  // Composed of:
  //   power (measurement): Rate of energy consumption (assumed context is
  //     electrical and real, unless otherwise modified).
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `power_sensor`, point type `sensor`. Measured as power;
  // values are in watts, the ontology's standard unit for that quantity.
  //
  // Expected range 0 to 1e+07. This is a data-quality expectation, not a
  // constraint: a reading outside it is a fault to report, not a message to
  // reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double power_sensor = 112 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "power_sensor"
      subfields: "power"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "power"
      standard_unit: "watts"
      expected_min: 0.0
      expected_max: 10000000.0
    }
  ];

  // The powerfactor sensor.
  //
  // Composed of:
  //   powerfactor (measurement): Ratio of real and apparent power.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `powerfactor_sensor`, point type `sensor`. Measured as
  // powerfactor; values are in no_units, the ontology's standard unit for
  // that quantity.
  //
  // Valid range -1 to 1, enforced by protovalidate.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double powerfactor_sensor = 113 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "powerfactor_sensor"
      subfields: "powerfactor"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "powerfactor"
      standard_unit: "no_units"
    },
    (buf.validate.field).double.gte = -1.0,
    (buf.validate.field).double.lte = 1.0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The pressurization request count.
  //
  // Composed of:
  //   pressurization (descriptor): Relating to the level of pressure in a
  //     system or vessel.
  //   request (descriptor): A signal from a system device that calls for
  //     more of a particular conditioning process (e.g., pressure requests
  //     from terminal units).
  //   count (point_type): Total count of actions or requests.
  //
  // Ontology field: `pressurization_request_count`, point type `count`.
  // Dimensionless: the ontology defines this point type as an integer count
  // rather than a measured quantity.
  //
  // Valid range from 0, enforced by protovalidate. Expected range up to 500.
  // This is a data-quality expectation, not a constraint: a reading outside
  // it is a fault to report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional int64 pressurization_request_count = 114 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "pressurization_request_count"
      subfields: "pressurization"
      subfields: "request"
      subfields: "count"
      point_type: "count"
      expected_max: 500.0
    },
    (buf.validate.field).int64.gte = 0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The primary return water flowrate sensor.
  //
  // Composed of:
  //   primary (descriptor): Associated with the primary (production) loop
  //     of a production process.
  //   return (descriptor): Measurement or process of media as it is
  //     returned from the end-use equipment within the system.
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   flowrate (measurement): Rate of fluid movement.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `primary_return_water_flowrate_sensor`, point type
  // `sensor`. Measured as flowrate; values are in cubic_meters_per_second,
  // the ontology's standard unit for that quantity.
  //
  // Expected range -0.000315451 to 0.315451. This is a data-quality
  // expectation, not a constraint: a reading outside it is a fault to
  // report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double primary_return_water_flowrate_sensor = 115 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "primary_return_water_flowrate_sensor"
      subfields: "primary"
      subfields: "return"
      subfields: "water"
      subfields: "flowrate"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "flowrate"
      standard_unit: "cubic_meters_per_second"
      expected_min: -0.000315
      expected_max: 0.315451
    }
  ];

  // The primary return water temperature sensor.
  //
  // Composed of:
  //   primary (descriptor): Associated with the primary (production) loop
  //     of a production process.
  //   return (descriptor): Measurement or process of media as it is
  //     returned from the end-use equipment within the system.
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `primary_return_water_temperature_sensor`, point type
  // `sensor`. Measured as temperature; values are in kelvin, the ontology's
  // standard unit for that quantity.
  //
  // Expected range 255.9259259 to 380.3703704. This is a data-quality
  // expectation, not a constraint: a reading outside it is a fault to
  // report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double primary_return_water_temperature_sensor = 116 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "primary_return_water_temperature_sensor"
      subfields: "primary"
      subfields: "return"
      subfields: "water"
      subfields: "temperature"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "temperature"
      standard_unit: "kelvin"
      expected_min: 255.925926
      expected_max: 380.37037
    }
  ];

  // The primary supply water flowrate sensor.
  //
  // Composed of:
  //   primary (descriptor): Associated with the primary (production) loop
  //     of a production process.
  //   supply (descriptor): Measurement or process of media as it is
  //     supplied to the end-use equipment within the system.
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   flowrate (measurement): Rate of fluid movement.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `primary_supply_water_flowrate_sensor`, point type
  // `sensor`. Measured as flowrate; values are in cubic_meters_per_second,
  // the ontology's standard unit for that quantity.
  //
  // Expected range -0.000315451 to 0.315451. This is a data-quality
  // expectation, not a constraint: a reading outside it is a fault to
  // report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double primary_supply_water_flowrate_sensor = 117 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "primary_supply_water_flowrate_sensor"
      subfields: "primary"
      subfields: "supply"
      subfields: "water"
      subfields: "flowrate"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "flowrate"
      standard_unit: "cubic_meters_per_second"
      expected_min: -0.000315
      expected_max: 0.315451
    }
  ];

  // The primary supply water flowrate setpoint.
  //
  // Composed of:
  //   primary (descriptor): Associated with the primary (production) loop
  //     of a production process.
  //   supply (descriptor): Measurement or process of media as it is
  //     supplied to the end-use equipment within the system.
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   flowrate (measurement): Rate of fluid movement.
  //   setpoint (point_type): Control target of process or system.
  //
  // Ontology field: `primary_supply_water_flowrate_setpoint`, point type
  // `setpoint`. Measured as flowrate; values are in cubic_meters_per_second,
  // the ontology's standard unit for that quantity.
  //
  // Expected range -0.000315451 to 0.315451. This is a data-quality
  // expectation, not a constraint: a reading outside it is a fault to
  // report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double primary_supply_water_flowrate_setpoint = 118 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "primary_supply_water_flowrate_setpoint"
      subfields: "primary"
      subfields: "supply"
      subfields: "water"
      subfields: "flowrate"
      subfields: "setpoint"
      point_type: "setpoint"
      measurement: "flowrate"
      standard_unit: "cubic_meters_per_second"
      expected_min: -0.000315
      expected_max: 0.315451
    }
  ];

  // The primary supply water temperature sensor.
  //
  // Composed of:
  //   primary (descriptor): Associated with the primary (production) loop
  //     of a production process.
  //   supply (descriptor): Measurement or process of media as it is
  //     supplied to the end-use equipment within the system.
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `primary_supply_water_temperature_sensor`, point type
  // `sensor`. Measured as temperature; values are in kelvin, the ontology's
  // standard unit for that quantity.
  //
  // Expected range 255.9259259 to 380.3703704. This is a data-quality
  // expectation, not a constraint: a reading outside it is a fault to
  // report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double primary_supply_water_temperature_sensor = 119 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "primary_supply_water_temperature_sensor"
      subfields: "primary"
      subfields: "supply"
      subfields: "water"
      subfields: "temperature"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "temperature"
      standard_unit: "kelvin"
      expected_min: 255.925926
      expected_max: 380.37037
    }
  ];

  // The primary supply water temperature setpoint.
  //
  // Composed of:
  //   primary (descriptor): Associated with the primary (production) loop
  //     of a production process.
  //   supply (descriptor): Measurement or process of media as it is
  //     supplied to the end-use equipment within the system.
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   setpoint (point_type): Control target of process or system.
  //
  // Ontology field: `primary_supply_water_temperature_setpoint`, point type
  // `setpoint`. Measured as temperature; values are in kelvin, the
  // ontology's standard unit for that quantity.
  //
  // Expected range 255.9259259 to 380.3703704. This is a data-quality
  // expectation, not a constraint: a reading outside it is a fault to
  // report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double primary_supply_water_temperature_setpoint = 120 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "primary_supply_water_temperature_setpoint"
      subfields: "primary"
      subfields: "supply"
      subfields: "water"
      subfields: "temperature"
      subfields: "setpoint"
      point_type: "setpoint"
      measurement: "temperature"
      standard_unit: "kelvin"
      expected_min: 255.925926
      expected_max: 380.37037
    }
  ];

  // The primary water cooling thermal power sensor.
  //
  // Composed of:
  //   primary (descriptor): Associated with the primary (production) loop
  //     of a production process.
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   cooling (descriptor): Process, measured effect or requirement for
  //     cooling.
  //   thermal (measurement_descriptor): Relating to the transfer of heat.
  //   power (measurement): Rate of energy consumption (assumed context is
  //     electrical and real, unless otherwise modified).
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `primary_water_cooling_thermal_power_sensor`, point type
  // `sensor`. Measured as power; values are in watts, the ontology's
  // standard unit for that quantity.
  //
  // Expected range 0 to 3.517e+06. This is a data-quality expectation, not a
  // constraint: a reading outside it is a fault to report, not a message to
  // reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double primary_water_cooling_thermal_power_sensor = 121 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "primary_water_cooling_thermal_power_sensor"
      subfields: "primary"
      subfields: "water"
      subfields: "cooling"
      subfields: "thermal"
      subfields: "power"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "power"
      standard_unit: "watts"
      expected_min: 0.0
      expected_max: 3517000.0
    }
  ];

  // The primary water differential pressure sensor.
  //
  // Composed of:
  //   primary (descriptor): Associated with the primary (production) loop
  //     of a production process.
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   differential (measurement_descriptor): Measured difference between
  //     two reference points (e.g. differential_water_pressure)
  //   pressure (measurement): Measured force per unit area within a system
  //     or process.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `primary_water_differential_pressure_sensor`, point type
  // `sensor`. Measured as pressure; values are in pascals, the ontology's
  // standard unit for that quantity.
  //
  // Expected range -34473.78945 to 344737.8945. This is a data-quality
  // expectation, not a constraint: a reading outside it is a fault to
  // report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double primary_water_differential_pressure_sensor = 122 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "primary_water_differential_pressure_sensor"
      subfields: "primary"
      subfields: "water"
      subfields: "differential"
      subfields: "pressure"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "pressure"
      standard_unit: "pascals"
      expected_min: -34473.78945
      expected_max: 344737.8945
    }
  ];

  // The primary water differential pressure setpoint.
  //
  // Composed of:
  //   primary (descriptor): Associated with the primary (production) loop
  //     of a production process.
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   differential (measurement_descriptor): Measured difference between
  //     two reference points (e.g. differential_water_pressure)
  //   pressure (measurement): Measured force per unit area within a system
  //     or process.
  //   setpoint (point_type): Control target of process or system.
  //
  // Ontology field: `primary_water_differential_pressure_setpoint`, point
  // type `setpoint`. Measured as pressure; values are in pascals, the
  // ontology's standard unit for that quantity.
  //
  // Expected range -34473.78945 to 344737.8945. This is a data-quality
  // expectation, not a constraint: a reading outside it is a fault to
  // report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double primary_water_differential_pressure_setpoint = 123 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "primary_water_differential_pressure_setpoint"
      subfields: "primary"
      subfields: "water"
      subfields: "differential"
      subfields: "pressure"
      subfields: "setpoint"
      point_type: "setpoint"
      measurement: "pressure"
      standard_unit: "pascals"
      expected_min: -34473.78945
      expected_max: 344737.8945
    }
  ];

  // The primary water heating thermal power sensor.
  //
  // Composed of:
  //   primary (descriptor): Associated with the primary (production) loop
  //     of a production process.
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   heating (descriptor): Process, measured effect or requirement for
  //     heating.
  //   thermal (measurement_descriptor): Relating to the transfer of heat.
  //   power (measurement): Rate of energy consumption (assumed context is
  //     electrical and real, unless otherwise modified).
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `primary_water_heating_thermal_power_sensor`, point type
  // `sensor`. Measured as power; values are in watts, the ontology's
  // standard unit for that quantity.
  //
  // Expected range 0 to 586142.14. This is a data-quality expectation, not a
  // constraint: a reading outside it is a fault to report, not a message to
  // reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double primary_water_heating_thermal_power_sensor = 124 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "primary_water_heating_thermal_power_sensor"
      subfields: "primary"
      subfields: "water"
      subfields: "heating"
      subfields: "thermal"
      subfields: "power"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "power"
      standard_unit: "watts"
      expected_min: 0.0
      expected_max: 586142.14
    }
  ];

  // The process water differential pressure sensor.
  //
  // Composed of:
  //   process (descriptor): Act of processing.
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   differential (measurement_descriptor): Measured difference between
  //     two reference points (e.g. differential_water_pressure)
  //   pressure (measurement): Measured force per unit area within a system
  //     or process.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `process_water_differential_pressure_sensor`, point type
  // `sensor`. Measured as pressure; values are in pascals, the ontology's
  // standard unit for that quantity.
  //
  // Expected range -3447.378945 to 344737.8945. This is a data-quality
  // expectation, not a constraint: a reading outside it is a fault to
  // report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  repeated double process_water_differential_pressure_sensor = 125 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "process_water_differential_pressure_sensor"
      subfields: "process"
      subfields: "water"
      subfields: "differential"
      subfields: "pressure"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "pressure"
      standard_unit: "pascals"
      enumerated: true
      expected_min: -3447.378945
      expected_max: 344737.8945
    }
  ];

  // The program differential pressure setpoint.
  //
  // Composed of:
  //   program (descriptor): Embedded, encoded instructions for the
  //     automatic performance of a task; typically within a controller.
  //   differential (measurement_descriptor): Measured difference between
  //     two reference points (e.g. differential_water_pressure)
  //   pressure (measurement): Measured force per unit area within a system
  //     or process.
  //   setpoint (point_type): Control target of process or system.
  //
  // Ontology field: `program_differential_pressure_setpoint`, point type
  // `setpoint`. Measured as pressure; values are in pascals, the ontology's
  // standard unit for that quantity.
  //
  // Expected range -6894.75789 to 137895.1578. This is a data-quality
  // expectation, not a constraint: a reading outside it is a fault to
  // report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double program_differential_pressure_setpoint = 126 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "program_differential_pressure_setpoint"
      subfields: "program"
      subfields: "differential"
      subfields: "pressure"
      subfields: "setpoint"
      point_type: "setpoint"
      measurement: "pressure"
      standard_unit: "pascals"
      expected_min: -6894.75789
      expected_max: 137895.1578
    }
  ];

  // The program supply water temperature setpoint.
  //
  // Composed of:
  //   program (descriptor): Embedded, encoded instructions for the
  //     automatic performance of a task; typically within a controller.
  //   supply (descriptor): Measurement or process of media as it is
  //     supplied to the end-use equipment within the system.
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   setpoint (point_type): Control target of process or system.
  //
  // Ontology field: `program_supply_water_temperature_setpoint`, point type
  // `setpoint`. Measured as temperature; values are in kelvin, the
  // ontology's standard unit for that quantity.
  //
  // Expected range 255.9259259 to 422.037037. This is a data-quality
  // expectation, not a constraint: a reading outside it is a fault to
  // report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double program_supply_water_temperature_setpoint = 127 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "program_supply_water_temperature_setpoint"
      subfields: "program"
      subfields: "supply"
      subfields: "water"
      subfields: "temperature"
      subfields: "setpoint"
      point_type: "setpoint"
      measurement: "temperature"
      standard_unit: "kelvin"
      expected_min: 255.925926
      expected_max: 422.037037
    }
  ];

  // The request run command.
  //
  // Composed of:
  //   request (descriptor): A signal from a system device that calls for
  //     more of a particular conditioning process (e.g., pressure requests
  //     from terminal units).
  //   run (measurement_descriptor): State of being active or operating. By
  //     default applies to control program for system (e.g., VAV program
  //     run_command).
  //   command (point_type): The signal given to make an action happen.
  //     Defaults to multistate unless used alongside a measurement_type.
  //
  // Ontology field: `request_run_command`, point type `command`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional OffOnValue request_run_command = 128 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "request_run_command"
      subfields: "request"
      subfields: "run"
      subfields: "command"
      point_type: "command"
    }
  ];

  // The return water changeover valve command.
  //
  // Composed of:
  //   return (descriptor): Measurement or process of media as it is
  //     returned from the end-use equipment within the system.
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   changeover (descriptor): Connecting to a different equipment circuit
  //     (e.g., switching between chiller and hot water).
  //   valve (component): Component which meters the flow of water within a
  //     system or device.
  //   command (point_type): The signal given to make an action happen.
  //     Defaults to multistate unless used alongside a measurement_type.
  //
  // Ontology field: `return_water_changeover_valve_command`, point type
  // `command`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ClosedOpenValue return_water_changeover_valve_command = 129 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "return_water_changeover_valve_command"
      subfields: "return"
      subfields: "water"
      subfields: "changeover"
      subfields: "valve"
      subfields: "command"
      point_type: "command"
    }
  ];

  // The return water changeover valve percentage sensor.
  //
  // Composed of:
  //   return (descriptor): Measurement or process of media as it is
  //     returned from the end-use equipment within the system.
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   changeover (descriptor): Connecting to a different equipment circuit
  //     (e.g., switching between chiller and hot water).
  //   valve (component): Component which meters the flow of water within a
  //     system or device.
  //   percentage (measurement): Measurement per hundred.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `return_water_changeover_valve_percentage_sensor`, point
  // type `sensor`. Measured as percentage; values are in percent, the
  // ontology's standard unit for that quantity.
  //
  // Valid range 0 to 100, enforced by protovalidate.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double return_water_changeover_valve_percentage_sensor = 130 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "return_water_changeover_valve_percentage_sensor"
      subfields: "return"
      subfields: "water"
      subfields: "changeover"
      subfields: "valve"
      subfields: "percentage"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "percentage"
      standard_unit: "percent"
    },
    (buf.validate.field).double.gte = 0.0,
    (buf.validate.field).double.lte = 100.0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The return water changeover valve status.
  //
  // Composed of:
  //   return (descriptor): Measurement or process of media as it is
  //     returned from the end-use equipment within the system.
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   changeover (descriptor): Connecting to a different equipment circuit
  //     (e.g., switching between chiller and hot water).
  //   valve (component): Component which meters the flow of water within a
  //     system or device.
  //   status (point_type): The multistate value indicating an observed
  //     state in a piece of equipment, often indicating if a command was
  //     effected. It is a neutral observation (i.e., no quality judgment of
  //     'good' or 'bad'). It also has no units of measurement; therefore, if
  //     combined with a measurement subfield, it will indicate that the
  //     field is the directional status based on some measurement of that
  //     type (e.g., power_status equates to an on/off value based on some
  //     inference of power).
  //
  // Ontology field: `return_water_changeover_valve_status`, point type
  // `status`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ClosedOpenValue return_water_changeover_valve_status = 131 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "return_water_changeover_valve_status"
      subfields: "return"
      subfields: "water"
      subfields: "changeover"
      subfields: "valve"
      subfields: "status"
      point_type: "status"
    }
  ];

  // The return water isolation valve command.
  //
  // Composed of:
  //   return (descriptor): Measurement or process of media as it is
  //     returned from the end-use equipment within the system.
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   isolation (descriptor): Process of isolating one component or
  //     process from another.
  //   valve (component): Component which meters the flow of water within a
  //     system or device.
  //   command (point_type): The signal given to make an action happen.
  //     Defaults to multistate unless used alongside a measurement_type.
  //
  // Ontology field: `return_water_isolation_valve_command`, point type
  // `command`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ClosedOpenValue return_water_isolation_valve_command = 132 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "return_water_isolation_valve_command"
      subfields: "return"
      subfields: "water"
      subfields: "isolation"
      subfields: "valve"
      subfields: "command"
      point_type: "command"
    }
  ];

  // The return water isolation valve percentage command.
  //
  // Composed of:
  //   return (descriptor): Measurement or process of media as it is
  //     returned from the end-use equipment within the system.
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   isolation (descriptor): Process of isolating one component or
  //     process from another.
  //   valve (component): Component which meters the flow of water within a
  //     system or device.
  //   percentage (measurement): Measurement per hundred.
  //   command (point_type): The signal given to make an action happen.
  //     Defaults to multistate unless used alongside a measurement_type.
  //
  // Ontology field: `return_water_isolation_valve_percentage_command`, point
  // type `command`. Measured as percentage; values are in percent, the
  // ontology's standard unit for that quantity.
  //
  // Valid range 0 to 100, enforced by protovalidate.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double return_water_isolation_valve_percentage_command = 133 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "return_water_isolation_valve_percentage_command"
      subfields: "return"
      subfields: "water"
      subfields: "isolation"
      subfields: "valve"
      subfields: "percentage"
      subfields: "command"
      point_type: "command"
      measurement: "percentage"
      standard_unit: "percent"
    },
    (buf.validate.field).double.gte = 0.0,
    (buf.validate.field).double.lte = 100.0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The return water isolation valve percentage sensor.
  //
  // Composed of:
  //   return (descriptor): Measurement or process of media as it is
  //     returned from the end-use equipment within the system.
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   isolation (descriptor): Process of isolating one component or
  //     process from another.
  //   valve (component): Component which meters the flow of water within a
  //     system or device.
  //   percentage (measurement): Measurement per hundred.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `return_water_isolation_valve_percentage_sensor`, point
  // type `sensor`. Measured as percentage; values are in percent, the
  // ontology's standard unit for that quantity.
  //
  // Valid range 0 to 100, enforced by protovalidate.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double return_water_isolation_valve_percentage_sensor = 134 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "return_water_isolation_valve_percentage_sensor"
      subfields: "return"
      subfields: "water"
      subfields: "isolation"
      subfields: "valve"
      subfields: "percentage"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "percentage"
      standard_unit: "percent"
    },
    (buf.validate.field).double.gte = 0.0,
    (buf.validate.field).double.lte = 100.0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The return water isolation valve status.
  //
  // Composed of:
  //   return (descriptor): Measurement or process of media as it is
  //     returned from the end-use equipment within the system.
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   isolation (descriptor): Process of isolating one component or
  //     process from another.
  //   valve (component): Component which meters the flow of water within a
  //     system or device.
  //   status (point_type): The multistate value indicating an observed
  //     state in a piece of equipment, often indicating if a command was
  //     effected. It is a neutral observation (i.e., no quality judgment of
  //     'good' or 'bad'). It also has no units of measurement; therefore, if
  //     combined with a measurement subfield, it will indicate that the
  //     field is the directional status based on some measurement of that
  //     type (e.g., power_status equates to an on/off value based on some
  //     inference of power).
  //
  // Ontology field: `return_water_isolation_valve_status`, point type
  // `status`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ClosedOpenValue return_water_isolation_valve_status = 135 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "return_water_isolation_valve_status"
      subfields: "return"
      subfields: "water"
      subfields: "isolation"
      subfields: "valve"
      subfields: "status"
      point_type: "status"
    }
  ];

  // The return water temperature sensor.
  //
  // Composed of:
  //   return (descriptor): Measurement or process of media as it is
  //     returned from the end-use equipment within the system.
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `return_water_temperature_sensor`, point type `sensor`.
  // Measured as temperature; values are in kelvin, the ontology's standard
  // unit for that quantity.
  //
  // Expected range 255.9259259 to 380.3703704. This is a data-quality
  // expectation, not a constraint: a reading outside it is a fault to
  // report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  repeated double return_water_temperature_sensor = 136 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "return_water_temperature_sensor"
      subfields: "return"
      subfields: "water"
      subfields: "temperature"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "temperature"
      standard_unit: "kelvin"
      enumerated: true
      expected_min: 255.925926
      expected_max: 380.37037
    }
  ];

  // The return water temperature setpoint.
  //
  // Composed of:
  //   return (descriptor): Measurement or process of media as it is
  //     returned from the end-use equipment within the system.
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   setpoint (point_type): Control target of process or system.
  //
  // Ontology field: `return_water_temperature_setpoint`, point type
  // `setpoint`. Measured as temperature; values are in kelvin, the
  // ontology's standard unit for that quantity.
  //
  // Expected range 255.9259259 to 380.3703704. This is a data-quality
  // expectation, not a constraint: a reading outside it is a fault to
  // report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double return_water_temperature_setpoint = 137 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "return_water_temperature_setpoint"
      subfields: "return"
      subfields: "water"
      subfields: "temperature"
      subfields: "setpoint"
      point_type: "setpoint"
      measurement: "temperature"
      standard_unit: "kelvin"
      expected_min: 255.925926
      expected_max: 380.37037
    }
  ];

  // The run command.
  //
  // Composed of:
  //   run (measurement_descriptor): State of being active or operating. By
  //     default applies to control program for system (e.g., VAV program
  //     run_command).
  //   command (point_type): The signal given to make an action happen.
  //     Defaults to multistate unless used alongside a measurement_type.
  //
  // Ontology field: `run_command`, point type `command`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional OffOnValue run_command = 138 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "run_command"
      subfields: "run"
      subfields: "command"
      point_type: "command"
    }
  ];

  // The run mode.
  //
  // Composed of:
  //   run (measurement_descriptor): State of being active or operating. By
  //     default applies to control program for system (e.g., VAV program
  //     run_command).
  //   mode (point_type): Distinct mode of operation within system. For
  //     example economizer_mode has modes of 'enabled' or 'disabled.'
  //
  // Ontology field: `run_mode`, point type `mode`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional AutoCommissioningCoolingDehumidifyFanOnlyFlushingHeatingManualNeutralOffUnknownValue run_mode = 139 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "run_mode"
      subfields: "run"
      subfields: "mode"
      point_type: "mode"
    }
  ];

  // The run status.
  //
  // Composed of:
  //   run (measurement_descriptor): State of being active or operating. By
  //     default applies to control program for system (e.g., VAV program
  //     run_command).
  //   status (point_type): The multistate value indicating an observed
  //     state in a piece of equipment, often indicating if a command was
  //     effected. It is a neutral observation (i.e., no quality judgment of
  //     'good' or 'bad'). It also has no units of measurement; therefore, if
  //     combined with a measurement subfield, it will indicate that the
  //     field is the directional status based on some measurement of that
  //     type (e.g., power_status equates to an on/off value based on some
  //     inference of power).
  //
  // Ontology field: `run_status`, point type `status`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional OffOnStandbyUnknownValue run_status = 140 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "run_status"
      subfields: "run"
      subfields: "status"
      point_type: "status"
    }
  ];

  // The run time accumulator.
  //
  // Composed of:
  //   run (measurement_descriptor): State of being active or operating. By
  //     default applies to control program for system (e.g., VAV program
  //     run_command).
  //   time (measurement): Defined duration of something.
  //   accumulator (point_type): The total accumulated quantity (e.g.,
  //     total energy accumulated).
  //
  // Ontology field: `run_time_accumulator`, point type `accumulator`.
  // Measured as time; values are in seconds, the ontology's standard unit
  // for that quantity.
  //
  // Valid range from 0, enforced by protovalidate. Expected range up to
  // 1.25. This is a data-quality expectation, not a constraint: a reading
  // outside it is a fault to report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double run_time_accumulator = 141 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "run_time_accumulator"
      subfields: "run"
      subfields: "time"
      subfields: "accumulator"
      point_type: "accumulator"
      measurement: "time"
      standard_unit: "seconds"
      expected_max: 1.25
    },
    (buf.validate.field).double.gte = 0.0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The schedule run command.
  //
  // Composed of:
  //   schedule (descriptor): Refers to the time-of-day run-time
  //     requirements for the equipment.
  //   run (measurement_descriptor): State of being active or operating. By
  //     default applies to control program for system (e.g., VAV program
  //     run_command).
  //   command (point_type): The signal given to make an action happen.
  //     Defaults to multistate unless used alongside a measurement_type.
  //
  // Ontology field: `schedule_run_command`, point type `command`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional OffOnUnknownValue schedule_run_command = 142 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "schedule_run_command"
      subfields: "schedule"
      subfields: "run"
      subfields: "command"
      point_type: "command"
    }
  ];

  // The secondary return water flowrate sensor.
  //
  // Composed of:
  //   secondary (descriptor): Associated with the secondary (distribution)
  //     loop of a produciton process.
  //   return (descriptor): Measurement or process of media as it is
  //     returned from the end-use equipment within the system.
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   flowrate (measurement): Rate of fluid movement.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `secondary_return_water_flowrate_sensor`, point type
  // `sensor`. Measured as flowrate; values are in cubic_meters_per_second,
  // the ontology's standard unit for that quantity.
  //
  // Expected range -0.000315451 to 0.315451. This is a data-quality
  // expectation, not a constraint: a reading outside it is a fault to
  // report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double secondary_return_water_flowrate_sensor = 143 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "secondary_return_water_flowrate_sensor"
      subfields: "secondary"
      subfields: "return"
      subfields: "water"
      subfields: "flowrate"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "flowrate"
      standard_unit: "cubic_meters_per_second"
      expected_min: -0.000315
      expected_max: 0.315451
    }
  ];

  // The secondary return water temperature sensor.
  //
  // Composed of:
  //   secondary (descriptor): Associated with the secondary (distribution)
  //     loop of a produciton process.
  //   return (descriptor): Measurement or process of media as it is
  //     returned from the end-use equipment within the system.
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `secondary_return_water_temperature_sensor`, point type
  // `sensor`. Measured as temperature; values are in kelvin, the ontology's
  // standard unit for that quantity.
  //
  // Expected range 255.9259259 to 380.3703704. This is a data-quality
  // expectation, not a constraint: a reading outside it is a fault to
  // report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double secondary_return_water_temperature_sensor = 144 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "secondary_return_water_temperature_sensor"
      subfields: "secondary"
      subfields: "return"
      subfields: "water"
      subfields: "temperature"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "temperature"
      standard_unit: "kelvin"
      expected_min: 255.925926
      expected_max: 380.37037
    }
  ];

  // The secondary supply water flowrate sensor.
  //
  // Composed of:
  //   secondary (descriptor): Associated with the secondary (distribution)
  //     loop of a produciton process.
  //   supply (descriptor): Measurement or process of media as it is
  //     supplied to the end-use equipment within the system.
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   flowrate (measurement): Rate of fluid movement.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `secondary_supply_water_flowrate_sensor`, point type
  // `sensor`. Measured as flowrate; values are in cubic_meters_per_second,
  // the ontology's standard unit for that quantity.
  //
  // Expected range -0.000315451 to 0.315451. This is a data-quality
  // expectation, not a constraint: a reading outside it is a fault to
  // report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double secondary_supply_water_flowrate_sensor = 145 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "secondary_supply_water_flowrate_sensor"
      subfields: "secondary"
      subfields: "supply"
      subfields: "water"
      subfields: "flowrate"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "flowrate"
      standard_unit: "cubic_meters_per_second"
      expected_min: -0.000315
      expected_max: 0.315451
    }
  ];

  // The secondary supply water flowrate setpoint.
  //
  // Composed of:
  //   secondary (descriptor): Associated with the secondary (distribution)
  //     loop of a produciton process.
  //   supply (descriptor): Measurement or process of media as it is
  //     supplied to the end-use equipment within the system.
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   flowrate (measurement): Rate of fluid movement.
  //   setpoint (point_type): Control target of process or system.
  //
  // Ontology field: `secondary_supply_water_flowrate_setpoint`, point type
  // `setpoint`. Measured as flowrate; values are in cubic_meters_per_second,
  // the ontology's standard unit for that quantity.
  //
  // Expected range -0.000315451 to 0.315451. This is a data-quality
  // expectation, not a constraint: a reading outside it is a fault to
  // report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double secondary_supply_water_flowrate_setpoint = 146 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "secondary_supply_water_flowrate_setpoint"
      subfields: "secondary"
      subfields: "supply"
      subfields: "water"
      subfields: "flowrate"
      subfields: "setpoint"
      point_type: "setpoint"
      measurement: "flowrate"
      standard_unit: "cubic_meters_per_second"
      expected_min: -0.000315
      expected_max: 0.315451
    }
  ];

  // The secondary supply water temperature sensor.
  //
  // Composed of:
  //   secondary (descriptor): Associated with the secondary (distribution)
  //     loop of a produciton process.
  //   supply (descriptor): Measurement or process of media as it is
  //     supplied to the end-use equipment within the system.
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `secondary_supply_water_temperature_sensor`, point type
  // `sensor`. Measured as temperature; values are in kelvin, the ontology's
  // standard unit for that quantity.
  //
  // Expected range 255.9259259 to 380.3703704. This is a data-quality
  // expectation, not a constraint: a reading outside it is a fault to
  // report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double secondary_supply_water_temperature_sensor = 147 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "secondary_supply_water_temperature_sensor"
      subfields: "secondary"
      subfields: "supply"
      subfields: "water"
      subfields: "temperature"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "temperature"
      standard_unit: "kelvin"
      expected_min: 255.925926
      expected_max: 380.37037
    }
  ];

  // The secondary supply water temperature setpoint.
  //
  // Composed of:
  //   secondary (descriptor): Associated with the secondary (distribution)
  //     loop of a produciton process.
  //   supply (descriptor): Measurement or process of media as it is
  //     supplied to the end-use equipment within the system.
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   setpoint (point_type): Control target of process or system.
  //
  // Ontology field: `secondary_supply_water_temperature_setpoint`, point
  // type `setpoint`. Measured as temperature; values are in kelvin, the
  // ontology's standard unit for that quantity.
  //
  // Expected range 255.9259259 to 380.3703704. This is a data-quality
  // expectation, not a constraint: a reading outside it is a fault to
  // report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double secondary_supply_water_temperature_setpoint = 148 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "secondary_supply_water_temperature_setpoint"
      subfields: "secondary"
      subfields: "supply"
      subfields: "water"
      subfields: "temperature"
      subfields: "setpoint"
      point_type: "setpoint"
      measurement: "temperature"
      standard_unit: "kelvin"
      expected_min: 255.925926
      expected_max: 380.37037
    }
  ];

  // The secondary water cooling thermal power sensor.
  //
  // Composed of:
  //   secondary (descriptor): Associated with the secondary (distribution)
  //     loop of a produciton process.
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   cooling (descriptor): Process, measured effect or requirement for
  //     cooling.
  //   thermal (measurement_descriptor): Relating to the transfer of heat.
  //   power (measurement): Rate of energy consumption (assumed context is
  //     electrical and real, unless otherwise modified).
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `secondary_water_cooling_thermal_power_sensor`, point
  // type `sensor`. Measured as power; values are in watts, the ontology's
  // standard unit for that quantity.
  //
  // Expected range 0 to 3.517e+06. This is a data-quality expectation, not a
  // constraint: a reading outside it is a fault to report, not a message to
  // reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double secondary_water_cooling_thermal_power_sensor = 149 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "secondary_water_cooling_thermal_power_sensor"
      subfields: "secondary"
      subfields: "water"
      subfields: "cooling"
      subfields: "thermal"
      subfields: "power"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "power"
      standard_unit: "watts"
      expected_min: 0.0
      expected_max: 3517000.0
    }
  ];

  // The secondary water differential pressure sensor.
  //
  // Composed of:
  //   secondary (descriptor): Associated with the secondary (distribution)
  //     loop of a produciton process.
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   differential (measurement_descriptor): Measured difference between
  //     two reference points (e.g. differential_water_pressure)
  //   pressure (measurement): Measured force per unit area within a system
  //     or process.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `secondary_water_differential_pressure_sensor`, point
  // type `sensor`. Measured as pressure; values are in pascals, the
  // ontology's standard unit for that quantity.
  //
  // Expected range -34473.78945 to 344737.8945. This is a data-quality
  // expectation, not a constraint: a reading outside it is a fault to
  // report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double secondary_water_differential_pressure_sensor = 150 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "secondary_water_differential_pressure_sensor"
      subfields: "secondary"
      subfields: "water"
      subfields: "differential"
      subfields: "pressure"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "pressure"
      standard_unit: "pascals"
      expected_min: -34473.78945
      expected_max: 344737.8945
    }
  ];

  // The secondary water differential pressure setpoint.
  //
  // Composed of:
  //   secondary (descriptor): Associated with the secondary (distribution)
  //     loop of a produciton process.
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   differential (measurement_descriptor): Measured difference between
  //     two reference points (e.g. differential_water_pressure)
  //   pressure (measurement): Measured force per unit area within a system
  //     or process.
  //   setpoint (point_type): Control target of process or system.
  //
  // Ontology field: `secondary_water_differential_pressure_setpoint`, point
  // type `setpoint`. Measured as pressure; values are in pascals, the
  // ontology's standard unit for that quantity.
  //
  // Expected range -34473.78945 to 344737.8945. This is a data-quality
  // expectation, not a constraint: a reading outside it is a fault to
  // report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double secondary_water_differential_pressure_setpoint = 151 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "secondary_water_differential_pressure_setpoint"
      subfields: "secondary"
      subfields: "water"
      subfields: "differential"
      subfields: "pressure"
      subfields: "setpoint"
      point_type: "setpoint"
      measurement: "pressure"
      standard_unit: "pascals"
      expected_min: -34473.78945
      expected_max: 344737.8945
    }
  ];

  // The secondary water heating thermal power sensor.
  //
  // Composed of:
  //   secondary (descriptor): Associated with the secondary (distribution)
  //     loop of a produciton process.
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   heating (descriptor): Process, measured effect or requirement for
  //     heating.
  //   thermal (measurement_descriptor): Relating to the transfer of heat.
  //   power (measurement): Rate of energy consumption (assumed context is
  //     electrical and real, unless otherwise modified).
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `secondary_water_heating_thermal_power_sensor`, point
  // type `sensor`. Measured as power; values are in watts, the ontology's
  // standard unit for that quantity.
  //
  // Expected range 0 to 586142.14. This is a data-quality expectation, not a
  // constraint: a reading outside it is a fault to report, not a message to
  // reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double secondary_water_heating_thermal_power_sensor = 152 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "secondary_water_heating_thermal_power_sensor"
      subfields: "secondary"
      subfields: "water"
      subfields: "heating"
      subfields: "thermal"
      subfields: "power"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "power"
      standard_unit: "watts"
      expected_min: 0.0
      expected_max: 586142.14
    }
  ];

  // The suction pressure sensor.
  //
  // Composed of:
  //   suction (descriptor): The production of a partial vacuum in order to
  //     force fluid into a vacant space (such as refrigerant in a
  //     compressor).
  //   pressure (measurement): Measured force per unit area within a system
  //     or process.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `suction_pressure_sensor`, point type `sensor`. Measured
  // as pressure; values are in pascals, the ontology's standard unit for
  // that quantity.
  //
  // Expected range -34473.78945 to 344737.8945. This is a data-quality
  // expectation, not a constraint: a reading outside it is a fault to
  // report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double suction_pressure_sensor = 153 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "suction_pressure_sensor"
      subfields: "suction"
      subfields: "pressure"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "pressure"
      standard_unit: "pascals"
      expected_min: -34473.78945
      expected_max: 344737.8945
    }
  ];

  // The supervisor alarm.
  //
  // Composed of:
  //   supervisor (descriptor): A program that controls a device from a
  //     supervisory (i.e., remote or external) level.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `supervisor_alarm`, point type `alarm`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue supervisor_alarm = 154 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "supervisor_alarm"
      subfields: "supervisor"
      subfields: "alarm"
      point_type: "alarm"
    }
  ];

  // The supervisor control mode.
  //
  // Composed of:
  //   supervisor (descriptor): A program that controls a device from a
  //     supervisory (i.e., remote or external) level.
  //   control (descriptor): The process of governing the actions of a
  //     device.
  //   mode (point_type): Distinct mode of operation within system. For
  //     example economizer_mode has modes of 'enabled' or 'disabled.'
  //
  // Ontology field: `supervisor_control_mode`, point type `mode`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional DisabledEnabledValue supervisor_control_mode = 155 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "supervisor_control_mode"
      subfields: "supervisor"
      subfields: "control"
      subfields: "mode"
      point_type: "mode"
    }
  ];

  // The supervisor differential pressure setpoint.
  //
  // Composed of:
  //   supervisor (descriptor): A program that controls a device from a
  //     supervisory (i.e., remote or external) level.
  //   differential (measurement_descriptor): Measured difference between
  //     two reference points (e.g. differential_water_pressure)
  //   pressure (measurement): Measured force per unit area within a system
  //     or process.
  //   setpoint (point_type): Control target of process or system.
  //
  // Ontology field: `supervisor_differential_pressure_setpoint`, point type
  // `setpoint`. Measured as pressure; values are in pascals, the ontology's
  // standard unit for that quantity.
  //
  // Expected range -6894.75789 to 137895.1578. This is a data-quality
  // expectation, not a constraint: a reading outside it is a fault to
  // report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double supervisor_differential_pressure_setpoint = 156 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "supervisor_differential_pressure_setpoint"
      subfields: "supervisor"
      subfields: "differential"
      subfields: "pressure"
      subfields: "setpoint"
      point_type: "setpoint"
      measurement: "pressure"
      standard_unit: "pascals"
      expected_min: -6894.75789
      expected_max: 137895.1578
    }
  ];

  // The supervisor run command.
  //
  // Composed of:
  //   supervisor (descriptor): A program that controls a device from a
  //     supervisory (i.e., remote or external) level.
  //   run (measurement_descriptor): State of being active or operating. By
  //     default applies to control program for system (e.g., VAV program
  //     run_command).
  //   command (point_type): The signal given to make an action happen.
  //     Defaults to multistate unless used alongside a measurement_type.
  //
  // Ontology field: `supervisor_run_command`, point type `command`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional OffOnValue supervisor_run_command = 157 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "supervisor_run_command"
      subfields: "supervisor"
      subfields: "run"
      subfields: "command"
      point_type: "command"
    }
  ];

  // The supervisor supply water temperature setpoint.
  //
  // Composed of:
  //   supervisor (descriptor): A program that controls a device from a
  //     supervisory (i.e., remote or external) level.
  //   supply (descriptor): Measurement or process of media as it is
  //     supplied to the end-use equipment within the system.
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   setpoint (point_type): Control target of process or system.
  //
  // Ontology field: `supervisor_supply_water_temperature_setpoint`, point
  // type `setpoint`. Measured as temperature; values are in kelvin, the
  // ontology's standard unit for that quantity.
  //
  // Expected range 255.9259259 to 422.037037. This is a data-quality
  // expectation, not a constraint: a reading outside it is a fault to
  // report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double supervisor_supply_water_temperature_setpoint = 158 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "supervisor_supply_water_temperature_setpoint"
      subfields: "supervisor"
      subfields: "supply"
      subfields: "water"
      subfields: "temperature"
      subfields: "setpoint"
      point_type: "setpoint"
      measurement: "temperature"
      standard_unit: "kelvin"
      expected_min: 255.925926
      expected_max: 422.037037
    }
  ];

  // The supply water changeover valve command.
  //
  // Composed of:
  //   supply (descriptor): Measurement or process of media as it is
  //     supplied to the end-use equipment within the system.
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   changeover (descriptor): Connecting to a different equipment circuit
  //     (e.g., switching between chiller and hot water).
  //   valve (component): Component which meters the flow of water within a
  //     system or device.
  //   command (point_type): The signal given to make an action happen.
  //     Defaults to multistate unless used alongside a measurement_type.
  //
  // Ontology field: `supply_water_changeover_valve_command`, point type
  // `command`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ClosedOpenValue supply_water_changeover_valve_command = 159 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "supply_water_changeover_valve_command"
      subfields: "supply"
      subfields: "water"
      subfields: "changeover"
      subfields: "valve"
      subfields: "command"
      point_type: "command"
    }
  ];

  // The supply water changeover valve percentage sensor.
  //
  // Composed of:
  //   supply (descriptor): Measurement or process of media as it is
  //     supplied to the end-use equipment within the system.
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   changeover (descriptor): Connecting to a different equipment circuit
  //     (e.g., switching between chiller and hot water).
  //   valve (component): Component which meters the flow of water within a
  //     system or device.
  //   percentage (measurement): Measurement per hundred.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `supply_water_changeover_valve_percentage_sensor`, point
  // type `sensor`. Measured as percentage; values are in percent, the
  // ontology's standard unit for that quantity.
  //
  // Valid range 0 to 100, enforced by protovalidate.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double supply_water_changeover_valve_percentage_sensor = 160 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "supply_water_changeover_valve_percentage_sensor"
      subfields: "supply"
      subfields: "water"
      subfields: "changeover"
      subfields: "valve"
      subfields: "percentage"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "percentage"
      standard_unit: "percent"
    },
    (buf.validate.field).double.gte = 0.0,
    (buf.validate.field).double.lte = 100.0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The supply water changeover valve status.
  //
  // Composed of:
  //   supply (descriptor): Measurement or process of media as it is
  //     supplied to the end-use equipment within the system.
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   changeover (descriptor): Connecting to a different equipment circuit
  //     (e.g., switching between chiller and hot water).
  //   valve (component): Component which meters the flow of water within a
  //     system or device.
  //   status (point_type): The multistate value indicating an observed
  //     state in a piece of equipment, often indicating if a command was
  //     effected. It is a neutral observation (i.e., no quality judgment of
  //     'good' or 'bad'). It also has no units of measurement; therefore, if
  //     combined with a measurement subfield, it will indicate that the
  //     field is the directional status based on some measurement of that
  //     type (e.g., power_status equates to an on/off value based on some
  //     inference of power).
  //
  // Ontology field: `supply_water_changeover_valve_status`, point type
  // `status`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ClosedOpenValue supply_water_changeover_valve_status = 161 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "supply_water_changeover_valve_status"
      subfields: "supply"
      subfields: "water"
      subfields: "changeover"
      subfields: "valve"
      subfields: "status"
      point_type: "status"
    }
  ];

  // The supply water isolation valve command.
  //
  // Composed of:
  //   supply (descriptor): Measurement or process of media as it is
  //     supplied to the end-use equipment within the system.
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   isolation (descriptor): Process of isolating one component or
  //     process from another.
  //   valve (component): Component which meters the flow of water within a
  //     system or device.
  //   command (point_type): The signal given to make an action happen.
  //     Defaults to multistate unless used alongside a measurement_type.
  //
  // Ontology field: `supply_water_isolation_valve_command`, point type
  // `command`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ClosedOpenValue supply_water_isolation_valve_command = 162 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "supply_water_isolation_valve_command"
      subfields: "supply"
      subfields: "water"
      subfields: "isolation"
      subfields: "valve"
      subfields: "command"
      point_type: "command"
    }
  ];

  // The supply water isolation valve percentage command.
  //
  // Composed of:
  //   supply (descriptor): Measurement or process of media as it is
  //     supplied to the end-use equipment within the system.
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   isolation (descriptor): Process of isolating one component or
  //     process from another.
  //   valve (component): Component which meters the flow of water within a
  //     system or device.
  //   percentage (measurement): Measurement per hundred.
  //   command (point_type): The signal given to make an action happen.
  //     Defaults to multistate unless used alongside a measurement_type.
  //
  // Ontology field: `supply_water_isolation_valve_percentage_command`, point
  // type `command`. Measured as percentage; values are in percent, the
  // ontology's standard unit for that quantity.
  //
  // Valid range 0 to 100, enforced by protovalidate.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double supply_water_isolation_valve_percentage_command = 163 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "supply_water_isolation_valve_percentage_command"
      subfields: "supply"
      subfields: "water"
      subfields: "isolation"
      subfields: "valve"
      subfields: "percentage"
      subfields: "command"
      point_type: "command"
      measurement: "percentage"
      standard_unit: "percent"
    },
    (buf.validate.field).double.gte = 0.0,
    (buf.validate.field).double.lte = 100.0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The supply water isolation valve percentage sensor.
  //
  // Composed of:
  //   supply (descriptor): Measurement or process of media as it is
  //     supplied to the end-use equipment within the system.
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   isolation (descriptor): Process of isolating one component or
  //     process from another.
  //   valve (component): Component which meters the flow of water within a
  //     system or device.
  //   percentage (measurement): Measurement per hundred.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `supply_water_isolation_valve_percentage_sensor`, point
  // type `sensor`. Measured as percentage; values are in percent, the
  // ontology's standard unit for that quantity.
  //
  // Valid range 0 to 100, enforced by protovalidate.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double supply_water_isolation_valve_percentage_sensor = 164 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "supply_water_isolation_valve_percentage_sensor"
      subfields: "supply"
      subfields: "water"
      subfields: "isolation"
      subfields: "valve"
      subfields: "percentage"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "percentage"
      standard_unit: "percent"
    },
    (buf.validate.field).double.gte = 0.0,
    (buf.validate.field).double.lte = 100.0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The supply water isolation valve status.
  //
  // Composed of:
  //   supply (descriptor): Measurement or process of media as it is
  //     supplied to the end-use equipment within the system.
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   isolation (descriptor): Process of isolating one component or
  //     process from another.
  //   valve (component): Component which meters the flow of water within a
  //     system or device.
  //   status (point_type): The multistate value indicating an observed
  //     state in a piece of equipment, often indicating if a command was
  //     effected. It is a neutral observation (i.e., no quality judgment of
  //     'good' or 'bad'). It also has no units of measurement; therefore, if
  //     combined with a measurement subfield, it will indicate that the
  //     field is the directional status based on some measurement of that
  //     type (e.g., power_status equates to an on/off value based on some
  //     inference of power).
  //
  // Ontology field: `supply_water_isolation_valve_status`, point type
  // `status`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ClosedOpenValue supply_water_isolation_valve_status = 165 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "supply_water_isolation_valve_status"
      subfields: "supply"
      subfields: "water"
      subfields: "isolation"
      subfields: "valve"
      subfields: "status"
      point_type: "status"
    }
  ];

  // The supply water temperature sensor.
  //
  // Composed of:
  //   supply (descriptor): Measurement or process of media as it is
  //     supplied to the end-use equipment within the system.
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `supply_water_temperature_sensor`, point type `sensor`.
  // Measured as temperature; values are in kelvin, the ontology's standard
  // unit for that quantity.
  //
  // Expected range 255.9259259 to 380.3703704. This is a data-quality
  // expectation, not a constraint: a reading outside it is a fault to
  // report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double supply_water_temperature_sensor = 166 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "supply_water_temperature_sensor"
      subfields: "supply"
      subfields: "water"
      subfields: "temperature"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "temperature"
      standard_unit: "kelvin"
      expected_min: 255.925926
      expected_max: 380.37037
    }
  ];

  // The supply water temperature setpoint.
  //
  // Composed of:
  //   supply (descriptor): Measurement or process of media as it is
  //     supplied to the end-use equipment within the system.
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   setpoint (point_type): Control target of process or system.
  //
  // Ontology field: `supply_water_temperature_setpoint`, point type
  // `setpoint`. Measured as temperature; values are in kelvin, the
  // ontology's standard unit for that quantity.
  //
  // Expected range 255.9259259 to 380.3703704. This is a data-quality
  // expectation, not a constraint: a reading outside it is a fault to
  // report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double supply_water_temperature_setpoint = 167 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "supply_water_temperature_setpoint"
      subfields: "supply"
      subfields: "water"
      subfields: "temperature"
      subfields: "setpoint"
      point_type: "setpoint"
      measurement: "temperature"
      standard_unit: "kelvin"
      expected_min: 255.925926
      expected_max: 380.37037
    }
  ];

  // The switch command.
  //
  // Composed of:
  //   switch (component): Component to disrupt an electrical cirucit.
  //   command (point_type): The signal given to make an action happen.
  //     Defaults to multistate unless used alongside a measurement_type.
  //
  // Ontology field: `switch_command`, point type `command`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional OffOnValue switch_command = 168 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "switch_command"
      subfields: "switch"
      subfields: "command"
      point_type: "command"
    }
  ];

  // The sync economizer mechanical cooling mode.
  //
  // Composed of:
  //   sync (descriptor): The simultaneous operation or activity of two or
  //     more things.
  //   economizer (descriptor): Process or component responsible for the
  //     utilization of free cooling.
  //   mechanical (descriptor): A process driven by machinery; specifically
  //     applied to cooling processes (e. g. using a refrigeration cycle for
  //     cooling).
  //   cooling (descriptor): Process, measured effect or requirement for
  //     cooling.
  //   mode (point_type): Distinct mode of operation within system. For
  //     example economizer_mode has modes of 'enabled' or 'disabled.'
  //
  // Ontology field: `sync_economizer_mechanical_cooling_mode`, point type
  // `mode`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional OffOnValue sync_economizer_mechanical_cooling_mode = 169 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "sync_economizer_mechanical_cooling_mode"
      subfields: "sync"
      subfields: "economizer"
      subfields: "mechanical"
      subfields: "cooling"
      subfields: "mode"
      point_type: "mode"
    }
  ];

  // The thermal power capacity.
  //
  // Composed of:
  //   thermal (measurement_descriptor): Relating to the transfer of heat.
  //   power (measurement): Rate of energy consumption (assumed context is
  //     electrical and real, unless otherwise modified).
  //   capacity (point_type): A design parameter quantity (e.g., design
  //     motor power capacity). Is always a maximum limit.
  //
  // Ontology field: `thermal_power_capacity`, point type `capacity`.
  // Measured as power; values are in watts, the ontology's standard unit for
  // that quantity.
  //
  // Valid range from 0, enforced by protovalidate. Expected range up to
  // 2.9307107e+06. This is a data-quality expectation, not a constraint: a
  // reading outside it is a fault to report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double thermal_power_capacity = 170 [
    (google.api.field_behavior) = OPTIONAL,
    (google.api.field_behavior) = IMMUTABLE,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "thermal_power_capacity"
      subfields: "thermal"
      subfields: "power"
      subfields: "capacity"
      point_type: "capacity"
      measurement: "power"
      standard_unit: "watts"
      expected_max: 2930710.7
    },
    (buf.validate.field).double.gte = 0.0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The thermally conditioned water differential pressure sensor.
  //
  // Composed of:
  //   thermally (descriptor): Relating to temperature regulation of a
  //     substance or environment
  //   conditioned (descriptor): A substance modified to meet specific
  //     requirements
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   differential (measurement_descriptor): Measured difference between
  //     two reference points (e.g. differential_water_pressure)
  //   pressure (measurement): Measured force per unit area within a system
  //     or process.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field:
  // `thermally_conditioned_water_differential_pressure_sensor`, point type
  // `sensor`. Measured as pressure; values are in pascals, the ontology's
  // standard unit for that quantity.
  //
  // Expected range -34473.78945 to 344737.8945. This is a data-quality
  // expectation, not a constraint: a reading outside it is a fault to
  // report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double thermally_conditioned_water_differential_pressure_sensor = 171 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "thermally_conditioned_water_differential_pressure_sensor"
      subfields: "thermally"
      subfields: "conditioned"
      subfields: "water"
      subfields: "differential"
      subfields: "pressure"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "pressure"
      standard_unit: "pascals"
      expected_min: -34473.78945
      expected_max: 344737.8945
    }
  ];

  // The thermally conditioned water differential pressure setpoint.
  //
  // Composed of:
  //   thermally (descriptor): Relating to temperature regulation of a
  //     substance or environment
  //   conditioned (descriptor): A substance modified to meet specific
  //     requirements
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   differential (measurement_descriptor): Measured difference between
  //     two reference points (e.g. differential_water_pressure)
  //   pressure (measurement): Measured force per unit area within a system
  //     or process.
  //   setpoint (point_type): Control target of process or system.
  //
  // Ontology field:
  // `thermally_conditioned_water_differential_pressure_setpoint`, point type
  // `setpoint`. Measured as pressure; values are in pascals, the ontology's
  // standard unit for that quantity.
  //
  // Expected range -34473.78945 to 344737.8945. This is a data-quality
  // expectation, not a constraint: a reading outside it is a fault to
  // report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double thermally_conditioned_water_differential_pressure_setpoint = 172 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "thermally_conditioned_water_differential_pressure_setpoint"
      subfields: "thermally"
      subfields: "conditioned"
      subfields: "water"
      subfields: "differential"
      subfields: "pressure"
      subfields: "setpoint"
      point_type: "setpoint"
      measurement: "pressure"
      standard_unit: "pascals"
      expected_min: -34473.78945
      expected_max: 344737.8945
    }
  ];

  // The user occupancy override status.
  //
  // Composed of:
  //   user (descriptor): A person who uses or operates something.
  //   occupancy (descriptor): State of being occupied.
  //   override (descriptor): An action that interrupts, cancels, or
  //     changes the current action or status.
  //   status (point_type): The multistate value indicating an observed
  //     state in a piece of equipment, often indicating if a command was
  //     effected. It is a neutral observation (i.e., no quality judgment of
  //     'good' or 'bad'). It also has no units of measurement; therefore, if
  //     combined with a measurement subfield, it will indicate that the
  //     field is the directional status based on some measurement of that
  //     type (e.g., power_status equates to an on/off value based on some
  //     inference of power).
  //
  // Ontology field: `user_occupancy_override_status`, point type `status`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional DisabledEnabledOccupiedUnoccupiedValue user_occupancy_override_status = 173 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "user_occupancy_override_status"
      subfields: "user"
      subfields: "occupancy"
      subfields: "override"
      subfields: "status"
      point_type: "status"
    }
  ];

  // The voltage sensor.
  //
  // Composed of:
  //   voltage (measurement): Electrical potential difference between two
  //     reference points.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `voltage_sensor`, point type `sensor`. Measured as
  // voltage; values are in volts, the ontology's standard unit for that
  // quantity.
  //
  // Expected range 0 to 1000. This is a data-quality expectation, not a
  // constraint: a reading outside it is a fault to report, not a message to
  // reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double voltage_sensor = 174 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "voltage_sensor"
      subfields: "voltage"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "voltage"
      standard_unit: "volts"
      expected_min: 0.0
      expected_max: 1000.0
    }
  ];

  // The warmup request count.
  //
  // Composed of:
  //   warmup (descriptor): Process, measured effect or requirement for
  //     heating; used following building unoccupied periods and in optimal
  //     start applications.
  //   request (descriptor): A signal from a system device that calls for
  //     more of a particular conditioning process (e.g., pressure requests
  //     from terminal units).
  //   count (point_type): Total count of actions or requests.
  //
  // Ontology field: `warmup_request_count`, point type `count`.
  // Dimensionless: the ontology defines this point type as an integer count
  // rather than a measured quantity.
  //
  // Valid range from 0, enforced by protovalidate. Expected range up to 500.
  // This is a data-quality expectation, not a constraint: a reading outside
  // it is a fault to report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional int64 warmup_request_count = 175 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "warmup_request_count"
      subfields: "warmup"
      subfields: "request"
      subfields: "count"
      point_type: "count"
      expected_max: 500.0
    },
    (buf.validate.field).int64.gte = 0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The warmup run command.
  //
  // Composed of:
  //   warmup (descriptor): Process, measured effect or requirement for
  //     heating; used following building unoccupied periods and in optimal
  //     start applications.
  //   run (measurement_descriptor): State of being active or operating. By
  //     default applies to control program for system (e.g., VAV program
  //     run_command).
  //   command (point_type): The signal given to make an action happen.
  //     Defaults to multistate unless used alongside a measurement_type.
  //
  // Ontology field: `warmup_run_command`, point type `command`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional OffOnValue warmup_run_command = 176 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "warmup_run_command"
      subfields: "warmup"
      subfields: "run"
      subfields: "command"
      point_type: "command"
    }
  ];

  // The water leak alarm.
  //
  // Composed of:
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   leak (descriptor): Media leaving the system abnormally.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `water_leak_alarm`, point type `alarm`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue water_leak_alarm = 177 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "water_leak_alarm"
      subfields: "water"
      subfields: "leak"
      subfields: "alarm"
      point_type: "alarm"
    }
  ];

  // Numbers whose field the ontology no longer declares. Never reissued: a
  // consumer may still hold a message that used one.
  reserved 30;
}
