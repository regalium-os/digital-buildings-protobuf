<!--
Generated by `just docs` from Google's Digital Buildings ontology,
revision 2026-09-09 (4a794ac).
DO NOT EDIT -- edit sync/internal/docs and regenerate.
-->

# LandlordEquipment

Equipment base on landlord's side.

|   |   |
| --- | --- |
| Ontology type | `HVAC/LANDLORD` |
| GUID | `254a4e0e-c8b1-4002-b4fd-f63fac89fe72` |
| Kind | equipment |
| Proto package | `protobuf.digitalbuildings.hvac.landlord_equipment.v1` |
| Resource name | `buildings/{building}/landlordEquipments/{landlord_equipment}` |
| Fields | 3 fields, 2 writable |
| Canonical types | 0 |

## Files

Rule 2: one message per file, plus its enums. `messages.proto` is the single exception, because a request and its response are meaningless apart.

| File | Contains |
| --- | --- |
| `landlord_equipment.proto` | the `LandlordEquipment` message |
| `service.proto` | the `LandlordEquipments` service |
| `messages.proto` | its request and response messages |
| `connection.proto` | `Connection` -- how this entity relates to another (rule 4) |
| `link.proto` | `Link` -- this entity's fields mapped onto another's |
| `field_translation.proto` | `FieldTranslation`, `UnitMapping`, `StateMapping`, `ValueRange` |
| `active_inactive_value.proto` | a multistate enum (rule 13) |

## Service

`LandlordEquipments` in `service.proto`. Every resource is a collection (rule 7): there are no singletons here, so it gets the full standard method set plus AIP-164 `Undelete`.

AIP-131 names the service for the collection rather than `LandlordEquipmentService`; rule 1 has why buf STANDARD is not selected.

| Method | HTTP |
| --- | --- |
| `GetLandlordEquipment` | `GET /v1/{name=buildings/*/landlordEquipments/*}` |
| `ListLandlordEquipments` | `GET /v1/{parent=buildings/*}/landlordEquipments` |
| `CreateLandlordEquipment` | `POST /v1/{parent=buildings/*}/landlordEquipments` |
| `UpdateLandlordEquipment` | `PATCH /v1/{landlord_equipment.name=buildings/*/landlordEquipments/*}` |
| `DeleteLandlordEquipment` | `DELETE /v1/{name=buildings/*/landlordEquipments/*}` |
| `UndeleteLandlordEquipment` | `POST /v1/{name=buildings/*/landlordEquipments/*}:undelete` |

## Fields

Every field is `optional` (rule 7): the resource carries the union of every field any of its canonical types uses, and no single device reports all of them.

**Writable** is rule 8. A `command` or a `setpoint` is `OPTIONAL` and can be set; everything else is `OUTPUT_ONLY`, because the equipment reports it and no API call sets it. An `update_mask` naming an `OUTPUT_ONLY` field is rejected rather than silently ignored.

| Field | Type | Writable | Unit / states | Ontology literal | Field no. |
| --- | --- | --- | --- | --- | --- |
| `failed_communication_alarm` | `ActiveInactiveValue` | no | `ActiveInactiveValue` | `failed_communication_alarm` | 16 |
| `manufacturer_label` | `string` | at create | -- | `manufacturer_label` | 17 |
| `model_label` | `string` | at create | -- | `model_label` | 18 |

### Multistate enums

1 enum, one per distinct state set rather than one per field. Across the ontology 602 multistate fields draw on only 40 sets, so an enum per field would mean 305 different spellings of {ACTIVE, INACTIVE} that no consumer could pass to a common function (rule 13).

Each is copied into this package rather than imported: AIP-215 forbids a field referencing a type in another proto package (rule 4).

| File | Enum | States |
| --- | --- | --- |
| `active_inactive_value.proto` | `ActiveInactiveValue` | `ACTIVE`, `INACTIVE` |

## Provenance

Generated from `HVAC/LANDLORD` in Google's Digital Buildings ontology.

- [Ontology concepts](https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md)
- [Abstract model](https://github.com/google/digitalbuildings/blob/master/ontology/docs/model.md)
- [Building configuration](https://github.com/google/digitalbuildings/blob/master/ontology/docs/building_config.md)
