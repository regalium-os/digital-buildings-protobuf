// Copyright 2026 RegaliumOS™.
// SPDX-License-Identifier: Apache-2.0
//
// Generated by sync from Google's Digital Buildings ontology,
// revision 2026-09-09 (4a794ac).
// Portions Copyright 2020 Google LLC, licensed under Apache-2.0.
// DO NOT EDIT.
//
// Regenerate with: just sync
// Revision history: docs/spec.md

syntax = "proto3";

package protobuf.digitalbuildings.hvac.fan_coil_unit.v1;

import "buf/validate/validate.proto";
import "google/api/field_behavior.proto";
import "google/api/field_info.proto";
import "google/api/resource.proto";
import "protobuf/digitalbuildings/annotations/v1/annotations.proto";
import "protobuf/digitalbuildings/hvac/fan_coil_unit/v1/absent_present_value.proto";
import "protobuf/digitalbuildings/hvac/fan_coil_unit/v1/active_inactive_value.proto";
import "protobuf/digitalbuildings/hvac/fan_coil_unit/v1/auto_closed_open_unknown_value.proto";
import "protobuf/digitalbuildings/hvac/fan_coil_unit/v1/auto_commissioning_cooling_dehumidify_fan_only_flushing_heating_manual_neutral_off_unknown_value.proto";
import "protobuf/digitalbuildings/hvac/fan_coil_unit/v1/auto_cooling_dehumidify_fan_only_heating_manual_off_value.proto";
import "protobuf/digitalbuildings/hvac/fan_coil_unit/v1/auto_eighty_percent_forty_percent_high_low_medium_off_sixty_percent_twenty_percent_unknown_vfd_value.proto";
import "protobuf/digitalbuildings/hvac/fan_coil_unit/v1/auto_manual_value.proto";
import "protobuf/digitalbuildings/hvac/fan_coil_unit/v1/auto_off_on_unknown_value.proto";
import "protobuf/digitalbuildings/hvac/fan_coil_unit/v1/auto_off_on_value.proto";
import "protobuf/digitalbuildings/hvac/fan_coil_unit/v1/bypass_vfd_value.proto";
import "protobuf/digitalbuildings/hvac/fan_coil_unit/v1/closed_open_value.proto";
import "protobuf/digitalbuildings/hvac/fan_coil_unit/v1/connection.proto";
import "protobuf/digitalbuildings/hvac/fan_coil_unit/v1/disabled_enabled_occupied_unoccupied_value.proto";
import "protobuf/digitalbuildings/hvac/fan_coil_unit/v1/fan_coil_unit_type.proto";
import "protobuf/digitalbuildings/hvac/fan_coil_unit/v1/field_translation.proto";
import "protobuf/digitalbuildings/hvac/fan_coil_unit/v1/inactive_occupied_unknown_unoccupied_value.proto";
import "protobuf/digitalbuildings/hvac/fan_coil_unit/v1/link.proto";
import "protobuf/digitalbuildings/hvac/fan_coil_unit/v1/off_on_standby_unknown_value.proto";
import "protobuf/digitalbuildings/hvac/fan_coil_unit/v1/off_on_unknown_value.proto";
import "protobuf/digitalbuildings/hvac/fan_coil_unit/v1/off_on_value.proto";

option java_multiple_files = true;
option java_outer_classname = "FanCoilUnitProto";
option java_package = "com.regalium.protobuf.digitalbuildings.hvac.fan_coil_unit.v1";

// Tag for fan-coil units. Fan coil units are distinguished from other
// air-side devices by the fact that they solely handle recirculated space
// air (they do not directly control their outside air, which separates them
// from AHUs).
//
// Ontology entity type `HVAC/FCU`, a general type carrying 195 canonical
// variant(s). Rule 7: the general type is the resource and each variant is a
// value of FanCoilUnitType, so this message holds the union of every field
// any variant uses -- all of them optional.
//
// Reference: Digital Buildings abstract model.
// https://github.com/google/digitalbuildings/blob/master/ontology/docs/model.md
message FanCoilUnit {
  option (google.api.resource) = {
    type: "digitalbuildings.googleapis.com/FanCoilUnit"
    pattern: "buildings/{building}/fanCoilUnits/{fan_coil_unit}"
    singular: "fanCoilUnit"
    plural: "fanCoilUnits"
  };
  option (protobuf.digitalbuildings.annotations.v1.entity_type) = {
    guid: "649fb29c-d8cc-49cd-a316-0e78259a8b76"
    namespace: "HVAC"
    general_type: "FCU"
  };

  // Resource name, "buildings/{building}/fanCoilUnits/{fan_coil_unit}".
  // Assigned by the server.
  string name = 1 [
    (google.api.field_behavior) = IDENTIFIER,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The ontology's GUID for this entity.
  //
  // Rule 10: this is the ontology's own identifier, not a second one minted
  // beside it. Digital Buildings assigns a stable UUID to every entity and
  // it survives renames, which is exactly what AIP-148 asks a `uid` to be.
  string uid = 2 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (google.api.field_info).format = UUID4,
    (buf.validate.field).string.uuid = true,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The building config's human-readable entity code, e.g. `FCU-123`.
  //
  // Locally unique rather than globally, chosen at design time, and it must
  // survive a round trip unchanged or every re-import duplicates the entity.
  // The old building-config format keys entities by this; the new one keys
  // them by `uid`. Both are representable because both exist.
  string code = 3 [
    (google.api.field_behavior) = OPTIONAL,
    (google.api.field_behavior) = IMMUTABLE,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The canonical type this entity conforms to.
  //
  // The variant's exact field sets ride on the enum value in
  // (annotations.canonical_type), so a validator can reject an entity
  // reporting a field its declared type does not have.
  FanCoilUnitType entity_type = 4 [(google.api.field_behavior) = OPTIONAL];

  // Directed relationships to other entities.
  //
  // Rule 4: a connection is not a field. AIP-215 forbids referencing a
  // message in another proto package, and the other end of a connection may
  // be any resource in any namespace -- so it is carried as a resource name
  // with a wildcard reference.
  repeated Connection connections = 5 [(google.api.field_behavior) = OPTIONAL];

  // Standard fields mapped from another entity's telemetry.
  //
  // The building config's `links` block: a virtual entity reports data
  // linked from reporting devices rather than emitting it itself.
  repeated Link links = 6 [(google.api.field_behavior) = OPTIONAL];

  // How this entity's native payload maps onto the standard fields, when it
  // reports telemetry directly.
  repeated FieldTranslation translations = 7 [(google.api.field_behavior) = OPTIONAL];

  // Held for the fields on every read path (rule 9).
  reserved 8 to 15;

  // The chilled return water temperature sensor.
  //
  // Composed of:
  //   chilled (descriptor): Cold water (typically 40 - 50F).
  //   return (descriptor): Measurement or process of media as it is
  //     returned from the end-use equipment within the system.
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `chilled_return_water_temperature_sensor`, point type
  // `sensor`. Measured as temperature; values are in kelvin, the ontology's
  // standard unit for that quantity.
  //
  // Expected range 255.9259259 to 308.1481481. This is a data-quality
  // expectation, not a constraint: a reading outside it is a fault to
  // report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double chilled_return_water_temperature_sensor = 16 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "chilled_return_water_temperature_sensor"
      subfields: "chilled"
      subfields: "return"
      subfields: "water"
      subfields: "temperature"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "temperature"
      standard_unit: "kelvin"
      expected_min: 255.925926
      expected_max: 308.148148
    }
  ];

  // The chilled supply water isolation valve command.
  //
  // Composed of:
  //   chilled (descriptor): Cold water (typically 40 - 50F).
  //   supply (descriptor): Measurement or process of media as it is
  //     supplied to the end-use equipment within the system.
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   isolation (descriptor): Process of isolating one component or
  //     process from another.
  //   valve (component): Component which meters the flow of water within a
  //     system or device.
  //   command (point_type): The signal given to make an action happen.
  //     Defaults to multistate unless used alongside a measurement_type.
  //
  // Ontology field: `chilled_supply_water_isolation_valve_command`, point
  // type `command`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ClosedOpenValue chilled_supply_water_isolation_valve_command = 17 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "chilled_supply_water_isolation_valve_command"
      subfields: "chilled"
      subfields: "supply"
      subfields: "water"
      subfields: "isolation"
      subfields: "valve"
      subfields: "command"
      point_type: "command"
    }
  ];

  // The chilled supply water isolation valve status.
  //
  // Composed of:
  //   chilled (descriptor): Cold water (typically 40 - 50F).
  //   supply (descriptor): Measurement or process of media as it is
  //     supplied to the end-use equipment within the system.
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   isolation (descriptor): Process of isolating one component or
  //     process from another.
  //   valve (component): Component which meters the flow of water within a
  //     system or device.
  //   status (point_type): The multistate value indicating an observed
  //     state in a piece of equipment, often indicating if a command was
  //     effected. It is a neutral observation (i.e., no quality judgment of
  //     'good' or 'bad'). It also has no units of measurement; therefore, if
  //     combined with a measurement subfield, it will indicate that the
  //     field is the directional status based on some measurement of that
  //     type (e.g., power_status equates to an on/off value based on some
  //     inference of power).
  //
  // Ontology field: `chilled_supply_water_isolation_valve_status`, point
  // type `status`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ClosedOpenValue chilled_supply_water_isolation_valve_status = 18 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "chilled_supply_water_isolation_valve_status"
      subfields: "chilled"
      subfields: "supply"
      subfields: "water"
      subfields: "isolation"
      subfields: "valve"
      subfields: "status"
      point_type: "status"
    }
  ];

  // The chilled supply water temperature sensor.
  //
  // Composed of:
  //   chilled (descriptor): Cold water (typically 40 - 50F).
  //   supply (descriptor): Measurement or process of media as it is
  //     supplied to the end-use equipment within the system.
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `chilled_supply_water_temperature_sensor`, point type
  // `sensor`. Measured as temperature; values are in kelvin, the ontology's
  // standard unit for that quantity.
  //
  // Expected range 255.9259259 to 308.1481481. This is a data-quality
  // expectation, not a constraint: a reading outside it is a fault to
  // report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double chilled_supply_water_temperature_sensor = 19 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "chilled_supply_water_temperature_sensor"
      subfields: "chilled"
      subfields: "supply"
      subfields: "water"
      subfields: "temperature"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "temperature"
      standard_unit: "kelvin"
      expected_min: 255.925926
      expected_max: 308.148148
    }
  ];

  // The chilled water flowrate sensor.
  //
  // Composed of:
  //   chilled (descriptor): Cold water (typically 40 - 50F).
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   flowrate (measurement): Rate of fluid movement.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `chilled_water_flowrate_sensor`, point type `sensor`.
  // Measured as flowrate; values are in cubic_meters_per_second, the
  // ontology's standard unit for that quantity.
  //
  // Expected range -0.000315451 to 0.315451. This is a data-quality
  // expectation, not a constraint: a reading outside it is a fault to
  // report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double chilled_water_flowrate_sensor = 20 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "chilled_water_flowrate_sensor"
      subfields: "chilled"
      subfields: "water"
      subfields: "flowrate"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "flowrate"
      standard_unit: "cubic_meters_per_second"
      expected_min: -0.000315
      expected_max: 0.315451
    }
  ];

  // The chilled water valve command.
  //
  // Composed of:
  //   chilled (descriptor): Cold water (typically 40 - 50F).
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   valve (component): Component which meters the flow of water within a
  //     system or device.
  //   command (point_type): The signal given to make an action happen.
  //     Defaults to multistate unless used alongside a measurement_type.
  //
  // Ontology field: `chilled_water_valve_command`, point type `command`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ClosedOpenValue chilled_water_valve_command = 21 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "chilled_water_valve_command"
      subfields: "chilled"
      subfields: "water"
      subfields: "valve"
      subfields: "command"
      point_type: "command"
    }
  ];

  // The chilled water valve percentage command.
  //
  // Composed of:
  //   chilled (descriptor): Cold water (typically 40 - 50F).
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   valve (component): Component which meters the flow of water within a
  //     system or device.
  //   percentage (measurement): Measurement per hundred.
  //   command (point_type): The signal given to make an action happen.
  //     Defaults to multistate unless used alongside a measurement_type.
  //
  // Ontology field: `chilled_water_valve_percentage_command`, point type
  // `command`. Measured as percentage; values are in percent, the ontology's
  // standard unit for that quantity.
  //
  // Valid range 0 to 100, enforced by protovalidate.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double chilled_water_valve_percentage_command = 22 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "chilled_water_valve_percentage_command"
      subfields: "chilled"
      subfields: "water"
      subfields: "valve"
      subfields: "percentage"
      subfields: "command"
      point_type: "command"
      measurement: "percentage"
      standard_unit: "percent"
    },
    (buf.validate.field).double.gte = 0.0,
    (buf.validate.field).double.lte = 100.0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The chilled water valve percentage sensor.
  //
  // Composed of:
  //   chilled (descriptor): Cold water (typically 40 - 50F).
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   valve (component): Component which meters the flow of water within a
  //     system or device.
  //   percentage (measurement): Measurement per hundred.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `chilled_water_valve_percentage_sensor`, point type
  // `sensor`. Measured as percentage; values are in percent, the ontology's
  // standard unit for that quantity.
  //
  // Valid range 0 to 100, enforced by protovalidate.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double chilled_water_valve_percentage_sensor = 23 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "chilled_water_valve_percentage_sensor"
      subfields: "chilled"
      subfields: "water"
      subfields: "valve"
      subfields: "percentage"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "percentage"
      standard_unit: "percent"
    },
    (buf.validate.field).double.gte = 0.0,
    (buf.validate.field).double.lte = 100.0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The chilled water valve status.
  //
  // Composed of:
  //   chilled (descriptor): Cold water (typically 40 - 50F).
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   valve (component): Component which meters the flow of water within a
  //     system or device.
  //   status (point_type): The multistate value indicating an observed
  //     state in a piece of equipment, often indicating if a command was
  //     effected. It is a neutral observation (i.e., no quality judgment of
  //     'good' or 'bad'). It also has no units of measurement; therefore, if
  //     combined with a measurement subfield, it will indicate that the
  //     field is the directional status based on some measurement of that
  //     type (e.g., power_status equates to an on/off value based on some
  //     inference of power).
  //
  // Ontology field: `chilled_water_valve_status`, point type `status`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ClosedOpenValue chilled_water_valve_status = 24 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "chilled_water_valve_status"
      subfields: "chilled"
      subfields: "water"
      subfields: "valve"
      subfields: "status"
      point_type: "status"
    }
  ];

  // The cold aisle air temperature sensor.
  //
  // Composed of:
  //   cold (descriptor): Used to describe something at a low (or
  //     relatively low) temperature.
  //   aisle (descriptor): Passage between two rows of server racks.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `cold_aisle_air_temperature_sensor`, point type
  // `sensor`. Measured as temperature; values are in kelvin, the ontology's
  // standard unit for that quantity.
  //
  // Expected range 255.9259259 to 322.037037. This is a data-quality
  // expectation, not a constraint: a reading outside it is a fault to
  // report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  repeated double cold_aisle_air_temperature_sensor = 25 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "cold_aisle_air_temperature_sensor"
      subfields: "cold"
      subfields: "aisle"
      subfields: "air"
      subfields: "temperature"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "temperature"
      standard_unit: "kelvin"
      enumerated: true
      expected_min: 255.925926
      expected_max: 322.037037
    }
  ];

  // The communication status.
  //
  // Composed of:
  //   communication (descriptor): Actual transmit and recieve of data or
  //     connectivity between devices.
  //   status (point_type): The multistate value indicating an observed
  //     state in a piece of equipment, often indicating if a command was
  //     effected. It is a neutral observation (i.e., no quality judgment of
  //     'good' or 'bad'). It also has no units of measurement; therefore, if
  //     combined with a measurement subfield, it will indicate that the
  //     field is the directional status based on some measurement of that
  //     type (e.g., power_status equates to an on/off value based on some
  //     inference of power).
  //
  // Ontology field: `communication_status`, point type `status`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional OffOnValue communication_status = 26 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "communication_status"
      subfields: "communication"
      subfields: "status"
      point_type: "status"
    }
  ];

  // The compressor lost power alarm.
  //
  // Composed of:
  //   compressor (component): Component which drives refrigerant
  //     compression (and thus cooling processes) within a device or system.
  //   lost (measurement_descriptor): Indicates that a point has lost some
  //     quality or quantity inadvertently.
  //   power (measurement): Rate of energy consumption (assumed context is
  //     electrical and real, unless otherwise modified).
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `compressor_lost_power_alarm`, point type `alarm`.
  // Measured as power; values are in watts, the ontology's standard unit for
  // that quantity.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue compressor_lost_power_alarm = 27 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "compressor_lost_power_alarm"
      subfields: "compressor"
      subfields: "lost"
      subfields: "power"
      subfields: "alarm"
      point_type: "alarm"
      measurement: "power"
      standard_unit: "watts"
    }
  ];

  // The compressor run command.
  //
  // Composed of:
  //   compressor (component): Component which drives refrigerant
  //     compression (and thus cooling processes) within a device or system.
  //   run (measurement_descriptor): State of being active or operating. By
  //     default applies to control program for system (e.g., VAV program
  //     run_command).
  //   command (point_type): The signal given to make an action happen.
  //     Defaults to multistate unless used alongside a measurement_type.
  //
  // Ontology field: `compressor_run_command`, point type `command`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  repeated OffOnUnknownValue compressor_run_command = 28 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "compressor_run_command"
      subfields: "compressor"
      subfields: "run"
      subfields: "command"
      point_type: "command"
      enumerated: true
    }
  ];

  // The compressor run status.
  //
  // Composed of:
  //   compressor (component): Component which drives refrigerant
  //     compression (and thus cooling processes) within a device or system.
  //   run (measurement_descriptor): State of being active or operating. By
  //     default applies to control program for system (e.g., VAV program
  //     run_command).
  //   status (point_type): The multistate value indicating an observed
  //     state in a piece of equipment, often indicating if a command was
  //     effected. It is a neutral observation (i.e., no quality judgment of
  //     'good' or 'bad'). It also has no units of measurement; therefore, if
  //     combined with a measurement subfield, it will indicate that the
  //     field is the directional status based on some measurement of that
  //     type (e.g., power_status equates to an on/off value based on some
  //     inference of power).
  //
  // Ontology field: `compressor_run_status`, point type `status`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  repeated OffOnUnknownValue compressor_run_status = 29 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "compressor_run_status"
      subfields: "compressor"
      subfields: "run"
      subfields: "status"
      point_type: "status"
      enumerated: true
    }
  ];

  // The compressor run time accumulator.
  //
  // Composed of:
  //   compressor (component): Component which drives refrigerant
  //     compression (and thus cooling processes) within a device or system.
  //   run (measurement_descriptor): State of being active or operating. By
  //     default applies to control program for system (e.g., VAV program
  //     run_command).
  //   time (measurement): Defined duration of something.
  //   accumulator (point_type): The total accumulated quantity (e.g.,
  //     total energy accumulated).
  //
  // Ontology field: `compressor_run_time_accumulator`, point type
  // `accumulator`. Measured as time; values are in seconds, the ontology's
  // standard unit for that quantity.
  //
  // Valid range from 0, enforced by protovalidate. Expected range up to
  // 1.25. This is a data-quality expectation, not a constraint: a reading
  // outside it is a fault to report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  repeated double compressor_run_time_accumulator = 30 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "compressor_run_time_accumulator"
      subfields: "compressor"
      subfields: "run"
      subfields: "time"
      subfields: "accumulator"
      point_type: "accumulator"
      measurement: "time"
      standard_unit: "seconds"
      enumerated: true
      expected_max: 1.25
    }
  ];

  // The compressor speed frequency sensor.
  //
  // Composed of:
  //   compressor (component): Component which drives refrigerant
  //     compression (and thus cooling processes) within a device or system.
  //   speed (measurement_descriptor): Numeric setting of how fast to run a
  //     device, in the specified units. Typically used to describe
  //     revolutions of a motor as a fraction of nominal or maximum.
  //   frequency (measurement): The rate of cycling within a process.
  //     Typically used to describe voltage (and amperage) oscillation within
  //     AC power distribution components.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `compressor_speed_frequency_sensor`, point type
  // `sensor`. Measured as frequency; values are in hertz, the ontology's
  // standard unit for that quantity.
  //
  // Expected range -0.5 to 65. This is a data-quality expectation, not a
  // constraint: a reading outside it is a fault to report, not a message to
  // reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double compressor_speed_frequency_sensor = 31 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "compressor_speed_frequency_sensor"
      subfields: "compressor"
      subfields: "speed"
      subfields: "frequency"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "frequency"
      standard_unit: "hertz"
      expected_min: -0.5
      expected_max: 65.0
    }
  ];

  // The compressor speed percentage command.
  //
  // Composed of:
  //   compressor (component): Component which drives refrigerant
  //     compression (and thus cooling processes) within a device or system.
  //   speed (measurement_descriptor): Numeric setting of how fast to run a
  //     device, in the specified units. Typically used to describe
  //     revolutions of a motor as a fraction of nominal or maximum.
  //   percentage (measurement): Measurement per hundred.
  //   command (point_type): The signal given to make an action happen.
  //     Defaults to multistate unless used alongside a measurement_type.
  //
  // Ontology field: `compressor_speed_percentage_command`, point type
  // `command`. Measured as percentage; values are in percent, the ontology's
  // standard unit for that quantity.
  //
  // Valid range 0 to 100, enforced by protovalidate.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double compressor_speed_percentage_command = 32 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "compressor_speed_percentage_command"
      subfields: "compressor"
      subfields: "speed"
      subfields: "percentage"
      subfields: "command"
      point_type: "command"
      measurement: "percentage"
      standard_unit: "percent"
    },
    (buf.validate.field).double.gte = 0.0,
    (buf.validate.field).double.lte = 100.0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The compressor speed percentage sensor.
  //
  // Composed of:
  //   compressor (component): Component which drives refrigerant
  //     compression (and thus cooling processes) within a device or system.
  //   speed (measurement_descriptor): Numeric setting of how fast to run a
  //     device, in the specified units. Typically used to describe
  //     revolutions of a motor as a fraction of nominal or maximum.
  //   percentage (measurement): Measurement per hundred.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `compressor_speed_percentage_sensor`, point type
  // `sensor`. Measured as percentage; values are in percent, the ontology's
  // standard unit for that quantity.
  //
  // Valid range 0 to 100, enforced by protovalidate.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double compressor_speed_percentage_sensor = 33 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "compressor_speed_percentage_sensor"
      subfields: "compressor"
      subfields: "speed"
      subfields: "percentage"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "percentage"
      standard_unit: "percent"
    },
    (buf.validate.field).double.gte = 0.0,
    (buf.validate.field).double.lte = 100.0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The condensate water alarm.
  //
  // Composed of:
  //   condensate (descriptor): Liquid that acculates on surfaces as a
  //     byproduct of condensation (i.e., water vapor condensing into liquid)
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `condensate_water_alarm`, point type `alarm`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue condensate_water_alarm = 34 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "condensate_water_alarm"
      subfields: "condensate"
      subfields: "water"
      subfields: "alarm"
      point_type: "alarm"
    }
  ];

  // The condensing fan run command.
  //
  // Composed of:
  //   condensing (descriptor): The process of a gas (or vapor) changing to
  //     liquid.
  //   fan (component): Component used for the distribution of air.
  //   run (measurement_descriptor): State of being active or operating. By
  //     default applies to control program for system (e.g., VAV program
  //     run_command).
  //   command (point_type): The signal given to make an action happen.
  //     Defaults to multistate unless used alongside a measurement_type.
  //
  // Ontology field: `condensing_fan_run_command`, point type `command`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional OffOnValue condensing_fan_run_command = 35 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "condensing_fan_run_command"
      subfields: "condensing"
      subfields: "fan"
      subfields: "run"
      subfields: "command"
      point_type: "command"
    }
  ];

  // The condensing fan run status.
  //
  // Composed of:
  //   condensing (descriptor): The process of a gas (or vapor) changing to
  //     liquid.
  //   fan (component): Component used for the distribution of air.
  //   run (measurement_descriptor): State of being active or operating. By
  //     default applies to control program for system (e.g., VAV program
  //     run_command).
  //   status (point_type): The multistate value indicating an observed
  //     state in a piece of equipment, often indicating if a command was
  //     effected. It is a neutral observation (i.e., no quality judgment of
  //     'good' or 'bad'). It also has no units of measurement; therefore, if
  //     combined with a measurement subfield, it will indicate that the
  //     field is the directional status based on some measurement of that
  //     type (e.g., power_status equates to an on/off value based on some
  //     inference of power).
  //
  // Ontology field: `condensing_fan_run_status`, point type `status`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional OffOnValue condensing_fan_run_status = 36 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "condensing_fan_run_status"
      subfields: "condensing"
      subfields: "fan"
      subfields: "run"
      subfields: "status"
      point_type: "status"
    }
  ];

  // The condensing fan speed percentage command.
  //
  // Composed of:
  //   condensing (descriptor): The process of a gas (or vapor) changing to
  //     liquid.
  //   fan (component): Component used for the distribution of air.
  //   speed (measurement_descriptor): Numeric setting of how fast to run a
  //     device, in the specified units. Typically used to describe
  //     revolutions of a motor as a fraction of nominal or maximum.
  //   percentage (measurement): Measurement per hundred.
  //   command (point_type): The signal given to make an action happen.
  //     Defaults to multistate unless used alongside a measurement_type.
  //
  // Ontology field: `condensing_fan_speed_percentage_command`, point type
  // `command`. Measured as percentage; values are in percent, the ontology's
  // standard unit for that quantity.
  //
  // Valid range 0 to 100, enforced by protovalidate.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double condensing_fan_speed_percentage_command = 37 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "condensing_fan_speed_percentage_command"
      subfields: "condensing"
      subfields: "fan"
      subfields: "speed"
      subfields: "percentage"
      subfields: "command"
      point_type: "command"
      measurement: "percentage"
      standard_unit: "percent"
    },
    (buf.validate.field).double.gte = 0.0,
    (buf.validate.field).double.lte = 100.0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The condensing return water isolation valve percentage command.
  //
  // Composed of:
  //   condensing (descriptor): The process of a gas (or vapor) changing to
  //     liquid.
  //   return (descriptor): Measurement or process of media as it is
  //     returned from the end-use equipment within the system.
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   isolation (descriptor): Process of isolating one component or
  //     process from another.
  //   valve (component): Component which meters the flow of water within a
  //     system or device.
  //   percentage (measurement): Measurement per hundred.
  //   command (point_type): The signal given to make an action happen.
  //     Defaults to multistate unless used alongside a measurement_type.
  //
  // Ontology field:
  // `condensing_return_water_isolation_valve_percentage_command`, point type
  // `command`. Measured as percentage; values are in percent, the ontology's
  // standard unit for that quantity.
  //
  // Valid range 0 to 100, enforced by protovalidate.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double condensing_return_water_isolation_valve_percentage_command = 38 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "condensing_return_water_isolation_valve_percentage_command"
      subfields: "condensing"
      subfields: "return"
      subfields: "water"
      subfields: "isolation"
      subfields: "valve"
      subfields: "percentage"
      subfields: "command"
      point_type: "command"
      measurement: "percentage"
      standard_unit: "percent"
    },
    (buf.validate.field).double.gte = 0.0,
    (buf.validate.field).double.lte = 100.0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The condensing return water isolation valve percentage sensor.
  //
  // Composed of:
  //   condensing (descriptor): The process of a gas (or vapor) changing to
  //     liquid.
  //   return (descriptor): Measurement or process of media as it is
  //     returned from the end-use equipment within the system.
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   isolation (descriptor): Process of isolating one component or
  //     process from another.
  //   valve (component): Component which meters the flow of water within a
  //     system or device.
  //   percentage (measurement): Measurement per hundred.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field:
  // `condensing_return_water_isolation_valve_percentage_sensor`, point type
  // `sensor`. Measured as percentage; values are in percent, the ontology's
  // standard unit for that quantity.
  //
  // Valid range 0 to 100, enforced by protovalidate.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double condensing_return_water_isolation_valve_percentage_sensor = 39 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "condensing_return_water_isolation_valve_percentage_sensor"
      subfields: "condensing"
      subfields: "return"
      subfields: "water"
      subfields: "isolation"
      subfields: "valve"
      subfields: "percentage"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "percentage"
      standard_unit: "percent"
    },
    (buf.validate.field).double.gte = 0.0,
    (buf.validate.field).double.lte = 100.0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The condensing return water isolation valve status.
  //
  // Composed of:
  //   condensing (descriptor): The process of a gas (or vapor) changing to
  //     liquid.
  //   return (descriptor): Measurement or process of media as it is
  //     returned from the end-use equipment within the system.
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   isolation (descriptor): Process of isolating one component or
  //     process from another.
  //   valve (component): Component which meters the flow of water within a
  //     system or device.
  //   status (point_type): The multistate value indicating an observed
  //     state in a piece of equipment, often indicating if a command was
  //     effected. It is a neutral observation (i.e., no quality judgment of
  //     'good' or 'bad'). It also has no units of measurement; therefore, if
  //     combined with a measurement subfield, it will indicate that the
  //     field is the directional status based on some measurement of that
  //     type (e.g., power_status equates to an on/off value based on some
  //     inference of power).
  //
  // Ontology field: `condensing_return_water_isolation_valve_status`, point
  // type `status`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ClosedOpenValue condensing_return_water_isolation_valve_status = 40 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "condensing_return_water_isolation_valve_status"
      subfields: "condensing"
      subfields: "return"
      subfields: "water"
      subfields: "isolation"
      subfields: "valve"
      subfields: "status"
      point_type: "status"
    }
  ];

  // The condensing return water temperature sensor.
  //
  // Composed of:
  //   condensing (descriptor): The process of a gas (or vapor) changing to
  //     liquid.
  //   return (descriptor): Measurement or process of media as it is
  //     returned from the end-use equipment within the system.
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `condensing_return_water_temperature_sensor`, point type
  // `sensor`. Measured as temperature; values are in kelvin, the ontology's
  // standard unit for that quantity.
  //
  // Expected range 255.9259259 to 308.1481481. This is a data-quality
  // expectation, not a constraint: a reading outside it is a fault to
  // report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double condensing_return_water_temperature_sensor = 41 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "condensing_return_water_temperature_sensor"
      subfields: "condensing"
      subfields: "return"
      subfields: "water"
      subfields: "temperature"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "temperature"
      standard_unit: "kelvin"
      expected_min: 255.925926
      expected_max: 308.148148
    }
  ];

  // The condensing supply water temperature sensor.
  //
  // Composed of:
  //   condensing (descriptor): The process of a gas (or vapor) changing to
  //     liquid.
  //   supply (descriptor): Measurement or process of media as it is
  //     supplied to the end-use equipment within the system.
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `condensing_supply_water_temperature_sensor`, point type
  // `sensor`. Measured as temperature; values are in kelvin, the ontology's
  // standard unit for that quantity.
  //
  // Expected range 255.9259259 to 308.1481481. This is a data-quality
  // expectation, not a constraint: a reading outside it is a fault to
  // report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double condensing_supply_water_temperature_sensor = 42 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "condensing_supply_water_temperature_sensor"
      subfields: "condensing"
      subfields: "supply"
      subfields: "water"
      subfields: "temperature"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "temperature"
      standard_unit: "kelvin"
      expected_min: 255.925926
      expected_max: 308.148148
    }
  ];

  // The control mode.
  //
  // Composed of:
  //   control (descriptor): The process of governing the actions of a
  //     device.
  //   mode (point_type): Distinct mode of operation within system. For
  //     example economizer_mode has modes of 'enabled' or 'disabled.'
  //
  // Ontology field: `control_mode`, point type `mode`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional AutoCoolingDehumidifyFanOnlyHeatingManualOffValue control_mode = 43 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "control_mode"
      subfields: "control"
      subfields: "mode"
      point_type: "mode"
    }
  ];

  // The cooldown run command.
  //
  // Composed of:
  //   cooldown (descriptor): Process, measured effect or requirement for
  //     cooling; used following building occupied periods.
  //   run (measurement_descriptor): State of being active or operating. By
  //     default applies to control program for system (e.g., VAV program
  //     run_command).
  //   command (point_type): The signal given to make an action happen.
  //     Defaults to multistate unless used alongside a measurement_type.
  //
  // Ontology field: `cooldown_run_command`, point type `command`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional OffOnValue cooldown_run_command = 44 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "cooldown_run_command"
      subfields: "cooldown"
      subfields: "run"
      subfields: "command"
      point_type: "command"
    }
  ];

  // The cooling percentage command.
  //
  // Composed of:
  //   cooling (descriptor): Process, measured effect or requirement for
  //     cooling.
  //   percentage (measurement): Measurement per hundred.
  //   command (point_type): The signal given to make an action happen.
  //     Defaults to multistate unless used alongside a measurement_type.
  //
  // Ontology field: `cooling_percentage_command`, point type `command`.
  // Measured as percentage; values are in percent, the ontology's standard
  // unit for that quantity.
  //
  // Valid range 0 to 100, enforced by protovalidate.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double cooling_percentage_command = 45 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "cooling_percentage_command"
      subfields: "cooling"
      subfields: "percentage"
      subfields: "command"
      point_type: "command"
      measurement: "percentage"
      standard_unit: "percent"
    },
    (buf.validate.field).double.gte = 0.0,
    (buf.validate.field).double.lte = 100.0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The cooling request count.
  //
  // Composed of:
  //   cooling (descriptor): Process, measured effect or requirement for
  //     cooling.
  //   request (descriptor): A signal from a system device that calls for
  //     more of a particular conditioning process (e.g., pressure requests
  //     from terminal units).
  //   count (point_type): Total count of actions or requests.
  //
  // Ontology field: `cooling_request_count`, point type `count`.
  // Dimensionless: the ontology defines this point type as an integer count
  // rather than a measured quantity.
  //
  // Valid range from 0, enforced by protovalidate. Expected range up to 500.
  // This is a data-quality expectation, not a constraint: a reading outside
  // it is a fault to report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional int64 cooling_request_count = 46 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "cooling_request_count"
      subfields: "cooling"
      subfields: "request"
      subfields: "count"
      point_type: "count"
      expected_max: 500.0
    },
    (buf.validate.field).int64.gte = 0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The cooling run command.
  //
  // Composed of:
  //   cooling (descriptor): Process, measured effect or requirement for
  //     cooling.
  //   run (measurement_descriptor): State of being active or operating. By
  //     default applies to control program for system (e.g., VAV program
  //     run_command).
  //   command (point_type): The signal given to make an action happen.
  //     Defaults to multistate unless used alongside a measurement_type.
  //
  // Ontology field: `cooling_run_command`, point type `command`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional OffOnUnknownValue cooling_run_command = 47 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "cooling_run_command"
      subfields: "cooling"
      subfields: "run"
      subfields: "command"
      point_type: "command"
    }
  ];

  // The cooling stage run count.
  //
  // Composed of:
  //   cooling (descriptor): Process, measured effect or requirement for
  //     cooling.
  //   stage (descriptor): Discrete step (i.e., stage) of device activity
  //     (such as heating and cooling outputs).
  //   run (measurement_descriptor): State of being active or operating. By
  //     default applies to control program for system (e.g., VAV program
  //     run_command).
  //   count (point_type): Total count of actions or requests.
  //
  // Ontology field: `cooling_stage_run_count`, point type `count`.
  // Dimensionless: the ontology defines this point type as an integer count
  // rather than a measured quantity.
  //
  // Valid range from 0, enforced by protovalidate. Expected range up to 500.
  // This is a data-quality expectation, not a constraint: a reading outside
  // it is a fault to report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional int64 cooling_stage_run_count = 48 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "cooling_stage_run_count"
      subfields: "cooling"
      subfields: "stage"
      subfields: "run"
      subfields: "count"
      point_type: "count"
      expected_max: 500.0
    },
    (buf.validate.field).int64.gte = 0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The cooling thermal power capacity.
  //
  // Composed of:
  //   cooling (descriptor): Process, measured effect or requirement for
  //     cooling.
  //   thermal (measurement_descriptor): Relating to the transfer of heat.
  //   power (measurement): Rate of energy consumption (assumed context is
  //     electrical and real, unless otherwise modified).
  //   capacity (point_type): A design parameter quantity (e.g., design
  //     motor power capacity). Is always a maximum limit.
  //
  // Ontology field: `cooling_thermal_power_capacity`, point type `capacity`.
  // Measured as power; values are in watts, the ontology's standard unit for
  // that quantity.
  //
  // Valid range from 0, enforced by protovalidate. Expected range up to
  // 2.9307107e+06. This is a data-quality expectation, not a constraint: a
  // reading outside it is a fault to report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double cooling_thermal_power_capacity = 49 [
    (google.api.field_behavior) = OPTIONAL,
    (google.api.field_behavior) = IMMUTABLE,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "cooling_thermal_power_capacity"
      subfields: "cooling"
      subfields: "thermal"
      subfields: "power"
      subfields: "capacity"
      point_type: "capacity"
      measurement: "power"
      standard_unit: "watts"
      expected_max: 2930710.7
    },
    (buf.validate.field).double.gte = 0.0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The current sensor.
  //
  // Composed of:
  //   current (measurement): Flow of electric charge.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `current_sensor`, point type `sensor`. Measured as
  // current; values are in amperes, the ontology's standard unit for that
  // quantity.
  //
  // Valid range from 0, enforced by protovalidate. Expected range up to 200.
  // This is a data-quality expectation, not a constraint: a reading outside
  // it is a fault to report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double current_sensor = 50 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "current_sensor"
      subfields: "current"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "current"
      standard_unit: "amperes"
      expected_max: 200.0
    },
    (buf.validate.field).double.gte = 0.0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The dehumidification run command.
  //
  // Composed of:
  //   dehumidification (descriptor): Process of removing moisture from
  //     air.
  //   run (measurement_descriptor): State of being active or operating. By
  //     default applies to control program for system (e.g., VAV program
  //     run_command).
  //   command (point_type): The signal given to make an action happen.
  //     Defaults to multistate unless used alongside a measurement_type.
  //
  // Ontology field: `dehumidification_run_command`, point type `command`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional OffOnValue dehumidification_run_command = 51 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "dehumidification_run_command"
      subfields: "dehumidification"
      subfields: "run"
      subfields: "command"
      point_type: "command"
    }
  ];

  // The dehumidification run status.
  //
  // Composed of:
  //   dehumidification (descriptor): Process of removing moisture from
  //     air.
  //   run (measurement_descriptor): State of being active or operating. By
  //     default applies to control program for system (e.g., VAV program
  //     run_command).
  //   status (point_type): The multistate value indicating an observed
  //     state in a piece of equipment, often indicating if a command was
  //     effected. It is a neutral observation (i.e., no quality judgment of
  //     'good' or 'bad'). It also has no units of measurement; therefore, if
  //     combined with a measurement subfield, it will indicate that the
  //     field is the directional status based on some measurement of that
  //     type (e.g., power_status equates to an on/off value based on some
  //     inference of power).
  //
  // Ontology field: `dehumidification_run_status`, point type `status`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional OffOnValue dehumidification_run_status = 52 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "dehumidification_run_status"
      subfields: "dehumidification"
      subfields: "run"
      subfields: "status"
      point_type: "status"
    }
  ];

  // The discharge air cooling temperature setpoint.
  //
  // Composed of:
  //   discharge (descriptor): Media leaving system to enter ambient
  //     conditioned space. Typically applies only to air-side systems.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   cooling (descriptor): Process, measured effect or requirement for
  //     cooling.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   setpoint (point_type): Control target of process or system.
  //
  // Ontology field: `discharge_air_cooling_temperature_setpoint`, point type
  // `setpoint`. Measured as temperature; values are in kelvin, the
  // ontology's standard unit for that quantity.
  //
  // Expected range 255.9259259 to 310.9259259. This is a data-quality
  // expectation, not a constraint: a reading outside it is a fault to
  // report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double discharge_air_cooling_temperature_setpoint = 53 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "discharge_air_cooling_temperature_setpoint"
      subfields: "discharge"
      subfields: "air"
      subfields: "cooling"
      subfields: "temperature"
      subfields: "setpoint"
      point_type: "setpoint"
      measurement: "temperature"
      standard_unit: "kelvin"
      expected_min: 255.925926
      expected_max: 310.925926
    }
  ];

  // The discharge air flowrate capacity.
  //
  // Composed of:
  //   discharge (descriptor): Media leaving system to enter ambient
  //     conditioned space. Typically applies only to air-side systems.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   flowrate (measurement): Rate of fluid movement.
  //   capacity (point_type): A design parameter quantity (e.g., design
  //     motor power capacity). Is always a maximum limit.
  //
  // Ontology field: `discharge_air_flowrate_capacity`, point type
  // `capacity`. Measured as flowrate; values are in cubic_meters_per_second,
  // the ontology's standard unit for that quantity.
  //
  // Valid range from 0, enforced by protovalidate. Expected range up to
  // 141.584233. This is a data-quality expectation, not a constraint: a
  // reading outside it is a fault to report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double discharge_air_flowrate_capacity = 54 [
    (google.api.field_behavior) = OPTIONAL,
    (google.api.field_behavior) = IMMUTABLE,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "discharge_air_flowrate_capacity"
      subfields: "discharge"
      subfields: "air"
      subfields: "flowrate"
      subfields: "capacity"
      point_type: "capacity"
      measurement: "flowrate"
      standard_unit: "cubic_meters_per_second"
      expected_max: 141.584233
    },
    (buf.validate.field).double.gte = 0.0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The discharge air flowrate sensor.
  //
  // Composed of:
  //   discharge (descriptor): Media leaving system to enter ambient
  //     conditioned space. Typically applies only to air-side systems.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   flowrate (measurement): Rate of fluid movement.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `discharge_air_flowrate_sensor`, point type `sensor`.
  // Measured as flowrate; values are in cubic_meters_per_second, the
  // ontology's standard unit for that quantity.
  //
  // Expected range -0.002359737216 to 94.38948864. This is a data-quality
  // expectation, not a constraint: a reading outside it is a fault to
  // report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double discharge_air_flowrate_sensor = 55 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "discharge_air_flowrate_sensor"
      subfields: "discharge"
      subfields: "air"
      subfields: "flowrate"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "flowrate"
      standard_unit: "cubic_meters_per_second"
      expected_min: -0.00236
      expected_max: 94.389489
    }
  ];

  // The discharge air heating temperature setpoint.
  //
  // Composed of:
  //   discharge (descriptor): Media leaving system to enter ambient
  //     conditioned space. Typically applies only to air-side systems.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   heating (descriptor): Process, measured effect or requirement for
  //     heating.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   setpoint (point_type): Control target of process or system.
  //
  // Ontology field: `discharge_air_heating_temperature_setpoint`, point type
  // `setpoint`. Measured as temperature; values are in kelvin, the
  // ontology's standard unit for that quantity.
  //
  // Expected range 255.9259259 to 338.7037037. This is a data-quality
  // expectation, not a constraint: a reading outside it is a fault to
  // report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double discharge_air_heating_temperature_setpoint = 56 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "discharge_air_heating_temperature_setpoint"
      subfields: "discharge"
      subfields: "air"
      subfields: "heating"
      subfields: "temperature"
      subfields: "setpoint"
      point_type: "setpoint"
      measurement: "temperature"
      standard_unit: "kelvin"
      expected_min: 255.925926
      expected_max: 338.703704
    }
  ];

  // The discharge air isolation damper command.
  //
  // Composed of:
  //   discharge (descriptor): Media leaving system to enter ambient
  //     conditioned space. Typically applies only to air-side systems.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   isolation (descriptor): Process of isolating one component or
  //     process from another.
  //   damper (component): Component which meters the flow of air within a
  //     system or device.
  //   command (point_type): The signal given to make an action happen.
  //     Defaults to multistate unless used alongside a measurement_type.
  //
  // Ontology field: `discharge_air_isolation_damper_command`, point type
  // `command`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  repeated ClosedOpenValue discharge_air_isolation_damper_command = 57 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "discharge_air_isolation_damper_command"
      subfields: "discharge"
      subfields: "air"
      subfields: "isolation"
      subfields: "damper"
      subfields: "command"
      point_type: "command"
      enumerated: true
    }
  ];

  // The discharge air isolation damper percentage command.
  //
  // Composed of:
  //   discharge (descriptor): Media leaving system to enter ambient
  //     conditioned space. Typically applies only to air-side systems.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   isolation (descriptor): Process of isolating one component or
  //     process from another.
  //   damper (component): Component which meters the flow of air within a
  //     system or device.
  //   percentage (measurement): Measurement per hundred.
  //   command (point_type): The signal given to make an action happen.
  //     Defaults to multistate unless used alongside a measurement_type.
  //
  // Ontology field: `discharge_air_isolation_damper_percentage_command`,
  // point type `command`. Measured as percentage; values are in percent, the
  // ontology's standard unit for that quantity.
  //
  // Valid range 0 to 100, enforced by protovalidate.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double discharge_air_isolation_damper_percentage_command = 58 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "discharge_air_isolation_damper_percentage_command"
      subfields: "discharge"
      subfields: "air"
      subfields: "isolation"
      subfields: "damper"
      subfields: "percentage"
      subfields: "command"
      point_type: "command"
      measurement: "percentage"
      standard_unit: "percent"
    },
    (buf.validate.field).double.gte = 0.0,
    (buf.validate.field).double.lte = 100.0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The discharge air isolation damper percentage sensor.
  //
  // Composed of:
  //   discharge (descriptor): Media leaving system to enter ambient
  //     conditioned space. Typically applies only to air-side systems.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   isolation (descriptor): Process of isolating one component or
  //     process from another.
  //   damper (component): Component which meters the flow of air within a
  //     system or device.
  //   percentage (measurement): Measurement per hundred.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `discharge_air_isolation_damper_percentage_sensor`,
  // point type `sensor`. Measured as percentage; values are in percent, the
  // ontology's standard unit for that quantity.
  //
  // Valid range 0 to 100, enforced by protovalidate.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double discharge_air_isolation_damper_percentage_sensor = 59 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "discharge_air_isolation_damper_percentage_sensor"
      subfields: "discharge"
      subfields: "air"
      subfields: "isolation"
      subfields: "damper"
      subfields: "percentage"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "percentage"
      standard_unit: "percent"
    },
    (buf.validate.field).double.gte = 0.0,
    (buf.validate.field).double.lte = 100.0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The discharge air isolation damper status.
  //
  // Composed of:
  //   discharge (descriptor): Media leaving system to enter ambient
  //     conditioned space. Typically applies only to air-side systems.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   isolation (descriptor): Process of isolating one component or
  //     process from another.
  //   damper (component): Component which meters the flow of air within a
  //     system or device.
  //   status (point_type): The multistate value indicating an observed
  //     state in a piece of equipment, often indicating if a command was
  //     effected. It is a neutral observation (i.e., no quality judgment of
  //     'good' or 'bad'). It also has no units of measurement; therefore, if
  //     combined with a measurement subfield, it will indicate that the
  //     field is the directional status based on some measurement of that
  //     type (e.g., power_status equates to an on/off value based on some
  //     inference of power).
  //
  // Ontology field: `discharge_air_isolation_damper_status`, point type
  // `status`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  repeated ClosedOpenValue discharge_air_isolation_damper_status = 60 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "discharge_air_isolation_damper_status"
      subfields: "discharge"
      subfields: "air"
      subfields: "isolation"
      subfields: "damper"
      subfields: "status"
      point_type: "status"
      enumerated: true
    }
  ];

  // The discharge air linearvelocity sensor.
  //
  // Composed of:
  //   discharge (descriptor): Media leaving system to enter ambient
  //     conditioned space. Typically applies only to air-side systems.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   linearvelocity (measurement): The rate of change of displacement
  //     with respect to time. For example, wind speed represents the linear
  //     velocity of the wind.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `discharge_air_linearvelocity_sensor`, point type
  // `sensor`. Measured as linearvelocity; values are in meters_per_second,
  // the ontology's standard unit for that quantity.
  //
  // Valid range from 0, enforced by protovalidate. Expected range up to
  // 25.4000508. This is a data-quality expectation, not a constraint: a
  // reading outside it is a fault to report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double discharge_air_linearvelocity_sensor = 61 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "discharge_air_linearvelocity_sensor"
      subfields: "discharge"
      subfields: "air"
      subfields: "linearvelocity"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "linearvelocity"
      standard_unit: "meters_per_second"
      expected_max: 25.400051
    },
    (buf.validate.field).double.gte = 0.0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The discharge air relative humidity sensor.
  //
  // Composed of:
  //   discharge (descriptor): Media leaving system to enter ambient
  //     conditioned space. Typically applies only to air-side systems.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   relative (measurement_descriptor): Quality of media with respect to
  //     theoretical minimum or maximum value for a given condition (e.g.,
  //     relative humidity).
  //   humidity (measurement): Concentration of water vapor in air
  //     (conditioned or unconditioned).
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `discharge_air_relative_humidity_sensor`, point type
  // `sensor`. Measured as humidity; values are in percent_relative_humidity,
  // the ontology's standard unit for that quantity.
  //
  // Valid range 0 to 100, enforced by protovalidate.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double discharge_air_relative_humidity_sensor = 62 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "discharge_air_relative_humidity_sensor"
      subfields: "discharge"
      subfields: "air"
      subfields: "relative"
      subfields: "humidity"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "humidity"
      standard_unit: "percent_relative_humidity"
    },
    (buf.validate.field).double.gte = 0.0,
    (buf.validate.field).double.lte = 100.0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The discharge air specificenthalpy sensor.
  //
  // Composed of:
  //   discharge (descriptor): Media leaving system to enter ambient
  //     conditioned space. Typically applies only to air-side systems.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   specificenthalpy (measurement): Measure of energy of air per unit
  //     mass.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `discharge_air_specificenthalpy_sensor`, point type
  // `sensor`. Measured as specificenthalpy; values are in
  // joules_per_kilogram, the ontology's standard unit for that quantity.
  //
  // Expected range 11630 to 93040. This is a data-quality expectation, not a
  // constraint: a reading outside it is a fault to report, not a message to
  // reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double discharge_air_specificenthalpy_sensor = 63 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "discharge_air_specificenthalpy_sensor"
      subfields: "discharge"
      subfields: "air"
      subfields: "specificenthalpy"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "specificenthalpy"
      standard_unit: "joules_per_kilogram"
      expected_min: 11630.0
      expected_max: 93040.0
    }
  ];

  // The discharge air static pressure sensor.
  //
  // Composed of:
  //   discharge (descriptor): Media leaving system to enter ambient
  //     conditioned space. Typically applies only to air-side systems.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   static (measurement_descriptor): Describing a system in equilibrium
  //     or at rest (e.g., supply_air_static_pressure_sensor).
  //   pressure (measurement): Measured force per unit area within a system
  //     or process.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `discharge_air_static_pressure_sensor`, point type
  // `sensor`. Measured as pressure; values are in pascals, the ontology's
  // standard unit for that quantity.
  //
  // Expected range -124.540012 to 996.320096. This is a data-quality
  // expectation, not a constraint: a reading outside it is a fault to
  // report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double discharge_air_static_pressure_sensor = 64 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "discharge_air_static_pressure_sensor"
      subfields: "discharge"
      subfields: "air"
      subfields: "static"
      subfields: "pressure"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "pressure"
      standard_unit: "pascals"
      expected_min: -124.540012
      expected_max: 996.320096
    }
  ];

  // The discharge air temperature sensor.
  //
  // Composed of:
  //   discharge (descriptor): Media leaving system to enter ambient
  //     conditioned space. Typically applies only to air-side systems.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `discharge_air_temperature_sensor`, point type `sensor`.
  // Measured as temperature; values are in kelvin, the ontology's standard
  // unit for that quantity.
  //
  // Expected range 255.9259259 to 338.7037037. This is a data-quality
  // expectation, not a constraint: a reading outside it is a fault to
  // report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double discharge_air_temperature_sensor = 65 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "discharge_air_temperature_sensor"
      subfields: "discharge"
      subfields: "air"
      subfields: "temperature"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "temperature"
      standard_unit: "kelvin"
      expected_min: 255.925926
      expected_max: 338.703704
    }
  ];

  // The discharge air temperature setpoint.
  //
  // Composed of:
  //   discharge (descriptor): Media leaving system to enter ambient
  //     conditioned space. Typically applies only to air-side systems.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   setpoint (point_type): Control target of process or system.
  //
  // Ontology field: `discharge_air_temperature_setpoint`, point type
  // `setpoint`. Measured as temperature; values are in kelvin, the
  // ontology's standard unit for that quantity.
  //
  // Expected range 255.9259259 to 338.7037037. This is a data-quality
  // expectation, not a constraint: a reading outside it is a fault to
  // report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double discharge_air_temperature_setpoint = 66 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "discharge_air_temperature_setpoint"
      subfields: "discharge"
      subfields: "air"
      subfields: "temperature"
      subfields: "setpoint"
      point_type: "setpoint"
      measurement: "temperature"
      standard_unit: "kelvin"
      expected_min: 255.925926
      expected_max: 338.703704
    }
  ];

  // The discharge fan current sensor.
  //
  // Composed of:
  //   discharge (descriptor): Media leaving system to enter ambient
  //     conditioned space. Typically applies only to air-side systems.
  //   fan (component): Component used for the distribution of air.
  //   current (measurement): Flow of electric charge.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `discharge_fan_current_sensor`, point type `sensor`.
  // Measured as current; values are in amperes, the ontology's standard unit
  // for that quantity.
  //
  // Valid range from 0, enforced by protovalidate. Expected range up to 100.
  // This is a data-quality expectation, not a constraint: a reading outside
  // it is a fault to report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  repeated double discharge_fan_current_sensor = 67 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "discharge_fan_current_sensor"
      subfields: "discharge"
      subfields: "fan"
      subfields: "current"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "current"
      standard_unit: "amperes"
      enumerated: true
      expected_max: 100.0
    }
  ];

  // The discharge fan energy accumulator.
  //
  // Composed of:
  //   discharge (descriptor): Media leaving system to enter ambient
  //     conditioned space. Typically applies only to air-side systems.
  //   fan (component): Component used for the distribution of air.
  //   energy (measurement): The quantitative property that must be
  //     transferred to an object to perform work (default electrical unless
  //     otherwise modified).
  //   accumulator (point_type): The total accumulated quantity (e.g.,
  //     total energy accumulated).
  //
  // Ontology field: `discharge_fan_energy_accumulator`, point type
  // `accumulator`. Measured as energy; values are in joules, the ontology's
  // standard unit for that quantity.
  //
  // Valid range from 0, enforced by protovalidate. Expected range up to
  // 3.6e+13. This is a data-quality expectation, not a constraint: a reading
  // outside it is a fault to report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double discharge_fan_energy_accumulator = 68 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "discharge_fan_energy_accumulator"
      subfields: "discharge"
      subfields: "fan"
      subfields: "energy"
      subfields: "accumulator"
      point_type: "accumulator"
      measurement: "energy"
      standard_unit: "joules"
      expected_max: 36000000000000.0
    },
    (buf.validate.field).double.gte = 0.0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The discharge fan lost power alarm.
  //
  // Composed of:
  //   discharge (descriptor): Media leaving system to enter ambient
  //     conditioned space. Typically applies only to air-side systems.
  //   fan (component): Component used for the distribution of air.
  //   lost (measurement_descriptor): Indicates that a point has lost some
  //     quality or quantity inadvertently.
  //   power (measurement): Rate of energy consumption (assumed context is
  //     electrical and real, unless otherwise modified).
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `discharge_fan_lost_power_alarm`, point type `alarm`.
  // Measured as power; values are in watts, the ontology's standard unit for
  // that quantity.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  repeated ActiveInactiveValue discharge_fan_lost_power_alarm = 69 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "discharge_fan_lost_power_alarm"
      subfields: "discharge"
      subfields: "fan"
      subfields: "lost"
      subfields: "power"
      subfields: "alarm"
      point_type: "alarm"
      measurement: "power"
      standard_unit: "watts"
      enumerated: true
    }
  ];

  // The discharge fan power capacity.
  //
  // Composed of:
  //   discharge (descriptor): Media leaving system to enter ambient
  //     conditioned space. Typically applies only to air-side systems.
  //   fan (component): Component used for the distribution of air.
  //   power (measurement): Rate of energy consumption (assumed context is
  //     electrical and real, unless otherwise modified).
  //   capacity (point_type): A design parameter quantity (e.g., design
  //     motor power capacity). Is always a maximum limit.
  //
  // Ontology field: `discharge_fan_power_capacity`, point type `capacity`.
  // Measured as power; values are in watts, the ontology's standard unit for
  // that quantity.
  //
  // Valid range from 0, enforced by protovalidate. Expected range up to
  // 100000. This is a data-quality expectation, not a constraint: a reading
  // outside it is a fault to report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double discharge_fan_power_capacity = 70 [
    (google.api.field_behavior) = OPTIONAL,
    (google.api.field_behavior) = IMMUTABLE,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "discharge_fan_power_capacity"
      subfields: "discharge"
      subfields: "fan"
      subfields: "power"
      subfields: "capacity"
      point_type: "capacity"
      measurement: "power"
      standard_unit: "watts"
      expected_max: 100000.0
    },
    (buf.validate.field).double.gte = 0.0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The discharge fan power sensor.
  //
  // Composed of:
  //   discharge (descriptor): Media leaving system to enter ambient
  //     conditioned space. Typically applies only to air-side systems.
  //   fan (component): Component used for the distribution of air.
  //   power (measurement): Rate of energy consumption (assumed context is
  //     electrical and real, unless otherwise modified).
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `discharge_fan_power_sensor`, point type `sensor`.
  // Measured as power; values are in watts, the ontology's standard unit for
  // that quantity.
  //
  // Expected range 0 to 100000. This is a data-quality expectation, not a
  // constraint: a reading outside it is a fault to report, not a message to
  // reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  repeated double discharge_fan_power_sensor = 71 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "discharge_fan_power_sensor"
      subfields: "discharge"
      subfields: "fan"
      subfields: "power"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "power"
      standard_unit: "watts"
      enumerated: true
      expected_min: 0.0
      expected_max: 100000.0
    }
  ];

  // The discharge fan rotationalvelocity sensor.
  //
  // Composed of:
  //   discharge (descriptor): Media leaving system to enter ambient
  //     conditioned space. Typically applies only to air-side systems.
  //   fan (component): Component used for the distribution of air.
  //   rotationalvelocity (measurement): Vector measure of rotation rate,
  //     that refers to how fast an object rotates or revolves relative to
  //     another point.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `discharge_fan_rotationalvelocity_sensor`, point type
  // `sensor`. Measured as rotationalvelocity; values are in
  // revolutions_per_minute, the ontology's standard unit for that quantity.
  //
  // Valid range from 0, enforced by protovalidate. Expected range up to
  // 4000. This is a data-quality expectation, not a constraint: a reading
  // outside it is a fault to report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  repeated double discharge_fan_rotationalvelocity_sensor = 72 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "discharge_fan_rotationalvelocity_sensor"
      subfields: "discharge"
      subfields: "fan"
      subfields: "rotationalvelocity"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "rotationalvelocity"
      standard_unit: "revolutions_per_minute"
      enumerated: true
      expected_max: 4000.0
    }
  ];

  // The discharge fan run command.
  //
  // Composed of:
  //   discharge (descriptor): Media leaving system to enter ambient
  //     conditioned space. Typically applies only to air-side systems.
  //   fan (component): Component used for the distribution of air.
  //   run (measurement_descriptor): State of being active or operating. By
  //     default applies to control program for system (e.g., VAV program
  //     run_command).
  //   command (point_type): The signal given to make an action happen.
  //     Defaults to multistate unless used alongside a measurement_type.
  //
  // Ontology field: `discharge_fan_run_command`, point type `command`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  repeated AutoOffOnUnknownValue discharge_fan_run_command = 73 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "discharge_fan_run_command"
      subfields: "discharge"
      subfields: "fan"
      subfields: "run"
      subfields: "command"
      point_type: "command"
      enumerated: true
    }
  ];

  // The discharge fan run mode.
  //
  // Composed of:
  //   discharge (descriptor): Media leaving system to enter ambient
  //     conditioned space. Typically applies only to air-side systems.
  //   fan (component): Component used for the distribution of air.
  //   run (measurement_descriptor): State of being active or operating. By
  //     default applies to control program for system (e.g., VAV program
  //     run_command).
  //   mode (point_type): Distinct mode of operation within system. For
  //     example economizer_mode has modes of 'enabled' or 'disabled.'
  //
  // Ontology field: `discharge_fan_run_mode`, point type `mode`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional AutoManualValue discharge_fan_run_mode = 74 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "discharge_fan_run_mode"
      subfields: "discharge"
      subfields: "fan"
      subfields: "run"
      subfields: "mode"
      point_type: "mode"
    }
  ];

  // The discharge fan run status.
  //
  // Composed of:
  //   discharge (descriptor): Media leaving system to enter ambient
  //     conditioned space. Typically applies only to air-side systems.
  //   fan (component): Component used for the distribution of air.
  //   run (measurement_descriptor): State of being active or operating. By
  //     default applies to control program for system (e.g., VAV program
  //     run_command).
  //   status (point_type): The multistate value indicating an observed
  //     state in a piece of equipment, often indicating if a command was
  //     effected. It is a neutral observation (i.e., no quality judgment of
  //     'good' or 'bad'). It also has no units of measurement; therefore, if
  //     combined with a measurement subfield, it will indicate that the
  //     field is the directional status based on some measurement of that
  //     type (e.g., power_status equates to an on/off value based on some
  //     inference of power).
  //
  // Ontology field: `discharge_fan_run_status`, point type `status`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  repeated AutoOffOnUnknownValue discharge_fan_run_status = 75 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "discharge_fan_run_status"
      subfields: "discharge"
      subfields: "fan"
      subfields: "run"
      subfields: "status"
      point_type: "status"
      enumerated: true
    }
  ];

  // The discharge fan run time accumulator.
  //
  // Composed of:
  //   discharge (descriptor): Media leaving system to enter ambient
  //     conditioned space. Typically applies only to air-side systems.
  //   fan (component): Component used for the distribution of air.
  //   run (measurement_descriptor): State of being active or operating. By
  //     default applies to control program for system (e.g., VAV program
  //     run_command).
  //   time (measurement): Defined duration of something.
  //   accumulator (point_type): The total accumulated quantity (e.g.,
  //     total energy accumulated).
  //
  // Ontology field: `discharge_fan_run_time_accumulator`, point type
  // `accumulator`. Measured as time; values are in seconds, the ontology's
  // standard unit for that quantity.
  //
  // Valid range from 0, enforced by protovalidate. Expected range up to
  // 1.25. This is a data-quality expectation, not a constraint: a reading
  // outside it is a fault to report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double discharge_fan_run_time_accumulator = 76 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "discharge_fan_run_time_accumulator"
      subfields: "discharge"
      subfields: "fan"
      subfields: "run"
      subfields: "time"
      subfields: "accumulator"
      point_type: "accumulator"
      measurement: "time"
      standard_unit: "seconds"
      expected_max: 1.25
    },
    (buf.validate.field).double.gte = 0.0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The discharge fan speed frequency command.
  //
  // Composed of:
  //   discharge (descriptor): Media leaving system to enter ambient
  //     conditioned space. Typically applies only to air-side systems.
  //   fan (component): Component used for the distribution of air.
  //   speed (measurement_descriptor): Numeric setting of how fast to run a
  //     device, in the specified units. Typically used to describe
  //     revolutions of a motor as a fraction of nominal or maximum.
  //   frequency (measurement): The rate of cycling within a process.
  //     Typically used to describe voltage (and amperage) oscillation within
  //     AC power distribution components.
  //   command (point_type): The signal given to make an action happen.
  //     Defaults to multistate unless used alongside a measurement_type.
  //
  // Ontology field: `discharge_fan_speed_frequency_command`, point type
  // `command`. Measured as frequency; values are in hertz, the ontology's
  // standard unit for that quantity.
  //
  // Expected range -0.5 to 65. This is a data-quality expectation, not a
  // constraint: a reading outside it is a fault to report, not a message to
  // reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double discharge_fan_speed_frequency_command = 77 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "discharge_fan_speed_frequency_command"
      subfields: "discharge"
      subfields: "fan"
      subfields: "speed"
      subfields: "frequency"
      subfields: "command"
      point_type: "command"
      measurement: "frequency"
      standard_unit: "hertz"
      expected_min: -0.5
      expected_max: 65.0
    }
  ];

  // The discharge fan speed frequency sensor.
  //
  // Composed of:
  //   discharge (descriptor): Media leaving system to enter ambient
  //     conditioned space. Typically applies only to air-side systems.
  //   fan (component): Component used for the distribution of air.
  //   speed (measurement_descriptor): Numeric setting of how fast to run a
  //     device, in the specified units. Typically used to describe
  //     revolutions of a motor as a fraction of nominal or maximum.
  //   frequency (measurement): The rate of cycling within a process.
  //     Typically used to describe voltage (and amperage) oscillation within
  //     AC power distribution components.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `discharge_fan_speed_frequency_sensor`, point type
  // `sensor`. Measured as frequency; values are in hertz, the ontology's
  // standard unit for that quantity.
  //
  // Expected range -0.5 to 65. This is a data-quality expectation, not a
  // constraint: a reading outside it is a fault to report, not a message to
  // reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  repeated double discharge_fan_speed_frequency_sensor = 78 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "discharge_fan_speed_frequency_sensor"
      subfields: "discharge"
      subfields: "fan"
      subfields: "speed"
      subfields: "frequency"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "frequency"
      standard_unit: "hertz"
      enumerated: true
      expected_min: -0.5
      expected_max: 65.0
    }
  ];

  // The discharge fan speed mode.
  //
  // Composed of:
  //   discharge (descriptor): Media leaving system to enter ambient
  //     conditioned space. Typically applies only to air-side systems.
  //   fan (component): Component used for the distribution of air.
  //   speed (measurement_descriptor): Numeric setting of how fast to run a
  //     device, in the specified units. Typically used to describe
  //     revolutions of a motor as a fraction of nominal or maximum.
  //   mode (point_type): Distinct mode of operation within system. For
  //     example economizer_mode has modes of 'enabled' or 'disabled.'
  //
  // Ontology field: `discharge_fan_speed_mode`, point type `mode`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  repeated AutoEightyPercentFortyPercentHighLowMediumOffSixtyPercentTwentyPercentUnknownVfdValue discharge_fan_speed_mode = 79 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "discharge_fan_speed_mode"
      subfields: "discharge"
      subfields: "fan"
      subfields: "speed"
      subfields: "mode"
      point_type: "mode"
      enumerated: true
    }
  ];

  // The discharge fan speed percentage command.
  //
  // Composed of:
  //   discharge (descriptor): Media leaving system to enter ambient
  //     conditioned space. Typically applies only to air-side systems.
  //   fan (component): Component used for the distribution of air.
  //   speed (measurement_descriptor): Numeric setting of how fast to run a
  //     device, in the specified units. Typically used to describe
  //     revolutions of a motor as a fraction of nominal or maximum.
  //   percentage (measurement): Measurement per hundred.
  //   command (point_type): The signal given to make an action happen.
  //     Defaults to multistate unless used alongside a measurement_type.
  //
  // Ontology field: `discharge_fan_speed_percentage_command`, point type
  // `command`. Measured as percentage; values are in percent, the ontology's
  // standard unit for that quantity.
  //
  // Valid range 0 to 100, enforced by protovalidate.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  repeated double discharge_fan_speed_percentage_command = 80 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "discharge_fan_speed_percentage_command"
      subfields: "discharge"
      subfields: "fan"
      subfields: "speed"
      subfields: "percentage"
      subfields: "command"
      point_type: "command"
      measurement: "percentage"
      standard_unit: "percent"
      enumerated: true
    }
  ];

  // The discharge fan speed percentage sensor.
  //
  // Composed of:
  //   discharge (descriptor): Media leaving system to enter ambient
  //     conditioned space. Typically applies only to air-side systems.
  //   fan (component): Component used for the distribution of air.
  //   speed (measurement_descriptor): Numeric setting of how fast to run a
  //     device, in the specified units. Typically used to describe
  //     revolutions of a motor as a fraction of nominal or maximum.
  //   percentage (measurement): Measurement per hundred.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `discharge_fan_speed_percentage_sensor`, point type
  // `sensor`. Measured as percentage; values are in percent, the ontology's
  // standard unit for that quantity.
  //
  // Valid range 0 to 100, enforced by protovalidate.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  repeated double discharge_fan_speed_percentage_sensor = 81 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "discharge_fan_speed_percentage_sensor"
      subfields: "discharge"
      subfields: "fan"
      subfields: "speed"
      subfields: "percentage"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "percentage"
      standard_unit: "percent"
      enumerated: true
    }
  ];

  // The discharge fan torque sensor.
  //
  // Composed of:
  //   discharge (descriptor): Media leaving system to enter ambient
  //     conditioned space. Typically applies only to air-side systems.
  //   fan (component): Component used for the distribution of air.
  //   torque (measurement): A force that causes rotation around an axis.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `discharge_fan_torque_sensor`, point type `sensor`.
  // Measured as torque; values are in newton_meters, the ontology's standard
  // unit for that quantity.
  //
  // Valid range from 0, enforced by protovalidate. Expected range up to 678.
  // This is a data-quality expectation, not a constraint: a reading outside
  // it is a fault to report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double discharge_fan_torque_sensor = 82 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "discharge_fan_torque_sensor"
      subfields: "discharge"
      subfields: "fan"
      subfields: "torque"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "torque"
      standard_unit: "newton_meters"
      expected_max: 678.0
    },
    (buf.validate.field).double.gte = 0.0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The discharge fan voltage sensor.
  //
  // Composed of:
  //   discharge (descriptor): Media leaving system to enter ambient
  //     conditioned space. Typically applies only to air-side systems.
  //   fan (component): Component used for the distribution of air.
  //   voltage (measurement): Electrical potential difference between two
  //     reference points.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `discharge_fan_voltage_sensor`, point type `sensor`.
  // Measured as voltage; values are in volts, the ontology's standard unit
  // for that quantity.
  //
  // Expected range 0 to 500. This is a data-quality expectation, not a
  // constraint: a reading outside it is a fault to report, not a message to
  // reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  repeated double discharge_fan_voltage_sensor = 83 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "discharge_fan_voltage_sensor"
      subfields: "discharge"
      subfields: "fan"
      subfields: "voltage"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "voltage"
      standard_unit: "volts"
      enumerated: true
      expected_min: 0.0
      expected_max: 500.0
    }
  ];

  // The drain pump run status.
  //
  // Composed of:
  //   drain (descriptor): Media drained from system; typically applied to
  //     water-side system
  //   pump (component): Component used for the distribution of liquid
  //     media (e.g., water, glycol).
  //   run (measurement_descriptor): State of being active or operating. By
  //     default applies to control program for system (e.g., VAV program
  //     run_command).
  //   status (point_type): The multistate value indicating an observed
  //     state in a piece of equipment, often indicating if a command was
  //     effected. It is a neutral observation (i.e., no quality judgment of
  //     'good' or 'bad'). It also has no units of measurement; therefore, if
  //     combined with a measurement subfield, it will indicate that the
  //     field is the directional status based on some measurement of that
  //     type (e.g., power_status equates to an on/off value based on some
  //     inference of power).
  //
  // Ontology field: `drain_pump_run_status`, point type `status`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional OffOnValue drain_pump_run_status = 84 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "drain_pump_run_status"
      subfields: "drain"
      subfields: "pump"
      subfields: "run"
      subfields: "status"
      point_type: "status"
    }
  ];

  // The economizer mode.
  //
  // Composed of:
  //   economizer (descriptor): Process or component responsible for the
  //     utilization of free cooling.
  //   mode (point_type): Distinct mode of operation within system. For
  //     example economizer_mode has modes of 'enabled' or 'disabled.'
  //
  // Ontology field: `economizer_mode`, point type `mode`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional AutoOffOnValue economizer_mode = 85 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "economizer_mode"
      subfields: "economizer"
      subfields: "mode"
      point_type: "mode"
    }
  ];

  // The efficiency percentage sensor.
  //
  // Composed of:
  //   efficiency (measurement_descriptor): The ratio of required input to
  //     actual output.
  //   percentage (measurement): Measurement per hundred.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `efficiency_percentage_sensor`, point type `sensor`.
  // Measured as percentage; values are in percent, the ontology's standard
  // unit for that quantity.
  //
  // Valid range 0 to 100, enforced by protovalidate.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double efficiency_percentage_sensor = 86 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "efficiency_percentage_sensor"
      subfields: "efficiency"
      subfields: "percentage"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "percentage"
      standard_unit: "percent"
    },
    (buf.validate.field).double.gte = 0.0,
    (buf.validate.field).double.lte = 100.0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The emergency alarm.
  //
  // Composed of:
  //   emergency (descriptor): An situation that poses an immediate risk to
  //     health, life, property, or environment.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `emergency_alarm`, point type `alarm`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue emergency_alarm = 87 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "emergency_alarm"
      subfields: "emergency"
      subfields: "alarm"
      point_type: "alarm"
    }
  ];

  // The energy accumulator.
  //
  // Composed of:
  //   energy (measurement): The quantitative property that must be
  //     transferred to an object to perform work (default electrical unless
  //     otherwise modified).
  //   accumulator (point_type): The total accumulated quantity (e.g.,
  //     total energy accumulated).
  //
  // Ontology field: `energy_accumulator`, point type `accumulator`. Measured
  // as energy; values are in joules, the ontology's standard unit for that
  // quantity.
  //
  // Valid range from 0, enforced by protovalidate. Expected range up to
  // 3.6e+19. This is a data-quality expectation, not a constraint: a reading
  // outside it is a fault to report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double energy_accumulator = 88 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "energy_accumulator"
      subfields: "energy"
      subfields: "accumulator"
      point_type: "accumulator"
      measurement: "energy"
      standard_unit: "joules"
      expected_max: 36000000000000000000.0
    },
    (buf.validate.field).double.gte = 0.0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The failed alarm.
  //
  // Composed of:
  //   failed (descriptor): Indicates that a point is not operating as
  //     intended or is incapable of operating.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `failed_alarm`, point type `alarm`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue failed_alarm = 89 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "failed_alarm"
      subfields: "failed"
      subfields: "alarm"
      point_type: "alarm"
    }
  ];

  // The failed chilled return water temperature alarm.
  //
  // Composed of:
  //   failed (descriptor): Indicates that a point is not operating as
  //     intended or is incapable of operating.
  //   chilled (descriptor): Cold water (typically 40 - 50F).
  //   return (descriptor): Measurement or process of media as it is
  //     returned from the end-use equipment within the system.
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `failed_chilled_return_water_temperature_alarm`, point
  // type `alarm`. Measured as temperature; values are in kelvin, the
  // ontology's standard unit for that quantity.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue failed_chilled_return_water_temperature_alarm = 90 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "failed_chilled_return_water_temperature_alarm"
      subfields: "failed"
      subfields: "chilled"
      subfields: "return"
      subfields: "water"
      subfields: "temperature"
      subfields: "alarm"
      point_type: "alarm"
      measurement: "temperature"
      standard_unit: "kelvin"
    }
  ];

  // The failed chilled supply water temperature alarm.
  //
  // Composed of:
  //   failed (descriptor): Indicates that a point is not operating as
  //     intended or is incapable of operating.
  //   chilled (descriptor): Cold water (typically 40 - 50F).
  //   supply (descriptor): Measurement or process of media as it is
  //     supplied to the end-use equipment within the system.
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `failed_chilled_supply_water_temperature_alarm`, point
  // type `alarm`. Measured as temperature; values are in kelvin, the
  // ontology's standard unit for that quantity.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue failed_chilled_supply_water_temperature_alarm = 91 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "failed_chilled_supply_water_temperature_alarm"
      subfields: "failed"
      subfields: "chilled"
      subfields: "supply"
      subfields: "water"
      subfields: "temperature"
      subfields: "alarm"
      point_type: "alarm"
      measurement: "temperature"
      standard_unit: "kelvin"
    }
  ];

  // The failed chilled water valve alarm.
  //
  // Composed of:
  //   failed (descriptor): Indicates that a point is not operating as
  //     intended or is incapable of operating.
  //   chilled (descriptor): Cold water (typically 40 - 50F).
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   valve (component): Component which meters the flow of water within a
  //     system or device.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `failed_chilled_water_valve_alarm`, point type `alarm`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue failed_chilled_water_valve_alarm = 92 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "failed_chilled_water_valve_alarm"
      subfields: "failed"
      subfields: "chilled"
      subfields: "water"
      subfields: "valve"
      subfields: "alarm"
      point_type: "alarm"
    }
  ];

  // The failed cold aisle air temperature alarm.
  //
  // Composed of:
  //   failed (descriptor): Indicates that a point is not operating as
  //     intended or is incapable of operating.
  //   cold (descriptor): Used to describe something at a low (or
  //     relatively low) temperature.
  //   aisle (descriptor): Passage between two rows of server racks.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `failed_cold_aisle_air_temperature_alarm`, point type
  // `alarm`. Measured as temperature; values are in kelvin, the ontology's
  // standard unit for that quantity.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  repeated ActiveInactiveValue failed_cold_aisle_air_temperature_alarm = 93 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "failed_cold_aisle_air_temperature_alarm"
      subfields: "failed"
      subfields: "cold"
      subfields: "aisle"
      subfields: "air"
      subfields: "temperature"
      subfields: "alarm"
      point_type: "alarm"
      measurement: "temperature"
      standard_unit: "kelvin"
      enumerated: true
    }
  ];

  // The failed communication alarm.
  //
  // Composed of:
  //   failed (descriptor): Indicates that a point is not operating as
  //     intended or is incapable of operating.
  //   communication (descriptor): Actual transmit and recieve of data or
  //     connectivity between devices.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `failed_communication_alarm`, point type `alarm`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue failed_communication_alarm = 94 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "failed_communication_alarm"
      subfields: "failed"
      subfields: "communication"
      subfields: "alarm"
      point_type: "alarm"
    }
  ];

  // The failed compressor alarm.
  //
  // Composed of:
  //   failed (descriptor): Indicates that a point is not operating as
  //     intended or is incapable of operating.
  //   compressor (component): Component which drives refrigerant
  //     compression (and thus cooling processes) within a device or system.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `failed_compressor_alarm`, point type `alarm`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  repeated ActiveInactiveValue failed_compressor_alarm = 95 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "failed_compressor_alarm"
      subfields: "failed"
      subfields: "compressor"
      subfields: "alarm"
      point_type: "alarm"
      enumerated: true
    }
  ];

  // The failed condensing return water isolation valve alarm.
  //
  // Composed of:
  //   failed (descriptor): Indicates that a point is not operating as
  //     intended or is incapable of operating.
  //   condensing (descriptor): The process of a gas (or vapor) changing to
  //     liquid.
  //   return (descriptor): Measurement or process of media as it is
  //     returned from the end-use equipment within the system.
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   isolation (descriptor): Process of isolating one component or
  //     process from another.
  //   valve (component): Component which meters the flow of water within a
  //     system or device.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `failed_condensing_return_water_isolation_valve_alarm`,
  // point type `alarm`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue failed_condensing_return_water_isolation_valve_alarm = 96 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "failed_condensing_return_water_isolation_valve_alarm"
      subfields: "failed"
      subfields: "condensing"
      subfields: "return"
      subfields: "water"
      subfields: "isolation"
      subfields: "valve"
      subfields: "alarm"
      point_type: "alarm"
    }
  ];

  // The failed discharge air isolation damper alarm.
  //
  // Composed of:
  //   failed (descriptor): Indicates that a point is not operating as
  //     intended or is incapable of operating.
  //   discharge (descriptor): Media leaving system to enter ambient
  //     conditioned space. Typically applies only to air-side systems.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   isolation (descriptor): Process of isolating one component or
  //     process from another.
  //   damper (component): Component which meters the flow of air within a
  //     system or device.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `failed_discharge_air_isolation_damper_alarm`, point
  // type `alarm`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue failed_discharge_air_isolation_damper_alarm = 97 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "failed_discharge_air_isolation_damper_alarm"
      subfields: "failed"
      subfields: "discharge"
      subfields: "air"
      subfields: "isolation"
      subfields: "damper"
      subfields: "alarm"
      point_type: "alarm"
    }
  ];

  // The failed discharge air temperature alarm.
  //
  // Composed of:
  //   failed (descriptor): Indicates that a point is not operating as
  //     intended or is incapable of operating.
  //   discharge (descriptor): Media leaving system to enter ambient
  //     conditioned space. Typically applies only to air-side systems.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `failed_discharge_air_temperature_alarm`, point type
  // `alarm`. Measured as temperature; values are in kelvin, the ontology's
  // standard unit for that quantity.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue failed_discharge_air_temperature_alarm = 98 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "failed_discharge_air_temperature_alarm"
      subfields: "failed"
      subfields: "discharge"
      subfields: "air"
      subfields: "temperature"
      subfields: "alarm"
      point_type: "alarm"
      measurement: "temperature"
      standard_unit: "kelvin"
    }
  ];

  // The failed discharge fan alarm.
  //
  // Composed of:
  //   failed (descriptor): Indicates that a point is not operating as
  //     intended or is incapable of operating.
  //   discharge (descriptor): Media leaving system to enter ambient
  //     conditioned space. Typically applies only to air-side systems.
  //   fan (component): Component used for the distribution of air.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `failed_discharge_fan_alarm`, point type `alarm`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  repeated ActiveInactiveValue failed_discharge_fan_alarm = 99 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "failed_discharge_fan_alarm"
      subfields: "failed"
      subfields: "discharge"
      subfields: "fan"
      subfields: "alarm"
      point_type: "alarm"
      enumerated: true
    }
  ];

  // The failed drain pump alarm.
  //
  // Composed of:
  //   failed (descriptor): Indicates that a point is not operating as
  //     intended or is incapable of operating.
  //   drain (descriptor): Media drained from system; typically applied to
  //     water-side system
  //   pump (component): Component used for the distribution of liquid
  //     media (e.g., water, glycol).
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `failed_drain_pump_alarm`, point type `alarm`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  repeated ActiveInactiveValue failed_drain_pump_alarm = 100 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "failed_drain_pump_alarm"
      subfields: "failed"
      subfields: "drain"
      subfields: "pump"
      subfields: "alarm"
      point_type: "alarm"
      enumerated: true
    }
  ];

  // The failed heat exchange alarm.
  //
  // Composed of:
  //   failed (descriptor): Indicates that a point is not operating as
  //     intended or is incapable of operating.
  //   heat (descriptor): Quality of media indicating energy level.
  //   exchange (descriptor): The act of giving somethng and receiving
  //     something else; for example, the transfer of heat from one fluid
  //     stream to another.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `failed_heat_exchange_alarm`, point type `alarm`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue failed_heat_exchange_alarm = 101 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "failed_heat_exchange_alarm"
      subfields: "failed"
      subfields: "heat"
      subfields: "exchange"
      subfields: "alarm"
      point_type: "alarm"
    }
  ];

  // The failed heater alarm.
  //
  // Composed of:
  //   failed (descriptor): Indicates that a point is not operating as
  //     intended or is incapable of operating.
  //   heater (component): Component which provides heat to media
  //     (typically through the combustion of gas or electric resistance).
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `failed_heater_alarm`, point type `alarm`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue failed_heater_alarm = 102 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "failed_heater_alarm"
      subfields: "failed"
      subfields: "heater"
      subfields: "alarm"
      point_type: "alarm"
    }
  ];

  // The failed heating alarm.
  //
  // Composed of:
  //   failed (descriptor): Indicates that a point is not operating as
  //     intended or is incapable of operating.
  //   heating (descriptor): Process, measured effect or requirement for
  //     heating.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `failed_heating_alarm`, point type `alarm`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue failed_heating_alarm = 103 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "failed_heating_alarm"
      subfields: "failed"
      subfields: "heating"
      subfields: "alarm"
      point_type: "alarm"
    }
  ];

  // The failed heating water valve alarm.
  //
  // Composed of:
  //   failed (descriptor): Indicates that a point is not operating as
  //     intended or is incapable of operating.
  //   heating (descriptor): Process, measured effect or requirement for
  //     heating.
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   valve (component): Component which meters the flow of water within a
  //     system or device.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `failed_heating_water_valve_alarm`, point type `alarm`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue failed_heating_water_valve_alarm = 104 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "failed_heating_water_valve_alarm"
      subfields: "failed"
      subfields: "heating"
      subfields: "water"
      subfields: "valve"
      subfields: "alarm"
      point_type: "alarm"
    }
  ];

  // The failed hot aisle air temperature alarm.
  //
  // Composed of:
  //   failed (descriptor): Indicates that a point is not operating as
  //     intended or is incapable of operating.
  //   hot (descriptor): Used to describe something at a high (or
  //     relatively high) temperature.
  //   aisle (descriptor): Passage between two rows of server racks.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `failed_hot_aisle_air_temperature_alarm`, point type
  // `alarm`. Measured as temperature; values are in kelvin, the ontology's
  // standard unit for that quantity.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  repeated ActiveInactiveValue failed_hot_aisle_air_temperature_alarm = 105 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "failed_hot_aisle_air_temperature_alarm"
      subfields: "failed"
      subfields: "hot"
      subfields: "aisle"
      subfields: "air"
      subfields: "temperature"
      subfields: "alarm"
      point_type: "alarm"
      measurement: "temperature"
      standard_unit: "kelvin"
      enumerated: true
    }
  ];

  // The failed humidifier alarm.
  //
  // Composed of:
  //   failed (descriptor): Indicates that a point is not operating as
  //     intended or is incapable of operating.
  //   humidifier (component): Component which humidifies (typically by
  //     adding water vapor to the air).
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `failed_humidifier_alarm`, point type `alarm`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  repeated ActiveInactiveValue failed_humidifier_alarm = 106 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "failed_humidifier_alarm"
      subfields: "failed"
      subfields: "humidifier"
      subfields: "alarm"
      point_type: "alarm"
      enumerated: true
    }
  ];

  // The failed inverter alarm.
  //
  // Composed of:
  //   failed (descriptor): Indicates that a point is not operating as
  //     intended or is incapable of operating.
  //   inverter (component): Device or circuitry that changes direct
  //     current to alternating current (e.g., variable frequency drive).
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `failed_inverter_alarm`, point type `alarm`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue failed_inverter_alarm = 107 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "failed_inverter_alarm"
      subfields: "failed"
      subfields: "inverter"
      subfields: "alarm"
      point_type: "alarm"
    }
  ];

  // The failed leaving cooling coil temperature alarm.
  //
  // Composed of:
  //   failed (descriptor): Indicates that a point is not operating as
  //     intended or is incapable of operating.
  //   leaving (descriptor): The process of exiting a thing or area where
  //     media leaves process (e.g., water leaving a heat exchanger).
  //   cooling (descriptor): Process, measured effect or requirement for
  //     cooling.
  //   coil (component): Component that exchanges heat between two media
  //     streams.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `failed_leaving_cooling_coil_temperature_alarm`, point
  // type `alarm`. Measured as temperature; values are in kelvin, the
  // ontology's standard unit for that quantity.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue failed_leaving_cooling_coil_temperature_alarm = 108 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "failed_leaving_cooling_coil_temperature_alarm"
      subfields: "failed"
      subfields: "leaving"
      subfields: "cooling"
      subfields: "coil"
      subfields: "temperature"
      subfields: "alarm"
      point_type: "alarm"
      measurement: "temperature"
      standard_unit: "kelvin"
    }
  ];

  // The failed mixed air temperature alarm.
  //
  // Composed of:
  //   failed (descriptor): Indicates that a point is not operating as
  //     intended or is incapable of operating.
  //   mixed (descriptor): Process or component used to mix multiple
  //     streams of air.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `failed_mixed_air_temperature_alarm`, point type
  // `alarm`. Measured as temperature; values are in kelvin, the ontology's
  // standard unit for that quantity.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue failed_mixed_air_temperature_alarm = 109 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "failed_mixed_air_temperature_alarm"
      subfields: "failed"
      subfields: "mixed"
      subfields: "air"
      subfields: "temperature"
      subfields: "alarm"
      point_type: "alarm"
      measurement: "temperature"
      standard_unit: "kelvin"
    }
  ];

  // The failed outside air temperature alarm.
  //
  // Composed of:
  //   failed (descriptor): Indicates that a point is not operating as
  //     intended or is incapable of operating.
  //   outside (descriptor): Process or measurement of local atmospheric
  //     conditions.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `failed_outside_air_temperature_alarm`, point type
  // `alarm`. Measured as temperature; values are in kelvin, the ontology's
  // standard unit for that quantity.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue failed_outside_air_temperature_alarm = 110 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "failed_outside_air_temperature_alarm"
      subfields: "failed"
      subfields: "outside"
      subfields: "air"
      subfields: "temperature"
      subfields: "alarm"
      point_type: "alarm"
      measurement: "temperature"
      standard_unit: "kelvin"
    }
  ];

  // The failed return air damper alarm.
  //
  // Composed of:
  //   failed (descriptor): Indicates that a point is not operating as
  //     intended or is incapable of operating.
  //   return (descriptor): Measurement or process of media as it is
  //     returned from the end-use equipment within the system.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   damper (component): Component which meters the flow of air within a
  //     system or device.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `failed_return_air_damper_alarm`, point type `alarm`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue failed_return_air_damper_alarm = 111 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "failed_return_air_damper_alarm"
      subfields: "failed"
      subfields: "return"
      subfields: "air"
      subfields: "damper"
      subfields: "alarm"
      point_type: "alarm"
    }
  ];

  // The failed return air isolation damper alarm.
  //
  // Composed of:
  //   failed (descriptor): Indicates that a point is not operating as
  //     intended or is incapable of operating.
  //   return (descriptor): Measurement or process of media as it is
  //     returned from the end-use equipment within the system.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   isolation (descriptor): Process of isolating one component or
  //     process from another.
  //   damper (component): Component which meters the flow of air within a
  //     system or device.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `failed_return_air_isolation_damper_alarm`, point type
  // `alarm`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue failed_return_air_isolation_damper_alarm = 112 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "failed_return_air_isolation_damper_alarm"
      subfields: "failed"
      subfields: "return"
      subfields: "air"
      subfields: "isolation"
      subfields: "damper"
      subfields: "alarm"
      point_type: "alarm"
    }
  ];

  // The failed return air temperature alarm.
  //
  // Composed of:
  //   failed (descriptor): Indicates that a point is not operating as
  //     intended or is incapable of operating.
  //   return (descriptor): Measurement or process of media as it is
  //     returned from the end-use equipment within the system.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `failed_return_air_temperature_alarm`, point type
  // `alarm`. Measured as temperature; values are in kelvin, the ontology's
  // standard unit for that quantity.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue failed_return_air_temperature_alarm = 113 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "failed_return_air_temperature_alarm"
      subfields: "failed"
      subfields: "return"
      subfields: "air"
      subfields: "temperature"
      subfields: "alarm"
      point_type: "alarm"
      measurement: "temperature"
      standard_unit: "kelvin"
    }
  ];

  // The failed return water temperature alarm.
  //
  // Composed of:
  //   failed (descriptor): Indicates that a point is not operating as
  //     intended or is incapable of operating.
  //   return (descriptor): Measurement or process of media as it is
  //     returned from the end-use equipment within the system.
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `failed_return_water_temperature_alarm`, point type
  // `alarm`. Measured as temperature; values are in kelvin, the ontology's
  // standard unit for that quantity.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue failed_return_water_temperature_alarm = 114 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "failed_return_water_temperature_alarm"
      subfields: "failed"
      subfields: "return"
      subfields: "water"
      subfields: "temperature"
      subfields: "alarm"
      point_type: "alarm"
      measurement: "temperature"
      standard_unit: "kelvin"
    }
  ];

  // The failed supply air isolation damper alarm.
  //
  // Composed of:
  //   failed (descriptor): Indicates that a point is not operating as
  //     intended or is incapable of operating.
  //   supply (descriptor): Measurement or process of media as it is
  //     supplied to the end-use equipment within the system.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   isolation (descriptor): Process of isolating one component or
  //     process from another.
  //   damper (component): Component which meters the flow of air within a
  //     system or device.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `failed_supply_air_isolation_damper_alarm`, point type
  // `alarm`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue failed_supply_air_isolation_damper_alarm = 115 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "failed_supply_air_isolation_damper_alarm"
      subfields: "failed"
      subfields: "supply"
      subfields: "air"
      subfields: "isolation"
      subfields: "damper"
      subfields: "alarm"
      point_type: "alarm"
    }
  ];

  // The failed supply air static pressure alarm.
  //
  // Composed of:
  //   failed (descriptor): Indicates that a point is not operating as
  //     intended or is incapable of operating.
  //   supply (descriptor): Measurement or process of media as it is
  //     supplied to the end-use equipment within the system.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   static (measurement_descriptor): Describing a system in equilibrium
  //     or at rest (e.g., supply_air_static_pressure_sensor).
  //   pressure (measurement): Measured force per unit area within a system
  //     or process.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `failed_supply_air_static_pressure_alarm`, point type
  // `alarm`. Measured as pressure; values are in pascals, the ontology's
  // standard unit for that quantity.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue failed_supply_air_static_pressure_alarm = 116 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "failed_supply_air_static_pressure_alarm"
      subfields: "failed"
      subfields: "supply"
      subfields: "air"
      subfields: "static"
      subfields: "pressure"
      subfields: "alarm"
      point_type: "alarm"
      measurement: "pressure"
      standard_unit: "pascals"
    }
  ];

  // The failed supply air temperature alarm.
  //
  // Composed of:
  //   failed (descriptor): Indicates that a point is not operating as
  //     intended or is incapable of operating.
  //   supply (descriptor): Measurement or process of media as it is
  //     supplied to the end-use equipment within the system.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `failed_supply_air_temperature_alarm`, point type
  // `alarm`. Measured as temperature; values are in kelvin, the ontology's
  // standard unit for that quantity.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue failed_supply_air_temperature_alarm = 117 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "failed_supply_air_temperature_alarm"
      subfields: "failed"
      subfields: "supply"
      subfields: "air"
      subfields: "temperature"
      subfields: "alarm"
      point_type: "alarm"
      measurement: "temperature"
      standard_unit: "kelvin"
    }
  ];

  // The failed supply fan alarm.
  //
  // Composed of:
  //   failed (descriptor): Indicates that a point is not operating as
  //     intended or is incapable of operating.
  //   supply (descriptor): Measurement or process of media as it is
  //     supplied to the end-use equipment within the system.
  //   fan (component): Component used for the distribution of air.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `failed_supply_fan_alarm`, point type `alarm`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue failed_supply_fan_alarm = 118 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "failed_supply_fan_alarm"
      subfields: "failed"
      subfields: "supply"
      subfields: "fan"
      subfields: "alarm"
      point_type: "alarm"
    }
  ];

  // The failed supply water temperature alarm.
  //
  // Composed of:
  //   failed (descriptor): Indicates that a point is not operating as
  //     intended or is incapable of operating.
  //   supply (descriptor): Measurement or process of media as it is
  //     supplied to the end-use equipment within the system.
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `failed_supply_water_temperature_alarm`, point type
  // `alarm`. Measured as temperature; values are in kelvin, the ontology's
  // standard unit for that quantity.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue failed_supply_water_temperature_alarm = 119 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "failed_supply_water_temperature_alarm"
      subfields: "failed"
      subfields: "supply"
      subfields: "water"
      subfields: "temperature"
      subfields: "alarm"
      point_type: "alarm"
      measurement: "temperature"
      standard_unit: "kelvin"
    }
  ];

  // The failed zone air co2 concentration alarm.
  //
  // Composed of:
  //   failed (descriptor): Indicates that a point is not operating as
  //     intended or is incapable of operating.
  //   zone (descriptor): Region of building which is conditioned.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   co2 (descriptor): Chemical formula and abbreviation for carbon
  //     dioxide.
  //   concentration (measurement): Concentration of something relative to
  //     something else (usually in parts per million or parts per billion).
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `failed_zone_air_co2_concentration_alarm`, point type
  // `alarm`. Measured as concentration; values are in parts_per_unit, the
  // ontology's standard unit for that quantity.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue failed_zone_air_co2_concentration_alarm = 324 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "failed_zone_air_co2_concentration_alarm"
      subfields: "failed"
      subfields: "zone"
      subfields: "air"
      subfields: "co2"
      subfields: "concentration"
      subfields: "alarm"
      point_type: "alarm"
      measurement: "concentration"
      standard_unit: "parts_per_unit"
    }
  ];

  // The failed zone air relative humidity alarm.
  //
  // Composed of:
  //   failed (descriptor): Indicates that a point is not operating as
  //     intended or is incapable of operating.
  //   zone (descriptor): Region of building which is conditioned.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   relative (measurement_descriptor): Quality of media with respect to
  //     theoretical minimum or maximum value for a given condition (e.g.,
  //     relative humidity).
  //   humidity (measurement): Concentration of water vapor in air
  //     (conditioned or unconditioned).
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `failed_zone_air_relative_humidity_alarm`, point type
  // `alarm`. Measured as humidity; values are in percent_relative_humidity,
  // the ontology's standard unit for that quantity.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue failed_zone_air_relative_humidity_alarm = 121 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "failed_zone_air_relative_humidity_alarm"
      subfields: "failed"
      subfields: "zone"
      subfields: "air"
      subfields: "relative"
      subfields: "humidity"
      subfields: "alarm"
      point_type: "alarm"
      measurement: "humidity"
      standard_unit: "percent_relative_humidity"
    }
  ];

  // The failed zone air temperature alarm.
  //
  // Composed of:
  //   failed (descriptor): Indicates that a point is not operating as
  //     intended or is incapable of operating.
  //   zone (descriptor): Region of building which is conditioned.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `failed_zone_air_temperature_alarm`, point type `alarm`.
  // Measured as temperature; values are in kelvin, the ontology's standard
  // unit for that quantity.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue failed_zone_air_temperature_alarm = 122 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "failed_zone_air_temperature_alarm"
      subfields: "failed"
      subfields: "zone"
      subfields: "air"
      subfields: "temperature"
      subfields: "alarm"
      point_type: "alarm"
      measurement: "temperature"
      standard_unit: "kelvin"
    }
  ];

  // The filter alarm.
  //
  // Composed of:
  //   filter (component): Component used for removing dust and other
  //     particulate matter from the air.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `filter_alarm`, point type `alarm`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  repeated ActiveInactiveValue filter_alarm = 123 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "filter_alarm"
      subfields: "filter"
      subfields: "alarm"
      point_type: "alarm"
      enumerated: true
    }
  ];

  // The filter differential pressure sensor.
  //
  // Composed of:
  //   filter (component): Component used for removing dust and other
  //     particulate matter from the air.
  //   differential (measurement_descriptor): Measured difference between
  //     two reference points (e.g. differential_water_pressure)
  //   pressure (measurement): Measured force per unit area within a system
  //     or process.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `filter_differential_pressure_sensor`, point type
  // `sensor`. Measured as pressure; values are in pascals, the ontology's
  // standard unit for that quantity.
  //
  // Expected range -249.080024 to 2490.80024. This is a data-quality
  // expectation, not a constraint: a reading outside it is a fault to
  // report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double filter_differential_pressure_sensor = 124 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "filter_differential_pressure_sensor"
      subfields: "filter"
      subfields: "differential"
      subfields: "pressure"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "pressure"
      standard_unit: "pascals"
      expected_min: -249.080024
      expected_max: 2490.80024
    }
  ];

  // The filter differential pressure status.
  //
  // Composed of:
  //   filter (component): Component used for removing dust and other
  //     particulate matter from the air.
  //   differential (measurement_descriptor): Measured difference between
  //     two reference points (e.g. differential_water_pressure)
  //   pressure (measurement): Measured force per unit area within a system
  //     or process.
  //   status (point_type): The multistate value indicating an observed
  //     state in a piece of equipment, often indicating if a command was
  //     effected. It is a neutral observation (i.e., no quality judgment of
  //     'good' or 'bad'). It also has no units of measurement; therefore, if
  //     combined with a measurement subfield, it will indicate that the
  //     field is the directional status based on some measurement of that
  //     type (e.g., power_status equates to an on/off value based on some
  //     inference of power).
  //
  // Ontology field: `filter_differential_pressure_status`, point type
  // `status`. Measured as pressure; values are in pascals, the ontology's
  // standard unit for that quantity.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  repeated AbsentPresentValue filter_differential_pressure_status = 125 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "filter_differential_pressure_status"
      subfields: "filter"
      subfields: "differential"
      subfields: "pressure"
      subfields: "status"
      point_type: "status"
      measurement: "pressure"
      standard_unit: "pascals"
      enumerated: true
    }
  ];

  // The fire alarm.
  //
  // Composed of:
  //   fire (descriptor): The combustion or burning of substances with the
  //     potential to cause physical damage.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `fire_alarm`, point type `alarm`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue fire_alarm = 126 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "fire_alarm"
      subfields: "fire"
      subfields: "alarm"
      point_type: "alarm"
    }
  ];

  // The flowrate capacity.
  //
  // Composed of:
  //   flowrate (measurement): Rate of fluid movement.
  //   capacity (point_type): A design parameter quantity (e.g., design
  //     motor power capacity). Is always a maximum limit.
  //
  // Ontology field: `flowrate_capacity`, point type `capacity`. Measured as
  // flowrate; values are in cubic_meters_per_second, the ontology's standard
  // unit for that quantity.
  //
  // Valid range from 0, enforced by protovalidate. Expected range up to
  // 141.584233. This is a data-quality expectation, not a constraint: a
  // reading outside it is a fault to report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double flowrate_capacity = 127 [
    (google.api.field_behavior) = OPTIONAL,
    (google.api.field_behavior) = IMMUTABLE,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "flowrate_capacity"
      subfields: "flowrate"
      subfields: "capacity"
      point_type: "capacity"
      measurement: "flowrate"
      standard_unit: "cubic_meters_per_second"
      expected_max: 141.584233
    },
    (buf.validate.field).double.gte = 0.0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The frost alarm.
  //
  // Composed of:
  //   frost (descriptor): The byproduct of freezing (i.e., liquid water
  //     turning into a solid on a surface).
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `frost_alarm`, point type `alarm`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue frost_alarm = 128 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "frost_alarm"
      subfields: "frost"
      subfields: "alarm"
      point_type: "alarm"
    }
  ];

  // The glycol valve percentage command.
  //
  // Composed of:
  //   glycol (descriptor): Liquid mixture consisting of glycol and water;
  //     typically used to increase the heat transfer abilities of
  //     traditional water.
  //   valve (component): Component which meters the flow of water within a
  //     system or device.
  //   percentage (measurement): Measurement per hundred.
  //   command (point_type): The signal given to make an action happen.
  //     Defaults to multistate unless used alongside a measurement_type.
  //
  // Ontology field: `glycol_valve_percentage_command`, point type `command`.
  // Measured as percentage; values are in percent, the ontology's standard
  // unit for that quantity.
  //
  // Valid range 0 to 100, enforced by protovalidate.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double glycol_valve_percentage_command = 129 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "glycol_valve_percentage_command"
      subfields: "glycol"
      subfields: "valve"
      subfields: "percentage"
      subfields: "command"
      point_type: "command"
      measurement: "percentage"
      standard_unit: "percent"
    },
    (buf.validate.field).double.gte = 0.0,
    (buf.validate.field).double.lte = 100.0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The heater run command.
  //
  // Composed of:
  //   heater (component): Component which provides heat to media
  //     (typically through the combustion of gas or electric resistance).
  //   run (measurement_descriptor): State of being active or operating. By
  //     default applies to control program for system (e.g., VAV program
  //     run_command).
  //   command (point_type): The signal given to make an action happen.
  //     Defaults to multistate unless used alongside a measurement_type.
  //
  // Ontology field: `heater_run_command`, point type `command`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  repeated OffOnUnknownValue heater_run_command = 130 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "heater_run_command"
      subfields: "heater"
      subfields: "run"
      subfields: "command"
      point_type: "command"
      enumerated: true
    }
  ];

  // The heater run status.
  //
  // Composed of:
  //   heater (component): Component which provides heat to media
  //     (typically through the combustion of gas or electric resistance).
  //   run (measurement_descriptor): State of being active or operating. By
  //     default applies to control program for system (e.g., VAV program
  //     run_command).
  //   status (point_type): The multistate value indicating an observed
  //     state in a piece of equipment, often indicating if a command was
  //     effected. It is a neutral observation (i.e., no quality judgment of
  //     'good' or 'bad'). It also has no units of measurement; therefore, if
  //     combined with a measurement subfield, it will indicate that the
  //     field is the directional status based on some measurement of that
  //     type (e.g., power_status equates to an on/off value based on some
  //     inference of power).
  //
  // Ontology field: `heater_run_status`, point type `status`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional OffOnValue heater_run_status = 131 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "heater_run_status"
      subfields: "heater"
      subfields: "run"
      subfields: "status"
      point_type: "status"
    }
  ];

  // The heater run time accumulator.
  //
  // Composed of:
  //   heater (component): Component which provides heat to media
  //     (typically through the combustion of gas or electric resistance).
  //   run (measurement_descriptor): State of being active or operating. By
  //     default applies to control program for system (e.g., VAV program
  //     run_command).
  //   time (measurement): Defined duration of something.
  //   accumulator (point_type): The total accumulated quantity (e.g.,
  //     total energy accumulated).
  //
  // Ontology field: `heater_run_time_accumulator`, point type `accumulator`.
  // Measured as time; values are in seconds, the ontology's standard unit
  // for that quantity.
  //
  // Valid range from 0, enforced by protovalidate. Expected range up to
  // 1.25. This is a data-quality expectation, not a constraint: a reading
  // outside it is a fault to report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double heater_run_time_accumulator = 132 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "heater_run_time_accumulator"
      subfields: "heater"
      subfields: "run"
      subfields: "time"
      subfields: "accumulator"
      point_type: "accumulator"
      measurement: "time"
      standard_unit: "seconds"
      expected_max: 1.25
    },
    (buf.validate.field).double.gte = 0.0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The heating percentage command.
  //
  // Composed of:
  //   heating (descriptor): Process, measured effect or requirement for
  //     heating.
  //   percentage (measurement): Measurement per hundred.
  //   command (point_type): The signal given to make an action happen.
  //     Defaults to multistate unless used alongside a measurement_type.
  //
  // Ontology field: `heating_percentage_command`, point type `command`.
  // Measured as percentage; values are in percent, the ontology's standard
  // unit for that quantity.
  //
  // Valid range 0 to 100, enforced by protovalidate.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double heating_percentage_command = 133 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "heating_percentage_command"
      subfields: "heating"
      subfields: "percentage"
      subfields: "command"
      point_type: "command"
      measurement: "percentage"
      standard_unit: "percent"
    },
    (buf.validate.field).double.gte = 0.0,
    (buf.validate.field).double.lte = 100.0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The heating percentage sensor.
  //
  // Composed of:
  //   heating (descriptor): Process, measured effect or requirement for
  //     heating.
  //   percentage (measurement): Measurement per hundred.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `heating_percentage_sensor`, point type `sensor`.
  // Measured as percentage; values are in percent, the ontology's standard
  // unit for that quantity.
  //
  // Valid range 0 to 100, enforced by protovalidate.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double heating_percentage_sensor = 134 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "heating_percentage_sensor"
      subfields: "heating"
      subfields: "percentage"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "percentage"
      standard_unit: "percent"
    },
    (buf.validate.field).double.gte = 0.0,
    (buf.validate.field).double.lte = 100.0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The heating request count.
  //
  // Composed of:
  //   heating (descriptor): Process, measured effect or requirement for
  //     heating.
  //   request (descriptor): A signal from a system device that calls for
  //     more of a particular conditioning process (e.g., pressure requests
  //     from terminal units).
  //   count (point_type): Total count of actions or requests.
  //
  // Ontology field: `heating_request_count`, point type `count`.
  // Dimensionless: the ontology defines this point type as an integer count
  // rather than a measured quantity.
  //
  // Valid range from 0, enforced by protovalidate. Expected range up to 250.
  // This is a data-quality expectation, not a constraint: a reading outside
  // it is a fault to report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional int64 heating_request_count = 135 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "heating_request_count"
      subfields: "heating"
      subfields: "request"
      subfields: "count"
      point_type: "count"
      expected_max: 250.0
    },
    (buf.validate.field).int64.gte = 0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The heating run command.
  //
  // Composed of:
  //   heating (descriptor): Process, measured effect or requirement for
  //     heating.
  //   run (measurement_descriptor): State of being active or operating. By
  //     default applies to control program for system (e.g., VAV program
  //     run_command).
  //   command (point_type): The signal given to make an action happen.
  //     Defaults to multistate unless used alongside a measurement_type.
  //
  // Ontology field: `heating_run_command`, point type `command`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional OffOnUnknownValue heating_run_command = 136 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "heating_run_command"
      subfields: "heating"
      subfields: "run"
      subfields: "command"
      point_type: "command"
    }
  ];

  // The heating stage run count.
  //
  // Composed of:
  //   heating (descriptor): Process, measured effect or requirement for
  //     heating.
  //   stage (descriptor): Discrete step (i.e., stage) of device activity
  //     (such as heating and cooling outputs).
  //   run (measurement_descriptor): State of being active or operating. By
  //     default applies to control program for system (e.g., VAV program
  //     run_command).
  //   count (point_type): Total count of actions or requests.
  //
  // Ontology field: `heating_stage_run_count`, point type `count`.
  // Dimensionless: the ontology defines this point type as an integer count
  // rather than a measured quantity.
  //
  // Valid range from 0, enforced by protovalidate. Expected range up to 10.
  // This is a data-quality expectation, not a constraint: a reading outside
  // it is a fault to report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional int64 heating_stage_run_count = 137 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "heating_stage_run_count"
      subfields: "heating"
      subfields: "stage"
      subfields: "run"
      subfields: "count"
      point_type: "count"
      expected_max: 10.0
    },
    (buf.validate.field).int64.gte = 0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The heating supply water isolation valve command.
  //
  // Composed of:
  //   heating (descriptor): Process, measured effect or requirement for
  //     heating.
  //   supply (descriptor): Measurement or process of media as it is
  //     supplied to the end-use equipment within the system.
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   isolation (descriptor): Process of isolating one component or
  //     process from another.
  //   valve (component): Component which meters the flow of water within a
  //     system or device.
  //   command (point_type): The signal given to make an action happen.
  //     Defaults to multistate unless used alongside a measurement_type.
  //
  // Ontology field: `heating_supply_water_isolation_valve_command`, point
  // type `command`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ClosedOpenValue heating_supply_water_isolation_valve_command = 138 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "heating_supply_water_isolation_valve_command"
      subfields: "heating"
      subfields: "supply"
      subfields: "water"
      subfields: "isolation"
      subfields: "valve"
      subfields: "command"
      point_type: "command"
    }
  ];

  // The heating supply water isolation valve status.
  //
  // Composed of:
  //   heating (descriptor): Process, measured effect or requirement for
  //     heating.
  //   supply (descriptor): Measurement or process of media as it is
  //     supplied to the end-use equipment within the system.
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   isolation (descriptor): Process of isolating one component or
  //     process from another.
  //   valve (component): Component which meters the flow of water within a
  //     system or device.
  //   status (point_type): The multistate value indicating an observed
  //     state in a piece of equipment, often indicating if a command was
  //     effected. It is a neutral observation (i.e., no quality judgment of
  //     'good' or 'bad'). It also has no units of measurement; therefore, if
  //     combined with a measurement subfield, it will indicate that the
  //     field is the directional status based on some measurement of that
  //     type (e.g., power_status equates to an on/off value based on some
  //     inference of power).
  //
  // Ontology field: `heating_supply_water_isolation_valve_status`, point
  // type `status`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ClosedOpenValue heating_supply_water_isolation_valve_status = 139 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "heating_supply_water_isolation_valve_status"
      subfields: "heating"
      subfields: "supply"
      subfields: "water"
      subfields: "isolation"
      subfields: "valve"
      subfields: "status"
      point_type: "status"
    }
  ];

  // The heating thermal power capacity.
  //
  // Composed of:
  //   heating (descriptor): Process, measured effect or requirement for
  //     heating.
  //   thermal (measurement_descriptor): Relating to the transfer of heat.
  //   power (measurement): Rate of energy consumption (assumed context is
  //     electrical and real, unless otherwise modified).
  //   capacity (point_type): A design parameter quantity (e.g., design
  //     motor power capacity). Is always a maximum limit.
  //
  // Ontology field: `heating_thermal_power_capacity`, point type `capacity`.
  // Measured as power; values are in watts, the ontology's standard unit for
  // that quantity.
  //
  // Valid range from 0, enforced by protovalidate. Expected range up to
  // 2.9307107e+06. This is a data-quality expectation, not a constraint: a
  // reading outside it is a fault to report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double heating_thermal_power_capacity = 140 [
    (google.api.field_behavior) = OPTIONAL,
    (google.api.field_behavior) = IMMUTABLE,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "heating_thermal_power_capacity"
      subfields: "heating"
      subfields: "thermal"
      subfields: "power"
      subfields: "capacity"
      point_type: "capacity"
      measurement: "power"
      standard_unit: "watts"
      expected_max: 2930710.7
    },
    (buf.validate.field).double.gte = 0.0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The heating water flowrate sensor.
  //
  // Composed of:
  //   heating (descriptor): Process, measured effect or requirement for
  //     heating.
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   flowrate (measurement): Rate of fluid movement.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `heating_water_flowrate_sensor`, point type `sensor`.
  // Measured as flowrate; values are in cubic_meters_per_second, the
  // ontology's standard unit for that quantity.
  //
  // Expected range -0.000315451 to 0.0315451. This is a data-quality
  // expectation, not a constraint: a reading outside it is a fault to
  // report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double heating_water_flowrate_sensor = 141 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "heating_water_flowrate_sensor"
      subfields: "heating"
      subfields: "water"
      subfields: "flowrate"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "flowrate"
      standard_unit: "cubic_meters_per_second"
      expected_min: -0.000315
      expected_max: 0.031545
    }
  ];

  // The heating water valve command.
  //
  // Composed of:
  //   heating (descriptor): Process, measured effect or requirement for
  //     heating.
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   valve (component): Component which meters the flow of water within a
  //     system or device.
  //   command (point_type): The signal given to make an action happen.
  //     Defaults to multistate unless used alongside a measurement_type.
  //
  // Ontology field: `heating_water_valve_command`, point type `command`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ClosedOpenValue heating_water_valve_command = 142 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "heating_water_valve_command"
      subfields: "heating"
      subfields: "water"
      subfields: "valve"
      subfields: "command"
      point_type: "command"
    }
  ];

  // The heating water valve percentage command.
  //
  // Composed of:
  //   heating (descriptor): Process, measured effect or requirement for
  //     heating.
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   valve (component): Component which meters the flow of water within a
  //     system or device.
  //   percentage (measurement): Measurement per hundred.
  //   command (point_type): The signal given to make an action happen.
  //     Defaults to multistate unless used alongside a measurement_type.
  //
  // Ontology field: `heating_water_valve_percentage_command`, point type
  // `command`. Measured as percentage; values are in percent, the ontology's
  // standard unit for that quantity.
  //
  // Valid range 0 to 100, enforced by protovalidate.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double heating_water_valve_percentage_command = 143 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "heating_water_valve_percentage_command"
      subfields: "heating"
      subfields: "water"
      subfields: "valve"
      subfields: "percentage"
      subfields: "command"
      point_type: "command"
      measurement: "percentage"
      standard_unit: "percent"
    },
    (buf.validate.field).double.gte = 0.0,
    (buf.validate.field).double.lte = 100.0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The heating water valve percentage sensor.
  //
  // Composed of:
  //   heating (descriptor): Process, measured effect or requirement for
  //     heating.
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   valve (component): Component which meters the flow of water within a
  //     system or device.
  //   percentage (measurement): Measurement per hundred.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `heating_water_valve_percentage_sensor`, point type
  // `sensor`. Measured as percentage; values are in percent, the ontology's
  // standard unit for that quantity.
  //
  // Valid range 0 to 100, enforced by protovalidate.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double heating_water_valve_percentage_sensor = 144 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "heating_water_valve_percentage_sensor"
      subfields: "heating"
      subfields: "water"
      subfields: "valve"
      subfields: "percentage"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "percentage"
      standard_unit: "percent"
    },
    (buf.validate.field).double.gte = 0.0,
    (buf.validate.field).double.lte = 100.0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The heating water valve status.
  //
  // Composed of:
  //   heating (descriptor): Process, measured effect or requirement for
  //     heating.
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   valve (component): Component which meters the flow of water within a
  //     system or device.
  //   status (point_type): The multistate value indicating an observed
  //     state in a piece of equipment, often indicating if a command was
  //     effected. It is a neutral observation (i.e., no quality judgment of
  //     'good' or 'bad'). It also has no units of measurement; therefore, if
  //     combined with a measurement subfield, it will indicate that the
  //     field is the directional status based on some measurement of that
  //     type (e.g., power_status equates to an on/off value based on some
  //     inference of power).
  //
  // Ontology field: `heating_water_valve_status`, point type `status`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ClosedOpenValue heating_water_valve_status = 145 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "heating_water_valve_status"
      subfields: "heating"
      subfields: "water"
      subfields: "valve"
      subfields: "status"
      point_type: "status"
    }
  ];

  // The high chilled return water temperature alarm.
  //
  // Composed of:
  //   high (descriptor): Level of control or measurement; above medium and
  //     low; opposite of low.
  //   chilled (descriptor): Cold water (typically 40 - 50F).
  //   return (descriptor): Measurement or process of media as it is
  //     returned from the end-use equipment within the system.
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `high_chilled_return_water_temperature_alarm`, point
  // type `alarm`. Measured as temperature; values are in kelvin, the
  // ontology's standard unit for that quantity.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue high_chilled_return_water_temperature_alarm = 146 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "high_chilled_return_water_temperature_alarm"
      subfields: "high"
      subfields: "chilled"
      subfields: "return"
      subfields: "water"
      subfields: "temperature"
      subfields: "alarm"
      point_type: "alarm"
      measurement: "temperature"
      standard_unit: "kelvin"
    }
  ];

  // The high chilled supply water temperature alarm.
  //
  // Composed of:
  //   high (descriptor): Level of control or measurement; above medium and
  //     low; opposite of low.
  //   chilled (descriptor): Cold water (typically 40 - 50F).
  //   supply (descriptor): Measurement or process of media as it is
  //     supplied to the end-use equipment within the system.
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `high_chilled_supply_water_temperature_alarm`, point
  // type `alarm`. Measured as temperature; values are in kelvin, the
  // ontology's standard unit for that quantity.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue high_chilled_supply_water_temperature_alarm = 147 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "high_chilled_supply_water_temperature_alarm"
      subfields: "high"
      subfields: "chilled"
      subfields: "supply"
      subfields: "water"
      subfields: "temperature"
      subfields: "alarm"
      point_type: "alarm"
      measurement: "temperature"
      standard_unit: "kelvin"
    }
  ];

  // The high cold aisle air temperature alarm.
  //
  // Composed of:
  //   high (descriptor): Level of control or measurement; above medium and
  //     low; opposite of low.
  //   cold (descriptor): Used to describe something at a low (or
  //     relatively low) temperature.
  //   aisle (descriptor): Passage between two rows of server racks.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `high_cold_aisle_air_temperature_alarm`, point type
  // `alarm`. Measured as temperature; values are in kelvin, the ontology's
  // standard unit for that quantity.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue high_cold_aisle_air_temperature_alarm = 148 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "high_cold_aisle_air_temperature_alarm"
      subfields: "high"
      subfields: "cold"
      subfields: "aisle"
      subfields: "air"
      subfields: "temperature"
      subfields: "alarm"
      point_type: "alarm"
      measurement: "temperature"
      standard_unit: "kelvin"
    }
  ];

  // The high condensing return water temperature alarm.
  //
  // Composed of:
  //   high (descriptor): Level of control or measurement; above medium and
  //     low; opposite of low.
  //   condensing (descriptor): The process of a gas (or vapor) changing to
  //     liquid.
  //   return (descriptor): Measurement or process of media as it is
  //     returned from the end-use equipment within the system.
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `high_condensing_return_water_temperature_alarm`, point
  // type `alarm`. Measured as temperature; values are in kelvin, the
  // ontology's standard unit for that quantity.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue high_condensing_return_water_temperature_alarm = 149 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "high_condensing_return_water_temperature_alarm"
      subfields: "high"
      subfields: "condensing"
      subfields: "return"
      subfields: "water"
      subfields: "temperature"
      subfields: "alarm"
      point_type: "alarm"
      measurement: "temperature"
      standard_unit: "kelvin"
    }
  ];

  // The high condensing supply water temperature alarm.
  //
  // Composed of:
  //   high (descriptor): Level of control or measurement; above medium and
  //     low; opposite of low.
  //   condensing (descriptor): The process of a gas (or vapor) changing to
  //     liquid.
  //   supply (descriptor): Measurement or process of media as it is
  //     supplied to the end-use equipment within the system.
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `high_condensing_supply_water_temperature_alarm`, point
  // type `alarm`. Measured as temperature; values are in kelvin, the
  // ontology's standard unit for that quantity.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue high_condensing_supply_water_temperature_alarm = 150 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "high_condensing_supply_water_temperature_alarm"
      subfields: "high"
      subfields: "condensing"
      subfields: "supply"
      subfields: "water"
      subfields: "temperature"
      subfields: "alarm"
      point_type: "alarm"
      measurement: "temperature"
      standard_unit: "kelvin"
    }
  ];

  // The high discharge air flowrate alarm.
  //
  // Composed of:
  //   high (descriptor): Level of control or measurement; above medium and
  //     low; opposite of low.
  //   discharge (descriptor): Media leaving system to enter ambient
  //     conditioned space. Typically applies only to air-side systems.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   flowrate (measurement): Rate of fluid movement.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `high_discharge_air_flowrate_alarm`, point type `alarm`.
  // Measured as flowrate; values are in cubic_meters_per_second, the
  // ontology's standard unit for that quantity.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue high_discharge_air_flowrate_alarm = 151 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "high_discharge_air_flowrate_alarm"
      subfields: "high"
      subfields: "discharge"
      subfields: "air"
      subfields: "flowrate"
      subfields: "alarm"
      point_type: "alarm"
      measurement: "flowrate"
      standard_unit: "cubic_meters_per_second"
    }
  ];

  // The high discharge air static pressure alarm.
  //
  // Composed of:
  //   high (descriptor): Level of control or measurement; above medium and
  //     low; opposite of low.
  //   discharge (descriptor): Media leaving system to enter ambient
  //     conditioned space. Typically applies only to air-side systems.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   static (measurement_descriptor): Describing a system in equilibrium
  //     or at rest (e.g., supply_air_static_pressure_sensor).
  //   pressure (measurement): Measured force per unit area within a system
  //     or process.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `high_discharge_air_static_pressure_alarm`, point type
  // `alarm`. Measured as pressure; values are in pascals, the ontology's
  // standard unit for that quantity.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue high_discharge_air_static_pressure_alarm = 152 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "high_discharge_air_static_pressure_alarm"
      subfields: "high"
      subfields: "discharge"
      subfields: "air"
      subfields: "static"
      subfields: "pressure"
      subfields: "alarm"
      point_type: "alarm"
      measurement: "pressure"
      standard_unit: "pascals"
    }
  ];

  // The high discharge air temperature alarm.
  //
  // Composed of:
  //   high (descriptor): Level of control or measurement; above medium and
  //     low; opposite of low.
  //   discharge (descriptor): Media leaving system to enter ambient
  //     conditioned space. Typically applies only to air-side systems.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `high_discharge_air_temperature_alarm`, point type
  // `alarm`. Measured as temperature; values are in kelvin, the ontology's
  // standard unit for that quantity.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue high_discharge_air_temperature_alarm = 153 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "high_discharge_air_temperature_alarm"
      subfields: "high"
      subfields: "discharge"
      subfields: "air"
      subfields: "temperature"
      subfields: "alarm"
      point_type: "alarm"
      measurement: "temperature"
      standard_unit: "kelvin"
    }
  ];

  // The high discharge fan speed command.
  //
  // Composed of:
  //   high (descriptor): Level of control or measurement; above medium and
  //     low; opposite of low.
  //   discharge (descriptor): Media leaving system to enter ambient
  //     conditioned space. Typically applies only to air-side systems.
  //   fan (component): Component used for the distribution of air.
  //   speed (measurement_descriptor): Numeric setting of how fast to run a
  //     device, in the specified units. Typically used to describe
  //     revolutions of a motor as a fraction of nominal or maximum.
  //   command (point_type): The signal given to make an action happen.
  //     Defaults to multistate unless used alongside a measurement_type.
  //
  // Ontology field: `high_discharge_fan_speed_command`, point type
  // `command`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional OffOnValue high_discharge_fan_speed_command = 154 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "high_discharge_fan_speed_command"
      subfields: "high"
      subfields: "discharge"
      subfields: "fan"
      subfields: "speed"
      subfields: "command"
      point_type: "command"
    }
  ];

  // The high hot aisle air temperature alarm.
  //
  // Composed of:
  //   high (descriptor): Level of control or measurement; above medium and
  //     low; opposite of low.
  //   hot (descriptor): Used to describe something at a high (or
  //     relatively high) temperature.
  //   aisle (descriptor): Passage between two rows of server racks.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `high_hot_aisle_air_temperature_alarm`, point type
  // `alarm`. Measured as temperature; values are in kelvin, the ontology's
  // standard unit for that quantity.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue high_hot_aisle_air_temperature_alarm = 155 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "high_hot_aisle_air_temperature_alarm"
      subfields: "high"
      subfields: "hot"
      subfields: "aisle"
      subfields: "air"
      subfields: "temperature"
      subfields: "alarm"
      point_type: "alarm"
      measurement: "temperature"
      standard_unit: "kelvin"
    }
  ];

  // The high leaving cooling coil temperature alarm.
  //
  // Composed of:
  //   high (descriptor): Level of control or measurement; above medium and
  //     low; opposite of low.
  //   leaving (descriptor): The process of exiting a thing or area where
  //     media leaves process (e.g., water leaving a heat exchanger).
  //   cooling (descriptor): Process, measured effect or requirement for
  //     cooling.
  //   coil (component): Component that exchanges heat between two media
  //     streams.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `high_leaving_cooling_coil_temperature_alarm`, point
  // type `alarm`. Measured as temperature; values are in kelvin, the
  // ontology's standard unit for that quantity.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue high_leaving_cooling_coil_temperature_alarm = 156 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "high_leaving_cooling_coil_temperature_alarm"
      subfields: "high"
      subfields: "leaving"
      subfields: "cooling"
      subfields: "coil"
      subfields: "temperature"
      subfields: "alarm"
      point_type: "alarm"
      measurement: "temperature"
      standard_unit: "kelvin"
    }
  ];

  // The high mixed air temperature alarm.
  //
  // Composed of:
  //   high (descriptor): Level of control or measurement; above medium and
  //     low; opposite of low.
  //   mixed (descriptor): Process or component used to mix multiple
  //     streams of air.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `high_mixed_air_temperature_alarm`, point type `alarm`.
  // Measured as temperature; values are in kelvin, the ontology's standard
  // unit for that quantity.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue high_mixed_air_temperature_alarm = 157 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "high_mixed_air_temperature_alarm"
      subfields: "high"
      subfields: "mixed"
      subfields: "air"
      subfields: "temperature"
      subfields: "alarm"
      point_type: "alarm"
      measurement: "temperature"
      standard_unit: "kelvin"
    }
  ];

  // The high return air relative humidity alarm.
  //
  // Composed of:
  //   high (descriptor): Level of control or measurement; above medium and
  //     low; opposite of low.
  //   return (descriptor): Measurement or process of media as it is
  //     returned from the end-use equipment within the system.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   relative (measurement_descriptor): Quality of media with respect to
  //     theoretical minimum or maximum value for a given condition (e.g.,
  //     relative humidity).
  //   humidity (measurement): Concentration of water vapor in air
  //     (conditioned or unconditioned).
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `high_return_air_relative_humidity_alarm`, point type
  // `alarm`. Measured as humidity; values are in percent_relative_humidity,
  // the ontology's standard unit for that quantity.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue high_return_air_relative_humidity_alarm = 158 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "high_return_air_relative_humidity_alarm"
      subfields: "high"
      subfields: "return"
      subfields: "air"
      subfields: "relative"
      subfields: "humidity"
      subfields: "alarm"
      point_type: "alarm"
      measurement: "humidity"
      standard_unit: "percent_relative_humidity"
    }
  ];

  // The high return air temperature alarm.
  //
  // Composed of:
  //   high (descriptor): Level of control or measurement; above medium and
  //     low; opposite of low.
  //   return (descriptor): Measurement or process of media as it is
  //     returned from the end-use equipment within the system.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `high_return_air_temperature_alarm`, point type `alarm`.
  // Measured as temperature; values are in kelvin, the ontology's standard
  // unit for that quantity.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue high_return_air_temperature_alarm = 159 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "high_return_air_temperature_alarm"
      subfields: "high"
      subfields: "return"
      subfields: "air"
      subfields: "temperature"
      subfields: "alarm"
      point_type: "alarm"
      measurement: "temperature"
      standard_unit: "kelvin"
    }
  ];

  // The high return water temperature alarm.
  //
  // Composed of:
  //   high (descriptor): Level of control or measurement; above medium and
  //     low; opposite of low.
  //   return (descriptor): Measurement or process of media as it is
  //     returned from the end-use equipment within the system.
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `high_return_water_temperature_alarm`, point type
  // `alarm`. Measured as temperature; values are in kelvin, the ontology's
  // standard unit for that quantity.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue high_return_water_temperature_alarm = 160 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "high_return_water_temperature_alarm"
      subfields: "high"
      subfields: "return"
      subfields: "water"
      subfields: "temperature"
      subfields: "alarm"
      point_type: "alarm"
      measurement: "temperature"
      standard_unit: "kelvin"
    }
  ];

  // The high supply air static pressure alarm.
  //
  // Composed of:
  //   high (descriptor): Level of control or measurement; above medium and
  //     low; opposite of low.
  //   supply (descriptor): Measurement or process of media as it is
  //     supplied to the end-use equipment within the system.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   static (measurement_descriptor): Describing a system in equilibrium
  //     or at rest (e.g., supply_air_static_pressure_sensor).
  //   pressure (measurement): Measured force per unit area within a system
  //     or process.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `high_supply_air_static_pressure_alarm`, point type
  // `alarm`. Measured as pressure; values are in pascals, the ontology's
  // standard unit for that quantity.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue high_supply_air_static_pressure_alarm = 161 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "high_supply_air_static_pressure_alarm"
      subfields: "high"
      subfields: "supply"
      subfields: "air"
      subfields: "static"
      subfields: "pressure"
      subfields: "alarm"
      point_type: "alarm"
      measurement: "pressure"
      standard_unit: "pascals"
    }
  ];

  // The high supply air temperature alarm.
  //
  // Composed of:
  //   high (descriptor): Level of control or measurement; above medium and
  //     low; opposite of low.
  //   supply (descriptor): Measurement or process of media as it is
  //     supplied to the end-use equipment within the system.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `high_supply_air_temperature_alarm`, point type `alarm`.
  // Measured as temperature; values are in kelvin, the ontology's standard
  // unit for that quantity.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue high_supply_air_temperature_alarm = 162 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "high_supply_air_temperature_alarm"
      subfields: "high"
      subfields: "supply"
      subfields: "air"
      subfields: "temperature"
      subfields: "alarm"
      point_type: "alarm"
      measurement: "temperature"
      standard_unit: "kelvin"
    }
  ];

  // The high supply water temperature alarm.
  //
  // Composed of:
  //   high (descriptor): Level of control or measurement; above medium and
  //     low; opposite of low.
  //   supply (descriptor): Measurement or process of media as it is
  //     supplied to the end-use equipment within the system.
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `high_supply_water_temperature_alarm`, point type
  // `alarm`. Measured as temperature; values are in kelvin, the ontology's
  // standard unit for that quantity.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue high_supply_water_temperature_alarm = 163 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "high_supply_water_temperature_alarm"
      subfields: "high"
      subfields: "supply"
      subfields: "water"
      subfields: "temperature"
      subfields: "alarm"
      point_type: "alarm"
      measurement: "temperature"
      standard_unit: "kelvin"
    }
  ];

  // The high zone air co2 concentration alarm.
  //
  // Composed of:
  //   high (descriptor): Level of control or measurement; above medium and
  //     low; opposite of low.
  //   zone (descriptor): Region of building which is conditioned.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   co2 (descriptor): Chemical formula and abbreviation for carbon
  //     dioxide.
  //   concentration (measurement): Concentration of something relative to
  //     something else (usually in parts per million or parts per billion).
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `high_zone_air_co2_concentration_alarm`, point type
  // `alarm`. Measured as concentration; values are in parts_per_unit, the
  // ontology's standard unit for that quantity.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue high_zone_air_co2_concentration_alarm = 325 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "high_zone_air_co2_concentration_alarm"
      subfields: "high"
      subfields: "zone"
      subfields: "air"
      subfields: "co2"
      subfields: "concentration"
      subfields: "alarm"
      point_type: "alarm"
      measurement: "concentration"
      standard_unit: "parts_per_unit"
    }
  ];

  // The high zone air relative humidity alarm.
  //
  // Composed of:
  //   high (descriptor): Level of control or measurement; above medium and
  //     low; opposite of low.
  //   zone (descriptor): Region of building which is conditioned.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   relative (measurement_descriptor): Quality of media with respect to
  //     theoretical minimum or maximum value for a given condition (e.g.,
  //     relative humidity).
  //   humidity (measurement): Concentration of water vapor in air
  //     (conditioned or unconditioned).
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `high_zone_air_relative_humidity_alarm`, point type
  // `alarm`. Measured as humidity; values are in percent_relative_humidity,
  // the ontology's standard unit for that quantity.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue high_zone_air_relative_humidity_alarm = 165 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "high_zone_air_relative_humidity_alarm"
      subfields: "high"
      subfields: "zone"
      subfields: "air"
      subfields: "relative"
      subfields: "humidity"
      subfields: "alarm"
      point_type: "alarm"
      measurement: "humidity"
      standard_unit: "percent_relative_humidity"
    }
  ];

  // The high zone air temperature alarm.
  //
  // Composed of:
  //   high (descriptor): Level of control or measurement; above medium and
  //     low; opposite of low.
  //   zone (descriptor): Region of building which is conditioned.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `high_zone_air_temperature_alarm`, point type `alarm`.
  // Measured as temperature; values are in kelvin, the ontology's standard
  // unit for that quantity.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue high_zone_air_temperature_alarm = 166 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "high_zone_air_temperature_alarm"
      subfields: "high"
      subfields: "zone"
      subfields: "air"
      subfields: "temperature"
      subfields: "alarm"
      point_type: "alarm"
      measurement: "temperature"
      standard_unit: "kelvin"
    }
  ];

  // The hot aisle air temperature sensor.
  //
  // Composed of:
  //   hot (descriptor): Used to describe something at a high (or
  //     relatively high) temperature.
  //   aisle (descriptor): Passage between two rows of server racks.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `hot_aisle_air_temperature_sensor`, point type `sensor`.
  // Measured as temperature; values are in kelvin, the ontology's standard
  // unit for that quantity.
  //
  // Expected range 255.9259259 to 322.037037. This is a data-quality
  // expectation, not a constraint: a reading outside it is a fault to
  // report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  repeated double hot_aisle_air_temperature_sensor = 167 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "hot_aisle_air_temperature_sensor"
      subfields: "hot"
      subfields: "aisle"
      subfields: "air"
      subfields: "temperature"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "temperature"
      standard_unit: "kelvin"
      enumerated: true
      expected_min: 255.925926
      expected_max: 322.037037
    }
  ];

  // The humidification percentage command.
  //
  // Composed of:
  //   humidification (descriptor): Process of adding moisture to air.
  //   percentage (measurement): Measurement per hundred.
  //   command (point_type): The signal given to make an action happen.
  //     Defaults to multistate unless used alongside a measurement_type.
  //
  // Ontology field: `humidification_percentage_command`, point type
  // `command`. Measured as percentage; values are in percent, the ontology's
  // standard unit for that quantity.
  //
  // Valid range 0 to 100, enforced by protovalidate.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  repeated double humidification_percentage_command = 168 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "humidification_percentage_command"
      subfields: "humidification"
      subfields: "percentage"
      subfields: "command"
      point_type: "command"
      measurement: "percentage"
      standard_unit: "percent"
      enumerated: true
    }
  ];

  // The humidification run command.
  //
  // Composed of:
  //   humidification (descriptor): Process of adding moisture to air.
  //   run (measurement_descriptor): State of being active or operating. By
  //     default applies to control program for system (e.g., VAV program
  //     run_command).
  //   command (point_type): The signal given to make an action happen.
  //     Defaults to multistate unless used alongside a measurement_type.
  //
  // Ontology field: `humidification_run_command`, point type `command`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  repeated OffOnValue humidification_run_command = 169 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "humidification_run_command"
      subfields: "humidification"
      subfields: "run"
      subfields: "command"
      point_type: "command"
      enumerated: true
    }
  ];

  // The humidification run status.
  //
  // Composed of:
  //   humidification (descriptor): Process of adding moisture to air.
  //   run (measurement_descriptor): State of being active or operating. By
  //     default applies to control program for system (e.g., VAV program
  //     run_command).
  //   status (point_type): The multistate value indicating an observed
  //     state in a piece of equipment, often indicating if a command was
  //     effected. It is a neutral observation (i.e., no quality judgment of
  //     'good' or 'bad'). It also has no units of measurement; therefore, if
  //     combined with a measurement subfield, it will indicate that the
  //     field is the directional status based on some measurement of that
  //     type (e.g., power_status equates to an on/off value based on some
  //     inference of power).
  //
  // Ontology field: `humidification_run_status`, point type `status`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  repeated OffOnValue humidification_run_status = 170 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "humidification_run_status"
      subfields: "humidification"
      subfields: "run"
      subfields: "status"
      point_type: "status"
      enumerated: true
    }
  ];

  // The humidifier run time accumulator.
  //
  // Composed of:
  //   humidifier (component): Component which humidifies (typically by
  //     adding water vapor to the air).
  //   run (measurement_descriptor): State of being active or operating. By
  //     default applies to control program for system (e.g., VAV program
  //     run_command).
  //   time (measurement): Defined duration of something.
  //   accumulator (point_type): The total accumulated quantity (e.g.,
  //     total energy accumulated).
  //
  // Ontology field: `humidifier_run_time_accumulator`, point type
  // `accumulator`. Measured as time; values are in seconds, the ontology's
  // standard unit for that quantity.
  //
  // Valid range from 0, enforced by protovalidate. Expected range up to
  // 1.25. This is a data-quality expectation, not a constraint: a reading
  // outside it is a fault to report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double humidifier_run_time_accumulator = 171 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "humidifier_run_time_accumulator"
      subfields: "humidifier"
      subfields: "run"
      subfields: "time"
      subfields: "accumulator"
      point_type: "accumulator"
      measurement: "time"
      standard_unit: "seconds"
      expected_max: 1.25
    },
    (buf.validate.field).double.gte = 0.0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The isolation damper status.
  //
  // Composed of:
  //   isolation (descriptor): Process of isolating one component or
  //     process from another.
  //   damper (component): Component which meters the flow of air within a
  //     system or device.
  //   status (point_type): The multistate value indicating an observed
  //     state in a piece of equipment, often indicating if a command was
  //     effected. It is a neutral observation (i.e., no quality judgment of
  //     'good' or 'bad'). It also has no units of measurement; therefore, if
  //     combined with a measurement subfield, it will indicate that the
  //     field is the directional status based on some measurement of that
  //     type (e.g., power_status equates to an on/off value based on some
  //     inference of power).
  //
  // Ontology field: `isolation_damper_status`, point type `status`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ClosedOpenValue isolation_damper_status = 172 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "isolation_damper_status"
      subfields: "isolation"
      subfields: "damper"
      subfields: "status"
      point_type: "status"
    }
  ];

  // The leaving cooling coil temperature sensor.
  //
  // Composed of:
  //   leaving (descriptor): The process of exiting a thing or area where
  //     media leaves process (e.g., water leaving a heat exchanger).
  //   cooling (descriptor): Process, measured effect or requirement for
  //     cooling.
  //   coil (component): Component that exchanges heat between two media
  //     streams.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `leaving_cooling_coil_temperature_sensor`, point type
  // `sensor`. Measured as temperature; values are in kelvin, the ontology's
  // standard unit for that quantity.
  //
  // Expected range 255.9259259 to 338.7037037. This is a data-quality
  // expectation, not a constraint: a reading outside it is a fault to
  // report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double leaving_cooling_coil_temperature_sensor = 173 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "leaving_cooling_coil_temperature_sensor"
      subfields: "leaving"
      subfields: "cooling"
      subfields: "coil"
      subfields: "temperature"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "temperature"
      standard_unit: "kelvin"
      expected_min: 255.925926
      expected_max: 338.703704
    }
  ];

  // The leaving heating coil temperature sensor.
  //
  // Composed of:
  //   leaving (descriptor): The process of exiting a thing or area where
  //     media leaves process (e.g., water leaving a heat exchanger).
  //   heating (descriptor): Process, measured effect or requirement for
  //     heating.
  //   coil (component): Component that exchanges heat between two media
  //     streams.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `leaving_heating_coil_temperature_sensor`, point type
  // `sensor`. Measured as temperature; values are in kelvin, the ontology's
  // standard unit for that quantity.
  //
  // Expected range 255.9259259 to 338.7037037. This is a data-quality
  // expectation, not a constraint: a reading outside it is a fault to
  // report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double leaving_heating_coil_temperature_sensor = 174 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "leaving_heating_coil_temperature_sensor"
      subfields: "leaving"
      subfields: "heating"
      subfields: "coil"
      subfields: "temperature"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "temperature"
      standard_unit: "kelvin"
      expected_min: 255.925926
      expected_max: 338.703704
    }
  ];

  // The load percentage sensor.
  //
  // Composed of:
  //   load (descriptor): Electrical load which indicates the consumption
  //     of electrical power.
  //   percentage (measurement): Measurement per hundred.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `load_percentage_sensor`, point type `sensor`. Measured
  // as percentage; values are in percent, the ontology's standard unit for
  // that quantity.
  //
  // Valid range 0 to 100, enforced by protovalidate.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double load_percentage_sensor = 175 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "load_percentage_sensor"
      subfields: "load"
      subfields: "percentage"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "percentage"
      standard_unit: "percent"
    },
    (buf.validate.field).double.gte = 0.0,
    (buf.validate.field).double.lte = 100.0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The local override alarm.
  //
  // Composed of:
  //   local (descriptor): A definite spatial location involving only a
  //     subset of the larger surrounding area.
  //   override (descriptor): An action that interrupts, cancels, or
  //     changes the current action or status.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `local_override_alarm`, point type `alarm`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue local_override_alarm = 176 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "local_override_alarm"
      subfields: "local"
      subfields: "override"
      subfields: "alarm"
      point_type: "alarm"
    }
  ];

  // The local override compressor alarm.
  //
  // Composed of:
  //   local (descriptor): A definite spatial location involving only a
  //     subset of the larger surrounding area.
  //   override (descriptor): An action that interrupts, cancels, or
  //     changes the current action or status.
  //   compressor (component): Component which drives refrigerant
  //     compression (and thus cooling processes) within a device or system.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `local_override_compressor_alarm`, point type `alarm`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue local_override_compressor_alarm = 177 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "local_override_compressor_alarm"
      subfields: "local"
      subfields: "override"
      subfields: "compressor"
      subfields: "alarm"
      point_type: "alarm"
    }
  ];

  // The local override discharge air isolation damper alarm.
  //
  // Composed of:
  //   local (descriptor): A definite spatial location involving only a
  //     subset of the larger surrounding area.
  //   override (descriptor): An action that interrupts, cancels, or
  //     changes the current action or status.
  //   discharge (descriptor): Media leaving system to enter ambient
  //     conditioned space. Typically applies only to air-side systems.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   isolation (descriptor): Process of isolating one component or
  //     process from another.
  //   damper (component): Component which meters the flow of air within a
  //     system or device.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `local_override_discharge_air_isolation_damper_alarm`,
  // point type `alarm`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue local_override_discharge_air_isolation_damper_alarm = 178 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "local_override_discharge_air_isolation_damper_alarm"
      subfields: "local"
      subfields: "override"
      subfields: "discharge"
      subfields: "air"
      subfields: "isolation"
      subfields: "damper"
      subfields: "alarm"
      point_type: "alarm"
    }
  ];

  // The lost power alarm.
  //
  // Composed of:
  //   lost (measurement_descriptor): Indicates that a point has lost some
  //     quality or quantity inadvertently.
  //   power (measurement): Rate of energy consumption (assumed context is
  //     electrical and real, unless otherwise modified).
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `lost_power_alarm`, point type `alarm`. Measured as
  // power; values are in watts, the ontology's standard unit for that
  // quantity.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue lost_power_alarm = 179 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "lost_power_alarm"
      subfields: "lost"
      subfields: "power"
      subfields: "alarm"
      point_type: "alarm"
      measurement: "power"
      standard_unit: "watts"
    }
  ];

  // The low chilled return water temperature alarm.
  //
  // Composed of:
  //   low (descriptor): Level of control or measurement; below medium and
  //     high; opposite of high.
  //   chilled (descriptor): Cold water (typically 40 - 50F).
  //   return (descriptor): Measurement or process of media as it is
  //     returned from the end-use equipment within the system.
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `low_chilled_return_water_temperature_alarm`, point type
  // `alarm`. Measured as temperature; values are in kelvin, the ontology's
  // standard unit for that quantity.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue low_chilled_return_water_temperature_alarm = 180 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "low_chilled_return_water_temperature_alarm"
      subfields: "low"
      subfields: "chilled"
      subfields: "return"
      subfields: "water"
      subfields: "temperature"
      subfields: "alarm"
      point_type: "alarm"
      measurement: "temperature"
      standard_unit: "kelvin"
    }
  ];

  // The low chilled supply water temperature alarm.
  //
  // Composed of:
  //   low (descriptor): Level of control or measurement; below medium and
  //     high; opposite of high.
  //   chilled (descriptor): Cold water (typically 40 - 50F).
  //   supply (descriptor): Measurement or process of media as it is
  //     supplied to the end-use equipment within the system.
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `low_chilled_supply_water_temperature_alarm`, point type
  // `alarm`. Measured as temperature; values are in kelvin, the ontology's
  // standard unit for that quantity.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue low_chilled_supply_water_temperature_alarm = 181 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "low_chilled_supply_water_temperature_alarm"
      subfields: "low"
      subfields: "chilled"
      subfields: "supply"
      subfields: "water"
      subfields: "temperature"
      subfields: "alarm"
      point_type: "alarm"
      measurement: "temperature"
      standard_unit: "kelvin"
    }
  ];

  // The low cold aisle air temperature alarm.
  //
  // Composed of:
  //   low (descriptor): Level of control or measurement; below medium and
  //     high; opposite of high.
  //   cold (descriptor): Used to describe something at a low (or
  //     relatively low) temperature.
  //   aisle (descriptor): Passage between two rows of server racks.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `low_cold_aisle_air_temperature_alarm`, point type
  // `alarm`. Measured as temperature; values are in kelvin, the ontology's
  // standard unit for that quantity.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue low_cold_aisle_air_temperature_alarm = 182 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "low_cold_aisle_air_temperature_alarm"
      subfields: "low"
      subfields: "cold"
      subfields: "aisle"
      subfields: "air"
      subfields: "temperature"
      subfields: "alarm"
      point_type: "alarm"
      measurement: "temperature"
      standard_unit: "kelvin"
    }
  ];

  // The low condensing return water temperature alarm.
  //
  // Composed of:
  //   low (descriptor): Level of control or measurement; below medium and
  //     high; opposite of high.
  //   condensing (descriptor): The process of a gas (or vapor) changing to
  //     liquid.
  //   return (descriptor): Measurement or process of media as it is
  //     returned from the end-use equipment within the system.
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `low_condensing_return_water_temperature_alarm`, point
  // type `alarm`. Measured as temperature; values are in kelvin, the
  // ontology's standard unit for that quantity.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue low_condensing_return_water_temperature_alarm = 183 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "low_condensing_return_water_temperature_alarm"
      subfields: "low"
      subfields: "condensing"
      subfields: "return"
      subfields: "water"
      subfields: "temperature"
      subfields: "alarm"
      point_type: "alarm"
      measurement: "temperature"
      standard_unit: "kelvin"
    }
  ];

  // The low condensing supply water temperature alarm.
  //
  // Composed of:
  //   low (descriptor): Level of control or measurement; below medium and
  //     high; opposite of high.
  //   condensing (descriptor): The process of a gas (or vapor) changing to
  //     liquid.
  //   supply (descriptor): Measurement or process of media as it is
  //     supplied to the end-use equipment within the system.
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `low_condensing_supply_water_temperature_alarm`, point
  // type `alarm`. Measured as temperature; values are in kelvin, the
  // ontology's standard unit for that quantity.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue low_condensing_supply_water_temperature_alarm = 184 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "low_condensing_supply_water_temperature_alarm"
      subfields: "low"
      subfields: "condensing"
      subfields: "supply"
      subfields: "water"
      subfields: "temperature"
      subfields: "alarm"
      point_type: "alarm"
      measurement: "temperature"
      standard_unit: "kelvin"
    }
  ];

  // The low discharge air flowrate alarm.
  //
  // Composed of:
  //   low (descriptor): Level of control or measurement; below medium and
  //     high; opposite of high.
  //   discharge (descriptor): Media leaving system to enter ambient
  //     conditioned space. Typically applies only to air-side systems.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   flowrate (measurement): Rate of fluid movement.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `low_discharge_air_flowrate_alarm`, point type `alarm`.
  // Measured as flowrate; values are in cubic_meters_per_second, the
  // ontology's standard unit for that quantity.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue low_discharge_air_flowrate_alarm = 185 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "low_discharge_air_flowrate_alarm"
      subfields: "low"
      subfields: "discharge"
      subfields: "air"
      subfields: "flowrate"
      subfields: "alarm"
      point_type: "alarm"
      measurement: "flowrate"
      standard_unit: "cubic_meters_per_second"
    }
  ];

  // The low discharge air static pressure alarm.
  //
  // Composed of:
  //   low (descriptor): Level of control or measurement; below medium and
  //     high; opposite of high.
  //   discharge (descriptor): Media leaving system to enter ambient
  //     conditioned space. Typically applies only to air-side systems.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   static (measurement_descriptor): Describing a system in equilibrium
  //     or at rest (e.g., supply_air_static_pressure_sensor).
  //   pressure (measurement): Measured force per unit area within a system
  //     or process.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `low_discharge_air_static_pressure_alarm`, point type
  // `alarm`. Measured as pressure; values are in pascals, the ontology's
  // standard unit for that quantity.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue low_discharge_air_static_pressure_alarm = 186 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "low_discharge_air_static_pressure_alarm"
      subfields: "low"
      subfields: "discharge"
      subfields: "air"
      subfields: "static"
      subfields: "pressure"
      subfields: "alarm"
      point_type: "alarm"
      measurement: "pressure"
      standard_unit: "pascals"
    }
  ];

  // The low discharge air temperature alarm.
  //
  // Composed of:
  //   low (descriptor): Level of control or measurement; below medium and
  //     high; opposite of high.
  //   discharge (descriptor): Media leaving system to enter ambient
  //     conditioned space. Typically applies only to air-side systems.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `low_discharge_air_temperature_alarm`, point type
  // `alarm`. Measured as temperature; values are in kelvin, the ontology's
  // standard unit for that quantity.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue low_discharge_air_temperature_alarm = 187 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "low_discharge_air_temperature_alarm"
      subfields: "low"
      subfields: "discharge"
      subfields: "air"
      subfields: "temperature"
      subfields: "alarm"
      point_type: "alarm"
      measurement: "temperature"
      standard_unit: "kelvin"
    }
  ];

  // The low discharge fan speed command.
  //
  // Composed of:
  //   low (descriptor): Level of control or measurement; below medium and
  //     high; opposite of high.
  //   discharge (descriptor): Media leaving system to enter ambient
  //     conditioned space. Typically applies only to air-side systems.
  //   fan (component): Component used for the distribution of air.
  //   speed (measurement_descriptor): Numeric setting of how fast to run a
  //     device, in the specified units. Typically used to describe
  //     revolutions of a motor as a fraction of nominal or maximum.
  //   command (point_type): The signal given to make an action happen.
  //     Defaults to multistate unless used alongside a measurement_type.
  //
  // Ontology field: `low_discharge_fan_speed_command`, point type `command`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional OffOnValue low_discharge_fan_speed_command = 188 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "low_discharge_fan_speed_command"
      subfields: "low"
      subfields: "discharge"
      subfields: "fan"
      subfields: "speed"
      subfields: "command"
      point_type: "command"
    }
  ];

  // The low hot aisle air temperature alarm.
  //
  // Composed of:
  //   low (descriptor): Level of control or measurement; below medium and
  //     high; opposite of high.
  //   hot (descriptor): Used to describe something at a high (or
  //     relatively high) temperature.
  //   aisle (descriptor): Passage between two rows of server racks.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `low_hot_aisle_air_temperature_alarm`, point type
  // `alarm`. Measured as temperature; values are in kelvin, the ontology's
  // standard unit for that quantity.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue low_hot_aisle_air_temperature_alarm = 189 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "low_hot_aisle_air_temperature_alarm"
      subfields: "low"
      subfields: "hot"
      subfields: "aisle"
      subfields: "air"
      subfields: "temperature"
      subfields: "alarm"
      point_type: "alarm"
      measurement: "temperature"
      standard_unit: "kelvin"
    }
  ];

  // The low leaving cooling coil temperature alarm.
  //
  // Composed of:
  //   low (descriptor): Level of control or measurement; below medium and
  //     high; opposite of high.
  //   leaving (descriptor): The process of exiting a thing or area where
  //     media leaves process (e.g., water leaving a heat exchanger).
  //   cooling (descriptor): Process, measured effect or requirement for
  //     cooling.
  //   coil (component): Component that exchanges heat between two media
  //     streams.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `low_leaving_cooling_coil_temperature_alarm`, point type
  // `alarm`. Measured as temperature; values are in kelvin, the ontology's
  // standard unit for that quantity.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue low_leaving_cooling_coil_temperature_alarm = 190 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "low_leaving_cooling_coil_temperature_alarm"
      subfields: "low"
      subfields: "leaving"
      subfields: "cooling"
      subfields: "coil"
      subfields: "temperature"
      subfields: "alarm"
      point_type: "alarm"
      measurement: "temperature"
      standard_unit: "kelvin"
    }
  ];

  // The low mixed air temperature alarm.
  //
  // Composed of:
  //   low (descriptor): Level of control or measurement; below medium and
  //     high; opposite of high.
  //   mixed (descriptor): Process or component used to mix multiple
  //     streams of air.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `low_mixed_air_temperature_alarm`, point type `alarm`.
  // Measured as temperature; values are in kelvin, the ontology's standard
  // unit for that quantity.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue low_mixed_air_temperature_alarm = 191 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "low_mixed_air_temperature_alarm"
      subfields: "low"
      subfields: "mixed"
      subfields: "air"
      subfields: "temperature"
      subfields: "alarm"
      point_type: "alarm"
      measurement: "temperature"
      standard_unit: "kelvin"
    }
  ];

  // The low return air relative humidity alarm.
  //
  // Composed of:
  //   low (descriptor): Level of control or measurement; below medium and
  //     high; opposite of high.
  //   return (descriptor): Measurement or process of media as it is
  //     returned from the end-use equipment within the system.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   relative (measurement_descriptor): Quality of media with respect to
  //     theoretical minimum or maximum value for a given condition (e.g.,
  //     relative humidity).
  //   humidity (measurement): Concentration of water vapor in air
  //     (conditioned or unconditioned).
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `low_return_air_relative_humidity_alarm`, point type
  // `alarm`. Measured as humidity; values are in percent_relative_humidity,
  // the ontology's standard unit for that quantity.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue low_return_air_relative_humidity_alarm = 192 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "low_return_air_relative_humidity_alarm"
      subfields: "low"
      subfields: "return"
      subfields: "air"
      subfields: "relative"
      subfields: "humidity"
      subfields: "alarm"
      point_type: "alarm"
      measurement: "humidity"
      standard_unit: "percent_relative_humidity"
    }
  ];

  // The low return air temperature alarm.
  //
  // Composed of:
  //   low (descriptor): Level of control or measurement; below medium and
  //     high; opposite of high.
  //   return (descriptor): Measurement or process of media as it is
  //     returned from the end-use equipment within the system.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `low_return_air_temperature_alarm`, point type `alarm`.
  // Measured as temperature; values are in kelvin, the ontology's standard
  // unit for that quantity.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue low_return_air_temperature_alarm = 193 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "low_return_air_temperature_alarm"
      subfields: "low"
      subfields: "return"
      subfields: "air"
      subfields: "temperature"
      subfields: "alarm"
      point_type: "alarm"
      measurement: "temperature"
      standard_unit: "kelvin"
    }
  ];

  // The low return water temperature alarm.
  //
  // Composed of:
  //   low (descriptor): Level of control or measurement; below medium and
  //     high; opposite of high.
  //   return (descriptor): Measurement or process of media as it is
  //     returned from the end-use equipment within the system.
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `low_return_water_temperature_alarm`, point type
  // `alarm`. Measured as temperature; values are in kelvin, the ontology's
  // standard unit for that quantity.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue low_return_water_temperature_alarm = 194 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "low_return_water_temperature_alarm"
      subfields: "low"
      subfields: "return"
      subfields: "water"
      subfields: "temperature"
      subfields: "alarm"
      point_type: "alarm"
      measurement: "temperature"
      standard_unit: "kelvin"
    }
  ];

  // The low supply air static pressure alarm.
  //
  // Composed of:
  //   low (descriptor): Level of control or measurement; below medium and
  //     high; opposite of high.
  //   supply (descriptor): Measurement or process of media as it is
  //     supplied to the end-use equipment within the system.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   static (measurement_descriptor): Describing a system in equilibrium
  //     or at rest (e.g., supply_air_static_pressure_sensor).
  //   pressure (measurement): Measured force per unit area within a system
  //     or process.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `low_supply_air_static_pressure_alarm`, point type
  // `alarm`. Measured as pressure; values are in pascals, the ontology's
  // standard unit for that quantity.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue low_supply_air_static_pressure_alarm = 195 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "low_supply_air_static_pressure_alarm"
      subfields: "low"
      subfields: "supply"
      subfields: "air"
      subfields: "static"
      subfields: "pressure"
      subfields: "alarm"
      point_type: "alarm"
      measurement: "pressure"
      standard_unit: "pascals"
    }
  ];

  // The low supply air temperature alarm.
  //
  // Composed of:
  //   low (descriptor): Level of control or measurement; below medium and
  //     high; opposite of high.
  //   supply (descriptor): Measurement or process of media as it is
  //     supplied to the end-use equipment within the system.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `low_supply_air_temperature_alarm`, point type `alarm`.
  // Measured as temperature; values are in kelvin, the ontology's standard
  // unit for that quantity.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue low_supply_air_temperature_alarm = 196 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "low_supply_air_temperature_alarm"
      subfields: "low"
      subfields: "supply"
      subfields: "air"
      subfields: "temperature"
      subfields: "alarm"
      point_type: "alarm"
      measurement: "temperature"
      standard_unit: "kelvin"
    }
  ];

  // The low supply water temperature alarm.
  //
  // Composed of:
  //   low (descriptor): Level of control or measurement; below medium and
  //     high; opposite of high.
  //   supply (descriptor): Measurement or process of media as it is
  //     supplied to the end-use equipment within the system.
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `low_supply_water_temperature_alarm`, point type
  // `alarm`. Measured as temperature; values are in kelvin, the ontology's
  // standard unit for that quantity.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue low_supply_water_temperature_alarm = 197 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "low_supply_water_temperature_alarm"
      subfields: "low"
      subfields: "supply"
      subfields: "water"
      subfields: "temperature"
      subfields: "alarm"
      point_type: "alarm"
      measurement: "temperature"
      standard_unit: "kelvin"
    }
  ];

  // The low zone air relative humidity alarm.
  //
  // Composed of:
  //   low (descriptor): Level of control or measurement; below medium and
  //     high; opposite of high.
  //   zone (descriptor): Region of building which is conditioned.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   relative (measurement_descriptor): Quality of media with respect to
  //     theoretical minimum or maximum value for a given condition (e.g.,
  //     relative humidity).
  //   humidity (measurement): Concentration of water vapor in air
  //     (conditioned or unconditioned).
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `low_zone_air_relative_humidity_alarm`, point type
  // `alarm`. Measured as humidity; values are in percent_relative_humidity,
  // the ontology's standard unit for that quantity.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue low_zone_air_relative_humidity_alarm = 198 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "low_zone_air_relative_humidity_alarm"
      subfields: "low"
      subfields: "zone"
      subfields: "air"
      subfields: "relative"
      subfields: "humidity"
      subfields: "alarm"
      point_type: "alarm"
      measurement: "humidity"
      standard_unit: "percent_relative_humidity"
    }
  ];

  // The low zone air temperature alarm.
  //
  // Composed of:
  //   low (descriptor): Level of control or measurement; below medium and
  //     high; opposite of high.
  //   zone (descriptor): Region of building which is conditioned.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `low_zone_air_temperature_alarm`, point type `alarm`.
  // Measured as temperature; values are in kelvin, the ontology's standard
  // unit for that quantity.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue low_zone_air_temperature_alarm = 199 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "low_zone_air_temperature_alarm"
      subfields: "low"
      subfields: "zone"
      subfields: "air"
      subfields: "temperature"
      subfields: "alarm"
      point_type: "alarm"
      measurement: "temperature"
      standard_unit: "kelvin"
    }
  ];

  // The manufacturer label.
  //
  // Composed of:
  //   manufacturer (descriptor): The company which produces the asset or
  //     device.
  //   label (point_type): Identifying alias for component or system.
  //
  // Ontology field: `manufacturer_label`, point type `label`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional string manufacturer_label = 200 [
    (google.api.field_behavior) = OPTIONAL,
    (google.api.field_behavior) = IMMUTABLE,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "manufacturer_label"
      subfields: "manufacturer"
      subfields: "label"
      point_type: "label"
    }
  ];

  // The master alarm.
  //
  // Composed of:
  //   master (descriptor): Serving as the highest-level point of
  //     consolidation that unites individual devices, sensors, alarms, etc.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `master_alarm`, point type `alarm`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue master_alarm = 201 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "master_alarm"
      subfields: "master"
      subfields: "alarm"
      point_type: "alarm"
    }
  ];

  // The medium discharge fan speed command.
  //
  // Composed of:
  //   medium (descriptor): Level of control or measurement; between high
  //     and low.
  //   discharge (descriptor): Media leaving system to enter ambient
  //     conditioned space. Typically applies only to air-side systems.
  //   fan (component): Component used for the distribution of air.
  //   speed (measurement_descriptor): Numeric setting of how fast to run a
  //     device, in the specified units. Typically used to describe
  //     revolutions of a motor as a fraction of nominal or maximum.
  //   command (point_type): The signal given to make an action happen.
  //     Defaults to multistate unless used alongside a measurement_type.
  //
  // Ontology field: `medium_discharge_fan_speed_command`, point type
  // `command`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional OffOnValue medium_discharge_fan_speed_command = 202 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "medium_discharge_fan_speed_command"
      subfields: "medium"
      subfields: "discharge"
      subfields: "fan"
      subfields: "speed"
      subfields: "command"
      point_type: "command"
    }
  ];

  // The mixed air temperature sensor.
  //
  // Composed of:
  //   mixed (descriptor): Process or component used to mix multiple
  //     streams of air.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `mixed_air_temperature_sensor`, point type `sensor`.
  // Measured as temperature; values are in kelvin, the ontology's standard
  // unit for that quantity.
  //
  // Expected range 255.9259259 to 338.7037037. This is a data-quality
  // expectation, not a constraint: a reading outside it is a fault to
  // report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double mixed_air_temperature_sensor = 203 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "mixed_air_temperature_sensor"
      subfields: "mixed"
      subfields: "air"
      subfields: "temperature"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "temperature"
      standard_unit: "kelvin"
      expected_min: 255.925926
      expected_max: 338.703704
    }
  ];

  // The mixed air temperature setpoint.
  //
  // Composed of:
  //   mixed (descriptor): Process or component used to mix multiple
  //     streams of air.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   setpoint (point_type): Control target of process or system.
  //
  // Ontology field: `mixed_air_temperature_setpoint`, point type `setpoint`.
  // Measured as temperature; values are in kelvin, the ontology's standard
  // unit for that quantity.
  //
  // Expected range 255.9259259 to 338.7037037. This is a data-quality
  // expectation, not a constraint: a reading outside it is a fault to
  // report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double mixed_air_temperature_setpoint = 204 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "mixed_air_temperature_setpoint"
      subfields: "mixed"
      subfields: "air"
      subfields: "temperature"
      subfields: "setpoint"
      point_type: "setpoint"
      measurement: "temperature"
      standard_unit: "kelvin"
      expected_min: 255.925926
      expected_max: 338.703704
    }
  ];

  // The model label.
  //
  // Composed of:
  //   model (descriptor): Particular design version of an asset (e.g.
  //     model_label).
  //   label (point_type): Identifying alias for component or system.
  //
  // Ontology field: `model_label`, point type `label`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional string model_label = 205 [
    (google.api.field_behavior) = OPTIONAL,
    (google.api.field_behavior) = IMMUTABLE,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "model_label"
      subfields: "model"
      subfields: "label"
      point_type: "label"
    }
  ];

  // The optimal stop command.
  //
  // Composed of:
  //   optimal (descriptor): A method or solution being the most desirable
  //     or effective of all possibilities. Typically used in 'optimal
  //     start/stop,' referring to the most efficient point in time at which
  //     a system can turn off or on, while maintaining desired conditions
  //     and avoiding excess energy consumption or equipment wear.
  //   stop (measurement_descriptor): Indicates a point in time or space at
  //     which something has its ending.
  //   command (point_type): The signal given to make an action happen.
  //     Defaults to multistate unless used alongside a measurement_type.
  //
  // Ontology field: `optimal_stop_command`, point type `command`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional OffOnValue optimal_stop_command = 206 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "optimal_stop_command"
      subfields: "optimal"
      subfields: "stop"
      subfields: "command"
      point_type: "command"
    }
  ];

  // The outside air aqi sensor.
  //
  // Composed of:
  //   outside (descriptor): Process or measurement of local atmospheric
  //     conditions.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   aqi (measurement): Abbreviation for air quality index, which is a
  //     set of special standard gradings for the pollution in air.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `outside_air_aqi_sensor`, point type `sensor`. Measured
  // as aqi; values are in epa, the ontology's standard unit for that
  // quantity.
  //
  // Valid range 0 to 500, enforced by protovalidate.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double outside_air_aqi_sensor = 207 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "outside_air_aqi_sensor"
      subfields: "outside"
      subfields: "air"
      subfields: "aqi"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "aqi"
      standard_unit: "epa"
    },
    (buf.validate.field).double.gte = 0.0,
    (buf.validate.field).double.lte = 500.0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The outside air co2 concentration sensor.
  //
  // Composed of:
  //   outside (descriptor): Process or measurement of local atmospheric
  //     conditions.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   co2 (descriptor): Chemical formula and abbreviation for carbon
  //     dioxide.
  //   concentration (measurement): Concentration of something relative to
  //     something else (usually in parts per million or parts per billion).
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `outside_air_co2_concentration_sensor`, point type
  // `sensor`. Measured as concentration; values are in parts_per_unit, the
  // ontology's standard unit for that quantity.
  //
  // Expected range 5e-05 to 0.005. This is a data-quality expectation, not a
  // constraint: a reading outside it is a fault to report, not a message to
  // reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double outside_air_co2_concentration_sensor = 326 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "outside_air_co2_concentration_sensor"
      subfields: "outside"
      subfields: "air"
      subfields: "co2"
      subfields: "concentration"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "concentration"
      standard_unit: "parts_per_unit"
      expected_min: 0.00005
      expected_max: 0.005
    }
  ];

  // The outside air dewpoint temperature sensor.
  //
  // Composed of:
  //   outside (descriptor): Process or measurement of local atmospheric
  //     conditions.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   dewpoint (measurement_descriptor): The thermodynamic point at which
  //     water condenses from standing air.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `outside_air_dewpoint_temperature_sensor`, point type
  // `sensor`. Measured as temperature; values are in kelvin, the ontology's
  // standard unit for that quantity.
  //
  // Expected range 255.9259259 to 310.9259259. This is a data-quality
  // expectation, not a constraint: a reading outside it is a fault to
  // report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double outside_air_dewpoint_temperature_sensor = 209 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "outside_air_dewpoint_temperature_sensor"
      subfields: "outside"
      subfields: "air"
      subfields: "dewpoint"
      subfields: "temperature"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "temperature"
      standard_unit: "kelvin"
      expected_min: 255.925926
      expected_max: 310.925926
    }
  ];

  // The outside air pm2pt5 density sensor.
  //
  // Composed of:
  //   outside (descriptor): Process or measurement of local atmospheric
  //     conditions.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   pm2pt5 (descriptor): Particulate of diameter 2.5 micrometers or
  //     less.
  //   density (measurement): Amount of mass contained in a unit volume.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `outside_air_pm2pt5_density_sensor`, point type
  // `sensor`. Measured as density; values are in kilograms_per_cubic_meter,
  // the ontology's standard unit for that quantity.
  //
  // Expected range 0 to 0.001. This is a data-quality expectation, not a
  // constraint: a reading outside it is a fault to report, not a message to
  // reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double outside_air_pm2pt5_density_sensor = 327 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "outside_air_pm2pt5_density_sensor"
      subfields: "outside"
      subfields: "air"
      subfields: "pm2pt5"
      subfields: "density"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "density"
      standard_unit: "kilograms_per_cubic_meter"
      expected_min: 0.0
      expected_max: 0.001
    }
  ];

  // The outside air pressure sensor.
  //
  // Composed of:
  //   outside (descriptor): Process or measurement of local atmospheric
  //     conditions.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   pressure (measurement): Measured force per unit area within a system
  //     or process.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `outside_air_pressure_sensor`, point type `sensor`.
  // Measured as pressure; values are in pascals, the ontology's standard
  // unit for that quantity.
  //
  // Expected range 500 to 200000. This is a data-quality expectation, not a
  // constraint: a reading outside it is a fault to report, not a message to
  // reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double outside_air_pressure_sensor = 211 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "outside_air_pressure_sensor"
      subfields: "outside"
      subfields: "air"
      subfields: "pressure"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "pressure"
      standard_unit: "pascals"
      expected_min: 500.0
      expected_max: 200000.0
    }
  ];

  // The outside air rain level sensor.
  //
  // Composed of:
  //   outside (descriptor): Process or measurement of local atmospheric
  //     conditions.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   rain (descriptor): Liquid water in the form of droplets that have
  //     condensed from atmospheric water vapor.
  //   level (measurement): The height of something with regard to a datum
  //     (e.g. the water level of a tank relative to the bottom).
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `outside_air_rain_level_sensor`, point type `sensor`.
  // Measured as level; values are in meters, the ontology's standard unit
  // for that quantity.
  //
  // Expected range 0 to 5e-05. This is a data-quality expectation, not a
  // constraint: a reading outside it is a fault to report, not a message to
  // reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double outside_air_rain_level_sensor = 212 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "outside_air_rain_level_sensor"
      subfields: "outside"
      subfields: "air"
      subfields: "rain"
      subfields: "level"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "level"
      standard_unit: "meters"
      expected_min: 0.0
      expected_max: 0.00005
    }
  ];

  // The outside air relative humidity sensor.
  //
  // Composed of:
  //   outside (descriptor): Process or measurement of local atmospheric
  //     conditions.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   relative (measurement_descriptor): Quality of media with respect to
  //     theoretical minimum or maximum value for a given condition (e.g.,
  //     relative humidity).
  //   humidity (measurement): Concentration of water vapor in air
  //     (conditioned or unconditioned).
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `outside_air_relative_humidity_sensor`, point type
  // `sensor`. Measured as humidity; values are in percent_relative_humidity,
  // the ontology's standard unit for that quantity.
  //
  // Valid range 0 to 100, enforced by protovalidate.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double outside_air_relative_humidity_sensor = 213 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "outside_air_relative_humidity_sensor"
      subfields: "outside"
      subfields: "air"
      subfields: "relative"
      subfields: "humidity"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "humidity"
      standard_unit: "percent_relative_humidity"
    },
    (buf.validate.field).double.gte = 0.0,
    (buf.validate.field).double.lte = 100.0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The outside air specificenthalpy sensor.
  //
  // Composed of:
  //   outside (descriptor): Process or measurement of local atmospheric
  //     conditions.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   specificenthalpy (measurement): Measure of energy of air per unit
  //     mass.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `outside_air_specificenthalpy_sensor`, point type
  // `sensor`. Measured as specificenthalpy; values are in
  // joules_per_kilogram, the ontology's standard unit for that quantity.
  //
  // Expected range 11630 to 93040. This is a data-quality expectation, not a
  // constraint: a reading outside it is a fault to report, not a message to
  // reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double outside_air_specificenthalpy_sensor = 214 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "outside_air_specificenthalpy_sensor"
      subfields: "outside"
      subfields: "air"
      subfields: "specificenthalpy"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "specificenthalpy"
      standard_unit: "joules_per_kilogram"
      expected_min: 11630.0
      expected_max: 93040.0
    }
  ];

  // The outside air temperature sensor.
  //
  // Composed of:
  //   outside (descriptor): Process or measurement of local atmospheric
  //     conditions.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `outside_air_temperature_sensor`, point type `sensor`.
  // Measured as temperature; values are in kelvin, the ontology's standard
  // unit for that quantity.
  //
  // Expected range 255.9259259 to 322.037037. This is a data-quality
  // expectation, not a constraint: a reading outside it is a fault to
  // report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double outside_air_temperature_sensor = 215 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "outside_air_temperature_sensor"
      subfields: "outside"
      subfields: "air"
      subfields: "temperature"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "temperature"
      standard_unit: "kelvin"
      expected_min: 255.925926
      expected_max: 322.037037
    }
  ];

  // The outside air voc concentration sensor.
  //
  // Composed of:
  //   outside (descriptor): Process or measurement of local atmospheric
  //     conditions.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   voc (descriptor): Abbreviation for volatile organic componds, which
  //     are groups of chemicals emitted by many household objects like
  //     furniture and paint.
  //   concentration (measurement): Concentration of something relative to
  //     something else (usually in parts per million or parts per billion).
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `outside_air_voc_concentration_sensor`, point type
  // `sensor`. Measured as concentration; values are in parts_per_unit, the
  // ontology's standard unit for that quantity.
  //
  // Expected range 0 to 0.005. This is a data-quality expectation, not a
  // constraint: a reading outside it is a fault to report, not a message to
  // reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double outside_air_voc_concentration_sensor = 216 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "outside_air_voc_concentration_sensor"
      subfields: "outside"
      subfields: "air"
      subfields: "voc"
      subfields: "concentration"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "concentration"
      standard_unit: "parts_per_unit"
      expected_min: 0.0
      expected_max: 0.005
    }
  ];

  // The outside air wetbulb temperature sensor.
  //
  // Composed of:
  //   outside (descriptor): Process or measurement of local atmospheric
  //     conditions.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   wetbulb (measurement_descriptor): Describes air temperature measured
  //     at 100% relative humidity (saturation).
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `outside_air_wetbulb_temperature_sensor`, point type
  // `sensor`. Measured as temperature; values are in kelvin, the ontology's
  // standard unit for that quantity.
  //
  // Expected range 255.9259259 to 322.037037. This is a data-quality
  // expectation, not a constraint: a reading outside it is a fault to
  // report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double outside_air_wetbulb_temperature_sensor = 217 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "outside_air_wetbulb_temperature_sensor"
      subfields: "outside"
      subfields: "air"
      subfields: "wetbulb"
      subfields: "temperature"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "temperature"
      standard_unit: "kelvin"
      expected_min: 255.925926
      expected_max: 322.037037
    }
  ];

  // The overload compressor alarm.
  //
  // Composed of:
  //   overload (descriptor): An electrical load that exceeds available
  //     electrical power.
  //   compressor (component): Component which drives refrigerant
  //     compression (and thus cooling processes) within a device or system.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `overload_compressor_alarm`, point type `alarm`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  repeated ActiveInactiveValue overload_compressor_alarm = 218 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "overload_compressor_alarm"
      subfields: "overload"
      subfields: "compressor"
      subfields: "alarm"
      point_type: "alarm"
      enumerated: true
    }
  ];

  // The overload discharge fan alarm.
  //
  // Composed of:
  //   overload (descriptor): An electrical load that exceeds available
  //     electrical power.
  //   discharge (descriptor): Media leaving system to enter ambient
  //     conditioned space. Typically applies only to air-side systems.
  //   fan (component): Component used for the distribution of air.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `overload_discharge_fan_alarm`, point type `alarm`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue overload_discharge_fan_alarm = 219 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "overload_discharge_fan_alarm"
      subfields: "overload"
      subfields: "discharge"
      subfields: "fan"
      subfields: "alarm"
      point_type: "alarm"
    }
  ];

  // The power capacity.
  //
  // Composed of:
  //   power (measurement): Rate of energy consumption (assumed context is
  //     electrical and real, unless otherwise modified).
  //   capacity (point_type): A design parameter quantity (e.g., design
  //     motor power capacity). Is always a maximum limit.
  //
  // Ontology field: `power_capacity`, point type `capacity`. Measured as
  // power; values are in watts, the ontology's standard unit for that
  // quantity.
  //
  // Valid range from 0, enforced by protovalidate. Expected range up to
  // 100000. This is a data-quality expectation, not a constraint: a reading
  // outside it is a fault to report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double power_capacity = 220 [
    (google.api.field_behavior) = OPTIONAL,
    (google.api.field_behavior) = IMMUTABLE,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "power_capacity"
      subfields: "power"
      subfields: "capacity"
      point_type: "capacity"
      measurement: "power"
      standard_unit: "watts"
      expected_max: 100000.0
    },
    (buf.validate.field).double.gte = 0.0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The power sensor.
  //
  // Composed of:
  //   power (measurement): Rate of energy consumption (assumed context is
  //     electrical and real, unless otherwise modified).
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `power_sensor`, point type `sensor`. Measured as power;
  // values are in watts, the ontology's standard unit for that quantity.
  //
  // Expected range 0 to 1e+07. This is a data-quality expectation, not a
  // constraint: a reading outside it is a fault to report, not a message to
  // reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double power_sensor = 221 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "power_sensor"
      subfields: "power"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "power"
      standard_unit: "watts"
      expected_min: 0.0
      expected_max: 10000000.0
    }
  ];

  // The powerfactor sensor.
  //
  // Composed of:
  //   powerfactor (measurement): Ratio of real and apparent power.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `powerfactor_sensor`, point type `sensor`. Measured as
  // powerfactor; values are in no_units, the ontology's standard unit for
  // that quantity.
  //
  // Valid range -1 to 1, enforced by protovalidate.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double powerfactor_sensor = 222 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "powerfactor_sensor"
      subfields: "powerfactor"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "powerfactor"
      standard_unit: "no_units"
    },
    (buf.validate.field).double.gte = -1.0,
    (buf.validate.field).double.lte = 1.0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The pressurization request count.
  //
  // Composed of:
  //   pressurization (descriptor): Relating to the level of pressure in a
  //     system or vessel.
  //   request (descriptor): A signal from a system device that calls for
  //     more of a particular conditioning process (e.g., pressure requests
  //     from terminal units).
  //   count (point_type): Total count of actions or requests.
  //
  // Ontology field: `pressurization_request_count`, point type `count`.
  // Dimensionless: the ontology defines this point type as an integer count
  // rather than a measured quantity.
  //
  // Valid range from 0, enforced by protovalidate. Expected range up to 500.
  // This is a data-quality expectation, not a constraint: a reading outside
  // it is a fault to report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional int64 pressurization_request_count = 223 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "pressurization_request_count"
      subfields: "pressurization"
      subfields: "request"
      subfields: "count"
      point_type: "count"
      expected_max: 500.0
    },
    (buf.validate.field).int64.gte = 0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The refrigerant condenser pressure sensor.
  //
  // Composed of:
  //   refrigerant (descriptor): Substance used in the mechanical
  //     refrigeration process (e.g., R-134a)
  //   condenser (component): The component of a refrigeration system that
  //     condenses refrigerant through the removal of heat (i.e., refrigerant
  //     vapor is condensed into liquid through the removal of heat).
  //   pressure (measurement): Measured force per unit area within a system
  //     or process.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `refrigerant_condenser_pressure_sensor`, point type
  // `sensor`. Measured as pressure; values are in pascals, the ontology's
  // standard unit for that quantity.
  //
  // Expected range 0 to 2.068427367e+06. This is a data-quality expectation,
  // not a constraint: a reading outside it is a fault to report, not a
  // message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  repeated double refrigerant_condenser_pressure_sensor = 224 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "refrigerant_condenser_pressure_sensor"
      subfields: "refrigerant"
      subfields: "condenser"
      subfields: "pressure"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "pressure"
      standard_unit: "pascals"
      enumerated: true
      expected_min: 0.0
      expected_max: 2068427.367
    }
  ];

  // The refrigerant differential pressure sensor.
  //
  // Composed of:
  //   refrigerant (descriptor): Substance used in the mechanical
  //     refrigeration process (e.g., R-134a)
  //   differential (measurement_descriptor): Measured difference between
  //     two reference points (e.g. differential_water_pressure)
  //   pressure (measurement): Measured force per unit area within a system
  //     or process.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `refrigerant_differential_pressure_sensor`, point type
  // `sensor`. Measured as pressure; values are in pascals, the ontology's
  // standard unit for that quantity.
  //
  // Expected range -3447.38 to 137895.2. This is a data-quality expectation,
  // not a constraint: a reading outside it is a fault to report, not a
  // message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  repeated double refrigerant_differential_pressure_sensor = 225 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "refrigerant_differential_pressure_sensor"
      subfields: "refrigerant"
      subfields: "differential"
      subfields: "pressure"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "pressure"
      standard_unit: "pascals"
      enumerated: true
      expected_min: -3447.38
      expected_max: 137895.2
    }
  ];

  // The refrigerant discharge pressure sensor.
  //
  // Composed of:
  //   refrigerant (descriptor): Substance used in the mechanical
  //     refrigeration process (e.g., R-134a)
  //   discharge (descriptor): Media leaving system to enter ambient
  //     conditioned space. Typically applies only to air-side systems.
  //   pressure (measurement): Measured force per unit area within a system
  //     or process.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `refrigerant_discharge_pressure_sensor`, point type
  // `sensor`. Measured as pressure; values are in pascals, the ontology's
  // standard unit for that quantity.
  //
  // Expected range -34500 to 2.7579029173e+06. This is a data-quality
  // expectation, not a constraint: a reading outside it is a fault to
  // report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double refrigerant_discharge_pressure_sensor = 226 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "refrigerant_discharge_pressure_sensor"
      subfields: "refrigerant"
      subfields: "discharge"
      subfields: "pressure"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "pressure"
      standard_unit: "pascals"
      expected_min: -34500.0
      expected_max: 2757902.9173
    }
  ];

  // The refrigerant discharge temperature sensor.
  //
  // Composed of:
  //   refrigerant (descriptor): Substance used in the mechanical
  //     refrigeration process (e.g., R-134a)
  //   discharge (descriptor): Media leaving system to enter ambient
  //     conditioned space. Typically applies only to air-side systems.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `refrigerant_discharge_temperature_sensor`, point type
  // `sensor`. Measured as temperature; values are in kelvin, the ontology's
  // standard unit for that quantity.
  //
  // Expected range 255.9259259 to 422.037037. This is a data-quality
  // expectation, not a constraint: a reading outside it is a fault to
  // report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double refrigerant_discharge_temperature_sensor = 227 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "refrigerant_discharge_temperature_sensor"
      subfields: "refrigerant"
      subfields: "discharge"
      subfields: "temperature"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "temperature"
      standard_unit: "kelvin"
      expected_min: 255.925926
      expected_max: 422.037037
    }
  ];

  // The refrigerant evaporator pressure sensor.
  //
  // Composed of:
  //   refrigerant (descriptor): Substance used in the mechanical
  //     refrigeration process (e.g., R-134a)
  //   evaporator (component): The component of a refrigeration system that
  //     evaporates refrigerant by adding heat (i.e., refrigerant liquid
  //     evaporates into vapor when heat from the surrounding air/water is
  //     transferred to it).
  //   pressure (measurement): Measured force per unit area within a system
  //     or process.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `refrigerant_evaporator_pressure_sensor`, point type
  // `sensor`. Measured as pressure; values are in pascals, the ontology's
  // standard unit for that quantity.
  //
  // Expected range 0 to 2.068427367e+06. This is a data-quality expectation,
  // not a constraint: a reading outside it is a fault to report, not a
  // message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  repeated double refrigerant_evaporator_pressure_sensor = 228 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "refrigerant_evaporator_pressure_sensor"
      subfields: "refrigerant"
      subfields: "evaporator"
      subfields: "pressure"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "pressure"
      standard_unit: "pascals"
      enumerated: true
      expected_min: 0.0
      expected_max: 2068427.367
    }
  ];

  // The refrigerant high pressure status.
  //
  // Composed of:
  //   refrigerant (descriptor): Substance used in the mechanical
  //     refrigeration process (e.g., R-134a)
  //   high (descriptor): Level of control or measurement; above medium and
  //     low; opposite of low.
  //   pressure (measurement): Measured force per unit area within a system
  //     or process.
  //   status (point_type): The multistate value indicating an observed
  //     state in a piece of equipment, often indicating if a command was
  //     effected. It is a neutral observation (i.e., no quality judgment of
  //     'good' or 'bad'). It also has no units of measurement; therefore, if
  //     combined with a measurement subfield, it will indicate that the
  //     field is the directional status based on some measurement of that
  //     type (e.g., power_status equates to an on/off value based on some
  //     inference of power).
  //
  // Ontology field: `refrigerant_high_pressure_status`, point type `status`.
  // Measured as pressure; values are in pascals, the ontology's standard
  // unit for that quantity.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue refrigerant_high_pressure_status = 229 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "refrigerant_high_pressure_status"
      subfields: "refrigerant"
      subfields: "high"
      subfields: "pressure"
      subfields: "status"
      point_type: "status"
      measurement: "pressure"
      standard_unit: "pascals"
    }
  ];

  // The refrigerant liquid pressure sensor.
  //
  // Composed of:
  //   refrigerant (descriptor): Substance used in the mechanical
  //     refrigeration process (e.g., R-134a)
  //   liquid (descriptor): A state of matter that flows and takes the form
  //     of its container, but which changes its volume very little with
  //     changes in pressure.
  //   pressure (measurement): Measured force per unit area within a system
  //     or process.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `refrigerant_liquid_pressure_sensor`, point type
  // `sensor`. Measured as pressure; values are in pascals, the ontology's
  // standard unit for that quantity.
  //
  // Expected range -34473.78945 to 2.7579029173e+06. This is a data-quality
  // expectation, not a constraint: a reading outside it is a fault to
  // report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double refrigerant_liquid_pressure_sensor = 230 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "refrigerant_liquid_pressure_sensor"
      subfields: "refrigerant"
      subfields: "liquid"
      subfields: "pressure"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "pressure"
      standard_unit: "pascals"
      expected_min: -34473.78945
      expected_max: 2757902.9173
    }
  ];

  // The refrigerant liquid saturation temperature sensor.
  //
  // Composed of:
  //   refrigerant (descriptor): Substance used in the mechanical
  //     refrigeration process (e.g., R-134a)
  //   liquid (descriptor): A state of matter that flows and takes the form
  //     of its container, but which changes its volume very little with
  //     changes in pressure.
  //   saturation (measurement_descriptor): Point at which no more of a
  //     material can be absorbed into another material.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `refrigerant_liquid_saturation_temperature_sensor`,
  // point type `sensor`. Measured as temperature; values are in kelvin, the
  // ontology's standard unit for that quantity.
  //
  // Expected range 255.9259259 to 422.037037. This is a data-quality
  // expectation, not a constraint: a reading outside it is a fault to
  // report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double refrigerant_liquid_saturation_temperature_sensor = 231 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "refrigerant_liquid_saturation_temperature_sensor"
      subfields: "refrigerant"
      subfields: "liquid"
      subfields: "saturation"
      subfields: "temperature"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "temperature"
      standard_unit: "kelvin"
      expected_min: 255.925926
      expected_max: 422.037037
    }
  ];

  // The refrigerant liquid temperature sensor.
  //
  // Composed of:
  //   refrigerant (descriptor): Substance used in the mechanical
  //     refrigeration process (e.g., R-134a)
  //   liquid (descriptor): A state of matter that flows and takes the form
  //     of its container, but which changes its volume very little with
  //     changes in pressure.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `refrigerant_liquid_temperature_sensor`, point type
  // `sensor`. Measured as temperature; values are in kelvin, the ontology's
  // standard unit for that quantity.
  //
  // Expected range 255.9259259 to 422.037037. This is a data-quality
  // expectation, not a constraint: a reading outside it is a fault to
  // report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double refrigerant_liquid_temperature_sensor = 232 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "refrigerant_liquid_temperature_sensor"
      subfields: "refrigerant"
      subfields: "liquid"
      subfields: "temperature"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "temperature"
      standard_unit: "kelvin"
      expected_min: 255.925926
      expected_max: 422.037037
    }
  ];

  // The refrigerant low pressure status.
  //
  // Composed of:
  //   refrigerant (descriptor): Substance used in the mechanical
  //     refrigeration process (e.g., R-134a)
  //   low (descriptor): Level of control or measurement; below medium and
  //     high; opposite of high.
  //   pressure (measurement): Measured force per unit area within a system
  //     or process.
  //   status (point_type): The multistate value indicating an observed
  //     state in a piece of equipment, often indicating if a command was
  //     effected. It is a neutral observation (i.e., no quality judgment of
  //     'good' or 'bad'). It also has no units of measurement; therefore, if
  //     combined with a measurement subfield, it will indicate that the
  //     field is the directional status based on some measurement of that
  //     type (e.g., power_status equates to an on/off value based on some
  //     inference of power).
  //
  // Ontology field: `refrigerant_low_pressure_status`, point type `status`.
  // Measured as pressure; values are in pascals, the ontology's standard
  // unit for that quantity.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue refrigerant_low_pressure_status = 233 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "refrigerant_low_pressure_status"
      subfields: "refrigerant"
      subfields: "low"
      subfields: "pressure"
      subfields: "status"
      point_type: "status"
      measurement: "pressure"
      standard_unit: "pascals"
    }
  ];

  // The refrigerant subcooling temperature sensor.
  //
  // Composed of:
  //   refrigerant (descriptor): Substance used in the mechanical
  //     refrigeration process (e.g., R-134a)
  //   subcooling (measurement_descriptor): The amount a liquid is cooled
  //     below its saturation point.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `refrigerant_subcooling_temperature_sensor`, point type
  // `sensor`. Measured as temperature; values are in kelvin, the ontology's
  // standard unit for that quantity.
  //
  // Expected range 227.5925926 to 366.4814815. This is a data-quality
  // expectation, not a constraint: a reading outside it is a fault to
  // report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double refrigerant_subcooling_temperature_sensor = 234 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "refrigerant_subcooling_temperature_sensor"
      subfields: "refrigerant"
      subfields: "subcooling"
      subfields: "temperature"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "temperature"
      standard_unit: "kelvin"
      expected_min: 227.592593
      expected_max: 366.481481
    }
  ];

  // The refrigerant suction pressure sensor.
  //
  // Composed of:
  //   refrigerant (descriptor): Substance used in the mechanical
  //     refrigeration process (e.g., R-134a)
  //   suction (descriptor): The production of a partial vacuum in order to
  //     force fluid into a vacant space (such as refrigerant in a
  //     compressor).
  //   pressure (measurement): Measured force per unit area within a system
  //     or process.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `refrigerant_suction_pressure_sensor`, point type
  // `sensor`. Measured as pressure; values are in pascals, the ontology's
  // standard unit for that quantity.
  //
  // Expected range -34473.78945 to 2.7579029173e+06. This is a data-quality
  // expectation, not a constraint: a reading outside it is a fault to
  // report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double refrigerant_suction_pressure_sensor = 235 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "refrigerant_suction_pressure_sensor"
      subfields: "refrigerant"
      subfields: "suction"
      subfields: "pressure"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "pressure"
      standard_unit: "pascals"
      expected_min: -34473.78945
      expected_max: 2757902.9173
    }
  ];

  // The refrigerant suction saturation temperature sensor.
  //
  // Composed of:
  //   refrigerant (descriptor): Substance used in the mechanical
  //     refrigeration process (e.g., R-134a)
  //   suction (descriptor): The production of a partial vacuum in order to
  //     force fluid into a vacant space (such as refrigerant in a
  //     compressor).
  //   saturation (measurement_descriptor): Point at which no more of a
  //     material can be absorbed into another material.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `refrigerant_suction_saturation_temperature_sensor`,
  // point type `sensor`. Measured as temperature; values are in kelvin, the
  // ontology's standard unit for that quantity.
  //
  // Expected range 255.9259259 to 422.037037. This is a data-quality
  // expectation, not a constraint: a reading outside it is a fault to
  // report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double refrigerant_suction_saturation_temperature_sensor = 236 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "refrigerant_suction_saturation_temperature_sensor"
      subfields: "refrigerant"
      subfields: "suction"
      subfields: "saturation"
      subfields: "temperature"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "temperature"
      standard_unit: "kelvin"
      expected_min: 255.925926
      expected_max: 422.037037
    }
  ];

  // The refrigerant suction superheat temperature sensor.
  //
  // Composed of:
  //   refrigerant (descriptor): Substance used in the mechanical
  //     refrigeration process (e.g., R-134a)
  //   suction (descriptor): The production of a partial vacuum in order to
  //     force fluid into a vacant space (such as refrigerant in a
  //     compressor).
  //   superheat (measurement_descriptor): The amount a gas is heated above
  //     its boiling point wihtout vaporization.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `refrigerant_suction_superheat_temperature_sensor`,
  // point type `sensor`. Measured as temperature; values are in kelvin, the
  // ontology's standard unit for that quantity.
  //
  // Expected range 255.9259259 to 422.037037. This is a data-quality
  // expectation, not a constraint: a reading outside it is a fault to
  // report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double refrigerant_suction_superheat_temperature_sensor = 237 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "refrigerant_suction_superheat_temperature_sensor"
      subfields: "refrigerant"
      subfields: "suction"
      subfields: "superheat"
      subfields: "temperature"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "temperature"
      standard_unit: "kelvin"
      expected_min: 255.925926
      expected_max: 422.037037
    }
  ];

  // The refrigerant suction temperature sensor.
  //
  // Composed of:
  //   refrigerant (descriptor): Substance used in the mechanical
  //     refrigeration process (e.g., R-134a)
  //   suction (descriptor): The production of a partial vacuum in order to
  //     force fluid into a vacant space (such as refrigerant in a
  //     compressor).
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `refrigerant_suction_temperature_sensor`, point type
  // `sensor`. Measured as temperature; values are in kelvin, the ontology's
  // standard unit for that quantity.
  //
  // Expected range 255.9259259 to 422.037037. This is a data-quality
  // expectation, not a constraint: a reading outside it is a fault to
  // report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double refrigerant_suction_temperature_sensor = 238 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "refrigerant_suction_temperature_sensor"
      subfields: "refrigerant"
      subfields: "suction"
      subfields: "temperature"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "temperature"
      standard_unit: "kelvin"
      expected_min: 255.925926
      expected_max: 422.037037
    }
  ];

  // The refrigerant vapor temperature sensor.
  //
  // Composed of:
  //   refrigerant (descriptor): Substance used in the mechanical
  //     refrigeration process (e.g., R-134a)
  //   vapor (descriptor): A substance in the gas phase at a temperature
  //     lower than its critical temperature.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `refrigerant_vapor_temperature_sensor`, point type
  // `sensor`. Measured as temperature; values are in kelvin, the ontology's
  // standard unit for that quantity.
  //
  // Expected range 255.9259259 to 422.037037. This is a data-quality
  // expectation, not a constraint: a reading outside it is a fault to
  // report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double refrigerant_vapor_temperature_sensor = 239 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "refrigerant_vapor_temperature_sensor"
      subfields: "refrigerant"
      subfields: "vapor"
      subfields: "temperature"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "temperature"
      standard_unit: "kelvin"
      expected_min: 255.925926
      expected_max: 422.037037
    }
  ];

  // The request run command.
  //
  // Composed of:
  //   request (descriptor): A signal from a system device that calls for
  //     more of a particular conditioning process (e.g., pressure requests
  //     from terminal units).
  //   run (measurement_descriptor): State of being active or operating. By
  //     default applies to control program for system (e.g., VAV program
  //     run_command).
  //   command (point_type): The signal given to make an action happen.
  //     Defaults to multistate unless used alongside a measurement_type.
  //
  // Ontology field: `request_run_command`, point type `command`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional OffOnValue request_run_command = 240 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "request_run_command"
      subfields: "request"
      subfields: "run"
      subfields: "command"
      point_type: "command"
    }
  ];

  // The return air cooling temperature setpoint.
  //
  // Composed of:
  //   return (descriptor): Measurement or process of media as it is
  //     returned from the end-use equipment within the system.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   cooling (descriptor): Process, measured effect or requirement for
  //     cooling.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   setpoint (point_type): Control target of process or system.
  //
  // Ontology field: `return_air_cooling_temperature_setpoint`, point type
  // `setpoint`. Measured as temperature; values are in kelvin, the
  // ontology's standard unit for that quantity.
  //
  // Expected range 255.9259259 to 308.1481481. This is a data-quality
  // expectation, not a constraint: a reading outside it is a fault to
  // report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double return_air_cooling_temperature_setpoint = 241 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "return_air_cooling_temperature_setpoint"
      subfields: "return"
      subfields: "air"
      subfields: "cooling"
      subfields: "temperature"
      subfields: "setpoint"
      point_type: "setpoint"
      measurement: "temperature"
      standard_unit: "kelvin"
      expected_min: 255.925926
      expected_max: 308.148148
    }
  ];

  // The return air damper command.
  //
  // Composed of:
  //   return (descriptor): Measurement or process of media as it is
  //     returned from the end-use equipment within the system.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   damper (component): Component which meters the flow of air within a
  //     system or device.
  //   command (point_type): The signal given to make an action happen.
  //     Defaults to multistate unless used alongside a measurement_type.
  //
  // Ontology field: `return_air_damper_command`, point type `command`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ClosedOpenValue return_air_damper_command = 242 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "return_air_damper_command"
      subfields: "return"
      subfields: "air"
      subfields: "damper"
      subfields: "command"
      point_type: "command"
    }
  ];

  // The return air damper status.
  //
  // Composed of:
  //   return (descriptor): Measurement or process of media as it is
  //     returned from the end-use equipment within the system.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   damper (component): Component which meters the flow of air within a
  //     system or device.
  //   status (point_type): The multistate value indicating an observed
  //     state in a piece of equipment, often indicating if a command was
  //     effected. It is a neutral observation (i.e., no quality judgment of
  //     'good' or 'bad'). It also has no units of measurement; therefore, if
  //     combined with a measurement subfield, it will indicate that the
  //     field is the directional status based on some measurement of that
  //     type (e.g., power_status equates to an on/off value based on some
  //     inference of power).
  //
  // Ontology field: `return_air_damper_status`, point type `status`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ClosedOpenValue return_air_damper_status = 243 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "return_air_damper_status"
      subfields: "return"
      subfields: "air"
      subfields: "damper"
      subfields: "status"
      point_type: "status"
    }
  ];

  // The return air heating temperature setpoint.
  //
  // Composed of:
  //   return (descriptor): Measurement or process of media as it is
  //     returned from the end-use equipment within the system.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   heating (descriptor): Process, measured effect or requirement for
  //     heating.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   setpoint (point_type): Control target of process or system.
  //
  // Ontology field: `return_air_heating_temperature_setpoint`, point type
  // `setpoint`. Measured as temperature; values are in kelvin, the
  // ontology's standard unit for that quantity.
  //
  // Expected range 255.9259259 to 308.1481481. This is a data-quality
  // expectation, not a constraint: a reading outside it is a fault to
  // report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double return_air_heating_temperature_setpoint = 244 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "return_air_heating_temperature_setpoint"
      subfields: "return"
      subfields: "air"
      subfields: "heating"
      subfields: "temperature"
      subfields: "setpoint"
      point_type: "setpoint"
      measurement: "temperature"
      standard_unit: "kelvin"
      expected_min: 255.925926
      expected_max: 308.148148
    }
  ];

  // The return air isolation damper command.
  //
  // Composed of:
  //   return (descriptor): Measurement or process of media as it is
  //     returned from the end-use equipment within the system.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   isolation (descriptor): Process of isolating one component or
  //     process from another.
  //   damper (component): Component which meters the flow of air within a
  //     system or device.
  //   command (point_type): The signal given to make an action happen.
  //     Defaults to multistate unless used alongside a measurement_type.
  //
  // Ontology field: `return_air_isolation_damper_command`, point type
  // `command`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  repeated ClosedOpenValue return_air_isolation_damper_command = 245 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "return_air_isolation_damper_command"
      subfields: "return"
      subfields: "air"
      subfields: "isolation"
      subfields: "damper"
      subfields: "command"
      point_type: "command"
      enumerated: true
    }
  ];

  // The return air isolation damper status.
  //
  // Composed of:
  //   return (descriptor): Measurement or process of media as it is
  //     returned from the end-use equipment within the system.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   isolation (descriptor): Process of isolating one component or
  //     process from another.
  //   damper (component): Component which meters the flow of air within a
  //     system or device.
  //   status (point_type): The multistate value indicating an observed
  //     state in a piece of equipment, often indicating if a command was
  //     effected. It is a neutral observation (i.e., no quality judgment of
  //     'good' or 'bad'). It also has no units of measurement; therefore, if
  //     combined with a measurement subfield, it will indicate that the
  //     field is the directional status based on some measurement of that
  //     type (e.g., power_status equates to an on/off value based on some
  //     inference of power).
  //
  // Ontology field: `return_air_isolation_damper_status`, point type
  // `status`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  repeated ClosedOpenValue return_air_isolation_damper_status = 246 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "return_air_isolation_damper_status"
      subfields: "return"
      subfields: "air"
      subfields: "isolation"
      subfields: "damper"
      subfields: "status"
      point_type: "status"
      enumerated: true
    }
  ];

  // The return air relative humidity sensor.
  //
  // Composed of:
  //   return (descriptor): Measurement or process of media as it is
  //     returned from the end-use equipment within the system.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   relative (measurement_descriptor): Quality of media with respect to
  //     theoretical minimum or maximum value for a given condition (e.g.,
  //     relative humidity).
  //   humidity (measurement): Concentration of water vapor in air
  //     (conditioned or unconditioned).
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `return_air_relative_humidity_sensor`, point type
  // `sensor`. Measured as humidity; values are in percent_relative_humidity,
  // the ontology's standard unit for that quantity.
  //
  // Valid range 0 to 100, enforced by protovalidate.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double return_air_relative_humidity_sensor = 247 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "return_air_relative_humidity_sensor"
      subfields: "return"
      subfields: "air"
      subfields: "relative"
      subfields: "humidity"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "humidity"
      standard_unit: "percent_relative_humidity"
    },
    (buf.validate.field).double.gte = 0.0,
    (buf.validate.field).double.lte = 100.0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The return air relative humidity setpoint.
  //
  // Composed of:
  //   return (descriptor): Measurement or process of media as it is
  //     returned from the end-use equipment within the system.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   relative (measurement_descriptor): Quality of media with respect to
  //     theoretical minimum or maximum value for a given condition (e.g.,
  //     relative humidity).
  //   humidity (measurement): Concentration of water vapor in air
  //     (conditioned or unconditioned).
  //   setpoint (point_type): Control target of process or system.
  //
  // Ontology field: `return_air_relative_humidity_setpoint`, point type
  // `setpoint`. Measured as humidity; values are in
  // percent_relative_humidity, the ontology's standard unit for that
  // quantity.
  //
  // Valid range 0 to 100, enforced by protovalidate.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double return_air_relative_humidity_setpoint = 248 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "return_air_relative_humidity_setpoint"
      subfields: "return"
      subfields: "air"
      subfields: "relative"
      subfields: "humidity"
      subfields: "setpoint"
      point_type: "setpoint"
      measurement: "humidity"
      standard_unit: "percent_relative_humidity"
    },
    (buf.validate.field).double.gte = 0.0,
    (buf.validate.field).double.lte = 100.0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The return air specificenthalpy sensor.
  //
  // Composed of:
  //   return (descriptor): Measurement or process of media as it is
  //     returned from the end-use equipment within the system.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   specificenthalpy (measurement): Measure of energy of air per unit
  //     mass.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `return_air_specificenthalpy_sensor`, point type
  // `sensor`. Measured as specificenthalpy; values are in
  // joules_per_kilogram, the ontology's standard unit for that quantity.
  //
  // Expected range 11630 to 93040. This is a data-quality expectation, not a
  // constraint: a reading outside it is a fault to report, not a message to
  // reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double return_air_specificenthalpy_sensor = 249 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "return_air_specificenthalpy_sensor"
      subfields: "return"
      subfields: "air"
      subfields: "specificenthalpy"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "specificenthalpy"
      standard_unit: "joules_per_kilogram"
      expected_min: 11630.0
      expected_max: 93040.0
    }
  ];

  // The return air temperature sensor.
  //
  // Composed of:
  //   return (descriptor): Measurement or process of media as it is
  //     returned from the end-use equipment within the system.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `return_air_temperature_sensor`, point type `sensor`.
  // Measured as temperature; values are in kelvin, the ontology's standard
  // unit for that quantity.
  //
  // Expected range 255.9259259 to 338.7037037. This is a data-quality
  // expectation, not a constraint: a reading outside it is a fault to
  // report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double return_air_temperature_sensor = 250 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "return_air_temperature_sensor"
      subfields: "return"
      subfields: "air"
      subfields: "temperature"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "temperature"
      standard_unit: "kelvin"
      expected_min: 255.925926
      expected_max: 338.703704
    }
  ];

  // The return air temperature setpoint.
  //
  // Composed of:
  //   return (descriptor): Measurement or process of media as it is
  //     returned from the end-use equipment within the system.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   setpoint (point_type): Control target of process or system.
  //
  // Ontology field: `return_air_temperature_setpoint`, point type
  // `setpoint`. Measured as temperature; values are in kelvin, the
  // ontology's standard unit for that quantity.
  //
  // Expected range 255.9259259 to 308.1481481. This is a data-quality
  // expectation, not a constraint: a reading outside it is a fault to
  // report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double return_air_temperature_setpoint = 251 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "return_air_temperature_setpoint"
      subfields: "return"
      subfields: "air"
      subfields: "temperature"
      subfields: "setpoint"
      point_type: "setpoint"
      measurement: "temperature"
      standard_unit: "kelvin"
      expected_min: 255.925926
      expected_max: 308.148148
    }
  ];

  // The return water temperature sensor.
  //
  // Composed of:
  //   return (descriptor): Measurement or process of media as it is
  //     returned from the end-use equipment within the system.
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `return_water_temperature_sensor`, point type `sensor`.
  // Measured as temperature; values are in kelvin, the ontology's standard
  // unit for that quantity.
  //
  // Expected range 255.9259259 to 380.3703704. This is a data-quality
  // expectation, not a constraint: a reading outside it is a fault to
  // report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double return_water_temperature_sensor = 252 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "return_water_temperature_sensor"
      subfields: "return"
      subfields: "water"
      subfields: "temperature"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "temperature"
      standard_unit: "kelvin"
      expected_min: 255.925926
      expected_max: 380.37037
    }
  ];

  // The reversing valve command.
  //
  // Composed of:
  //   reversing (descriptor): Reverses direction of flow (e.g., the
  //     reversing valve on a heat pump changes the direction that the
  //     refrigerant flows around the circuit).
  //   valve (component): Component which meters the flow of water within a
  //     system or device.
  //   command (point_type): The signal given to make an action happen.
  //     Defaults to multistate unless used alongside a measurement_type.
  //
  // Ontology field: `reversing_valve_command`, point type `command`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional AutoClosedOpenUnknownValue reversing_valve_command = 253 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "reversing_valve_command"
      subfields: "reversing"
      subfields: "valve"
      subfields: "command"
      point_type: "command"
    }
  ];

  // The run command.
  //
  // Composed of:
  //   run (measurement_descriptor): State of being active or operating. By
  //     default applies to control program for system (e.g., VAV program
  //     run_command).
  //   command (point_type): The signal given to make an action happen.
  //     Defaults to multistate unless used alongside a measurement_type.
  //
  // Ontology field: `run_command`, point type `command`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional OffOnValue run_command = 254 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "run_command"
      subfields: "run"
      subfields: "command"
      point_type: "command"
    }
  ];

  // The run mode.
  //
  // Composed of:
  //   run (measurement_descriptor): State of being active or operating. By
  //     default applies to control program for system (e.g., VAV program
  //     run_command).
  //   mode (point_type): Distinct mode of operation within system. For
  //     example economizer_mode has modes of 'enabled' or 'disabled.'
  //
  // Ontology field: `run_mode`, point type `mode`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional AutoCommissioningCoolingDehumidifyFanOnlyFlushingHeatingManualNeutralOffUnknownValue run_mode = 255 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "run_mode"
      subfields: "run"
      subfields: "mode"
      point_type: "mode"
    }
  ];

  // The run status.
  //
  // Composed of:
  //   run (measurement_descriptor): State of being active or operating. By
  //     default applies to control program for system (e.g., VAV program
  //     run_command).
  //   status (point_type): The multistate value indicating an observed
  //     state in a piece of equipment, often indicating if a command was
  //     effected. It is a neutral observation (i.e., no quality judgment of
  //     'good' or 'bad'). It also has no units of measurement; therefore, if
  //     combined with a measurement subfield, it will indicate that the
  //     field is the directional status based on some measurement of that
  //     type (e.g., power_status equates to an on/off value based on some
  //     inference of power).
  //
  // Ontology field: `run_status`, point type `status`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional OffOnStandbyUnknownValue run_status = 256 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "run_status"
      subfields: "run"
      subfields: "status"
      point_type: "status"
    }
  ];

  // The run time accumulator.
  //
  // Composed of:
  //   run (measurement_descriptor): State of being active or operating. By
  //     default applies to control program for system (e.g., VAV program
  //     run_command).
  //   time (measurement): Defined duration of something.
  //   accumulator (point_type): The total accumulated quantity (e.g.,
  //     total energy accumulated).
  //
  // Ontology field: `run_time_accumulator`, point type `accumulator`.
  // Measured as time; values are in seconds, the ontology's standard unit
  // for that quantity.
  //
  // Valid range from 0, enforced by protovalidate. Expected range up to
  // 1.25. This is a data-quality expectation, not a constraint: a reading
  // outside it is a fault to report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double run_time_accumulator = 257 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "run_time_accumulator"
      subfields: "run"
      subfields: "time"
      subfields: "accumulator"
      point_type: "accumulator"
      measurement: "time"
      standard_unit: "seconds"
      expected_max: 1.25
    },
    (buf.validate.field).double.gte = 0.0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The schedule run command.
  //
  // Composed of:
  //   schedule (descriptor): Refers to the time-of-day run-time
  //     requirements for the equipment.
  //   run (measurement_descriptor): State of being active or operating. By
  //     default applies to control program for system (e.g., VAV program
  //     run_command).
  //   command (point_type): The signal given to make an action happen.
  //     Defaults to multistate unless used alongside a measurement_type.
  //
  // Ontology field: `schedule_run_command`, point type `command`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional OffOnUnknownValue schedule_run_command = 258 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "schedule_run_command"
      subfields: "schedule"
      subfields: "run"
      subfields: "command"
      point_type: "command"
    }
  ];

  // The smoke alarm.
  //
  // Composed of:
  //   smoke (descriptor): Visible, suspended particles created during a
  //     fire.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `smoke_alarm`, point type `alarm`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue smoke_alarm = 259 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "smoke_alarm"
      subfields: "smoke"
      subfields: "alarm"
      point_type: "alarm"
    }
  ];

  // The supply air damper percentage command.
  //
  // Composed of:
  //   supply (descriptor): Measurement or process of media as it is
  //     supplied to the end-use equipment within the system.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   damper (component): Component which meters the flow of air within a
  //     system or device.
  //   percentage (measurement): Measurement per hundred.
  //   command (point_type): The signal given to make an action happen.
  //     Defaults to multistate unless used alongside a measurement_type.
  //
  // Ontology field: `supply_air_damper_percentage_command`, point type
  // `command`. Measured as percentage; values are in percent, the ontology's
  // standard unit for that quantity.
  //
  // Valid range 0 to 100, enforced by protovalidate.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double supply_air_damper_percentage_command = 260 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "supply_air_damper_percentage_command"
      subfields: "supply"
      subfields: "air"
      subfields: "damper"
      subfields: "percentage"
      subfields: "command"
      point_type: "command"
      measurement: "percentage"
      standard_unit: "percent"
    },
    (buf.validate.field).double.gte = 0.0,
    (buf.validate.field).double.lte = 100.0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The supply air flowrate capacity.
  //
  // Composed of:
  //   supply (descriptor): Measurement or process of media as it is
  //     supplied to the end-use equipment within the system.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   flowrate (measurement): Rate of fluid movement.
  //   capacity (point_type): A design parameter quantity (e.g., design
  //     motor power capacity). Is always a maximum limit.
  //
  // Ontology field: `supply_air_flowrate_capacity`, point type `capacity`.
  // Measured as flowrate; values are in cubic_meters_per_second, the
  // ontology's standard unit for that quantity.
  //
  // Valid range from 0, enforced by protovalidate. Expected range up to
  // 141.584233. This is a data-quality expectation, not a constraint: a
  // reading outside it is a fault to report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double supply_air_flowrate_capacity = 261 [
    (google.api.field_behavior) = OPTIONAL,
    (google.api.field_behavior) = IMMUTABLE,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "supply_air_flowrate_capacity"
      subfields: "supply"
      subfields: "air"
      subfields: "flowrate"
      subfields: "capacity"
      point_type: "capacity"
      measurement: "flowrate"
      standard_unit: "cubic_meters_per_second"
      expected_max: 141.584233
    },
    (buf.validate.field).double.gte = 0.0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The supply air flowrate sensor.
  //
  // Composed of:
  //   supply (descriptor): Measurement or process of media as it is
  //     supplied to the end-use equipment within the system.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   flowrate (measurement): Rate of fluid movement.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `supply_air_flowrate_sensor`, point type `sensor`.
  // Measured as flowrate; values are in cubic_meters_per_second, the
  // ontology's standard unit for that quantity.
  //
  // Expected range -0.002359737216 to 94.38948864. This is a data-quality
  // expectation, not a constraint: a reading outside it is a fault to
  // report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double supply_air_flowrate_sensor = 262 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "supply_air_flowrate_sensor"
      subfields: "supply"
      subfields: "air"
      subfields: "flowrate"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "flowrate"
      standard_unit: "cubic_meters_per_second"
      expected_min: -0.00236
      expected_max: 94.389489
    }
  ];

  // The supply air isolation damper command.
  //
  // Composed of:
  //   supply (descriptor): Measurement or process of media as it is
  //     supplied to the end-use equipment within the system.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   isolation (descriptor): Process of isolating one component or
  //     process from another.
  //   damper (component): Component which meters the flow of air within a
  //     system or device.
  //   command (point_type): The signal given to make an action happen.
  //     Defaults to multistate unless used alongside a measurement_type.
  //
  // Ontology field: `supply_air_isolation_damper_command`, point type
  // `command`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ClosedOpenValue supply_air_isolation_damper_command = 263 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "supply_air_isolation_damper_command"
      subfields: "supply"
      subfields: "air"
      subfields: "isolation"
      subfields: "damper"
      subfields: "command"
      point_type: "command"
    }
  ];

  // The supply air isolation damper status.
  //
  // Composed of:
  //   supply (descriptor): Measurement or process of media as it is
  //     supplied to the end-use equipment within the system.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   isolation (descriptor): Process of isolating one component or
  //     process from another.
  //   damper (component): Component which meters the flow of air within a
  //     system or device.
  //   status (point_type): The multistate value indicating an observed
  //     state in a piece of equipment, often indicating if a command was
  //     effected. It is a neutral observation (i.e., no quality judgment of
  //     'good' or 'bad'). It also has no units of measurement; therefore, if
  //     combined with a measurement subfield, it will indicate that the
  //     field is the directional status based on some measurement of that
  //     type (e.g., power_status equates to an on/off value based on some
  //     inference of power).
  //
  // Ontology field: `supply_air_isolation_damper_status`, point type
  // `status`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ClosedOpenValue supply_air_isolation_damper_status = 264 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "supply_air_isolation_damper_status"
      subfields: "supply"
      subfields: "air"
      subfields: "isolation"
      subfields: "damper"
      subfields: "status"
      point_type: "status"
    }
  ];

  // The supply air relative humidity sensor.
  //
  // Composed of:
  //   supply (descriptor): Measurement or process of media as it is
  //     supplied to the end-use equipment within the system.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   relative (measurement_descriptor): Quality of media with respect to
  //     theoretical minimum or maximum value for a given condition (e.g.,
  //     relative humidity).
  //   humidity (measurement): Concentration of water vapor in air
  //     (conditioned or unconditioned).
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `supply_air_relative_humidity_sensor`, point type
  // `sensor`. Measured as humidity; values are in percent_relative_humidity,
  // the ontology's standard unit for that quantity.
  //
  // Valid range 0 to 100, enforced by protovalidate.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double supply_air_relative_humidity_sensor = 265 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "supply_air_relative_humidity_sensor"
      subfields: "supply"
      subfields: "air"
      subfields: "relative"
      subfields: "humidity"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "humidity"
      standard_unit: "percent_relative_humidity"
    },
    (buf.validate.field).double.gte = 0.0,
    (buf.validate.field).double.lte = 100.0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The supply air static pressure sensor.
  //
  // Composed of:
  //   supply (descriptor): Measurement or process of media as it is
  //     supplied to the end-use equipment within the system.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   static (measurement_descriptor): Describing a system in equilibrium
  //     or at rest (e.g., supply_air_static_pressure_sensor).
  //   pressure (measurement): Measured force per unit area within a system
  //     or process.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `supply_air_static_pressure_sensor`, point type
  // `sensor`. Measured as pressure; values are in pascals, the ontology's
  // standard unit for that quantity.
  //
  // Expected range -124.540012 to 2490.80024. This is a data-quality
  // expectation, not a constraint: a reading outside it is a fault to
  // report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double supply_air_static_pressure_sensor = 266 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "supply_air_static_pressure_sensor"
      subfields: "supply"
      subfields: "air"
      subfields: "static"
      subfields: "pressure"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "pressure"
      standard_unit: "pascals"
      expected_min: -124.540012
      expected_max: 2490.80024
    }
  ];

  // The supply air static pressure setpoint.
  //
  // Composed of:
  //   supply (descriptor): Measurement or process of media as it is
  //     supplied to the end-use equipment within the system.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   static (measurement_descriptor): Describing a system in equilibrium
  //     or at rest (e.g., supply_air_static_pressure_sensor).
  //   pressure (measurement): Measured force per unit area within a system
  //     or process.
  //   setpoint (point_type): Control target of process or system.
  //
  // Ontology field: `supply_air_static_pressure_setpoint`, point type
  // `setpoint`. Measured as pressure; values are in pascals, the ontology's
  // standard unit for that quantity.
  //
  // Expected range -124.540012 to 2490.80024. This is a data-quality
  // expectation, not a constraint: a reading outside it is a fault to
  // report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double supply_air_static_pressure_setpoint = 267 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "supply_air_static_pressure_setpoint"
      subfields: "supply"
      subfields: "air"
      subfields: "static"
      subfields: "pressure"
      subfields: "setpoint"
      point_type: "setpoint"
      measurement: "pressure"
      standard_unit: "pascals"
      expected_min: -124.540012
      expected_max: 2490.80024
    }
  ];

  // The supply air temperature sensor.
  //
  // Composed of:
  //   supply (descriptor): Measurement or process of media as it is
  //     supplied to the end-use equipment within the system.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `supply_air_temperature_sensor`, point type `sensor`.
  // Measured as temperature; values are in kelvin, the ontology's standard
  // unit for that quantity.
  //
  // Expected range 255.9259259 to 338.7037037. This is a data-quality
  // expectation, not a constraint: a reading outside it is a fault to
  // report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double supply_air_temperature_sensor = 268 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "supply_air_temperature_sensor"
      subfields: "supply"
      subfields: "air"
      subfields: "temperature"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "temperature"
      standard_unit: "kelvin"
      expected_min: 255.925926
      expected_max: 338.703704
    }
  ];

  // The supply air temperature setpoint.
  //
  // Composed of:
  //   supply (descriptor): Measurement or process of media as it is
  //     supplied to the end-use equipment within the system.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   setpoint (point_type): Control target of process or system.
  //
  // Ontology field: `supply_air_temperature_setpoint`, point type
  // `setpoint`. Measured as temperature; values are in kelvin, the
  // ontology's standard unit for that quantity.
  //
  // Expected range 255.9259259 to 338.7037037. This is a data-quality
  // expectation, not a constraint: a reading outside it is a fault to
  // report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double supply_air_temperature_setpoint = 269 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "supply_air_temperature_setpoint"
      subfields: "supply"
      subfields: "air"
      subfields: "temperature"
      subfields: "setpoint"
      point_type: "setpoint"
      measurement: "temperature"
      standard_unit: "kelvin"
      expected_min: 255.925926
      expected_max: 338.703704
    }
  ];

  // The supply fan current sensor.
  //
  // Composed of:
  //   supply (descriptor): Measurement or process of media as it is
  //     supplied to the end-use equipment within the system.
  //   fan (component): Component used for the distribution of air.
  //   current (measurement): Flow of electric charge.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `supply_fan_current_sensor`, point type `sensor`.
  // Measured as current; values are in amperes, the ontology's standard unit
  // for that quantity.
  //
  // Valid range from 0, enforced by protovalidate. Expected range up to 100.
  // This is a data-quality expectation, not a constraint: a reading outside
  // it is a fault to report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double supply_fan_current_sensor = 270 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "supply_fan_current_sensor"
      subfields: "supply"
      subfields: "fan"
      subfields: "current"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "current"
      standard_unit: "amperes"
      expected_max: 100.0
    },
    (buf.validate.field).double.gte = 0.0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The supply fan energy accumulator.
  //
  // Composed of:
  //   supply (descriptor): Measurement or process of media as it is
  //     supplied to the end-use equipment within the system.
  //   fan (component): Component used for the distribution of air.
  //   energy (measurement): The quantitative property that must be
  //     transferred to an object to perform work (default electrical unless
  //     otherwise modified).
  //   accumulator (point_type): The total accumulated quantity (e.g.,
  //     total energy accumulated).
  //
  // Ontology field: `supply_fan_energy_accumulator`, point type
  // `accumulator`. Measured as energy; values are in joules, the ontology's
  // standard unit for that quantity.
  //
  // Valid range from 0, enforced by protovalidate. Expected range up to
  // 3.6e+13. This is a data-quality expectation, not a constraint: a reading
  // outside it is a fault to report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double supply_fan_energy_accumulator = 271 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "supply_fan_energy_accumulator"
      subfields: "supply"
      subfields: "fan"
      subfields: "energy"
      subfields: "accumulator"
      point_type: "accumulator"
      measurement: "energy"
      standard_unit: "joules"
      expected_max: 36000000000000.0
    },
    (buf.validate.field).double.gte = 0.0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The supply fan lost power alarm.
  //
  // Composed of:
  //   supply (descriptor): Measurement or process of media as it is
  //     supplied to the end-use equipment within the system.
  //   fan (component): Component used for the distribution of air.
  //   lost (measurement_descriptor): Indicates that a point has lost some
  //     quality or quantity inadvertently.
  //   power (measurement): Rate of energy consumption (assumed context is
  //     electrical and real, unless otherwise modified).
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `supply_fan_lost_power_alarm`, point type `alarm`.
  // Measured as power; values are in watts, the ontology's standard unit for
  // that quantity.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue supply_fan_lost_power_alarm = 272 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "supply_fan_lost_power_alarm"
      subfields: "supply"
      subfields: "fan"
      subfields: "lost"
      subfields: "power"
      subfields: "alarm"
      point_type: "alarm"
      measurement: "power"
      standard_unit: "watts"
    }
  ];

  // The supply fan power capacity.
  //
  // Composed of:
  //   supply (descriptor): Measurement or process of media as it is
  //     supplied to the end-use equipment within the system.
  //   fan (component): Component used for the distribution of air.
  //   power (measurement): Rate of energy consumption (assumed context is
  //     electrical and real, unless otherwise modified).
  //   capacity (point_type): A design parameter quantity (e.g., design
  //     motor power capacity). Is always a maximum limit.
  //
  // Ontology field: `supply_fan_power_capacity`, point type `capacity`.
  // Measured as power; values are in watts, the ontology's standard unit for
  // that quantity.
  //
  // Valid range from 0, enforced by protovalidate. Expected range up to
  // 100000. This is a data-quality expectation, not a constraint: a reading
  // outside it is a fault to report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double supply_fan_power_capacity = 273 [
    (google.api.field_behavior) = OPTIONAL,
    (google.api.field_behavior) = IMMUTABLE,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "supply_fan_power_capacity"
      subfields: "supply"
      subfields: "fan"
      subfields: "power"
      subfields: "capacity"
      point_type: "capacity"
      measurement: "power"
      standard_unit: "watts"
      expected_max: 100000.0
    },
    (buf.validate.field).double.gte = 0.0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The supply fan power sensor.
  //
  // Composed of:
  //   supply (descriptor): Measurement or process of media as it is
  //     supplied to the end-use equipment within the system.
  //   fan (component): Component used for the distribution of air.
  //   power (measurement): Rate of energy consumption (assumed context is
  //     electrical and real, unless otherwise modified).
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `supply_fan_power_sensor`, point type `sensor`. Measured
  // as power; values are in watts, the ontology's standard unit for that
  // quantity.
  //
  // Expected range 0 to 100000. This is a data-quality expectation, not a
  // constraint: a reading outside it is a fault to report, not a message to
  // reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double supply_fan_power_sensor = 274 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "supply_fan_power_sensor"
      subfields: "supply"
      subfields: "fan"
      subfields: "power"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "power"
      standard_unit: "watts"
      expected_min: 0.0
      expected_max: 100000.0
    }
  ];

  // The supply fan power status.
  //
  // Composed of:
  //   supply (descriptor): Measurement or process of media as it is
  //     supplied to the end-use equipment within the system.
  //   fan (component): Component used for the distribution of air.
  //   power (measurement): Rate of energy consumption (assumed context is
  //     electrical and real, unless otherwise modified).
  //   status (point_type): The multistate value indicating an observed
  //     state in a piece of equipment, often indicating if a command was
  //     effected. It is a neutral observation (i.e., no quality judgment of
  //     'good' or 'bad'). It also has no units of measurement; therefore, if
  //     combined with a measurement subfield, it will indicate that the
  //     field is the directional status based on some measurement of that
  //     type (e.g., power_status equates to an on/off value based on some
  //     inference of power).
  //
  // Ontology field: `supply_fan_power_status`, point type `status`. Measured
  // as power; values are in watts, the ontology's standard unit for that
  // quantity.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional OffOnValue supply_fan_power_status = 275 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "supply_fan_power_status"
      subfields: "supply"
      subfields: "fan"
      subfields: "power"
      subfields: "status"
      point_type: "status"
      measurement: "power"
      standard_unit: "watts"
    }
  ];

  // The supply fan run command.
  //
  // Composed of:
  //   supply (descriptor): Measurement or process of media as it is
  //     supplied to the end-use equipment within the system.
  //   fan (component): Component used for the distribution of air.
  //   run (measurement_descriptor): State of being active or operating. By
  //     default applies to control program for system (e.g., VAV program
  //     run_command).
  //   command (point_type): The signal given to make an action happen.
  //     Defaults to multistate unless used alongside a measurement_type.
  //
  // Ontology field: `supply_fan_run_command`, point type `command`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional OffOnValue supply_fan_run_command = 276 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "supply_fan_run_command"
      subfields: "supply"
      subfields: "fan"
      subfields: "run"
      subfields: "command"
      point_type: "command"
    }
  ];

  // The supply fan run mode.
  //
  // Composed of:
  //   supply (descriptor): Measurement or process of media as it is
  //     supplied to the end-use equipment within the system.
  //   fan (component): Component used for the distribution of air.
  //   run (measurement_descriptor): State of being active or operating. By
  //     default applies to control program for system (e.g., VAV program
  //     run_command).
  //   mode (point_type): Distinct mode of operation within system. For
  //     example economizer_mode has modes of 'enabled' or 'disabled.'
  //
  // Ontology field: `supply_fan_run_mode`, point type `mode`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional AutoManualValue supply_fan_run_mode = 277 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "supply_fan_run_mode"
      subfields: "supply"
      subfields: "fan"
      subfields: "run"
      subfields: "mode"
      point_type: "mode"
    }
  ];

  // The supply fan run status.
  //
  // Composed of:
  //   supply (descriptor): Measurement or process of media as it is
  //     supplied to the end-use equipment within the system.
  //   fan (component): Component used for the distribution of air.
  //   run (measurement_descriptor): State of being active or operating. By
  //     default applies to control program for system (e.g., VAV program
  //     run_command).
  //   status (point_type): The multistate value indicating an observed
  //     state in a piece of equipment, often indicating if a command was
  //     effected. It is a neutral observation (i.e., no quality judgment of
  //     'good' or 'bad'). It also has no units of measurement; therefore, if
  //     combined with a measurement subfield, it will indicate that the
  //     field is the directional status based on some measurement of that
  //     type (e.g., power_status equates to an on/off value based on some
  //     inference of power).
  //
  // Ontology field: `supply_fan_run_status`, point type `status`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional OffOnUnknownValue supply_fan_run_status = 278 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "supply_fan_run_status"
      subfields: "supply"
      subfields: "fan"
      subfields: "run"
      subfields: "status"
      point_type: "status"
    }
  ];

  // The supply fan run time accumulator.
  //
  // Composed of:
  //   supply (descriptor): Measurement or process of media as it is
  //     supplied to the end-use equipment within the system.
  //   fan (component): Component used for the distribution of air.
  //   run (measurement_descriptor): State of being active or operating. By
  //     default applies to control program for system (e.g., VAV program
  //     run_command).
  //   time (measurement): Defined duration of something.
  //   accumulator (point_type): The total accumulated quantity (e.g.,
  //     total energy accumulated).
  //
  // Ontology field: `supply_fan_run_time_accumulator`, point type
  // `accumulator`. Measured as time; values are in seconds, the ontology's
  // standard unit for that quantity.
  //
  // Valid range from 0, enforced by protovalidate. Expected range up to
  // 1.25. This is a data-quality expectation, not a constraint: a reading
  // outside it is a fault to report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double supply_fan_run_time_accumulator = 279 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "supply_fan_run_time_accumulator"
      subfields: "supply"
      subfields: "fan"
      subfields: "run"
      subfields: "time"
      subfields: "accumulator"
      point_type: "accumulator"
      measurement: "time"
      standard_unit: "seconds"
      expected_max: 1.25
    },
    (buf.validate.field).double.gte = 0.0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The supply fan speed frequency sensor.
  //
  // Composed of:
  //   supply (descriptor): Measurement or process of media as it is
  //     supplied to the end-use equipment within the system.
  //   fan (component): Component used for the distribution of air.
  //   speed (measurement_descriptor): Numeric setting of how fast to run a
  //     device, in the specified units. Typically used to describe
  //     revolutions of a motor as a fraction of nominal or maximum.
  //   frequency (measurement): The rate of cycling within a process.
  //     Typically used to describe voltage (and amperage) oscillation within
  //     AC power distribution components.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `supply_fan_speed_frequency_sensor`, point type
  // `sensor`. Measured as frequency; values are in hertz, the ontology's
  // standard unit for that quantity.
  //
  // Expected range -0.5 to 65. This is a data-quality expectation, not a
  // constraint: a reading outside it is a fault to report, not a message to
  // reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double supply_fan_speed_frequency_sensor = 280 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "supply_fan_speed_frequency_sensor"
      subfields: "supply"
      subfields: "fan"
      subfields: "speed"
      subfields: "frequency"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "frequency"
      standard_unit: "hertz"
      expected_min: -0.5
      expected_max: 65.0
    }
  ];

  // The supply fan speed mode.
  //
  // Composed of:
  //   supply (descriptor): Measurement or process of media as it is
  //     supplied to the end-use equipment within the system.
  //   fan (component): Component used for the distribution of air.
  //   speed (measurement_descriptor): Numeric setting of how fast to run a
  //     device, in the specified units. Typically used to describe
  //     revolutions of a motor as a fraction of nominal or maximum.
  //   mode (point_type): Distinct mode of operation within system. For
  //     example economizer_mode has modes of 'enabled' or 'disabled.'
  //
  // Ontology field: `supply_fan_speed_mode`, point type `mode`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional BypassVfdValue supply_fan_speed_mode = 281 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "supply_fan_speed_mode"
      subfields: "supply"
      subfields: "fan"
      subfields: "speed"
      subfields: "mode"
      point_type: "mode"
    }
  ];

  // The supply fan speed percentage command.
  //
  // Composed of:
  //   supply (descriptor): Measurement or process of media as it is
  //     supplied to the end-use equipment within the system.
  //   fan (component): Component used for the distribution of air.
  //   speed (measurement_descriptor): Numeric setting of how fast to run a
  //     device, in the specified units. Typically used to describe
  //     revolutions of a motor as a fraction of nominal or maximum.
  //   percentage (measurement): Measurement per hundred.
  //   command (point_type): The signal given to make an action happen.
  //     Defaults to multistate unless used alongside a measurement_type.
  //
  // Ontology field: `supply_fan_speed_percentage_command`, point type
  // `command`. Measured as percentage; values are in percent, the ontology's
  // standard unit for that quantity.
  //
  // Valid range 0 to 100, enforced by protovalidate.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double supply_fan_speed_percentage_command = 282 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "supply_fan_speed_percentage_command"
      subfields: "supply"
      subfields: "fan"
      subfields: "speed"
      subfields: "percentage"
      subfields: "command"
      point_type: "command"
      measurement: "percentage"
      standard_unit: "percent"
    },
    (buf.validate.field).double.gte = 0.0,
    (buf.validate.field).double.lte = 100.0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The supply fan speed percentage sensor.
  //
  // Composed of:
  //   supply (descriptor): Measurement or process of media as it is
  //     supplied to the end-use equipment within the system.
  //   fan (component): Component used for the distribution of air.
  //   speed (measurement_descriptor): Numeric setting of how fast to run a
  //     device, in the specified units. Typically used to describe
  //     revolutions of a motor as a fraction of nominal or maximum.
  //   percentage (measurement): Measurement per hundred.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `supply_fan_speed_percentage_sensor`, point type
  // `sensor`. Measured as percentage; values are in percent, the ontology's
  // standard unit for that quantity.
  //
  // Valid range 0 to 100, enforced by protovalidate.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double supply_fan_speed_percentage_sensor = 283 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "supply_fan_speed_percentage_sensor"
      subfields: "supply"
      subfields: "fan"
      subfields: "speed"
      subfields: "percentage"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "percentage"
      standard_unit: "percent"
    },
    (buf.validate.field).double.gte = 0.0,
    (buf.validate.field).double.lte = 100.0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The supply fan torque sensor.
  //
  // Composed of:
  //   supply (descriptor): Measurement or process of media as it is
  //     supplied to the end-use equipment within the system.
  //   fan (component): Component used for the distribution of air.
  //   torque (measurement): A force that causes rotation around an axis.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `supply_fan_torque_sensor`, point type `sensor`.
  // Measured as torque; values are in newton_meters, the ontology's standard
  // unit for that quantity.
  //
  // Valid range from 0, enforced by protovalidate. Expected range up to 678.
  // This is a data-quality expectation, not a constraint: a reading outside
  // it is a fault to report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double supply_fan_torque_sensor = 284 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "supply_fan_torque_sensor"
      subfields: "supply"
      subfields: "fan"
      subfields: "torque"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "torque"
      standard_unit: "newton_meters"
      expected_max: 678.0
    },
    (buf.validate.field).double.gte = 0.0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The supply fan voltage sensor.
  //
  // Composed of:
  //   supply (descriptor): Measurement or process of media as it is
  //     supplied to the end-use equipment within the system.
  //   fan (component): Component used for the distribution of air.
  //   voltage (measurement): Electrical potential difference between two
  //     reference points.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `supply_fan_voltage_sensor`, point type `sensor`.
  // Measured as voltage; values are in volts, the ontology's standard unit
  // for that quantity.
  //
  // Expected range 0 to 500. This is a data-quality expectation, not a
  // constraint: a reading outside it is a fault to report, not a message to
  // reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double supply_fan_voltage_sensor = 285 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "supply_fan_voltage_sensor"
      subfields: "supply"
      subfields: "fan"
      subfields: "voltage"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "voltage"
      standard_unit: "volts"
      expected_min: 0.0
      expected_max: 500.0
    }
  ];

  // The supply water temperature sensor.
  //
  // Composed of:
  //   supply (descriptor): Measurement or process of media as it is
  //     supplied to the end-use equipment within the system.
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `supply_water_temperature_sensor`, point type `sensor`.
  // Measured as temperature; values are in kelvin, the ontology's standard
  // unit for that quantity.
  //
  // Expected range 255.9259259 to 380.3703704. This is a data-quality
  // expectation, not a constraint: a reading outside it is a fault to
  // report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double supply_water_temperature_sensor = 286 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "supply_water_temperature_sensor"
      subfields: "supply"
      subfields: "water"
      subfields: "temperature"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "temperature"
      standard_unit: "kelvin"
      expected_min: 255.925926
      expected_max: 380.37037
    }
  ];

  // The switch command.
  //
  // Composed of:
  //   switch (component): Component to disrupt an electrical cirucit.
  //   command (point_type): The signal given to make an action happen.
  //     Defaults to multistate unless used alongside a measurement_type.
  //
  // Ontology field: `switch_command`, point type `command`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional OffOnValue switch_command = 287 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "switch_command"
      subfields: "switch"
      subfields: "command"
      point_type: "command"
    }
  ];

  // The ultraviolet irradiance sensor.
  //
  // Composed of:
  //   ultraviolet (descriptor): A short wavelength type of light that is
  //     beyond the visible spectrum. It is damaging to DNA and cells, and is
  //     commonly used to sterilize surfaces.
  //   irradiance (measurement): The flux of radiant energy per unit area
  //     (normal to the direction of flow of radiant energy through a
  //     medium).
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `ultraviolet_irradiance_sensor`, point type `sensor`.
  // Measured as irradiance; values are in watts_per_square_meter_irradiance,
  // the ontology's standard unit for that quantity.
  //
  // Valid range from 0, enforced by protovalidate. Expected range up to
  // 2000. This is a data-quality expectation, not a constraint: a reading
  // outside it is a fault to report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double ultraviolet_irradiance_sensor = 288 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "ultraviolet_irradiance_sensor"
      subfields: "ultraviolet"
      subfields: "irradiance"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "irradiance"
      standard_unit: "watts_per_square_meter_irradiance"
      expected_max: 2000.0
    },
    (buf.validate.field).double.gte = 0.0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The user occupancy override status.
  //
  // Composed of:
  //   user (descriptor): A person who uses or operates something.
  //   occupancy (descriptor): State of being occupied.
  //   override (descriptor): An action that interrupts, cancels, or
  //     changes the current action or status.
  //   status (point_type): The multistate value indicating an observed
  //     state in a piece of equipment, often indicating if a command was
  //     effected. It is a neutral observation (i.e., no quality judgment of
  //     'good' or 'bad'). It also has no units of measurement; therefore, if
  //     combined with a measurement subfield, it will indicate that the
  //     field is the directional status based on some measurement of that
  //     type (e.g., power_status equates to an on/off value based on some
  //     inference of power).
  //
  // Ontology field: `user_occupancy_override_status`, point type `status`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional DisabledEnabledOccupiedUnoccupiedValue user_occupancy_override_status = 289 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "user_occupancy_override_status"
      subfields: "user"
      subfields: "occupancy"
      subfields: "override"
      subfields: "status"
      point_type: "status"
    }
  ];

  // The ventilation request count.
  //
  // Composed of:
  //   ventilation (descriptor): Process used to provide fresh (typically
  //     atmospheric) air into a system or zone.
  //   request (descriptor): A signal from a system device that calls for
  //     more of a particular conditioning process (e.g., pressure requests
  //     from terminal units).
  //   count (point_type): Total count of actions or requests.
  //
  // Ontology field: `ventilation_request_count`, point type `count`.
  // Dimensionless: the ontology defines this point type as an integer count
  // rather than a measured quantity.
  //
  // Valid range from 0, enforced by protovalidate. Expected range up to 500.
  // This is a data-quality expectation, not a constraint: a reading outside
  // it is a fault to report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional int64 ventilation_request_count = 290 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "ventilation_request_count"
      subfields: "ventilation"
      subfields: "request"
      subfields: "count"
      point_type: "count"
      expected_max: 500.0
    },
    (buf.validate.field).int64.gte = 0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The voltage sensor.
  //
  // Composed of:
  //   voltage (measurement): Electrical potential difference between two
  //     reference points.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `voltage_sensor`, point type `sensor`. Measured as
  // voltage; values are in volts, the ontology's standard unit for that
  // quantity.
  //
  // Expected range 0 to 1000. This is a data-quality expectation, not a
  // constraint: a reading outside it is a fault to report, not a message to
  // reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double voltage_sensor = 291 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "voltage_sensor"
      subfields: "voltage"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "voltage"
      standard_unit: "volts"
      expected_min: 0.0
      expected_max: 1000.0
    }
  ];

  // The warmup request count.
  //
  // Composed of:
  //   warmup (descriptor): Process, measured effect or requirement for
  //     heating; used following building unoccupied periods and in optimal
  //     start applications.
  //   request (descriptor): A signal from a system device that calls for
  //     more of a particular conditioning process (e.g., pressure requests
  //     from terminal units).
  //   count (point_type): Total count of actions or requests.
  //
  // Ontology field: `warmup_request_count`, point type `count`.
  // Dimensionless: the ontology defines this point type as an integer count
  // rather than a measured quantity.
  //
  // Valid range from 0, enforced by protovalidate. Expected range up to 500.
  // This is a data-quality expectation, not a constraint: a reading outside
  // it is a fault to report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional int64 warmup_request_count = 292 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "warmup_request_count"
      subfields: "warmup"
      subfields: "request"
      subfields: "count"
      point_type: "count"
      expected_max: 500.0
    },
    (buf.validate.field).int64.gte = 0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The warmup run command.
  //
  // Composed of:
  //   warmup (descriptor): Process, measured effect or requirement for
  //     heating; used following building unoccupied periods and in optimal
  //     start applications.
  //   run (measurement_descriptor): State of being active or operating. By
  //     default applies to control program for system (e.g., VAV program
  //     run_command).
  //   command (point_type): The signal given to make an action happen.
  //     Defaults to multistate unless used alongside a measurement_type.
  //
  // Ontology field: `warmup_run_command`, point type `command`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional OffOnValue warmup_run_command = 293 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "warmup_run_command"
      subfields: "warmup"
      subfields: "run"
      subfields: "command"
      point_type: "command"
    }
  ];

  // The water leak alarm.
  //
  // Composed of:
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   leak (descriptor): Media leaving the system abnormally.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `water_leak_alarm`, point type `alarm`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue water_leak_alarm = 294 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "water_leak_alarm"
      subfields: "water"
      subfields: "leak"
      subfields: "alarm"
      point_type: "alarm"
    }
  ];

  // The wind direction angle sensor.
  //
  // Composed of:
  //   wind (descriptor): Movement of ambient air.
  //   direction (descriptor): Direction in which particular entity is
  //     moving or is present.
  //   angle (measurement): The figure formed by two rays, sharing a common
  //     endpoint.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `wind_direction_angle_sensor`, point type `sensor`.
  // Measured as angle; values are in degrees, the ontology's standard unit
  // for that quantity.
  //
  // Valid range 0 to 360, enforced by protovalidate.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double wind_direction_angle_sensor = 295 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "wind_direction_angle_sensor"
      subfields: "wind"
      subfields: "direction"
      subfields: "angle"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "angle"
      standard_unit: "degrees"
    },
    (buf.validate.field).double.gte = 0.0,
    (buf.validate.field).double.lte = 360.0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The wind linearvelocity sensor.
  //
  // Composed of:
  //   wind (descriptor): Movement of ambient air.
  //   linearvelocity (measurement): The rate of change of displacement
  //     with respect to time. For example, wind speed represents the linear
  //     velocity of the wind.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `wind_linearvelocity_sensor`, point type `sensor`.
  // Measured as linearvelocity; values are in meters_per_second, the
  // ontology's standard unit for that quantity.
  //
  // Expected range 0 to 100. This is a data-quality expectation, not a
  // constraint: a reading outside it is a fault to report, not a message to
  // reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double wind_linearvelocity_sensor = 296 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "wind_linearvelocity_sensor"
      subfields: "wind"
      subfields: "linearvelocity"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "linearvelocity"
      standard_unit: "meters_per_second"
      expected_min: 0.0
      expected_max: 100.0
    }
  ];

  // The zone air co2 concentration sensor.
  //
  // Composed of:
  //   zone (descriptor): Region of building which is conditioned.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   co2 (descriptor): Chemical formula and abbreviation for carbon
  //     dioxide.
  //   concentration (measurement): Concentration of something relative to
  //     something else (usually in parts per million or parts per billion).
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `zone_air_co2_concentration_sensor`, point type
  // `sensor`. Measured as concentration; values are in parts_per_unit, the
  // ontology's standard unit for that quantity.
  //
  // Expected range 5e-05 to 0.005. This is a data-quality expectation, not a
  // constraint: a reading outside it is a fault to report, not a message to
  // reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double zone_air_co2_concentration_sensor = 328 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "zone_air_co2_concentration_sensor"
      subfields: "zone"
      subfields: "air"
      subfields: "co2"
      subfields: "concentration"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "concentration"
      standard_unit: "parts_per_unit"
      expected_min: 0.00005
      expected_max: 0.005
    }
  ];

  // The zone air co2 concentration setpoint.
  //
  // Composed of:
  //   zone (descriptor): Region of building which is conditioned.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   co2 (descriptor): Chemical formula and abbreviation for carbon
  //     dioxide.
  //   concentration (measurement): Concentration of something relative to
  //     something else (usually in parts per million or parts per billion).
  //   setpoint (point_type): Control target of process or system.
  //
  // Ontology field: `zone_air_co2_concentration_setpoint`, point type
  // `setpoint`. Measured as concentration; values are in parts_per_unit, the
  // ontology's standard unit for that quantity.
  //
  // Expected range 5e-05 to 0.005. This is a data-quality expectation, not a
  // constraint: a reading outside it is a fault to report, not a message to
  // reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double zone_air_co2_concentration_setpoint = 329 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "zone_air_co2_concentration_setpoint"
      subfields: "zone"
      subfields: "air"
      subfields: "co2"
      subfields: "concentration"
      subfields: "setpoint"
      point_type: "setpoint"
      measurement: "concentration"
      standard_unit: "parts_per_unit"
      expected_min: 0.00005
      expected_max: 0.005
    }
  ];

  // The zone air co2 deadband concentration setpoint.
  //
  // Composed of:
  //   zone (descriptor): Region of building which is conditioned.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   co2 (descriptor): Chemical formula and abbreviation for carbon
  //     dioxide.
  //   deadband (measurement_descriptor): Represents a range in which the
  //     controller does not do anything.
  //   concentration (measurement): Concentration of something relative to
  //     something else (usually in parts per million or parts per billion).
  //   setpoint (point_type): Control target of process or system.
  //
  // Ontology field: `zone_air_co2_deadband_concentration_setpoint`, point
  // type `setpoint`. Measured as concentration; values are in
  // parts_per_unit, the ontology's standard unit for that quantity.
  //
  // Expected range 5e-05 to 0.005. This is a data-quality expectation, not a
  // constraint: a reading outside it is a fault to report, not a message to
  // reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double zone_air_co2_deadband_concentration_setpoint = 330 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "zone_air_co2_deadband_concentration_setpoint"
      subfields: "zone"
      subfields: "air"
      subfields: "co2"
      subfields: "deadband"
      subfields: "concentration"
      subfields: "setpoint"
      point_type: "setpoint"
      measurement: "concentration"
      standard_unit: "parts_per_unit"
      expected_min: 0.00005
      expected_max: 0.005
    }
  ];

  // The zone air cooling temperature setpoint.
  //
  // Composed of:
  //   zone (descriptor): Region of building which is conditioned.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   cooling (descriptor): Process, measured effect or requirement for
  //     cooling.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   setpoint (point_type): Control target of process or system.
  //
  // Ontology field: `zone_air_cooling_temperature_setpoint`, point type
  // `setpoint`. Measured as temperature; values are in kelvin, the
  // ontology's standard unit for that quantity.
  //
  // Expected range 255.9259259 to 308.1481481. This is a data-quality
  // expectation, not a constraint: a reading outside it is a fault to
  // report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double zone_air_cooling_temperature_setpoint = 300 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "zone_air_cooling_temperature_setpoint"
      subfields: "zone"
      subfields: "air"
      subfields: "cooling"
      subfields: "temperature"
      subfields: "setpoint"
      point_type: "setpoint"
      measurement: "temperature"
      standard_unit: "kelvin"
      expected_min: 255.925926
      expected_max: 308.148148
    }
  ];

  // The zone air deadband temperature setpoint.
  //
  // Composed of:
  //   zone (descriptor): Region of building which is conditioned.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   deadband (measurement_descriptor): Represents a range in which the
  //     controller does not do anything.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   setpoint (point_type): Control target of process or system.
  //
  // Ontology field: `zone_air_deadband_temperature_setpoint`, point type
  // `setpoint`. Measured as temperature; values are in kelvin, the
  // ontology's standard unit for that quantity.
  //
  // Expected range 255.9259259 to 308.1481481. This is a data-quality
  // expectation, not a constraint: a reading outside it is a fault to
  // report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double zone_air_deadband_temperature_setpoint = 301 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "zone_air_deadband_temperature_setpoint"
      subfields: "zone"
      subfields: "air"
      subfields: "deadband"
      subfields: "temperature"
      subfields: "setpoint"
      point_type: "setpoint"
      measurement: "temperature"
      standard_unit: "kelvin"
      expected_min: 255.925926
      expected_max: 308.148148
    }
  ];

  // The zone air dehumidification relative humidity setpoint.
  //
  // Composed of:
  //   zone (descriptor): Region of building which is conditioned.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   dehumidification (descriptor): Process of removing moisture from
  //     air.
  //   relative (measurement_descriptor): Quality of media with respect to
  //     theoretical minimum or maximum value for a given condition (e.g.,
  //     relative humidity).
  //   humidity (measurement): Concentration of water vapor in air
  //     (conditioned or unconditioned).
  //   setpoint (point_type): Control target of process or system.
  //
  // Ontology field: `zone_air_dehumidification_relative_humidity_setpoint`,
  // point type `setpoint`. Measured as humidity; values are in
  // percent_relative_humidity, the ontology's standard unit for that
  // quantity.
  //
  // Valid range 0 to 100, enforced by protovalidate.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double zone_air_dehumidification_relative_humidity_setpoint = 302 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "zone_air_dehumidification_relative_humidity_setpoint"
      subfields: "zone"
      subfields: "air"
      subfields: "dehumidification"
      subfields: "relative"
      subfields: "humidity"
      subfields: "setpoint"
      point_type: "setpoint"
      measurement: "humidity"
      standard_unit: "percent_relative_humidity"
    },
    (buf.validate.field).double.gte = 0.0,
    (buf.validate.field).double.lte = 100.0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The zone air dewpoint temperature sensor.
  //
  // Composed of:
  //   zone (descriptor): Region of building which is conditioned.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   dewpoint (measurement_descriptor): The thermodynamic point at which
  //     water condenses from standing air.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `zone_air_dewpoint_temperature_sensor`, point type
  // `sensor`. Measured as temperature; values are in kelvin, the ontology's
  // standard unit for that quantity.
  //
  // Expected range 255.9259259 to 310.9259259. This is a data-quality
  // expectation, not a constraint: a reading outside it is a fault to
  // report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double zone_air_dewpoint_temperature_sensor = 303 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "zone_air_dewpoint_temperature_sensor"
      subfields: "zone"
      subfields: "air"
      subfields: "dewpoint"
      subfields: "temperature"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "temperature"
      standard_unit: "kelvin"
      expected_min: 255.925926
      expected_max: 310.925926
    }
  ];

  // The zone air dewpoint temperature setpoint.
  //
  // Composed of:
  //   zone (descriptor): Region of building which is conditioned.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   dewpoint (measurement_descriptor): The thermodynamic point at which
  //     water condenses from standing air.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   setpoint (point_type): Control target of process or system.
  //
  // Ontology field: `zone_air_dewpoint_temperature_setpoint`, point type
  // `setpoint`. Measured as temperature; values are in kelvin, the
  // ontology's standard unit for that quantity.
  //
  // Expected range 255.9259259 to 310.9259259. This is a data-quality
  // expectation, not a constraint: a reading outside it is a fault to
  // report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double zone_air_dewpoint_temperature_setpoint = 304 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "zone_air_dewpoint_temperature_setpoint"
      subfields: "zone"
      subfields: "air"
      subfields: "dewpoint"
      subfields: "temperature"
      subfields: "setpoint"
      point_type: "setpoint"
      measurement: "temperature"
      standard_unit: "kelvin"
      expected_min: 255.925926
      expected_max: 310.925926
    }
  ];

  // The zone air heating temperature setpoint.
  //
  // Composed of:
  //   zone (descriptor): Region of building which is conditioned.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   heating (descriptor): Process, measured effect or requirement for
  //     heating.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   setpoint (point_type): Control target of process or system.
  //
  // Ontology field: `zone_air_heating_temperature_setpoint`, point type
  // `setpoint`. Measured as temperature; values are in kelvin, the
  // ontology's standard unit for that quantity.
  //
  // Expected range 255.9259259 to 308.1481481. This is a data-quality
  // expectation, not a constraint: a reading outside it is a fault to
  // report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double zone_air_heating_temperature_setpoint = 305 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "zone_air_heating_temperature_setpoint"
      subfields: "zone"
      subfields: "air"
      subfields: "heating"
      subfields: "temperature"
      subfields: "setpoint"
      point_type: "setpoint"
      measurement: "temperature"
      standard_unit: "kelvin"
      expected_min: 255.925926
      expected_max: 308.148148
    }
  ];

  // The zone air humidification relative humidity setpoint.
  //
  // Composed of:
  //   zone (descriptor): Region of building which is conditioned.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   humidification (descriptor): Process of adding moisture to air.
  //   relative (measurement_descriptor): Quality of media with respect to
  //     theoretical minimum or maximum value for a given condition (e.g.,
  //     relative humidity).
  //   humidity (measurement): Concentration of water vapor in air
  //     (conditioned or unconditioned).
  //   setpoint (point_type): Control target of process or system.
  //
  // Ontology field: `zone_air_humidification_relative_humidity_setpoint`,
  // point type `setpoint`. Measured as humidity; values are in
  // percent_relative_humidity, the ontology's standard unit for that
  // quantity.
  //
  // Valid range 0 to 100, enforced by protovalidate.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double zone_air_humidification_relative_humidity_setpoint = 306 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "zone_air_humidification_relative_humidity_setpoint"
      subfields: "zone"
      subfields: "air"
      subfields: "humidification"
      subfields: "relative"
      subfields: "humidity"
      subfields: "setpoint"
      point_type: "setpoint"
      measurement: "humidity"
      standard_unit: "percent_relative_humidity"
    },
    (buf.validate.field).double.gte = 0.0,
    (buf.validate.field).double.lte = 100.0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The zone air occupied cooling temperature setpoint.
  //
  // Composed of:
  //   zone (descriptor): Region of building which is conditioned.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   occupied (descriptor): State of being within scheduled (time of day
  //     or week) run-time parameters.
  //   cooling (descriptor): Process, measured effect or requirement for
  //     cooling.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   setpoint (point_type): Control target of process or system.
  //
  // Ontology field: `zone_air_occupied_cooling_temperature_setpoint`, point
  // type `setpoint`. Measured as temperature; values are in kelvin, the
  // ontology's standard unit for that quantity.
  //
  // Expected range 290 to 305. This is a data-quality expectation, not a
  // constraint: a reading outside it is a fault to report, not a message to
  // reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double zone_air_occupied_cooling_temperature_setpoint = 307 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "zone_air_occupied_cooling_temperature_setpoint"
      subfields: "zone"
      subfields: "air"
      subfields: "occupied"
      subfields: "cooling"
      subfields: "temperature"
      subfields: "setpoint"
      point_type: "setpoint"
      measurement: "temperature"
      standard_unit: "kelvin"
      expected_min: 290.0
      expected_max: 305.0
    }
  ];

  // The zone air occupied heating temperature setpoint.
  //
  // Composed of:
  //   zone (descriptor): Region of building which is conditioned.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   occupied (descriptor): State of being within scheduled (time of day
  //     or week) run-time parameters.
  //   heating (descriptor): Process, measured effect or requirement for
  //     heating.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   setpoint (point_type): Control target of process or system.
  //
  // Ontology field: `zone_air_occupied_heating_temperature_setpoint`, point
  // type `setpoint`. Measured as temperature; values are in kelvin, the
  // ontology's standard unit for that quantity.
  //
  // Expected range 255.9259259 to 308.1481481. This is a data-quality
  // expectation, not a constraint: a reading outside it is a fault to
  // report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double zone_air_occupied_heating_temperature_setpoint = 308 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "zone_air_occupied_heating_temperature_setpoint"
      subfields: "zone"
      subfields: "air"
      subfields: "occupied"
      subfields: "heating"
      subfields: "temperature"
      subfields: "setpoint"
      point_type: "setpoint"
      measurement: "temperature"
      standard_unit: "kelvin"
      expected_min: 255.925926
      expected_max: 308.148148
    }
  ];

  // The zone air relative humidity sensor.
  //
  // Composed of:
  //   zone (descriptor): Region of building which is conditioned.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   relative (measurement_descriptor): Quality of media with respect to
  //     theoretical minimum or maximum value for a given condition (e.g.,
  //     relative humidity).
  //   humidity (measurement): Concentration of water vapor in air
  //     (conditioned or unconditioned).
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `zone_air_relative_humidity_sensor`, point type
  // `sensor`. Measured as humidity; values are in percent_relative_humidity,
  // the ontology's standard unit for that quantity.
  //
  // Valid range 0 to 100, enforced by protovalidate.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double zone_air_relative_humidity_sensor = 309 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "zone_air_relative_humidity_sensor"
      subfields: "zone"
      subfields: "air"
      subfields: "relative"
      subfields: "humidity"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "humidity"
      standard_unit: "percent_relative_humidity"
    },
    (buf.validate.field).double.gte = 0.0,
    (buf.validate.field).double.lte = 100.0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The zone air relative humidity setpoint.
  //
  // Composed of:
  //   zone (descriptor): Region of building which is conditioned.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   relative (measurement_descriptor): Quality of media with respect to
  //     theoretical minimum or maximum value for a given condition (e.g.,
  //     relative humidity).
  //   humidity (measurement): Concentration of water vapor in air
  //     (conditioned or unconditioned).
  //   setpoint (point_type): Control target of process or system.
  //
  // Ontology field: `zone_air_relative_humidity_setpoint`, point type
  // `setpoint`. Measured as humidity; values are in
  // percent_relative_humidity, the ontology's standard unit for that
  // quantity.
  //
  // Valid range 0 to 100, enforced by protovalidate.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double zone_air_relative_humidity_setpoint = 310 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "zone_air_relative_humidity_setpoint"
      subfields: "zone"
      subfields: "air"
      subfields: "relative"
      subfields: "humidity"
      subfields: "setpoint"
      point_type: "setpoint"
      measurement: "humidity"
      standard_unit: "percent_relative_humidity"
    },
    (buf.validate.field).double.gte = 0.0,
    (buf.validate.field).double.lte = 100.0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The zone air specificenthalpy sensor.
  //
  // Composed of:
  //   zone (descriptor): Region of building which is conditioned.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   specificenthalpy (measurement): Measure of energy of air per unit
  //     mass.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `zone_air_specificenthalpy_sensor`, point type `sensor`.
  // Measured as specificenthalpy; values are in joules_per_kilogram, the
  // ontology's standard unit for that quantity.
  //
  // Expected range 11630 to 93040. This is a data-quality expectation, not a
  // constraint: a reading outside it is a fault to report, not a message to
  // reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double zone_air_specificenthalpy_sensor = 311 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "zone_air_specificenthalpy_sensor"
      subfields: "zone"
      subfields: "air"
      subfields: "specificenthalpy"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "specificenthalpy"
      standard_unit: "joules_per_kilogram"
      expected_min: 11630.0
      expected_max: 93040.0
    }
  ];

  // The zone air temperature sensor.
  //
  // Composed of:
  //   zone (descriptor): Region of building which is conditioned.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `zone_air_temperature_sensor`, point type `sensor`.
  // Measured as temperature; values are in kelvin, the ontology's standard
  // unit for that quantity.
  //
  // Expected range 255.9259259 to 322.037037. This is a data-quality
  // expectation, not a constraint: a reading outside it is a fault to
  // report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double zone_air_temperature_sensor = 312 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "zone_air_temperature_sensor"
      subfields: "zone"
      subfields: "air"
      subfields: "temperature"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "temperature"
      standard_unit: "kelvin"
      expected_min: 255.925926
      expected_max: 322.037037
    }
  ];

  // The zone air temperature setpoint.
  //
  // Composed of:
  //   zone (descriptor): Region of building which is conditioned.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   setpoint (point_type): Control target of process or system.
  //
  // Ontology field: `zone_air_temperature_setpoint`, point type `setpoint`.
  // Measured as temperature; values are in kelvin, the ontology's standard
  // unit for that quantity.
  //
  // Expected range 255.9259259 to 308.1481481. This is a data-quality
  // expectation, not a constraint: a reading outside it is a fault to
  // report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double zone_air_temperature_setpoint = 313 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "zone_air_temperature_setpoint"
      subfields: "zone"
      subfields: "air"
      subfields: "temperature"
      subfields: "setpoint"
      point_type: "setpoint"
      measurement: "temperature"
      standard_unit: "kelvin"
      expected_min: 255.925926
      expected_max: 308.148148
    }
  ];

  // The zone air unoccupied cooling temperature setpoint.
  //
  // Composed of:
  //   zone (descriptor): Region of building which is conditioned.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   unoccupied (descriptor): State of being outside of scheduled (time
  //     of day or week) run-time parameters.
  //   cooling (descriptor): Process, measured effect or requirement for
  //     cooling.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   setpoint (point_type): Control target of process or system.
  //
  // Ontology field: `zone_air_unoccupied_cooling_temperature_setpoint`,
  // point type `setpoint`. Measured as temperature; values are in kelvin,
  // the ontology's standard unit for that quantity.
  //
  // Expected range 255.9259259 to 308.1481481. This is a data-quality
  // expectation, not a constraint: a reading outside it is a fault to
  // report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double zone_air_unoccupied_cooling_temperature_setpoint = 314 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "zone_air_unoccupied_cooling_temperature_setpoint"
      subfields: "zone"
      subfields: "air"
      subfields: "unoccupied"
      subfields: "cooling"
      subfields: "temperature"
      subfields: "setpoint"
      point_type: "setpoint"
      measurement: "temperature"
      standard_unit: "kelvin"
      expected_min: 255.925926
      expected_max: 308.148148
    }
  ];

  // The zone air unoccupied heating temperature setpoint.
  //
  // Composed of:
  //   zone (descriptor): Region of building which is conditioned.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   unoccupied (descriptor): State of being outside of scheduled (time
  //     of day or week) run-time parameters.
  //   heating (descriptor): Process, measured effect or requirement for
  //     heating.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   setpoint (point_type): Control target of process or system.
  //
  // Ontology field: `zone_air_unoccupied_heating_temperature_setpoint`,
  // point type `setpoint`. Measured as temperature; values are in kelvin,
  // the ontology's standard unit for that quantity.
  //
  // Expected range 255.9259259 to 308.1481481. This is a data-quality
  // expectation, not a constraint: a reading outside it is a fault to
  // report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double zone_air_unoccupied_heating_temperature_setpoint = 315 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "zone_air_unoccupied_heating_temperature_setpoint"
      subfields: "zone"
      subfields: "air"
      subfields: "unoccupied"
      subfields: "heating"
      subfields: "temperature"
      subfields: "setpoint"
      point_type: "setpoint"
      measurement: "temperature"
      standard_unit: "kelvin"
      expected_min: 255.925926
      expected_max: 308.148148
    }
  ];

  // The zone occupancy status.
  //
  // Composed of:
  //   zone (descriptor): Region of building which is conditioned.
  //   occupancy (descriptor): State of being occupied.
  //   status (point_type): The multistate value indicating an observed
  //     state in a piece of equipment, often indicating if a command was
  //     effected. It is a neutral observation (i.e., no quality judgment of
  //     'good' or 'bad'). It also has no units of measurement; therefore, if
  //     combined with a measurement subfield, it will indicate that the
  //     field is the directional status based on some measurement of that
  //     type (e.g., power_status equates to an on/off value based on some
  //     inference of power).
  //
  // Ontology field: `zone_occupancy_status`, point type `status`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional InactiveOccupiedUnknownUnoccupiedValue zone_occupancy_status = 316 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "zone_occupancy_status"
      subfields: "zone"
      subfields: "occupancy"
      subfields: "status"
      point_type: "status"
    }
  ];

  // Numbers whose field the ontology no longer declares. Never reissued: a
  // consumer may still hold a message that used one.
  reserved 120, 164, 208, 210, 297, 298, 299, 317, 318, 319, 320, 321, 322, 323;
}
