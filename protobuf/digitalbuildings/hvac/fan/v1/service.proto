// Copyright 2026 RegaliumOS™.
// SPDX-License-Identifier: Apache-2.0
//
// Generated by sync from Google's Digital Buildings ontology,
// revision 2026-09-09 (4a794ac).
// Portions Copyright 2020 Google LLC, licensed under Apache-2.0.
// DO NOT EDIT.
//
// Regenerate with: just sync
// Revision history: docs/spec.md

syntax = "proto3";

package protobuf.digitalbuildings.hvac.fan.v1;

import "google/api/annotations.proto";
import "google/api/client.proto";
import "google/protobuf/empty.proto";
import "protobuf/digitalbuildings/hvac/fan/v1/fan.proto";
import "protobuf/digitalbuildings/hvac/fan/v1/messages.proto";

option java_multiple_files = true;
option java_outer_classname = "ServiceProto";
option java_package = "com.regalium.protobuf.digitalbuildings.hvac.fan.v1";

// Manages fans.
//
// AIP-131 names the service for the collection, not `FanService`: that is
// buf STANDARD's convention and the two contradict, which is why buf.yaml
// selects BASIC and lets api-linter own API design (rule 1).
service Fans {
  option (google.api.default_host) = "digitalbuildings.googleapis.com";

  // Returns one fan.
  rpc GetFan(GetFanRequest) returns (Fan) {
    option (google.api.http) = {get: "/v1/{name=buildings/*/fans/*}"};
    option (google.api.method_signature) = "name";
  }

  // Lists the fans in a building.
  rpc ListFans(ListFansRequest) returns (ListFansResponse) {
    option (google.api.http) = {get: "/v1/{parent=buildings/*}/fans"};
    option (google.api.method_signature) = "parent";
  }

  // Creates a fan.
  rpc CreateFan(CreateFanRequest) returns (Fan) {
    option (google.api.http) = {
      post: "/v1/{parent=buildings/*}/fans"
      body: "fan"
    };
    option (google.api.method_signature) = "parent,fan,fan_id";
  }

  // Updates a fan.
  //
  // Rule 8: an update_mask naming an OUTPUT_ONLY field is rejected rather
  // than silently ignored. Most fields here are OUTPUT_ONLY -- the equipment
  // reports them and no API call sets them.
  rpc UpdateFan(UpdateFanRequest) returns (Fan) {
    option (google.api.http) = {
      patch: "/v1/{fan.name=buildings/*/fans/*}"
      body: "fan"
    };
    option (google.api.method_signature) = "fan,update_mask";
  }

  // Deletes a fan.
  rpc DeleteFan(DeleteFanRequest) returns (google.protobuf.Empty) {
    option (google.api.http) = {delete: "/v1/{name=buildings/*/fans/*}"};
    option (google.api.method_signature) = "name";
  }

  // Restores a deleted fan (AIP-164 <https://aip.dev/164>).
  rpc UndeleteFan(UndeleteFanRequest) returns (Fan) {
    option (google.api.http) = {
      post: "/v1/{name=buildings/*/fans/*}:undelete"
      body: "*"
    };
    option (google.api.method_signature) = "name";
  }
}
