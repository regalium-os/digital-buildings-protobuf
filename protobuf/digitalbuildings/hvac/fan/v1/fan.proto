// Copyright 2026 RegaliumOS™.
// SPDX-License-Identifier: Apache-2.0
//
// Generated by sync from Google's Digital Buildings ontology,
// revision 2026-09-09 (4a794ac).
// Portions Copyright 2020 Google LLC, licensed under Apache-2.0.
// DO NOT EDIT.
//
// Regenerate with: just sync
// Revision history: docs/spec.md

syntax = "proto3";

package protobuf.digitalbuildings.hvac.fan.v1;

import "buf/validate/validate.proto";
import "google/api/field_behavior.proto";
import "google/api/field_info.proto";
import "google/api/resource.proto";
import "protobuf/digitalbuildings/annotations/v1/annotations.proto";
import "protobuf/digitalbuildings/hvac/fan/v1/absent_present_value.proto";
import "protobuf/digitalbuildings/hvac/fan/v1/active_inactive_value.proto";
import "protobuf/digitalbuildings/hvac/fan/v1/auto_bypass_high_low_off_vfd_value.proto";
import "protobuf/digitalbuildings/hvac/fan/v1/auto_commissioning_cooling_dehumidify_fan_only_flushing_heating_manual_neutral_off_unknown_value.proto";
import "protobuf/digitalbuildings/hvac/fan/v1/auto_cooling_dehumidify_fan_only_heating_manual_off_value.proto";
import "protobuf/digitalbuildings/hvac/fan/v1/auto_manual_value.proto";
import "protobuf/digitalbuildings/hvac/fan/v1/closed_open_value.proto";
import "protobuf/digitalbuildings/hvac/fan/v1/connection.proto";
import "protobuf/digitalbuildings/hvac/fan/v1/disabled_enabled_occupied_unoccupied_value.proto";
import "protobuf/digitalbuildings/hvac/fan/v1/fan_type.proto";
import "protobuf/digitalbuildings/hvac/fan/v1/field_translation.proto";
import "protobuf/digitalbuildings/hvac/fan/v1/inactive_occupied_unknown_unoccupied_value.proto";
import "protobuf/digitalbuildings/hvac/fan/v1/link.proto";
import "protobuf/digitalbuildings/hvac/fan/v1/local_remote_value.proto";
import "protobuf/digitalbuildings/hvac/fan/v1/off_on_standby_unknown_value.proto";
import "protobuf/digitalbuildings/hvac/fan/v1/off_on_unknown_value.proto";
import "protobuf/digitalbuildings/hvac/fan/v1/off_on_value.proto";

option java_multiple_files = true;
option java_outer_classname = "FanProto";
option java_package = "com.regalium.protobuf.digitalbuildings.hvac.fan.v1";

// Tag for stand-alone fans. This is distinct from integral fans (i.e. fans
// contained within another device, such as an AHU). They transfer air from
// one space to another, or to ambient. They are unidirectional.
//
// Ontology entity type `HVAC/FAN`, a general type carrying 86 canonical
// variant(s). Rule 7: the general type is the resource and each variant is a
// value of FanType, so this message holds the union of every field any
// variant uses -- all of them optional.
//
// Reference: Digital Buildings abstract model.
// https://github.com/google/digitalbuildings/blob/master/ontology/docs/model.md
message Fan {
  option (google.api.resource) = {
    type: "digitalbuildings.googleapis.com/Fan"
    pattern: "buildings/{building}/fans/{fan}"
    singular: "fan"
    plural: "fans"
  };
  option (protobuf.digitalbuildings.annotations.v1.entity_type) = {
    guid: "5c59fe04-9af7-48d7-86c2-5410b4c01a3e"
    namespace: "HVAC"
    general_type: "FAN"
  };

  // Resource name, "buildings/{building}/fans/{fan}". Assigned by the
  // server.
  string name = 1 [
    (google.api.field_behavior) = IDENTIFIER,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The ontology's GUID for this entity.
  //
  // Rule 10: this is the ontology's own identifier, not a second one minted
  // beside it. Digital Buildings assigns a stable UUID to every entity and
  // it survives renames, which is exactly what AIP-148 asks a `uid` to be.
  string uid = 2 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (google.api.field_info).format = UUID4,
    (buf.validate.field).string.uuid = true,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The building config's human-readable entity code, e.g. `FCU-123`.
  //
  // Locally unique rather than globally, chosen at design time, and it must
  // survive a round trip unchanged or every re-import duplicates the entity.
  // The old building-config format keys entities by this; the new one keys
  // them by `uid`. Both are representable because both exist.
  string code = 3 [
    (google.api.field_behavior) = OPTIONAL,
    (google.api.field_behavior) = IMMUTABLE,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The canonical type this entity conforms to.
  //
  // The variant's exact field sets ride on the enum value in
  // (annotations.canonical_type), so a validator can reject an entity
  // reporting a field its declared type does not have.
  FanType entity_type = 4 [(google.api.field_behavior) = OPTIONAL];

  // Directed relationships to other entities.
  //
  // Rule 4: a connection is not a field. AIP-215 forbids referencing a
  // message in another proto package, and the other end of a connection may
  // be any resource in any namespace -- so it is carried as a resource name
  // with a wildcard reference.
  repeated Connection connections = 5 [(google.api.field_behavior) = OPTIONAL];

  // Standard fields mapped from another entity's telemetry.
  //
  // The building config's `links` block: a virtual entity reports data
  // linked from reporting devices rather than emitting it itself.
  repeated Link links = 6 [(google.api.field_behavior) = OPTIONAL];

  // How this entity's native payload maps onto the standard fields, when it
  // reports telemetry directly.
  repeated FieldTranslation translations = 7 [(google.api.field_behavior) = OPTIONAL];

  // Held for the fields on every read path (rule 9).
  reserved 8 to 15;

  // The air pressure sensor.
  //
  // Composed of:
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   pressure (measurement): Measured force per unit area within a system
  //     or process.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `air_pressure_sensor`, point type `sensor`. Measured as
  // pressure; values are in pascals, the ontology's standard unit for that
  // quantity.
  //
  // Expected range 500 to 200000. This is a data-quality expectation, not a
  // constraint: a reading outside it is a fault to report, not a message to
  // reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double air_pressure_sensor = 16 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "air_pressure_sensor"
      subfields: "air"
      subfields: "pressure"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "pressure"
      standard_unit: "pascals"
      expected_min: 500.0
      expected_max: 200000.0
    }
  ];

  // The average line current sensor.
  //
  // Composed of:
  //   average (aggregation): Average value; the arithmetic mean of a set
  //     of numeric values (e.g., average_zone_air_temperature_sensor is a
  //     spatial aggregation of multiple zone temperature sensors).
  //   line (descriptor): Refers to the incoming electrical feed (e.g.,
  //     line current).
  //   current (measurement): Flow of electric charge.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `average_line_current_sensor`, point type `sensor`.
  // Measured as current; values are in amperes, the ontology's standard unit
  // for that quantity.
  //
  // Valid range from 0, enforced by protovalidate. Expected range up to
  // 2000. This is a data-quality expectation, not a constraint: a reading
  // outside it is a fault to report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double average_line_current_sensor = 17 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "average_line_current_sensor"
      subfields: "average"
      subfields: "line"
      subfields: "current"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "current"
      standard_unit: "amperes"
      expected_max: 2000.0
    },
    (buf.validate.field).double.gte = 0.0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The average line neutral voltage sensor.
  //
  // Composed of:
  //   average (aggregation): Average value; the arithmetic mean of a set
  //     of numeric values (e.g., average_zone_air_temperature_sensor is a
  //     spatial aggregation of multiple zone temperature sensors).
  //   line (descriptor): Refers to the incoming electrical feed (e.g.,
  //     line current).
  //   neutral (descriptor): Line in the electrical cirucit which carries
  //     current back to the source. Distinct from ground.
  //   voltage (measurement): Electrical potential difference between two
  //     reference points.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `average_line_neutral_voltage_sensor`, point type
  // `sensor`. Measured as voltage; values are in volts, the ontology's
  // standard unit for that quantity.
  //
  // Expected range 0 to 20000. This is a data-quality expectation, not a
  // constraint: a reading outside it is a fault to report, not a message to
  // reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double average_line_neutral_voltage_sensor = 18 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "average_line_neutral_voltage_sensor"
      subfields: "average"
      subfields: "line"
      subfields: "neutral"
      subfields: "voltage"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "voltage"
      standard_unit: "volts"
      expected_min: 0.0
      expected_max: 20000.0
    }
  ];

  // The building air static pressure sensor.
  //
  // Composed of:
  //   building (descriptor): Applies to the entire building or group of
  //     zones within building (e.g., building_air_static_pressure_sensor).
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   static (measurement_descriptor): Describing a system in equilibrium
  //     or at rest (e.g., supply_air_static_pressure_sensor).
  //   pressure (measurement): Measured force per unit area within a system
  //     or process.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `building_air_static_pressure_sensor`, point type
  // `sensor`. Measured as pressure; values are in pascals, the ontology's
  // standard unit for that quantity.
  //
  // Expected range -249.080024 to 249.080024. This is a data-quality
  // expectation, not a constraint: a reading outside it is a fault to
  // report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double building_air_static_pressure_sensor = 19 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "building_air_static_pressure_sensor"
      subfields: "building"
      subfields: "air"
      subfields: "static"
      subfields: "pressure"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "pressure"
      standard_unit: "pascals"
      expected_min: -249.080024
      expected_max: 249.080024
    }
  ];

  // The building air static pressure setpoint.
  //
  // Composed of:
  //   building (descriptor): Applies to the entire building or group of
  //     zones within building (e.g., building_air_static_pressure_sensor).
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   static (measurement_descriptor): Describing a system in equilibrium
  //     or at rest (e.g., supply_air_static_pressure_sensor).
  //   pressure (measurement): Measured force per unit area within a system
  //     or process.
  //   setpoint (point_type): Control target of process or system.
  //
  // Ontology field: `building_air_static_pressure_setpoint`, point type
  // `setpoint`. Measured as pressure; values are in pascals, the ontology's
  // standard unit for that quantity.
  //
  // Expected range -249.080024 to 249.080024. This is a data-quality
  // expectation, not a constraint: a reading outside it is a fault to
  // report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double building_air_static_pressure_setpoint = 20 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "building_air_static_pressure_setpoint"
      subfields: "building"
      subfields: "air"
      subfields: "static"
      subfields: "pressure"
      subfields: "setpoint"
      point_type: "setpoint"
      measurement: "pressure"
      standard_unit: "pascals"
      expected_min: -249.080024
      expected_max: 249.080024
    }
  ];

  // The bypass air damper percentage command.
  //
  // Composed of:
  //   bypass (descriptor): Route which fluid takes to bypass a process.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   damper (component): Component which meters the flow of air within a
  //     system or device.
  //   percentage (measurement): Measurement per hundred.
  //   command (point_type): The signal given to make an action happen.
  //     Defaults to multistate unless used alongside a measurement_type.
  //
  // Ontology field: `bypass_air_damper_percentage_command`, point type
  // `command`. Measured as percentage; values are in percent, the ontology's
  // standard unit for that quantity.
  //
  // Valid range 0 to 100, enforced by protovalidate.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double bypass_air_damper_percentage_command = 21 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "bypass_air_damper_percentage_command"
      subfields: "bypass"
      subfields: "air"
      subfields: "damper"
      subfields: "percentage"
      subfields: "command"
      point_type: "command"
      measurement: "percentage"
      standard_unit: "percent"
    },
    (buf.validate.field).double.gte = 0.0,
    (buf.validate.field).double.lte = 100.0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The bypass air damper percentage sensor.
  //
  // Composed of:
  //   bypass (descriptor): Route which fluid takes to bypass a process.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   damper (component): Component which meters the flow of air within a
  //     system or device.
  //   percentage (measurement): Measurement per hundred.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `bypass_air_damper_percentage_sensor`, point type
  // `sensor`. Measured as percentage; values are in percent, the ontology's
  // standard unit for that quantity.
  //
  // Valid range 0 to 100, enforced by protovalidate.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double bypass_air_damper_percentage_sensor = 22 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "bypass_air_damper_percentage_sensor"
      subfields: "bypass"
      subfields: "air"
      subfields: "damper"
      subfields: "percentage"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "percentage"
      standard_unit: "percent"
    },
    (buf.validate.field).double.gte = 0.0,
    (buf.validate.field).double.lte = 100.0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The communication status.
  //
  // Composed of:
  //   communication (descriptor): Actual transmit and recieve of data or
  //     connectivity between devices.
  //   status (point_type): The multistate value indicating an observed
  //     state in a piece of equipment, often indicating if a command was
  //     effected. It is a neutral observation (i.e., no quality judgment of
  //     'good' or 'bad'). It also has no units of measurement; therefore, if
  //     combined with a measurement subfield, it will indicate that the
  //     field is the directional status based on some measurement of that
  //     type (e.g., power_status equates to an on/off value based on some
  //     inference of power).
  //
  // Ontology field: `communication_status`, point type `status`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional OffOnValue communication_status = 23 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "communication_status"
      subfields: "communication"
      subfields: "status"
      point_type: "status"
    }
  ];

  // The control mode.
  //
  // Composed of:
  //   control (descriptor): The process of governing the actions of a
  //     device.
  //   mode (point_type): Distinct mode of operation within system. For
  //     example economizer_mode has modes of 'enabled' or 'disabled.'
  //
  // Ontology field: `control_mode`, point type `mode`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional AutoCoolingDehumidifyFanOnlyHeatingManualOffValue control_mode = 24 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "control_mode"
      subfields: "control"
      subfields: "mode"
      point_type: "mode"
    }
  ];

  // The control status.
  //
  // Composed of:
  //   control (descriptor): The process of governing the actions of a
  //     device.
  //   status (point_type): The multistate value indicating an observed
  //     state in a piece of equipment, often indicating if a command was
  //     effected. It is a neutral observation (i.e., no quality judgment of
  //     'good' or 'bad'). It also has no units of measurement; therefore, if
  //     combined with a measurement subfield, it will indicate that the
  //     field is the directional status based on some measurement of that
  //     type (e.g., power_status equates to an on/off value based on some
  //     inference of power).
  //
  // Ontology field: `control_status`, point type `status`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional LocalRemoteValue control_status = 25 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "control_status"
      subfields: "control"
      subfields: "status"
      point_type: "status"
    }
  ];

  // The cooling request count.
  //
  // Composed of:
  //   cooling (descriptor): Process, measured effect or requirement for
  //     cooling.
  //   request (descriptor): A signal from a system device that calls for
  //     more of a particular conditioning process (e.g., pressure requests
  //     from terminal units).
  //   count (point_type): Total count of actions or requests.
  //
  // Ontology field: `cooling_request_count`, point type `count`.
  // Dimensionless: the ontology defines this point type as an integer count
  // rather than a measured quantity.
  //
  // Valid range from 0, enforced by protovalidate. Expected range up to 500.
  // This is a data-quality expectation, not a constraint: a reading outside
  // it is a fault to report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional int64 cooling_request_count = 26 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "cooling_request_count"
      subfields: "cooling"
      subfields: "request"
      subfields: "count"
      point_type: "count"
      expected_max: 500.0
    },
    (buf.validate.field).int64.gte = 0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The current sensor.
  //
  // Composed of:
  //   current (measurement): Flow of electric charge.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `current_sensor`, point type `sensor`. Measured as
  // current; values are in amperes, the ontology's standard unit for that
  // quantity.
  //
  // Valid range from 0, enforced by protovalidate. Expected range up to 200.
  // This is a data-quality expectation, not a constraint: a reading outside
  // it is a fault to report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double current_sensor = 27 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "current_sensor"
      subfields: "current"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "current"
      standard_unit: "amperes"
      expected_max: 200.0
    },
    (buf.validate.field).double.gte = 0.0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The damper percentage command.
  //
  // Composed of:
  //   damper (component): Component which meters the flow of air within a
  //     system or device.
  //   percentage (measurement): Measurement per hundred.
  //   command (point_type): The signal given to make an action happen.
  //     Defaults to multistate unless used alongside a measurement_type.
  //
  // Ontology field: `damper_percentage_command`, point type `command`.
  // Measured as percentage; values are in percent, the ontology's standard
  // unit for that quantity.
  //
  // Valid range 0 to 100, enforced by protovalidate.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double damper_percentage_command = 28 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "damper_percentage_command"
      subfields: "damper"
      subfields: "percentage"
      subfields: "command"
      point_type: "command"
      measurement: "percentage"
      standard_unit: "percent"
    },
    (buf.validate.field).double.gte = 0.0,
    (buf.validate.field).double.lte = 100.0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The damper percentage sensor.
  //
  // Composed of:
  //   damper (component): Component which meters the flow of air within a
  //     system or device.
  //   percentage (measurement): Measurement per hundred.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `damper_percentage_sensor`, point type `sensor`.
  // Measured as percentage; values are in percent, the ontology's standard
  // unit for that quantity.
  //
  // Valid range 0 to 100, enforced by protovalidate.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double damper_percentage_sensor = 29 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "damper_percentage_sensor"
      subfields: "damper"
      subfields: "percentage"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "percentage"
      standard_unit: "percent"
    },
    (buf.validate.field).double.gte = 0.0,
    (buf.validate.field).double.lte = 100.0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The differential pressure sensor.
  //
  // Composed of:
  //   differential (measurement_descriptor): Measured difference between
  //     two reference points (e.g. differential_water_pressure)
  //   pressure (measurement): Measured force per unit area within a system
  //     or process.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `differential_pressure_sensor`, point type `sensor`.
  // Measured as pressure; values are in pascals, the ontology's standard
  // unit for that quantity.
  //
  // Expected range -34473.78945 to 344737.8945. This is a data-quality
  // expectation, not a constraint: a reading outside it is a fault to
  // report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  repeated double differential_pressure_sensor = 30 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "differential_pressure_sensor"
      subfields: "differential"
      subfields: "pressure"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "pressure"
      standard_unit: "pascals"
      enumerated: true
      expected_min: -34473.78945
      expected_max: 344737.8945
    }
  ];

  // The differential pressure setpoint.
  //
  // Composed of:
  //   differential (measurement_descriptor): Measured difference between
  //     two reference points (e.g. differential_water_pressure)
  //   pressure (measurement): Measured force per unit area within a system
  //     or process.
  //   setpoint (point_type): Control target of process or system.
  //
  // Ontology field: `differential_pressure_setpoint`, point type `setpoint`.
  // Measured as pressure; values are in pascals, the ontology's standard
  // unit for that quantity.
  //
  // Expected range -34473.78945 to 344737.8945. This is a data-quality
  // expectation, not a constraint: a reading outside it is a fault to
  // report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double differential_pressure_setpoint = 31 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "differential_pressure_setpoint"
      subfields: "differential"
      subfields: "pressure"
      subfields: "setpoint"
      point_type: "setpoint"
      measurement: "pressure"
      standard_unit: "pascals"
      expected_min: -34473.78945
      expected_max: 344737.8945
    }
  ];

  // The discharge air flowrate sensor.
  //
  // Composed of:
  //   discharge (descriptor): Media leaving system to enter ambient
  //     conditioned space. Typically applies only to air-side systems.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   flowrate (measurement): Rate of fluid movement.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `discharge_air_flowrate_sensor`, point type `sensor`.
  // Measured as flowrate; values are in cubic_meters_per_second, the
  // ontology's standard unit for that quantity.
  //
  // Expected range -0.002359737216 to 94.38948864. This is a data-quality
  // expectation, not a constraint: a reading outside it is a fault to
  // report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double discharge_air_flowrate_sensor = 32 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "discharge_air_flowrate_sensor"
      subfields: "discharge"
      subfields: "air"
      subfields: "flowrate"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "flowrate"
      standard_unit: "cubic_meters_per_second"
      expected_min: -0.00236
      expected_max: 94.389489
    }
  ];

  // The discharge air static pressure sensor.
  //
  // Composed of:
  //   discharge (descriptor): Media leaving system to enter ambient
  //     conditioned space. Typically applies only to air-side systems.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   static (measurement_descriptor): Describing a system in equilibrium
  //     or at rest (e.g., supply_air_static_pressure_sensor).
  //   pressure (measurement): Measured force per unit area within a system
  //     or process.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `discharge_air_static_pressure_sensor`, point type
  // `sensor`. Measured as pressure; values are in pascals, the ontology's
  // standard unit for that quantity.
  //
  // Expected range -124.540012 to 996.320096. This is a data-quality
  // expectation, not a constraint: a reading outside it is a fault to
  // report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double discharge_air_static_pressure_sensor = 33 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "discharge_air_static_pressure_sensor"
      subfields: "discharge"
      subfields: "air"
      subfields: "static"
      subfields: "pressure"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "pressure"
      standard_unit: "pascals"
      expected_min: -124.540012
      expected_max: 996.320096
    }
  ];

  // The discharge air temperature sensor.
  //
  // Composed of:
  //   discharge (descriptor): Media leaving system to enter ambient
  //     conditioned space. Typically applies only to air-side systems.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `discharge_air_temperature_sensor`, point type `sensor`.
  // Measured as temperature; values are in kelvin, the ontology's standard
  // unit for that quantity.
  //
  // Expected range 255.9259259 to 338.7037037. This is a data-quality
  // expectation, not a constraint: a reading outside it is a fault to
  // report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double discharge_air_temperature_sensor = 34 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "discharge_air_temperature_sensor"
      subfields: "discharge"
      subfields: "air"
      subfields: "temperature"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "temperature"
      standard_unit: "kelvin"
      expected_min: 255.925926
      expected_max: 338.703704
    }
  ];

  // The dishwasher run status.
  //
  // Composed of:
  //   dishwasher (component): Device that washes dishes.
  //   run (measurement_descriptor): State of being active or operating. By
  //     default applies to control program for system (e.g., VAV program
  //     run_command).
  //   status (point_type): The multistate value indicating an observed
  //     state in a piece of equipment, often indicating if a command was
  //     effected. It is a neutral observation (i.e., no quality judgment of
  //     'good' or 'bad'). It also has no units of measurement; therefore, if
  //     combined with a measurement subfield, it will indicate that the
  //     field is the directional status based on some measurement of that
  //     type (e.g., power_status equates to an on/off value based on some
  //     inference of power).
  //
  // Ontology field: `dishwasher_run_status`, point type `status`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  repeated OffOnValue dishwasher_run_status = 35 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "dishwasher_run_status"
      subfields: "dishwasher"
      subfields: "run"
      subfields: "status"
      point_type: "status"
      enumerated: true
    }
  ];

  // The dryer run status.
  //
  // Composed of:
  //   dryer (component): A component used for drying clothes.
  //   run (measurement_descriptor): State of being active or operating. By
  //     default applies to control program for system (e.g., VAV program
  //     run_command).
  //   status (point_type): The multistate value indicating an observed
  //     state in a piece of equipment, often indicating if a command was
  //     effected. It is a neutral observation (i.e., no quality judgment of
  //     'good' or 'bad'). It also has no units of measurement; therefore, if
  //     combined with a measurement subfield, it will indicate that the
  //     field is the directional status based on some measurement of that
  //     type (e.g., power_status equates to an on/off value based on some
  //     inference of power).
  //
  // Ontology field: `dryer_run_status`, point type `status`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  repeated OffOnValue dryer_run_status = 36 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "dryer_run_status"
      subfields: "dryer"
      subfields: "run"
      subfields: "status"
      point_type: "status"
      enumerated: true
    }
  ];

  // The efficiency percentage sensor.
  //
  // Composed of:
  //   efficiency (measurement_descriptor): The ratio of required input to
  //     actual output.
  //   percentage (measurement): Measurement per hundred.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `efficiency_percentage_sensor`, point type `sensor`.
  // Measured as percentage; values are in percent, the ontology's standard
  // unit for that quantity.
  //
  // Valid range 0 to 100, enforced by protovalidate.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double efficiency_percentage_sensor = 37 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "efficiency_percentage_sensor"
      subfields: "efficiency"
      subfields: "percentage"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "percentage"
      standard_unit: "percent"
    },
    (buf.validate.field).double.gte = 0.0,
    (buf.validate.field).double.lte = 100.0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The energy accumulator.
  //
  // Composed of:
  //   energy (measurement): The quantitative property that must be
  //     transferred to an object to perform work (default electrical unless
  //     otherwise modified).
  //   accumulator (point_type): The total accumulated quantity (e.g.,
  //     total energy accumulated).
  //
  // Ontology field: `energy_accumulator`, point type `accumulator`. Measured
  // as energy; values are in joules, the ontology's standard unit for that
  // quantity.
  //
  // Valid range from 0, enforced by protovalidate. Expected range up to
  // 3.6e+19. This is a data-quality expectation, not a constraint: a reading
  // outside it is a fault to report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double energy_accumulator = 38 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "energy_accumulator"
      subfields: "energy"
      subfields: "accumulator"
      point_type: "accumulator"
      measurement: "energy"
      standard_unit: "joules"
      expected_max: 36000000000000000000.0
    },
    (buf.validate.field).double.gte = 0.0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The entering heat recovery coil temperature sensor.
  //
  // Composed of:
  //   entering (descriptor): The process of going into a thing (e.g.,
  //     water entering a heat exchanger).
  //   heat (descriptor): Quality of media indicating energy level.
  //   recovery (descriptor): Component or process used for the reclamation
  //     of heat.
  //   coil (component): Component that exchanges heat between two media
  //     streams.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `entering_heat_recovery_coil_temperature_sensor`, point
  // type `sensor`. Measured as temperature; values are in kelvin, the
  // ontology's standard unit for that quantity.
  //
  // Expected range 277.5925926 to 310.9259259. This is a data-quality
  // expectation, not a constraint: a reading outside it is a fault to
  // report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double entering_heat_recovery_coil_temperature_sensor = 39 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "entering_heat_recovery_coil_temperature_sensor"
      subfields: "entering"
      subfields: "heat"
      subfields: "recovery"
      subfields: "coil"
      subfields: "temperature"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "temperature"
      standard_unit: "kelvin"
      expected_min: 277.592593
      expected_max: 310.925926
    }
  ];

  // The exhaust air damper command.
  //
  // Composed of:
  //   exhaust (descriptor): Process or component used in the removal of
  //     air from a conditioned system to the outside atmosphere.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   damper (component): Component which meters the flow of air within a
  //     system or device.
  //   command (point_type): The signal given to make an action happen.
  //     Defaults to multistate unless used alongside a measurement_type.
  //
  // Ontology field: `exhaust_air_damper_command`, point type `command`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  repeated ClosedOpenValue exhaust_air_damper_command = 40 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "exhaust_air_damper_command"
      subfields: "exhaust"
      subfields: "air"
      subfields: "damper"
      subfields: "command"
      point_type: "command"
      enumerated: true
    }
  ];

  // The exhaust air damper percentage command.
  //
  // Composed of:
  //   exhaust (descriptor): Process or component used in the removal of
  //     air from a conditioned system to the outside atmosphere.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   damper (component): Component which meters the flow of air within a
  //     system or device.
  //   percentage (measurement): Measurement per hundred.
  //   command (point_type): The signal given to make an action happen.
  //     Defaults to multistate unless used alongside a measurement_type.
  //
  // Ontology field: `exhaust_air_damper_percentage_command`, point type
  // `command`. Measured as percentage; values are in percent, the ontology's
  // standard unit for that quantity.
  //
  // Valid range 0 to 100, enforced by protovalidate.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double exhaust_air_damper_percentage_command = 41 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "exhaust_air_damper_percentage_command"
      subfields: "exhaust"
      subfields: "air"
      subfields: "damper"
      subfields: "percentage"
      subfields: "command"
      point_type: "command"
      measurement: "percentage"
      standard_unit: "percent"
    },
    (buf.validate.field).double.gte = 0.0,
    (buf.validate.field).double.lte = 100.0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The exhaust air damper percentage sensor.
  //
  // Composed of:
  //   exhaust (descriptor): Process or component used in the removal of
  //     air from a conditioned system to the outside atmosphere.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   damper (component): Component which meters the flow of air within a
  //     system or device.
  //   percentage (measurement): Measurement per hundred.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `exhaust_air_damper_percentage_sensor`, point type
  // `sensor`. Measured as percentage; values are in percent, the ontology's
  // standard unit for that quantity.
  //
  // Valid range 0 to 100, enforced by protovalidate.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double exhaust_air_damper_percentage_sensor = 42 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "exhaust_air_damper_percentage_sensor"
      subfields: "exhaust"
      subfields: "air"
      subfields: "damper"
      subfields: "percentage"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "percentage"
      standard_unit: "percent"
    },
    (buf.validate.field).double.gte = 0.0,
    (buf.validate.field).double.lte = 100.0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The exhaust air damper status.
  //
  // Composed of:
  //   exhaust (descriptor): Process or component used in the removal of
  //     air from a conditioned system to the outside atmosphere.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   damper (component): Component which meters the flow of air within a
  //     system or device.
  //   status (point_type): The multistate value indicating an observed
  //     state in a piece of equipment, often indicating if a command was
  //     effected. It is a neutral observation (i.e., no quality judgment of
  //     'good' or 'bad'). It also has no units of measurement; therefore, if
  //     combined with a measurement subfield, it will indicate that the
  //     field is the directional status based on some measurement of that
  //     type (e.g., power_status equates to an on/off value based on some
  //     inference of power).
  //
  // Ontology field: `exhaust_air_damper_status`, point type `status`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  repeated ClosedOpenValue exhaust_air_damper_status = 43 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "exhaust_air_damper_status"
      subfields: "exhaust"
      subfields: "air"
      subfields: "damper"
      subfields: "status"
      point_type: "status"
      enumerated: true
    }
  ];

  // The exhaust air flowrate sensor.
  //
  // Composed of:
  //   exhaust (descriptor): Process or component used in the removal of
  //     air from a conditioned system to the outside atmosphere.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   flowrate (measurement): Rate of fluid movement.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `exhaust_air_flowrate_sensor`, point type `sensor`.
  // Measured as flowrate; values are in cubic_meters_per_second, the
  // ontology's standard unit for that quantity.
  //
  // Expected range -0.002359737216 to 94.38948864. This is a data-quality
  // expectation, not a constraint: a reading outside it is a fault to
  // report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double exhaust_air_flowrate_sensor = 44 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "exhaust_air_flowrate_sensor"
      subfields: "exhaust"
      subfields: "air"
      subfields: "flowrate"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "flowrate"
      standard_unit: "cubic_meters_per_second"
      expected_min: -0.00236
      expected_max: 94.389489
    }
  ];

  // The exhaust air flowrate setpoint.
  //
  // Composed of:
  //   exhaust (descriptor): Process or component used in the removal of
  //     air from a conditioned system to the outside atmosphere.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   flowrate (measurement): Rate of fluid movement.
  //   setpoint (point_type): Control target of process or system.
  //
  // Ontology field: `exhaust_air_flowrate_setpoint`, point type `setpoint`.
  // Measured as flowrate; values are in cubic_meters_per_second, the
  // ontology's standard unit for that quantity.
  //
  // Expected range 0 to 23.59737216. This is a data-quality expectation, not
  // a constraint: a reading outside it is a fault to report, not a message
  // to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double exhaust_air_flowrate_setpoint = 45 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "exhaust_air_flowrate_setpoint"
      subfields: "exhaust"
      subfields: "air"
      subfields: "flowrate"
      subfields: "setpoint"
      point_type: "setpoint"
      measurement: "flowrate"
      standard_unit: "cubic_meters_per_second"
      expected_min: 0.0
      expected_max: 23.597372
    }
  ];

  // The exhaust air linearvelocity sensor.
  //
  // Composed of:
  //   exhaust (descriptor): Process or component used in the removal of
  //     air from a conditioned system to the outside atmosphere.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   linearvelocity (measurement): The rate of change of displacement
  //     with respect to time. For example, wind speed represents the linear
  //     velocity of the wind.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `exhaust_air_linearvelocity_sensor`, point type
  // `sensor`. Measured as linearvelocity; values are in meters_per_second,
  // the ontology's standard unit for that quantity.
  //
  // Valid range from 0, enforced by protovalidate. Expected range up to
  // 25.4000508. This is a data-quality expectation, not a constraint: a
  // reading outside it is a fault to report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double exhaust_air_linearvelocity_sensor = 46 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "exhaust_air_linearvelocity_sensor"
      subfields: "exhaust"
      subfields: "air"
      subfields: "linearvelocity"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "linearvelocity"
      standard_unit: "meters_per_second"
      expected_max: 25.400051
    },
    (buf.validate.field).double.gte = 0.0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The exhaust air static pressure sensor.
  //
  // Composed of:
  //   exhaust (descriptor): Process or component used in the removal of
  //     air from a conditioned system to the outside atmosphere.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   static (measurement_descriptor): Describing a system in equilibrium
  //     or at rest (e.g., supply_air_static_pressure_sensor).
  //   pressure (measurement): Measured force per unit area within a system
  //     or process.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `exhaust_air_static_pressure_sensor`, point type
  // `sensor`. Measured as pressure; values are in pascals, the ontology's
  // standard unit for that quantity.
  //
  // Expected range -996.320096 to 996.320096. This is a data-quality
  // expectation, not a constraint: a reading outside it is a fault to
  // report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double exhaust_air_static_pressure_sensor = 47 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "exhaust_air_static_pressure_sensor"
      subfields: "exhaust"
      subfields: "air"
      subfields: "static"
      subfields: "pressure"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "pressure"
      standard_unit: "pascals"
      expected_min: -996.320096
      expected_max: 996.320096
    }
  ];

  // The exhaust air static pressure setpoint.
  //
  // Composed of:
  //   exhaust (descriptor): Process or component used in the removal of
  //     air from a conditioned system to the outside atmosphere.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   static (measurement_descriptor): Describing a system in equilibrium
  //     or at rest (e.g., supply_air_static_pressure_sensor).
  //   pressure (measurement): Measured force per unit area within a system
  //     or process.
  //   setpoint (point_type): Control target of process or system.
  //
  // Ontology field: `exhaust_air_static_pressure_setpoint`, point type
  // `setpoint`. Measured as pressure; values are in pascals, the ontology's
  // standard unit for that quantity.
  //
  // Expected range -996.320096 to 996.320096. This is a data-quality
  // expectation, not a constraint: a reading outside it is a fault to
  // report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double exhaust_air_static_pressure_setpoint = 48 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "exhaust_air_static_pressure_setpoint"
      subfields: "exhaust"
      subfields: "air"
      subfields: "static"
      subfields: "pressure"
      subfields: "setpoint"
      point_type: "setpoint"
      measurement: "pressure"
      standard_unit: "pascals"
      expected_min: -996.320096
      expected_max: 996.320096
    }
  ];

  // The exhaust air temperature sensor.
  //
  // Composed of:
  //   exhaust (descriptor): Process or component used in the removal of
  //     air from a conditioned system to the outside atmosphere.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `exhaust_air_temperature_sensor`, point type `sensor`.
  // Measured as temperature; values are in kelvin, the ontology's standard
  // unit for that quantity.
  //
  // Expected range 255.9259259 to 338.7037037. This is a data-quality
  // expectation, not a constraint: a reading outside it is a fault to
  // report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  repeated double exhaust_air_temperature_sensor = 49 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "exhaust_air_temperature_sensor"
      subfields: "exhaust"
      subfields: "air"
      subfields: "temperature"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "temperature"
      standard_unit: "kelvin"
      enumerated: true
      expected_min: 255.925926
      expected_max: 338.703704
    }
  ];

  // The failed alarm.
  //
  // Composed of:
  //   failed (descriptor): Indicates that a point is not operating as
  //     intended or is incapable of operating.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `failed_alarm`, point type `alarm`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue failed_alarm = 50 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "failed_alarm"
      subfields: "failed"
      subfields: "alarm"
      point_type: "alarm"
    }
  ];

  // The failed communication alarm.
  //
  // Composed of:
  //   failed (descriptor): Indicates that a point is not operating as
  //     intended or is incapable of operating.
  //   communication (descriptor): Actual transmit and recieve of data or
  //     connectivity between devices.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `failed_communication_alarm`, point type `alarm`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue failed_communication_alarm = 51 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "failed_communication_alarm"
      subfields: "failed"
      subfields: "communication"
      subfields: "alarm"
      point_type: "alarm"
    }
  ];

  // The failed differential pressure alarm.
  //
  // Composed of:
  //   failed (descriptor): Indicates that a point is not operating as
  //     intended or is incapable of operating.
  //   differential (measurement_descriptor): Measured difference between
  //     two reference points (e.g. differential_water_pressure)
  //   pressure (measurement): Measured force per unit area within a system
  //     or process.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `failed_differential_pressure_alarm`, point type
  // `alarm`. Measured as pressure; values are in pascals, the ontology's
  // standard unit for that quantity.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue failed_differential_pressure_alarm = 52 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "failed_differential_pressure_alarm"
      subfields: "failed"
      subfields: "differential"
      subfields: "pressure"
      subfields: "alarm"
      point_type: "alarm"
      measurement: "pressure"
      standard_unit: "pascals"
    }
  ];

  // The failed discharge air temperature alarm.
  //
  // Composed of:
  //   failed (descriptor): Indicates that a point is not operating as
  //     intended or is incapable of operating.
  //   discharge (descriptor): Media leaving system to enter ambient
  //     conditioned space. Typically applies only to air-side systems.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `failed_discharge_air_temperature_alarm`, point type
  // `alarm`. Measured as temperature; values are in kelvin, the ontology's
  // standard unit for that quantity.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue failed_discharge_air_temperature_alarm = 53 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "failed_discharge_air_temperature_alarm"
      subfields: "failed"
      subfields: "discharge"
      subfields: "air"
      subfields: "temperature"
      subfields: "alarm"
      point_type: "alarm"
      measurement: "temperature"
      standard_unit: "kelvin"
    }
  ];

  // The failed exhaust air damper alarm.
  //
  // Composed of:
  //   failed (descriptor): Indicates that a point is not operating as
  //     intended or is incapable of operating.
  //   exhaust (descriptor): Process or component used in the removal of
  //     air from a conditioned system to the outside atmosphere.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   damper (component): Component which meters the flow of air within a
  //     system or device.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `failed_exhaust_air_damper_alarm`, point type `alarm`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue failed_exhaust_air_damper_alarm = 54 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "failed_exhaust_air_damper_alarm"
      subfields: "failed"
      subfields: "exhaust"
      subfields: "air"
      subfields: "damper"
      subfields: "alarm"
      point_type: "alarm"
    }
  ];

  // The failed exhaust air static pressure alarm.
  //
  // Composed of:
  //   failed (descriptor): Indicates that a point is not operating as
  //     intended or is incapable of operating.
  //   exhaust (descriptor): Process or component used in the removal of
  //     air from a conditioned system to the outside atmosphere.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   static (measurement_descriptor): Describing a system in equilibrium
  //     or at rest (e.g., supply_air_static_pressure_sensor).
  //   pressure (measurement): Measured force per unit area within a system
  //     or process.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `failed_exhaust_air_static_pressure_alarm`, point type
  // `alarm`. Measured as pressure; values are in pascals, the ontology's
  // standard unit for that quantity.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue failed_exhaust_air_static_pressure_alarm = 55 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "failed_exhaust_air_static_pressure_alarm"
      subfields: "failed"
      subfields: "exhaust"
      subfields: "air"
      subfields: "static"
      subfields: "pressure"
      subfields: "alarm"
      point_type: "alarm"
      measurement: "pressure"
      standard_unit: "pascals"
    }
  ];

  // The failed exhaust air temperature alarm.
  //
  // Composed of:
  //   failed (descriptor): Indicates that a point is not operating as
  //     intended or is incapable of operating.
  //   exhaust (descriptor): Process or component used in the removal of
  //     air from a conditioned system to the outside atmosphere.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `failed_exhaust_air_temperature_alarm`, point type
  // `alarm`. Measured as temperature; values are in kelvin, the ontology's
  // standard unit for that quantity.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue failed_exhaust_air_temperature_alarm = 56 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "failed_exhaust_air_temperature_alarm"
      subfields: "failed"
      subfields: "exhaust"
      subfields: "air"
      subfields: "temperature"
      subfields: "alarm"
      point_type: "alarm"
      measurement: "temperature"
      standard_unit: "kelvin"
    }
  ];

  // The failed inverter alarm.
  //
  // Composed of:
  //   failed (descriptor): Indicates that a point is not operating as
  //     intended or is incapable of operating.
  //   inverter (component): Device or circuitry that changes direct
  //     current to alternating current (e.g., variable frequency drive).
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `failed_inverter_alarm`, point type `alarm`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue failed_inverter_alarm = 57 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "failed_inverter_alarm"
      subfields: "failed"
      subfields: "inverter"
      subfields: "alarm"
      point_type: "alarm"
    }
  ];

  // The failed isolation damper alarm.
  //
  // Composed of:
  //   failed (descriptor): Indicates that a point is not operating as
  //     intended or is incapable of operating.
  //   isolation (descriptor): Process of isolating one component or
  //     process from another.
  //   damper (component): Component which meters the flow of air within a
  //     system or device.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `failed_isolation_damper_alarm`, point type `alarm`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue failed_isolation_damper_alarm = 58 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "failed_isolation_damper_alarm"
      subfields: "failed"
      subfields: "isolation"
      subfields: "damper"
      subfields: "alarm"
      point_type: "alarm"
    }
  ];

  // The failed outside air flowrate alarm.
  //
  // Composed of:
  //   failed (descriptor): Indicates that a point is not operating as
  //     intended or is incapable of operating.
  //   outside (descriptor): Process or measurement of local atmospheric
  //     conditions.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   flowrate (measurement): Rate of fluid movement.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `failed_outside_air_flowrate_alarm`, point type `alarm`.
  // Measured as flowrate; values are in cubic_meters_per_second, the
  // ontology's standard unit for that quantity.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue failed_outside_air_flowrate_alarm = 59 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "failed_outside_air_flowrate_alarm"
      subfields: "failed"
      subfields: "outside"
      subfields: "air"
      subfields: "flowrate"
      subfields: "alarm"
      point_type: "alarm"
      measurement: "flowrate"
      standard_unit: "cubic_meters_per_second"
    }
  ];

  // The failed return air isolation damper alarm.
  //
  // Composed of:
  //   failed (descriptor): Indicates that a point is not operating as
  //     intended or is incapable of operating.
  //   return (descriptor): Measurement or process of media as it is
  //     returned from the end-use equipment within the system.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   isolation (descriptor): Process of isolating one component or
  //     process from another.
  //   damper (component): Component which meters the flow of air within a
  //     system or device.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `failed_return_air_isolation_damper_alarm`, point type
  // `alarm`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue failed_return_air_isolation_damper_alarm = 60 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "failed_return_air_isolation_damper_alarm"
      subfields: "failed"
      subfields: "return"
      subfields: "air"
      subfields: "isolation"
      subfields: "damper"
      subfields: "alarm"
      point_type: "alarm"
    }
  ];

  // The failed smoke detection alarm.
  //
  // Composed of:
  //   failed (descriptor): Indicates that a point is not operating as
  //     intended or is incapable of operating.
  //   smoke (descriptor): Visible, suspended particles created during a
  //     fire.
  //   detection (descriptor): Process of identifying conditions.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `failed_smoke_detection_alarm`, point type `alarm`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue failed_smoke_detection_alarm = 61 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "failed_smoke_detection_alarm"
      subfields: "failed"
      subfields: "smoke"
      subfields: "detection"
      subfields: "alarm"
      point_type: "alarm"
    }
  ];

  // The failed supply air static pressure alarm.
  //
  // Composed of:
  //   failed (descriptor): Indicates that a point is not operating as
  //     intended or is incapable of operating.
  //   supply (descriptor): Measurement or process of media as it is
  //     supplied to the end-use equipment within the system.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   static (measurement_descriptor): Describing a system in equilibrium
  //     or at rest (e.g., supply_air_static_pressure_sensor).
  //   pressure (measurement): Measured force per unit area within a system
  //     or process.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `failed_supply_air_static_pressure_alarm`, point type
  // `alarm`. Measured as pressure; values are in pascals, the ontology's
  // standard unit for that quantity.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue failed_supply_air_static_pressure_alarm = 62 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "failed_supply_air_static_pressure_alarm"
      subfields: "failed"
      subfields: "supply"
      subfields: "air"
      subfields: "static"
      subfields: "pressure"
      subfields: "alarm"
      point_type: "alarm"
      measurement: "pressure"
      standard_unit: "pascals"
    }
  ];

  // The failed supply fan alarm.
  //
  // Composed of:
  //   failed (descriptor): Indicates that a point is not operating as
  //     intended or is incapable of operating.
  //   supply (descriptor): Measurement or process of media as it is
  //     supplied to the end-use equipment within the system.
  //   fan (component): Component used for the distribution of air.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `failed_supply_fan_alarm`, point type `alarm`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue failed_supply_fan_alarm = 63 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "failed_supply_fan_alarm"
      subfields: "failed"
      subfields: "supply"
      subfields: "fan"
      subfields: "alarm"
      point_type: "alarm"
    }
  ];

  // The failed zone air co2 concentration alarm.
  //
  // Composed of:
  //   failed (descriptor): Indicates that a point is not operating as
  //     intended or is incapable of operating.
  //   zone (descriptor): Region of building which is conditioned.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   co2 (descriptor): Chemical formula and abbreviation for carbon
  //     dioxide.
  //   concentration (measurement): Concentration of something relative to
  //     something else (usually in parts per million or parts per billion).
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `failed_zone_air_co2_concentration_alarm`, point type
  // `alarm`. Measured as concentration; values are in parts_per_unit, the
  // ontology's standard unit for that quantity.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue failed_zone_air_co2_concentration_alarm = 216 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "failed_zone_air_co2_concentration_alarm"
      subfields: "failed"
      subfields: "zone"
      subfields: "air"
      subfields: "co2"
      subfields: "concentration"
      subfields: "alarm"
      point_type: "alarm"
      measurement: "concentration"
      standard_unit: "parts_per_unit"
    }
  ];

  // The failed zone air co concentration alarm.
  //
  // Composed of:
  //   failed (descriptor): Indicates that a point is not operating as
  //     intended or is incapable of operating.
  //   zone (descriptor): Region of building which is conditioned.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   co (descriptor): Chemical formula and abbreviation for carbon
  //     monoxide.
  //   concentration (measurement): Concentration of something relative to
  //     something else (usually in parts per million or parts per billion).
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `failed_zone_air_co_concentration_alarm`, point type
  // `alarm`. Measured as concentration; values are in parts_per_unit, the
  // ontology's standard unit for that quantity.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue failed_zone_air_co_concentration_alarm = 65 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "failed_zone_air_co_concentration_alarm"
      subfields: "failed"
      subfields: "zone"
      subfields: "air"
      subfields: "co"
      subfields: "concentration"
      subfields: "alarm"
      point_type: "alarm"
      measurement: "concentration"
      standard_unit: "parts_per_unit"
    }
  ];

  // The failed zone air refrigerant concentration alarm.
  //
  // Composed of:
  //   failed (descriptor): Indicates that a point is not operating as
  //     intended or is incapable of operating.
  //   zone (descriptor): Region of building which is conditioned.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   refrigerant (descriptor): Substance used in the mechanical
  //     refrigeration process (e.g., R-134a)
  //   concentration (measurement): Concentration of something relative to
  //     something else (usually in parts per million or parts per billion).
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `failed_zone_air_refrigerant_concentration_alarm`, point
  // type `alarm`. Measured as concentration; values are in parts_per_unit,
  // the ontology's standard unit for that quantity.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue failed_zone_air_refrigerant_concentration_alarm = 66 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "failed_zone_air_refrigerant_concentration_alarm"
      subfields: "failed"
      subfields: "zone"
      subfields: "air"
      subfields: "refrigerant"
      subfields: "concentration"
      subfields: "alarm"
      point_type: "alarm"
      measurement: "concentration"
      standard_unit: "parts_per_unit"
    }
  ];

  // The failed zone air relative humidity alarm.
  //
  // Composed of:
  //   failed (descriptor): Indicates that a point is not operating as
  //     intended or is incapable of operating.
  //   zone (descriptor): Region of building which is conditioned.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   relative (measurement_descriptor): Quality of media with respect to
  //     theoretical minimum or maximum value for a given condition (e.g.,
  //     relative humidity).
  //   humidity (measurement): Concentration of water vapor in air
  //     (conditioned or unconditioned).
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `failed_zone_air_relative_humidity_alarm`, point type
  // `alarm`. Measured as humidity; values are in percent_relative_humidity,
  // the ontology's standard unit for that quantity.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue failed_zone_air_relative_humidity_alarm = 67 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "failed_zone_air_relative_humidity_alarm"
      subfields: "failed"
      subfields: "zone"
      subfields: "air"
      subfields: "relative"
      subfields: "humidity"
      subfields: "alarm"
      point_type: "alarm"
      measurement: "humidity"
      standard_unit: "percent_relative_humidity"
    }
  ];

  // The failed zone air temperature alarm.
  //
  // Composed of:
  //   failed (descriptor): Indicates that a point is not operating as
  //     intended or is incapable of operating.
  //   zone (descriptor): Region of building which is conditioned.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `failed_zone_air_temperature_alarm`, point type `alarm`.
  // Measured as temperature; values are in kelvin, the ontology's standard
  // unit for that quantity.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue failed_zone_air_temperature_alarm = 68 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "failed_zone_air_temperature_alarm"
      subfields: "failed"
      subfields: "zone"
      subfields: "air"
      subfields: "temperature"
      subfields: "alarm"
      point_type: "alarm"
      measurement: "temperature"
      standard_unit: "kelvin"
    }
  ];

  // The filter alarm.
  //
  // Composed of:
  //   filter (component): Component used for removing dust and other
  //     particulate matter from the air.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `filter_alarm`, point type `alarm`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  repeated ActiveInactiveValue filter_alarm = 69 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "filter_alarm"
      subfields: "filter"
      subfields: "alarm"
      point_type: "alarm"
      enumerated: true
    }
  ];

  // The filter differential pressure sensor.
  //
  // Composed of:
  //   filter (component): Component used for removing dust and other
  //     particulate matter from the air.
  //   differential (measurement_descriptor): Measured difference between
  //     two reference points (e.g. differential_water_pressure)
  //   pressure (measurement): Measured force per unit area within a system
  //     or process.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `filter_differential_pressure_sensor`, point type
  // `sensor`. Measured as pressure; values are in pascals, the ontology's
  // standard unit for that quantity.
  //
  // Expected range -249.080024 to 2490.80024. This is a data-quality
  // expectation, not a constraint: a reading outside it is a fault to
  // report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double filter_differential_pressure_sensor = 70 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "filter_differential_pressure_sensor"
      subfields: "filter"
      subfields: "differential"
      subfields: "pressure"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "pressure"
      standard_unit: "pascals"
      expected_min: -249.080024
      expected_max: 2490.80024
    }
  ];

  // The filter differential pressure status.
  //
  // Composed of:
  //   filter (component): Component used for removing dust and other
  //     particulate matter from the air.
  //   differential (measurement_descriptor): Measured difference between
  //     two reference points (e.g. differential_water_pressure)
  //   pressure (measurement): Measured force per unit area within a system
  //     or process.
  //   status (point_type): The multistate value indicating an observed
  //     state in a piece of equipment, often indicating if a command was
  //     effected. It is a neutral observation (i.e., no quality judgment of
  //     'good' or 'bad'). It also has no units of measurement; therefore, if
  //     combined with a measurement subfield, it will indicate that the
  //     field is the directional status based on some measurement of that
  //     type (e.g., power_status equates to an on/off value based on some
  //     inference of power).
  //
  // Ontology field: `filter_differential_pressure_status`, point type
  // `status`. Measured as pressure; values are in pascals, the ontology's
  // standard unit for that quantity.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  repeated AbsentPresentValue filter_differential_pressure_status = 71 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "filter_differential_pressure_status"
      subfields: "filter"
      subfields: "differential"
      subfields: "pressure"
      subfields: "status"
      point_type: "status"
      measurement: "pressure"
      standard_unit: "pascals"
      enumerated: true
    }
  ];

  // The fire alarm.
  //
  // Composed of:
  //   fire (descriptor): The combustion or burning of substances with the
  //     potential to cause physical damage.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `fire_alarm`, point type `alarm`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue fire_alarm = 72 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "fire_alarm"
      subfields: "fire"
      subfields: "alarm"
      point_type: "alarm"
    }
  ];

  // The flowrate capacity.
  //
  // Composed of:
  //   flowrate (measurement): Rate of fluid movement.
  //   capacity (point_type): A design parameter quantity (e.g., design
  //     motor power capacity). Is always a maximum limit.
  //
  // Ontology field: `flowrate_capacity`, point type `capacity`. Measured as
  // flowrate; values are in cubic_meters_per_second, the ontology's standard
  // unit for that quantity.
  //
  // Valid range from 0, enforced by protovalidate. Expected range up to
  // 141.584233. This is a data-quality expectation, not a constraint: a
  // reading outside it is a fault to report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double flowrate_capacity = 73 [
    (google.api.field_behavior) = OPTIONAL,
    (google.api.field_behavior) = IMMUTABLE,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "flowrate_capacity"
      subfields: "flowrate"
      subfields: "capacity"
      point_type: "capacity"
      measurement: "flowrate"
      standard_unit: "cubic_meters_per_second"
      expected_max: 141.584233
    },
    (buf.validate.field).double.gte = 0.0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The heat recovery water flowrate sensor.
  //
  // Composed of:
  //   heat (descriptor): Quality of media indicating energy level.
  //   recovery (descriptor): Component or process used for the reclamation
  //     of heat.
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   flowrate (measurement): Rate of fluid movement.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `heat_recovery_water_flowrate_sensor`, point type
  // `sensor`. Measured as flowrate; values are in cubic_meters_per_second,
  // the ontology's standard unit for that quantity.
  //
  // Expected range -0.000315451 to 0.315451. This is a data-quality
  // expectation, not a constraint: a reading outside it is a fault to
  // report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double heat_recovery_water_flowrate_sensor = 74 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "heat_recovery_water_flowrate_sensor"
      subfields: "heat"
      subfields: "recovery"
      subfields: "water"
      subfields: "flowrate"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "flowrate"
      standard_unit: "cubic_meters_per_second"
      expected_min: -0.000315
      expected_max: 0.315451
    }
  ];

  // The heat recovery water isolation valve command.
  //
  // Composed of:
  //   heat (descriptor): Quality of media indicating energy level.
  //   recovery (descriptor): Component or process used for the reclamation
  //     of heat.
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   isolation (descriptor): Process of isolating one component or
  //     process from another.
  //   valve (component): Component which meters the flow of water within a
  //     system or device.
  //   command (point_type): The signal given to make an action happen.
  //     Defaults to multistate unless used alongside a measurement_type.
  //
  // Ontology field: `heat_recovery_water_isolation_valve_command`, point
  // type `command`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ClosedOpenValue heat_recovery_water_isolation_valve_command = 75 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "heat_recovery_water_isolation_valve_command"
      subfields: "heat"
      subfields: "recovery"
      subfields: "water"
      subfields: "isolation"
      subfields: "valve"
      subfields: "command"
      point_type: "command"
    }
  ];

  // The heating request count.
  //
  // Composed of:
  //   heating (descriptor): Process, measured effect or requirement for
  //     heating.
  //   request (descriptor): A signal from a system device that calls for
  //     more of a particular conditioning process (e.g., pressure requests
  //     from terminal units).
  //   count (point_type): Total count of actions or requests.
  //
  // Ontology field: `heating_request_count`, point type `count`.
  // Dimensionless: the ontology defines this point type as an integer count
  // rather than a measured quantity.
  //
  // Valid range from 0, enforced by protovalidate. Expected range up to 250.
  // This is a data-quality expectation, not a constraint: a reading outside
  // it is a fault to report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional int64 heating_request_count = 76 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "heating_request_count"
      subfields: "heating"
      subfields: "request"
      subfields: "count"
      point_type: "count"
      expected_max: 250.0
    },
    (buf.validate.field).int64.gte = 0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The high building air static pressure alarm.
  //
  // Composed of:
  //   high (descriptor): Level of control or measurement; above medium and
  //     low; opposite of low.
  //   building (descriptor): Applies to the entire building or group of
  //     zones within building (e.g., building_air_static_pressure_sensor).
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   static (measurement_descriptor): Describing a system in equilibrium
  //     or at rest (e.g., supply_air_static_pressure_sensor).
  //   pressure (measurement): Measured force per unit area within a system
  //     or process.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `high_building_air_static_pressure_alarm`, point type
  // `alarm`. Measured as pressure; values are in pascals, the ontology's
  // standard unit for that quantity.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue high_building_air_static_pressure_alarm = 77 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "high_building_air_static_pressure_alarm"
      subfields: "high"
      subfields: "building"
      subfields: "air"
      subfields: "static"
      subfields: "pressure"
      subfields: "alarm"
      point_type: "alarm"
      measurement: "pressure"
      standard_unit: "pascals"
    }
  ];

  // The high differential pressure alarm.
  //
  // Composed of:
  //   high (descriptor): Level of control or measurement; above medium and
  //     low; opposite of low.
  //   differential (measurement_descriptor): Measured difference between
  //     two reference points (e.g. differential_water_pressure)
  //   pressure (measurement): Measured force per unit area within a system
  //     or process.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `high_differential_pressure_alarm`, point type `alarm`.
  // Measured as pressure; values are in pascals, the ontology's standard
  // unit for that quantity.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  repeated ActiveInactiveValue high_differential_pressure_alarm = 78 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "high_differential_pressure_alarm"
      subfields: "high"
      subfields: "differential"
      subfields: "pressure"
      subfields: "alarm"
      point_type: "alarm"
      measurement: "pressure"
      standard_unit: "pascals"
      enumerated: true
    }
  ];

  // The high discharge air static pressure alarm.
  //
  // Composed of:
  //   high (descriptor): Level of control or measurement; above medium and
  //     low; opposite of low.
  //   discharge (descriptor): Media leaving system to enter ambient
  //     conditioned space. Typically applies only to air-side systems.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   static (measurement_descriptor): Describing a system in equilibrium
  //     or at rest (e.g., supply_air_static_pressure_sensor).
  //   pressure (measurement): Measured force per unit area within a system
  //     or process.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `high_discharge_air_static_pressure_alarm`, point type
  // `alarm`. Measured as pressure; values are in pascals, the ontology's
  // standard unit for that quantity.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue high_discharge_air_static_pressure_alarm = 79 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "high_discharge_air_static_pressure_alarm"
      subfields: "high"
      subfields: "discharge"
      subfields: "air"
      subfields: "static"
      subfields: "pressure"
      subfields: "alarm"
      point_type: "alarm"
      measurement: "pressure"
      standard_unit: "pascals"
    }
  ];

  // The high discharge air temperature alarm.
  //
  // Composed of:
  //   high (descriptor): Level of control or measurement; above medium and
  //     low; opposite of low.
  //   discharge (descriptor): Media leaving system to enter ambient
  //     conditioned space. Typically applies only to air-side systems.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `high_discharge_air_temperature_alarm`, point type
  // `alarm`. Measured as temperature; values are in kelvin, the ontology's
  // standard unit for that quantity.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue high_discharge_air_temperature_alarm = 80 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "high_discharge_air_temperature_alarm"
      subfields: "high"
      subfields: "discharge"
      subfields: "air"
      subfields: "temperature"
      subfields: "alarm"
      point_type: "alarm"
      measurement: "temperature"
      standard_unit: "kelvin"
    }
  ];

  // The high exhaust air static pressure alarm.
  //
  // Composed of:
  //   high (descriptor): Level of control or measurement; above medium and
  //     low; opposite of low.
  //   exhaust (descriptor): Process or component used in the removal of
  //     air from a conditioned system to the outside atmosphere.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   static (measurement_descriptor): Describing a system in equilibrium
  //     or at rest (e.g., supply_air_static_pressure_sensor).
  //   pressure (measurement): Measured force per unit area within a system
  //     or process.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `high_exhaust_air_static_pressure_alarm`, point type
  // `alarm`. Measured as pressure; values are in pascals, the ontology's
  // standard unit for that quantity.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue high_exhaust_air_static_pressure_alarm = 81 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "high_exhaust_air_static_pressure_alarm"
      subfields: "high"
      subfields: "exhaust"
      subfields: "air"
      subfields: "static"
      subfields: "pressure"
      subfields: "alarm"
      point_type: "alarm"
      measurement: "pressure"
      standard_unit: "pascals"
    }
  ];

  // The high exhaust fan speed command.
  //
  // Composed of:
  //   high (descriptor): Level of control or measurement; above medium and
  //     low; opposite of low.
  //   exhaust (descriptor): Process or component used in the removal of
  //     air from a conditioned system to the outside atmosphere.
  //   fan (component): Component used for the distribution of air.
  //   speed (measurement_descriptor): Numeric setting of how fast to run a
  //     device, in the specified units. Typically used to describe
  //     revolutions of a motor as a fraction of nominal or maximum.
  //   command (point_type): The signal given to make an action happen.
  //     Defaults to multistate unless used alongside a measurement_type.
  //
  // Ontology field: `high_exhaust_fan_speed_command`, point type `command`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional OffOnValue high_exhaust_fan_speed_command = 82 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "high_exhaust_fan_speed_command"
      subfields: "high"
      subfields: "exhaust"
      subfields: "fan"
      subfields: "speed"
      subfields: "command"
      point_type: "command"
    }
  ];

  // The high exhaust fan speed status.
  //
  // Composed of:
  //   high (descriptor): Level of control or measurement; above medium and
  //     low; opposite of low.
  //   exhaust (descriptor): Process or component used in the removal of
  //     air from a conditioned system to the outside atmosphere.
  //   fan (component): Component used for the distribution of air.
  //   speed (measurement_descriptor): Numeric setting of how fast to run a
  //     device, in the specified units. Typically used to describe
  //     revolutions of a motor as a fraction of nominal or maximum.
  //   status (point_type): The multistate value indicating an observed
  //     state in a piece of equipment, often indicating if a command was
  //     effected. It is a neutral observation (i.e., no quality judgment of
  //     'good' or 'bad'). It also has no units of measurement; therefore, if
  //     combined with a measurement subfield, it will indicate that the
  //     field is the directional status based on some measurement of that
  //     type (e.g., power_status equates to an on/off value based on some
  //     inference of power).
  //
  // Ontology field: `high_exhaust_fan_speed_status`, point type `status`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional OffOnValue high_exhaust_fan_speed_status = 83 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "high_exhaust_fan_speed_status"
      subfields: "high"
      subfields: "exhaust"
      subfields: "fan"
      subfields: "speed"
      subfields: "status"
      point_type: "status"
    }
  ];

  // The high limit outside air temperature setpoint.
  //
  // Composed of:
  //   high (descriptor): Level of control or measurement; above medium and
  //     low; opposite of low.
  //   limit (descriptor): A boundary condition for a control (e.g., low
  //     limit).
  //   outside (descriptor): Process or measurement of local atmospheric
  //     conditions.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   setpoint (point_type): Control target of process or system.
  //
  // Ontology field: `high_limit_outside_air_temperature_setpoint`, point
  // type `setpoint`. Measured as temperature; values are in kelvin, the
  // ontology's standard unit for that quantity.
  //
  // Expected range 255.9259259 to 322.037037. This is a data-quality
  // expectation, not a constraint: a reading outside it is a fault to
  // report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double high_limit_outside_air_temperature_setpoint = 84 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "high_limit_outside_air_temperature_setpoint"
      subfields: "high"
      subfields: "limit"
      subfields: "outside"
      subfields: "air"
      subfields: "temperature"
      subfields: "setpoint"
      point_type: "setpoint"
      measurement: "temperature"
      standard_unit: "kelvin"
      expected_min: 255.925926
      expected_max: 322.037037
    }
  ];

  // The high limit speed percentage setpoint.
  //
  // Composed of:
  //   high (descriptor): Level of control or measurement; above medium and
  //     low; opposite of low.
  //   limit (descriptor): A boundary condition for a control (e.g., low
  //     limit).
  //   speed (measurement_descriptor): Numeric setting of how fast to run a
  //     device, in the specified units. Typically used to describe
  //     revolutions of a motor as a fraction of nominal or maximum.
  //   percentage (measurement): Measurement per hundred.
  //   setpoint (point_type): Control target of process or system.
  //
  // Ontology field: `high_limit_speed_percentage_setpoint`, point type
  // `setpoint`. Measured as percentage; values are in percent, the
  // ontology's standard unit for that quantity.
  //
  // Valid range 0 to 100, enforced by protovalidate.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double high_limit_speed_percentage_setpoint = 85 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "high_limit_speed_percentage_setpoint"
      subfields: "high"
      subfields: "limit"
      subfields: "speed"
      subfields: "percentage"
      subfields: "setpoint"
      point_type: "setpoint"
      measurement: "percentage"
      standard_unit: "percent"
    },
    (buf.validate.field).double.gte = 0.0,
    (buf.validate.field).double.lte = 100.0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The high limit zone air co concentration setpoint.
  //
  // Composed of:
  //   high (descriptor): Level of control or measurement; above medium and
  //     low; opposite of low.
  //   limit (descriptor): A boundary condition for a control (e.g., low
  //     limit).
  //   zone (descriptor): Region of building which is conditioned.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   co (descriptor): Chemical formula and abbreviation for carbon
  //     monoxide.
  //   concentration (measurement): Concentration of something relative to
  //     something else (usually in parts per million or parts per billion).
  //   setpoint (point_type): Control target of process or system.
  //
  // Ontology field: `high_limit_zone_air_co_concentration_setpoint`, point
  // type `setpoint`. Measured as concentration; values are in
  // parts_per_unit, the ontology's standard unit for that quantity.
  //
  // Expected range 0.00035 to 0.0015. This is a data-quality expectation,
  // not a constraint: a reading outside it is a fault to report, not a
  // message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double high_limit_zone_air_co_concentration_setpoint = 86 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "high_limit_zone_air_co_concentration_setpoint"
      subfields: "high"
      subfields: "limit"
      subfields: "zone"
      subfields: "air"
      subfields: "co"
      subfields: "concentration"
      subfields: "setpoint"
      point_type: "setpoint"
      measurement: "concentration"
      standard_unit: "parts_per_unit"
      expected_min: 0.00035
      expected_max: 0.0015
    }
  ];

  // The high limit zone air no2 concentration setpoint.
  //
  // Composed of:
  //   high (descriptor): Level of control or measurement; above medium and
  //     low; opposite of low.
  //   limit (descriptor): A boundary condition for a control (e.g., low
  //     limit).
  //   zone (descriptor): Region of building which is conditioned.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   no2 (descriptor): Chemical formula and abbreviation for nitrogen
  //     dioxide.
  //   concentration (measurement): Concentration of something relative to
  //     something else (usually in parts per million or parts per billion).
  //   setpoint (point_type): Control target of process or system.
  //
  // Ontology field: `high_limit_zone_air_no2_concentration_setpoint`, point
  // type `setpoint`. Measured as concentration; values are in
  // parts_per_unit, the ontology's standard unit for that quantity.
  //
  // Valid range from 0, enforced by protovalidate. Expected range up to
  // 0.0005. This is a data-quality expectation, not a constraint: a reading
  // outside it is a fault to report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double high_limit_zone_air_no2_concentration_setpoint = 217 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "high_limit_zone_air_no2_concentration_setpoint"
      subfields: "high"
      subfields: "limit"
      subfields: "zone"
      subfields: "air"
      subfields: "no2"
      subfields: "concentration"
      subfields: "setpoint"
      point_type: "setpoint"
      measurement: "concentration"
      standard_unit: "parts_per_unit"
      expected_max: 0.0005
    },
    (buf.validate.field).double.gte = 0.0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The high limit zone air no concentration setpoint.
  //
  // Composed of:
  //   high (descriptor): Level of control or measurement; above medium and
  //     low; opposite of low.
  //   limit (descriptor): A boundary condition for a control (e.g., low
  //     limit).
  //   zone (descriptor): Region of building which is conditioned.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   no (descriptor): Chemical formula and abbreviation for nitrogen
  //     monoxide, which is a colorless gas and a toxic air pollutant.
  //   concentration (measurement): Concentration of something relative to
  //     something else (usually in parts per million or parts per billion).
  //   setpoint (point_type): Control target of process or system.
  //
  // Ontology field: `high_limit_zone_air_no_concentration_setpoint`, point
  // type `setpoint`. Measured as concentration; values are in
  // parts_per_unit, the ontology's standard unit for that quantity.
  //
  // Valid range from 0, enforced by protovalidate. Expected range up to
  // 0.0005. This is a data-quality expectation, not a constraint: a reading
  // outside it is a fault to report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double high_limit_zone_air_no_concentration_setpoint = 88 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "high_limit_zone_air_no_concentration_setpoint"
      subfields: "high"
      subfields: "limit"
      subfields: "zone"
      subfields: "air"
      subfields: "no"
      subfields: "concentration"
      subfields: "setpoint"
      point_type: "setpoint"
      measurement: "concentration"
      standard_unit: "parts_per_unit"
      expected_max: 0.0005
    },
    (buf.validate.field).double.gte = 0.0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The high speed command.
  //
  // Composed of:
  //   high (descriptor): Level of control or measurement; above medium and
  //     low; opposite of low.
  //   speed (measurement_descriptor): Numeric setting of how fast to run a
  //     device, in the specified units. Typically used to describe
  //     revolutions of a motor as a fraction of nominal or maximum.
  //   command (point_type): The signal given to make an action happen.
  //     Defaults to multistate unless used alongside a measurement_type.
  //
  // Ontology field: `high_speed_command`, point type `command`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional OffOnValue high_speed_command = 89 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "high_speed_command"
      subfields: "high"
      subfields: "speed"
      subfields: "command"
      point_type: "command"
    }
  ];

  // The high speed status.
  //
  // Composed of:
  //   high (descriptor): Level of control or measurement; above medium and
  //     low; opposite of low.
  //   speed (measurement_descriptor): Numeric setting of how fast to run a
  //     device, in the specified units. Typically used to describe
  //     revolutions of a motor as a fraction of nominal or maximum.
  //   status (point_type): The multistate value indicating an observed
  //     state in a piece of equipment, often indicating if a command was
  //     effected. It is a neutral observation (i.e., no quality judgment of
  //     'good' or 'bad'). It also has no units of measurement; therefore, if
  //     combined with a measurement subfield, it will indicate that the
  //     field is the directional status based on some measurement of that
  //     type (e.g., power_status equates to an on/off value based on some
  //     inference of power).
  //
  // Ontology field: `high_speed_status`, point type `status`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional OffOnValue high_speed_status = 90 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "high_speed_status"
      subfields: "high"
      subfields: "speed"
      subfields: "status"
      point_type: "status"
    }
  ];

  // The high supply air static pressure alarm.
  //
  // Composed of:
  //   high (descriptor): Level of control or measurement; above medium and
  //     low; opposite of low.
  //   supply (descriptor): Measurement or process of media as it is
  //     supplied to the end-use equipment within the system.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   static (measurement_descriptor): Describing a system in equilibrium
  //     or at rest (e.g., supply_air_static_pressure_sensor).
  //   pressure (measurement): Measured force per unit area within a system
  //     or process.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `high_supply_air_static_pressure_alarm`, point type
  // `alarm`. Measured as pressure; values are in pascals, the ontology's
  // standard unit for that quantity.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue high_supply_air_static_pressure_alarm = 91 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "high_supply_air_static_pressure_alarm"
      subfields: "high"
      subfields: "supply"
      subfields: "air"
      subfields: "static"
      subfields: "pressure"
      subfields: "alarm"
      point_type: "alarm"
      measurement: "pressure"
      standard_unit: "pascals"
    }
  ];

  // The high supply air temperature alarm.
  //
  // Composed of:
  //   high (descriptor): Level of control or measurement; above medium and
  //     low; opposite of low.
  //   supply (descriptor): Measurement or process of media as it is
  //     supplied to the end-use equipment within the system.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `high_supply_air_temperature_alarm`, point type `alarm`.
  // Measured as temperature; values are in kelvin, the ontology's standard
  // unit for that quantity.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue high_supply_air_temperature_alarm = 92 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "high_supply_air_temperature_alarm"
      subfields: "high"
      subfields: "supply"
      subfields: "air"
      subfields: "temperature"
      subfields: "alarm"
      point_type: "alarm"
      measurement: "temperature"
      standard_unit: "kelvin"
    }
  ];

  // The high zone air co2 concentration alarm.
  //
  // Composed of:
  //   high (descriptor): Level of control or measurement; above medium and
  //     low; opposite of low.
  //   zone (descriptor): Region of building which is conditioned.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   co2 (descriptor): Chemical formula and abbreviation for carbon
  //     dioxide.
  //   concentration (measurement): Concentration of something relative to
  //     something else (usually in parts per million or parts per billion).
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `high_zone_air_co2_concentration_alarm`, point type
  // `alarm`. Measured as concentration; values are in parts_per_unit, the
  // ontology's standard unit for that quantity.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue high_zone_air_co2_concentration_alarm = 218 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "high_zone_air_co2_concentration_alarm"
      subfields: "high"
      subfields: "zone"
      subfields: "air"
      subfields: "co2"
      subfields: "concentration"
      subfields: "alarm"
      point_type: "alarm"
      measurement: "concentration"
      standard_unit: "parts_per_unit"
    }
  ];

  // The high zone air co concentration alarm.
  //
  // Composed of:
  //   high (descriptor): Level of control or measurement; above medium and
  //     low; opposite of low.
  //   zone (descriptor): Region of building which is conditioned.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   co (descriptor): Chemical formula and abbreviation for carbon
  //     monoxide.
  //   concentration (measurement): Concentration of something relative to
  //     something else (usually in parts per million or parts per billion).
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `high_zone_air_co_concentration_alarm`, point type
  // `alarm`. Measured as concentration; values are in parts_per_unit, the
  // ontology's standard unit for that quantity.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue high_zone_air_co_concentration_alarm = 94 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "high_zone_air_co_concentration_alarm"
      subfields: "high"
      subfields: "zone"
      subfields: "air"
      subfields: "co"
      subfields: "concentration"
      subfields: "alarm"
      point_type: "alarm"
      measurement: "concentration"
      standard_unit: "parts_per_unit"
    }
  ];

  // The high zone air refrigerant concentration alarm.
  //
  // Composed of:
  //   high (descriptor): Level of control or measurement; above medium and
  //     low; opposite of low.
  //   zone (descriptor): Region of building which is conditioned.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   refrigerant (descriptor): Substance used in the mechanical
  //     refrigeration process (e.g., R-134a)
  //   concentration (measurement): Concentration of something relative to
  //     something else (usually in parts per million or parts per billion).
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `high_zone_air_refrigerant_concentration_alarm`, point
  // type `alarm`. Measured as concentration; values are in parts_per_unit,
  // the ontology's standard unit for that quantity.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue high_zone_air_refrigerant_concentration_alarm = 95 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "high_zone_air_refrigerant_concentration_alarm"
      subfields: "high"
      subfields: "zone"
      subfields: "air"
      subfields: "refrigerant"
      subfields: "concentration"
      subfields: "alarm"
      point_type: "alarm"
      measurement: "concentration"
      standard_unit: "parts_per_unit"
    }
  ];

  // The high zone air relative humidity alarm.
  //
  // Composed of:
  //   high (descriptor): Level of control or measurement; above medium and
  //     low; opposite of low.
  //   zone (descriptor): Region of building which is conditioned.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   relative (measurement_descriptor): Quality of media with respect to
  //     theoretical minimum or maximum value for a given condition (e.g.,
  //     relative humidity).
  //   humidity (measurement): Concentration of water vapor in air
  //     (conditioned or unconditioned).
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `high_zone_air_relative_humidity_alarm`, point type
  // `alarm`. Measured as humidity; values are in percent_relative_humidity,
  // the ontology's standard unit for that quantity.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue high_zone_air_relative_humidity_alarm = 96 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "high_zone_air_relative_humidity_alarm"
      subfields: "high"
      subfields: "zone"
      subfields: "air"
      subfields: "relative"
      subfields: "humidity"
      subfields: "alarm"
      point_type: "alarm"
      measurement: "humidity"
      standard_unit: "percent_relative_humidity"
    }
  ];

  // The high zone air static pressure alarm.
  //
  // Composed of:
  //   high (descriptor): Level of control or measurement; above medium and
  //     low; opposite of low.
  //   zone (descriptor): Region of building which is conditioned.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   static (measurement_descriptor): Describing a system in equilibrium
  //     or at rest (e.g., supply_air_static_pressure_sensor).
  //   pressure (measurement): Measured force per unit area within a system
  //     or process.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `high_zone_air_static_pressure_alarm`, point type
  // `alarm`. Measured as pressure; values are in pascals, the ontology's
  // standard unit for that quantity.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue high_zone_air_static_pressure_alarm = 97 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "high_zone_air_static_pressure_alarm"
      subfields: "high"
      subfields: "zone"
      subfields: "air"
      subfields: "static"
      subfields: "pressure"
      subfields: "alarm"
      point_type: "alarm"
      measurement: "pressure"
      standard_unit: "pascals"
    }
  ];

  // The high zone air temperature alarm.
  //
  // Composed of:
  //   high (descriptor): Level of control or measurement; above medium and
  //     low; opposite of low.
  //   zone (descriptor): Region of building which is conditioned.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `high_zone_air_temperature_alarm`, point type `alarm`.
  // Measured as temperature; values are in kelvin, the ontology's standard
  // unit for that quantity.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue high_zone_air_temperature_alarm = 98 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "high_zone_air_temperature_alarm"
      subfields: "high"
      subfields: "zone"
      subfields: "air"
      subfields: "temperature"
      subfields: "alarm"
      point_type: "alarm"
      measurement: "temperature"
      standard_unit: "kelvin"
    }
  ];

  // The input inverter frequency sensor.
  //
  // Composed of:
  //   input (descriptor): The input to a system.
  //   inverter (component): Device or circuitry that changes direct
  //     current to alternating current (e.g., variable frequency drive).
  //   frequency (measurement): The rate of cycling within a process.
  //     Typically used to describe voltage (and amperage) oscillation within
  //     AC power distribution components.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `input_inverter_frequency_sensor`, point type `sensor`.
  // Measured as frequency; values are in hertz, the ontology's standard unit
  // for that quantity.
  //
  // Expected range -0.5 to 65. This is a data-quality expectation, not a
  // constraint: a reading outside it is a fault to report, not a message to
  // reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double input_inverter_frequency_sensor = 99 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "input_inverter_frequency_sensor"
      subfields: "input"
      subfields: "inverter"
      subfields: "frequency"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "frequency"
      standard_unit: "hertz"
      expected_min: -0.5
      expected_max: 65.0
    }
  ];

  // The isolation damper percentage command.
  //
  // Composed of:
  //   isolation (descriptor): Process of isolating one component or
  //     process from another.
  //   damper (component): Component which meters the flow of air within a
  //     system or device.
  //   percentage (measurement): Measurement per hundred.
  //   command (point_type): The signal given to make an action happen.
  //     Defaults to multistate unless used alongside a measurement_type.
  //
  // Ontology field: `isolation_damper_percentage_command`, point type
  // `command`. Measured as percentage; values are in percent, the ontology's
  // standard unit for that quantity.
  //
  // Valid range 0 to 100, enforced by protovalidate.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double isolation_damper_percentage_command = 100 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "isolation_damper_percentage_command"
      subfields: "isolation"
      subfields: "damper"
      subfields: "percentage"
      subfields: "command"
      point_type: "command"
      measurement: "percentage"
      standard_unit: "percent"
    },
    (buf.validate.field).double.gte = 0.0,
    (buf.validate.field).double.lte = 100.0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The isolation damper percentage sensor.
  //
  // Composed of:
  //   isolation (descriptor): Process of isolating one component or
  //     process from another.
  //   damper (component): Component which meters the flow of air within a
  //     system or device.
  //   percentage (measurement): Measurement per hundred.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `isolation_damper_percentage_sensor`, point type
  // `sensor`. Measured as percentage; values are in percent, the ontology's
  // standard unit for that quantity.
  //
  // Valid range 0 to 100, enforced by protovalidate.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double isolation_damper_percentage_sensor = 101 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "isolation_damper_percentage_sensor"
      subfields: "isolation"
      subfields: "damper"
      subfields: "percentage"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "percentage"
      standard_unit: "percent"
    },
    (buf.validate.field).double.gte = 0.0,
    (buf.validate.field).double.lte = 100.0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The leaving heat recovery coil temperature sensor.
  //
  // Composed of:
  //   leaving (descriptor): The process of exiting a thing or area where
  //     media leaves process (e.g., water leaving a heat exchanger).
  //   heat (descriptor): Quality of media indicating energy level.
  //   recovery (descriptor): Component or process used for the reclamation
  //     of heat.
  //   coil (component): Component that exchanges heat between two media
  //     streams.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `leaving_heat_recovery_coil_temperature_sensor`, point
  // type `sensor`. Measured as temperature; values are in kelvin, the
  // ontology's standard unit for that quantity.
  //
  // Expected range 255.9259259 to 338.7037037. This is a data-quality
  // expectation, not a constraint: a reading outside it is a fault to
  // report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double leaving_heat_recovery_coil_temperature_sensor = 102 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "leaving_heat_recovery_coil_temperature_sensor"
      subfields: "leaving"
      subfields: "heat"
      subfields: "recovery"
      subfields: "coil"
      subfields: "temperature"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "temperature"
      standard_unit: "kelvin"
      expected_min: 255.925926
      expected_max: 338.703704
    }
  ];

  // The load percentage sensor.
  //
  // Composed of:
  //   load (descriptor): Electrical load which indicates the consumption
  //     of electrical power.
  //   percentage (measurement): Measurement per hundred.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `load_percentage_sensor`, point type `sensor`. Measured
  // as percentage; values are in percent, the ontology's standard unit for
  // that quantity.
  //
  // Valid range 0 to 100, enforced by protovalidate.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double load_percentage_sensor = 103 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "load_percentage_sensor"
      subfields: "load"
      subfields: "percentage"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "percentage"
      standard_unit: "percent"
    },
    (buf.validate.field).double.gte = 0.0,
    (buf.validate.field).double.lte = 100.0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The local override alarm.
  //
  // Composed of:
  //   local (descriptor): A definite spatial location involving only a
  //     subset of the larger surrounding area.
  //   override (descriptor): An action that interrupts, cancels, or
  //     changes the current action or status.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `local_override_alarm`, point type `alarm`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue local_override_alarm = 104 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "local_override_alarm"
      subfields: "local"
      subfields: "override"
      subfields: "alarm"
      point_type: "alarm"
    }
  ];

  // The lost power alarm.
  //
  // Composed of:
  //   lost (measurement_descriptor): Indicates that a point has lost some
  //     quality or quantity inadvertently.
  //   power (measurement): Rate of energy consumption (assumed context is
  //     electrical and real, unless otherwise modified).
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `lost_power_alarm`, point type `alarm`. Measured as
  // power; values are in watts, the ontology's standard unit for that
  // quantity.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue lost_power_alarm = 105 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "lost_power_alarm"
      subfields: "lost"
      subfields: "power"
      subfields: "alarm"
      point_type: "alarm"
      measurement: "power"
      standard_unit: "watts"
    }
  ];

  // The low building air static pressure alarm.
  //
  // Composed of:
  //   low (descriptor): Level of control or measurement; below medium and
  //     high; opposite of high.
  //   building (descriptor): Applies to the entire building or group of
  //     zones within building (e.g., building_air_static_pressure_sensor).
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   static (measurement_descriptor): Describing a system in equilibrium
  //     or at rest (e.g., supply_air_static_pressure_sensor).
  //   pressure (measurement): Measured force per unit area within a system
  //     or process.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `low_building_air_static_pressure_alarm`, point type
  // `alarm`. Measured as pressure; values are in pascals, the ontology's
  // standard unit for that quantity.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue low_building_air_static_pressure_alarm = 106 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "low_building_air_static_pressure_alarm"
      subfields: "low"
      subfields: "building"
      subfields: "air"
      subfields: "static"
      subfields: "pressure"
      subfields: "alarm"
      point_type: "alarm"
      measurement: "pressure"
      standard_unit: "pascals"
    }
  ];

  // The low differential pressure alarm.
  //
  // Composed of:
  //   low (descriptor): Level of control or measurement; below medium and
  //     high; opposite of high.
  //   differential (measurement_descriptor): Measured difference between
  //     two reference points (e.g. differential_water_pressure)
  //   pressure (measurement): Measured force per unit area within a system
  //     or process.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `low_differential_pressure_alarm`, point type `alarm`.
  // Measured as pressure; values are in pascals, the ontology's standard
  // unit for that quantity.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue low_differential_pressure_alarm = 107 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "low_differential_pressure_alarm"
      subfields: "low"
      subfields: "differential"
      subfields: "pressure"
      subfields: "alarm"
      point_type: "alarm"
      measurement: "pressure"
      standard_unit: "pascals"
    }
  ];

  // The low discharge air flowrate alarm.
  //
  // Composed of:
  //   low (descriptor): Level of control or measurement; below medium and
  //     high; opposite of high.
  //   discharge (descriptor): Media leaving system to enter ambient
  //     conditioned space. Typically applies only to air-side systems.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   flowrate (measurement): Rate of fluid movement.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `low_discharge_air_flowrate_alarm`, point type `alarm`.
  // Measured as flowrate; values are in cubic_meters_per_second, the
  // ontology's standard unit for that quantity.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue low_discharge_air_flowrate_alarm = 108 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "low_discharge_air_flowrate_alarm"
      subfields: "low"
      subfields: "discharge"
      subfields: "air"
      subfields: "flowrate"
      subfields: "alarm"
      point_type: "alarm"
      measurement: "flowrate"
      standard_unit: "cubic_meters_per_second"
    }
  ];

  // The low discharge air static pressure alarm.
  //
  // Composed of:
  //   low (descriptor): Level of control or measurement; below medium and
  //     high; opposite of high.
  //   discharge (descriptor): Media leaving system to enter ambient
  //     conditioned space. Typically applies only to air-side systems.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   static (measurement_descriptor): Describing a system in equilibrium
  //     or at rest (e.g., supply_air_static_pressure_sensor).
  //   pressure (measurement): Measured force per unit area within a system
  //     or process.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `low_discharge_air_static_pressure_alarm`, point type
  // `alarm`. Measured as pressure; values are in pascals, the ontology's
  // standard unit for that quantity.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue low_discharge_air_static_pressure_alarm = 109 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "low_discharge_air_static_pressure_alarm"
      subfields: "low"
      subfields: "discharge"
      subfields: "air"
      subfields: "static"
      subfields: "pressure"
      subfields: "alarm"
      point_type: "alarm"
      measurement: "pressure"
      standard_unit: "pascals"
    }
  ];

  // The low discharge air temperature alarm.
  //
  // Composed of:
  //   low (descriptor): Level of control or measurement; below medium and
  //     high; opposite of high.
  //   discharge (descriptor): Media leaving system to enter ambient
  //     conditioned space. Typically applies only to air-side systems.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `low_discharge_air_temperature_alarm`, point type
  // `alarm`. Measured as temperature; values are in kelvin, the ontology's
  // standard unit for that quantity.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue low_discharge_air_temperature_alarm = 110 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "low_discharge_air_temperature_alarm"
      subfields: "low"
      subfields: "discharge"
      subfields: "air"
      subfields: "temperature"
      subfields: "alarm"
      point_type: "alarm"
      measurement: "temperature"
      standard_unit: "kelvin"
    }
  ];

  // The low exhaust air static pressure alarm.
  //
  // Composed of:
  //   low (descriptor): Level of control or measurement; below medium and
  //     high; opposite of high.
  //   exhaust (descriptor): Process or component used in the removal of
  //     air from a conditioned system to the outside atmosphere.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   static (measurement_descriptor): Describing a system in equilibrium
  //     or at rest (e.g., supply_air_static_pressure_sensor).
  //   pressure (measurement): Measured force per unit area within a system
  //     or process.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `low_exhaust_air_static_pressure_alarm`, point type
  // `alarm`. Measured as pressure; values are in pascals, the ontology's
  // standard unit for that quantity.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue low_exhaust_air_static_pressure_alarm = 111 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "low_exhaust_air_static_pressure_alarm"
      subfields: "low"
      subfields: "exhaust"
      subfields: "air"
      subfields: "static"
      subfields: "pressure"
      subfields: "alarm"
      point_type: "alarm"
      measurement: "pressure"
      standard_unit: "pascals"
    }
  ];

  // The low exhaust fan speed command.
  //
  // Composed of:
  //   low (descriptor): Level of control or measurement; below medium and
  //     high; opposite of high.
  //   exhaust (descriptor): Process or component used in the removal of
  //     air from a conditioned system to the outside atmosphere.
  //   fan (component): Component used for the distribution of air.
  //   speed (measurement_descriptor): Numeric setting of how fast to run a
  //     device, in the specified units. Typically used to describe
  //     revolutions of a motor as a fraction of nominal or maximum.
  //   command (point_type): The signal given to make an action happen.
  //     Defaults to multistate unless used alongside a measurement_type.
  //
  // Ontology field: `low_exhaust_fan_speed_command`, point type `command`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional OffOnValue low_exhaust_fan_speed_command = 112 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "low_exhaust_fan_speed_command"
      subfields: "low"
      subfields: "exhaust"
      subfields: "fan"
      subfields: "speed"
      subfields: "command"
      point_type: "command"
    }
  ];

  // The low exhaust fan speed status.
  //
  // Composed of:
  //   low (descriptor): Level of control or measurement; below medium and
  //     high; opposite of high.
  //   exhaust (descriptor): Process or component used in the removal of
  //     air from a conditioned system to the outside atmosphere.
  //   fan (component): Component used for the distribution of air.
  //   speed (measurement_descriptor): Numeric setting of how fast to run a
  //     device, in the specified units. Typically used to describe
  //     revolutions of a motor as a fraction of nominal or maximum.
  //   status (point_type): The multistate value indicating an observed
  //     state in a piece of equipment, often indicating if a command was
  //     effected. It is a neutral observation (i.e., no quality judgment of
  //     'good' or 'bad'). It also has no units of measurement; therefore, if
  //     combined with a measurement subfield, it will indicate that the
  //     field is the directional status based on some measurement of that
  //     type (e.g., power_status equates to an on/off value based on some
  //     inference of power).
  //
  // Ontology field: `low_exhaust_fan_speed_status`, point type `status`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional OffOnValue low_exhaust_fan_speed_status = 113 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "low_exhaust_fan_speed_status"
      subfields: "low"
      subfields: "exhaust"
      subfields: "fan"
      subfields: "speed"
      subfields: "status"
      point_type: "status"
    }
  ];

  // The low limit speed percentage setpoint.
  //
  // Composed of:
  //   low (descriptor): Level of control or measurement; below medium and
  //     high; opposite of high.
  //   limit (descriptor): A boundary condition for a control (e.g., low
  //     limit).
  //   speed (measurement_descriptor): Numeric setting of how fast to run a
  //     device, in the specified units. Typically used to describe
  //     revolutions of a motor as a fraction of nominal or maximum.
  //   percentage (measurement): Measurement per hundred.
  //   setpoint (point_type): Control target of process or system.
  //
  // Ontology field: `low_limit_speed_percentage_setpoint`, point type
  // `setpoint`. Measured as percentage; values are in percent, the
  // ontology's standard unit for that quantity.
  //
  // Valid range 0 to 100, enforced by protovalidate.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double low_limit_speed_percentage_setpoint = 114 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "low_limit_speed_percentage_setpoint"
      subfields: "low"
      subfields: "limit"
      subfields: "speed"
      subfields: "percentage"
      subfields: "setpoint"
      point_type: "setpoint"
      measurement: "percentage"
      standard_unit: "percent"
    },
    (buf.validate.field).double.gte = 0.0,
    (buf.validate.field).double.lte = 100.0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The low limit zone air co concentration setpoint.
  //
  // Composed of:
  //   low (descriptor): Level of control or measurement; below medium and
  //     high; opposite of high.
  //   limit (descriptor): A boundary condition for a control (e.g., low
  //     limit).
  //   zone (descriptor): Region of building which is conditioned.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   co (descriptor): Chemical formula and abbreviation for carbon
  //     monoxide.
  //   concentration (measurement): Concentration of something relative to
  //     something else (usually in parts per million or parts per billion).
  //   setpoint (point_type): Control target of process or system.
  //
  // Ontology field: `low_limit_zone_air_co_concentration_setpoint`, point
  // type `setpoint`. Measured as concentration; values are in
  // parts_per_unit, the ontology's standard unit for that quantity.
  //
  // Expected range 0.00035 to 0.0015. This is a data-quality expectation,
  // not a constraint: a reading outside it is a fault to report, not a
  // message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double low_limit_zone_air_co_concentration_setpoint = 115 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "low_limit_zone_air_co_concentration_setpoint"
      subfields: "low"
      subfields: "limit"
      subfields: "zone"
      subfields: "air"
      subfields: "co"
      subfields: "concentration"
      subfields: "setpoint"
      point_type: "setpoint"
      measurement: "concentration"
      standard_unit: "parts_per_unit"
      expected_min: 0.00035
      expected_max: 0.0015
    }
  ];

  // The low limit zone air no2 concentration setpoint.
  //
  // Composed of:
  //   low (descriptor): Level of control or measurement; below medium and
  //     high; opposite of high.
  //   limit (descriptor): A boundary condition for a control (e.g., low
  //     limit).
  //   zone (descriptor): Region of building which is conditioned.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   no2 (descriptor): Chemical formula and abbreviation for nitrogen
  //     dioxide.
  //   concentration (measurement): Concentration of something relative to
  //     something else (usually in parts per million or parts per billion).
  //   setpoint (point_type): Control target of process or system.
  //
  // Ontology field: `low_limit_zone_air_no2_concentration_setpoint`, point
  // type `setpoint`. Measured as concentration; values are in
  // parts_per_unit, the ontology's standard unit for that quantity.
  //
  // Valid range from 0, enforced by protovalidate. Expected range up to
  // 0.0005. This is a data-quality expectation, not a constraint: a reading
  // outside it is a fault to report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double low_limit_zone_air_no2_concentration_setpoint = 219 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "low_limit_zone_air_no2_concentration_setpoint"
      subfields: "low"
      subfields: "limit"
      subfields: "zone"
      subfields: "air"
      subfields: "no2"
      subfields: "concentration"
      subfields: "setpoint"
      point_type: "setpoint"
      measurement: "concentration"
      standard_unit: "parts_per_unit"
      expected_max: 0.0005
    },
    (buf.validate.field).double.gte = 0.0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The low limit zone air no concentration setpoint.
  //
  // Composed of:
  //   low (descriptor): Level of control or measurement; below medium and
  //     high; opposite of high.
  //   limit (descriptor): A boundary condition for a control (e.g., low
  //     limit).
  //   zone (descriptor): Region of building which is conditioned.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   no (descriptor): Chemical formula and abbreviation for nitrogen
  //     monoxide, which is a colorless gas and a toxic air pollutant.
  //   concentration (measurement): Concentration of something relative to
  //     something else (usually in parts per million or parts per billion).
  //   setpoint (point_type): Control target of process or system.
  //
  // Ontology field: `low_limit_zone_air_no_concentration_setpoint`, point
  // type `setpoint`. Measured as concentration; values are in
  // parts_per_unit, the ontology's standard unit for that quantity.
  //
  // Valid range from 0, enforced by protovalidate. Expected range up to
  // 0.0005. This is a data-quality expectation, not a constraint: a reading
  // outside it is a fault to report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double low_limit_zone_air_no_concentration_setpoint = 117 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "low_limit_zone_air_no_concentration_setpoint"
      subfields: "low"
      subfields: "limit"
      subfields: "zone"
      subfields: "air"
      subfields: "no"
      subfields: "concentration"
      subfields: "setpoint"
      point_type: "setpoint"
      measurement: "concentration"
      standard_unit: "parts_per_unit"
      expected_max: 0.0005
    },
    (buf.validate.field).double.gte = 0.0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The low limit zone air o2 percentage setpoint.
  //
  // Composed of:
  //   low (descriptor): Level of control or measurement; below medium and
  //     high; opposite of high.
  //   limit (descriptor): A boundary condition for a control (e.g., low
  //     limit).
  //   zone (descriptor): Region of building which is conditioned.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   o2 (descriptor): Chemical formula and abbreviation for oxygen.
  //   percentage (measurement): Measurement per hundred.
  //   setpoint (point_type): Control target of process or system.
  //
  // Ontology field: `low_limit_zone_air_o2_percentage_setpoint`, point type
  // `setpoint`. Measured as percentage; values are in percent, the
  // ontology's standard unit for that quantity.
  //
  // Valid range 0 to 100, enforced by protovalidate.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double low_limit_zone_air_o2_percentage_setpoint = 220 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "low_limit_zone_air_o2_percentage_setpoint"
      subfields: "low"
      subfields: "limit"
      subfields: "zone"
      subfields: "air"
      subfields: "o2"
      subfields: "percentage"
      subfields: "setpoint"
      point_type: "setpoint"
      measurement: "percentage"
      standard_unit: "percent"
    },
    (buf.validate.field).double.gte = 0.0,
    (buf.validate.field).double.lte = 100.0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The low speed command.
  //
  // Composed of:
  //   low (descriptor): Level of control or measurement; below medium and
  //     high; opposite of high.
  //   speed (measurement_descriptor): Numeric setting of how fast to run a
  //     device, in the specified units. Typically used to describe
  //     revolutions of a motor as a fraction of nominal or maximum.
  //   command (point_type): The signal given to make an action happen.
  //     Defaults to multistate unless used alongside a measurement_type.
  //
  // Ontology field: `low_speed_command`, point type `command`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional OffOnValue low_speed_command = 119 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "low_speed_command"
      subfields: "low"
      subfields: "speed"
      subfields: "command"
      point_type: "command"
    }
  ];

  // The low speed status.
  //
  // Composed of:
  //   low (descriptor): Level of control or measurement; below medium and
  //     high; opposite of high.
  //   speed (measurement_descriptor): Numeric setting of how fast to run a
  //     device, in the specified units. Typically used to describe
  //     revolutions of a motor as a fraction of nominal or maximum.
  //   status (point_type): The multistate value indicating an observed
  //     state in a piece of equipment, often indicating if a command was
  //     effected. It is a neutral observation (i.e., no quality judgment of
  //     'good' or 'bad'). It also has no units of measurement; therefore, if
  //     combined with a measurement subfield, it will indicate that the
  //     field is the directional status based on some measurement of that
  //     type (e.g., power_status equates to an on/off value based on some
  //     inference of power).
  //
  // Ontology field: `low_speed_status`, point type `status`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional OffOnValue low_speed_status = 120 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "low_speed_status"
      subfields: "low"
      subfields: "speed"
      subfields: "status"
      point_type: "status"
    }
  ];

  // The low supply air static pressure alarm.
  //
  // Composed of:
  //   low (descriptor): Level of control or measurement; below medium and
  //     high; opposite of high.
  //   supply (descriptor): Measurement or process of media as it is
  //     supplied to the end-use equipment within the system.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   static (measurement_descriptor): Describing a system in equilibrium
  //     or at rest (e.g., supply_air_static_pressure_sensor).
  //   pressure (measurement): Measured force per unit area within a system
  //     or process.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `low_supply_air_static_pressure_alarm`, point type
  // `alarm`. Measured as pressure; values are in pascals, the ontology's
  // standard unit for that quantity.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue low_supply_air_static_pressure_alarm = 121 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "low_supply_air_static_pressure_alarm"
      subfields: "low"
      subfields: "supply"
      subfields: "air"
      subfields: "static"
      subfields: "pressure"
      subfields: "alarm"
      point_type: "alarm"
      measurement: "pressure"
      standard_unit: "pascals"
    }
  ];

  // The low supply air temperature alarm.
  //
  // Composed of:
  //   low (descriptor): Level of control or measurement; below medium and
  //     high; opposite of high.
  //   supply (descriptor): Measurement or process of media as it is
  //     supplied to the end-use equipment within the system.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `low_supply_air_temperature_alarm`, point type `alarm`.
  // Measured as temperature; values are in kelvin, the ontology's standard
  // unit for that quantity.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue low_supply_air_temperature_alarm = 122 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "low_supply_air_temperature_alarm"
      subfields: "low"
      subfields: "supply"
      subfields: "air"
      subfields: "temperature"
      subfields: "alarm"
      point_type: "alarm"
      measurement: "temperature"
      standard_unit: "kelvin"
    }
  ];

  // The low zone air o2 percentage alarm.
  //
  // Composed of:
  //   low (descriptor): Level of control or measurement; below medium and
  //     high; opposite of high.
  //   zone (descriptor): Region of building which is conditioned.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   o2 (descriptor): Chemical formula and abbreviation for oxygen.
  //   percentage (measurement): Measurement per hundred.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `low_zone_air_o2_percentage_alarm`, point type `alarm`.
  // Measured as percentage; values are in percent, the ontology's standard
  // unit for that quantity.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  repeated ActiveInactiveValue low_zone_air_o2_percentage_alarm = 221 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "low_zone_air_o2_percentage_alarm"
      subfields: "low"
      subfields: "zone"
      subfields: "air"
      subfields: "o2"
      subfields: "percentage"
      subfields: "alarm"
      point_type: "alarm"
      measurement: "percentage"
      standard_unit: "percent"
      enumerated: true
    }
  ];

  // The low zone air relative humidity alarm.
  //
  // Composed of:
  //   low (descriptor): Level of control or measurement; below medium and
  //     high; opposite of high.
  //   zone (descriptor): Region of building which is conditioned.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   relative (measurement_descriptor): Quality of media with respect to
  //     theoretical minimum or maximum value for a given condition (e.g.,
  //     relative humidity).
  //   humidity (measurement): Concentration of water vapor in air
  //     (conditioned or unconditioned).
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `low_zone_air_relative_humidity_alarm`, point type
  // `alarm`. Measured as humidity; values are in percent_relative_humidity,
  // the ontology's standard unit for that quantity.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue low_zone_air_relative_humidity_alarm = 124 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "low_zone_air_relative_humidity_alarm"
      subfields: "low"
      subfields: "zone"
      subfields: "air"
      subfields: "relative"
      subfields: "humidity"
      subfields: "alarm"
      point_type: "alarm"
      measurement: "humidity"
      standard_unit: "percent_relative_humidity"
    }
  ];

  // The low zone air static pressure alarm.
  //
  // Composed of:
  //   low (descriptor): Level of control or measurement; below medium and
  //     high; opposite of high.
  //   zone (descriptor): Region of building which is conditioned.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   static (measurement_descriptor): Describing a system in equilibrium
  //     or at rest (e.g., supply_air_static_pressure_sensor).
  //   pressure (measurement): Measured force per unit area within a system
  //     or process.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `low_zone_air_static_pressure_alarm`, point type
  // `alarm`. Measured as pressure; values are in pascals, the ontology's
  // standard unit for that quantity.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue low_zone_air_static_pressure_alarm = 125 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "low_zone_air_static_pressure_alarm"
      subfields: "low"
      subfields: "zone"
      subfields: "air"
      subfields: "static"
      subfields: "pressure"
      subfields: "alarm"
      point_type: "alarm"
      measurement: "pressure"
      standard_unit: "pascals"
    }
  ];

  // The low zone air temperature alarm.
  //
  // Composed of:
  //   low (descriptor): Level of control or measurement; below medium and
  //     high; opposite of high.
  //   zone (descriptor): Region of building which is conditioned.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `low_zone_air_temperature_alarm`, point type `alarm`.
  // Measured as temperature; values are in kelvin, the ontology's standard
  // unit for that quantity.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue low_zone_air_temperature_alarm = 126 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "low_zone_air_temperature_alarm"
      subfields: "low"
      subfields: "zone"
      subfields: "air"
      subfields: "temperature"
      subfields: "alarm"
      point_type: "alarm"
      measurement: "temperature"
      standard_unit: "kelvin"
    }
  ];

  // The manufacturer label.
  //
  // Composed of:
  //   manufacturer (descriptor): The company which produces the asset or
  //     device.
  //   label (point_type): Identifying alias for component or system.
  //
  // Ontology field: `manufacturer_label`, point type `label`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional string manufacturer_label = 127 [
    (google.api.field_behavior) = OPTIONAL,
    (google.api.field_behavior) = IMMUTABLE,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "manufacturer_label"
      subfields: "manufacturer"
      subfields: "label"
      point_type: "label"
    }
  ];

  // The master alarm.
  //
  // Composed of:
  //   master (descriptor): Serving as the highest-level point of
  //     consolidation that unites individual devices, sensors, alarms, etc.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `master_alarm`, point type `alarm`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue master_alarm = 128 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "master_alarm"
      subfields: "master"
      subfields: "alarm"
      point_type: "alarm"
    }
  ];

  // The max zone air co concentration sensor.
  //
  // Composed of:
  //   max (aggregation): Maximum value; the largest value in a set of
  //     numeric values. Can be used for spatial (e.g.,
  //     max_zone_air_co2_concentration_sensor) or temporal (e.g.,
  //     tenminutefixed_max_xaxis_frequency_sensor) aggregations.
  //   zone (descriptor): Region of building which is conditioned.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   co (descriptor): Chemical formula and abbreviation for carbon
  //     monoxide.
  //   concentration (measurement): Concentration of something relative to
  //     something else (usually in parts per million or parts per billion).
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `max_zone_air_co_concentration_sensor`, point type
  // `sensor`. Measured as concentration; values are in parts_per_unit, the
  // ontology's standard unit for that quantity.
  //
  // Expected range 0.00035 to 0.0015. This is a data-quality expectation,
  // not a constraint: a reading outside it is a fault to report, not a
  // message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double max_zone_air_co_concentration_sensor = 129 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "max_zone_air_co_concentration_sensor"
      subfields: "max"
      subfields: "zone"
      subfields: "air"
      subfields: "co"
      subfields: "concentration"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "concentration"
      standard_unit: "parts_per_unit"
      expected_min: 0.00035
      expected_max: 0.0015
    }
  ];

  // The medium speed command.
  //
  // Composed of:
  //   medium (descriptor): Level of control or measurement; between high
  //     and low.
  //   speed (measurement_descriptor): Numeric setting of how fast to run a
  //     device, in the specified units. Typically used to describe
  //     revolutions of a motor as a fraction of nominal or maximum.
  //   command (point_type): The signal given to make an action happen.
  //     Defaults to multistate unless used alongside a measurement_type.
  //
  // Ontology field: `medium_speed_command`, point type `command`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional OffOnValue medium_speed_command = 130 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "medium_speed_command"
      subfields: "medium"
      subfields: "speed"
      subfields: "command"
      point_type: "command"
    }
  ];

  // The medium speed status.
  //
  // Composed of:
  //   medium (descriptor): Level of control or measurement; between high
  //     and low.
  //   speed (measurement_descriptor): Numeric setting of how fast to run a
  //     device, in the specified units. Typically used to describe
  //     revolutions of a motor as a fraction of nominal or maximum.
  //   status (point_type): The multistate value indicating an observed
  //     state in a piece of equipment, often indicating if a command was
  //     effected. It is a neutral observation (i.e., no quality judgment of
  //     'good' or 'bad'). It also has no units of measurement; therefore, if
  //     combined with a measurement subfield, it will indicate that the
  //     field is the directional status based on some measurement of that
  //     type (e.g., power_status equates to an on/off value based on some
  //     inference of power).
  //
  // Ontology field: `medium_speed_status`, point type `status`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional OffOnValue medium_speed_status = 131 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "medium_speed_status"
      subfields: "medium"
      subfields: "speed"
      subfields: "status"
      point_type: "status"
    }
  ];

  // The model label.
  //
  // Composed of:
  //   model (descriptor): Particular design version of an asset (e.g.
  //     model_label).
  //   label (point_type): Identifying alias for component or system.
  //
  // Ontology field: `model_label`, point type `label`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional string model_label = 132 [
    (google.api.field_behavior) = OPTIONAL,
    (google.api.field_behavior) = IMMUTABLE,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "model_label"
      subfields: "model"
      subfields: "label"
      point_type: "label"
    }
  ];

  // The motor temperature sensor.
  //
  // Composed of:
  //   motor (component): A machine, especially one powered by electricity
  //     or internal combustion, that supplies motive power for a vehicle or
  //     for some other device with moving parts.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `motor_temperature_sensor`, point type `sensor`.
  // Measured as temperature; values are in kelvin, the ontology's standard
  // unit for that quantity.
  //
  // Expected range 255.9259259 to 422.037037. This is a data-quality
  // expectation, not a constraint: a reading outside it is a fault to
  // report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double motor_temperature_sensor = 133 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "motor_temperature_sensor"
      subfields: "motor"
      subfields: "temperature"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "temperature"
      standard_unit: "kelvin"
      expected_min: 255.925926
      expected_max: 422.037037
    }
  ];

  // The optimal stop command.
  //
  // Composed of:
  //   optimal (descriptor): A method or solution being the most desirable
  //     or effective of all possibilities. Typically used in 'optimal
  //     start/stop,' referring to the most efficient point in time at which
  //     a system can turn off or on, while maintaining desired conditions
  //     and avoiding excess energy consumption or equipment wear.
  //   stop (measurement_descriptor): Indicates a point in time or space at
  //     which something has its ending.
  //   command (point_type): The signal given to make an action happen.
  //     Defaults to multistate unless used alongside a measurement_type.
  //
  // Ontology field: `optimal_stop_command`, point type `command`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional OffOnValue optimal_stop_command = 134 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "optimal_stop_command"
      subfields: "optimal"
      subfields: "stop"
      subfields: "command"
      point_type: "command"
    }
  ];

  // The output inverter power sensor.
  //
  // Composed of:
  //   output (descriptor): The output of a control loop (e.g. PID loop
  //     output).
  //   inverter (component): Device or circuitry that changes direct
  //     current to alternating current (e.g., variable frequency drive).
  //   power (measurement): Rate of energy consumption (assumed context is
  //     electrical and real, unless otherwise modified).
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `output_inverter_power_sensor`, point type `sensor`.
  // Measured as power; values are in watts, the ontology's standard unit for
  // that quantity.
  //
  // Expected range 0 to 1e+07. This is a data-quality expectation, not a
  // constraint: a reading outside it is a fault to report, not a message to
  // reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double output_inverter_power_sensor = 135 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "output_inverter_power_sensor"
      subfields: "output"
      subfields: "inverter"
      subfields: "power"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "power"
      standard_unit: "watts"
      expected_min: 0.0
      expected_max: 10000000.0
    }
  ];

  // The output inverter voltage sensor.
  //
  // Composed of:
  //   output (descriptor): The output of a control loop (e.g. PID loop
  //     output).
  //   inverter (component): Device or circuitry that changes direct
  //     current to alternating current (e.g., variable frequency drive).
  //   voltage (measurement): Electrical potential difference between two
  //     reference points.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `output_inverter_voltage_sensor`, point type `sensor`.
  // Measured as voltage; values are in volts, the ontology's standard unit
  // for that quantity.
  //
  // Expected range 0 to 500. This is a data-quality expectation, not a
  // constraint: a reading outside it is a fault to report, not a message to
  // reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double output_inverter_voltage_sensor = 136 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "output_inverter_voltage_sensor"
      subfields: "output"
      subfields: "inverter"
      subfields: "voltage"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "voltage"
      standard_unit: "volts"
      expected_min: 0.0
      expected_max: 500.0
    }
  ];

  // The outside air damper percentage sensor.
  //
  // Composed of:
  //   outside (descriptor): Process or measurement of local atmospheric
  //     conditions.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   damper (component): Component which meters the flow of air within a
  //     system or device.
  //   percentage (measurement): Measurement per hundred.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `outside_air_damper_percentage_sensor`, point type
  // `sensor`. Measured as percentage; values are in percent, the ontology's
  // standard unit for that quantity.
  //
  // Valid range 0 to 100, enforced by protovalidate.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double outside_air_damper_percentage_sensor = 137 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "outside_air_damper_percentage_sensor"
      subfields: "outside"
      subfields: "air"
      subfields: "damper"
      subfields: "percentage"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "percentage"
      standard_unit: "percent"
    },
    (buf.validate.field).double.gte = 0.0,
    (buf.validate.field).double.lte = 100.0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The outside air flowrate sensor.
  //
  // Composed of:
  //   outside (descriptor): Process or measurement of local atmospheric
  //     conditions.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   flowrate (measurement): Rate of fluid movement.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `outside_air_flowrate_sensor`, point type `sensor`.
  // Measured as flowrate; values are in cubic_meters_per_second, the
  // ontology's standard unit for that quantity.
  //
  // Expected range -0.002359737216 to 47.19474432. This is a data-quality
  // expectation, not a constraint: a reading outside it is a fault to
  // report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double outside_air_flowrate_sensor = 138 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "outside_air_flowrate_sensor"
      subfields: "outside"
      subfields: "air"
      subfields: "flowrate"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "flowrate"
      standard_unit: "cubic_meters_per_second"
      expected_min: -0.00236
      expected_max: 47.194744
    }
  ];

  // The outside air temperature sensor.
  //
  // Composed of:
  //   outside (descriptor): Process or measurement of local atmospheric
  //     conditions.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `outside_air_temperature_sensor`, point type `sensor`.
  // Measured as temperature; values are in kelvin, the ontology's standard
  // unit for that quantity.
  //
  // Expected range 255.9259259 to 322.037037. This is a data-quality
  // expectation, not a constraint: a reading outside it is a fault to
  // report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double outside_air_temperature_sensor = 139 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "outside_air_temperature_sensor"
      subfields: "outside"
      subfields: "air"
      subfields: "temperature"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "temperature"
      standard_unit: "kelvin"
      expected_min: 255.925926
      expected_max: 322.037037
    }
  ];

  // The power capacity.
  //
  // Composed of:
  //   power (measurement): Rate of energy consumption (assumed context is
  //     electrical and real, unless otherwise modified).
  //   capacity (point_type): A design parameter quantity (e.g., design
  //     motor power capacity). Is always a maximum limit.
  //
  // Ontology field: `power_capacity`, point type `capacity`. Measured as
  // power; values are in watts, the ontology's standard unit for that
  // quantity.
  //
  // Valid range from 0, enforced by protovalidate. Expected range up to
  // 100000. This is a data-quality expectation, not a constraint: a reading
  // outside it is a fault to report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double power_capacity = 140 [
    (google.api.field_behavior) = OPTIONAL,
    (google.api.field_behavior) = IMMUTABLE,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "power_capacity"
      subfields: "power"
      subfields: "capacity"
      point_type: "capacity"
      measurement: "power"
      standard_unit: "watts"
      expected_max: 100000.0
    },
    (buf.validate.field).double.gte = 0.0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The power sensor.
  //
  // Composed of:
  //   power (measurement): Rate of energy consumption (assumed context is
  //     electrical and real, unless otherwise modified).
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `power_sensor`, point type `sensor`. Measured as power;
  // values are in watts, the ontology's standard unit for that quantity.
  //
  // Expected range 0 to 1e+07. This is a data-quality expectation, not a
  // constraint: a reading outside it is a fault to report, not a message to
  // reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double power_sensor = 141 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "power_sensor"
      subfields: "power"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "power"
      standard_unit: "watts"
      expected_min: 0.0
      expected_max: 10000000.0
    }
  ];

  // The powerfactor sensor.
  //
  // Composed of:
  //   powerfactor (measurement): Ratio of real and apparent power.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `powerfactor_sensor`, point type `sensor`. Measured as
  // powerfactor; values are in no_units, the ontology's standard unit for
  // that quantity.
  //
  // Valid range -1 to 1, enforced by protovalidate.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double powerfactor_sensor = 142 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "powerfactor_sensor"
      subfields: "powerfactor"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "powerfactor"
      standard_unit: "no_units"
    },
    (buf.validate.field).double.gte = -1.0,
    (buf.validate.field).double.lte = 1.0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The pressurization request count.
  //
  // Composed of:
  //   pressurization (descriptor): Relating to the level of pressure in a
  //     system or vessel.
  //   request (descriptor): A signal from a system device that calls for
  //     more of a particular conditioning process (e.g., pressure requests
  //     from terminal units).
  //   count (point_type): Total count of actions or requests.
  //
  // Ontology field: `pressurization_request_count`, point type `count`.
  // Dimensionless: the ontology defines this point type as an integer count
  // rather than a measured quantity.
  //
  // Valid range from 0, enforced by protovalidate. Expected range up to 500.
  // This is a data-quality expectation, not a constraint: a reading outside
  // it is a fault to report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional int64 pressurization_request_count = 143 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "pressurization_request_count"
      subfields: "pressurization"
      subfields: "request"
      subfields: "count"
      point_type: "count"
      expected_max: 500.0
    },
    (buf.validate.field).int64.gte = 0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The refrigerant leak detection status.
  //
  // Composed of:
  //   refrigerant (descriptor): Substance used in the mechanical
  //     refrigeration process (e.g., R-134a)
  //   leak (descriptor): Media leaving the system abnormally.
  //   detection (descriptor): Process of identifying conditions.
  //   status (point_type): The multistate value indicating an observed
  //     state in a piece of equipment, often indicating if a command was
  //     effected. It is a neutral observation (i.e., no quality judgment of
  //     'good' or 'bad'). It also has no units of measurement; therefore, if
  //     combined with a measurement subfield, it will indicate that the
  //     field is the directional status based on some measurement of that
  //     type (e.g., power_status equates to an on/off value based on some
  //     inference of power).
  //
  // Ontology field: `refrigerant_leak_detection_status`, point type
  // `status`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue refrigerant_leak_detection_status = 144 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "refrigerant_leak_detection_status"
      subfields: "refrigerant"
      subfields: "leak"
      subfields: "detection"
      subfields: "status"
      point_type: "status"
    }
  ];

  // The request run command.
  //
  // Composed of:
  //   request (descriptor): A signal from a system device that calls for
  //     more of a particular conditioning process (e.g., pressure requests
  //     from terminal units).
  //   run (measurement_descriptor): State of being active or operating. By
  //     default applies to control program for system (e.g., VAV program
  //     run_command).
  //   command (point_type): The signal given to make an action happen.
  //     Defaults to multistate unless used alongside a measurement_type.
  //
  // Ontology field: `request_run_command`, point type `command`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional OffOnValue request_run_command = 145 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "request_run_command"
      subfields: "request"
      subfields: "run"
      subfields: "command"
      point_type: "command"
    }
  ];

  // The return air isolation damper command.
  //
  // Composed of:
  //   return (descriptor): Measurement or process of media as it is
  //     returned from the end-use equipment within the system.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   isolation (descriptor): Process of isolating one component or
  //     process from another.
  //   damper (component): Component which meters the flow of air within a
  //     system or device.
  //   command (point_type): The signal given to make an action happen.
  //     Defaults to multistate unless used alongside a measurement_type.
  //
  // Ontology field: `return_air_isolation_damper_command`, point type
  // `command`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ClosedOpenValue return_air_isolation_damper_command = 146 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "return_air_isolation_damper_command"
      subfields: "return"
      subfields: "air"
      subfields: "isolation"
      subfields: "damper"
      subfields: "command"
      point_type: "command"
    }
  ];

  // The return air isolation damper status.
  //
  // Composed of:
  //   return (descriptor): Measurement or process of media as it is
  //     returned from the end-use equipment within the system.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   isolation (descriptor): Process of isolating one component or
  //     process from another.
  //   damper (component): Component which meters the flow of air within a
  //     system or device.
  //   status (point_type): The multistate value indicating an observed
  //     state in a piece of equipment, often indicating if a command was
  //     effected. It is a neutral observation (i.e., no quality judgment of
  //     'good' or 'bad'). It also has no units of measurement; therefore, if
  //     combined with a measurement subfield, it will indicate that the
  //     field is the directional status based on some measurement of that
  //     type (e.g., power_status equates to an on/off value based on some
  //     inference of power).
  //
  // Ontology field: `return_air_isolation_damper_status`, point type
  // `status`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ClosedOpenValue return_air_isolation_damper_status = 147 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "return_air_isolation_damper_status"
      subfields: "return"
      subfields: "air"
      subfields: "isolation"
      subfields: "damper"
      subfields: "status"
      point_type: "status"
    }
  ];

  // The rotationalvelocity sensor.
  //
  // Composed of:
  //   rotationalvelocity (measurement): Vector measure of rotation rate,
  //     that refers to how fast an object rotates or revolves relative to
  //     another point.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `rotationalvelocity_sensor`, point type `sensor`.
  // Measured as rotationalvelocity; values are in revolutions_per_minute,
  // the ontology's standard unit for that quantity.
  //
  // Expected range 0 to 4000. This is a data-quality expectation, not a
  // constraint: a reading outside it is a fault to report, not a message to
  // reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double rotationalvelocity_sensor = 148 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "rotationalvelocity_sensor"
      subfields: "rotationalvelocity"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "rotationalvelocity"
      standard_unit: "revolutions_per_minute"
      expected_min: 0.0
      expected_max: 4000.0
    }
  ];

  // The run command.
  //
  // Composed of:
  //   run (measurement_descriptor): State of being active or operating. By
  //     default applies to control program for system (e.g., VAV program
  //     run_command).
  //   command (point_type): The signal given to make an action happen.
  //     Defaults to multistate unless used alongside a measurement_type.
  //
  // Ontology field: `run_command`, point type `command`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional OffOnValue run_command = 149 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "run_command"
      subfields: "run"
      subfields: "command"
      point_type: "command"
    }
  ];

  // The run mode.
  //
  // Composed of:
  //   run (measurement_descriptor): State of being active or operating. By
  //     default applies to control program for system (e.g., VAV program
  //     run_command).
  //   mode (point_type): Distinct mode of operation within system. For
  //     example economizer_mode has modes of 'enabled' or 'disabled.'
  //
  // Ontology field: `run_mode`, point type `mode`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional AutoCommissioningCoolingDehumidifyFanOnlyFlushingHeatingManualNeutralOffUnknownValue run_mode = 150 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "run_mode"
      subfields: "run"
      subfields: "mode"
      point_type: "mode"
    }
  ];

  // The run status.
  //
  // Composed of:
  //   run (measurement_descriptor): State of being active or operating. By
  //     default applies to control program for system (e.g., VAV program
  //     run_command).
  //   status (point_type): The multistate value indicating an observed
  //     state in a piece of equipment, often indicating if a command was
  //     effected. It is a neutral observation (i.e., no quality judgment of
  //     'good' or 'bad'). It also has no units of measurement; therefore, if
  //     combined with a measurement subfield, it will indicate that the
  //     field is the directional status based on some measurement of that
  //     type (e.g., power_status equates to an on/off value based on some
  //     inference of power).
  //
  // Ontology field: `run_status`, point type `status`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional OffOnStandbyUnknownValue run_status = 151 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "run_status"
      subfields: "run"
      subfields: "status"
      point_type: "status"
    }
  ];

  // The run time accumulator.
  //
  // Composed of:
  //   run (measurement_descriptor): State of being active or operating. By
  //     default applies to control program for system (e.g., VAV program
  //     run_command).
  //   time (measurement): Defined duration of something.
  //   accumulator (point_type): The total accumulated quantity (e.g.,
  //     total energy accumulated).
  //
  // Ontology field: `run_time_accumulator`, point type `accumulator`.
  // Measured as time; values are in seconds, the ontology's standard unit
  // for that quantity.
  //
  // Valid range from 0, enforced by protovalidate. Expected range up to
  // 1.25. This is a data-quality expectation, not a constraint: a reading
  // outside it is a fault to report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double run_time_accumulator = 152 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "run_time_accumulator"
      subfields: "run"
      subfields: "time"
      subfields: "accumulator"
      point_type: "accumulator"
      measurement: "time"
      standard_unit: "seconds"
      expected_max: 1.25
    },
    (buf.validate.field).double.gte = 0.0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The schedule run command.
  //
  // Composed of:
  //   schedule (descriptor): Refers to the time-of-day run-time
  //     requirements for the equipment.
  //   run (measurement_descriptor): State of being active or operating. By
  //     default applies to control program for system (e.g., VAV program
  //     run_command).
  //   command (point_type): The signal given to make an action happen.
  //     Defaults to multistate unless used alongside a measurement_type.
  //
  // Ontology field: `schedule_run_command`, point type `command`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional OffOnUnknownValue schedule_run_command = 153 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "schedule_run_command"
      subfields: "schedule"
      subfields: "run"
      subfields: "command"
      point_type: "command"
    }
  ];

  // The scrubber run command.
  //
  // Composed of:
  //   scrubber (component): An equipment that is used to remove harmful
  //     materials from exhaust air before they are released into the
  //     environment
  //   run (measurement_descriptor): State of being active or operating. By
  //     default applies to control program for system (e.g., VAV program
  //     run_command).
  //   command (point_type): The signal given to make an action happen.
  //     Defaults to multistate unless used alongside a measurement_type.
  //
  // Ontology field: `scrubber_run_command`, point type `command`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional OffOnValue scrubber_run_command = 154 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "scrubber_run_command"
      subfields: "scrubber"
      subfields: "run"
      subfields: "command"
      point_type: "command"
    }
  ];

  // The scrubber run mode.
  //
  // Composed of:
  //   scrubber (component): An equipment that is used to remove harmful
  //     materials from exhaust air before they are released into the
  //     environment
  //   run (measurement_descriptor): State of being active or operating. By
  //     default applies to control program for system (e.g., VAV program
  //     run_command).
  //   mode (point_type): Distinct mode of operation within system. For
  //     example economizer_mode has modes of 'enabled' or 'disabled.'
  //
  // Ontology field: `scrubber_run_mode`, point type `mode`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional AutoManualValue scrubber_run_mode = 155 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "scrubber_run_mode"
      subfields: "scrubber"
      subfields: "run"
      subfields: "mode"
      point_type: "mode"
    }
  ];

  // The scrubber run status.
  //
  // Composed of:
  //   scrubber (component): An equipment that is used to remove harmful
  //     materials from exhaust air before they are released into the
  //     environment
  //   run (measurement_descriptor): State of being active or operating. By
  //     default applies to control program for system (e.g., VAV program
  //     run_command).
  //   status (point_type): The multistate value indicating an observed
  //     state in a piece of equipment, often indicating if a command was
  //     effected. It is a neutral observation (i.e., no quality judgment of
  //     'good' or 'bad'). It also has no units of measurement; therefore, if
  //     combined with a measurement subfield, it will indicate that the
  //     field is the directional status based on some measurement of that
  //     type (e.g., power_status equates to an on/off value based on some
  //     inference of power).
  //
  // Ontology field: `scrubber_run_status`, point type `status`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional OffOnValue scrubber_run_status = 156 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "scrubber_run_status"
      subfields: "scrubber"
      subfields: "run"
      subfields: "status"
      point_type: "status"
    }
  ];

  // The speed frequency command.
  //
  // Composed of:
  //   speed (measurement_descriptor): Numeric setting of how fast to run a
  //     device, in the specified units. Typically used to describe
  //     revolutions of a motor as a fraction of nominal or maximum.
  //   frequency (measurement): The rate of cycling within a process.
  //     Typically used to describe voltage (and amperage) oscillation within
  //     AC power distribution components.
  //   command (point_type): The signal given to make an action happen.
  //     Defaults to multistate unless used alongside a measurement_type.
  //
  // Ontology field: `speed_frequency_command`, point type `command`.
  // Measured as frequency; values are in hertz, the ontology's standard unit
  // for that quantity.
  //
  // Expected range -0.5 to 65. This is a data-quality expectation, not a
  // constraint: a reading outside it is a fault to report, not a message to
  // reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double speed_frequency_command = 157 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "speed_frequency_command"
      subfields: "speed"
      subfields: "frequency"
      subfields: "command"
      point_type: "command"
      measurement: "frequency"
      standard_unit: "hertz"
      expected_min: -0.5
      expected_max: 65.0
    }
  ];

  // The speed frequency sensor.
  //
  // Composed of:
  //   speed (measurement_descriptor): Numeric setting of how fast to run a
  //     device, in the specified units. Typically used to describe
  //     revolutions of a motor as a fraction of nominal or maximum.
  //   frequency (measurement): The rate of cycling within a process.
  //     Typically used to describe voltage (and amperage) oscillation within
  //     AC power distribution components.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `speed_frequency_sensor`, point type `sensor`. Measured
  // as frequency; values are in hertz, the ontology's standard unit for that
  // quantity.
  //
  // Expected range -0.5 to 65. This is a data-quality expectation, not a
  // constraint: a reading outside it is a fault to report, not a message to
  // reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double speed_frequency_sensor = 158 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "speed_frequency_sensor"
      subfields: "speed"
      subfields: "frequency"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "frequency"
      standard_unit: "hertz"
      expected_min: -0.5
      expected_max: 65.0
    }
  ];

  // The speed mode.
  //
  // Composed of:
  //   speed (measurement_descriptor): Numeric setting of how fast to run a
  //     device, in the specified units. Typically used to describe
  //     revolutions of a motor as a fraction of nominal or maximum.
  //   mode (point_type): Distinct mode of operation within system. For
  //     example economizer_mode has modes of 'enabled' or 'disabled.'
  //
  // Ontology field: `speed_mode`, point type `mode`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional AutoBypassHighLowOffVfdValue speed_mode = 159 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "speed_mode"
      subfields: "speed"
      subfields: "mode"
      point_type: "mode"
    }
  ];

  // The speed percentage command.
  //
  // Composed of:
  //   speed (measurement_descriptor): Numeric setting of how fast to run a
  //     device, in the specified units. Typically used to describe
  //     revolutions of a motor as a fraction of nominal or maximum.
  //   percentage (measurement): Measurement per hundred.
  //   command (point_type): The signal given to make an action happen.
  //     Defaults to multistate unless used alongside a measurement_type.
  //
  // Ontology field: `speed_percentage_command`, point type `command`.
  // Measured as percentage; values are in percent, the ontology's standard
  // unit for that quantity.
  //
  // Valid range 0 to 100, enforced by protovalidate.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double speed_percentage_command = 160 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "speed_percentage_command"
      subfields: "speed"
      subfields: "percentage"
      subfields: "command"
      point_type: "command"
      measurement: "percentage"
      standard_unit: "percent"
    },
    (buf.validate.field).double.gte = 0.0,
    (buf.validate.field).double.lte = 100.0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The speed percentage sensor.
  //
  // Composed of:
  //   speed (measurement_descriptor): Numeric setting of how fast to run a
  //     device, in the specified units. Typically used to describe
  //     revolutions of a motor as a fraction of nominal or maximum.
  //   percentage (measurement): Measurement per hundred.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `speed_percentage_sensor`, point type `sensor`. Measured
  // as percentage; values are in percent, the ontology's standard unit for
  // that quantity.
  //
  // Valid range 0 to 100, enforced by protovalidate.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double speed_percentage_sensor = 161 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "speed_percentage_sensor"
      subfields: "speed"
      subfields: "percentage"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "percentage"
      standard_unit: "percent"
    },
    (buf.validate.field).double.gte = 0.0,
    (buf.validate.field).double.lte = 100.0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The suction pressure sensor.
  //
  // Composed of:
  //   suction (descriptor): The production of a partial vacuum in order to
  //     force fluid into a vacant space (such as refrigerant in a
  //     compressor).
  //   pressure (measurement): Measured force per unit area within a system
  //     or process.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `suction_pressure_sensor`, point type `sensor`. Measured
  // as pressure; values are in pascals, the ontology's standard unit for
  // that quantity.
  //
  // Expected range -34473.78945 to 344737.8945. This is a data-quality
  // expectation, not a constraint: a reading outside it is a fault to
  // report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double suction_pressure_sensor = 162 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "suction_pressure_sensor"
      subfields: "suction"
      subfields: "pressure"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "pressure"
      standard_unit: "pascals"
      expected_min: -34473.78945
      expected_max: 344737.8945
    }
  ];

  // The supply air damper percentage command.
  //
  // Composed of:
  //   supply (descriptor): Measurement or process of media as it is
  //     supplied to the end-use equipment within the system.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   damper (component): Component which meters the flow of air within a
  //     system or device.
  //   percentage (measurement): Measurement per hundred.
  //   command (point_type): The signal given to make an action happen.
  //     Defaults to multistate unless used alongside a measurement_type.
  //
  // Ontology field: `supply_air_damper_percentage_command`, point type
  // `command`. Measured as percentage; values are in percent, the ontology's
  // standard unit for that quantity.
  //
  // Valid range 0 to 100, enforced by protovalidate.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double supply_air_damper_percentage_command = 163 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "supply_air_damper_percentage_command"
      subfields: "supply"
      subfields: "air"
      subfields: "damper"
      subfields: "percentage"
      subfields: "command"
      point_type: "command"
      measurement: "percentage"
      standard_unit: "percent"
    },
    (buf.validate.field).double.gte = 0.0,
    (buf.validate.field).double.lte = 100.0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The supply air flowrate sensor.
  //
  // Composed of:
  //   supply (descriptor): Measurement or process of media as it is
  //     supplied to the end-use equipment within the system.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   flowrate (measurement): Rate of fluid movement.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `supply_air_flowrate_sensor`, point type `sensor`.
  // Measured as flowrate; values are in cubic_meters_per_second, the
  // ontology's standard unit for that quantity.
  //
  // Expected range -0.002359737216 to 94.38948864. This is a data-quality
  // expectation, not a constraint: a reading outside it is a fault to
  // report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double supply_air_flowrate_sensor = 164 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "supply_air_flowrate_sensor"
      subfields: "supply"
      subfields: "air"
      subfields: "flowrate"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "flowrate"
      standard_unit: "cubic_meters_per_second"
      expected_min: -0.00236
      expected_max: 94.389489
    }
  ];

  // The supply air static pressure sensor.
  //
  // Composed of:
  //   supply (descriptor): Measurement or process of media as it is
  //     supplied to the end-use equipment within the system.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   static (measurement_descriptor): Describing a system in equilibrium
  //     or at rest (e.g., supply_air_static_pressure_sensor).
  //   pressure (measurement): Measured force per unit area within a system
  //     or process.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `supply_air_static_pressure_sensor`, point type
  // `sensor`. Measured as pressure; values are in pascals, the ontology's
  // standard unit for that quantity.
  //
  // Expected range -124.540012 to 2490.80024. This is a data-quality
  // expectation, not a constraint: a reading outside it is a fault to
  // report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double supply_air_static_pressure_sensor = 165 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "supply_air_static_pressure_sensor"
      subfields: "supply"
      subfields: "air"
      subfields: "static"
      subfields: "pressure"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "pressure"
      standard_unit: "pascals"
      expected_min: -124.540012
      expected_max: 2490.80024
    }
  ];

  // The supply air static pressure setpoint.
  //
  // Composed of:
  //   supply (descriptor): Measurement or process of media as it is
  //     supplied to the end-use equipment within the system.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   static (measurement_descriptor): Describing a system in equilibrium
  //     or at rest (e.g., supply_air_static_pressure_sensor).
  //   pressure (measurement): Measured force per unit area within a system
  //     or process.
  //   setpoint (point_type): Control target of process or system.
  //
  // Ontology field: `supply_air_static_pressure_setpoint`, point type
  // `setpoint`. Measured as pressure; values are in pascals, the ontology's
  // standard unit for that quantity.
  //
  // Expected range -124.540012 to 2490.80024. This is a data-quality
  // expectation, not a constraint: a reading outside it is a fault to
  // report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double supply_air_static_pressure_setpoint = 166 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "supply_air_static_pressure_setpoint"
      subfields: "supply"
      subfields: "air"
      subfields: "static"
      subfields: "pressure"
      subfields: "setpoint"
      point_type: "setpoint"
      measurement: "pressure"
      standard_unit: "pascals"
      expected_min: -124.540012
      expected_max: 2490.80024
    }
  ];

  // The supply air temperature sensor.
  //
  // Composed of:
  //   supply (descriptor): Measurement or process of media as it is
  //     supplied to the end-use equipment within the system.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `supply_air_temperature_sensor`, point type `sensor`.
  // Measured as temperature; values are in kelvin, the ontology's standard
  // unit for that quantity.
  //
  // Expected range 255.9259259 to 338.7037037. This is a data-quality
  // expectation, not a constraint: a reading outside it is a fault to
  // report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double supply_air_temperature_sensor = 167 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "supply_air_temperature_sensor"
      subfields: "supply"
      subfields: "air"
      subfields: "temperature"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "temperature"
      standard_unit: "kelvin"
      expected_min: 255.925926
      expected_max: 338.703704
    }
  ];

  // The supply fan run command.
  //
  // Composed of:
  //   supply (descriptor): Measurement or process of media as it is
  //     supplied to the end-use equipment within the system.
  //   fan (component): Component used for the distribution of air.
  //   run (measurement_descriptor): State of being active or operating. By
  //     default applies to control program for system (e.g., VAV program
  //     run_command).
  //   command (point_type): The signal given to make an action happen.
  //     Defaults to multistate unless used alongside a measurement_type.
  //
  // Ontology field: `supply_fan_run_command`, point type `command`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional OffOnValue supply_fan_run_command = 168 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "supply_fan_run_command"
      subfields: "supply"
      subfields: "fan"
      subfields: "run"
      subfields: "command"
      point_type: "command"
    }
  ];

  // The supply fan run status.
  //
  // Composed of:
  //   supply (descriptor): Measurement or process of media as it is
  //     supplied to the end-use equipment within the system.
  //   fan (component): Component used for the distribution of air.
  //   run (measurement_descriptor): State of being active or operating. By
  //     default applies to control program for system (e.g., VAV program
  //     run_command).
  //   status (point_type): The multistate value indicating an observed
  //     state in a piece of equipment, often indicating if a command was
  //     effected. It is a neutral observation (i.e., no quality judgment of
  //     'good' or 'bad'). It also has no units of measurement; therefore, if
  //     combined with a measurement subfield, it will indicate that the
  //     field is the directional status based on some measurement of that
  //     type (e.g., power_status equates to an on/off value based on some
  //     inference of power).
  //
  // Ontology field: `supply_fan_run_status`, point type `status`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional OffOnUnknownValue supply_fan_run_status = 169 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "supply_fan_run_status"
      subfields: "supply"
      subfields: "fan"
      subfields: "run"
      subfields: "status"
      point_type: "status"
    }
  ];

  // The supply fan speed frequency sensor.
  //
  // Composed of:
  //   supply (descriptor): Measurement or process of media as it is
  //     supplied to the end-use equipment within the system.
  //   fan (component): Component used for the distribution of air.
  //   speed (measurement_descriptor): Numeric setting of how fast to run a
  //     device, in the specified units. Typically used to describe
  //     revolutions of a motor as a fraction of nominal or maximum.
  //   frequency (measurement): The rate of cycling within a process.
  //     Typically used to describe voltage (and amperage) oscillation within
  //     AC power distribution components.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `supply_fan_speed_frequency_sensor`, point type
  // `sensor`. Measured as frequency; values are in hertz, the ontology's
  // standard unit for that quantity.
  //
  // Expected range -0.5 to 65. This is a data-quality expectation, not a
  // constraint: a reading outside it is a fault to report, not a message to
  // reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double supply_fan_speed_frequency_sensor = 170 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "supply_fan_speed_frequency_sensor"
      subfields: "supply"
      subfields: "fan"
      subfields: "speed"
      subfields: "frequency"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "frequency"
      standard_unit: "hertz"
      expected_min: -0.5
      expected_max: 65.0
    }
  ];

  // The supply fan speed percentage command.
  //
  // Composed of:
  //   supply (descriptor): Measurement or process of media as it is
  //     supplied to the end-use equipment within the system.
  //   fan (component): Component used for the distribution of air.
  //   speed (measurement_descriptor): Numeric setting of how fast to run a
  //     device, in the specified units. Typically used to describe
  //     revolutions of a motor as a fraction of nominal or maximum.
  //   percentage (measurement): Measurement per hundred.
  //   command (point_type): The signal given to make an action happen.
  //     Defaults to multistate unless used alongside a measurement_type.
  //
  // Ontology field: `supply_fan_speed_percentage_command`, point type
  // `command`. Measured as percentage; values are in percent, the ontology's
  // standard unit for that quantity.
  //
  // Valid range 0 to 100, enforced by protovalidate.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double supply_fan_speed_percentage_command = 171 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "supply_fan_speed_percentage_command"
      subfields: "supply"
      subfields: "fan"
      subfields: "speed"
      subfields: "percentage"
      subfields: "command"
      point_type: "command"
      measurement: "percentage"
      standard_unit: "percent"
    },
    (buf.validate.field).double.gte = 0.0,
    (buf.validate.field).double.lte = 100.0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The switch command.
  //
  // Composed of:
  //   switch (component): Component to disrupt an electrical cirucit.
  //   command (point_type): The signal given to make an action happen.
  //     Defaults to multistate unless used alongside a measurement_type.
  //
  // Ontology field: `switch_command`, point type `command`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional OffOnValue switch_command = 172 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "switch_command"
      subfields: "switch"
      subfields: "command"
      point_type: "command"
    }
  ];

  // The torque sensor.
  //
  // Composed of:
  //   torque (measurement): A force that causes rotation around an axis.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `torque_sensor`, point type `sensor`. Measured as
  // torque; values are in newton_meters, the ontology's standard unit for
  // that quantity.
  //
  // Valid range from 0, enforced by protovalidate. Expected range up to 678.
  // This is a data-quality expectation, not a constraint: a reading outside
  // it is a fault to report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double torque_sensor = 173 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "torque_sensor"
      subfields: "torque"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "torque"
      standard_unit: "newton_meters"
      expected_max: 678.0
    },
    (buf.validate.field).double.gte = 0.0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The user occupancy override status.
  //
  // Composed of:
  //   user (descriptor): A person who uses or operates something.
  //   occupancy (descriptor): State of being occupied.
  //   override (descriptor): An action that interrupts, cancels, or
  //     changes the current action or status.
  //   status (point_type): The multistate value indicating an observed
  //     state in a piece of equipment, often indicating if a command was
  //     effected. It is a neutral observation (i.e., no quality judgment of
  //     'good' or 'bad'). It also has no units of measurement; therefore, if
  //     combined with a measurement subfield, it will indicate that the
  //     field is the directional status based on some measurement of that
  //     type (e.g., power_status equates to an on/off value based on some
  //     inference of power).
  //
  // Ontology field: `user_occupancy_override_status`, point type `status`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional DisabledEnabledOccupiedUnoccupiedValue user_occupancy_override_status = 174 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "user_occupancy_override_status"
      subfields: "user"
      subfields: "occupancy"
      subfields: "override"
      subfields: "status"
      point_type: "status"
    }
  ];

  // The ventilation request count.
  //
  // Composed of:
  //   ventilation (descriptor): Process used to provide fresh (typically
  //     atmospheric) air into a system or zone.
  //   request (descriptor): A signal from a system device that calls for
  //     more of a particular conditioning process (e.g., pressure requests
  //     from terminal units).
  //   count (point_type): Total count of actions or requests.
  //
  // Ontology field: `ventilation_request_count`, point type `count`.
  // Dimensionless: the ontology defines this point type as an integer count
  // rather than a measured quantity.
  //
  // Valid range from 0, enforced by protovalidate. Expected range up to 500.
  // This is a data-quality expectation, not a constraint: a reading outside
  // it is a fault to report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional int64 ventilation_request_count = 175 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "ventilation_request_count"
      subfields: "ventilation"
      subfields: "request"
      subfields: "count"
      point_type: "count"
      expected_max: 500.0
    },
    (buf.validate.field).int64.gte = 0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The voltage sensor.
  //
  // Composed of:
  //   voltage (measurement): Electrical potential difference between two
  //     reference points.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `voltage_sensor`, point type `sensor`. Measured as
  // voltage; values are in volts, the ontology's standard unit for that
  // quantity.
  //
  // Expected range 0 to 1000. This is a data-quality expectation, not a
  // constraint: a reading outside it is a fault to report, not a message to
  // reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double voltage_sensor = 176 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "voltage_sensor"
      subfields: "voltage"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "voltage"
      standard_unit: "volts"
      expected_min: 0.0
      expected_max: 1000.0
    }
  ];

  // The warmup request count.
  //
  // Composed of:
  //   warmup (descriptor): Process, measured effect or requirement for
  //     heating; used following building unoccupied periods and in optimal
  //     start applications.
  //   request (descriptor): A signal from a system device that calls for
  //     more of a particular conditioning process (e.g., pressure requests
  //     from terminal units).
  //   count (point_type): Total count of actions or requests.
  //
  // Ontology field: `warmup_request_count`, point type `count`.
  // Dimensionless: the ontology defines this point type as an integer count
  // rather than a measured quantity.
  //
  // Valid range from 0, enforced by protovalidate. Expected range up to 500.
  // This is a data-quality expectation, not a constraint: a reading outside
  // it is a fault to report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional int64 warmup_request_count = 177 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "warmup_request_count"
      subfields: "warmup"
      subfields: "request"
      subfields: "count"
      point_type: "count"
      expected_max: 500.0
    },
    (buf.validate.field).int64.gte = 0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The warmup run command.
  //
  // Composed of:
  //   warmup (descriptor): Process, measured effect or requirement for
  //     heating; used following building unoccupied periods and in optimal
  //     start applications.
  //   run (measurement_descriptor): State of being active or operating. By
  //     default applies to control program for system (e.g., VAV program
  //     run_command).
  //   command (point_type): The signal given to make an action happen.
  //     Defaults to multistate unless used alongside a measurement_type.
  //
  // Ontology field: `warmup_run_command`, point type `command`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional OffOnValue warmup_run_command = 178 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "warmup_run_command"
      subfields: "warmup"
      subfields: "run"
      subfields: "command"
      point_type: "command"
    }
  ];

  // The zone air co2 concentration sensor.
  //
  // Composed of:
  //   zone (descriptor): Region of building which is conditioned.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   co2 (descriptor): Chemical formula and abbreviation for carbon
  //     dioxide.
  //   concentration (measurement): Concentration of something relative to
  //     something else (usually in parts per million or parts per billion).
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `zone_air_co2_concentration_sensor`, point type
  // `sensor`. Measured as concentration; values are in parts_per_unit, the
  // ontology's standard unit for that quantity.
  //
  // Expected range 5e-05 to 0.005. This is a data-quality expectation, not a
  // constraint: a reading outside it is a fault to report, not a message to
  // reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double zone_air_co2_concentration_sensor = 222 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "zone_air_co2_concentration_sensor"
      subfields: "zone"
      subfields: "air"
      subfields: "co2"
      subfields: "concentration"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "concentration"
      standard_unit: "parts_per_unit"
      expected_min: 0.00005
      expected_max: 0.005
    }
  ];

  // The zone air co2 concentration setpoint.
  //
  // Composed of:
  //   zone (descriptor): Region of building which is conditioned.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   co2 (descriptor): Chemical formula and abbreviation for carbon
  //     dioxide.
  //   concentration (measurement): Concentration of something relative to
  //     something else (usually in parts per million or parts per billion).
  //   setpoint (point_type): Control target of process or system.
  //
  // Ontology field: `zone_air_co2_concentration_setpoint`, point type
  // `setpoint`. Measured as concentration; values are in parts_per_unit, the
  // ontology's standard unit for that quantity.
  //
  // Expected range 5e-05 to 0.005. This is a data-quality expectation, not a
  // constraint: a reading outside it is a fault to report, not a message to
  // reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double zone_air_co2_concentration_setpoint = 223 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "zone_air_co2_concentration_setpoint"
      subfields: "zone"
      subfields: "air"
      subfields: "co2"
      subfields: "concentration"
      subfields: "setpoint"
      point_type: "setpoint"
      measurement: "concentration"
      standard_unit: "parts_per_unit"
      expected_min: 0.00005
      expected_max: 0.005
    }
  ];

  // The zone air co2 deadband concentration setpoint.
  //
  // Composed of:
  //   zone (descriptor): Region of building which is conditioned.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   co2 (descriptor): Chemical formula and abbreviation for carbon
  //     dioxide.
  //   deadband (measurement_descriptor): Represents a range in which the
  //     controller does not do anything.
  //   concentration (measurement): Concentration of something relative to
  //     something else (usually in parts per million or parts per billion).
  //   setpoint (point_type): Control target of process or system.
  //
  // Ontology field: `zone_air_co2_deadband_concentration_setpoint`, point
  // type `setpoint`. Measured as concentration; values are in
  // parts_per_unit, the ontology's standard unit for that quantity.
  //
  // Expected range 5e-05 to 0.005. This is a data-quality expectation, not a
  // constraint: a reading outside it is a fault to report, not a message to
  // reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double zone_air_co2_deadband_concentration_setpoint = 224 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "zone_air_co2_deadband_concentration_setpoint"
      subfields: "zone"
      subfields: "air"
      subfields: "co2"
      subfields: "deadband"
      subfields: "concentration"
      subfields: "setpoint"
      point_type: "setpoint"
      measurement: "concentration"
      standard_unit: "parts_per_unit"
      expected_min: 0.00005
      expected_max: 0.005
    }
  ];

  // The zone air co concentration sensor.
  //
  // Composed of:
  //   zone (descriptor): Region of building which is conditioned.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   co (descriptor): Chemical formula and abbreviation for carbon
  //     monoxide.
  //   concentration (measurement): Concentration of something relative to
  //     something else (usually in parts per million or parts per billion).
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `zone_air_co_concentration_sensor`, point type `sensor`.
  // Measured as concentration; values are in parts_per_unit, the ontology's
  // standard unit for that quantity.
  //
  // Expected range 0.00035 to 0.0015. This is a data-quality expectation,
  // not a constraint: a reading outside it is a fault to report, not a
  // message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  repeated double zone_air_co_concentration_sensor = 182 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "zone_air_co_concentration_sensor"
      subfields: "zone"
      subfields: "air"
      subfields: "co"
      subfields: "concentration"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "concentration"
      standard_unit: "parts_per_unit"
      enumerated: true
      expected_min: 0.00035
      expected_max: 0.0015
    }
  ];

  // The zone air co concentration setpoint.
  //
  // Composed of:
  //   zone (descriptor): Region of building which is conditioned.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   co (descriptor): Chemical formula and abbreviation for carbon
  //     monoxide.
  //   concentration (measurement): Concentration of something relative to
  //     something else (usually in parts per million or parts per billion).
  //   setpoint (point_type): Control target of process or system.
  //
  // Ontology field: `zone_air_co_concentration_setpoint`, point type
  // `setpoint`. Measured as concentration; values are in parts_per_unit, the
  // ontology's standard unit for that quantity.
  //
  // Expected range 0.00035 to 0.0015. This is a data-quality expectation,
  // not a constraint: a reading outside it is a fault to report, not a
  // message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double zone_air_co_concentration_setpoint = 183 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "zone_air_co_concentration_setpoint"
      subfields: "zone"
      subfields: "air"
      subfields: "co"
      subfields: "concentration"
      subfields: "setpoint"
      point_type: "setpoint"
      measurement: "concentration"
      standard_unit: "parts_per_unit"
      expected_min: 0.00035
      expected_max: 0.0015
    }
  ];

  // The zone air cooling temperature setpoint.
  //
  // Composed of:
  //   zone (descriptor): Region of building which is conditioned.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   cooling (descriptor): Process, measured effect or requirement for
  //     cooling.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   setpoint (point_type): Control target of process or system.
  //
  // Ontology field: `zone_air_cooling_temperature_setpoint`, point type
  // `setpoint`. Measured as temperature; values are in kelvin, the
  // ontology's standard unit for that quantity.
  //
  // Expected range 255.9259259 to 308.1481481. This is a data-quality
  // expectation, not a constraint: a reading outside it is a fault to
  // report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double zone_air_cooling_temperature_setpoint = 184 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "zone_air_cooling_temperature_setpoint"
      subfields: "zone"
      subfields: "air"
      subfields: "cooling"
      subfields: "temperature"
      subfields: "setpoint"
      point_type: "setpoint"
      measurement: "temperature"
      standard_unit: "kelvin"
      expected_min: 255.925926
      expected_max: 308.148148
    }
  ];

  // The zone air deadband temperature setpoint.
  //
  // Composed of:
  //   zone (descriptor): Region of building which is conditioned.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   deadband (measurement_descriptor): Represents a range in which the
  //     controller does not do anything.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   setpoint (point_type): Control target of process or system.
  //
  // Ontology field: `zone_air_deadband_temperature_setpoint`, point type
  // `setpoint`. Measured as temperature; values are in kelvin, the
  // ontology's standard unit for that quantity.
  //
  // Expected range 255.9259259 to 308.1481481. This is a data-quality
  // expectation, not a constraint: a reading outside it is a fault to
  // report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double zone_air_deadband_temperature_setpoint = 185 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "zone_air_deadband_temperature_setpoint"
      subfields: "zone"
      subfields: "air"
      subfields: "deadband"
      subfields: "temperature"
      subfields: "setpoint"
      point_type: "setpoint"
      measurement: "temperature"
      standard_unit: "kelvin"
      expected_min: 255.925926
      expected_max: 308.148148
    }
  ];

  // The zone air dewpoint temperature sensor.
  //
  // Composed of:
  //   zone (descriptor): Region of building which is conditioned.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   dewpoint (measurement_descriptor): The thermodynamic point at which
  //     water condenses from standing air.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `zone_air_dewpoint_temperature_sensor`, point type
  // `sensor`. Measured as temperature; values are in kelvin, the ontology's
  // standard unit for that quantity.
  //
  // Expected range 255.9259259 to 310.9259259. This is a data-quality
  // expectation, not a constraint: a reading outside it is a fault to
  // report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double zone_air_dewpoint_temperature_sensor = 186 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "zone_air_dewpoint_temperature_sensor"
      subfields: "zone"
      subfields: "air"
      subfields: "dewpoint"
      subfields: "temperature"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "temperature"
      standard_unit: "kelvin"
      expected_min: 255.925926
      expected_max: 310.925926
    }
  ];

  // The zone air dewpoint temperature setpoint.
  //
  // Composed of:
  //   zone (descriptor): Region of building which is conditioned.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   dewpoint (measurement_descriptor): The thermodynamic point at which
  //     water condenses from standing air.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   setpoint (point_type): Control target of process or system.
  //
  // Ontology field: `zone_air_dewpoint_temperature_setpoint`, point type
  // `setpoint`. Measured as temperature; values are in kelvin, the
  // ontology's standard unit for that quantity.
  //
  // Expected range 255.9259259 to 310.9259259. This is a data-quality
  // expectation, not a constraint: a reading outside it is a fault to
  // report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double zone_air_dewpoint_temperature_setpoint = 187 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "zone_air_dewpoint_temperature_setpoint"
      subfields: "zone"
      subfields: "air"
      subfields: "dewpoint"
      subfields: "temperature"
      subfields: "setpoint"
      point_type: "setpoint"
      measurement: "temperature"
      standard_unit: "kelvin"
      expected_min: 255.925926
      expected_max: 310.925926
    }
  ];

  // The zone air heating temperature setpoint.
  //
  // Composed of:
  //   zone (descriptor): Region of building which is conditioned.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   heating (descriptor): Process, measured effect or requirement for
  //     heating.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   setpoint (point_type): Control target of process or system.
  //
  // Ontology field: `zone_air_heating_temperature_setpoint`, point type
  // `setpoint`. Measured as temperature; values are in kelvin, the
  // ontology's standard unit for that quantity.
  //
  // Expected range 255.9259259 to 308.1481481. This is a data-quality
  // expectation, not a constraint: a reading outside it is a fault to
  // report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double zone_air_heating_temperature_setpoint = 188 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "zone_air_heating_temperature_setpoint"
      subfields: "zone"
      subfields: "air"
      subfields: "heating"
      subfields: "temperature"
      subfields: "setpoint"
      point_type: "setpoint"
      measurement: "temperature"
      standard_unit: "kelvin"
      expected_min: 255.925926
      expected_max: 308.148148
    }
  ];

  // The zone air no2 concentration sensor.
  //
  // Composed of:
  //   zone (descriptor): Region of building which is conditioned.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   no2 (descriptor): Chemical formula and abbreviation for nitrogen
  //     dioxide.
  //   concentration (measurement): Concentration of something relative to
  //     something else (usually in parts per million or parts per billion).
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `zone_air_no2_concentration_sensor`, point type
  // `sensor`. Measured as concentration; values are in parts_per_unit, the
  // ontology's standard unit for that quantity.
  //
  // Expected range 0 to 0.0001. This is a data-quality expectation, not a
  // constraint: a reading outside it is a fault to report, not a message to
  // reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  repeated double zone_air_no2_concentration_sensor = 225 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "zone_air_no2_concentration_sensor"
      subfields: "zone"
      subfields: "air"
      subfields: "no2"
      subfields: "concentration"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "concentration"
      standard_unit: "parts_per_unit"
      enumerated: true
      expected_min: 0.0
      expected_max: 0.0001
    }
  ];

  // The zone air no concentration sensor.
  //
  // Composed of:
  //   zone (descriptor): Region of building which is conditioned.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   no (descriptor): Chemical formula and abbreviation for nitrogen
  //     monoxide, which is a colorless gas and a toxic air pollutant.
  //   concentration (measurement): Concentration of something relative to
  //     something else (usually in parts per million or parts per billion).
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `zone_air_no_concentration_sensor`, point type `sensor`.
  // Measured as concentration; values are in parts_per_unit, the ontology's
  // standard unit for that quantity.
  //
  // Expected range 0 to 0.0001. This is a data-quality expectation, not a
  // constraint: a reading outside it is a fault to report, not a message to
  // reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double zone_air_no_concentration_sensor = 190 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "zone_air_no_concentration_sensor"
      subfields: "zone"
      subfields: "air"
      subfields: "no"
      subfields: "concentration"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "concentration"
      standard_unit: "parts_per_unit"
      expected_min: 0.0
      expected_max: 0.0001
    }
  ];

  // The zone air o2 percentage sensor.
  //
  // Composed of:
  //   zone (descriptor): Region of building which is conditioned.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   o2 (descriptor): Chemical formula and abbreviation for oxygen.
  //   percentage (measurement): Measurement per hundred.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `zone_air_o2_percentage_sensor`, point type `sensor`.
  // Measured as percentage; values are in percent, the ontology's standard
  // unit for that quantity.
  //
  // Valid range 0 to 100, enforced by protovalidate.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  repeated double zone_air_o2_percentage_sensor = 226 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "zone_air_o2_percentage_sensor"
      subfields: "zone"
      subfields: "air"
      subfields: "o2"
      subfields: "percentage"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "percentage"
      standard_unit: "percent"
      enumerated: true
    }
  ];

  // The zone air occupied cooling temperature setpoint.
  //
  // Composed of:
  //   zone (descriptor): Region of building which is conditioned.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   occupied (descriptor): State of being within scheduled (time of day
  //     or week) run-time parameters.
  //   cooling (descriptor): Process, measured effect or requirement for
  //     cooling.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   setpoint (point_type): Control target of process or system.
  //
  // Ontology field: `zone_air_occupied_cooling_temperature_setpoint`, point
  // type `setpoint`. Measured as temperature; values are in kelvin, the
  // ontology's standard unit for that quantity.
  //
  // Expected range 290 to 305. This is a data-quality expectation, not a
  // constraint: a reading outside it is a fault to report, not a message to
  // reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double zone_air_occupied_cooling_temperature_setpoint = 192 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "zone_air_occupied_cooling_temperature_setpoint"
      subfields: "zone"
      subfields: "air"
      subfields: "occupied"
      subfields: "cooling"
      subfields: "temperature"
      subfields: "setpoint"
      point_type: "setpoint"
      measurement: "temperature"
      standard_unit: "kelvin"
      expected_min: 290.0
      expected_max: 305.0
    }
  ];

  // The zone air occupied heating temperature setpoint.
  //
  // Composed of:
  //   zone (descriptor): Region of building which is conditioned.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   occupied (descriptor): State of being within scheduled (time of day
  //     or week) run-time parameters.
  //   heating (descriptor): Process, measured effect or requirement for
  //     heating.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   setpoint (point_type): Control target of process or system.
  //
  // Ontology field: `zone_air_occupied_heating_temperature_setpoint`, point
  // type `setpoint`. Measured as temperature; values are in kelvin, the
  // ontology's standard unit for that quantity.
  //
  // Expected range 255.9259259 to 308.1481481. This is a data-quality
  // expectation, not a constraint: a reading outside it is a fault to
  // report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double zone_air_occupied_heating_temperature_setpoint = 193 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "zone_air_occupied_heating_temperature_setpoint"
      subfields: "zone"
      subfields: "air"
      subfields: "occupied"
      subfields: "heating"
      subfields: "temperature"
      subfields: "setpoint"
      point_type: "setpoint"
      measurement: "temperature"
      standard_unit: "kelvin"
      expected_min: 255.925926
      expected_max: 308.148148
    }
  ];

  // The zone air refrigerant concentration sensor.
  //
  // Composed of:
  //   zone (descriptor): Region of building which is conditioned.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   refrigerant (descriptor): Substance used in the mechanical
  //     refrigeration process (e.g., R-134a)
  //   concentration (measurement): Concentration of something relative to
  //     something else (usually in parts per million or parts per billion).
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `zone_air_refrigerant_concentration_sensor`, point type
  // `sensor`. Measured as concentration; values are in parts_per_unit, the
  // ontology's standard unit for that quantity.
  //
  // Expected range 0.00035 to 0.0015. This is a data-quality expectation,
  // not a constraint: a reading outside it is a fault to report, not a
  // message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  repeated double zone_air_refrigerant_concentration_sensor = 194 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "zone_air_refrigerant_concentration_sensor"
      subfields: "zone"
      subfields: "air"
      subfields: "refrigerant"
      subfields: "concentration"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "concentration"
      standard_unit: "parts_per_unit"
      enumerated: true
      expected_min: 0.00035
      expected_max: 0.0015
    }
  ];

  // The zone air refrigerant concentration setpoint.
  //
  // Composed of:
  //   zone (descriptor): Region of building which is conditioned.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   refrigerant (descriptor): Substance used in the mechanical
  //     refrigeration process (e.g., R-134a)
  //   concentration (measurement): Concentration of something relative to
  //     something else (usually in parts per million or parts per billion).
  //   setpoint (point_type): Control target of process or system.
  //
  // Ontology field: `zone_air_refrigerant_concentration_setpoint`, point
  // type `setpoint`. Measured as concentration; values are in
  // parts_per_unit, the ontology's standard unit for that quantity.
  //
  // Expected range 0.00035 to 0.0015. This is a data-quality expectation,
  // not a constraint: a reading outside it is a fault to report, not a
  // message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double zone_air_refrigerant_concentration_setpoint = 195 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "zone_air_refrigerant_concentration_setpoint"
      subfields: "zone"
      subfields: "air"
      subfields: "refrigerant"
      subfields: "concentration"
      subfields: "setpoint"
      point_type: "setpoint"
      measurement: "concentration"
      standard_unit: "parts_per_unit"
      expected_min: 0.00035
      expected_max: 0.0015
    }
  ];

  // The zone air relative humidity sensor.
  //
  // Composed of:
  //   zone (descriptor): Region of building which is conditioned.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   relative (measurement_descriptor): Quality of media with respect to
  //     theoretical minimum or maximum value for a given condition (e.g.,
  //     relative humidity).
  //   humidity (measurement): Concentration of water vapor in air
  //     (conditioned or unconditioned).
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `zone_air_relative_humidity_sensor`, point type
  // `sensor`. Measured as humidity; values are in percent_relative_humidity,
  // the ontology's standard unit for that quantity.
  //
  // Valid range 0 to 100, enforced by protovalidate.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double zone_air_relative_humidity_sensor = 196 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "zone_air_relative_humidity_sensor"
      subfields: "zone"
      subfields: "air"
      subfields: "relative"
      subfields: "humidity"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "humidity"
      standard_unit: "percent_relative_humidity"
    },
    (buf.validate.field).double.gte = 0.0,
    (buf.validate.field).double.lte = 100.0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The zone air specificenthalpy sensor.
  //
  // Composed of:
  //   zone (descriptor): Region of building which is conditioned.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   specificenthalpy (measurement): Measure of energy of air per unit
  //     mass.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `zone_air_specificenthalpy_sensor`, point type `sensor`.
  // Measured as specificenthalpy; values are in joules_per_kilogram, the
  // ontology's standard unit for that quantity.
  //
  // Expected range 11630 to 93040. This is a data-quality expectation, not a
  // constraint: a reading outside it is a fault to report, not a message to
  // reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double zone_air_specificenthalpy_sensor = 197 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "zone_air_specificenthalpy_sensor"
      subfields: "zone"
      subfields: "air"
      subfields: "specificenthalpy"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "specificenthalpy"
      standard_unit: "joules_per_kilogram"
      expected_min: 11630.0
      expected_max: 93040.0
    }
  ];

  // The zone air static pressure sensor.
  //
  // Composed of:
  //   zone (descriptor): Region of building which is conditioned.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   static (measurement_descriptor): Describing a system in equilibrium
  //     or at rest (e.g., supply_air_static_pressure_sensor).
  //   pressure (measurement): Measured force per unit area within a system
  //     or process.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `zone_air_static_pressure_sensor`, point type `sensor`.
  // Measured as pressure; values are in pascals, the ontology's standard
  // unit for that quantity.
  //
  // Expected range -249.080024 to 249.080024. This is a data-quality
  // expectation, not a constraint: a reading outside it is a fault to
  // report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double zone_air_static_pressure_sensor = 198 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "zone_air_static_pressure_sensor"
      subfields: "zone"
      subfields: "air"
      subfields: "static"
      subfields: "pressure"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "pressure"
      standard_unit: "pascals"
      expected_min: -249.080024
      expected_max: 249.080024
    }
  ];

  // The zone air static pressure setpoint.
  //
  // Composed of:
  //   zone (descriptor): Region of building which is conditioned.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   static (measurement_descriptor): Describing a system in equilibrium
  //     or at rest (e.g., supply_air_static_pressure_sensor).
  //   pressure (measurement): Measured force per unit area within a system
  //     or process.
  //   setpoint (point_type): Control target of process or system.
  //
  // Ontology field: `zone_air_static_pressure_setpoint`, point type
  // `setpoint`. Measured as pressure; values are in pascals, the ontology's
  // standard unit for that quantity.
  //
  // Expected range -124.540012 to 124.540012. This is a data-quality
  // expectation, not a constraint: a reading outside it is a fault to
  // report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double zone_air_static_pressure_setpoint = 199 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "zone_air_static_pressure_setpoint"
      subfields: "zone"
      subfields: "air"
      subfields: "static"
      subfields: "pressure"
      subfields: "setpoint"
      point_type: "setpoint"
      measurement: "pressure"
      standard_unit: "pascals"
      expected_min: -124.540012
      expected_max: 124.540012
    }
  ];

  // The zone air temperature sensor.
  //
  // Composed of:
  //   zone (descriptor): Region of building which is conditioned.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `zone_air_temperature_sensor`, point type `sensor`.
  // Measured as temperature; values are in kelvin, the ontology's standard
  // unit for that quantity.
  //
  // Expected range 255.9259259 to 322.037037. This is a data-quality
  // expectation, not a constraint: a reading outside it is a fault to
  // report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double zone_air_temperature_sensor = 200 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "zone_air_temperature_sensor"
      subfields: "zone"
      subfields: "air"
      subfields: "temperature"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "temperature"
      standard_unit: "kelvin"
      expected_min: 255.925926
      expected_max: 322.037037
    }
  ];

  // The zone air temperature setpoint.
  //
  // Composed of:
  //   zone (descriptor): Region of building which is conditioned.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   setpoint (point_type): Control target of process or system.
  //
  // Ontology field: `zone_air_temperature_setpoint`, point type `setpoint`.
  // Measured as temperature; values are in kelvin, the ontology's standard
  // unit for that quantity.
  //
  // Expected range 255.9259259 to 308.1481481. This is a data-quality
  // expectation, not a constraint: a reading outside it is a fault to
  // report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double zone_air_temperature_setpoint = 201 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "zone_air_temperature_setpoint"
      subfields: "zone"
      subfields: "air"
      subfields: "temperature"
      subfields: "setpoint"
      point_type: "setpoint"
      measurement: "temperature"
      standard_unit: "kelvin"
      expected_min: 255.925926
      expected_max: 308.148148
    }
  ];

  // The zone air unoccupied cooling temperature setpoint.
  //
  // Composed of:
  //   zone (descriptor): Region of building which is conditioned.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   unoccupied (descriptor): State of being outside of scheduled (time
  //     of day or week) run-time parameters.
  //   cooling (descriptor): Process, measured effect or requirement for
  //     cooling.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   setpoint (point_type): Control target of process or system.
  //
  // Ontology field: `zone_air_unoccupied_cooling_temperature_setpoint`,
  // point type `setpoint`. Measured as temperature; values are in kelvin,
  // the ontology's standard unit for that quantity.
  //
  // Expected range 255.9259259 to 308.1481481. This is a data-quality
  // expectation, not a constraint: a reading outside it is a fault to
  // report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double zone_air_unoccupied_cooling_temperature_setpoint = 202 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "zone_air_unoccupied_cooling_temperature_setpoint"
      subfields: "zone"
      subfields: "air"
      subfields: "unoccupied"
      subfields: "cooling"
      subfields: "temperature"
      subfields: "setpoint"
      point_type: "setpoint"
      measurement: "temperature"
      standard_unit: "kelvin"
      expected_min: 255.925926
      expected_max: 308.148148
    }
  ];

  // The zone air unoccupied heating temperature setpoint.
  //
  // Composed of:
  //   zone (descriptor): Region of building which is conditioned.
  //   air (descriptor): Atmospheric air, either conditioned or
  //     unconditioned.
  //   unoccupied (descriptor): State of being outside of scheduled (time
  //     of day or week) run-time parameters.
  //   heating (descriptor): Process, measured effect or requirement for
  //     heating.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   setpoint (point_type): Control target of process or system.
  //
  // Ontology field: `zone_air_unoccupied_heating_temperature_setpoint`,
  // point type `setpoint`. Measured as temperature; values are in kelvin,
  // the ontology's standard unit for that quantity.
  //
  // Expected range 255.9259259 to 308.1481481. This is a data-quality
  // expectation, not a constraint: a reading outside it is a fault to
  // report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double zone_air_unoccupied_heating_temperature_setpoint = 203 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "zone_air_unoccupied_heating_temperature_setpoint"
      subfields: "zone"
      subfields: "air"
      subfields: "unoccupied"
      subfields: "heating"
      subfields: "temperature"
      subfields: "setpoint"
      point_type: "setpoint"
      measurement: "temperature"
      standard_unit: "kelvin"
      expected_min: 255.925926
      expected_max: 308.148148
    }
  ];

  // The zone occupancy status.
  //
  // Composed of:
  //   zone (descriptor): Region of building which is conditioned.
  //   occupancy (descriptor): State of being occupied.
  //   status (point_type): The multistate value indicating an observed
  //     state in a piece of equipment, often indicating if a command was
  //     effected. It is a neutral observation (i.e., no quality judgment of
  //     'good' or 'bad'). It also has no units of measurement; therefore, if
  //     combined with a measurement subfield, it will indicate that the
  //     field is the directional status based on some measurement of that
  //     type (e.g., power_status equates to an on/off value based on some
  //     inference of power).
  //
  // Ontology field: `zone_occupancy_status`, point type `status`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional InactiveOccupiedUnknownUnoccupiedValue zone_occupancy_status = 204 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "zone_occupancy_status"
      subfields: "zone"
      subfields: "occupancy"
      subfields: "status"
      point_type: "status"
    }
  ];

  // Numbers whose field the ontology no longer declares. Never reissued: a
  // consumer may still hold a message that used one.
  reserved 64, 87, 93, 116, 118, 123, 179, 180, 181, 189, 191, 205, 206, 207, 208, 209, 210, 211, 212, 213, 214, 215;
}
