// Copyright 2026 RegaliumOS™.
// SPDX-License-Identifier: Apache-2.0
//
// Generated by sync from Google's Digital Buildings ontology,
// revision 2026-09-09 (4a794ac).
// Portions Copyright 2020 Google LLC, licensed under Apache-2.0.
// DO NOT EDIT.
//
// Regenerate with: just sync
// Revision history: docs/spec.md

syntax = "proto3";

package protobuf.digitalbuildings.hvac.air_source_heat_pump.v1;

import "buf/validate/validate.proto";
import "google/api/field_behavior.proto";
import "google/api/field_info.proto";
import "google/api/resource.proto";
import "protobuf/digitalbuildings/annotations/v1/annotations.proto";
import "protobuf/digitalbuildings/hvac/air_source_heat_pump/v1/active_inactive_value.proto";
import "protobuf/digitalbuildings/hvac/air_source_heat_pump/v1/air_source_heat_pump_type.proto";
import "protobuf/digitalbuildings/hvac/air_source_heat_pump/v1/auto_closed_open_unknown_value.proto";
import "protobuf/digitalbuildings/hvac/air_source_heat_pump/v1/auto_commissioning_cooling_dehumidify_fan_only_flushing_heating_manual_neutral_off_unknown_value.proto";
import "protobuf/digitalbuildings/hvac/air_source_heat_pump/v1/auto_cooling_dehumidify_fan_only_heating_manual_off_value.proto";
import "protobuf/digitalbuildings/hvac/air_source_heat_pump/v1/closed_open_value.proto";
import "protobuf/digitalbuildings/hvac/air_source_heat_pump/v1/connection.proto";
import "protobuf/digitalbuildings/hvac/air_source_heat_pump/v1/disabled_enabled_occupied_unoccupied_value.proto";
import "protobuf/digitalbuildings/hvac/air_source_heat_pump/v1/field_translation.proto";
import "protobuf/digitalbuildings/hvac/air_source_heat_pump/v1/link.proto";
import "protobuf/digitalbuildings/hvac/air_source_heat_pump/v1/off_on_standby_unknown_value.proto";
import "protobuf/digitalbuildings/hvac/air_source_heat_pump/v1/off_on_unknown_value.proto";
import "protobuf/digitalbuildings/hvac/air_source_heat_pump/v1/off_on_value.proto";

option java_multiple_files = true;
option java_outer_classname = "AirSourceHeatPumpProto";
option java_package = "com.regalium.protobuf.digitalbuildings.hvac.air_source_heat_pump.v1";

// Tag for air-source heat pumps. An ASHP is a device that transfers heat
// from the surrounding air into a heating water system through the use of a
// reversed refrigeration cycle.
//
// Ontology entity type `HVAC/ASHP`, a general type carrying 20 canonical
// variant(s). Rule 7: the general type is the resource and each variant is a
// value of AirSourceHeatPumpType, so this message holds the union of every
// field any variant uses -- all of them optional.
//
// Reference: Digital Buildings abstract model.
// https://github.com/google/digitalbuildings/blob/master/ontology/docs/model.md
message AirSourceHeatPump {
  option (google.api.resource) = {
    type: "digitalbuildings.googleapis.com/AirSourceHeatPump"
    pattern: "buildings/{building}/airSourceHeatPumps/{air_source_heat_pump}"
    singular: "airSourceHeatPump"
    plural: "airSourceHeatPumps"
  };
  option (protobuf.digitalbuildings.annotations.v1.entity_type) = {
    guid: "548532cf-e175-47e5-b6a1-82ac92362d38"
    namespace: "HVAC"
    general_type: "ASHP"
  };

  // Resource name,
  // "buildings/{building}/airSourceHeatPumps/{air_source_heat_pump}".
  // Assigned by the server.
  string name = 1 [
    (google.api.field_behavior) = IDENTIFIER,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The ontology's GUID for this entity.
  //
  // Rule 10: this is the ontology's own identifier, not a second one minted
  // beside it. Digital Buildings assigns a stable UUID to every entity and
  // it survives renames, which is exactly what AIP-148 asks a `uid` to be.
  string uid = 2 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (google.api.field_info).format = UUID4,
    (buf.validate.field).string.uuid = true,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The building config's human-readable entity code, e.g. `FCU-123`.
  //
  // Locally unique rather than globally, chosen at design time, and it must
  // survive a round trip unchanged or every re-import duplicates the entity.
  // The old building-config format keys entities by this; the new one keys
  // them by `uid`. Both are representable because both exist.
  string code = 3 [
    (google.api.field_behavior) = OPTIONAL,
    (google.api.field_behavior) = IMMUTABLE,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The canonical type this entity conforms to.
  //
  // The variant's exact field sets ride on the enum value in
  // (annotations.canonical_type), so a validator can reject an entity
  // reporting a field its declared type does not have.
  AirSourceHeatPumpType entity_type = 4 [(google.api.field_behavior) = OPTIONAL];

  // Directed relationships to other entities.
  //
  // Rule 4: a connection is not a field. AIP-215 forbids referencing a
  // message in another proto package, and the other end of a connection may
  // be any resource in any namespace -- so it is carried as a resource name
  // with a wildcard reference.
  repeated Connection connections = 5 [(google.api.field_behavior) = OPTIONAL];

  // Standard fields mapped from another entity's telemetry.
  //
  // The building config's `links` block: a virtual entity reports data
  // linked from reporting devices rather than emitting it itself.
  repeated Link links = 6 [(google.api.field_behavior) = OPTIONAL];

  // How this entity's native payload maps onto the standard fields, when it
  // reports telemetry directly.
  repeated FieldTranslation translations = 7 [(google.api.field_behavior) = OPTIONAL];

  // Held for the fields on every read path (rule 9).
  reserved 8 to 15;

  // The chilled return water isolation valve command.
  //
  // Composed of:
  //   chilled (descriptor): Cold water (typically 40 - 50F).
  //   return (descriptor): Measurement or process of media as it is
  //     returned from the end-use equipment within the system.
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   isolation (descriptor): Process of isolating one component or
  //     process from another.
  //   valve (component): Component which meters the flow of water within a
  //     system or device.
  //   command (point_type): The signal given to make an action happen.
  //     Defaults to multistate unless used alongside a measurement_type.
  //
  // Ontology field: `chilled_return_water_isolation_valve_command`, point
  // type `command`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ClosedOpenValue chilled_return_water_isolation_valve_command = 16 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "chilled_return_water_isolation_valve_command"
      subfields: "chilled"
      subfields: "return"
      subfields: "water"
      subfields: "isolation"
      subfields: "valve"
      subfields: "command"
      point_type: "command"
    }
  ];

  // The chilled return water isolation valve percentage command.
  //
  // Composed of:
  //   chilled (descriptor): Cold water (typically 40 - 50F).
  //   return (descriptor): Measurement or process of media as it is
  //     returned from the end-use equipment within the system.
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   isolation (descriptor): Process of isolating one component or
  //     process from another.
  //   valve (component): Component which meters the flow of water within a
  //     system or device.
  //   percentage (measurement): Measurement per hundred.
  //   command (point_type): The signal given to make an action happen.
  //     Defaults to multistate unless used alongside a measurement_type.
  //
  // Ontology field:
  // `chilled_return_water_isolation_valve_percentage_command`, point type
  // `command`. Measured as percentage; values are in percent, the ontology's
  // standard unit for that quantity.
  //
  // Valid range 0 to 100, enforced by protovalidate.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double chilled_return_water_isolation_valve_percentage_command = 17 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "chilled_return_water_isolation_valve_percentage_command"
      subfields: "chilled"
      subfields: "return"
      subfields: "water"
      subfields: "isolation"
      subfields: "valve"
      subfields: "percentage"
      subfields: "command"
      point_type: "command"
      measurement: "percentage"
      standard_unit: "percent"
    },
    (buf.validate.field).double.gte = 0.0,
    (buf.validate.field).double.lte = 100.0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The chilled return water isolation valve percentage sensor.
  //
  // Composed of:
  //   chilled (descriptor): Cold water (typically 40 - 50F).
  //   return (descriptor): Measurement or process of media as it is
  //     returned from the end-use equipment within the system.
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   isolation (descriptor): Process of isolating one component or
  //     process from another.
  //   valve (component): Component which meters the flow of water within a
  //     system or device.
  //   percentage (measurement): Measurement per hundred.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field:
  // `chilled_return_water_isolation_valve_percentage_sensor`, point type
  // `sensor`. Measured as percentage; values are in percent, the ontology's
  // standard unit for that quantity.
  //
  // Valid range 0 to 100, enforced by protovalidate.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double chilled_return_water_isolation_valve_percentage_sensor = 18 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "chilled_return_water_isolation_valve_percentage_sensor"
      subfields: "chilled"
      subfields: "return"
      subfields: "water"
      subfields: "isolation"
      subfields: "valve"
      subfields: "percentage"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "percentage"
      standard_unit: "percent"
    },
    (buf.validate.field).double.gte = 0.0,
    (buf.validate.field).double.lte = 100.0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The chilled return water isolation valve status.
  //
  // Composed of:
  //   chilled (descriptor): Cold water (typically 40 - 50F).
  //   return (descriptor): Measurement or process of media as it is
  //     returned from the end-use equipment within the system.
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   isolation (descriptor): Process of isolating one component or
  //     process from another.
  //   valve (component): Component which meters the flow of water within a
  //     system or device.
  //   status (point_type): The multistate value indicating an observed
  //     state in a piece of equipment, often indicating if a command was
  //     effected. It is a neutral observation (i.e., no quality judgment of
  //     'good' or 'bad'). It also has no units of measurement; therefore, if
  //     combined with a measurement subfield, it will indicate that the
  //     field is the directional status based on some measurement of that
  //     type (e.g., power_status equates to an on/off value based on some
  //     inference of power).
  //
  // Ontology field: `chilled_return_water_isolation_valve_status`, point
  // type `status`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ClosedOpenValue chilled_return_water_isolation_valve_status = 19 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "chilled_return_water_isolation_valve_status"
      subfields: "chilled"
      subfields: "return"
      subfields: "water"
      subfields: "isolation"
      subfields: "valve"
      subfields: "status"
      point_type: "status"
    }
  ];

  // The chilled return water temperature sensor.
  //
  // Composed of:
  //   chilled (descriptor): Cold water (typically 40 - 50F).
  //   return (descriptor): Measurement or process of media as it is
  //     returned from the end-use equipment within the system.
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `chilled_return_water_temperature_sensor`, point type
  // `sensor`. Measured as temperature; values are in kelvin, the ontology's
  // standard unit for that quantity.
  //
  // Expected range 255.9259259 to 308.1481481. This is a data-quality
  // expectation, not a constraint: a reading outside it is a fault to
  // report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double chilled_return_water_temperature_sensor = 20 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "chilled_return_water_temperature_sensor"
      subfields: "chilled"
      subfields: "return"
      subfields: "water"
      subfields: "temperature"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "temperature"
      standard_unit: "kelvin"
      expected_min: 255.925926
      expected_max: 308.148148
    }
  ];

  // The chilled supply water isolation valve command.
  //
  // Composed of:
  //   chilled (descriptor): Cold water (typically 40 - 50F).
  //   supply (descriptor): Measurement or process of media as it is
  //     supplied to the end-use equipment within the system.
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   isolation (descriptor): Process of isolating one component or
  //     process from another.
  //   valve (component): Component which meters the flow of water within a
  //     system or device.
  //   command (point_type): The signal given to make an action happen.
  //     Defaults to multistate unless used alongside a measurement_type.
  //
  // Ontology field: `chilled_supply_water_isolation_valve_command`, point
  // type `command`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ClosedOpenValue chilled_supply_water_isolation_valve_command = 21 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "chilled_supply_water_isolation_valve_command"
      subfields: "chilled"
      subfields: "supply"
      subfields: "water"
      subfields: "isolation"
      subfields: "valve"
      subfields: "command"
      point_type: "command"
    }
  ];

  // The chilled supply water isolation valve percentage sensor.
  //
  // Composed of:
  //   chilled (descriptor): Cold water (typically 40 - 50F).
  //   supply (descriptor): Measurement or process of media as it is
  //     supplied to the end-use equipment within the system.
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   isolation (descriptor): Process of isolating one component or
  //     process from another.
  //   valve (component): Component which meters the flow of water within a
  //     system or device.
  //   percentage (measurement): Measurement per hundred.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field:
  // `chilled_supply_water_isolation_valve_percentage_sensor`, point type
  // `sensor`. Measured as percentage; values are in percent, the ontology's
  // standard unit for that quantity.
  //
  // Valid range 0 to 100, enforced by protovalidate.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double chilled_supply_water_isolation_valve_percentage_sensor = 22 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "chilled_supply_water_isolation_valve_percentage_sensor"
      subfields: "chilled"
      subfields: "supply"
      subfields: "water"
      subfields: "isolation"
      subfields: "valve"
      subfields: "percentage"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "percentage"
      standard_unit: "percent"
    },
    (buf.validate.field).double.gte = 0.0,
    (buf.validate.field).double.lte = 100.0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The chilled supply water isolation valve status.
  //
  // Composed of:
  //   chilled (descriptor): Cold water (typically 40 - 50F).
  //   supply (descriptor): Measurement or process of media as it is
  //     supplied to the end-use equipment within the system.
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   isolation (descriptor): Process of isolating one component or
  //     process from another.
  //   valve (component): Component which meters the flow of water within a
  //     system or device.
  //   status (point_type): The multistate value indicating an observed
  //     state in a piece of equipment, often indicating if a command was
  //     effected. It is a neutral observation (i.e., no quality judgment of
  //     'good' or 'bad'). It also has no units of measurement; therefore, if
  //     combined with a measurement subfield, it will indicate that the
  //     field is the directional status based on some measurement of that
  //     type (e.g., power_status equates to an on/off value based on some
  //     inference of power).
  //
  // Ontology field: `chilled_supply_water_isolation_valve_status`, point
  // type `status`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ClosedOpenValue chilled_supply_water_isolation_valve_status = 23 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "chilled_supply_water_isolation_valve_status"
      subfields: "chilled"
      subfields: "supply"
      subfields: "water"
      subfields: "isolation"
      subfields: "valve"
      subfields: "status"
      point_type: "status"
    }
  ];

  // The chilled supply water temperature sensor.
  //
  // Composed of:
  //   chilled (descriptor): Cold water (typically 40 - 50F).
  //   supply (descriptor): Measurement or process of media as it is
  //     supplied to the end-use equipment within the system.
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `chilled_supply_water_temperature_sensor`, point type
  // `sensor`. Measured as temperature; values are in kelvin, the ontology's
  // standard unit for that quantity.
  //
  // Expected range 255.9259259 to 308.1481481. This is a data-quality
  // expectation, not a constraint: a reading outside it is a fault to
  // report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double chilled_supply_water_temperature_sensor = 24 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "chilled_supply_water_temperature_sensor"
      subfields: "chilled"
      subfields: "supply"
      subfields: "water"
      subfields: "temperature"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "temperature"
      standard_unit: "kelvin"
      expected_min: 255.925926
      expected_max: 308.148148
    }
  ];

  // The chilled supply water temperature setpoint.
  //
  // Composed of:
  //   chilled (descriptor): Cold water (typically 40 - 50F).
  //   supply (descriptor): Measurement or process of media as it is
  //     supplied to the end-use equipment within the system.
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   setpoint (point_type): Control target of process or system.
  //
  // Ontology field: `chilled_supply_water_temperature_setpoint`, point type
  // `setpoint`. Measured as temperature; values are in kelvin, the
  // ontology's standard unit for that quantity.
  //
  // Expected range 255.9259259 to 308.1481481. This is a data-quality
  // expectation, not a constraint: a reading outside it is a fault to
  // report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double chilled_supply_water_temperature_setpoint = 25 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "chilled_supply_water_temperature_setpoint"
      subfields: "chilled"
      subfields: "supply"
      subfields: "water"
      subfields: "temperature"
      subfields: "setpoint"
      point_type: "setpoint"
      measurement: "temperature"
      standard_unit: "kelvin"
      expected_min: 255.925926
      expected_max: 308.148148
    }
  ];

  // The chilled water circulation pump run command.
  //
  // Composed of:
  //   chilled (descriptor): Cold water (typically 40 - 50F).
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   circulation (descriptor): Process or component used to circulate
  //     fluid through a device or system; for exmaple, a circulation pump
  //     onboard a boiler.
  //   pump (component): Component used for the distribution of liquid
  //     media (e.g., water, glycol).
  //   run (measurement_descriptor): State of being active or operating. By
  //     default applies to control program for system (e.g., VAV program
  //     run_command).
  //   command (point_type): The signal given to make an action happen.
  //     Defaults to multistate unless used alongside a measurement_type.
  //
  // Ontology field: `chilled_water_circulation_pump_run_command`, point type
  // `command`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  repeated OffOnValue chilled_water_circulation_pump_run_command = 26 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "chilled_water_circulation_pump_run_command"
      subfields: "chilled"
      subfields: "water"
      subfields: "circulation"
      subfields: "pump"
      subfields: "run"
      subfields: "command"
      point_type: "command"
      enumerated: true
    }
  ];

  // The chilled water circulation pump run status.
  //
  // Composed of:
  //   chilled (descriptor): Cold water (typically 40 - 50F).
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   circulation (descriptor): Process or component used to circulate
  //     fluid through a device or system; for exmaple, a circulation pump
  //     onboard a boiler.
  //   pump (component): Component used for the distribution of liquid
  //     media (e.g., water, glycol).
  //   run (measurement_descriptor): State of being active or operating. By
  //     default applies to control program for system (e.g., VAV program
  //     run_command).
  //   status (point_type): The multistate value indicating an observed
  //     state in a piece of equipment, often indicating if a command was
  //     effected. It is a neutral observation (i.e., no quality judgment of
  //     'good' or 'bad'). It also has no units of measurement; therefore, if
  //     combined with a measurement subfield, it will indicate that the
  //     field is the directional status based on some measurement of that
  //     type (e.g., power_status equates to an on/off value based on some
  //     inference of power).
  //
  // Ontology field: `chilled_water_circulation_pump_run_status`, point type
  // `status`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  repeated OffOnValue chilled_water_circulation_pump_run_status = 27 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "chilled_water_circulation_pump_run_status"
      subfields: "chilled"
      subfields: "water"
      subfields: "circulation"
      subfields: "pump"
      subfields: "run"
      subfields: "status"
      point_type: "status"
      enumerated: true
    }
  ];

  // The chilled water differential pressure sensor.
  //
  // Composed of:
  //   chilled (descriptor): Cold water (typically 40 - 50F).
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   differential (measurement_descriptor): Measured difference between
  //     two reference points (e.g. differential_water_pressure)
  //   pressure (measurement): Measured force per unit area within a system
  //     or process.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `chilled_water_differential_pressure_sensor`, point type
  // `sensor`. Measured as pressure; values are in pascals, the ontology's
  // standard unit for that quantity.
  //
  // Expected range -34473.78945 to 344737.8945. This is a data-quality
  // expectation, not a constraint: a reading outside it is a fault to
  // report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double chilled_water_differential_pressure_sensor = 28 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "chilled_water_differential_pressure_sensor"
      subfields: "chilled"
      subfields: "water"
      subfields: "differential"
      subfields: "pressure"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "pressure"
      standard_unit: "pascals"
      expected_min: -34473.78945
      expected_max: 344737.8945
    }
  ];

  // The chilled water differential pressure setpoint.
  //
  // Composed of:
  //   chilled (descriptor): Cold water (typically 40 - 50F).
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   differential (measurement_descriptor): Measured difference between
  //     two reference points (e.g. differential_water_pressure)
  //   pressure (measurement): Measured force per unit area within a system
  //     or process.
  //   setpoint (point_type): Control target of process or system.
  //
  // Ontology field: `chilled_water_differential_pressure_setpoint`, point
  // type `setpoint`. Measured as pressure; values are in pascals, the
  // ontology's standard unit for that quantity.
  //
  // Expected range -34473.78945 to 344737.8945. This is a data-quality
  // expectation, not a constraint: a reading outside it is a fault to
  // report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double chilled_water_differential_pressure_setpoint = 29 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "chilled_water_differential_pressure_setpoint"
      subfields: "chilled"
      subfields: "water"
      subfields: "differential"
      subfields: "pressure"
      subfields: "setpoint"
      point_type: "setpoint"
      measurement: "pressure"
      standard_unit: "pascals"
      expected_min: -34473.78945
      expected_max: 344737.8945
    }
  ];

  // The circulation pump current sensor.
  //
  // Composed of:
  //   circulation (descriptor): Process or component used to circulate
  //     fluid through a device or system; for exmaple, a circulation pump
  //     onboard a boiler.
  //   pump (component): Component used for the distribution of liquid
  //     media (e.g., water, glycol).
  //   current (measurement): Flow of electric charge.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `circulation_pump_current_sensor`, point type `sensor`.
  // Measured as current; values are in amperes, the ontology's standard unit
  // for that quantity.
  //
  // Valid range from 0, enforced by protovalidate. Expected range up to 200.
  // This is a data-quality expectation, not a constraint: a reading outside
  // it is a fault to report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  repeated double circulation_pump_current_sensor = 30 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "circulation_pump_current_sensor"
      subfields: "circulation"
      subfields: "pump"
      subfields: "current"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "current"
      standard_unit: "amperes"
      enumerated: true
      expected_max: 200.0
    }
  ];

  // The circulation pump energy accumulator.
  //
  // Composed of:
  //   circulation (descriptor): Process or component used to circulate
  //     fluid through a device or system; for exmaple, a circulation pump
  //     onboard a boiler.
  //   pump (component): Component used for the distribution of liquid
  //     media (e.g., water, glycol).
  //   energy (measurement): The quantitative property that must be
  //     transferred to an object to perform work (default electrical unless
  //     otherwise modified).
  //   accumulator (point_type): The total accumulated quantity (e.g.,
  //     total energy accumulated).
  //
  // Ontology field: `circulation_pump_energy_accumulator`, point type
  // `accumulator`. Measured as energy; values are in joules, the ontology's
  // standard unit for that quantity.
  //
  // Valid range from 0, enforced by protovalidate. Expected range up to
  // 3.6e+13. This is a data-quality expectation, not a constraint: a reading
  // outside it is a fault to report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  repeated double circulation_pump_energy_accumulator = 31 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "circulation_pump_energy_accumulator"
      subfields: "circulation"
      subfields: "pump"
      subfields: "energy"
      subfields: "accumulator"
      point_type: "accumulator"
      measurement: "energy"
      standard_unit: "joules"
      enumerated: true
      expected_max: 36000000000000.0
    }
  ];

  // The circulation pump power sensor.
  //
  // Composed of:
  //   circulation (descriptor): Process or component used to circulate
  //     fluid through a device or system; for exmaple, a circulation pump
  //     onboard a boiler.
  //   pump (component): Component used for the distribution of liquid
  //     media (e.g., water, glycol).
  //   power (measurement): Rate of energy consumption (assumed context is
  //     electrical and real, unless otherwise modified).
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `circulation_pump_power_sensor`, point type `sensor`.
  // Measured as power; values are in watts, the ontology's standard unit for
  // that quantity.
  //
  // Expected range 0 to 1e+07. This is a data-quality expectation, not a
  // constraint: a reading outside it is a fault to report, not a message to
  // reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  repeated double circulation_pump_power_sensor = 32 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "circulation_pump_power_sensor"
      subfields: "circulation"
      subfields: "pump"
      subfields: "power"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "power"
      standard_unit: "watts"
      enumerated: true
      expected_min: 0.0
      expected_max: 10000000.0
    }
  ];

  // The circulation pump run command.
  //
  // Composed of:
  //   circulation (descriptor): Process or component used to circulate
  //     fluid through a device or system; for exmaple, a circulation pump
  //     onboard a boiler.
  //   pump (component): Component used for the distribution of liquid
  //     media (e.g., water, glycol).
  //   run (measurement_descriptor): State of being active or operating. By
  //     default applies to control program for system (e.g., VAV program
  //     run_command).
  //   command (point_type): The signal given to make an action happen.
  //     Defaults to multistate unless used alongside a measurement_type.
  //
  // Ontology field: `circulation_pump_run_command`, point type `command`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  repeated OffOnValue circulation_pump_run_command = 33 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "circulation_pump_run_command"
      subfields: "circulation"
      subfields: "pump"
      subfields: "run"
      subfields: "command"
      point_type: "command"
      enumerated: true
    }
  ];

  // The circulation pump run status.
  //
  // Composed of:
  //   circulation (descriptor): Process or component used to circulate
  //     fluid through a device or system; for exmaple, a circulation pump
  //     onboard a boiler.
  //   pump (component): Component used for the distribution of liquid
  //     media (e.g., water, glycol).
  //   run (measurement_descriptor): State of being active or operating. By
  //     default applies to control program for system (e.g., VAV program
  //     run_command).
  //   status (point_type): The multistate value indicating an observed
  //     state in a piece of equipment, often indicating if a command was
  //     effected. It is a neutral observation (i.e., no quality judgment of
  //     'good' or 'bad'). It also has no units of measurement; therefore, if
  //     combined with a measurement subfield, it will indicate that the
  //     field is the directional status based on some measurement of that
  //     type (e.g., power_status equates to an on/off value based on some
  //     inference of power).
  //
  // Ontology field: `circulation_pump_run_status`, point type `status`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  repeated OffOnValue circulation_pump_run_status = 34 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "circulation_pump_run_status"
      subfields: "circulation"
      subfields: "pump"
      subfields: "run"
      subfields: "status"
      point_type: "status"
      enumerated: true
    }
  ];

  // The circulation pump speed frequency sensor.
  //
  // Composed of:
  //   circulation (descriptor): Process or component used to circulate
  //     fluid through a device or system; for exmaple, a circulation pump
  //     onboard a boiler.
  //   pump (component): Component used for the distribution of liquid
  //     media (e.g., water, glycol).
  //   speed (measurement_descriptor): Numeric setting of how fast to run a
  //     device, in the specified units. Typically used to describe
  //     revolutions of a motor as a fraction of nominal or maximum.
  //   frequency (measurement): The rate of cycling within a process.
  //     Typically used to describe voltage (and amperage) oscillation within
  //     AC power distribution components.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `circulation_pump_speed_frequency_sensor`, point type
  // `sensor`. Measured as frequency; values are in hertz, the ontology's
  // standard unit for that quantity.
  //
  // Expected range -0.5 to 65. This is a data-quality expectation, not a
  // constraint: a reading outside it is a fault to report, not a message to
  // reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  repeated double circulation_pump_speed_frequency_sensor = 35 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "circulation_pump_speed_frequency_sensor"
      subfields: "circulation"
      subfields: "pump"
      subfields: "speed"
      subfields: "frequency"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "frequency"
      standard_unit: "hertz"
      enumerated: true
      expected_min: -0.5
      expected_max: 65.0
    }
  ];

  // The circulation pump speed percentage command.
  //
  // Composed of:
  //   circulation (descriptor): Process or component used to circulate
  //     fluid through a device or system; for exmaple, a circulation pump
  //     onboard a boiler.
  //   pump (component): Component used for the distribution of liquid
  //     media (e.g., water, glycol).
  //   speed (measurement_descriptor): Numeric setting of how fast to run a
  //     device, in the specified units. Typically used to describe
  //     revolutions of a motor as a fraction of nominal or maximum.
  //   percentage (measurement): Measurement per hundred.
  //   command (point_type): The signal given to make an action happen.
  //     Defaults to multistate unless used alongside a measurement_type.
  //
  // Ontology field: `circulation_pump_speed_percentage_command`, point type
  // `command`. Measured as percentage; values are in percent, the ontology's
  // standard unit for that quantity.
  //
  // Valid range 0 to 100, enforced by protovalidate.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  repeated double circulation_pump_speed_percentage_command = 36 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "circulation_pump_speed_percentage_command"
      subfields: "circulation"
      subfields: "pump"
      subfields: "speed"
      subfields: "percentage"
      subfields: "command"
      point_type: "command"
      measurement: "percentage"
      standard_unit: "percent"
      enumerated: true
    }
  ];

  // The communication status.
  //
  // Composed of:
  //   communication (descriptor): Actual transmit and recieve of data or
  //     connectivity between devices.
  //   status (point_type): The multistate value indicating an observed
  //     state in a piece of equipment, often indicating if a command was
  //     effected. It is a neutral observation (i.e., no quality judgment of
  //     'good' or 'bad'). It also has no units of measurement; therefore, if
  //     combined with a measurement subfield, it will indicate that the
  //     field is the directional status based on some measurement of that
  //     type (e.g., power_status equates to an on/off value based on some
  //     inference of power).
  //
  // Ontology field: `communication_status`, point type `status`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional OffOnValue communication_status = 37 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "communication_status"
      subfields: "communication"
      subfields: "status"
      point_type: "status"
    }
  ];

  // The compressor current sensor.
  //
  // Composed of:
  //   compressor (component): Component which drives refrigerant
  //     compression (and thus cooling processes) within a device or system.
  //   current (measurement): Flow of electric charge.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `compressor_current_sensor`, point type `sensor`.
  // Measured as current; values are in amperes, the ontology's standard unit
  // for that quantity.
  //
  // Valid range from 0, enforced by protovalidate. Expected range up to 100.
  // This is a data-quality expectation, not a constraint: a reading outside
  // it is a fault to report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  repeated double compressor_current_sensor = 38 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "compressor_current_sensor"
      subfields: "compressor"
      subfields: "current"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "current"
      standard_unit: "amperes"
      enumerated: true
      expected_max: 100.0
    }
  ];

  // The compressor rotationalvelocity sensor.
  //
  // Composed of:
  //   compressor (component): Component which drives refrigerant
  //     compression (and thus cooling processes) within a device or system.
  //   rotationalvelocity (measurement): Vector measure of rotation rate,
  //     that refers to how fast an object rotates or revolves relative to
  //     another point.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `compressor_rotationalvelocity_sensor`, point type
  // `sensor`. Measured as rotationalvelocity; values are in
  // revolutions_per_minute, the ontology's standard unit for that quantity.
  //
  // Valid range 0 to 4000, enforced by protovalidate.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  repeated double compressor_rotationalvelocity_sensor = 39 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "compressor_rotationalvelocity_sensor"
      subfields: "compressor"
      subfields: "rotationalvelocity"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "rotationalvelocity"
      standard_unit: "revolutions_per_minute"
      enumerated: true
    }
  ];

  // The compressor run command.
  //
  // Composed of:
  //   compressor (component): Component which drives refrigerant
  //     compression (and thus cooling processes) within a device or system.
  //   run (measurement_descriptor): State of being active or operating. By
  //     default applies to control program for system (e.g., VAV program
  //     run_command).
  //   command (point_type): The signal given to make an action happen.
  //     Defaults to multistate unless used alongside a measurement_type.
  //
  // Ontology field: `compressor_run_command`, point type `command`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  repeated OffOnUnknownValue compressor_run_command = 40 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "compressor_run_command"
      subfields: "compressor"
      subfields: "run"
      subfields: "command"
      point_type: "command"
      enumerated: true
    }
  ];

  // The compressor run status.
  //
  // Composed of:
  //   compressor (component): Component which drives refrigerant
  //     compression (and thus cooling processes) within a device or system.
  //   run (measurement_descriptor): State of being active or operating. By
  //     default applies to control program for system (e.g., VAV program
  //     run_command).
  //   status (point_type): The multistate value indicating an observed
  //     state in a piece of equipment, often indicating if a command was
  //     effected. It is a neutral observation (i.e., no quality judgment of
  //     'good' or 'bad'). It also has no units of measurement; therefore, if
  //     combined with a measurement subfield, it will indicate that the
  //     field is the directional status based on some measurement of that
  //     type (e.g., power_status equates to an on/off value based on some
  //     inference of power).
  //
  // Ontology field: `compressor_run_status`, point type `status`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  repeated OffOnUnknownValue compressor_run_status = 41 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "compressor_run_status"
      subfields: "compressor"
      subfields: "run"
      subfields: "status"
      point_type: "status"
      enumerated: true
    }
  ];

  // The compressor speed frequency sensor.
  //
  // Composed of:
  //   compressor (component): Component which drives refrigerant
  //     compression (and thus cooling processes) within a device or system.
  //   speed (measurement_descriptor): Numeric setting of how fast to run a
  //     device, in the specified units. Typically used to describe
  //     revolutions of a motor as a fraction of nominal or maximum.
  //   frequency (measurement): The rate of cycling within a process.
  //     Typically used to describe voltage (and amperage) oscillation within
  //     AC power distribution components.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `compressor_speed_frequency_sensor`, point type
  // `sensor`. Measured as frequency; values are in hertz, the ontology's
  // standard unit for that quantity.
  //
  // Expected range -0.5 to 65. This is a data-quality expectation, not a
  // constraint: a reading outside it is a fault to report, not a message to
  // reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  repeated double compressor_speed_frequency_sensor = 42 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "compressor_speed_frequency_sensor"
      subfields: "compressor"
      subfields: "speed"
      subfields: "frequency"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "frequency"
      standard_unit: "hertz"
      enumerated: true
      expected_min: -0.5
      expected_max: 65.0
    }
  ];

  // The compressor speed percentage command.
  //
  // Composed of:
  //   compressor (component): Component which drives refrigerant
  //     compression (and thus cooling processes) within a device or system.
  //   speed (measurement_descriptor): Numeric setting of how fast to run a
  //     device, in the specified units. Typically used to describe
  //     revolutions of a motor as a fraction of nominal or maximum.
  //   percentage (measurement): Measurement per hundred.
  //   command (point_type): The signal given to make an action happen.
  //     Defaults to multistate unless used alongside a measurement_type.
  //
  // Ontology field: `compressor_speed_percentage_command`, point type
  // `command`. Measured as percentage; values are in percent, the ontology's
  // standard unit for that quantity.
  //
  // Valid range 0 to 100, enforced by protovalidate.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double compressor_speed_percentage_command = 43 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "compressor_speed_percentage_command"
      subfields: "compressor"
      subfields: "speed"
      subfields: "percentage"
      subfields: "command"
      point_type: "command"
      measurement: "percentage"
      standard_unit: "percent"
    },
    (buf.validate.field).double.gte = 0.0,
    (buf.validate.field).double.lte = 100.0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The compressor speed percentage sensor.
  //
  // Composed of:
  //   compressor (component): Component which drives refrigerant
  //     compression (and thus cooling processes) within a device or system.
  //   speed (measurement_descriptor): Numeric setting of how fast to run a
  //     device, in the specified units. Typically used to describe
  //     revolutions of a motor as a fraction of nominal or maximum.
  //   percentage (measurement): Measurement per hundred.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `compressor_speed_percentage_sensor`, point type
  // `sensor`. Measured as percentage; values are in percent, the ontology's
  // standard unit for that quantity.
  //
  // Valid range 0 to 100, enforced by protovalidate.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  repeated double compressor_speed_percentage_sensor = 44 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "compressor_speed_percentage_sensor"
      subfields: "compressor"
      subfields: "speed"
      subfields: "percentage"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "percentage"
      standard_unit: "percent"
      enumerated: true
    }
  ];

  // The condensing fan run command.
  //
  // Composed of:
  //   condensing (descriptor): The process of a gas (or vapor) changing to
  //     liquid.
  //   fan (component): Component used for the distribution of air.
  //   run (measurement_descriptor): State of being active or operating. By
  //     default applies to control program for system (e.g., VAV program
  //     run_command).
  //   command (point_type): The signal given to make an action happen.
  //     Defaults to multistate unless used alongside a measurement_type.
  //
  // Ontology field: `condensing_fan_run_command`, point type `command`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  repeated OffOnValue condensing_fan_run_command = 45 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "condensing_fan_run_command"
      subfields: "condensing"
      subfields: "fan"
      subfields: "run"
      subfields: "command"
      point_type: "command"
      enumerated: true
    }
  ];

  // The condensing fan run status.
  //
  // Composed of:
  //   condensing (descriptor): The process of a gas (or vapor) changing to
  //     liquid.
  //   fan (component): Component used for the distribution of air.
  //   run (measurement_descriptor): State of being active or operating. By
  //     default applies to control program for system (e.g., VAV program
  //     run_command).
  //   status (point_type): The multistate value indicating an observed
  //     state in a piece of equipment, often indicating if a command was
  //     effected. It is a neutral observation (i.e., no quality judgment of
  //     'good' or 'bad'). It also has no units of measurement; therefore, if
  //     combined with a measurement subfield, it will indicate that the
  //     field is the directional status based on some measurement of that
  //     type (e.g., power_status equates to an on/off value based on some
  //     inference of power).
  //
  // Ontology field: `condensing_fan_run_status`, point type `status`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  repeated OffOnValue condensing_fan_run_status = 46 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "condensing_fan_run_status"
      subfields: "condensing"
      subfields: "fan"
      subfields: "run"
      subfields: "status"
      point_type: "status"
      enumerated: true
    }
  ];

  // The condensing fan speed percentage command.
  //
  // Composed of:
  //   condensing (descriptor): The process of a gas (or vapor) changing to
  //     liquid.
  //   fan (component): Component used for the distribution of air.
  //   speed (measurement_descriptor): Numeric setting of how fast to run a
  //     device, in the specified units. Typically used to describe
  //     revolutions of a motor as a fraction of nominal or maximum.
  //   percentage (measurement): Measurement per hundred.
  //   command (point_type): The signal given to make an action happen.
  //     Defaults to multistate unless used alongside a measurement_type.
  //
  // Ontology field: `condensing_fan_speed_percentage_command`, point type
  // `command`. Measured as percentage; values are in percent, the ontology's
  // standard unit for that quantity.
  //
  // Valid range 0 to 100, enforced by protovalidate.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  repeated double condensing_fan_speed_percentage_command = 47 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "condensing_fan_speed_percentage_command"
      subfields: "condensing"
      subfields: "fan"
      subfields: "speed"
      subfields: "percentage"
      subfields: "command"
      point_type: "command"
      measurement: "percentage"
      standard_unit: "percent"
      enumerated: true
    }
  ];

  // The control mode.
  //
  // Composed of:
  //   control (descriptor): The process of governing the actions of a
  //     device.
  //   mode (point_type): Distinct mode of operation within system. For
  //     example economizer_mode has modes of 'enabled' or 'disabled.'
  //
  // Ontology field: `control_mode`, point type `mode`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional AutoCoolingDehumidifyFanOnlyHeatingManualOffValue control_mode = 48 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "control_mode"
      subfields: "control"
      subfields: "mode"
      point_type: "mode"
    }
  ];

  // The cooling percentage command.
  //
  // Composed of:
  //   cooling (descriptor): Process, measured effect or requirement for
  //     cooling.
  //   percentage (measurement): Measurement per hundred.
  //   command (point_type): The signal given to make an action happen.
  //     Defaults to multistate unless used alongside a measurement_type.
  //
  // Ontology field: `cooling_percentage_command`, point type `command`.
  // Measured as percentage; values are in percent, the ontology's standard
  // unit for that quantity.
  //
  // Valid range 0 to 100, enforced by protovalidate.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double cooling_percentage_command = 49 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "cooling_percentage_command"
      subfields: "cooling"
      subfields: "percentage"
      subfields: "command"
      point_type: "command"
      measurement: "percentage"
      standard_unit: "percent"
    },
    (buf.validate.field).double.gte = 0.0,
    (buf.validate.field).double.lte = 100.0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The cooling request count.
  //
  // Composed of:
  //   cooling (descriptor): Process, measured effect or requirement for
  //     cooling.
  //   request (descriptor): A signal from a system device that calls for
  //     more of a particular conditioning process (e.g., pressure requests
  //     from terminal units).
  //   count (point_type): Total count of actions or requests.
  //
  // Ontology field: `cooling_request_count`, point type `count`.
  // Dimensionless: the ontology defines this point type as an integer count
  // rather than a measured quantity.
  //
  // Valid range from 0, enforced by protovalidate. Expected range up to 500.
  // This is a data-quality expectation, not a constraint: a reading outside
  // it is a fault to report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional int64 cooling_request_count = 50 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "cooling_request_count"
      subfields: "cooling"
      subfields: "request"
      subfields: "count"
      point_type: "count"
      expected_max: 500.0
    },
    (buf.validate.field).int64.gte = 0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The cooling run command.
  //
  // Composed of:
  //   cooling (descriptor): Process, measured effect or requirement for
  //     cooling.
  //   run (measurement_descriptor): State of being active or operating. By
  //     default applies to control program for system (e.g., VAV program
  //     run_command).
  //   command (point_type): The signal given to make an action happen.
  //     Defaults to multistate unless used alongside a measurement_type.
  //
  // Ontology field: `cooling_run_command`, point type `command`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional OffOnUnknownValue cooling_run_command = 51 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "cooling_run_command"
      subfields: "cooling"
      subfields: "run"
      subfields: "command"
      point_type: "command"
    }
  ];

  // The cooling thermal power capacity.
  //
  // Composed of:
  //   cooling (descriptor): Process, measured effect or requirement for
  //     cooling.
  //   thermal (measurement_descriptor): Relating to the transfer of heat.
  //   power (measurement): Rate of energy consumption (assumed context is
  //     electrical and real, unless otherwise modified).
  //   capacity (point_type): A design parameter quantity (e.g., design
  //     motor power capacity). Is always a maximum limit.
  //
  // Ontology field: `cooling_thermal_power_capacity`, point type `capacity`.
  // Measured as power; values are in watts, the ontology's standard unit for
  // that quantity.
  //
  // Valid range from 0, enforced by protovalidate. Expected range up to
  // 2.9307107e+06. This is a data-quality expectation, not a constraint: a
  // reading outside it is a fault to report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double cooling_thermal_power_capacity = 52 [
    (google.api.field_behavior) = OPTIONAL,
    (google.api.field_behavior) = IMMUTABLE,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "cooling_thermal_power_capacity"
      subfields: "cooling"
      subfields: "thermal"
      subfields: "power"
      subfields: "capacity"
      point_type: "capacity"
      measurement: "power"
      standard_unit: "watts"
      expected_max: 2930710.7
    },
    (buf.validate.field).double.gte = 0.0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The current sensor.
  //
  // Composed of:
  //   current (measurement): Flow of electric charge.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `current_sensor`, point type `sensor`. Measured as
  // current; values are in amperes, the ontology's standard unit for that
  // quantity.
  //
  // Valid range from 0, enforced by protovalidate. Expected range up to 200.
  // This is a data-quality expectation, not a constraint: a reading outside
  // it is a fault to report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double current_sensor = 53 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "current_sensor"
      subfields: "current"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "current"
      standard_unit: "amperes"
      expected_max: 200.0
    },
    (buf.validate.field).double.gte = 0.0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The differential pressure sensor.
  //
  // Composed of:
  //   differential (measurement_descriptor): Measured difference between
  //     two reference points (e.g. differential_water_pressure)
  //   pressure (measurement): Measured force per unit area within a system
  //     or process.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `differential_pressure_sensor`, point type `sensor`.
  // Measured as pressure; values are in pascals, the ontology's standard
  // unit for that quantity.
  //
  // Expected range -34473.78945 to 344737.8945. This is a data-quality
  // expectation, not a constraint: a reading outside it is a fault to
  // report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double differential_pressure_sensor = 54 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "differential_pressure_sensor"
      subfields: "differential"
      subfields: "pressure"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "pressure"
      standard_unit: "pascals"
      expected_min: -34473.78945
      expected_max: 344737.8945
    }
  ];

  // The differential pressure setpoint.
  //
  // Composed of:
  //   differential (measurement_descriptor): Measured difference between
  //     two reference points (e.g. differential_water_pressure)
  //   pressure (measurement): Measured force per unit area within a system
  //     or process.
  //   setpoint (point_type): Control target of process or system.
  //
  // Ontology field: `differential_pressure_setpoint`, point type `setpoint`.
  // Measured as pressure; values are in pascals, the ontology's standard
  // unit for that quantity.
  //
  // Expected range -34473.78945 to 344737.8945. This is a data-quality
  // expectation, not a constraint: a reading outside it is a fault to
  // report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double differential_pressure_setpoint = 55 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "differential_pressure_setpoint"
      subfields: "differential"
      subfields: "pressure"
      subfields: "setpoint"
      point_type: "setpoint"
      measurement: "pressure"
      standard_unit: "pascals"
      expected_min: -34473.78945
      expected_max: 344737.8945
    }
  ];

  // The efficiency percentage sensor.
  //
  // Composed of:
  //   efficiency (measurement_descriptor): The ratio of required input to
  //     actual output.
  //   percentage (measurement): Measurement per hundred.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `efficiency_percentage_sensor`, point type `sensor`.
  // Measured as percentage; values are in percent, the ontology's standard
  // unit for that quantity.
  //
  // Valid range 0 to 100, enforced by protovalidate.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double efficiency_percentage_sensor = 56 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "efficiency_percentage_sensor"
      subfields: "efficiency"
      subfields: "percentage"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "percentage"
      standard_unit: "percent"
    },
    (buf.validate.field).double.gte = 0.0,
    (buf.validate.field).double.lte = 100.0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The energy accumulator.
  //
  // Composed of:
  //   energy (measurement): The quantitative property that must be
  //     transferred to an object to perform work (default electrical unless
  //     otherwise modified).
  //   accumulator (point_type): The total accumulated quantity (e.g.,
  //     total energy accumulated).
  //
  // Ontology field: `energy_accumulator`, point type `accumulator`. Measured
  // as energy; values are in joules, the ontology's standard unit for that
  // quantity.
  //
  // Valid range from 0, enforced by protovalidate. Expected range up to
  // 3.6e+19. This is a data-quality expectation, not a constraint: a reading
  // outside it is a fault to report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double energy_accumulator = 57 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "energy_accumulator"
      subfields: "energy"
      subfields: "accumulator"
      point_type: "accumulator"
      measurement: "energy"
      standard_unit: "joules"
      expected_max: 36000000000000000000.0
    },
    (buf.validate.field).double.gte = 0.0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The failed alarm.
  //
  // Composed of:
  //   failed (descriptor): Indicates that a point is not operating as
  //     intended or is incapable of operating.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `failed_alarm`, point type `alarm`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue failed_alarm = 58 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "failed_alarm"
      subfields: "failed"
      subfields: "alarm"
      point_type: "alarm"
    }
  ];

  // The failed chilled return water isolation valve alarm.
  //
  // Composed of:
  //   failed (descriptor): Indicates that a point is not operating as
  //     intended or is incapable of operating.
  //   chilled (descriptor): Cold water (typically 40 - 50F).
  //   return (descriptor): Measurement or process of media as it is
  //     returned from the end-use equipment within the system.
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   isolation (descriptor): Process of isolating one component or
  //     process from another.
  //   valve (component): Component which meters the flow of water within a
  //     system or device.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `failed_chilled_return_water_isolation_valve_alarm`,
  // point type `alarm`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue failed_chilled_return_water_isolation_valve_alarm = 59 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "failed_chilled_return_water_isolation_valve_alarm"
      subfields: "failed"
      subfields: "chilled"
      subfields: "return"
      subfields: "water"
      subfields: "isolation"
      subfields: "valve"
      subfields: "alarm"
      point_type: "alarm"
    }
  ];

  // The failed chilled return water temperature alarm.
  //
  // Composed of:
  //   failed (descriptor): Indicates that a point is not operating as
  //     intended or is incapable of operating.
  //   chilled (descriptor): Cold water (typically 40 - 50F).
  //   return (descriptor): Measurement or process of media as it is
  //     returned from the end-use equipment within the system.
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `failed_chilled_return_water_temperature_alarm`, point
  // type `alarm`. Measured as temperature; values are in kelvin, the
  // ontology's standard unit for that quantity.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue failed_chilled_return_water_temperature_alarm = 60 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "failed_chilled_return_water_temperature_alarm"
      subfields: "failed"
      subfields: "chilled"
      subfields: "return"
      subfields: "water"
      subfields: "temperature"
      subfields: "alarm"
      point_type: "alarm"
      measurement: "temperature"
      standard_unit: "kelvin"
    }
  ];

  // The failed chilled supply water isolation valve alarm.
  //
  // Composed of:
  //   failed (descriptor): Indicates that a point is not operating as
  //     intended or is incapable of operating.
  //   chilled (descriptor): Cold water (typically 40 - 50F).
  //   supply (descriptor): Measurement or process of media as it is
  //     supplied to the end-use equipment within the system.
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   isolation (descriptor): Process of isolating one component or
  //     process from another.
  //   valve (component): Component which meters the flow of water within a
  //     system or device.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `failed_chilled_supply_water_isolation_valve_alarm`,
  // point type `alarm`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue failed_chilled_supply_water_isolation_valve_alarm = 61 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "failed_chilled_supply_water_isolation_valve_alarm"
      subfields: "failed"
      subfields: "chilled"
      subfields: "supply"
      subfields: "water"
      subfields: "isolation"
      subfields: "valve"
      subfields: "alarm"
      point_type: "alarm"
    }
  ];

  // The failed chilled supply water temperature alarm.
  //
  // Composed of:
  //   failed (descriptor): Indicates that a point is not operating as
  //     intended or is incapable of operating.
  //   chilled (descriptor): Cold water (typically 40 - 50F).
  //   supply (descriptor): Measurement or process of media as it is
  //     supplied to the end-use equipment within the system.
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `failed_chilled_supply_water_temperature_alarm`, point
  // type `alarm`. Measured as temperature; values are in kelvin, the
  // ontology's standard unit for that quantity.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue failed_chilled_supply_water_temperature_alarm = 62 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "failed_chilled_supply_water_temperature_alarm"
      subfields: "failed"
      subfields: "chilled"
      subfields: "supply"
      subfields: "water"
      subfields: "temperature"
      subfields: "alarm"
      point_type: "alarm"
      measurement: "temperature"
      standard_unit: "kelvin"
    }
  ];

  // The failed chilled water differential pressure alarm.
  //
  // Composed of:
  //   failed (descriptor): Indicates that a point is not operating as
  //     intended or is incapable of operating.
  //   chilled (descriptor): Cold water (typically 40 - 50F).
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   differential (measurement_descriptor): Measured difference between
  //     two reference points (e.g. differential_water_pressure)
  //   pressure (measurement): Measured force per unit area within a system
  //     or process.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `failed_chilled_water_differential_pressure_alarm`,
  // point type `alarm`. Measured as pressure; values are in pascals, the
  // ontology's standard unit for that quantity.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue failed_chilled_water_differential_pressure_alarm = 63 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "failed_chilled_water_differential_pressure_alarm"
      subfields: "failed"
      subfields: "chilled"
      subfields: "water"
      subfields: "differential"
      subfields: "pressure"
      subfields: "alarm"
      point_type: "alarm"
      measurement: "pressure"
      standard_unit: "pascals"
    }
  ];

  // The failed circulation pump alarm.
  //
  // Composed of:
  //   failed (descriptor): Indicates that a point is not operating as
  //     intended or is incapable of operating.
  //   circulation (descriptor): Process or component used to circulate
  //     fluid through a device or system; for exmaple, a circulation pump
  //     onboard a boiler.
  //   pump (component): Component used for the distribution of liquid
  //     media (e.g., water, glycol).
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `failed_circulation_pump_alarm`, point type `alarm`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  repeated ActiveInactiveValue failed_circulation_pump_alarm = 64 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "failed_circulation_pump_alarm"
      subfields: "failed"
      subfields: "circulation"
      subfields: "pump"
      subfields: "alarm"
      point_type: "alarm"
      enumerated: true
    }
  ];

  // The failed communication alarm.
  //
  // Composed of:
  //   failed (descriptor): Indicates that a point is not operating as
  //     intended or is incapable of operating.
  //   communication (descriptor): Actual transmit and recieve of data or
  //     connectivity between devices.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `failed_communication_alarm`, point type `alarm`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue failed_communication_alarm = 65 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "failed_communication_alarm"
      subfields: "failed"
      subfields: "communication"
      subfields: "alarm"
      point_type: "alarm"
    }
  ];

  // The failed compressor alarm.
  //
  // Composed of:
  //   failed (descriptor): Indicates that a point is not operating as
  //     intended or is incapable of operating.
  //   compressor (component): Component which drives refrigerant
  //     compression (and thus cooling processes) within a device or system.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `failed_compressor_alarm`, point type `alarm`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  repeated ActiveInactiveValue failed_compressor_alarm = 66 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "failed_compressor_alarm"
      subfields: "failed"
      subfields: "compressor"
      subfields: "alarm"
      point_type: "alarm"
      enumerated: true
    }
  ];

  // The failed cooling alarm.
  //
  // Composed of:
  //   failed (descriptor): Indicates that a point is not operating as
  //     intended or is incapable of operating.
  //   cooling (descriptor): Process, measured effect or requirement for
  //     cooling.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `failed_cooling_alarm`, point type `alarm`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue failed_cooling_alarm = 67 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "failed_cooling_alarm"
      subfields: "failed"
      subfields: "cooling"
      subfields: "alarm"
      point_type: "alarm"
    }
  ];

  // The failed differential pressure alarm.
  //
  // Composed of:
  //   failed (descriptor): Indicates that a point is not operating as
  //     intended or is incapable of operating.
  //   differential (measurement_descriptor): Measured difference between
  //     two reference points (e.g. differential_water_pressure)
  //   pressure (measurement): Measured force per unit area within a system
  //     or process.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `failed_differential_pressure_alarm`, point type
  // `alarm`. Measured as pressure; values are in pascals, the ontology's
  // standard unit for that quantity.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue failed_differential_pressure_alarm = 68 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "failed_differential_pressure_alarm"
      subfields: "failed"
      subfields: "differential"
      subfields: "pressure"
      subfields: "alarm"
      point_type: "alarm"
      measurement: "pressure"
      standard_unit: "pascals"
    }
  ];

  // The failed heating alarm.
  //
  // Composed of:
  //   failed (descriptor): Indicates that a point is not operating as
  //     intended or is incapable of operating.
  //   heating (descriptor): Process, measured effect or requirement for
  //     heating.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `failed_heating_alarm`, point type `alarm`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue failed_heating_alarm = 69 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "failed_heating_alarm"
      subfields: "failed"
      subfields: "heating"
      subfields: "alarm"
      point_type: "alarm"
    }
  ];

  // The failed heating return water isolation valve alarm.
  //
  // Composed of:
  //   failed (descriptor): Indicates that a point is not operating as
  //     intended or is incapable of operating.
  //   heating (descriptor): Process, measured effect or requirement for
  //     heating.
  //   return (descriptor): Measurement or process of media as it is
  //     returned from the end-use equipment within the system.
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   isolation (descriptor): Process of isolating one component or
  //     process from another.
  //   valve (component): Component which meters the flow of water within a
  //     system or device.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `failed_heating_return_water_isolation_valve_alarm`,
  // point type `alarm`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue failed_heating_return_water_isolation_valve_alarm = 70 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "failed_heating_return_water_isolation_valve_alarm"
      subfields: "failed"
      subfields: "heating"
      subfields: "return"
      subfields: "water"
      subfields: "isolation"
      subfields: "valve"
      subfields: "alarm"
      point_type: "alarm"
    }
  ];

  // The failed isolation valve alarm.
  //
  // Composed of:
  //   failed (descriptor): Indicates that a point is not operating as
  //     intended or is incapable of operating.
  //   isolation (descriptor): Process of isolating one component or
  //     process from another.
  //   valve (component): Component which meters the flow of water within a
  //     system or device.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `failed_isolation_valve_alarm`, point type `alarm`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue failed_isolation_valve_alarm = 71 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "failed_isolation_valve_alarm"
      subfields: "failed"
      subfields: "isolation"
      subfields: "valve"
      subfields: "alarm"
      point_type: "alarm"
    }
  ];

  // The failed return water isolation valve alarm.
  //
  // Composed of:
  //   failed (descriptor): Indicates that a point is not operating as
  //     intended or is incapable of operating.
  //   return (descriptor): Measurement or process of media as it is
  //     returned from the end-use equipment within the system.
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   isolation (descriptor): Process of isolating one component or
  //     process from another.
  //   valve (component): Component which meters the flow of water within a
  //     system or device.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `failed_return_water_isolation_valve_alarm`, point type
  // `alarm`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue failed_return_water_isolation_valve_alarm = 72 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "failed_return_water_isolation_valve_alarm"
      subfields: "failed"
      subfields: "return"
      subfields: "water"
      subfields: "isolation"
      subfields: "valve"
      subfields: "alarm"
      point_type: "alarm"
    }
  ];

  // The failed return water temperature alarm.
  //
  // Composed of:
  //   failed (descriptor): Indicates that a point is not operating as
  //     intended or is incapable of operating.
  //   return (descriptor): Measurement or process of media as it is
  //     returned from the end-use equipment within the system.
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `failed_return_water_temperature_alarm`, point type
  // `alarm`. Measured as temperature; values are in kelvin, the ontology's
  // standard unit for that quantity.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue failed_return_water_temperature_alarm = 73 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "failed_return_water_temperature_alarm"
      subfields: "failed"
      subfields: "return"
      subfields: "water"
      subfields: "temperature"
      subfields: "alarm"
      point_type: "alarm"
      measurement: "temperature"
      standard_unit: "kelvin"
    }
  ];

  // The failed supply water isolation valve alarm.
  //
  // Composed of:
  //   failed (descriptor): Indicates that a point is not operating as
  //     intended or is incapable of operating.
  //   supply (descriptor): Measurement or process of media as it is
  //     supplied to the end-use equipment within the system.
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   isolation (descriptor): Process of isolating one component or
  //     process from another.
  //   valve (component): Component which meters the flow of water within a
  //     system or device.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `failed_supply_water_isolation_valve_alarm`, point type
  // `alarm`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue failed_supply_water_isolation_valve_alarm = 74 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "failed_supply_water_isolation_valve_alarm"
      subfields: "failed"
      subfields: "supply"
      subfields: "water"
      subfields: "isolation"
      subfields: "valve"
      subfields: "alarm"
      point_type: "alarm"
    }
  ];

  // The failed supply water temperature alarm.
  //
  // Composed of:
  //   failed (descriptor): Indicates that a point is not operating as
  //     intended or is incapable of operating.
  //   supply (descriptor): Measurement or process of media as it is
  //     supplied to the end-use equipment within the system.
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `failed_supply_water_temperature_alarm`, point type
  // `alarm`. Measured as temperature; values are in kelvin, the ontology's
  // standard unit for that quantity.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue failed_supply_water_temperature_alarm = 75 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "failed_supply_water_temperature_alarm"
      subfields: "failed"
      subfields: "supply"
      subfields: "water"
      subfields: "temperature"
      subfields: "alarm"
      point_type: "alarm"
      measurement: "temperature"
      standard_unit: "kelvin"
    }
  ];

  // The flowrate capacity.
  //
  // Composed of:
  //   flowrate (measurement): Rate of fluid movement.
  //   capacity (point_type): A design parameter quantity (e.g., design
  //     motor power capacity). Is always a maximum limit.
  //
  // Ontology field: `flowrate_capacity`, point type `capacity`. Measured as
  // flowrate; values are in cubic_meters_per_second, the ontology's standard
  // unit for that quantity.
  //
  // Valid range from 0, enforced by protovalidate. Expected range up to
  // 141.584233. This is a data-quality expectation, not a constraint: a
  // reading outside it is a fault to report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double flowrate_capacity = 76 [
    (google.api.field_behavior) = OPTIONAL,
    (google.api.field_behavior) = IMMUTABLE,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "flowrate_capacity"
      subfields: "flowrate"
      subfields: "capacity"
      point_type: "capacity"
      measurement: "flowrate"
      standard_unit: "cubic_meters_per_second"
      expected_max: 141.584233
    },
    (buf.validate.field).double.gte = 0.0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The heating percentage command.
  //
  // Composed of:
  //   heating (descriptor): Process, measured effect or requirement for
  //     heating.
  //   percentage (measurement): Measurement per hundred.
  //   command (point_type): The signal given to make an action happen.
  //     Defaults to multistate unless used alongside a measurement_type.
  //
  // Ontology field: `heating_percentage_command`, point type `command`.
  // Measured as percentage; values are in percent, the ontology's standard
  // unit for that quantity.
  //
  // Valid range 0 to 100, enforced by protovalidate.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double heating_percentage_command = 77 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "heating_percentage_command"
      subfields: "heating"
      subfields: "percentage"
      subfields: "command"
      point_type: "command"
      measurement: "percentage"
      standard_unit: "percent"
    },
    (buf.validate.field).double.gte = 0.0,
    (buf.validate.field).double.lte = 100.0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The heating request count.
  //
  // Composed of:
  //   heating (descriptor): Process, measured effect or requirement for
  //     heating.
  //   request (descriptor): A signal from a system device that calls for
  //     more of a particular conditioning process (e.g., pressure requests
  //     from terminal units).
  //   count (point_type): Total count of actions or requests.
  //
  // Ontology field: `heating_request_count`, point type `count`.
  // Dimensionless: the ontology defines this point type as an integer count
  // rather than a measured quantity.
  //
  // Valid range from 0, enforced by protovalidate. Expected range up to 250.
  // This is a data-quality expectation, not a constraint: a reading outside
  // it is a fault to report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional int64 heating_request_count = 78 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "heating_request_count"
      subfields: "heating"
      subfields: "request"
      subfields: "count"
      point_type: "count"
      expected_max: 250.0
    },
    (buf.validate.field).int64.gte = 0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The heating return water isolation valve command.
  //
  // Composed of:
  //   heating (descriptor): Process, measured effect or requirement for
  //     heating.
  //   return (descriptor): Measurement or process of media as it is
  //     returned from the end-use equipment within the system.
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   isolation (descriptor): Process of isolating one component or
  //     process from another.
  //   valve (component): Component which meters the flow of water within a
  //     system or device.
  //   command (point_type): The signal given to make an action happen.
  //     Defaults to multistate unless used alongside a measurement_type.
  //
  // Ontology field: `heating_return_water_isolation_valve_command`, point
  // type `command`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ClosedOpenValue heating_return_water_isolation_valve_command = 79 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "heating_return_water_isolation_valve_command"
      subfields: "heating"
      subfields: "return"
      subfields: "water"
      subfields: "isolation"
      subfields: "valve"
      subfields: "command"
      point_type: "command"
    }
  ];

  // The heating return water isolation valve percentage command.
  //
  // Composed of:
  //   heating (descriptor): Process, measured effect or requirement for
  //     heating.
  //   return (descriptor): Measurement or process of media as it is
  //     returned from the end-use equipment within the system.
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   isolation (descriptor): Process of isolating one component or
  //     process from another.
  //   valve (component): Component which meters the flow of water within a
  //     system or device.
  //   percentage (measurement): Measurement per hundred.
  //   command (point_type): The signal given to make an action happen.
  //     Defaults to multistate unless used alongside a measurement_type.
  //
  // Ontology field:
  // `heating_return_water_isolation_valve_percentage_command`, point type
  // `command`. Measured as percentage; values are in percent, the ontology's
  // standard unit for that quantity.
  //
  // Valid range 0 to 100, enforced by protovalidate.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double heating_return_water_isolation_valve_percentage_command = 80 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "heating_return_water_isolation_valve_percentage_command"
      subfields: "heating"
      subfields: "return"
      subfields: "water"
      subfields: "isolation"
      subfields: "valve"
      subfields: "percentage"
      subfields: "command"
      point_type: "command"
      measurement: "percentage"
      standard_unit: "percent"
    },
    (buf.validate.field).double.gte = 0.0,
    (buf.validate.field).double.lte = 100.0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The heating return water isolation valve percentage sensor.
  //
  // Composed of:
  //   heating (descriptor): Process, measured effect or requirement for
  //     heating.
  //   return (descriptor): Measurement or process of media as it is
  //     returned from the end-use equipment within the system.
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   isolation (descriptor): Process of isolating one component or
  //     process from another.
  //   valve (component): Component which meters the flow of water within a
  //     system or device.
  //   percentage (measurement): Measurement per hundred.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field:
  // `heating_return_water_isolation_valve_percentage_sensor`, point type
  // `sensor`. Measured as percentage; values are in percent, the ontology's
  // standard unit for that quantity.
  //
  // Valid range 0 to 100, enforced by protovalidate.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double heating_return_water_isolation_valve_percentage_sensor = 81 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "heating_return_water_isolation_valve_percentage_sensor"
      subfields: "heating"
      subfields: "return"
      subfields: "water"
      subfields: "isolation"
      subfields: "valve"
      subfields: "percentage"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "percentage"
      standard_unit: "percent"
    },
    (buf.validate.field).double.gte = 0.0,
    (buf.validate.field).double.lte = 100.0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The heating return water isolation valve status.
  //
  // Composed of:
  //   heating (descriptor): Process, measured effect or requirement for
  //     heating.
  //   return (descriptor): Measurement or process of media as it is
  //     returned from the end-use equipment within the system.
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   isolation (descriptor): Process of isolating one component or
  //     process from another.
  //   valve (component): Component which meters the flow of water within a
  //     system or device.
  //   status (point_type): The multistate value indicating an observed
  //     state in a piece of equipment, often indicating if a command was
  //     effected. It is a neutral observation (i.e., no quality judgment of
  //     'good' or 'bad'). It also has no units of measurement; therefore, if
  //     combined with a measurement subfield, it will indicate that the
  //     field is the directional status based on some measurement of that
  //     type (e.g., power_status equates to an on/off value based on some
  //     inference of power).
  //
  // Ontology field: `heating_return_water_isolation_valve_status`, point
  // type `status`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ClosedOpenValue heating_return_water_isolation_valve_status = 82 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "heating_return_water_isolation_valve_status"
      subfields: "heating"
      subfields: "return"
      subfields: "water"
      subfields: "isolation"
      subfields: "valve"
      subfields: "status"
      point_type: "status"
    }
  ];

  // The heating return water temperature sensor.
  //
  // Composed of:
  //   heating (descriptor): Process, measured effect or requirement for
  //     heating.
  //   return (descriptor): Measurement or process of media as it is
  //     returned from the end-use equipment within the system.
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `heating_return_water_temperature_sensor`, point type
  // `sensor`. Measured as temperature; values are in kelvin, the ontology's
  // standard unit for that quantity.
  //
  // Expected range 255.9259259 to 380.3703704. This is a data-quality
  // expectation, not a constraint: a reading outside it is a fault to
  // report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double heating_return_water_temperature_sensor = 83 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "heating_return_water_temperature_sensor"
      subfields: "heating"
      subfields: "return"
      subfields: "water"
      subfields: "temperature"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "temperature"
      standard_unit: "kelvin"
      expected_min: 255.925926
      expected_max: 380.37037
    }
  ];

  // The heating run command.
  //
  // Composed of:
  //   heating (descriptor): Process, measured effect or requirement for
  //     heating.
  //   run (measurement_descriptor): State of being active or operating. By
  //     default applies to control program for system (e.g., VAV program
  //     run_command).
  //   command (point_type): The signal given to make an action happen.
  //     Defaults to multistate unless used alongside a measurement_type.
  //
  // Ontology field: `heating_run_command`, point type `command`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional OffOnUnknownValue heating_run_command = 84 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "heating_run_command"
      subfields: "heating"
      subfields: "run"
      subfields: "command"
      point_type: "command"
    }
  ];

  // The heating run status.
  //
  // Composed of:
  //   heating (descriptor): Process, measured effect or requirement for
  //     heating.
  //   run (measurement_descriptor): State of being active or operating. By
  //     default applies to control program for system (e.g., VAV program
  //     run_command).
  //   status (point_type): The multistate value indicating an observed
  //     state in a piece of equipment, often indicating if a command was
  //     effected. It is a neutral observation (i.e., no quality judgment of
  //     'good' or 'bad'). It also has no units of measurement; therefore, if
  //     combined with a measurement subfield, it will indicate that the
  //     field is the directional status based on some measurement of that
  //     type (e.g., power_status equates to an on/off value based on some
  //     inference of power).
  //
  // Ontology field: `heating_run_status`, point type `status`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional OffOnValue heating_run_status = 85 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "heating_run_status"
      subfields: "heating"
      subfields: "run"
      subfields: "status"
      point_type: "status"
    }
  ];

  // The heating supply water flowrate sensor.
  //
  // Composed of:
  //   heating (descriptor): Process, measured effect or requirement for
  //     heating.
  //   supply (descriptor): Measurement or process of media as it is
  //     supplied to the end-use equipment within the system.
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   flowrate (measurement): Rate of fluid movement.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `heating_supply_water_flowrate_sensor`, point type
  // `sensor`. Measured as flowrate; values are in cubic_meters_per_second,
  // the ontology's standard unit for that quantity.
  //
  // Expected range -0.000315451 to 0.315451. This is a data-quality
  // expectation, not a constraint: a reading outside it is a fault to
  // report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double heating_supply_water_flowrate_sensor = 86 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "heating_supply_water_flowrate_sensor"
      subfields: "heating"
      subfields: "supply"
      subfields: "water"
      subfields: "flowrate"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "flowrate"
      standard_unit: "cubic_meters_per_second"
      expected_min: -0.000315
      expected_max: 0.315451
    }
  ];

  // The heating supply water isolation valve command.
  //
  // Composed of:
  //   heating (descriptor): Process, measured effect or requirement for
  //     heating.
  //   supply (descriptor): Measurement or process of media as it is
  //     supplied to the end-use equipment within the system.
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   isolation (descriptor): Process of isolating one component or
  //     process from another.
  //   valve (component): Component which meters the flow of water within a
  //     system or device.
  //   command (point_type): The signal given to make an action happen.
  //     Defaults to multistate unless used alongside a measurement_type.
  //
  // Ontology field: `heating_supply_water_isolation_valve_command`, point
  // type `command`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ClosedOpenValue heating_supply_water_isolation_valve_command = 87 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "heating_supply_water_isolation_valve_command"
      subfields: "heating"
      subfields: "supply"
      subfields: "water"
      subfields: "isolation"
      subfields: "valve"
      subfields: "command"
      point_type: "command"
    }
  ];

  // The heating supply water isolation valve percentage command.
  //
  // Composed of:
  //   heating (descriptor): Process, measured effect or requirement for
  //     heating.
  //   supply (descriptor): Measurement or process of media as it is
  //     supplied to the end-use equipment within the system.
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   isolation (descriptor): Process of isolating one component or
  //     process from another.
  //   valve (component): Component which meters the flow of water within a
  //     system or device.
  //   percentage (measurement): Measurement per hundred.
  //   command (point_type): The signal given to make an action happen.
  //     Defaults to multistate unless used alongside a measurement_type.
  //
  // Ontology field:
  // `heating_supply_water_isolation_valve_percentage_command`, point type
  // `command`. Measured as percentage; values are in percent, the ontology's
  // standard unit for that quantity.
  //
  // Valid range 0 to 100, enforced by protovalidate.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double heating_supply_water_isolation_valve_percentage_command = 88 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "heating_supply_water_isolation_valve_percentage_command"
      subfields: "heating"
      subfields: "supply"
      subfields: "water"
      subfields: "isolation"
      subfields: "valve"
      subfields: "percentage"
      subfields: "command"
      point_type: "command"
      measurement: "percentage"
      standard_unit: "percent"
    },
    (buf.validate.field).double.gte = 0.0,
    (buf.validate.field).double.lte = 100.0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The heating supply water isolation valve percentage sensor.
  //
  // Composed of:
  //   heating (descriptor): Process, measured effect or requirement for
  //     heating.
  //   supply (descriptor): Measurement or process of media as it is
  //     supplied to the end-use equipment within the system.
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   isolation (descriptor): Process of isolating one component or
  //     process from another.
  //   valve (component): Component which meters the flow of water within a
  //     system or device.
  //   percentage (measurement): Measurement per hundred.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field:
  // `heating_supply_water_isolation_valve_percentage_sensor`, point type
  // `sensor`. Measured as percentage; values are in percent, the ontology's
  // standard unit for that quantity.
  //
  // Valid range 0 to 100, enforced by protovalidate.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double heating_supply_water_isolation_valve_percentage_sensor = 89 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "heating_supply_water_isolation_valve_percentage_sensor"
      subfields: "heating"
      subfields: "supply"
      subfields: "water"
      subfields: "isolation"
      subfields: "valve"
      subfields: "percentage"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "percentage"
      standard_unit: "percent"
    },
    (buf.validate.field).double.gte = 0.0,
    (buf.validate.field).double.lte = 100.0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The heating supply water isolation valve status.
  //
  // Composed of:
  //   heating (descriptor): Process, measured effect or requirement for
  //     heating.
  //   supply (descriptor): Measurement or process of media as it is
  //     supplied to the end-use equipment within the system.
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   isolation (descriptor): Process of isolating one component or
  //     process from another.
  //   valve (component): Component which meters the flow of water within a
  //     system or device.
  //   status (point_type): The multistate value indicating an observed
  //     state in a piece of equipment, often indicating if a command was
  //     effected. It is a neutral observation (i.e., no quality judgment of
  //     'good' or 'bad'). It also has no units of measurement; therefore, if
  //     combined with a measurement subfield, it will indicate that the
  //     field is the directional status based on some measurement of that
  //     type (e.g., power_status equates to an on/off value based on some
  //     inference of power).
  //
  // Ontology field: `heating_supply_water_isolation_valve_status`, point
  // type `status`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ClosedOpenValue heating_supply_water_isolation_valve_status = 90 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "heating_supply_water_isolation_valve_status"
      subfields: "heating"
      subfields: "supply"
      subfields: "water"
      subfields: "isolation"
      subfields: "valve"
      subfields: "status"
      point_type: "status"
    }
  ];

  // The heating supply water temperature sensor.
  //
  // Composed of:
  //   heating (descriptor): Process, measured effect or requirement for
  //     heating.
  //   supply (descriptor): Measurement or process of media as it is
  //     supplied to the end-use equipment within the system.
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `heating_supply_water_temperature_sensor`, point type
  // `sensor`. Measured as temperature; values are in kelvin, the ontology's
  // standard unit for that quantity.
  //
  // Expected range 255.9259259 to 380.3703704. This is a data-quality
  // expectation, not a constraint: a reading outside it is a fault to
  // report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double heating_supply_water_temperature_sensor = 91 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "heating_supply_water_temperature_sensor"
      subfields: "heating"
      subfields: "supply"
      subfields: "water"
      subfields: "temperature"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "temperature"
      standard_unit: "kelvin"
      expected_min: 255.925926
      expected_max: 380.37037
    }
  ];

  // The heating supply water temperature setpoint.
  //
  // Composed of:
  //   heating (descriptor): Process, measured effect or requirement for
  //     heating.
  //   supply (descriptor): Measurement or process of media as it is
  //     supplied to the end-use equipment within the system.
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   setpoint (point_type): Control target of process or system.
  //
  // Ontology field: `heating_supply_water_temperature_setpoint`, point type
  // `setpoint`. Measured as temperature; values are in kelvin, the
  // ontology's standard unit for that quantity.
  //
  // Expected range 255.9259259 to 380.3703704. This is a data-quality
  // expectation, not a constraint: a reading outside it is a fault to
  // report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double heating_supply_water_temperature_setpoint = 92 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "heating_supply_water_temperature_setpoint"
      subfields: "heating"
      subfields: "supply"
      subfields: "water"
      subfields: "temperature"
      subfields: "setpoint"
      point_type: "setpoint"
      measurement: "temperature"
      standard_unit: "kelvin"
      expected_min: 255.925926
      expected_max: 380.37037
    }
  ];

  // The heating thermal power capacity.
  //
  // Composed of:
  //   heating (descriptor): Process, measured effect or requirement for
  //     heating.
  //   thermal (measurement_descriptor): Relating to the transfer of heat.
  //   power (measurement): Rate of energy consumption (assumed context is
  //     electrical and real, unless otherwise modified).
  //   capacity (point_type): A design parameter quantity (e.g., design
  //     motor power capacity). Is always a maximum limit.
  //
  // Ontology field: `heating_thermal_power_capacity`, point type `capacity`.
  // Measured as power; values are in watts, the ontology's standard unit for
  // that quantity.
  //
  // Valid range from 0, enforced by protovalidate. Expected range up to
  // 2.9307107e+06. This is a data-quality expectation, not a constraint: a
  // reading outside it is a fault to report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double heating_thermal_power_capacity = 93 [
    (google.api.field_behavior) = OPTIONAL,
    (google.api.field_behavior) = IMMUTABLE,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "heating_thermal_power_capacity"
      subfields: "heating"
      subfields: "thermal"
      subfields: "power"
      subfields: "capacity"
      point_type: "capacity"
      measurement: "power"
      standard_unit: "watts"
      expected_max: 2930710.7
    },
    (buf.validate.field).double.gte = 0.0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The heating thermal power sensor.
  //
  // Composed of:
  //   heating (descriptor): Process, measured effect or requirement for
  //     heating.
  //   thermal (measurement_descriptor): Relating to the transfer of heat.
  //   power (measurement): Rate of energy consumption (assumed context is
  //     electrical and real, unless otherwise modified).
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `heating_thermal_power_sensor`, point type `sensor`.
  // Measured as power; values are in watts, the ontology's standard unit for
  // that quantity.
  //
  // Expected range 0 to 586142.14. This is a data-quality expectation, not a
  // constraint: a reading outside it is a fault to report, not a message to
  // reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double heating_thermal_power_sensor = 94 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "heating_thermal_power_sensor"
      subfields: "heating"
      subfields: "thermal"
      subfields: "power"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "power"
      standard_unit: "watts"
      expected_min: 0.0
      expected_max: 586142.14
    }
  ];

  // The heating water circulation pump run command.
  //
  // Composed of:
  //   heating (descriptor): Process, measured effect or requirement for
  //     heating.
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   circulation (descriptor): Process or component used to circulate
  //     fluid through a device or system; for exmaple, a circulation pump
  //     onboard a boiler.
  //   pump (component): Component used for the distribution of liquid
  //     media (e.g., water, glycol).
  //   run (measurement_descriptor): State of being active or operating. By
  //     default applies to control program for system (e.g., VAV program
  //     run_command).
  //   command (point_type): The signal given to make an action happen.
  //     Defaults to multistate unless used alongside a measurement_type.
  //
  // Ontology field: `heating_water_circulation_pump_run_command`, point type
  // `command`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  repeated OffOnValue heating_water_circulation_pump_run_command = 95 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "heating_water_circulation_pump_run_command"
      subfields: "heating"
      subfields: "water"
      subfields: "circulation"
      subfields: "pump"
      subfields: "run"
      subfields: "command"
      point_type: "command"
      enumerated: true
    }
  ];

  // The heating water circulation pump run status.
  //
  // Composed of:
  //   heating (descriptor): Process, measured effect or requirement for
  //     heating.
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   circulation (descriptor): Process or component used to circulate
  //     fluid through a device or system; for exmaple, a circulation pump
  //     onboard a boiler.
  //   pump (component): Component used for the distribution of liquid
  //     media (e.g., water, glycol).
  //   run (measurement_descriptor): State of being active or operating. By
  //     default applies to control program for system (e.g., VAV program
  //     run_command).
  //   status (point_type): The multistate value indicating an observed
  //     state in a piece of equipment, often indicating if a command was
  //     effected. It is a neutral observation (i.e., no quality judgment of
  //     'good' or 'bad'). It also has no units of measurement; therefore, if
  //     combined with a measurement subfield, it will indicate that the
  //     field is the directional status based on some measurement of that
  //     type (e.g., power_status equates to an on/off value based on some
  //     inference of power).
  //
  // Ontology field: `heating_water_circulation_pump_run_status`, point type
  // `status`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  repeated OffOnValue heating_water_circulation_pump_run_status = 96 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "heating_water_circulation_pump_run_status"
      subfields: "heating"
      subfields: "water"
      subfields: "circulation"
      subfields: "pump"
      subfields: "run"
      subfields: "status"
      point_type: "status"
      enumerated: true
    }
  ];

  // The heating water differential pressure sensor.
  //
  // Composed of:
  //   heating (descriptor): Process, measured effect or requirement for
  //     heating.
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   differential (measurement_descriptor): Measured difference between
  //     two reference points (e.g. differential_water_pressure)
  //   pressure (measurement): Measured force per unit area within a system
  //     or process.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `heating_water_differential_pressure_sensor`, point type
  // `sensor`. Measured as pressure; values are in pascals, the ontology's
  // standard unit for that quantity.
  //
  // Expected range -34473.78945 to 344737.8945. This is a data-quality
  // expectation, not a constraint: a reading outside it is a fault to
  // report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double heating_water_differential_pressure_sensor = 97 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "heating_water_differential_pressure_sensor"
      subfields: "heating"
      subfields: "water"
      subfields: "differential"
      subfields: "pressure"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "pressure"
      standard_unit: "pascals"
      expected_min: -34473.78945
      expected_max: 344737.8945
    }
  ];

  // The high chilled return water temperature alarm.
  //
  // Composed of:
  //   high (descriptor): Level of control or measurement; above medium and
  //     low; opposite of low.
  //   chilled (descriptor): Cold water (typically 40 - 50F).
  //   return (descriptor): Measurement or process of media as it is
  //     returned from the end-use equipment within the system.
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `high_chilled_return_water_temperature_alarm`, point
  // type `alarm`. Measured as temperature; values are in kelvin, the
  // ontology's standard unit for that quantity.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue high_chilled_return_water_temperature_alarm = 98 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "high_chilled_return_water_temperature_alarm"
      subfields: "high"
      subfields: "chilled"
      subfields: "return"
      subfields: "water"
      subfields: "temperature"
      subfields: "alarm"
      point_type: "alarm"
      measurement: "temperature"
      standard_unit: "kelvin"
    }
  ];

  // The high chilled supply water temperature alarm.
  //
  // Composed of:
  //   high (descriptor): Level of control or measurement; above medium and
  //     low; opposite of low.
  //   chilled (descriptor): Cold water (typically 40 - 50F).
  //   supply (descriptor): Measurement or process of media as it is
  //     supplied to the end-use equipment within the system.
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `high_chilled_supply_water_temperature_alarm`, point
  // type `alarm`. Measured as temperature; values are in kelvin, the
  // ontology's standard unit for that quantity.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue high_chilled_supply_water_temperature_alarm = 99 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "high_chilled_supply_water_temperature_alarm"
      subfields: "high"
      subfields: "chilled"
      subfields: "supply"
      subfields: "water"
      subfields: "temperature"
      subfields: "alarm"
      point_type: "alarm"
      measurement: "temperature"
      standard_unit: "kelvin"
    }
  ];

  // The high chilled water differential pressure alarm.
  //
  // Composed of:
  //   high (descriptor): Level of control or measurement; above medium and
  //     low; opposite of low.
  //   chilled (descriptor): Cold water (typically 40 - 50F).
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   differential (measurement_descriptor): Measured difference between
  //     two reference points (e.g. differential_water_pressure)
  //   pressure (measurement): Measured force per unit area within a system
  //     or process.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `high_chilled_water_differential_pressure_alarm`, point
  // type `alarm`. Measured as pressure; values are in pascals, the
  // ontology's standard unit for that quantity.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue high_chilled_water_differential_pressure_alarm = 100 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "high_chilled_water_differential_pressure_alarm"
      subfields: "high"
      subfields: "chilled"
      subfields: "water"
      subfields: "differential"
      subfields: "pressure"
      subfields: "alarm"
      point_type: "alarm"
      measurement: "pressure"
      standard_unit: "pascals"
    }
  ];

  // The high differential pressure alarm.
  //
  // Composed of:
  //   high (descriptor): Level of control or measurement; above medium and
  //     low; opposite of low.
  //   differential (measurement_descriptor): Measured difference between
  //     two reference points (e.g. differential_water_pressure)
  //   pressure (measurement): Measured force per unit area within a system
  //     or process.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `high_differential_pressure_alarm`, point type `alarm`.
  // Measured as pressure; values are in pascals, the ontology's standard
  // unit for that quantity.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue high_differential_pressure_alarm = 101 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "high_differential_pressure_alarm"
      subfields: "high"
      subfields: "differential"
      subfields: "pressure"
      subfields: "alarm"
      point_type: "alarm"
      measurement: "pressure"
      standard_unit: "pascals"
    }
  ];

  // The high heating supply water temperature alarm.
  //
  // Composed of:
  //   high (descriptor): Level of control or measurement; above medium and
  //     low; opposite of low.
  //   heating (descriptor): Process, measured effect or requirement for
  //     heating.
  //   supply (descriptor): Measurement or process of media as it is
  //     supplied to the end-use equipment within the system.
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `high_heating_supply_water_temperature_alarm`, point
  // type `alarm`. Measured as temperature; values are in kelvin, the
  // ontology's standard unit for that quantity.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue high_heating_supply_water_temperature_alarm = 102 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "high_heating_supply_water_temperature_alarm"
      subfields: "high"
      subfields: "heating"
      subfields: "supply"
      subfields: "water"
      subfields: "temperature"
      subfields: "alarm"
      point_type: "alarm"
      measurement: "temperature"
      standard_unit: "kelvin"
    }
  ];

  // The high refrigerant pressure alarm.
  //
  // Composed of:
  //   high (descriptor): Level of control or measurement; above medium and
  //     low; opposite of low.
  //   refrigerant (descriptor): Substance used in the mechanical
  //     refrigeration process (e.g., R-134a)
  //   pressure (measurement): Measured force per unit area within a system
  //     or process.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `high_refrigerant_pressure_alarm`, point type `alarm`.
  // Measured as pressure; values are in pascals, the ontology's standard
  // unit for that quantity.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  repeated ActiveInactiveValue high_refrigerant_pressure_alarm = 103 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "high_refrigerant_pressure_alarm"
      subfields: "high"
      subfields: "refrigerant"
      subfields: "pressure"
      subfields: "alarm"
      point_type: "alarm"
      measurement: "pressure"
      standard_unit: "pascals"
      enumerated: true
    }
  ];

  // The high return water temperature alarm.
  //
  // Composed of:
  //   high (descriptor): Level of control or measurement; above medium and
  //     low; opposite of low.
  //   return (descriptor): Measurement or process of media as it is
  //     returned from the end-use equipment within the system.
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `high_return_water_temperature_alarm`, point type
  // `alarm`. Measured as temperature; values are in kelvin, the ontology's
  // standard unit for that quantity.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue high_return_water_temperature_alarm = 104 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "high_return_water_temperature_alarm"
      subfields: "high"
      subfields: "return"
      subfields: "water"
      subfields: "temperature"
      subfields: "alarm"
      point_type: "alarm"
      measurement: "temperature"
      standard_unit: "kelvin"
    }
  ];

  // The high supply water temperature alarm.
  //
  // Composed of:
  //   high (descriptor): Level of control or measurement; above medium and
  //     low; opposite of low.
  //   supply (descriptor): Measurement or process of media as it is
  //     supplied to the end-use equipment within the system.
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `high_supply_water_temperature_alarm`, point type
  // `alarm`. Measured as temperature; values are in kelvin, the ontology's
  // standard unit for that quantity.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue high_supply_water_temperature_alarm = 105 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "high_supply_water_temperature_alarm"
      subfields: "high"
      subfields: "supply"
      subfields: "water"
      subfields: "temperature"
      subfields: "alarm"
      point_type: "alarm"
      measurement: "temperature"
      standard_unit: "kelvin"
    }
  ];

  // The load percentage sensor.
  //
  // Composed of:
  //   load (descriptor): Electrical load which indicates the consumption
  //     of electrical power.
  //   percentage (measurement): Measurement per hundred.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `load_percentage_sensor`, point type `sensor`. Measured
  // as percentage; values are in percent, the ontology's standard unit for
  // that quantity.
  //
  // Valid range 0 to 100, enforced by protovalidate.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  repeated double load_percentage_sensor = 106 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "load_percentage_sensor"
      subfields: "load"
      subfields: "percentage"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "percentage"
      standard_unit: "percent"
      enumerated: true
    }
  ];

  // The local override alarm.
  //
  // Composed of:
  //   local (descriptor): A definite spatial location involving only a
  //     subset of the larger surrounding area.
  //   override (descriptor): An action that interrupts, cancels, or
  //     changes the current action or status.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `local_override_alarm`, point type `alarm`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue local_override_alarm = 107 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "local_override_alarm"
      subfields: "local"
      subfields: "override"
      subfields: "alarm"
      point_type: "alarm"
    }
  ];

  // The local override isolation valve alarm.
  //
  // Composed of:
  //   local (descriptor): A definite spatial location involving only a
  //     subset of the larger surrounding area.
  //   override (descriptor): An action that interrupts, cancels, or
  //     changes the current action or status.
  //   isolation (descriptor): Process of isolating one component or
  //     process from another.
  //   valve (component): Component which meters the flow of water within a
  //     system or device.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `local_override_isolation_valve_alarm`, point type
  // `alarm`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue local_override_isolation_valve_alarm = 108 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "local_override_isolation_valve_alarm"
      subfields: "local"
      subfields: "override"
      subfields: "isolation"
      subfields: "valve"
      subfields: "alarm"
      point_type: "alarm"
    }
  ];

  // The lost power alarm.
  //
  // Composed of:
  //   lost (measurement_descriptor): Indicates that a point has lost some
  //     quality or quantity inadvertently.
  //   power (measurement): Rate of energy consumption (assumed context is
  //     electrical and real, unless otherwise modified).
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `lost_power_alarm`, point type `alarm`. Measured as
  // power; values are in watts, the ontology's standard unit for that
  // quantity.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue lost_power_alarm = 109 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "lost_power_alarm"
      subfields: "lost"
      subfields: "power"
      subfields: "alarm"
      point_type: "alarm"
      measurement: "power"
      standard_unit: "watts"
    }
  ];

  // The low chilled return water temperature alarm.
  //
  // Composed of:
  //   low (descriptor): Level of control or measurement; below medium and
  //     high; opposite of high.
  //   chilled (descriptor): Cold water (typically 40 - 50F).
  //   return (descriptor): Measurement or process of media as it is
  //     returned from the end-use equipment within the system.
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `low_chilled_return_water_temperature_alarm`, point type
  // `alarm`. Measured as temperature; values are in kelvin, the ontology's
  // standard unit for that quantity.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue low_chilled_return_water_temperature_alarm = 110 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "low_chilled_return_water_temperature_alarm"
      subfields: "low"
      subfields: "chilled"
      subfields: "return"
      subfields: "water"
      subfields: "temperature"
      subfields: "alarm"
      point_type: "alarm"
      measurement: "temperature"
      standard_unit: "kelvin"
    }
  ];

  // The low chilled supply water temperature alarm.
  //
  // Composed of:
  //   low (descriptor): Level of control or measurement; below medium and
  //     high; opposite of high.
  //   chilled (descriptor): Cold water (typically 40 - 50F).
  //   supply (descriptor): Measurement or process of media as it is
  //     supplied to the end-use equipment within the system.
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `low_chilled_supply_water_temperature_alarm`, point type
  // `alarm`. Measured as temperature; values are in kelvin, the ontology's
  // standard unit for that quantity.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue low_chilled_supply_water_temperature_alarm = 111 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "low_chilled_supply_water_temperature_alarm"
      subfields: "low"
      subfields: "chilled"
      subfields: "supply"
      subfields: "water"
      subfields: "temperature"
      subfields: "alarm"
      point_type: "alarm"
      measurement: "temperature"
      standard_unit: "kelvin"
    }
  ];

  // The low chilled water differential pressure alarm.
  //
  // Composed of:
  //   low (descriptor): Level of control or measurement; below medium and
  //     high; opposite of high.
  //   chilled (descriptor): Cold water (typically 40 - 50F).
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   differential (measurement_descriptor): Measured difference between
  //     two reference points (e.g. differential_water_pressure)
  //   pressure (measurement): Measured force per unit area within a system
  //     or process.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `low_chilled_water_differential_pressure_alarm`, point
  // type `alarm`. Measured as pressure; values are in pascals, the
  // ontology's standard unit for that quantity.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue low_chilled_water_differential_pressure_alarm = 112 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "low_chilled_water_differential_pressure_alarm"
      subfields: "low"
      subfields: "chilled"
      subfields: "water"
      subfields: "differential"
      subfields: "pressure"
      subfields: "alarm"
      point_type: "alarm"
      measurement: "pressure"
      standard_unit: "pascals"
    }
  ];

  // The low differential pressure alarm.
  //
  // Composed of:
  //   low (descriptor): Level of control or measurement; below medium and
  //     high; opposite of high.
  //   differential (measurement_descriptor): Measured difference between
  //     two reference points (e.g. differential_water_pressure)
  //   pressure (measurement): Measured force per unit area within a system
  //     or process.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `low_differential_pressure_alarm`, point type `alarm`.
  // Measured as pressure; values are in pascals, the ontology's standard
  // unit for that quantity.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue low_differential_pressure_alarm = 113 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "low_differential_pressure_alarm"
      subfields: "low"
      subfields: "differential"
      subfields: "pressure"
      subfields: "alarm"
      point_type: "alarm"
      measurement: "pressure"
      standard_unit: "pascals"
    }
  ];

  // The low heating supply water temperature alarm.
  //
  // Composed of:
  //   low (descriptor): Level of control or measurement; below medium and
  //     high; opposite of high.
  //   heating (descriptor): Process, measured effect or requirement for
  //     heating.
  //   supply (descriptor): Measurement or process of media as it is
  //     supplied to the end-use equipment within the system.
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `low_heating_supply_water_temperature_alarm`, point type
  // `alarm`. Measured as temperature; values are in kelvin, the ontology's
  // standard unit for that quantity.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue low_heating_supply_water_temperature_alarm = 114 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "low_heating_supply_water_temperature_alarm"
      subfields: "low"
      subfields: "heating"
      subfields: "supply"
      subfields: "water"
      subfields: "temperature"
      subfields: "alarm"
      point_type: "alarm"
      measurement: "temperature"
      standard_unit: "kelvin"
    }
  ];

  // The low refrigerant pressure alarm.
  //
  // Composed of:
  //   low (descriptor): Level of control or measurement; below medium and
  //     high; opposite of high.
  //   refrigerant (descriptor): Substance used in the mechanical
  //     refrigeration process (e.g., R-134a)
  //   pressure (measurement): Measured force per unit area within a system
  //     or process.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `low_refrigerant_pressure_alarm`, point type `alarm`.
  // Measured as pressure; values are in pascals, the ontology's standard
  // unit for that quantity.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  repeated ActiveInactiveValue low_refrigerant_pressure_alarm = 115 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "low_refrigerant_pressure_alarm"
      subfields: "low"
      subfields: "refrigerant"
      subfields: "pressure"
      subfields: "alarm"
      point_type: "alarm"
      measurement: "pressure"
      standard_unit: "pascals"
      enumerated: true
    }
  ];

  // The low return water temperature alarm.
  //
  // Composed of:
  //   low (descriptor): Level of control or measurement; below medium and
  //     high; opposite of high.
  //   return (descriptor): Measurement or process of media as it is
  //     returned from the end-use equipment within the system.
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `low_return_water_temperature_alarm`, point type
  // `alarm`. Measured as temperature; values are in kelvin, the ontology's
  // standard unit for that quantity.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue low_return_water_temperature_alarm = 116 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "low_return_water_temperature_alarm"
      subfields: "low"
      subfields: "return"
      subfields: "water"
      subfields: "temperature"
      subfields: "alarm"
      point_type: "alarm"
      measurement: "temperature"
      standard_unit: "kelvin"
    }
  ];

  // The low supply water temperature alarm.
  //
  // Composed of:
  //   low (descriptor): Level of control or measurement; below medium and
  //     high; opposite of high.
  //   supply (descriptor): Measurement or process of media as it is
  //     supplied to the end-use equipment within the system.
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `low_supply_water_temperature_alarm`, point type
  // `alarm`. Measured as temperature; values are in kelvin, the ontology's
  // standard unit for that quantity.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue low_supply_water_temperature_alarm = 117 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "low_supply_water_temperature_alarm"
      subfields: "low"
      subfields: "supply"
      subfields: "water"
      subfields: "temperature"
      subfields: "alarm"
      point_type: "alarm"
      measurement: "temperature"
      standard_unit: "kelvin"
    }
  ];

  // The manufacturer label.
  //
  // Composed of:
  //   manufacturer (descriptor): The company which produces the asset or
  //     device.
  //   label (point_type): Identifying alias for component or system.
  //
  // Ontology field: `manufacturer_label`, point type `label`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional string manufacturer_label = 118 [
    (google.api.field_behavior) = OPTIONAL,
    (google.api.field_behavior) = IMMUTABLE,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "manufacturer_label"
      subfields: "manufacturer"
      subfields: "label"
      point_type: "label"
    }
  ];

  // The model label.
  //
  // Composed of:
  //   model (descriptor): Particular design version of an asset (e.g.
  //     model_label).
  //   label (point_type): Identifying alias for component or system.
  //
  // Ontology field: `model_label`, point type `label`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional string model_label = 119 [
    (google.api.field_behavior) = OPTIONAL,
    (google.api.field_behavior) = IMMUTABLE,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "model_label"
      subfields: "model"
      subfields: "label"
      point_type: "label"
    }
  ];

  // The optimal stop command.
  //
  // Composed of:
  //   optimal (descriptor): A method or solution being the most desirable
  //     or effective of all possibilities. Typically used in 'optimal
  //     start/stop,' referring to the most efficient point in time at which
  //     a system can turn off or on, while maintaining desired conditions
  //     and avoiding excess energy consumption or equipment wear.
  //   stop (measurement_descriptor): Indicates a point in time or space at
  //     which something has its ending.
  //   command (point_type): The signal given to make an action happen.
  //     Defaults to multistate unless used alongside a measurement_type.
  //
  // Ontology field: `optimal_stop_command`, point type `command`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional OffOnValue optimal_stop_command = 120 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "optimal_stop_command"
      subfields: "optimal"
      subfields: "stop"
      subfields: "command"
      point_type: "command"
    }
  ];

  // The power capacity.
  //
  // Composed of:
  //   power (measurement): Rate of energy consumption (assumed context is
  //     electrical and real, unless otherwise modified).
  //   capacity (point_type): A design parameter quantity (e.g., design
  //     motor power capacity). Is always a maximum limit.
  //
  // Ontology field: `power_capacity`, point type `capacity`. Measured as
  // power; values are in watts, the ontology's standard unit for that
  // quantity.
  //
  // Valid range from 0, enforced by protovalidate. Expected range up to
  // 100000. This is a data-quality expectation, not a constraint: a reading
  // outside it is a fault to report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double power_capacity = 121 [
    (google.api.field_behavior) = OPTIONAL,
    (google.api.field_behavior) = IMMUTABLE,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "power_capacity"
      subfields: "power"
      subfields: "capacity"
      point_type: "capacity"
      measurement: "power"
      standard_unit: "watts"
      expected_max: 100000.0
    },
    (buf.validate.field).double.gte = 0.0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The power sensor.
  //
  // Composed of:
  //   power (measurement): Rate of energy consumption (assumed context is
  //     electrical and real, unless otherwise modified).
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `power_sensor`, point type `sensor`. Measured as power;
  // values are in watts, the ontology's standard unit for that quantity.
  //
  // Expected range 0 to 1e+07. This is a data-quality expectation, not a
  // constraint: a reading outside it is a fault to report, not a message to
  // reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double power_sensor = 122 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "power_sensor"
      subfields: "power"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "power"
      standard_unit: "watts"
      expected_min: 0.0
      expected_max: 10000000.0
    }
  ];

  // The powerfactor sensor.
  //
  // Composed of:
  //   powerfactor (measurement): Ratio of real and apparent power.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `powerfactor_sensor`, point type `sensor`. Measured as
  // powerfactor; values are in no_units, the ontology's standard unit for
  // that quantity.
  //
  // Valid range -1 to 1, enforced by protovalidate.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double powerfactor_sensor = 123 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "powerfactor_sensor"
      subfields: "powerfactor"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "powerfactor"
      standard_unit: "no_units"
    },
    (buf.validate.field).double.gte = -1.0,
    (buf.validate.field).double.lte = 1.0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The pressurization request count.
  //
  // Composed of:
  //   pressurization (descriptor): Relating to the level of pressure in a
  //     system or vessel.
  //   request (descriptor): A signal from a system device that calls for
  //     more of a particular conditioning process (e.g., pressure requests
  //     from terminal units).
  //   count (point_type): Total count of actions or requests.
  //
  // Ontology field: `pressurization_request_count`, point type `count`.
  // Dimensionless: the ontology defines this point type as an integer count
  // rather than a measured quantity.
  //
  // Valid range from 0, enforced by protovalidate. Expected range up to 500.
  // This is a data-quality expectation, not a constraint: a reading outside
  // it is a fault to report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional int64 pressurization_request_count = 124 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "pressurization_request_count"
      subfields: "pressurization"
      subfields: "request"
      subfields: "count"
      point_type: "count"
      expected_max: 500.0
    },
    (buf.validate.field).int64.gte = 0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The refrigerant condenser pressure sensor.
  //
  // Composed of:
  //   refrigerant (descriptor): Substance used in the mechanical
  //     refrigeration process (e.g., R-134a)
  //   condenser (component): The component of a refrigeration system that
  //     condenses refrigerant through the removal of heat (i.e., refrigerant
  //     vapor is condensed into liquid through the removal of heat).
  //   pressure (measurement): Measured force per unit area within a system
  //     or process.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `refrigerant_condenser_pressure_sensor`, point type
  // `sensor`. Measured as pressure; values are in pascals, the ontology's
  // standard unit for that quantity.
  //
  // Expected range 0 to 2.068427367e+06. This is a data-quality expectation,
  // not a constraint: a reading outside it is a fault to report, not a
  // message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  repeated double refrigerant_condenser_pressure_sensor = 125 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "refrigerant_condenser_pressure_sensor"
      subfields: "refrigerant"
      subfields: "condenser"
      subfields: "pressure"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "pressure"
      standard_unit: "pascals"
      enumerated: true
      expected_min: 0.0
      expected_max: 2068427.367
    }
  ];

  // The refrigerant differential pressure sensor.
  //
  // Composed of:
  //   refrigerant (descriptor): Substance used in the mechanical
  //     refrigeration process (e.g., R-134a)
  //   differential (measurement_descriptor): Measured difference between
  //     two reference points (e.g. differential_water_pressure)
  //   pressure (measurement): Measured force per unit area within a system
  //     or process.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `refrigerant_differential_pressure_sensor`, point type
  // `sensor`. Measured as pressure; values are in pascals, the ontology's
  // standard unit for that quantity.
  //
  // Expected range -3447.38 to 137895.2. This is a data-quality expectation,
  // not a constraint: a reading outside it is a fault to report, not a
  // message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  repeated double refrigerant_differential_pressure_sensor = 126 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "refrigerant_differential_pressure_sensor"
      subfields: "refrigerant"
      subfields: "differential"
      subfields: "pressure"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "pressure"
      standard_unit: "pascals"
      enumerated: true
      expected_min: -3447.38
      expected_max: 137895.2
    }
  ];

  // The refrigerant discharge pressure sensor.
  //
  // Composed of:
  //   refrigerant (descriptor): Substance used in the mechanical
  //     refrigeration process (e.g., R-134a)
  //   discharge (descriptor): Media leaving system to enter ambient
  //     conditioned space. Typically applies only to air-side systems.
  //   pressure (measurement): Measured force per unit area within a system
  //     or process.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `refrigerant_discharge_pressure_sensor`, point type
  // `sensor`. Measured as pressure; values are in pascals, the ontology's
  // standard unit for that quantity.
  //
  // Expected range -34500 to 2.7579029173e+06. This is a data-quality
  // expectation, not a constraint: a reading outside it is a fault to
  // report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  repeated double refrigerant_discharge_pressure_sensor = 127 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "refrigerant_discharge_pressure_sensor"
      subfields: "refrigerant"
      subfields: "discharge"
      subfields: "pressure"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "pressure"
      standard_unit: "pascals"
      enumerated: true
      expected_min: -34500.0
      expected_max: 2757902.9173
    }
  ];

  // The refrigerant discharge temperature sensor.
  //
  // Composed of:
  //   refrigerant (descriptor): Substance used in the mechanical
  //     refrigeration process (e.g., R-134a)
  //   discharge (descriptor): Media leaving system to enter ambient
  //     conditioned space. Typically applies only to air-side systems.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `refrigerant_discharge_temperature_sensor`, point type
  // `sensor`. Measured as temperature; values are in kelvin, the ontology's
  // standard unit for that quantity.
  //
  // Expected range 255.9259259 to 422.037037. This is a data-quality
  // expectation, not a constraint: a reading outside it is a fault to
  // report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  repeated double refrigerant_discharge_temperature_sensor = 128 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "refrigerant_discharge_temperature_sensor"
      subfields: "refrigerant"
      subfields: "discharge"
      subfields: "temperature"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "temperature"
      standard_unit: "kelvin"
      enumerated: true
      expected_min: 255.925926
      expected_max: 422.037037
    }
  ];

  // The refrigerant evaporator pressure sensor.
  //
  // Composed of:
  //   refrigerant (descriptor): Substance used in the mechanical
  //     refrigeration process (e.g., R-134a)
  //   evaporator (component): The component of a refrigeration system that
  //     evaporates refrigerant by adding heat (i.e., refrigerant liquid
  //     evaporates into vapor when heat from the surrounding air/water is
  //     transferred to it).
  //   pressure (measurement): Measured force per unit area within a system
  //     or process.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `refrigerant_evaporator_pressure_sensor`, point type
  // `sensor`. Measured as pressure; values are in pascals, the ontology's
  // standard unit for that quantity.
  //
  // Expected range 0 to 2.068427367e+06. This is a data-quality expectation,
  // not a constraint: a reading outside it is a fault to report, not a
  // message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  repeated double refrigerant_evaporator_pressure_sensor = 129 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "refrigerant_evaporator_pressure_sensor"
      subfields: "refrigerant"
      subfields: "evaporator"
      subfields: "pressure"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "pressure"
      standard_unit: "pascals"
      enumerated: true
      expected_min: 0.0
      expected_max: 2068427.367
    }
  ];

  // The refrigerant injection pressure sensor.
  //
  // Composed of:
  //   refrigerant (descriptor): Substance used in the mechanical
  //     refrigeration process (e.g., R-134a)
  //   injection (descriptor): A process that introduces a substance, such
  //     as refrigerant, water, or air, into the system by using pressure or
  //     vacuum, to enhance performance, improve efficiency, or maintain
  //     proper operation.
  //   pressure (measurement): Measured force per unit area within a system
  //     or process.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `refrigerant_injection_pressure_sensor`, point type
  // `sensor`. Measured as pressure; values are in pascals, the ontology's
  // standard unit for that quantity.
  //
  // Expected range 0 to 2.068427367e+06. This is a data-quality expectation,
  // not a constraint: a reading outside it is a fault to report, not a
  // message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  repeated double refrigerant_injection_pressure_sensor = 130 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "refrigerant_injection_pressure_sensor"
      subfields: "refrigerant"
      subfields: "injection"
      subfields: "pressure"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "pressure"
      standard_unit: "pascals"
      enumerated: true
      expected_min: 0.0
      expected_max: 2068427.367
    }
  ];

  // The refrigerant injection suction superheat temperature sensor.
  //
  // Composed of:
  //   refrigerant (descriptor): Substance used in the mechanical
  //     refrigeration process (e.g., R-134a)
  //   injection (descriptor): A process that introduces a substance, such
  //     as refrigerant, water, or air, into the system by using pressure or
  //     vacuum, to enhance performance, improve efficiency, or maintain
  //     proper operation.
  //   suction (descriptor): The production of a partial vacuum in order to
  //     force fluid into a vacant space (such as refrigerant in a
  //     compressor).
  //   superheat (measurement_descriptor): The amount a gas is heated above
  //     its boiling point wihtout vaporization.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field:
  // `refrigerant_injection_suction_superheat_temperature_sensor`, point type
  // `sensor`. Measured as temperature; values are in kelvin, the ontology's
  // standard unit for that quantity.
  //
  // Expected range 255.9259259 to 422.037037. This is a data-quality
  // expectation, not a constraint: a reading outside it is a fault to
  // report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  repeated double refrigerant_injection_suction_superheat_temperature_sensor = 131 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "refrigerant_injection_suction_superheat_temperature_sensor"
      subfields: "refrigerant"
      subfields: "injection"
      subfields: "suction"
      subfields: "superheat"
      subfields: "temperature"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "temperature"
      standard_unit: "kelvin"
      enumerated: true
      expected_min: 255.925926
      expected_max: 422.037037
    }
  ];

  // The refrigerant injection suction temperature sensor.
  //
  // Composed of:
  //   refrigerant (descriptor): Substance used in the mechanical
  //     refrigeration process (e.g., R-134a)
  //   injection (descriptor): A process that introduces a substance, such
  //     as refrigerant, water, or air, into the system by using pressure or
  //     vacuum, to enhance performance, improve efficiency, or maintain
  //     proper operation.
  //   suction (descriptor): The production of a partial vacuum in order to
  //     force fluid into a vacant space (such as refrigerant in a
  //     compressor).
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `refrigerant_injection_suction_temperature_sensor`,
  // point type `sensor`. Measured as temperature; values are in kelvin, the
  // ontology's standard unit for that quantity.
  //
  // Expected range 255.9259259 to 422.037037. This is a data-quality
  // expectation, not a constraint: a reading outside it is a fault to
  // report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  repeated double refrigerant_injection_suction_temperature_sensor = 132 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "refrigerant_injection_suction_temperature_sensor"
      subfields: "refrigerant"
      subfields: "injection"
      subfields: "suction"
      subfields: "temperature"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "temperature"
      standard_unit: "kelvin"
      enumerated: true
      expected_min: 255.925926
      expected_max: 422.037037
    }
  ];

  // The refrigerant liquid pressure sensor.
  //
  // Composed of:
  //   refrigerant (descriptor): Substance used in the mechanical
  //     refrigeration process (e.g., R-134a)
  //   liquid (descriptor): A state of matter that flows and takes the form
  //     of its container, but which changes its volume very little with
  //     changes in pressure.
  //   pressure (measurement): Measured force per unit area within a system
  //     or process.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `refrigerant_liquid_pressure_sensor`, point type
  // `sensor`. Measured as pressure; values are in pascals, the ontology's
  // standard unit for that quantity.
  //
  // Expected range -34473.78945 to 2.7579029173e+06. This is a data-quality
  // expectation, not a constraint: a reading outside it is a fault to
  // report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  repeated double refrigerant_liquid_pressure_sensor = 133 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "refrigerant_liquid_pressure_sensor"
      subfields: "refrigerant"
      subfields: "liquid"
      subfields: "pressure"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "pressure"
      standard_unit: "pascals"
      enumerated: true
      expected_min: -34473.78945
      expected_max: 2757902.9173
    }
  ];

  // The refrigerant liquid saturation temperature sensor.
  //
  // Composed of:
  //   refrigerant (descriptor): Substance used in the mechanical
  //     refrigeration process (e.g., R-134a)
  //   liquid (descriptor): A state of matter that flows and takes the form
  //     of its container, but which changes its volume very little with
  //     changes in pressure.
  //   saturation (measurement_descriptor): Point at which no more of a
  //     material can be absorbed into another material.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `refrigerant_liquid_saturation_temperature_sensor`,
  // point type `sensor`. Measured as temperature; values are in kelvin, the
  // ontology's standard unit for that quantity.
  //
  // Expected range 255.9259259 to 422.037037. This is a data-quality
  // expectation, not a constraint: a reading outside it is a fault to
  // report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  repeated double refrigerant_liquid_saturation_temperature_sensor = 134 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "refrigerant_liquid_saturation_temperature_sensor"
      subfields: "refrigerant"
      subfields: "liquid"
      subfields: "saturation"
      subfields: "temperature"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "temperature"
      standard_unit: "kelvin"
      enumerated: true
      expected_min: 255.925926
      expected_max: 422.037037
    }
  ];

  // The refrigerant liquid temperature sensor.
  //
  // Composed of:
  //   refrigerant (descriptor): Substance used in the mechanical
  //     refrigeration process (e.g., R-134a)
  //   liquid (descriptor): A state of matter that flows and takes the form
  //     of its container, but which changes its volume very little with
  //     changes in pressure.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `refrigerant_liquid_temperature_sensor`, point type
  // `sensor`. Measured as temperature; values are in kelvin, the ontology's
  // standard unit for that quantity.
  //
  // Expected range 255.9259259 to 422.037037. This is a data-quality
  // expectation, not a constraint: a reading outside it is a fault to
  // report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  repeated double refrigerant_liquid_temperature_sensor = 135 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "refrigerant_liquid_temperature_sensor"
      subfields: "refrigerant"
      subfields: "liquid"
      subfields: "temperature"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "temperature"
      standard_unit: "kelvin"
      enumerated: true
      expected_min: 255.925926
      expected_max: 422.037037
    }
  ];

  // The refrigerant subcooling temperature sensor.
  //
  // Composed of:
  //   refrigerant (descriptor): Substance used in the mechanical
  //     refrigeration process (e.g., R-134a)
  //   subcooling (measurement_descriptor): The amount a liquid is cooled
  //     below its saturation point.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `refrigerant_subcooling_temperature_sensor`, point type
  // `sensor`. Measured as temperature; values are in kelvin, the ontology's
  // standard unit for that quantity.
  //
  // Expected range 227.5925926 to 366.4814815. This is a data-quality
  // expectation, not a constraint: a reading outside it is a fault to
  // report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  repeated double refrigerant_subcooling_temperature_sensor = 136 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "refrigerant_subcooling_temperature_sensor"
      subfields: "refrigerant"
      subfields: "subcooling"
      subfields: "temperature"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "temperature"
      standard_unit: "kelvin"
      enumerated: true
      expected_min: 227.592593
      expected_max: 366.481481
    }
  ];

  // The refrigerant suction pressure sensor.
  //
  // Composed of:
  //   refrigerant (descriptor): Substance used in the mechanical
  //     refrigeration process (e.g., R-134a)
  //   suction (descriptor): The production of a partial vacuum in order to
  //     force fluid into a vacant space (such as refrigerant in a
  //     compressor).
  //   pressure (measurement): Measured force per unit area within a system
  //     or process.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `refrigerant_suction_pressure_sensor`, point type
  // `sensor`. Measured as pressure; values are in pascals, the ontology's
  // standard unit for that quantity.
  //
  // Expected range -34473.78945 to 2.7579029173e+06. This is a data-quality
  // expectation, not a constraint: a reading outside it is a fault to
  // report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  repeated double refrigerant_suction_pressure_sensor = 137 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "refrigerant_suction_pressure_sensor"
      subfields: "refrigerant"
      subfields: "suction"
      subfields: "pressure"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "pressure"
      standard_unit: "pascals"
      enumerated: true
      expected_min: -34473.78945
      expected_max: 2757902.9173
    }
  ];

  // The refrigerant suction saturation temperature sensor.
  //
  // Composed of:
  //   refrigerant (descriptor): Substance used in the mechanical
  //     refrigeration process (e.g., R-134a)
  //   suction (descriptor): The production of a partial vacuum in order to
  //     force fluid into a vacant space (such as refrigerant in a
  //     compressor).
  //   saturation (measurement_descriptor): Point at which no more of a
  //     material can be absorbed into another material.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `refrigerant_suction_saturation_temperature_sensor`,
  // point type `sensor`. Measured as temperature; values are in kelvin, the
  // ontology's standard unit for that quantity.
  //
  // Expected range 255.9259259 to 422.037037. This is a data-quality
  // expectation, not a constraint: a reading outside it is a fault to
  // report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  repeated double refrigerant_suction_saturation_temperature_sensor = 138 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "refrigerant_suction_saturation_temperature_sensor"
      subfields: "refrigerant"
      subfields: "suction"
      subfields: "saturation"
      subfields: "temperature"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "temperature"
      standard_unit: "kelvin"
      enumerated: true
      expected_min: 255.925926
      expected_max: 422.037037
    }
  ];

  // The refrigerant suction superheat temperature sensor.
  //
  // Composed of:
  //   refrigerant (descriptor): Substance used in the mechanical
  //     refrigeration process (e.g., R-134a)
  //   suction (descriptor): The production of a partial vacuum in order to
  //     force fluid into a vacant space (such as refrigerant in a
  //     compressor).
  //   superheat (measurement_descriptor): The amount a gas is heated above
  //     its boiling point wihtout vaporization.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `refrigerant_suction_superheat_temperature_sensor`,
  // point type `sensor`. Measured as temperature; values are in kelvin, the
  // ontology's standard unit for that quantity.
  //
  // Expected range 255.9259259 to 422.037037. This is a data-quality
  // expectation, not a constraint: a reading outside it is a fault to
  // report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  repeated double refrigerant_suction_superheat_temperature_sensor = 139 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "refrigerant_suction_superheat_temperature_sensor"
      subfields: "refrigerant"
      subfields: "suction"
      subfields: "superheat"
      subfields: "temperature"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "temperature"
      standard_unit: "kelvin"
      enumerated: true
      expected_min: 255.925926
      expected_max: 422.037037
    }
  ];

  // The refrigerant suction temperature sensor.
  //
  // Composed of:
  //   refrigerant (descriptor): Substance used in the mechanical
  //     refrigeration process (e.g., R-134a)
  //   suction (descriptor): The production of a partial vacuum in order to
  //     force fluid into a vacant space (such as refrigerant in a
  //     compressor).
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `refrigerant_suction_temperature_sensor`, point type
  // `sensor`. Measured as temperature; values are in kelvin, the ontology's
  // standard unit for that quantity.
  //
  // Expected range 255.9259259 to 422.037037. This is a data-quality
  // expectation, not a constraint: a reading outside it is a fault to
  // report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  repeated double refrigerant_suction_temperature_sensor = 140 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "refrigerant_suction_temperature_sensor"
      subfields: "refrigerant"
      subfields: "suction"
      subfields: "temperature"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "temperature"
      standard_unit: "kelvin"
      enumerated: true
      expected_min: 255.925926
      expected_max: 422.037037
    }
  ];

  // The refrigerant vapor temperature sensor.
  //
  // Composed of:
  //   refrigerant (descriptor): Substance used in the mechanical
  //     refrigeration process (e.g., R-134a)
  //   vapor (descriptor): A substance in the gas phase at a temperature
  //     lower than its critical temperature.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `refrigerant_vapor_temperature_sensor`, point type
  // `sensor`. Measured as temperature; values are in kelvin, the ontology's
  // standard unit for that quantity.
  //
  // Expected range 255.9259259 to 422.037037. This is a data-quality
  // expectation, not a constraint: a reading outside it is a fault to
  // report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  repeated double refrigerant_vapor_temperature_sensor = 141 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "refrigerant_vapor_temperature_sensor"
      subfields: "refrigerant"
      subfields: "vapor"
      subfields: "temperature"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "temperature"
      standard_unit: "kelvin"
      enumerated: true
      expected_min: 255.925926
      expected_max: 422.037037
    }
  ];

  // The request run command.
  //
  // Composed of:
  //   request (descriptor): A signal from a system device that calls for
  //     more of a particular conditioning process (e.g., pressure requests
  //     from terminal units).
  //   run (measurement_descriptor): State of being active or operating. By
  //     default applies to control program for system (e.g., VAV program
  //     run_command).
  //   command (point_type): The signal given to make an action happen.
  //     Defaults to multistate unless used alongside a measurement_type.
  //
  // Ontology field: `request_run_command`, point type `command`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional OffOnValue request_run_command = 142 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "request_run_command"
      subfields: "request"
      subfields: "run"
      subfields: "command"
      point_type: "command"
    }
  ];

  // The return water isolation valve command.
  //
  // Composed of:
  //   return (descriptor): Measurement or process of media as it is
  //     returned from the end-use equipment within the system.
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   isolation (descriptor): Process of isolating one component or
  //     process from another.
  //   valve (component): Component which meters the flow of water within a
  //     system or device.
  //   command (point_type): The signal given to make an action happen.
  //     Defaults to multistate unless used alongside a measurement_type.
  //
  // Ontology field: `return_water_isolation_valve_command`, point type
  // `command`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ClosedOpenValue return_water_isolation_valve_command = 143 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "return_water_isolation_valve_command"
      subfields: "return"
      subfields: "water"
      subfields: "isolation"
      subfields: "valve"
      subfields: "command"
      point_type: "command"
    }
  ];

  // The return water isolation valve percentage sensor.
  //
  // Composed of:
  //   return (descriptor): Measurement or process of media as it is
  //     returned from the end-use equipment within the system.
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   isolation (descriptor): Process of isolating one component or
  //     process from another.
  //   valve (component): Component which meters the flow of water within a
  //     system or device.
  //   percentage (measurement): Measurement per hundred.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `return_water_isolation_valve_percentage_sensor`, point
  // type `sensor`. Measured as percentage; values are in percent, the
  // ontology's standard unit for that quantity.
  //
  // Valid range 0 to 100, enforced by protovalidate.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double return_water_isolation_valve_percentage_sensor = 144 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "return_water_isolation_valve_percentage_sensor"
      subfields: "return"
      subfields: "water"
      subfields: "isolation"
      subfields: "valve"
      subfields: "percentage"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "percentage"
      standard_unit: "percent"
    },
    (buf.validate.field).double.gte = 0.0,
    (buf.validate.field).double.lte = 100.0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The return water isolation valve status.
  //
  // Composed of:
  //   return (descriptor): Measurement or process of media as it is
  //     returned from the end-use equipment within the system.
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   isolation (descriptor): Process of isolating one component or
  //     process from another.
  //   valve (component): Component which meters the flow of water within a
  //     system or device.
  //   status (point_type): The multistate value indicating an observed
  //     state in a piece of equipment, often indicating if a command was
  //     effected. It is a neutral observation (i.e., no quality judgment of
  //     'good' or 'bad'). It also has no units of measurement; therefore, if
  //     combined with a measurement subfield, it will indicate that the
  //     field is the directional status based on some measurement of that
  //     type (e.g., power_status equates to an on/off value based on some
  //     inference of power).
  //
  // Ontology field: `return_water_isolation_valve_status`, point type
  // `status`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ClosedOpenValue return_water_isolation_valve_status = 145 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "return_water_isolation_valve_status"
      subfields: "return"
      subfields: "water"
      subfields: "isolation"
      subfields: "valve"
      subfields: "status"
      point_type: "status"
    }
  ];

  // The return water temperature sensor.
  //
  // Composed of:
  //   return (descriptor): Measurement or process of media as it is
  //     returned from the end-use equipment within the system.
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `return_water_temperature_sensor`, point type `sensor`.
  // Measured as temperature; values are in kelvin, the ontology's standard
  // unit for that quantity.
  //
  // Expected range 255.9259259 to 380.3703704. This is a data-quality
  // expectation, not a constraint: a reading outside it is a fault to
  // report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  repeated double return_water_temperature_sensor = 146 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "return_water_temperature_sensor"
      subfields: "return"
      subfields: "water"
      subfields: "temperature"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "temperature"
      standard_unit: "kelvin"
      enumerated: true
      expected_min: 255.925926
      expected_max: 380.37037
    }
  ];

  // The reversing valve command.
  //
  // Composed of:
  //   reversing (descriptor): Reverses direction of flow (e.g., the
  //     reversing valve on a heat pump changes the direction that the
  //     refrigerant flows around the circuit).
  //   valve (component): Component which meters the flow of water within a
  //     system or device.
  //   command (point_type): The signal given to make an action happen.
  //     Defaults to multistate unless used alongside a measurement_type.
  //
  // Ontology field: `reversing_valve_command`, point type `command`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  repeated AutoClosedOpenUnknownValue reversing_valve_command = 147 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "reversing_valve_command"
      subfields: "reversing"
      subfields: "valve"
      subfields: "command"
      point_type: "command"
      enumerated: true
    }
  ];

  // The run command.
  //
  // Composed of:
  //   run (measurement_descriptor): State of being active or operating. By
  //     default applies to control program for system (e.g., VAV program
  //     run_command).
  //   command (point_type): The signal given to make an action happen.
  //     Defaults to multistate unless used alongside a measurement_type.
  //
  // Ontology field: `run_command`, point type `command`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional OffOnValue run_command = 148 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "run_command"
      subfields: "run"
      subfields: "command"
      point_type: "command"
    }
  ];

  // The run mode.
  //
  // Composed of:
  //   run (measurement_descriptor): State of being active or operating. By
  //     default applies to control program for system (e.g., VAV program
  //     run_command).
  //   mode (point_type): Distinct mode of operation within system. For
  //     example economizer_mode has modes of 'enabled' or 'disabled.'
  //
  // Ontology field: `run_mode`, point type `mode`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional AutoCommissioningCoolingDehumidifyFanOnlyFlushingHeatingManualNeutralOffUnknownValue run_mode = 149 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "run_mode"
      subfields: "run"
      subfields: "mode"
      point_type: "mode"
    }
  ];

  // The run status.
  //
  // Composed of:
  //   run (measurement_descriptor): State of being active or operating. By
  //     default applies to control program for system (e.g., VAV program
  //     run_command).
  //   status (point_type): The multistate value indicating an observed
  //     state in a piece of equipment, often indicating if a command was
  //     effected. It is a neutral observation (i.e., no quality judgment of
  //     'good' or 'bad'). It also has no units of measurement; therefore, if
  //     combined with a measurement subfield, it will indicate that the
  //     field is the directional status based on some measurement of that
  //     type (e.g., power_status equates to an on/off value based on some
  //     inference of power).
  //
  // Ontology field: `run_status`, point type `status`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional OffOnStandbyUnknownValue run_status = 150 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "run_status"
      subfields: "run"
      subfields: "status"
      point_type: "status"
    }
  ];

  // The run time accumulator.
  //
  // Composed of:
  //   run (measurement_descriptor): State of being active or operating. By
  //     default applies to control program for system (e.g., VAV program
  //     run_command).
  //   time (measurement): Defined duration of something.
  //   accumulator (point_type): The total accumulated quantity (e.g.,
  //     total energy accumulated).
  //
  // Ontology field: `run_time_accumulator`, point type `accumulator`.
  // Measured as time; values are in seconds, the ontology's standard unit
  // for that quantity.
  //
  // Valid range from 0, enforced by protovalidate. Expected range up to
  // 1.25. This is a data-quality expectation, not a constraint: a reading
  // outside it is a fault to report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double run_time_accumulator = 151 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "run_time_accumulator"
      subfields: "run"
      subfields: "time"
      subfields: "accumulator"
      point_type: "accumulator"
      measurement: "time"
      standard_unit: "seconds"
      expected_max: 1.25
    },
    (buf.validate.field).double.gte = 0.0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The schedule run command.
  //
  // Composed of:
  //   schedule (descriptor): Refers to the time-of-day run-time
  //     requirements for the equipment.
  //   run (measurement_descriptor): State of being active or operating. By
  //     default applies to control program for system (e.g., VAV program
  //     run_command).
  //   command (point_type): The signal given to make an action happen.
  //     Defaults to multistate unless used alongside a measurement_type.
  //
  // Ontology field: `schedule_run_command`, point type `command`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional OffOnUnknownValue schedule_run_command = 152 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "schedule_run_command"
      subfields: "schedule"
      subfields: "run"
      subfields: "command"
      point_type: "command"
    }
  ];

  // The suction pressure sensor.
  //
  // Composed of:
  //   suction (descriptor): The production of a partial vacuum in order to
  //     force fluid into a vacant space (such as refrigerant in a
  //     compressor).
  //   pressure (measurement): Measured force per unit area within a system
  //     or process.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `suction_pressure_sensor`, point type `sensor`. Measured
  // as pressure; values are in pascals, the ontology's standard unit for
  // that quantity.
  //
  // Expected range -34473.78945 to 344737.8945. This is a data-quality
  // expectation, not a constraint: a reading outside it is a fault to
  // report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double suction_pressure_sensor = 153 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "suction_pressure_sensor"
      subfields: "suction"
      subfields: "pressure"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "pressure"
      standard_unit: "pascals"
      expected_min: -34473.78945
      expected_max: 344737.8945
    }
  ];

  // The supply water isolation valve command.
  //
  // Composed of:
  //   supply (descriptor): Measurement or process of media as it is
  //     supplied to the end-use equipment within the system.
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   isolation (descriptor): Process of isolating one component or
  //     process from another.
  //   valve (component): Component which meters the flow of water within a
  //     system or device.
  //   command (point_type): The signal given to make an action happen.
  //     Defaults to multistate unless used alongside a measurement_type.
  //
  // Ontology field: `supply_water_isolation_valve_command`, point type
  // `command`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ClosedOpenValue supply_water_isolation_valve_command = 154 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "supply_water_isolation_valve_command"
      subfields: "supply"
      subfields: "water"
      subfields: "isolation"
      subfields: "valve"
      subfields: "command"
      point_type: "command"
    }
  ];

  // The supply water isolation valve percentage sensor.
  //
  // Composed of:
  //   supply (descriptor): Measurement or process of media as it is
  //     supplied to the end-use equipment within the system.
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   isolation (descriptor): Process of isolating one component or
  //     process from another.
  //   valve (component): Component which meters the flow of water within a
  //     system or device.
  //   percentage (measurement): Measurement per hundred.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `supply_water_isolation_valve_percentage_sensor`, point
  // type `sensor`. Measured as percentage; values are in percent, the
  // ontology's standard unit for that quantity.
  //
  // Valid range 0 to 100, enforced by protovalidate.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double supply_water_isolation_valve_percentage_sensor = 155 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "supply_water_isolation_valve_percentage_sensor"
      subfields: "supply"
      subfields: "water"
      subfields: "isolation"
      subfields: "valve"
      subfields: "percentage"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "percentage"
      standard_unit: "percent"
    },
    (buf.validate.field).double.gte = 0.0,
    (buf.validate.field).double.lte = 100.0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The supply water isolation valve status.
  //
  // Composed of:
  //   supply (descriptor): Measurement or process of media as it is
  //     supplied to the end-use equipment within the system.
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   isolation (descriptor): Process of isolating one component or
  //     process from another.
  //   valve (component): Component which meters the flow of water within a
  //     system or device.
  //   status (point_type): The multistate value indicating an observed
  //     state in a piece of equipment, often indicating if a command was
  //     effected. It is a neutral observation (i.e., no quality judgment of
  //     'good' or 'bad'). It also has no units of measurement; therefore, if
  //     combined with a measurement subfield, it will indicate that the
  //     field is the directional status based on some measurement of that
  //     type (e.g., power_status equates to an on/off value based on some
  //     inference of power).
  //
  // Ontology field: `supply_water_isolation_valve_status`, point type
  // `status`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ClosedOpenValue supply_water_isolation_valve_status = 156 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "supply_water_isolation_valve_status"
      subfields: "supply"
      subfields: "water"
      subfields: "isolation"
      subfields: "valve"
      subfields: "status"
      point_type: "status"
    }
  ];

  // The supply water temperature sensor.
  //
  // Composed of:
  //   supply (descriptor): Measurement or process of media as it is
  //     supplied to the end-use equipment within the system.
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `supply_water_temperature_sensor`, point type `sensor`.
  // Measured as temperature; values are in kelvin, the ontology's standard
  // unit for that quantity.
  //
  // Expected range 255.9259259 to 380.3703704. This is a data-quality
  // expectation, not a constraint: a reading outside it is a fault to
  // report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double supply_water_temperature_sensor = 157 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "supply_water_temperature_sensor"
      subfields: "supply"
      subfields: "water"
      subfields: "temperature"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "temperature"
      standard_unit: "kelvin"
      expected_min: 255.925926
      expected_max: 380.37037
    }
  ];

  // The supply water temperature setpoint.
  //
  // Composed of:
  //   supply (descriptor): Measurement or process of media as it is
  //     supplied to the end-use equipment within the system.
  //   water (descriptor): Water in liquid form, conditioned or
  //     unconditioned.
  //   temperature (measurement): Measures the temperature of media within
  //     a process or system.
  //   setpoint (point_type): Control target of process or system.
  //
  // Ontology field: `supply_water_temperature_setpoint`, point type
  // `setpoint`. Measured as temperature; values are in kelvin, the
  // ontology's standard unit for that quantity.
  //
  // Expected range 255.9259259 to 380.3703704. This is a data-quality
  // expectation, not a constraint: a reading outside it is a fault to
  // report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double supply_water_temperature_setpoint = 158 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "supply_water_temperature_setpoint"
      subfields: "supply"
      subfields: "water"
      subfields: "temperature"
      subfields: "setpoint"
      point_type: "setpoint"
      measurement: "temperature"
      standard_unit: "kelvin"
      expected_min: 255.925926
      expected_max: 380.37037
    }
  ];

  // The switch command.
  //
  // Composed of:
  //   switch (component): Component to disrupt an electrical cirucit.
  //   command (point_type): The signal given to make an action happen.
  //     Defaults to multistate unless used alongside a measurement_type.
  //
  // Ontology field: `switch_command`, point type `command`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional OffOnValue switch_command = 159 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "switch_command"
      subfields: "switch"
      subfields: "command"
      point_type: "command"
    }
  ];

  // The user occupancy override status.
  //
  // Composed of:
  //   user (descriptor): A person who uses or operates something.
  //   occupancy (descriptor): State of being occupied.
  //   override (descriptor): An action that interrupts, cancels, or
  //     changes the current action or status.
  //   status (point_type): The multistate value indicating an observed
  //     state in a piece of equipment, often indicating if a command was
  //     effected. It is a neutral observation (i.e., no quality judgment of
  //     'good' or 'bad'). It also has no units of measurement; therefore, if
  //     combined with a measurement subfield, it will indicate that the
  //     field is the directional status based on some measurement of that
  //     type (e.g., power_status equates to an on/off value based on some
  //     inference of power).
  //
  // Ontology field: `user_occupancy_override_status`, point type `status`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional DisabledEnabledOccupiedUnoccupiedValue user_occupancy_override_status = 160 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "user_occupancy_override_status"
      subfields: "user"
      subfields: "occupancy"
      subfields: "override"
      subfields: "status"
      point_type: "status"
    }
  ];

  // The voltage sensor.
  //
  // Composed of:
  //   voltage (measurement): Electrical potential difference between two
  //     reference points.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `voltage_sensor`, point type `sensor`. Measured as
  // voltage; values are in volts, the ontology's standard unit for that
  // quantity.
  //
  // Expected range 0 to 1000. This is a data-quality expectation, not a
  // constraint: a reading outside it is a fault to report, not a message to
  // reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double voltage_sensor = 161 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "voltage_sensor"
      subfields: "voltage"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "voltage"
      standard_unit: "volts"
      expected_min: 0.0
      expected_max: 1000.0
    }
  ];

  // The warmup request count.
  //
  // Composed of:
  //   warmup (descriptor): Process, measured effect or requirement for
  //     heating; used following building unoccupied periods and in optimal
  //     start applications.
  //   request (descriptor): A signal from a system device that calls for
  //     more of a particular conditioning process (e.g., pressure requests
  //     from terminal units).
  //   count (point_type): Total count of actions or requests.
  //
  // Ontology field: `warmup_request_count`, point type `count`.
  // Dimensionless: the ontology defines this point type as an integer count
  // rather than a measured quantity.
  //
  // Valid range from 0, enforced by protovalidate. Expected range up to 500.
  // This is a data-quality expectation, not a constraint: a reading outside
  // it is a fault to report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional int64 warmup_request_count = 162 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "warmup_request_count"
      subfields: "warmup"
      subfields: "request"
      subfields: "count"
      point_type: "count"
      expected_max: 500.0
    },
    (buf.validate.field).int64.gte = 0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];
}
