// Copyright 2026 RegaliumOS™.
// SPDX-License-Identifier: Apache-2.0
//
// Generated by sync from Google's Digital Buildings ontology,
// revision 2026-09-09 (4a794ac).
// Portions Copyright 2020 Google LLC, licensed under Apache-2.0.
// DO NOT EDIT.
//
// Regenerate with: just sync
// Revision history: docs/spec.md

syntax = "proto3";

package protobuf.digitalbuildings.facilities.corridor.v1;

import "google/api/annotations.proto";
import "google/api/client.proto";
import "google/protobuf/empty.proto";
import "protobuf/digitalbuildings/facilities/corridor/v1/corridor.proto";
import "protobuf/digitalbuildings/facilities/corridor/v1/messages.proto";

option java_multiple_files = true;
option java_outer_classname = "ServiceProto";
option java_package = "com.regalium.protobuf.digitalbuildings.facilities.corridor.v1";

// Manages corridors.
//
// AIP-131 names the service for the collection, not `CorridorService`: that
// is buf STANDARD's convention and the two contradict, which is why buf.yaml
// selects BASIC and lets api-linter own API design (rule 1).
service Corridors {
  option (google.api.default_host) = "digitalbuildings.googleapis.com";

  // Returns one corridor.
  rpc GetCorridor(GetCorridorRequest) returns (Corridor) {
    option (google.api.http) = {get: "/v1/{name=buildings/*/corridors/*}"};
    option (google.api.method_signature) = "name";
  }

  // Lists the corridors in a building.
  rpc ListCorridors(ListCorridorsRequest) returns (ListCorridorsResponse) {
    option (google.api.http) = {get: "/v1/{parent=buildings/*}/corridors"};
    option (google.api.method_signature) = "parent";
  }

  // Creates a corridor.
  rpc CreateCorridor(CreateCorridorRequest) returns (Corridor) {
    option (google.api.http) = {
      post: "/v1/{parent=buildings/*}/corridors"
      body: "corridor"
    };
    option (google.api.method_signature) = "parent,corridor,corridor_id";
  }

  // Updates a corridor.
  //
  // Rule 8: an update_mask naming an OUTPUT_ONLY field is rejected rather
  // than silently ignored. Most fields here are OUTPUT_ONLY -- the equipment
  // reports them and no API call sets them.
  rpc UpdateCorridor(UpdateCorridorRequest) returns (Corridor) {
    option (google.api.http) = {
      patch: "/v1/{corridor.name=buildings/*/corridors/*}"
      body: "corridor"
    };
    option (google.api.method_signature) = "corridor,update_mask";
  }

  // Deletes a corridor.
  rpc DeleteCorridor(DeleteCorridorRequest) returns (google.protobuf.Empty) {
    option (google.api.http) = {delete: "/v1/{name=buildings/*/corridors/*}"};
    option (google.api.method_signature) = "name";
  }

  // Restores a deleted corridor (AIP-164 <https://aip.dev/164>).
  rpc UndeleteCorridor(UndeleteCorridorRequest) returns (Corridor) {
    option (google.api.http) = {
      post: "/v1/{name=buildings/*/corridors/*}:undelete"
      body: "*"
    };
    option (google.api.method_signature) = "name";
  }
}
