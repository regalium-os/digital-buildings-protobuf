// Copyright 2026 RegaliumOS™.
// SPDX-License-Identifier: Apache-2.0
//
// Generated by sync from Google's Digital Buildings ontology,
// revision 2026-09-09 (4a794ac).
// Portions Copyright 2020 Google LLC, licensed under Apache-2.0.
// DO NOT EDIT.
//
// Regenerate with: just sync
// Revision history: docs/spec.md

syntax = "proto3";

package protobuf.digitalbuildings.electrical.uninterruptible_power_supply.v1;

import "buf/validate/validate.proto";
import "google/api/field_behavior.proto";
import "google/api/field_info.proto";
import "google/api/resource.proto";
import "protobuf/digitalbuildings/annotations/v1/annotations.proto";
import "protobuf/digitalbuildings/electrical/uninterruptible_power_supply/v1/absent_present_value.proto";
import "protobuf/digitalbuildings/electrical/uninterruptible_power_supply/v1/active_inactive_value.proto";
import "protobuf/digitalbuildings/electrical/uninterruptible_power_supply/v1/asynchronous_synchronous_value.proto";
import "protobuf/digitalbuildings/electrical/uninterruptible_power_supply/v1/battery_normal_off_value.proto";
import "protobuf/digitalbuildings/electrical/uninterruptible_power_supply/v1/battery_off_on_softstart_value.proto";
import "protobuf/digitalbuildings/electrical/uninterruptible_power_supply/v1/bypass_inactive_value.proto";
import "protobuf/digitalbuildings/electrical/uninterruptible_power_supply/v1/connection.proto";
import "protobuf/digitalbuildings/electrical/uninterruptible_power_supply/v1/field_translation.proto";
import "protobuf/digitalbuildings/electrical/uninterruptible_power_supply/v1/link.proto";
import "protobuf/digitalbuildings/electrical/uninterruptible_power_supply/v1/normal_reversed_value.proto";
import "protobuf/digitalbuildings/electrical/uninterruptible_power_supply/v1/off_on_standby_unknown_value.proto";
import "protobuf/digitalbuildings/electrical/uninterruptible_power_supply/v1/off_on_value.proto";
import "protobuf/digitalbuildings/electrical/uninterruptible_power_supply/v1/uninterruptible_power_supply_type.proto";

option java_multiple_files = true;
option java_outer_classname = "UninterruptiblePowerSupplyProto";
option java_package = "com.regalium.protobuf.digitalbuildings.electrical.uninterruptible_power_supply.v1";

// Tag for all uninterruptible power supply (UPS) units. A UPS provides power
// in the event of grid power failure, protecting critical equipment from
// damage.
//
// Ontology entity type `ELECTRICAL/UPS`, a general type carrying 7 canonical
// variant(s). Rule 7: the general type is the resource and each variant is a
// value of UninterruptiblePowerSupplyType, so this message holds the union
// of every field any variant uses -- all of them optional.
//
// Reference: Digital Buildings abstract model.
// https://github.com/google/digitalbuildings/blob/master/ontology/docs/model.md
message UninterruptiblePowerSupply {
  option (google.api.resource) = {
    type: "digitalbuildings.googleapis.com/UninterruptiblePowerSupply"
    pattern: "buildings/{building}/uninterruptiblePowerSupplies/{uninterruptible_power_supply}"
    singular: "uninterruptiblePowerSupply"
    plural: "uninterruptiblePowerSupplies"
  };
  option (protobuf.digitalbuildings.annotations.v1.entity_type) = {
    guid: "53a1903c-7b32-47da-a7ef-94b047a1aa50"
    namespace: "ELECTRICAL"
    general_type: "UPS"
  };

  // Resource name,
  // "buildings/{building}/uninterruptiblePowerSupplies/{uninterruptible_power_supply}".
  // Assigned by the server.
  string name = 1 [
    (google.api.field_behavior) = IDENTIFIER,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The ontology's GUID for this entity.
  //
  // Rule 10: this is the ontology's own identifier, not a second one minted
  // beside it. Digital Buildings assigns a stable UUID to every entity and
  // it survives renames, which is exactly what AIP-148 asks a `uid` to be.
  string uid = 2 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (google.api.field_info).format = UUID4,
    (buf.validate.field).string.uuid = true,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The building config's human-readable entity code, e.g. `FCU-123`.
  //
  // Locally unique rather than globally, chosen at design time, and it must
  // survive a round trip unchanged or every re-import duplicates the entity.
  // The old building-config format keys entities by this; the new one keys
  // them by `uid`. Both are representable because both exist.
  string code = 3 [
    (google.api.field_behavior) = OPTIONAL,
    (google.api.field_behavior) = IMMUTABLE,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The canonical type this entity conforms to.
  //
  // The variant's exact field sets ride on the enum value in
  // (annotations.canonical_type), so a validator can reject an entity
  // reporting a field its declared type does not have.
  UninterruptiblePowerSupplyType entity_type = 4 [(google.api.field_behavior) = OPTIONAL];

  // Directed relationships to other entities.
  //
  // Rule 4: a connection is not a field. AIP-215 forbids referencing a
  // message in another proto package, and the other end of a connection may
  // be any resource in any namespace -- so it is carried as a resource name
  // with a wildcard reference.
  repeated Connection connections = 5 [(google.api.field_behavior) = OPTIONAL];

  // Standard fields mapped from another entity's telemetry.
  //
  // The building config's `links` block: a virtual entity reports data
  // linked from reporting devices rather than emitting it itself.
  repeated Link links = 6 [(google.api.field_behavior) = OPTIONAL];

  // How this entity's native payload maps onto the standard fields, when it
  // reports telemetry directly.
  repeated FieldTranslation translations = 7 [(google.api.field_behavior) = OPTIONAL];

  // Held for the fields on every read path (rule 9).
  reserved 8 to 15;

  // The apparent power sensor.
  //
  // Composed of:
  //   apparent (measurement_descriptor): The combination of reactive and
  //     real components (power).
  //   power (measurement): Rate of energy consumption (assumed context is
  //     electrical and real, unless otherwise modified).
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `apparent_power_sensor`, point type `sensor`. Measured
  // as power; values are in watts, the ontology's standard unit for that
  // quantity.
  //
  // Valid range from 0, enforced by protovalidate. Expected range up to
  // 1e+07. This is a data-quality expectation, not a constraint: a reading
  // outside it is a fault to report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double apparent_power_sensor = 16 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "apparent_power_sensor"
      subfields: "apparent"
      subfields: "power"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "power"
      standard_unit: "watts"
      expected_max: 10000000.0
    },
    (buf.validate.field).double.gte = 0.0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The battery charge percentage sensor.
  //
  // Composed of:
  //   battery (component): A device used to store power for use at a later
  //     time (e.g., in an emergency where grid power is unavailable).
  //   charge (measurement_descriptor): The state of power storage (e.g.,
  //     charged, not charged, discharged)
  //   percentage (measurement): Measurement per hundred.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `battery_charge_percentage_sensor`, point type `sensor`.
  // Measured as percentage; values are in percent, the ontology's standard
  // unit for that quantity.
  //
  // Valid range 0 to 100, enforced by protovalidate.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double battery_charge_percentage_sensor = 17 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "battery_charge_percentage_sensor"
      subfields: "battery"
      subfields: "charge"
      subfields: "percentage"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "percentage"
      standard_unit: "percent"
    },
    (buf.validate.field).double.gte = 0.0,
    (buf.validate.field).double.lte = 100.0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The battery percentage sensor.
  //
  // Composed of:
  //   battery (component): A device used to store power for use at a later
  //     time (e.g., in an emergency where grid power is unavailable).
  //   percentage (measurement): Measurement per hundred.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `battery_percentage_sensor`, point type `sensor`.
  // Measured as percentage; values are in percent, the ontology's standard
  // unit for that quantity.
  //
  // Valid range 0 to 100, enforced by protovalidate.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double battery_percentage_sensor = 18 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "battery_percentage_sensor"
      subfields: "battery"
      subfields: "percentage"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "percentage"
      standard_unit: "percent"
    },
    (buf.validate.field).double.gte = 0.0,
    (buf.validate.field).double.lte = 100.0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The battery status.
  //
  // Composed of:
  //   battery (component): A device used to store power for use at a later
  //     time (e.g., in an emergency where grid power is unavailable).
  //   status (point_type): The multistate value indicating an observed
  //     state in a piece of equipment, often indicating if a command was
  //     effected. It is a neutral observation (i.e., no quality judgment of
  //     'good' or 'bad'). It also has no units of measurement; therefore, if
  //     combined with a measurement subfield, it will indicate that the
  //     field is the directional status based on some measurement of that
  //     type (e.g., power_status equates to an on/off value based on some
  //     inference of power).
  //
  // Ontology field: `battery_status`, point type `status`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue battery_status = 19 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "battery_status"
      subfields: "battery"
      subfields: "status"
      point_type: "status"
    }
  ];

  // The battery test status.
  //
  // Composed of:
  //   battery (component): A device used to store power for use at a later
  //     time (e.g., in an emergency where grid power is unavailable).
  //   test (measurement_descriptor): A procedure intended to establish the
  //     quality, performance, or reliability of something, especially before
  //     it is taken into widespread use.
  //   status (point_type): The multistate value indicating an observed
  //     state in a piece of equipment, often indicating if a command was
  //     effected. It is a neutral observation (i.e., no quality judgment of
  //     'good' or 'bad'). It also has no units of measurement; therefore, if
  //     combined with a measurement subfield, it will indicate that the
  //     field is the directional status based on some measurement of that
  //     type (e.g., power_status equates to an on/off value based on some
  //     inference of power).
  //
  // Ontology field: `battery_test_status`, point type `status`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue battery_test_status = 20 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "battery_test_status"
      subfields: "battery"
      subfields: "test"
      subfields: "status"
      point_type: "status"
    }
  ];

  // The battery voltage sensor.
  //
  // Composed of:
  //   battery (component): A device used to store power for use at a later
  //     time (e.g., in an emergency where grid power is unavailable).
  //   voltage (measurement): Electrical potential difference between two
  //     reference points.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `battery_voltage_sensor`, point type `sensor`. Measured
  // as voltage; values are in volts, the ontology's standard unit for that
  // quantity.
  //
  // Expected range 0 to 15. This is a data-quality expectation, not a
  // constraint: a reading outside it is a fault to report, not a message to
  // reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double battery_voltage_sensor = 21 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "battery_voltage_sensor"
      subfields: "battery"
      subfields: "voltage"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "voltage"
      standard_unit: "volts"
      expected_min: 0.0
      expected_max: 15.0
    }
  ];

  // The bypass frequency sensor.
  //
  // Composed of:
  //   bypass (descriptor): Route which fluid takes to bypass a process.
  //   frequency (measurement): The rate of cycling within a process.
  //     Typically used to describe voltage (and amperage) oscillation within
  //     AC power distribution components.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `bypass_frequency_sensor`, point type `sensor`. Measured
  // as frequency; values are in hertz, the ontology's standard unit for that
  // quantity.
  //
  // Expected range -0.5 to 65. This is a data-quality expectation, not a
  // constraint: a reading outside it is a fault to report, not a message to
  // reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double bypass_frequency_sensor = 22 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "bypass_frequency_sensor"
      subfields: "bypass"
      subfields: "frequency"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "frequency"
      standard_unit: "hertz"
      expected_min: -0.5
      expected_max: 65.0
    }
  ];

  // The bypass phase1 neutral voltage sensor.
  //
  // Composed of:
  //   bypass (descriptor): Route which fluid takes to bypass a process.
  //   phase1 (descriptor): The first (nominally A) phase of three-phase
  //     power distribution systems.
  //   neutral (descriptor): Line in the electrical cirucit which carries
  //     current back to the source. Distinct from ground.
  //   voltage (measurement): Electrical potential difference between two
  //     reference points.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `bypass_phase1_neutral_voltage_sensor`, point type
  // `sensor`. Measured as voltage; values are in volts, the ontology's
  // standard unit for that quantity.
  //
  // Expected range 0 to 500. This is a data-quality expectation, not a
  // constraint: a reading outside it is a fault to report, not a message to
  // reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double bypass_phase1_neutral_voltage_sensor = 104 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "bypass_phase1_neutral_voltage_sensor"
      subfields: "bypass"
      subfields: "phase1"
      subfields: "neutral"
      subfields: "voltage"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "voltage"
      standard_unit: "volts"
      expected_min: 0.0
      expected_max: 500.0
    }
  ];

  // The bypass phase2 neutral voltage sensor.
  //
  // Composed of:
  //   bypass (descriptor): Route which fluid takes to bypass a process.
  //   phase2 (descriptor): The second (nominally B) phase of three-phase
  //     power distribution systems.
  //   neutral (descriptor): Line in the electrical cirucit which carries
  //     current back to the source. Distinct from ground.
  //   voltage (measurement): Electrical potential difference between two
  //     reference points.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `bypass_phase2_neutral_voltage_sensor`, point type
  // `sensor`. Measured as voltage; values are in volts, the ontology's
  // standard unit for that quantity.
  //
  // Expected range 0 to 500. This is a data-quality expectation, not a
  // constraint: a reading outside it is a fault to report, not a message to
  // reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double bypass_phase2_neutral_voltage_sensor = 105 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "bypass_phase2_neutral_voltage_sensor"
      subfields: "bypass"
      subfields: "phase2"
      subfields: "neutral"
      subfields: "voltage"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "voltage"
      standard_unit: "volts"
      expected_min: 0.0
      expected_max: 500.0
    }
  ];

  // The bypass phase3 neutral voltage sensor.
  //
  // Composed of:
  //   bypass (descriptor): Route which fluid takes to bypass a process.
  //   phase3 (descriptor): The third (nominally C) phase of three-phase
  //     power distribution systems.
  //   neutral (descriptor): Line in the electrical cirucit which carries
  //     current back to the source. Distinct from ground.
  //   voltage (measurement): Electrical potential difference between two
  //     reference points.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `bypass_phase3_neutral_voltage_sensor`, point type
  // `sensor`. Measured as voltage; values are in volts, the ontology's
  // standard unit for that quantity.
  //
  // Expected range 0 to 500. This is a data-quality expectation, not a
  // constraint: a reading outside it is a fault to report, not a message to
  // reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double bypass_phase3_neutral_voltage_sensor = 106 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "bypass_phase3_neutral_voltage_sensor"
      subfields: "bypass"
      subfields: "phase3"
      subfields: "neutral"
      subfields: "voltage"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "voltage"
      standard_unit: "volts"
      expected_min: 0.0
      expected_max: 500.0
    }
  ];

  // The bypass phase status.
  //
  // Composed of:
  //   bypass (descriptor): Route which fluid takes to bypass a process.
  //   phase (descriptor): A phase of three-phase power distribution
  //     systems.
  //   status (point_type): The multistate value indicating an observed
  //     state in a piece of equipment, often indicating if a command was
  //     effected. It is a neutral observation (i.e., no quality judgment of
  //     'good' or 'bad'). It also has no units of measurement; therefore, if
  //     combined with a measurement subfield, it will indicate that the
  //     field is the directional status based on some measurement of that
  //     type (e.g., power_status equates to an on/off value based on some
  //     inference of power).
  //
  // Ontology field: `bypass_phase_status`, point type `status`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional NormalReversedValue bypass_phase_status = 26 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "bypass_phase_status"
      subfields: "bypass"
      subfields: "phase"
      subfields: "status"
      point_type: "status"
    }
  ];

  // The bypass status.
  //
  // Composed of:
  //   bypass (descriptor): Route which fluid takes to bypass a process.
  //   status (point_type): The multistate value indicating an observed
  //     state in a piece of equipment, often indicating if a command was
  //     effected. It is a neutral observation (i.e., no quality judgment of
  //     'good' or 'bad'). It also has no units of measurement; therefore, if
  //     combined with a measurement subfield, it will indicate that the
  //     field is the directional status based on some measurement of that
  //     type (e.g., power_status equates to an on/off value based on some
  //     inference of power).
  //
  // Ontology field: `bypass_status`, point type `status`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional BypassInactiveValue bypass_status = 27 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "bypass_status"
      subfields: "bypass"
      subfields: "status"
      point_type: "status"
    }
  ];

  // The energy accumulator.
  //
  // Composed of:
  //   energy (measurement): The quantitative property that must be
  //     transferred to an object to perform work (default electrical unless
  //     otherwise modified).
  //   accumulator (point_type): The total accumulated quantity (e.g.,
  //     total energy accumulated).
  //
  // Ontology field: `energy_accumulator`, point type `accumulator`. Measured
  // as energy; values are in joules, the ontology's standard unit for that
  // quantity.
  //
  // Valid range from 0, enforced by protovalidate. Expected range up to
  // 3.6e+19. This is a data-quality expectation, not a constraint: a reading
  // outside it is a fault to report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double energy_accumulator = 28 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "energy_accumulator"
      subfields: "energy"
      subfields: "accumulator"
      point_type: "accumulator"
      measurement: "energy"
      standard_unit: "joules"
      expected_max: 36000000000000000000.0
    },
    (buf.validate.field).double.gte = 0.0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The failed alarm.
  //
  // Composed of:
  //   failed (descriptor): Indicates that a point is not operating as
  //     intended or is incapable of operating.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `failed_alarm`, point type `alarm`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue failed_alarm = 29 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "failed_alarm"
      subfields: "failed"
      subfields: "alarm"
      point_type: "alarm"
    }
  ];

  // The failed exhaust fan alarm.
  //
  // Composed of:
  //   failed (descriptor): Indicates that a point is not operating as
  //     intended or is incapable of operating.
  //   exhaust (descriptor): Process or component used in the removal of
  //     air from a conditioned system to the outside atmosphere.
  //   fan (component): Component used for the distribution of air.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `failed_exhaust_fan_alarm`, point type `alarm`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue failed_exhaust_fan_alarm = 30 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "failed_exhaust_fan_alarm"
      subfields: "failed"
      subfields: "exhaust"
      subfields: "fan"
      subfields: "alarm"
      point_type: "alarm"
    }
  ];

  // The failed inverter alarm.
  //
  // Composed of:
  //   failed (descriptor): Indicates that a point is not operating as
  //     intended or is incapable of operating.
  //   inverter (component): Device or circuitry that changes direct
  //     current to alternating current (e.g., variable frequency drive).
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `failed_inverter_alarm`, point type `alarm`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue failed_inverter_alarm = 31 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "failed_inverter_alarm"
      subfields: "failed"
      subfields: "inverter"
      subfields: "alarm"
      point_type: "alarm"
    }
  ];

  // The input frequency sensor.
  //
  // Composed of:
  //   input (descriptor): The input to a system.
  //   frequency (measurement): The rate of cycling within a process.
  //     Typically used to describe voltage (and amperage) oscillation within
  //     AC power distribution components.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `input_frequency_sensor`, point type `sensor`. Measured
  // as frequency; values are in hertz, the ontology's standard unit for that
  // quantity.
  //
  // Expected range -0.5 to 65. This is a data-quality expectation, not a
  // constraint: a reading outside it is a fault to report, not a message to
  // reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double input_frequency_sensor = 32 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "input_frequency_sensor"
      subfields: "input"
      subfields: "frequency"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "frequency"
      standard_unit: "hertz"
      expected_min: -0.5
      expected_max: 65.0
    }
  ];

  // The input phase1 current sensor.
  //
  // Composed of:
  //   input (descriptor): The input to a system.
  //   phase1 (descriptor): The first (nominally A) phase of three-phase
  //     power distribution systems.
  //   current (measurement): Flow of electric charge.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `input_phase1_current_sensor`, point type `sensor`.
  // Measured as current; values are in amperes, the ontology's standard unit
  // for that quantity.
  //
  // Valid range from 0, enforced by protovalidate. Expected range up to 500.
  // This is a data-quality expectation, not a constraint: a reading outside
  // it is a fault to report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double input_phase1_current_sensor = 107 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "input_phase1_current_sensor"
      subfields: "input"
      subfields: "phase1"
      subfields: "current"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "current"
      standard_unit: "amperes"
      expected_max: 500.0
    },
    (buf.validate.field).double.gte = 0.0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The input phase1 neutral voltage sensor.
  //
  // Composed of:
  //   input (descriptor): The input to a system.
  //   phase1 (descriptor): The first (nominally A) phase of three-phase
  //     power distribution systems.
  //   neutral (descriptor): Line in the electrical cirucit which carries
  //     current back to the source. Distinct from ground.
  //   voltage (measurement): Electrical potential difference between two
  //     reference points.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `input_phase1_neutral_voltage_sensor`, point type
  // `sensor`. Measured as voltage; values are in volts, the ontology's
  // standard unit for that quantity.
  //
  // Expected range 0 to 500. This is a data-quality expectation, not a
  // constraint: a reading outside it is a fault to report, not a message to
  // reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double input_phase1_neutral_voltage_sensor = 108 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "input_phase1_neutral_voltage_sensor"
      subfields: "input"
      subfields: "phase1"
      subfields: "neutral"
      subfields: "voltage"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "voltage"
      standard_unit: "volts"
      expected_min: 0.0
      expected_max: 500.0
    }
  ];

  // The input phase1 phase2 voltage sensor.
  //
  // Composed of:
  //   input (descriptor): The input to a system.
  //   phase1 (descriptor): The first (nominally A) phase of three-phase
  //     power distribution systems.
  //   phase2 (descriptor): The second (nominally B) phase of three-phase
  //     power distribution systems.
  //   voltage (measurement): Electrical potential difference between two
  //     reference points.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `input_phase1_phase2_voltage_sensor`, point type
  // `sensor`. Measured as voltage; values are in volts, the ontology's
  // standard unit for that quantity.
  //
  // Expected range 0 to 500. This is a data-quality expectation, not a
  // constraint: a reading outside it is a fault to report, not a message to
  // reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double input_phase1_phase2_voltage_sensor = 109 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "input_phase1_phase2_voltage_sensor"
      subfields: "input"
      subfields: "phase1"
      subfields: "phase2"
      subfields: "voltage"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "voltage"
      standard_unit: "volts"
      expected_min: 0.0
      expected_max: 500.0
    }
  ];

  // The input phase2 current sensor.
  //
  // Composed of:
  //   input (descriptor): The input to a system.
  //   phase2 (descriptor): The second (nominally B) phase of three-phase
  //     power distribution systems.
  //   current (measurement): Flow of electric charge.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `input_phase2_current_sensor`, point type `sensor`.
  // Measured as current; values are in amperes, the ontology's standard unit
  // for that quantity.
  //
  // Valid range from 0, enforced by protovalidate. Expected range up to 500.
  // This is a data-quality expectation, not a constraint: a reading outside
  // it is a fault to report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double input_phase2_current_sensor = 110 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "input_phase2_current_sensor"
      subfields: "input"
      subfields: "phase2"
      subfields: "current"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "current"
      standard_unit: "amperes"
      expected_max: 500.0
    },
    (buf.validate.field).double.gte = 0.0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The input phase2 neutral voltage sensor.
  //
  // Composed of:
  //   input (descriptor): The input to a system.
  //   phase2 (descriptor): The second (nominally B) phase of three-phase
  //     power distribution systems.
  //   neutral (descriptor): Line in the electrical cirucit which carries
  //     current back to the source. Distinct from ground.
  //   voltage (measurement): Electrical potential difference between two
  //     reference points.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `input_phase2_neutral_voltage_sensor`, point type
  // `sensor`. Measured as voltage; values are in volts, the ontology's
  // standard unit for that quantity.
  //
  // Expected range 0 to 500. This is a data-quality expectation, not a
  // constraint: a reading outside it is a fault to report, not a message to
  // reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double input_phase2_neutral_voltage_sensor = 111 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "input_phase2_neutral_voltage_sensor"
      subfields: "input"
      subfields: "phase2"
      subfields: "neutral"
      subfields: "voltage"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "voltage"
      standard_unit: "volts"
      expected_min: 0.0
      expected_max: 500.0
    }
  ];

  // The input phase2 phase3 voltage sensor.
  //
  // Composed of:
  //   input (descriptor): The input to a system.
  //   phase2 (descriptor): The second (nominally B) phase of three-phase
  //     power distribution systems.
  //   phase3 (descriptor): The third (nominally C) phase of three-phase
  //     power distribution systems.
  //   voltage (measurement): Electrical potential difference between two
  //     reference points.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `input_phase2_phase3_voltage_sensor`, point type
  // `sensor`. Measured as voltage; values are in volts, the ontology's
  // standard unit for that quantity.
  //
  // Expected range 0 to 500. This is a data-quality expectation, not a
  // constraint: a reading outside it is a fault to report, not a message to
  // reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double input_phase2_phase3_voltage_sensor = 112 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "input_phase2_phase3_voltage_sensor"
      subfields: "input"
      subfields: "phase2"
      subfields: "phase3"
      subfields: "voltage"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "voltage"
      standard_unit: "volts"
      expected_min: 0.0
      expected_max: 500.0
    }
  ];

  // The input phase3 current sensor.
  //
  // Composed of:
  //   input (descriptor): The input to a system.
  //   phase3 (descriptor): The third (nominally C) phase of three-phase
  //     power distribution systems.
  //   current (measurement): Flow of electric charge.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `input_phase3_current_sensor`, point type `sensor`.
  // Measured as current; values are in amperes, the ontology's standard unit
  // for that quantity.
  //
  // Valid range from 0, enforced by protovalidate. Expected range up to 500.
  // This is a data-quality expectation, not a constraint: a reading outside
  // it is a fault to report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double input_phase3_current_sensor = 113 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "input_phase3_current_sensor"
      subfields: "input"
      subfields: "phase3"
      subfields: "current"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "current"
      standard_unit: "amperes"
      expected_max: 500.0
    },
    (buf.validate.field).double.gte = 0.0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The input phase3 neutral voltage sensor.
  //
  // Composed of:
  //   input (descriptor): The input to a system.
  //   phase3 (descriptor): The third (nominally C) phase of three-phase
  //     power distribution systems.
  //   neutral (descriptor): Line in the electrical cirucit which carries
  //     current back to the source. Distinct from ground.
  //   voltage (measurement): Electrical potential difference between two
  //     reference points.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `input_phase3_neutral_voltage_sensor`, point type
  // `sensor`. Measured as voltage; values are in volts, the ontology's
  // standard unit for that quantity.
  //
  // Expected range 0 to 500. This is a data-quality expectation, not a
  // constraint: a reading outside it is a fault to report, not a message to
  // reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double input_phase3_neutral_voltage_sensor = 114 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "input_phase3_neutral_voltage_sensor"
      subfields: "input"
      subfields: "phase3"
      subfields: "neutral"
      subfields: "voltage"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "voltage"
      standard_unit: "volts"
      expected_min: 0.0
      expected_max: 500.0
    }
  ];

  // The input phase3 phase1 voltage sensor.
  //
  // Composed of:
  //   input (descriptor): The input to a system.
  //   phase3 (descriptor): The third (nominally C) phase of three-phase
  //     power distribution systems.
  //   phase1 (descriptor): The first (nominally A) phase of three-phase
  //     power distribution systems.
  //   voltage (measurement): Electrical potential difference between two
  //     reference points.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `input_phase3_phase1_voltage_sensor`, point type
  // `sensor`. Measured as voltage; values are in volts, the ontology's
  // standard unit for that quantity.
  //
  // Expected range 0 to 500. This is a data-quality expectation, not a
  // constraint: a reading outside it is a fault to report, not a message to
  // reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double input_phase3_phase1_voltage_sensor = 115 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "input_phase3_phase1_voltage_sensor"
      subfields: "input"
      subfields: "phase3"
      subfields: "phase1"
      subfields: "voltage"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "voltage"
      standard_unit: "volts"
      expected_min: 0.0
      expected_max: 500.0
    }
  ];

  // The input supply mode.
  //
  // Composed of:
  //   input (descriptor): The input to a system.
  //   supply (descriptor): Measurement or process of media as it is
  //     supplied to the end-use equipment within the system.
  //   mode (point_type): Distinct mode of operation within system. For
  //     example economizer_mode has modes of 'enabled' or 'disabled.'
  //
  // Ontology field: `input_supply_mode`, point type `mode`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional BatteryNormalOffValue input_supply_mode = 42 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "input_supply_mode"
      subfields: "input"
      subfields: "supply"
      subfields: "mode"
      point_type: "mode"
    }
  ];

  // The inverter mode.
  //
  // Composed of:
  //   inverter (component): Device or circuitry that changes direct
  //     current to alternating current (e.g., variable frequency drive).
  //   mode (point_type): Distinct mode of operation within system. For
  //     example economizer_mode has modes of 'enabled' or 'disabled.'
  //
  // Ontology field: `inverter_mode`, point type `mode`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional AsynchronousSynchronousValue inverter_mode = 43 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "inverter_mode"
      subfields: "inverter"
      subfields: "mode"
      point_type: "mode"
    }
  ];

  // The inverter status.
  //
  // Composed of:
  //   inverter (component): Device or circuitry that changes direct
  //     current to alternating current (e.g., variable frequency drive).
  //   status (point_type): The multistate value indicating an observed
  //     state in a piece of equipment, often indicating if a command was
  //     effected. It is a neutral observation (i.e., no quality judgment of
  //     'good' or 'bad'). It also has no units of measurement; therefore, if
  //     combined with a measurement subfield, it will indicate that the
  //     field is the directional status based on some measurement of that
  //     type (e.g., power_status equates to an on/off value based on some
  //     inference of power).
  //
  // Ontology field: `inverter_status`, point type `status`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional OffOnValue inverter_status = 44 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "inverter_status"
      subfields: "inverter"
      subfields: "status"
      point_type: "status"
    }
  ];

  // The line frequency sensor.
  //
  // Composed of:
  //   line (descriptor): Refers to the incoming electrical feed (e.g.,
  //     line current).
  //   frequency (measurement): The rate of cycling within a process.
  //     Typically used to describe voltage (and amperage) oscillation within
  //     AC power distribution components.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `line_frequency_sensor`, point type `sensor`. Measured
  // as frequency; values are in hertz, the ontology's standard unit for that
  // quantity.
  //
  // Expected range -0.5 to 65. This is a data-quality expectation, not a
  // constraint: a reading outside it is a fault to report, not a message to
  // reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double line_frequency_sensor = 45 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "line_frequency_sensor"
      subfields: "line"
      subfields: "frequency"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "frequency"
      standard_unit: "hertz"
      expected_min: -0.5
      expected_max: 65.0
    }
  ];

  // The load percentage sensor.
  //
  // Composed of:
  //   load (descriptor): Electrical load which indicates the consumption
  //     of electrical power.
  //   percentage (measurement): Measurement per hundred.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `load_percentage_sensor`, point type `sensor`. Measured
  // as percentage; values are in percent, the ontology's standard unit for
  // that quantity.
  //
  // Valid range 0 to 100, enforced by protovalidate.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double load_percentage_sensor = 46 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "load_percentage_sensor"
      subfields: "load"
      subfields: "percentage"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "percentage"
      standard_unit: "percent"
    },
    (buf.validate.field).double.gte = 0.0,
    (buf.validate.field).double.lte = 100.0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The lost power alarm.
  //
  // Composed of:
  //   lost (measurement_descriptor): Indicates that a point has lost some
  //     quality or quantity inadvertently.
  //   power (measurement): Rate of energy consumption (assumed context is
  //     electrical and real, unless otherwise modified).
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `lost_power_alarm`, point type `alarm`. Measured as
  // power; values are in watts, the ontology's standard unit for that
  // quantity.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue lost_power_alarm = 47 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "lost_power_alarm"
      subfields: "lost"
      subfields: "power"
      subfields: "alarm"
      point_type: "alarm"
      measurement: "power"
      standard_unit: "watts"
    }
  ];

  // The low battery charge alarm.
  //
  // Composed of:
  //   low (descriptor): Level of control or measurement; below medium and
  //     high; opposite of high.
  //   battery (component): A device used to store power for use at a later
  //     time (e.g., in an emergency where grid power is unavailable).
  //   charge (measurement_descriptor): The state of power storage (e.g.,
  //     charged, not charged, discharged)
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `low_battery_charge_alarm`, point type `alarm`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue low_battery_charge_alarm = 48 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "low_battery_charge_alarm"
      subfields: "low"
      subfields: "battery"
      subfields: "charge"
      subfields: "alarm"
      point_type: "alarm"
    }
  ];

  // The manufacturer label.
  //
  // Composed of:
  //   manufacturer (descriptor): The company which produces the asset or
  //     device.
  //   label (point_type): Identifying alias for component or system.
  //
  // Ontology field: `manufacturer_label`, point type `label`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional string manufacturer_label = 49 [
    (google.api.field_behavior) = OPTIONAL,
    (google.api.field_behavior) = IMMUTABLE,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "manufacturer_label"
      subfields: "manufacturer"
      subfields: "label"
      point_type: "label"
    }
  ];

  // The model label.
  //
  // Composed of:
  //   model (descriptor): Particular design version of an asset (e.g.
  //     model_label).
  //   label (point_type): Identifying alias for component or system.
  //
  // Ontology field: `model_label`, point type `label`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional string model_label = 50 [
    (google.api.field_behavior) = OPTIONAL,
    (google.api.field_behavior) = IMMUTABLE,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "model_label"
      subfields: "model"
      subfields: "label"
      point_type: "label"
    }
  ];

  // The output apparent power sensor.
  //
  // Composed of:
  //   output (descriptor): The output of a control loop (e.g. PID loop
  //     output).
  //   apparent (measurement_descriptor): The combination of reactive and
  //     real components (power).
  //   power (measurement): Rate of energy consumption (assumed context is
  //     electrical and real, unless otherwise modified).
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `output_apparent_power_sensor`, point type `sensor`.
  // Measured as power; values are in watts, the ontology's standard unit for
  // that quantity.
  //
  // Expected range 0 to 1e+07. This is a data-quality expectation, not a
  // constraint: a reading outside it is a fault to report, not a message to
  // reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double output_apparent_power_sensor = 51 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "output_apparent_power_sensor"
      subfields: "output"
      subfields: "apparent"
      subfields: "power"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "power"
      standard_unit: "watts"
      expected_min: 0.0
      expected_max: 10000000.0
    }
  ];

  // The output frequency sensor.
  //
  // Composed of:
  //   output (descriptor): The output of a control loop (e.g. PID loop
  //     output).
  //   frequency (measurement): The rate of cycling within a process.
  //     Typically used to describe voltage (and amperage) oscillation within
  //     AC power distribution components.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `output_frequency_sensor`, point type `sensor`. Measured
  // as frequency; values are in hertz, the ontology's standard unit for that
  // quantity.
  //
  // Expected range -0.5 to 65. This is a data-quality expectation, not a
  // constraint: a reading outside it is a fault to report, not a message to
  // reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double output_frequency_sensor = 52 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "output_frequency_sensor"
      subfields: "output"
      subfields: "frequency"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "frequency"
      standard_unit: "hertz"
      expected_min: -0.5
      expected_max: 65.0
    }
  ];

  // The output phase1 current sensor.
  //
  // Composed of:
  //   output (descriptor): The output of a control loop (e.g. PID loop
  //     output).
  //   phase1 (descriptor): The first (nominally A) phase of three-phase
  //     power distribution systems.
  //   current (measurement): Flow of electric charge.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `output_phase1_current_sensor`, point type `sensor`.
  // Measured as current; values are in amperes, the ontology's standard unit
  // for that quantity.
  //
  // Valid range from 0, enforced by protovalidate. Expected range up to 500.
  // This is a data-quality expectation, not a constraint: a reading outside
  // it is a fault to report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double output_phase1_current_sensor = 116 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "output_phase1_current_sensor"
      subfields: "output"
      subfields: "phase1"
      subfields: "current"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "current"
      standard_unit: "amperes"
      expected_max: 500.0
    },
    (buf.validate.field).double.gte = 0.0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The output phase1 neutral voltage sensor.
  //
  // Composed of:
  //   output (descriptor): The output of a control loop (e.g. PID loop
  //     output).
  //   phase1 (descriptor): The first (nominally A) phase of three-phase
  //     power distribution systems.
  //   neutral (descriptor): Line in the electrical cirucit which carries
  //     current back to the source. Distinct from ground.
  //   voltage (measurement): Electrical potential difference between two
  //     reference points.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `output_phase1_neutral_voltage_sensor`, point type
  // `sensor`. Measured as voltage; values are in volts, the ontology's
  // standard unit for that quantity.
  //
  // Expected range 0 to 500. This is a data-quality expectation, not a
  // constraint: a reading outside it is a fault to report, not a message to
  // reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double output_phase1_neutral_voltage_sensor = 117 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "output_phase1_neutral_voltage_sensor"
      subfields: "output"
      subfields: "phase1"
      subfields: "neutral"
      subfields: "voltage"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "voltage"
      standard_unit: "volts"
      expected_min: 0.0
      expected_max: 500.0
    }
  ];

  // The output phase1 phase2 voltage sensor.
  //
  // Composed of:
  //   output (descriptor): The output of a control loop (e.g. PID loop
  //     output).
  //   phase1 (descriptor): The first (nominally A) phase of three-phase
  //     power distribution systems.
  //   phase2 (descriptor): The second (nominally B) phase of three-phase
  //     power distribution systems.
  //   voltage (measurement): Electrical potential difference between two
  //     reference points.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `output_phase1_phase2_voltage_sensor`, point type
  // `sensor`. Measured as voltage; values are in volts, the ontology's
  // standard unit for that quantity.
  //
  // Expected range 0 to 500. This is a data-quality expectation, not a
  // constraint: a reading outside it is a fault to report, not a message to
  // reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double output_phase1_phase2_voltage_sensor = 118 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "output_phase1_phase2_voltage_sensor"
      subfields: "output"
      subfields: "phase1"
      subfields: "phase2"
      subfields: "voltage"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "voltage"
      standard_unit: "volts"
      expected_min: 0.0
      expected_max: 500.0
    }
  ];

  // The output phase2 current sensor.
  //
  // Composed of:
  //   output (descriptor): The output of a control loop (e.g. PID loop
  //     output).
  //   phase2 (descriptor): The second (nominally B) phase of three-phase
  //     power distribution systems.
  //   current (measurement): Flow of electric charge.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `output_phase2_current_sensor`, point type `sensor`.
  // Measured as current; values are in amperes, the ontology's standard unit
  // for that quantity.
  //
  // Valid range from 0, enforced by protovalidate. Expected range up to 500.
  // This is a data-quality expectation, not a constraint: a reading outside
  // it is a fault to report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double output_phase2_current_sensor = 119 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "output_phase2_current_sensor"
      subfields: "output"
      subfields: "phase2"
      subfields: "current"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "current"
      standard_unit: "amperes"
      expected_max: 500.0
    },
    (buf.validate.field).double.gte = 0.0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The output phase2 neutral voltage sensor.
  //
  // Composed of:
  //   output (descriptor): The output of a control loop (e.g. PID loop
  //     output).
  //   phase2 (descriptor): The second (nominally B) phase of three-phase
  //     power distribution systems.
  //   neutral (descriptor): Line in the electrical cirucit which carries
  //     current back to the source. Distinct from ground.
  //   voltage (measurement): Electrical potential difference between two
  //     reference points.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `output_phase2_neutral_voltage_sensor`, point type
  // `sensor`. Measured as voltage; values are in volts, the ontology's
  // standard unit for that quantity.
  //
  // Expected range 0 to 500. This is a data-quality expectation, not a
  // constraint: a reading outside it is a fault to report, not a message to
  // reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double output_phase2_neutral_voltage_sensor = 120 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "output_phase2_neutral_voltage_sensor"
      subfields: "output"
      subfields: "phase2"
      subfields: "neutral"
      subfields: "voltage"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "voltage"
      standard_unit: "volts"
      expected_min: 0.0
      expected_max: 500.0
    }
  ];

  // The output phase2 phase3 voltage sensor.
  //
  // Composed of:
  //   output (descriptor): The output of a control loop (e.g. PID loop
  //     output).
  //   phase2 (descriptor): The second (nominally B) phase of three-phase
  //     power distribution systems.
  //   phase3 (descriptor): The third (nominally C) phase of three-phase
  //     power distribution systems.
  //   voltage (measurement): Electrical potential difference between two
  //     reference points.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `output_phase2_phase3_voltage_sensor`, point type
  // `sensor`. Measured as voltage; values are in volts, the ontology's
  // standard unit for that quantity.
  //
  // Expected range 0 to 500. This is a data-quality expectation, not a
  // constraint: a reading outside it is a fault to report, not a message to
  // reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double output_phase2_phase3_voltage_sensor = 121 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "output_phase2_phase3_voltage_sensor"
      subfields: "output"
      subfields: "phase2"
      subfields: "phase3"
      subfields: "voltage"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "voltage"
      standard_unit: "volts"
      expected_min: 0.0
      expected_max: 500.0
    }
  ];

  // The output phase3 current sensor.
  //
  // Composed of:
  //   output (descriptor): The output of a control loop (e.g. PID loop
  //     output).
  //   phase3 (descriptor): The third (nominally C) phase of three-phase
  //     power distribution systems.
  //   current (measurement): Flow of electric charge.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `output_phase3_current_sensor`, point type `sensor`.
  // Measured as current; values are in amperes, the ontology's standard unit
  // for that quantity.
  //
  // Valid range from 0, enforced by protovalidate. Expected range up to 500.
  // This is a data-quality expectation, not a constraint: a reading outside
  // it is a fault to report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double output_phase3_current_sensor = 122 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "output_phase3_current_sensor"
      subfields: "output"
      subfields: "phase3"
      subfields: "current"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "current"
      standard_unit: "amperes"
      expected_max: 500.0
    },
    (buf.validate.field).double.gte = 0.0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The output phase3 neutral voltage sensor.
  //
  // Composed of:
  //   output (descriptor): The output of a control loop (e.g. PID loop
  //     output).
  //   phase3 (descriptor): The third (nominally C) phase of three-phase
  //     power distribution systems.
  //   neutral (descriptor): Line in the electrical cirucit which carries
  //     current back to the source. Distinct from ground.
  //   voltage (measurement): Electrical potential difference between two
  //     reference points.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `output_phase3_neutral_voltage_sensor`, point type
  // `sensor`. Measured as voltage; values are in volts, the ontology's
  // standard unit for that quantity.
  //
  // Expected range 0 to 500. This is a data-quality expectation, not a
  // constraint: a reading outside it is a fault to report, not a message to
  // reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double output_phase3_neutral_voltage_sensor = 123 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "output_phase3_neutral_voltage_sensor"
      subfields: "output"
      subfields: "phase3"
      subfields: "neutral"
      subfields: "voltage"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "voltage"
      standard_unit: "volts"
      expected_min: 0.0
      expected_max: 500.0
    }
  ];

  // The output phase3 phase1 voltage sensor.
  //
  // Composed of:
  //   output (descriptor): The output of a control loop (e.g. PID loop
  //     output).
  //   phase3 (descriptor): The third (nominally C) phase of three-phase
  //     power distribution systems.
  //   phase1 (descriptor): The first (nominally A) phase of three-phase
  //     power distribution systems.
  //   voltage (measurement): Electrical potential difference between two
  //     reference points.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `output_phase3_phase1_voltage_sensor`, point type
  // `sensor`. Measured as voltage; values are in volts, the ontology's
  // standard unit for that quantity.
  //
  // Expected range 0 to 500. This is a data-quality expectation, not a
  // constraint: a reading outside it is a fault to report, not a message to
  // reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double output_phase3_phase1_voltage_sensor = 124 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "output_phase3_phase1_voltage_sensor"
      subfields: "output"
      subfields: "phase3"
      subfields: "phase1"
      subfields: "voltage"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "voltage"
      standard_unit: "volts"
      expected_min: 0.0
      expected_max: 500.0
    }
  ];

  // The output power sensor.
  //
  // Composed of:
  //   output (descriptor): The output of a control loop (e.g. PID loop
  //     output).
  //   power (measurement): Rate of energy consumption (assumed context is
  //     electrical and real, unless otherwise modified).
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `output_power_sensor`, point type `sensor`. Measured as
  // power; values are in watts, the ontology's standard unit for that
  // quantity.
  //
  // Expected range 0 to 1e+07. This is a data-quality expectation, not a
  // constraint: a reading outside it is a fault to report, not a message to
  // reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double output_power_sensor = 62 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "output_power_sensor"
      subfields: "output"
      subfields: "power"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "power"
      standard_unit: "watts"
      expected_min: 0.0
      expected_max: 10000000.0
    }
  ];

  // The overload alarm.
  //
  // Composed of:
  //   overload (descriptor): An electrical load that exceeds available
  //     electrical power.
  //   alarm (point_type): A point that interprets some input values
  //     qualitatively (e.g., as good or bad, normal or in alarm, etc.).
  //     Alarms are always binary.
  //
  // Ontology field: `overload_alarm`, point type `alarm`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional ActiveInactiveValue overload_alarm = 63 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "overload_alarm"
      subfields: "overload"
      subfields: "alarm"
      point_type: "alarm"
    }
  ];

  // The phase1 apparent power sensor.
  //
  // Composed of:
  //   phase1 (descriptor): The first (nominally A) phase of three-phase
  //     power distribution systems.
  //   apparent (measurement_descriptor): The combination of reactive and
  //     real components (power).
  //   power (measurement): Rate of energy consumption (assumed context is
  //     electrical and real, unless otherwise modified).
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `phase1_apparent_power_sensor`, point type `sensor`.
  // Measured as power; values are in watts, the ontology's standard unit for
  // that quantity.
  //
  // Expected range 0 to 1e+07. This is a data-quality expectation, not a
  // constraint: a reading outside it is a fault to report, not a message to
  // reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double phase1_apparent_power_sensor = 125 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "phase1_apparent_power_sensor"
      subfields: "phase1"
      subfields: "apparent"
      subfields: "power"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "power"
      standard_unit: "watts"
      expected_min: 0.0
      expected_max: 10000000.0
    }
  ];

  // The phase1 power sensor.
  //
  // Composed of:
  //   phase1 (descriptor): The first (nominally A) phase of three-phase
  //     power distribution systems.
  //   power (measurement): Rate of energy consumption (assumed context is
  //     electrical and real, unless otherwise modified).
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `phase1_power_sensor`, point type `sensor`. Measured as
  // power; values are in watts, the ontology's standard unit for that
  // quantity.
  //
  // Expected range 0 to 1e+07. This is a data-quality expectation, not a
  // constraint: a reading outside it is a fault to report, not a message to
  // reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double phase1_power_sensor = 126 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "phase1_power_sensor"
      subfields: "phase1"
      subfields: "power"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "power"
      standard_unit: "watts"
      expected_min: 0.0
      expected_max: 10000000.0
    }
  ];

  // The phase2 apparent power sensor.
  //
  // Composed of:
  //   phase2 (descriptor): The second (nominally B) phase of three-phase
  //     power distribution systems.
  //   apparent (measurement_descriptor): The combination of reactive and
  //     real components (power).
  //   power (measurement): Rate of energy consumption (assumed context is
  //     electrical and real, unless otherwise modified).
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `phase2_apparent_power_sensor`, point type `sensor`.
  // Measured as power; values are in watts, the ontology's standard unit for
  // that quantity.
  //
  // Expected range 0 to 1e+07. This is a data-quality expectation, not a
  // constraint: a reading outside it is a fault to report, not a message to
  // reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double phase2_apparent_power_sensor = 127 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "phase2_apparent_power_sensor"
      subfields: "phase2"
      subfields: "apparent"
      subfields: "power"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "power"
      standard_unit: "watts"
      expected_min: 0.0
      expected_max: 10000000.0
    }
  ];

  // The phase2 power sensor.
  //
  // Composed of:
  //   phase2 (descriptor): The second (nominally B) phase of three-phase
  //     power distribution systems.
  //   power (measurement): Rate of energy consumption (assumed context is
  //     electrical and real, unless otherwise modified).
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `phase2_power_sensor`, point type `sensor`. Measured as
  // power; values are in watts, the ontology's standard unit for that
  // quantity.
  //
  // Expected range 0 to 1e+07. This is a data-quality expectation, not a
  // constraint: a reading outside it is a fault to report, not a message to
  // reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double phase2_power_sensor = 128 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "phase2_power_sensor"
      subfields: "phase2"
      subfields: "power"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "power"
      standard_unit: "watts"
      expected_min: 0.0
      expected_max: 10000000.0
    }
  ];

  // The phase3 apparent power sensor.
  //
  // Composed of:
  //   phase3 (descriptor): The third (nominally C) phase of three-phase
  //     power distribution systems.
  //   apparent (measurement_descriptor): The combination of reactive and
  //     real components (power).
  //   power (measurement): Rate of energy consumption (assumed context is
  //     electrical and real, unless otherwise modified).
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `phase3_apparent_power_sensor`, point type `sensor`.
  // Measured as power; values are in watts, the ontology's standard unit for
  // that quantity.
  //
  // Expected range 0 to 1e+07. This is a data-quality expectation, not a
  // constraint: a reading outside it is a fault to report, not a message to
  // reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double phase3_apparent_power_sensor = 129 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "phase3_apparent_power_sensor"
      subfields: "phase3"
      subfields: "apparent"
      subfields: "power"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "power"
      standard_unit: "watts"
      expected_min: 0.0
      expected_max: 10000000.0
    }
  ];

  // The phase3 power sensor.
  //
  // Composed of:
  //   phase3 (descriptor): The third (nominally C) phase of three-phase
  //     power distribution systems.
  //   power (measurement): Rate of energy consumption (assumed context is
  //     electrical and real, unless otherwise modified).
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `phase3_power_sensor`, point type `sensor`. Measured as
  // power; values are in watts, the ontology's standard unit for that
  // quantity.
  //
  // Expected range 0 to 1e+07. This is a data-quality expectation, not a
  // constraint: a reading outside it is a fault to report, not a message to
  // reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double phase3_power_sensor = 130 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "phase3_power_sensor"
      subfields: "phase3"
      subfields: "power"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "power"
      standard_unit: "watts"
      expected_min: 0.0
      expected_max: 10000000.0
    }
  ];

  // The power sensor.
  //
  // Composed of:
  //   power (measurement): Rate of energy consumption (assumed context is
  //     electrical and real, unless otherwise modified).
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `power_sensor`, point type `sensor`. Measured as power;
  // values are in watts, the ontology's standard unit for that quantity.
  //
  // Expected range 0 to 1e+07. This is a data-quality expectation, not a
  // constraint: a reading outside it is a fault to report, not a message to
  // reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double power_sensor = 70 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "power_sensor"
      subfields: "power"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "power"
      standard_unit: "watts"
      expected_min: 0.0
      expected_max: 10000000.0
    }
  ];

  // The power status.
  //
  // Composed of:
  //   power (measurement): Rate of energy consumption (assumed context is
  //     electrical and real, unless otherwise modified).
  //   status (point_type): The multistate value indicating an observed
  //     state in a piece of equipment, often indicating if a command was
  //     effected. It is a neutral observation (i.e., no quality judgment of
  //     'good' or 'bad'). It also has no units of measurement; therefore, if
  //     combined with a measurement subfield, it will indicate that the
  //     field is the directional status based on some measurement of that
  //     type (e.g., power_status equates to an on/off value based on some
  //     inference of power).
  //
  // Ontology field: `power_status`, point type `status`. Measured as power;
  // values are in watts, the ontology's standard unit for that quantity.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional AbsentPresentValue power_status = 71 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "power_status"
      subfields: "power"
      subfields: "status"
      point_type: "status"
      measurement: "power"
      standard_unit: "watts"
    }
  ];

  // The powerfactor sensor.
  //
  // Composed of:
  //   powerfactor (measurement): Ratio of real and apparent power.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `powerfactor_sensor`, point type `sensor`. Measured as
  // powerfactor; values are in no_units, the ontology's standard unit for
  // that quantity.
  //
  // Valid range -1 to 1, enforced by protovalidate.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double powerfactor_sensor = 72 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "powerfactor_sensor"
      subfields: "powerfactor"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "powerfactor"
      standard_unit: "no_units"
    },
    (buf.validate.field).double.gte = -1.0,
    (buf.validate.field).double.lte = 1.0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The rectifier mode.
  //
  // Composed of:
  //   rectifier (component): Component that converts alternating current
  //     to direct current.
  //   mode (point_type): Distinct mode of operation within system. For
  //     example economizer_mode has modes of 'enabled' or 'disabled.'
  //
  // Ontology field: `rectifier_mode`, point type `mode`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional BatteryOffOnSoftstartValue rectifier_mode = 73 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "rectifier_mode"
      subfields: "rectifier"
      subfields: "mode"
      point_type: "mode"
    }
  ];

  // The remaining charge time sensor.
  //
  // Composed of:
  //   remaining (descriptor): The amount of something that is left at some
  //     point in a process (e.g., the remaining charge a battery has left).
  //   charge (measurement_descriptor): The state of power storage (e.g.,
  //     charged, not charged, discharged)
  //   time (measurement): Defined duration of something.
  //   sensor (point_type): Component used to measure some quality of a
  //     system or process. In some instances, it can be feedback for an
  //     analog command (e.g., speed_percentage_sensor is the feedback from
  //     speed_percentage_command).
  //
  // Ontology field: `remaining_charge_time_sensor`, point type `sensor`.
  // Measured as time; values are in seconds, the ontology's standard unit
  // for that quantity.
  //
  // Valid range from 0, enforced by protovalidate. Expected range up to
  // 100000. This is a data-quality expectation, not a constraint: a reading
  // outside it is a fault to report, not a message to reject.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional double remaining_charge_time_sensor = 74 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "remaining_charge_time_sensor"
      subfields: "remaining"
      subfields: "charge"
      subfields: "time"
      subfields: "sensor"
      point_type: "sensor"
      measurement: "time"
      standard_unit: "seconds"
      expected_max: 100000.0
    },
    (buf.validate.field).double.gte = 0.0,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // The run command.
  //
  // Composed of:
  //   run (measurement_descriptor): State of being active or operating. By
  //     default applies to control program for system (e.g., VAV program
  //     run_command).
  //   command (point_type): The signal given to make an action happen.
  //     Defaults to multistate unless used alongside a measurement_type.
  //
  // Ontology field: `run_command`, point type `command`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional OffOnValue run_command = 75 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "run_command"
      subfields: "run"
      subfields: "command"
      point_type: "command"
    }
  ];

  // The run status.
  //
  // Composed of:
  //   run (measurement_descriptor): State of being active or operating. By
  //     default applies to control program for system (e.g., VAV program
  //     run_command).
  //   status (point_type): The multistate value indicating an observed
  //     state in a piece of equipment, often indicating if a command was
  //     effected. It is a neutral observation (i.e., no quality judgment of
  //     'good' or 'bad'). It also has no units of measurement; therefore, if
  //     combined with a measurement subfield, it will indicate that the
  //     field is the directional status based on some measurement of that
  //     type (e.g., power_status equates to an on/off value based on some
  //     inference of power).
  //
  // Ontology field: `run_status`, point type `status`.
  //
  // Reference: Digital Buildings ontology.
  // https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md
  optional OffOnStandbyUnknownValue run_status = 76 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.digitalbuildings.annotations.v1.field) = {
      literal: "run_status"
      subfields: "run"
      subfields: "status"
      point_type: "status"
    }
  ];

  // Numbers whose field the ontology no longer declares. Never reissued: a
  // consumer may still hold a message that used one.
  reserved 23, 24, 25, 33, 34, 35, 36, 37, 38, 39, 40, 41, 53, 54, 55, 56, 57, 58, 59, 60, 61, 64, 65, 66, 67, 68, 69, 77, 78, 79, 80, 81, 82, 83, 84, 85, 86, 87, 88, 89, 90, 91, 92, 93, 94, 95, 96, 97, 98, 99, 100, 101, 102, 103;
}
