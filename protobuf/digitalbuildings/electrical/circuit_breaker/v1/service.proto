// Copyright 2026 RegaliumOS™.
// SPDX-License-Identifier: Apache-2.0
//
// Generated by sync from Google's Digital Buildings ontology,
// revision 2026-09-09 (4a794ac).
// Portions Copyright 2020 Google LLC, licensed under Apache-2.0.
// DO NOT EDIT.
//
// Regenerate with: just sync
// Revision history: docs/spec.md

syntax = "proto3";

package protobuf.digitalbuildings.electrical.circuit_breaker.v1;

import "google/api/annotations.proto";
import "google/api/client.proto";
import "google/protobuf/empty.proto";
import "protobuf/digitalbuildings/electrical/circuit_breaker/v1/circuit_breaker.proto";
import "protobuf/digitalbuildings/electrical/circuit_breaker/v1/messages.proto";

option java_multiple_files = true;
option java_outer_classname = "ServiceProto";
option java_package = "com.regalium.protobuf.digitalbuildings.electrical.circuit_breaker.v1";

// Manages circuit breakers.
//
// AIP-131 names the service for the collection, not `CircuitBreakerService`:
// that is buf STANDARD's convention and the two contradict, which is why
// buf.yaml selects BASIC and lets api-linter own API design (rule 1).
service CircuitBreakers {
  option (google.api.default_host) = "digitalbuildings.googleapis.com";

  // Returns one circuit breaker.
  rpc GetCircuitBreaker(GetCircuitBreakerRequest) returns (CircuitBreaker) {
    option (google.api.http) = {get: "/v1/{name=buildings/*/circuitBreakers/*}"};
    option (google.api.method_signature) = "name";
  }

  // Lists the circuit breakers in a building.
  rpc ListCircuitBreakers(ListCircuitBreakersRequest) returns (ListCircuitBreakersResponse) {
    option (google.api.http) = {get: "/v1/{parent=buildings/*}/circuitBreakers"};
    option (google.api.method_signature) = "parent";
  }

  // Creates a circuit breaker.
  rpc CreateCircuitBreaker(CreateCircuitBreakerRequest) returns (CircuitBreaker) {
    option (google.api.http) = {
      post: "/v1/{parent=buildings/*}/circuitBreakers"
      body: "circuit_breaker"
    };
    option (google.api.method_signature) = "parent,circuit_breaker,circuit_breaker_id";
  }

  // Updates a circuit breaker.
  //
  // Rule 8: an update_mask naming an OUTPUT_ONLY field is rejected rather
  // than silently ignored. Most fields here are OUTPUT_ONLY -- the equipment
  // reports them and no API call sets them.
  rpc UpdateCircuitBreaker(UpdateCircuitBreakerRequest) returns (CircuitBreaker) {
    option (google.api.http) = {
      patch: "/v1/{circuit_breaker.name=buildings/*/circuitBreakers/*}"
      body: "circuit_breaker"
    };
    option (google.api.method_signature) = "circuit_breaker,update_mask";
  }

  // Deletes a circuit breaker.
  rpc DeleteCircuitBreaker(DeleteCircuitBreakerRequest) returns (google.protobuf.Empty) {
    option (google.api.http) = {delete: "/v1/{name=buildings/*/circuitBreakers/*}"};
    option (google.api.method_signature) = "name";
  }

  // Restores a deleted circuit breaker (AIP-164 <https://aip.dev/164>).
  rpc UndeleteCircuitBreaker(UndeleteCircuitBreakerRequest) returns (CircuitBreaker) {
    option (google.api.http) = {
      post: "/v1/{name=buildings/*/circuitBreakers/*}:undelete"
      body: "*"
    };
    option (google.api.method_signature) = "name";
  }
}
