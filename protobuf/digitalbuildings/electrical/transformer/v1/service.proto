// Copyright 2026 RegaliumOS™.
// SPDX-License-Identifier: Apache-2.0
//
// Generated by sync from Google's Digital Buildings ontology,
// revision 2026-09-09 (4a794ac).
// Portions Copyright 2020 Google LLC, licensed under Apache-2.0.
// DO NOT EDIT.
//
// Regenerate with: just sync
// Revision history: docs/spec.md

syntax = "proto3";

package protobuf.digitalbuildings.electrical.transformer.v1;

import "google/api/annotations.proto";
import "google/api/client.proto";
import "google/protobuf/empty.proto";
import "protobuf/digitalbuildings/electrical/transformer/v1/messages.proto";
import "protobuf/digitalbuildings/electrical/transformer/v1/transformer.proto";

option java_multiple_files = true;
option java_outer_classname = "ServiceProto";
option java_package = "com.regalium.protobuf.digitalbuildings.electrical.transformer.v1";

// Manages transformers.
//
// AIP-131 names the service for the collection, not `TransformerService`:
// that is buf STANDARD's convention and the two contradict, which is why
// buf.yaml selects BASIC and lets api-linter own API design (rule 1).
service Transformers {
  option (google.api.default_host) = "digitalbuildings.googleapis.com";

  // Returns one transformer.
  rpc GetTransformer(GetTransformerRequest) returns (Transformer) {
    option (google.api.http) = {get: "/v1/{name=buildings/*/transformers/*}"};
    option (google.api.method_signature) = "name";
  }

  // Lists the transformers in a building.
  rpc ListTransformers(ListTransformersRequest) returns (ListTransformersResponse) {
    option (google.api.http) = {get: "/v1/{parent=buildings/*}/transformers"};
    option (google.api.method_signature) = "parent";
  }

  // Creates a transformer.
  rpc CreateTransformer(CreateTransformerRequest) returns (Transformer) {
    option (google.api.http) = {
      post: "/v1/{parent=buildings/*}/transformers"
      body: "transformer"
    };
    option (google.api.method_signature) = "parent,transformer,transformer_id";
  }

  // Updates a transformer.
  //
  // Rule 8: an update_mask naming an OUTPUT_ONLY field is rejected rather
  // than silently ignored. Most fields here are OUTPUT_ONLY -- the equipment
  // reports them and no API call sets them.
  rpc UpdateTransformer(UpdateTransformerRequest) returns (Transformer) {
    option (google.api.http) = {
      patch: "/v1/{transformer.name=buildings/*/transformers/*}"
      body: "transformer"
    };
    option (google.api.method_signature) = "transformer,update_mask";
  }

  // Deletes a transformer.
  rpc DeleteTransformer(DeleteTransformerRequest) returns (google.protobuf.Empty) {
    option (google.api.http) = {delete: "/v1/{name=buildings/*/transformers/*}"};
    option (google.api.method_signature) = "name";
  }

  // Restores a deleted transformer (AIP-164 <https://aip.dev/164>).
  rpc UndeleteTransformer(UndeleteTransformerRequest) returns (Transformer) {
    option (google.api.http) = {
      post: "/v1/{name=buildings/*/transformers/*}:undelete"
      body: "*"
    };
    option (google.api.method_signature) = "name";
  }
}
