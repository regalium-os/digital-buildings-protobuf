<!--
Generated by `just docs` from Google's Digital Buildings ontology,
revision 2026-09-09 (4a794ac).
DO NOT EDIT -- edit sync/internal/docs and regenerate.
-->

# Transformer

Tag for transformers, which are devices that transfer electric energy from one alternating-current circuit to one or more other circuits by either increasing (stepping up) or reducing (stepping down) the voltage.

|   |   |
| --- | --- |
| Ontology type | `ELECTRICAL/TXMR` |
| GUID | `89a9ced0-3ee2-4d12-94b4-84e7179766e6` |
| Kind | equipment |
| Proto package | `protobuf.digitalbuildings.electrical.transformer.v1` |
| Resource name | `buildings/{building}/transformers/{transformer}` |
| Fields | 7 fields, 3 writable |
| Canonical types | 1 |

## Files

Rule 2: one message per file, plus its enums. `messages.proto` is the single exception, because a request and its response are meaningless apart.

| File | Contains |
| --- | --- |
| `transformer.proto` | the `Transformer` message |
| `transformer_type.proto` | `TransformerType`, one value per canonical type (rule 7) |
| `service.proto` | the `Transformers` service |
| `messages.proto` | its request and response messages |
| `connection.proto` | `Connection` -- how this entity relates to another (rule 4) |
| `link.proto` | `Link` -- this entity's fields mapped onto another's |
| `field_translation.proto` | `FieldTranslation`, `UnitMapping`, `StateMapping`, `ValueRange` |
| `absent_present_value.proto` | a multistate enum (rule 13) |
| `active_inactive_value.proto` | a multistate enum (rule 13) |
| `off_on_standby_unknown_value.proto` | a multistate enum (rule 13) |
| `off_on_value.proto` | a multistate enum (rule 13) |

## Service

`Transformers` in `service.proto`. Every resource is a collection (rule 7): there are no singletons here, so it gets the full standard method set plus AIP-164 `Undelete`.

AIP-131 names the service for the collection rather than `TransformerService`; rule 1 has why buf STANDARD is not selected.

| Method | HTTP |
| --- | --- |
| `GetTransformer` | `GET /v1/{name=buildings/*/transformers/*}` |
| `ListTransformers` | `GET /v1/{parent=buildings/*}/transformers` |
| `CreateTransformer` | `POST /v1/{parent=buildings/*}/transformers` |
| `UpdateTransformer` | `PATCH /v1/{transformer.name=buildings/*/transformers/*}` |
| `DeleteTransformer` | `DELETE /v1/{name=buildings/*/transformers/*}` |
| `UndeleteTransformer` | `POST /v1/{name=buildings/*/transformers/*}:undelete` |

## Fields

Every field is `optional` (rule 7): the resource carries the union of every field any of its canonical types uses, and no single device reports all of them.

**Writable** is rule 8. A `command` or a `setpoint` is `OPTIONAL` and can be set; everything else is `OUTPUT_ONLY`, because the equipment reports it and no API call sets it. An `update_mask` naming an `OUTPUT_ONLY` field is rejected rather than silently ignored.

| Field | Type | Writable | Unit / states | Ontology literal | Field no. |
| --- | --- | --- | --- | --- | --- |
| `failed_alarm` | `ActiveInactiveValue` | no | `ActiveInactiveValue` | `failed_alarm` | 16 |
| `lost_power_alarm` | `ActiveInactiveValue` | no | `ActiveInactiveValue` | `lost_power_alarm` | 17 |
| `manufacturer_label` | `string` | at create | -- | `manufacturer_label` | 18 |
| `model_label` | `string` | at create | -- | `model_label` | 19 |
| `power_status` | `AbsentPresentValue` | no | `AbsentPresentValue` | `power_status` | 20 |
| `run_command` | `OffOnValue` | yes | `OffOnValue` | `run_command` | 21 |
| `run_status` | `OffOnStandbyUnknownValue` | no | `OffOnStandbyUnknownValue` | `run_status` | 22 |

### Multistate enums

4 enums, one per distinct state set rather than one per field. Across the ontology 602 multistate fields draw on only 40 sets, so an enum per field would mean 305 different spellings of {ACTIVE, INACTIVE} that no consumer could pass to a common function (rule 13).

Each is copied into this package rather than imported: AIP-215 forbids a field referencing a type in another proto package (rule 4).

| File | Enum | States |
| --- | --- | --- |
| `absent_present_value.proto` | `AbsentPresentValue` | `ABSENT`, `PRESENT` |
| `active_inactive_value.proto` | `ActiveInactiveValue` | `ACTIVE`, `INACTIVE` |
| `off_on_standby_unknown_value.proto` | `OffOnStandbyUnknownValue` | `OFF`, `ON`, `STANDBY`, `UNKNOWN` |
| `off_on_value.proto` | `OffOnValue` | `OFF`, `ON` |

## Canonical types

1 canonical type, each a value of `TransformerType`. The ontology's name is kept verbatim, because that name *is* the identifier.

Each value carries its exact `implements` / `uses` / `opt_uses` sets in `(annotations.canonical_type)`, so a validator can still reject a variant reporting a field its declared type does not have. That is what makes the cut in rule 7 lossless.

| Enum value | Ontology type | Implements | Description |
| --- | --- | --- | --- |
| `TRANSFORMER_TYPE_TXMR_SS` | `ELECTRICAL/TXMR_SS` | `SS`, `TXMR` | Standard transformer. |

## Provenance

Generated from `ELECTRICAL/TXMR` in Google's Digital Buildings ontology.

- [Ontology concepts](https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md)
- [Abstract model](https://github.com/google/digitalbuildings/blob/master/ontology/docs/model.md)
- [Building configuration](https://github.com/google/digitalbuildings/blob/master/ontology/docs/building_config.md)
