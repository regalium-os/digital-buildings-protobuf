<!--
Generated by `just docs` from Google's Digital Buildings ontology,
revision 2026-09-09 (4a794ac).
DO NOT EDIT -- edit sync/internal/docs and regenerate.
-->

# HeatTracer

Tag for a heat tracer. A heat tracer is typically a heated coil that is wound around pipes to prevent the liquid inside from freezing.

|   |   |
| --- | --- |
| Ontology type | `SAFETY/EHT` |
| GUID | `700c69bb-8d37-4e5f-806f-ae64d007cf35` |
| Kind | equipment |
| Proto package | `protobuf.digitalbuildings.safety.heat_tracer.v1` |
| Resource name | `buildings/{building}/heatTracers/{heat_tracer}` |
| Fields | 4 fields, 3 writable |
| Canonical types | 1 |

## Files

Rule 2: one message per file, plus its enums. `messages.proto` is the single exception, because a request and its response are meaningless apart.

| File | Contains |
| --- | --- |
| `heat_tracer.proto` | the `HeatTracer` message |
| `heat_tracer_type.proto` | `HeatTracerType`, one value per canonical type (rule 7) |
| `service.proto` | the `HeatTracers` service |
| `messages.proto` | its request and response messages |
| `connection.proto` | `Connection` -- how this entity relates to another (rule 4) |
| `link.proto` | `Link` -- this entity's fields mapped onto another's |
| `field_translation.proto` | `FieldTranslation`, `UnitMapping`, `StateMapping`, `ValueRange` |
| `off_on_standby_unknown_value.proto` | a multistate enum (rule 13) |
| `off_on_value.proto` | a multistate enum (rule 13) |

## Service

`HeatTracers` in `service.proto`. Every resource is a collection (rule 7): there are no singletons here, so it gets the full standard method set plus AIP-164 `Undelete`.

AIP-131 names the service for the collection rather than `HeatTracerService`; rule 1 has why buf STANDARD is not selected.

| Method | HTTP |
| --- | --- |
| `GetHeatTracer` | `GET /v1/{name=buildings/*/heatTracers/*}` |
| `ListHeatTracers` | `GET /v1/{parent=buildings/*}/heatTracers` |
| `CreateHeatTracer` | `POST /v1/{parent=buildings/*}/heatTracers` |
| `UpdateHeatTracer` | `PATCH /v1/{heat_tracer.name=buildings/*/heatTracers/*}` |
| `DeleteHeatTracer` | `DELETE /v1/{name=buildings/*/heatTracers/*}` |
| `UndeleteHeatTracer` | `POST /v1/{name=buildings/*/heatTracers/*}:undelete` |

## Fields

Every field is `optional` (rule 7): the resource carries the union of every field any of its canonical types uses, and no single device reports all of them.

**Writable** is rule 8. A `command` or a `setpoint` is `OPTIONAL` and can be set; everything else is `OUTPUT_ONLY`, because the equipment reports it and no API call sets it. An `update_mask` naming an `OUTPUT_ONLY` field is rejected rather than silently ignored.

| Field | Type | Writable | Unit / states | Ontology literal | Field no. |
| --- | --- | --- | --- | --- | --- |
| `manufacturer_label` | `string` | at create | -- | `manufacturer_label` | 16 |
| `model_label` | `string` | at create | -- | `model_label` | 17 |
| `run_command` | `OffOnValue` | yes | `OffOnValue` | `run_command` | 18 |
| `run_status` | `OffOnStandbyUnknownValue` | no | `OffOnStandbyUnknownValue` | `run_status` | 19 |

### Multistate enums

2 enums, one per distinct state set rather than one per field. Across the ontology 602 multistate fields draw on only 40 sets, so an enum per field would mean 305 different spellings of {ACTIVE, INACTIVE} that no consumer could pass to a common function (rule 13).

Each is copied into this package rather than imported: AIP-215 forbids a field referencing a type in another proto package (rule 4).

| File | Enum | States |
| --- | --- | --- |
| `off_on_standby_unknown_value.proto` | `OffOnStandbyUnknownValue` | `OFF`, `ON`, `STANDBY`, `UNKNOWN` |
| `off_on_value.proto` | `OffOnValue` | `OFF`, `ON` |

## Canonical types

1 canonical type, each a value of `HeatTracerType`. The ontology's name is kept verbatim, because that name *is* the identifier.

Each value carries its exact `implements` / `uses` / `opt_uses` sets in `(annotations.canonical_type)`, so a validator can still reject a variant reporting a field its declared type does not have. That is what makes the cut in rule 7 lossless.

| Enum value | Ontology type | Implements | Description |
| --- | --- | --- | --- |
| `HEAT_TRACER_TYPE_EHT_SS` | `SAFETY/EHT_SS` | `SS`, `EHT` | Basic heat tracer with start/stop |

## Provenance

Generated from `SAFETY/EHT` in Google's Digital Buildings ontology.

- [Ontology concepts](https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md)
- [Abstract model](https://github.com/google/digitalbuildings/blob/master/ontology/docs/model.md)
- [Building configuration](https://github.com/google/digitalbuildings/blob/master/ontology/docs/building_config.md)
