// Copyright 2026 RegaliumOS™.
// SPDX-License-Identifier: Apache-2.0
//
// Generated by sync from Google's Digital Buildings ontology,
// revision 2026-09-09 (4a794ac).
// Portions Copyright 2020 Google LLC, licensed under Apache-2.0.
// DO NOT EDIT.
//
// Regenerate with: just sync
// Revision history: docs/spec.md

syntax = "proto3";

package protobuf.digitalbuildings.safety.heat_tracer.v1;

import "google/api/annotations.proto";
import "google/api/client.proto";
import "google/protobuf/empty.proto";
import "protobuf/digitalbuildings/safety/heat_tracer/v1/heat_tracer.proto";
import "protobuf/digitalbuildings/safety/heat_tracer/v1/messages.proto";

option java_multiple_files = true;
option java_outer_classname = "ServiceProto";
option java_package = "com.regalium.protobuf.digitalbuildings.safety.heat_tracer.v1";

// Manages heat tracers.
//
// AIP-131 names the service for the collection, not `HeatTracerService`:
// that is buf STANDARD's convention and the two contradict, which is why
// buf.yaml selects BASIC and lets api-linter own API design (rule 1).
service HeatTracers {
  option (google.api.default_host) = "digitalbuildings.googleapis.com";

  // Returns one heat tracer.
  rpc GetHeatTracer(GetHeatTracerRequest) returns (HeatTracer) {
    option (google.api.http) = {get: "/v1/{name=buildings/*/heatTracers/*}"};
    option (google.api.method_signature) = "name";
  }

  // Lists the heat tracers in a building.
  rpc ListHeatTracers(ListHeatTracersRequest) returns (ListHeatTracersResponse) {
    option (google.api.http) = {get: "/v1/{parent=buildings/*}/heatTracers"};
    option (google.api.method_signature) = "parent";
  }

  // Creates a heat tracer.
  rpc CreateHeatTracer(CreateHeatTracerRequest) returns (HeatTracer) {
    option (google.api.http) = {
      post: "/v1/{parent=buildings/*}/heatTracers"
      body: "heat_tracer"
    };
    option (google.api.method_signature) = "parent,heat_tracer,heat_tracer_id";
  }

  // Updates a heat tracer.
  //
  // Rule 8: an update_mask naming an OUTPUT_ONLY field is rejected rather
  // than silently ignored. Most fields here are OUTPUT_ONLY -- the equipment
  // reports them and no API call sets them.
  rpc UpdateHeatTracer(UpdateHeatTracerRequest) returns (HeatTracer) {
    option (google.api.http) = {
      patch: "/v1/{heat_tracer.name=buildings/*/heatTracers/*}"
      body: "heat_tracer"
    };
    option (google.api.method_signature) = "heat_tracer,update_mask";
  }

  // Deletes a heat tracer.
  rpc DeleteHeatTracer(DeleteHeatTracerRequest) returns (google.protobuf.Empty) {
    option (google.api.http) = {delete: "/v1/{name=buildings/*/heatTracers/*}"};
    option (google.api.method_signature) = "name";
  }

  // Restores a deleted heat tracer (AIP-164 <https://aip.dev/164>).
  rpc UndeleteHeatTracer(UndeleteHeatTracerRequest) returns (HeatTracer) {
    option (google.api.http) = {
      post: "/v1/{name=buildings/*/heatTracers/*}:undelete"
      body: "*"
    };
    option (google.api.method_signature) = "name";
  }
}
