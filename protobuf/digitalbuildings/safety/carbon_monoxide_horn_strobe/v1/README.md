<!--
Generated by `just docs` from Google's Digital Buildings ontology,
revision 2026-09-09 (4a794ac).
DO NOT EDIT -- edit sync/internal/docs and regenerate.
-->

# CarbonMonoxideHornStrobe

Carbon monoxide horn strobe.

|   |   |
| --- | --- |
| Ontology type | `SAFETY/COHS` |
| GUID | `e9fba3d0-a64a-413e-b225-e3b0b76c2cc7` |
| Kind | equipment |
| Proto package | `protobuf.digitalbuildings.safety.carbon_monoxide_horn_strobe.v1` |
| Resource name | `buildings/{building}/carbonMonoxideHornStrobes/{carbon_monoxide_horn_strobe}` |
| Fields | 4 fields, 2 writable |
| Canonical types | 1 |

## Files

Rule 2: one message per file, plus its enums. `messages.proto` is the single exception, because a request and its response are meaningless apart.

| File | Contains |
| --- | --- |
| `carbon_monoxide_horn_strobe.proto` | the `CarbonMonoxideHornStrobe` message |
| `carbon_monoxide_horn_strobe_type.proto` | `CarbonMonoxideHornStrobeType`, one value per canonical type (rule 7) |
| `service.proto` | the `CarbonMonoxideHornStrobes` service |
| `messages.proto` | its request and response messages |
| `connection.proto` | `Connection` -- how this entity relates to another (rule 4) |
| `link.proto` | `Link` -- this entity's fields mapped onto another's |
| `field_translation.proto` | `FieldTranslation`, `UnitMapping`, `StateMapping`, `ValueRange` |
| `off_on_value.proto` | a multistate enum (rule 13) |

## Service

`CarbonMonoxideHornStrobes` in `service.proto`. Every resource is a collection (rule 7): there are no singletons here, so it gets the full standard method set plus AIP-164 `Undelete`.

AIP-131 names the service for the collection rather than `CarbonMonoxideHornStrobeService`; rule 1 has why buf STANDARD is not selected.

| Method | HTTP |
| --- | --- |
| `GetCarbonMonoxideHornStrobe` | `GET /v1/{name=buildings/*/carbonMonoxideHornStrobes/*}` |
| `ListCarbonMonoxideHornStrobes` | `GET /v1/{parent=buildings/*}/carbonMonoxideHornStrobes` |
| `CreateCarbonMonoxideHornStrobe` | `POST /v1/{parent=buildings/*}/carbonMonoxideHornStrobes` |
| `UpdateCarbonMonoxideHornStrobe` | `PATCH /v1/{carbon_monoxide_horn_strobe.name=buildings/*/carbonMonoxideHornStrobes/*}` |
| `DeleteCarbonMonoxideHornStrobe` | `DELETE /v1/{name=buildings/*/carbonMonoxideHornStrobes/*}` |
| `UndeleteCarbonMonoxideHornStrobe` | `POST /v1/{name=buildings/*/carbonMonoxideHornStrobes/*}:undelete` |

## Fields

Every field is `optional` (rule 7): the resource carries the union of every field any of its canonical types uses, and no single device reports all of them.

**Writable** is rule 8. A `command` or a `setpoint` is `OPTIONAL` and can be set; everything else is `OUTPUT_ONLY`, because the equipment reports it and no API call sets it. An `update_mask` naming an `OUTPUT_ONLY` field is rejected rather than silently ignored.

| Field | Type | Writable | Unit / states | Ontology literal | Field no. |
| --- | --- | --- | --- | --- | --- |
| `horn_run_status` | `OffOnValue` | no | `OffOnValue` | `horn_run_status` | 16 |
| `manufacturer_label` | `string` | at create | -- | `manufacturer_label` | 17 |
| `model_label` | `string` | at create | -- | `model_label` | 18 |
| `strobe_run_status` | `OffOnValue` | no | `OffOnValue` | `strobe_run_status` | 19 |

### Multistate enums

1 enum, one per distinct state set rather than one per field. Across the ontology 602 multistate fields draw on only 40 sets, so an enum per field would mean 305 different spellings of {ACTIVE, INACTIVE} that no consumer could pass to a common function (rule 13).

Each is copied into this package rather than imported: AIP-215 forbids a field referencing a type in another proto package (rule 4).

| File | Enum | States |
| --- | --- | --- |
| `off_on_value.proto` | `OffOnValue` | `OFF`, `ON` |

## Canonical types

1 canonical type, each a value of `CarbonMonoxideHornStrobeType`. The ontology's name is kept verbatim, because that name *is* the identifier.

Each value carries its exact `implements` / `uses` / `opt_uses` sets in `(annotations.canonical_type)`, so a validator can still reject a variant reporting a field its declared type does not have. That is what makes the cut in rule 7 lossless.

| Enum value | Ontology type | Implements | Description |
| --- | --- | --- | --- |
| `CARBON_MONOXIDE_HORN_STROBE_TYPE_COHS_HSM` | `SAFETY/COHS_HSM` | `COHS`, `HSM` | Standard carbon monoxide horn strobe. |

## Provenance

Generated from `SAFETY/COHS` in Google's Digital Buildings ontology.

- [Ontology concepts](https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md)
- [Abstract model](https://github.com/google/digitalbuildings/blob/master/ontology/docs/model.md)
- [Building configuration](https://github.com/google/digitalbuildings/blob/master/ontology/docs/building_config.md)
