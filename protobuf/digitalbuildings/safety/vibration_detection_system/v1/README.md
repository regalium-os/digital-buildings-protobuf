<!--
Generated by `just docs` from Google's Digital Buildings ontology,
revision 2026-09-09 (4a794ac).
DO NOT EDIT -- edit sync/internal/docs and regenerate.
-->

# VibrationDetectionSystem

Generic vibration detection system.

|   |   |
| --- | --- |
| Ontology type | `SAFETY/VBS` |
| GUID | `a900c3d7-8398-4459-b6c0-754315ae4a02` |
| Kind | equipment |
| Proto package | `protobuf.digitalbuildings.safety.vibration_detection_system.v1` |
| Resource name | `buildings/{building}/vibrationDetectionSystems/{vibration_detection_system}` |
| Fields | 3 fields, 2 writable |
| Canonical types | 1 |

## Files

Rule 2: one message per file, plus its enums. `messages.proto` is the single exception, because a request and its response are meaningless apart.

| File | Contains |
| --- | --- |
| `vibration_detection_system.proto` | the `VibrationDetectionSystem` message |
| `vibration_detection_system_type.proto` | `VibrationDetectionSystemType`, one value per canonical type (rule 7) |
| `service.proto` | the `VibrationDetectionSystems` service |
| `messages.proto` | its request and response messages |
| `connection.proto` | `Connection` -- how this entity relates to another (rule 4) |
| `link.proto` | `Link` -- this entity's fields mapped onto another's |
| `field_translation.proto` | `FieldTranslation`, `UnitMapping`, `StateMapping`, `ValueRange` |
| `active_inactive_value.proto` | a multistate enum (rule 13) |

## Service

`VibrationDetectionSystems` in `service.proto`. Every resource is a collection (rule 7): there are no singletons here, so it gets the full standard method set plus AIP-164 `Undelete`.

AIP-131 names the service for the collection rather than `VibrationDetectionSystemService`; rule 1 has why buf STANDARD is not selected.

| Method | HTTP |
| --- | --- |
| `GetVibrationDetectionSystem` | `GET /v1/{name=buildings/*/vibrationDetectionSystems/*}` |
| `ListVibrationDetectionSystems` | `GET /v1/{parent=buildings/*}/vibrationDetectionSystems` |
| `CreateVibrationDetectionSystem` | `POST /v1/{parent=buildings/*}/vibrationDetectionSystems` |
| `UpdateVibrationDetectionSystem` | `PATCH /v1/{vibration_detection_system.name=buildings/*/vibrationDetectionSystems/*}` |
| `DeleteVibrationDetectionSystem` | `DELETE /v1/{name=buildings/*/vibrationDetectionSystems/*}` |
| `UndeleteVibrationDetectionSystem` | `POST /v1/{name=buildings/*/vibrationDetectionSystems/*}:undelete` |

## Fields

Every field is `optional` (rule 7): the resource carries the union of every field any of its canonical types uses, and no single device reports all of them.

**Writable** is rule 8. A `command` or a `setpoint` is `OPTIONAL` and can be set; everything else is `OUTPUT_ONLY`, because the equipment reports it and no API call sets it. An `update_mask` naming an `OUTPUT_ONLY` field is rejected rather than silently ignored.

| Field | Type | Writable | Unit / states | Ontology literal | Field no. |
| --- | --- | --- | --- | --- | --- |
| `manufacturer_label` | `string` | at create | -- | `manufacturer_label` | 16 |
| `model_label` | `string` | at create | -- | `model_label` | 17 |
| `vibration_alarm` | `repeated ActiveInactiveValue` | no | `ActiveInactiveValue` | `vibration_alarm` | 18 |

### Multistate enums

1 enum, one per distinct state set rather than one per field. Across the ontology 602 multistate fields draw on only 40 sets, so an enum per field would mean 305 different spellings of {ACTIVE, INACTIVE} that no consumer could pass to a common function (rule 13).

Each is copied into this package rather than imported: AIP-215 forbids a field referencing a type in another proto package (rule 4).

| File | Enum | States |
| --- | --- | --- |
| `active_inactive_value.proto` | `ActiveInactiveValue` | `ACTIVE`, `INACTIVE` |

## Canonical types

1 canonical type, each a value of `VibrationDetectionSystemType`. The ontology's name is kept verbatim, because that name *is* the identifier.

Each value carries its exact `implements` / `uses` / `opt_uses` sets in `(annotations.canonical_type)`, so a validator can still reject a variant reporting a field its declared type does not have. That is what makes the cut in rule 7 lossless.

| Enum value | Ontology type | Implements | Description |
| --- | --- | --- | --- |
| `VIBRATION_DETECTION_SYSTEM_TYPE_VBS_VBA_6X` | `SAFETY/VBS_VBA6X` | `VBA6X`, `VBS` | Six alarm vibration detection system. |

## Provenance

Generated from `SAFETY/VBS` in Google's Digital Buildings ontology.

- [Ontology concepts](https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md)
- [Abstract model](https://github.com/google/digitalbuildings/blob/master/ontology/docs/model.md)
- [Building configuration](https://github.com/google/digitalbuildings/blob/master/ontology/docs/building_config.md)
