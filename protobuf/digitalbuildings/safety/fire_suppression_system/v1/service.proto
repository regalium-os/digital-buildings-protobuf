// Copyright 2026 RegaliumOS™.
// SPDX-License-Identifier: Apache-2.0
//
// Generated by sync from Google's Digital Buildings ontology,
// revision 2026-09-09 (4a794ac).
// Portions Copyright 2020 Google LLC, licensed under Apache-2.0.
// DO NOT EDIT.
//
// Regenerate with: just sync
// Revision history: docs/spec.md

syntax = "proto3";

package protobuf.digitalbuildings.safety.fire_suppression_system.v1;

import "google/api/annotations.proto";
import "google/api/client.proto";
import "google/protobuf/empty.proto";
import "protobuf/digitalbuildings/safety/fire_suppression_system/v1/fire_suppression_system.proto";
import "protobuf/digitalbuildings/safety/fire_suppression_system/v1/messages.proto";

option java_multiple_files = true;
option java_outer_classname = "ServiceProto";
option java_package = "com.regalium.protobuf.digitalbuildings.safety.fire_suppression_system.v1";

// Manages fire suppression systems.
//
// AIP-131 names the service for the collection, not
// `FireSuppressionSystemService`: that is buf STANDARD's convention and the
// two contradict, which is why buf.yaml selects BASIC and lets api-linter
// own API design (rule 1).
service FireSuppressionSystems {
  option (google.api.default_host) = "digitalbuildings.googleapis.com";

  // Returns one fire suppression system.
  rpc GetFireSuppressionSystem(GetFireSuppressionSystemRequest) returns (FireSuppressionSystem) {
    option (google.api.http) = {get: "/v1/{name=buildings/*/fireSuppressionSystems/*}"};
    option (google.api.method_signature) = "name";
  }

  // Lists the fire suppression systems in a building.
  rpc ListFireSuppressionSystems(ListFireSuppressionSystemsRequest) returns (ListFireSuppressionSystemsResponse) {
    option (google.api.http) = {get: "/v1/{parent=buildings/*}/fireSuppressionSystems"};
    option (google.api.method_signature) = "parent";
  }

  // Creates a fire suppression system.
  rpc CreateFireSuppressionSystem(CreateFireSuppressionSystemRequest) returns (FireSuppressionSystem) {
    option (google.api.http) = {
      post: "/v1/{parent=buildings/*}/fireSuppressionSystems"
      body: "fire_suppression_system"
    };
    option (google.api.method_signature) = "parent,fire_suppression_system,fire_suppression_system_id";
  }

  // Updates a fire suppression system.
  //
  // Rule 8: an update_mask naming an OUTPUT_ONLY field is rejected rather
  // than silently ignored. Most fields here are OUTPUT_ONLY -- the equipment
  // reports them and no API call sets them.
  rpc UpdateFireSuppressionSystem(UpdateFireSuppressionSystemRequest) returns (FireSuppressionSystem) {
    option (google.api.http) = {
      patch: "/v1/{fire_suppression_system.name=buildings/*/fireSuppressionSystems/*}"
      body: "fire_suppression_system"
    };
    option (google.api.method_signature) = "fire_suppression_system,update_mask";
  }

  // Deletes a fire suppression system.
  rpc DeleteFireSuppressionSystem(DeleteFireSuppressionSystemRequest) returns (google.protobuf.Empty) {
    option (google.api.http) = {delete: "/v1/{name=buildings/*/fireSuppressionSystems/*}"};
    option (google.api.method_signature) = "name";
  }

  // Restores a deleted fire suppression system (AIP-164
  // <https://aip.dev/164>).
  rpc UndeleteFireSuppressionSystem(UndeleteFireSuppressionSystemRequest) returns (FireSuppressionSystem) {
    option (google.api.http) = {
      post: "/v1/{name=buildings/*/fireSuppressionSystems/*}:undelete"
      body: "*"
    };
    option (google.api.method_signature) = "name";
  }
}
