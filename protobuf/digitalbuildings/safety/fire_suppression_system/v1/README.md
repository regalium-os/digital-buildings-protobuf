<!--
Generated by `just docs` from Google's Digital Buildings ontology,
revision 2026-09-09 (4a794ac).
DO NOT EDIT -- edit sync/internal/docs and regenerate.
-->

# FireSuppressionSystem

Generic fire suppression system which uses gas to extinguish fire.

|   |   |
| --- | --- |
| Ontology type | `SAFETY/FSS` |
| GUID | `2c9ed5e4-51d2-4883-a9b5-aa09a22f7bb4` |
| Kind | equipment |
| Proto package | `protobuf.digitalbuildings.safety.fire_suppression_system.v1` |
| Resource name | `buildings/{building}/fireSuppressionSystems/{fire_suppression_system}` |
| Fields | 5 fields, 2 writable |
| Canonical types | 2 |

## Files

Rule 2: one message per file, plus its enums. `messages.proto` is the single exception, because a request and its response are meaningless apart.

| File | Contains |
| --- | --- |
| `fire_suppression_system.proto` | the `FireSuppressionSystem` message |
| `fire_suppression_system_type.proto` | `FireSuppressionSystemType`, one value per canonical type (rule 7) |
| `service.proto` | the `FireSuppressionSystems` service |
| `messages.proto` | its request and response messages |
| `connection.proto` | `Connection` -- how this entity relates to another (rule 4) |
| `link.proto` | `Link` -- this entity's fields mapped onto another's |
| `field_translation.proto` | `FieldTranslation`, `UnitMapping`, `StateMapping`, `ValueRange` |
| `active_inactive_value.proto` | a multistate enum (rule 13) |
| `closed_open_value.proto` | a multistate enum (rule 13) |

## Service

`FireSuppressionSystems` in `service.proto`. Every resource is a collection (rule 7): there are no singletons here, so it gets the full standard method set plus AIP-164 `Undelete`.

AIP-131 names the service for the collection rather than `FireSuppressionSystemService`; rule 1 has why buf STANDARD is not selected.

| Method | HTTP |
| --- | --- |
| `GetFireSuppressionSystem` | `GET /v1/{name=buildings/*/fireSuppressionSystems/*}` |
| `ListFireSuppressionSystems` | `GET /v1/{parent=buildings/*}/fireSuppressionSystems` |
| `CreateFireSuppressionSystem` | `POST /v1/{parent=buildings/*}/fireSuppressionSystems` |
| `UpdateFireSuppressionSystem` | `PATCH /v1/{fire_suppression_system.name=buildings/*/fireSuppressionSystems/*}` |
| `DeleteFireSuppressionSystem` | `DELETE /v1/{name=buildings/*/fireSuppressionSystems/*}` |
| `UndeleteFireSuppressionSystem` | `POST /v1/{name=buildings/*/fireSuppressionSystems/*}:undelete` |

## Fields

Every field is `optional` (rule 7): the resource carries the union of every field any of its canonical types uses, and no single device reports all of them.

**Writable** is rule 8. A `command` or a `setpoint` is `OPTIONAL` and can be set; everything else is `OUTPUT_ONLY`, because the equipment reports it and no API call sets it. An `update_mask` naming an `OUTPUT_ONLY` field is rejected rather than silently ignored.

| Field | Type | Writable | Unit / states | Ontology literal | Field no. |
| --- | --- | --- | --- | --- | --- |
| `fire_alarm` | `repeated ActiveInactiveValue` | no | `ActiveInactiveValue` | `fire_alarm` | 16 |
| `gas_release_valve_status` | `ClosedOpenValue` | no | `ClosedOpenValue` | `gas_release_valve_status` | 17 |
| `manufacturer_label` | `string` | at create | -- | `manufacturer_label` | 18 |
| `master_alarm` | `ActiveInactiveValue` | no | `ActiveInactiveValue` | `master_alarm` | 19 |
| `model_label` | `string` | at create | -- | `model_label` | 20 |

### Multistate enums

2 enums, one per distinct state set rather than one per field. Across the ontology 602 multistate fields draw on only 40 sets, so an enum per field would mean 305 different spellings of {ACTIVE, INACTIVE} that no consumer could pass to a common function (rule 13).

Each is copied into this package rather than imported: AIP-215 forbids a field referencing a type in another proto package (rule 4).

| File | Enum | States |
| --- | --- | --- |
| `active_inactive_value.proto` | `ActiveInactiveValue` | `ACTIVE`, `INACTIVE` |
| `closed_open_value.proto` | `ClosedOpenValue` | `CLOSED`, `OPEN` |

## Canonical types

2 canonical types, each a value of `FireSuppressionSystemType`. The ontology's name is kept verbatim, because that name *is* the identifier.

Each value carries its exact `implements` / `uses` / `opt_uses` sets in `(annotations.canonical_type)`, so a validator can still reject a variant reporting a field its declared type does not have. That is what makes the cut in rule 7 lossless.

| Enum value | Ontology type | Implements | Description |
| --- | --- | --- | --- |
| `FIRE_SUPPRESSION_SYSTEM_TYPE_FSS_FA_2X_GS` | `SAFETY/FSS_FA2X_GS` | `FA2X`, `FSS` | Fire Suppression System with with 2-level alarm and gas suppression. |
| `FIRE_SUPPRESSION_SYSTEM_TYPE_FSS_STD` | `SAFETY/FSS_STD` | `FSS` | Basic fire suppression. |

## Provenance

Generated from `SAFETY/FSS` in Google's Digital Buildings ontology.

- [Ontology concepts](https://github.com/google/digitalbuildings/blob/master/ontology/docs/ontology.md)
- [Abstract model](https://github.com/google/digitalbuildings/blob/master/ontology/docs/model.md)
- [Building configuration](https://github.com/google/digitalbuildings/blob/master/ontology/docs/building_config.md)
